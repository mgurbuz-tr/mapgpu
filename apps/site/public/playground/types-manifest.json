{"@mapgpu/core":{"engine/AnimationManager.d.ts":"/**\n * AnimationManager — requestAnimationFrame tabanlı animasyon yönetimi\n *\n * animate(from, to, duration, easing, onUpdate) ile animasyon başlat.\n * cancel(handle) ile animasyonu iptal et.\n * update(timestamp) ile RenderLoop'tan çağrılarak aktif animasyonları ilerlet.\n * Built-in easing: linear, easeInOut, easeIn, easeOut.\n * Multiple concurrent animasyonlar desteklenir.\n */\nexport type EasingFunction = (t: number) => number;\nexport declare const Easing: {\n    readonly linear: (t: number) => number;\n    readonly easeIn: (t: number) => number;\n    readonly easeOut: (t: number) => number;\n    readonly easeInOut: (t: number) => number;\n};\nexport type EasingName = keyof typeof Easing;\nexport interface AnimationHandle {\n    /** Unique animation id */\n    readonly id: number;\n    /** Cancel this animation */\n    cancel(): void;\n    /** Whether this animation has completed or been cancelled */\n    readonly done: boolean;\n}\nexport interface AnimateOptions {\n    /** Start value */\n    from: number;\n    /** End value */\n    to: number;\n    /** Duration in milliseconds */\n    duration: number;\n    /** Easing function name or custom function */\n    easing?: EasingName | EasingFunction;\n    /** Called on each update with the interpolated value and normalized progress [0,1] */\n    onUpdate: (value: number, progress: number) => void;\n    /** Called when animation completes (not called if cancelled) */\n    onComplete?: () => void;\n}\nexport declare class AnimationManager {\n    private _animations;\n    private _nextId;\n    /**\n     * Start a new animation.\n     */\n    animate(options: AnimateOptions): AnimationHandle;\n    /**\n     * Cancel an active animation.\n     */\n    cancel(id: number): void;\n    /**\n     * Update all active animations. Called from RenderLoop on each frame.\n     * @param timestamp Current time in milliseconds (e.g., performance.now())\n     */\n    update(timestamp: number): void;\n    /**\n     * Cancel all active animations.\n     */\n    cancelAll(): void;\n    /**\n     * Number of currently active animations.\n     */\n    get activeCount(): number;\n    /**\n     * Clean up all animations.\n     */\n    destroy(): void;\n}\n//# sourceMappingURL=AnimationManager.d.ts.map","engine/CameraController2D.d.ts":"/**\n * CameraController2D — 2D kamera yönetimi\n *\n * View state (center, zoom, rotation) yönetir.\n * Ortho projection ve view matrix hesaplar.\n * Extent hesaplama (viewport boyutuna göre).\n *\n * Render coordinate space: EPSG:3857 (Web Mercator)\n */\nimport type { Extent } from '../interfaces/index';\nexport interface ViewState {\n    /** Center in EPSG:3857 [x, y] */\n    center: [number, number];\n    /** Zoom level (0–22+) */\n    zoom: number;\n    /** Rotation in radians */\n    rotation: number;\n}\nexport interface CameraController2DOptions {\n    center?: [number, number];\n    zoom?: number;\n    rotation?: number;\n    minZoom?: number;\n    maxZoom?: number;\n    viewportWidth?: number;\n    viewportHeight?: number;\n}\nexport declare class CameraController2D {\n    private _center;\n    private _zoom;\n    private _rotation;\n    private _minZoom;\n    private _maxZoom;\n    private _viewportWidth;\n    private _viewportHeight;\n    private _dirty;\n    private _viewMatrix;\n    private _projectionMatrix;\n    constructor(options?: CameraController2DOptions);\n    get center(): [number, number];\n    get zoom(): number;\n    get rotation(): number;\n    get minZoom(): number;\n    get maxZoom(): number;\n    get viewportWidth(): number;\n    get viewportHeight(): number;\n    get dirty(): boolean;\n    setCenter(center: [number, number]): void;\n    setZoom(zoom: number): void;\n    setRotation(rotation: number): void;\n    setViewport(width: number, height: number): void;\n    zoomIn(): void;\n    zoomOut(): void;\n    clearDirty(): void;\n    get viewMatrix(): Float32Array;\n    get projectionMatrix(): Float32Array;\n    /**\n     * Calculates the visible extent in EPSG:3857 coordinates.\n     * Takes into account center, zoom, rotation, and viewport size.\n     */\n    getExtent(): Extent;\n    /**\n     * Convert screen coordinates to map coordinates (EPSG:3857).\n     */\n    screenToMap(screenX: number, screenY: number): [number, number];\n    /**\n     * Convert map coordinates (EPSG:3857) to screen coordinates.\n     */\n    mapToScreen(mapX: number, mapY: number): [number, number];\n    /**\n     * Meters per pixel at current zoom level.\n     * At zoom 0 the entire world (2 * WORLD_HALF meters) fits into 256 pixels.\n     */\n    private _getResolution;\n    private _clampZoom;\n    /**\n     * Build orthographic projection and view matrices.\n     * Column-major layout (WebGPU/OpenGL convention).\n     */\n    private _updateMatrices;\n}\n//# sourceMappingURL=CameraController2D.d.ts.map","engine/CameraController3D.d.ts":"/**\n * CameraController3D — 3D perspective kamera yonetimi\n *\n * Globe mode 3D kamera: position (lon, lat, altitude), heading, tilt, roll.\n * Perspective projection ve view matrix hesaplar.\n * Ray casting ile screen/world donusumu.\n *\n * Koordinat uzayi: WGS84 spherical -> ECEF cartesian\n */\ntype Vec3 = [number, number, number];\nexport interface CameraPosition3D {\n    /** Longitude in degrees */\n    longitude: number;\n    /** Latitude in degrees */\n    latitude: number;\n    /** Altitude above the ellipsoid surface in metres */\n    altitude: number;\n}\nexport interface CameraController3DOptions {\n    position?: CameraPosition3D;\n    /** Heading in degrees (0=north, clockwise) */\n    heading?: number;\n    /** Tilt in degrees (0=looking straight down, 90=horizontal) */\n    tilt?: number;\n    /** Roll in degrees */\n    roll?: number;\n    /** Field-of-view in degrees */\n    fov?: number;\n    /** Viewport width in pixels */\n    viewportWidth?: number;\n    /** Viewport height in pixels */\n    viewportHeight?: number;\n    /** Minimum altitude in metres */\n    minAltitude?: number;\n    /** Maximum altitude in metres */\n    maxAltitude?: number;\n}\nexport declare class CameraController3D {\n    private _longitude;\n    private _latitude;\n    private _altitude;\n    private _heading;\n    private _tilt;\n    private _roll;\n    private _fov;\n    private _viewportWidth;\n    private _viewportHeight;\n    private _minAltitude;\n    private _maxAltitude;\n    private _dirty;\n    private _viewMatrix;\n    private _projectionMatrix;\n    private _nearOverride;\n    private _farOverride;\n    private _reversedZ;\n    constructor(options?: CameraController3DOptions);\n    get longitude(): number;\n    get latitude(): number;\n    get altitude(): number;\n    get position(): CameraPosition3D;\n    /** Heading in degrees */\n    get heading(): number;\n    /** Tilt in degrees */\n    get tilt(): number;\n    /** Roll in degrees */\n    get roll(): number;\n    /** FOV in degrees */\n    get fov(): number;\n    get viewportWidth(): number;\n    get viewportHeight(): number;\n    get minAltitude(): number;\n    get maxAltitude(): number;\n    get dirty(): boolean;\n    /** View matrix (column-major, 16 floats) */\n    get viewMatrix(): Float32Array;\n    /** Projection matrix (column-major, 16 floats) */\n    get projectionMatrix(): Float32Array;\n    /** Compute dynamic near plane based on altitude */\n    get nearPlane(): number;\n    /** Compute dynamic far plane based on altitude */\n    get farPlane(): number;\n    setPosition(longitude: number, latitude: number, altitude: number): void;\n    /** Set heading in degrees (0=north, clockwise) */\n    setHeading(degrees: number): void;\n    /** Set tilt in degrees (0=looking straight down, 90=horizontal) */\n    setTilt(degrees: number): void;\n    /** Set roll in degrees */\n    setRoll(degrees: number): void;\n    /** Set viewport size */\n    setViewport(width: number, height: number): void;\n    clearDirty(): void;\n    /** Enable reversed-Z projection (near=1, far=0). Call before first render. */\n    setReversedZ(enabled: boolean): void;\n    /** Whether reversed-Z projection is active */\n    get reversedZ(): boolean;\n    /** Override near/far planes with tight values computed from visible tiles */\n    setNearFarOverride(near: number, far: number): void;\n    /** Clear near/far override, reverting to dynamic calculation */\n    clearNearFarOverride(): void;\n    /**\n     * Point the camera at the given target location.\n     * This adjusts heading and tilt to look at the target from the current position.\n     */\n    lookAt(longitude: number, latitude: number, altitude?: number): void;\n    /**\n     * Convert screen coordinates to world coordinates (lon, lat) using\n     * ray-sphere intersection against the Earth sphere.\n     * Returns null if the ray doesn't hit the sphere.\n     */\n    screenToWorld(screenX: number, screenY: number): [number, number] | null;\n    /**\n     * Convert world coordinates (lon, lat, alt) to screen coordinates.\n     * Returns null if the point is behind the camera.\n     */\n    worldToScreen(longitude: number, latitude: number, altitude?: number): [number, number] | null;\n    /** Camera position in ECEF coordinates */\n    getPositionECEF(): Vec3;\n    private _clampAltitude;\n    private _clampTilt;\n    /**\n     * Build perspective projection and view matrices.\n     * Column-major layout (WebGPU convention).\n     */\n    private _updateMatrices;\n    /** Convert screen coordinates to a ray in ECEF space */\n    private _screenToRay;\n}\nexport {};\n//# sourceMappingURL=CameraController3D.d.ts.map","engine/CommandSystem.d.ts":"/**\n * CommandSystem — Undo/Redo yeteneği olan command pattern\n *\n * execute(command), undo(), redo() ile komut yönetimi.\n * Command interface: { execute(): void, undo(): void, description: string }\n * History stack (configurable max size).\n * Event: command-executed, command-undone, command-redone.\n */\nexport interface ICommand {\n    /** Execute the command */\n    execute(): void;\n    /** Undo the command */\n    undo(): void;\n    /** Human-readable description */\n    description: string;\n}\nexport interface CommandSystemEvents {\n    [key: string]: unknown;\n    'command-executed': {\n        command: ICommand;\n    };\n    'command-undone': {\n        command: ICommand;\n    };\n    'command-redone': {\n        command: ICommand;\n    };\n}\nexport interface CommandSystemOptions {\n    /** Maximum undo history size. Default: 50 */\n    maxHistorySize?: number;\n}\nexport declare class CommandSystem {\n    private _undoStack;\n    private _redoStack;\n    private _maxHistorySize;\n    private _events;\n    constructor(options?: CommandSystemOptions);\n    /**\n     * Execute a command and push it onto the undo stack.\n     * Clears the redo stack (new action invalidates redo history).\n     */\n    execute(command: ICommand): void;\n    /**\n     * Undo the last executed command.\n     * Returns true if an undo was performed, false if nothing to undo.\n     */\n    undo(): boolean;\n    /**\n     * Redo the last undone command.\n     * Returns true if a redo was performed, false if nothing to redo.\n     */\n    redo(): boolean;\n    /**\n     * Can we undo?\n     */\n    get canUndo(): boolean;\n    /**\n     * Can we redo?\n     */\n    get canRedo(): boolean;\n    /**\n     * Number of commands in the undo stack.\n     */\n    get undoCount(): number;\n    /**\n     * Number of commands in the redo stack.\n     */\n    get redoCount(): number;\n    /**\n     * Clear all history.\n     */\n    clear(): void;\n    on<K extends keyof CommandSystemEvents>(event: K, handler: (data: CommandSystemEvents[K]) => void): void;\n    off<K extends keyof CommandSystemEvents>(event: K, handler: (data: CommandSystemEvents[K]) => void): void;\n    destroy(): void;\n}\n//# sourceMappingURL=CommandSystem.d.ts.map","engine/Diagnostics.d.ts":"/**\n * Diagnostics — Frame timing statistics\n *\n * record(frameDuration) ile her frame sonunda süreyi kaydet.\n * getStats() ile fps, avgFrameTime, p95FrameTime, totalFrames, droppedFrames al.\n * Rolling window: son 120 frame.\n * reset() ile istatistikleri sıfırla.\n */\nexport interface DiagnosticStats {\n    /** Frames per second (based on average frame time in the rolling window) */\n    fps: number;\n    /** Average frame time in milliseconds */\n    avgFrameTime: number;\n    /** 95th percentile frame time in milliseconds */\n    p95FrameTime: number;\n    /** Total number of frames recorded (lifetime, not just window) */\n    totalFrames: number;\n    /** Frames exceeding the dropped frame threshold */\n    droppedFrames: number;\n}\nexport interface DiagnosticsOptions {\n    /** Rolling window size. Default: 120 */\n    windowSize?: number;\n    /** Frame time threshold (ms) above which a frame is considered dropped. Default: 33.33 (~30fps) */\n    droppedFrameThreshold?: number;\n}\nexport declare class Diagnostics {\n    private _windowSize;\n    private _droppedFrameThreshold;\n    /** Circular buffer for rolling window */\n    private _frameTimes;\n    /** Current write position in the circular buffer */\n    private _writeIndex;\n    /** Number of samples currently in the buffer (up to windowSize) */\n    private _sampleCount;\n    private _totalFrames;\n    private _droppedFrames;\n    constructor(options?: DiagnosticsOptions);\n    /**\n     * Record a frame duration. Called at the end of each frame.\n     * @param frameDurationMs Duration of the frame in milliseconds\n     */\n    record(frameDurationMs: number): void;\n    /**\n     * Get current diagnostic statistics.\n     */\n    getStats(): DiagnosticStats;\n    /**\n     * Reset all statistics and the rolling window.\n     */\n    reset(): void;\n    /**\n     * Extract the active samples from the circular buffer.\n     */\n    private _getActiveSamples;\n}\n//# sourceMappingURL=Diagnostics.d.ts.map","engine/ExtrusionConverter.d.ts":"/**\n * ExtrusionConverter — Polygon features → extruded 3D geometry\n *\n * Converts polygon features with height attributes into 3D vertex data\n * suitable for GPU rendering. Produces roof, wall, and optional floor\n * geometry with per-face normals for directional lighting.\n *\n * Vertex layout: [px, py, pz, nx, ny, nz, cx, cy] — 8 floats (32 bytes) per vertex.\n * XY are in Mercator [0..1] (normalized from EPSG:3857), Z is in metres (height).\n * CX, CY are the building centroid in Mercator [0..1] (for per-building animation delay).\n *\n * Normalization avoids Float32 precision loss: EPSG:3857 values (~±20M)\n * would lose ~0.5m accuracy in Float32, while [0..1] gives ~2.4mm precision.\n */\nimport type { Feature, Geometry } from '../interfaces/index';\ntype GeometryLike = Geometry & {\n    spatialReference?: string;\n};\ntype FeatureLike = Pick<Feature, 'attributes'> & {\n    geometry: GeometryLike;\n};\nexport interface ExtrusionVertexData {\n    /** Float32Array: [px,py,pz, nx,ny,nz, cx,cy, ...] — 8 floats per vertex */\n    vertices: Float32Array;\n    /** Uint32Array triangle-list indices */\n    indices: Uint32Array;\n    /** Number of indices */\n    indexCount: number;\n}\n/**\n * Extrude polygon features into 3D geometry.\n *\n * For each feature with polygon geometry:\n * 1. Read height/minHeight from attributes\n * 2. Convert ring coordinates to EPSG:3857\n * 3. Generate roof (earcut triangulation at Z=height)\n * 4. Generate walls (quad per edge, bottom=minHeight, top=height)\n * 5. Generate floor if minHeight > 0 and below roof height\n *\n * @param features - Features with Polygon/MultiPolygon geometry\n * @param heightField - Attribute field name for building height (metres)\n * @param minHeightField - Attribute field name for minimum height (metres)\n * @returns Extruded vertex data, or null if no valid geometry\n */\nexport declare function extrudePolygonFeatures(features: readonly FeatureLike[], heightField: string, minHeightField: string): ExtrusionVertexData | null;\nexport {};\n//# sourceMappingURL=ExtrusionConverter.d.ts.map","engine/FootprintMeshGenerator.d.ts":"/**\n * FootprintMeshGenerator — Generate 3D meshes directly from footprint polygon rings.\n *\n * The key insight: footprint preview polygon and 3D mesh use the SAME ring coordinates.\n * No unit mesh + transform — the mesh IS the footprint extruded into 3D.\n *\n * Outputs vertices in Mercator [0..1] XY + height(m) Z — same as extrusion pipeline.\n */\nimport type { Mesh3DVertexData } from './Mesh3DConverter';\n/**\n * Generate a cone mesh from a footprint polygon ring.\n *\n * - Base ring at Z=0 (the footprint)\n * - Apex at center + Z=height\n * - Side triangles connecting ring edges to apex\n * - Base disk cap\n *\n * @param ring - Closed footprint ring [[lon,lat], ...] (last = first)\n * @param centerLonLat - Center point [lon, lat]\n * @param heightM - Cone height in metres\n */\nexport declare function createConeFromFootprint(ring: number[][], centerLonLat: [number, number], heightM: number): Mesh3DVertexData;\n/**\n * Generate a hemisphere dome mesh from a footprint polygon ring.\n *\n * - Equator ring at Z=0 (the footprint)\n * - Dome layers rising from equator to pole\n * - Each layer = footprint ring scaled inward toward center\n * - Smooth normals via per-vertex averaging\n * - Base disk cap\n *\n * @param ring - Closed footprint ring [[lon,lat], ...] (last = first)\n * @param centerLonLat - Center point [lon, lat]\n * @param heightM - Dome height (= radius for true hemisphere)\n * @param layers - Number of dome layers (default 12)\n */\nexport declare function createHemisphereFromFootprint(ring: number[][], centerLonLat: [number, number], heightM: number, layers?: number): Mesh3DVertexData;\n//# sourceMappingURL=FootprintMeshGenerator.d.ts.map","engine/FrustumCuller.d.ts":"/**\n * FrustumCuller — View frustum gorunurluk testi\n *\n * 6 frustum duzlemini view-projection matrisinden cikarir.\n * AABB (Axis-Aligned Bounding Box) ve sphere intersection testleri.\n * 3D tile/terrain culling icin kullanilir.\n */\n/**\n * A plane in Hessian normal form: ax + by + cz + d = 0\n * where (a, b, c) is the inward-pointing normal.\n */\nexport interface FrustumPlane {\n    a: number;\n    b: number;\n    c: number;\n    d: number;\n}\n/** The six frustum planes */\nexport interface FrustumPlanes {\n    left: FrustumPlane;\n    right: FrustumPlane;\n    bottom: FrustumPlane;\n    top: FrustumPlane;\n    near: FrustumPlane;\n    far: FrustumPlane;\n}\n/**\n * Axis-Aligned Bounding Box (AABB)\n */\nexport interface AABB {\n    minX: number;\n    minY: number;\n    minZ: number;\n    maxX: number;\n    maxY: number;\n    maxZ: number;\n}\n/** Intersection result */\nexport type IntersectResult = 'inside' | 'outside' | 'intersecting';\nexport declare class FrustumCuller {\n    private _planes;\n    /**\n     * Extract the 6 frustum planes from a combined view-projection matrix.\n     * Matrix must be column-major (WebGPU/OpenGL convention).\n     *\n     * The planes point inward (a point is visible if it is on the positive side\n     * of all six planes).\n     */\n    extractPlanes(viewProjectionMatrix: Float32Array): FrustumPlanes;\n    /**\n     * Test whether an AABB is visible within the frustum.\n     * Returns 'inside', 'outside', or 'intersecting'.\n     */\n    isBoxVisible(bbox: AABB): IntersectResult;\n    /**\n     * Test whether a bounding sphere is visible within the frustum.\n     * Returns 'inside', 'outside', or 'intersecting'.\n     */\n    isSphereVisible(center: [number, number, number], radius: number): IntersectResult;\n    /**\n     * Get the currently cached planes (call extractPlanes first).\n     */\n    get planes(): readonly FrustumPlane[];\n}\n/**\n * Extract frustum planes from a view-projection matrix.\n * Can be used without constructing a FrustumCuller instance.\n *\n * Uses WebGPU clip space conventions:\n *   -w ≤ x ≤ w   (left/right)\n *   -w ≤ y ≤ w   (bottom/top)\n *    0 ≤ z ≤ w   (near/far — NOT [-w,w] like OpenGL)\n *\n * Near plane: z ≥ 0  →  row2  (NOT row3+row2 which is OpenGL's z ≥ -w)\n * Far plane:  z ≤ w  →  row3 - row2\n */\nexport declare function extractFrustumPlanes(viewProjectionMatrix: Float32Array): FrustumPlane[];\n/**\n * Test an AABB against a set of frustum planes.\n */\nexport declare function testAABBFrustum(bbox: AABB, planes: readonly FrustumPlane[]): IntersectResult;\n/**\n * Test a bounding sphere against a set of frustum planes.\n */\nexport declare function testSphereFrustum(center: [number, number, number], radius: number, planes: readonly FrustumPlane[]): IntersectResult;\n//# sourceMappingURL=FrustumCuller.d.ts.map","engine/GeometryConverter.d.ts":"/**\n * GeometryConverter — GeoJSON Feature → GPU vertex/index arrays\n *\n * Stateless utility: converts Feature[] into typed arrays ready for GPU upload.\n * Coordinate conversion: EPSG:4326 (lon/lat) → EPSG:3857 (Web Mercator).\n *\n * Output formats match the render pipeline vertex layouts:\n * - Points:   Float32Array [x, y, 0, x, y, 0, ...]  (12 bytes/vertex, instance)\n * - Lines:    Float32Array [prevX,prevY,prevZ, currX,currY,currZ, nextX,nextY,nextZ, side] (40 bytes/vertex)\n *             Uint32Array  indices\n * - Polygons: Float32Array [x, y, 0, ...]  (12 bytes/vertex)\n *             Uint32Array  indices (triangulated)\n */\nimport type { Feature, Geometry } from '../interfaces/index';\ntype GeometryLike = Geometry & {\n    spatialReference?: string;\n};\ntype FeatureLike = Pick<Feature, 'attributes'> & {\n    geometry: GeometryLike;\n};\nexport interface PointVertexData {\n    /** Float32Array: [x,y,z, x,y,z, ...] in EPSG:3857 */\n    vertices: Float32Array;\n    /** Number of points */\n    count: number;\n}\nexport interface LineVertexData {\n    /** Float32Array: [prevX,prevY,prevZ, currX,currY,currZ, nextX,nextY,nextZ, side, ...] */\n    vertices: Float32Array;\n    /** Uint32Array triangle-list indices */\n    indices: Uint32Array;\n    /** Number of indices */\n    indexCount: number;\n}\nexport interface PolygonVertexData {\n    /** Float32Array: [x,y,z, x,y,z, ...] in EPSG:3857 */\n    vertices: Float32Array;\n    /** Uint32Array triangle-list indices */\n    indices: Uint32Array;\n    /** Number of indices */\n    indexCount: number;\n}\nexport interface ModelInstanceData {\n    /** Float32Array: [mercX, mercY, mercZ, scale, heading, pitch, roll, anchorZ] per instance (32 bytes) */\n    instances: Float32Array;\n    /** Number of instances (Point features) */\n    count: number;\n}\nexport declare class GeometryConverter {\n    /**\n     * Extract all Point/MultiPoint geometries from features → vertex data.\n     */\n    static pointsFromFeatures(features: readonly FeatureLike[]): PointVertexData | null;\n    /**\n     * Extract all LineString/MultiLineString geometries → vertex + index data.\n     */\n    static linesFromFeatures(features: readonly FeatureLike[]): LineVertexData | null;\n    /**\n     * Extract all Polygon/MultiPolygon geometries → triangulated vertex + index data.\n     */\n    static polygonsFromFeatures(features: readonly FeatureLike[]): PolygonVertexData | null;\n    /**\n     * Extract Point/MultiPoint features into model instance data.\n     * Per-feature attributes (heading, pitch, roll, scale) override symbol defaults.\n     * Instance layout: [mercX, mercY, mercZ, scale, heading, pitch, roll, anchorZ] — 8 floats (32 bytes)\n     */\n    static modelInstancesFromFeatures(features: readonly FeatureLike[], defaultScale: number, symbolHeading: number | undefined, symbolPitch: number | undefined, symbolRoll: number | undefined, defaultAnchorZ: number): ModelInstanceData | null;\n    private static _extractPoints;\n    private static _extractLines;\n    private static _extractPolygons;\n    private static _triangulatePolygon;\n    /**\n     * Build line vertex + index buffers from polylines.\n     * Each polyline point generates 2 vertices (side=+1 and side=-1).\n     * Each segment generates 2 triangles (6 indices).\n     * Vertex layout: prev(3) + curr(3) + next(3) + side(1) + cumulDist(1) = 11 floats (44 bytes)\n     */\n    private static _buildLineBuffers;\n}\nexport {};\n//# sourceMappingURL=GeometryConverter.d.ts.map","engine/GeometryConverterECEF.d.ts":"/**\n * GeometryConverterECEF — GeoJSON Feature → GPU vertex/index arrays (ECEF)\n *\n * 3D globe rendering icin lonLat → ECEF donusumu yapan converter.\n * GeometryConverter pattern'ini birebir takip eder ama Mercator yerine ECEF uretir.\n *\n * Output formats match the render pipeline vertex layouts:\n * - Points:   Float32Array [x, y, z, x, y, z, ...]  (12 bytes/vertex, instance)\n * - Lines:    Float32Array [prevX,prevY,prevZ, currX,currY,currZ, nextX,nextY,nextZ, side] (40 bytes/vertex)\n *             Uint32Array  indices\n * - Polygons: Float32Array [x, y, z, ...]  (12 bytes/vertex)\n *             Uint32Array  indices (triangulated)\n */\nimport type { Feature } from '../interfaces/index';\nimport type { PointVertexData, LineVertexData, PolygonVertexData } from './GeometryConverter';\n/** Convert geodetic (lon, lat, alt) to ECEF cartesian (metres) */\nexport declare function lonLatToECEF(lon: number, lat: number, alt: number): [number, number, number];\nexport declare class GeometryConverterECEF {\n    /**\n     * Extract all Point/MultiPoint geometries from features → ECEF vertex data.\n     */\n    static pointsFromFeatures(features: Feature[]): PointVertexData | null;\n    /**\n     * Extract all LineString/MultiLineString geometries → ECEF vertex + index data.\n     * Segments > 0.5 degrees are subdivided for great-circle approximation.\n     */\n    static linesFromFeatures(features: Feature[]): LineVertexData | null;\n    /**\n     * Extract all Polygon/MultiPolygon geometries → triangulated ECEF vertex + index data.\n     */\n    static polygonsFromFeatures(features: Feature[]): PolygonVertexData | null;\n    private static _extractPoints;\n    private static _extractLines;\n    /**\n     * Subdivide long segments and convert to ECEF coordinates.\n     * Ensures great-circle arcs don't deviate visually from the globe surface.\n     */\n    private static _subdivideAndConvertLine;\n    private static _extractPolygons;\n    private static _triangulatePolygon;\n    /**\n     * Build line vertex + index buffers from ECEF polylines.\n     * Each polyline point generates 2 vertices (side=+1 and side=-1).\n     * Each segment generates 2 triangles (6 indices).\n     */\n    private static _buildLineBuffers;\n}\n//# sourceMappingURL=GeometryConverterECEF.d.ts.map","engine/GlobeInteraction.d.ts":"/**\n * GlobeInteraction — Globe mouse/touch interaction\n *\n * MapLibre'nin VerticalPerspectiveCameraHelper pattern'i:\n * - Pan: ray-sphere intersection ile globe yüzeyinde sürükleme\n * - Zoom: wheel/pinch (target-fixed — fare noktası sabit kalır)\n * - Pitch/Bearing: right-click drag\n * - Latitude clamp: ±85.051129°\n *\n * VerticalPerspectiveTransform'u doğrudan kontrol eder.\n */\nimport type { VerticalPerspectiveTransform } from './projections/VerticalPerspectiveTransform';\nexport interface GlobeInteractionOptions {\n    pan?: boolean;\n    zoom?: boolean;\n    keyboard?: boolean;\n    doubleClickZoom?: boolean;\n    pitchBearing?: boolean;\n    zoomSpeed?: number;\n    /** Returns current globeness (0=flat, 1=globe). Used to dampen pointer-fixed zoom compensation. */\n    getGlobeness?: () => number;\n}\nexport declare class GlobeInteraction {\n    private readonly _element;\n    private readonly _transform;\n    private readonly _onDirty;\n    private readonly _onViewChange;\n    private readonly _panEnabled;\n    private readonly _zoomEnabled;\n    private readonly _keyboardEnabled;\n    private readonly _doubleClickZoom;\n    private readonly _pitchBearingEnabled;\n    private readonly _zoomSpeed;\n    private readonly _getGlobeness;\n    private _dragging;\n    private _lastPointerX;\n    private _lastPointerY;\n    private _activePointerId;\n    private _dragButton;\n    private _pointers;\n    private _lastPinchDist;\n    private _lastClickTime;\n    private readonly _onPointerDown;\n    private readonly _onPointerMove;\n    private readonly _onPointerUp;\n    private readonly _onWheel;\n    private readonly _onKeyDown;\n    private readonly _onContextMenu;\n    private _destroyed;\n    constructor(element: HTMLElement, transform: VerticalPerspectiveTransform, onDirty: () => void, onViewChange: () => void, options?: GlobeInteractionOptions);\n    private _handlePointerDown;\n    private _handlePointerMove;\n    private _handlePointerUp;\n    private _handlePan;\n    private _handleWheel;\n    private _handleDoubleClick;\n    private _handleKeyDown;\n    private _getPinchDistance;\n    destroy(): void;\n}\n//# sourceMappingURL=GlobeInteraction.d.ts.map","engine/GlobeView.d.ts":"/**\n * GlobeView — Globe public API\n *\n * MapView2D ile aynı lifecycle pattern:\n * - constructor: canvas oluştur, GPU init et, RenderLoop başlat\n * - goTo(): animasyonlu navigasyon\n * - toMap/toScreen: koordinat dönüşümü (screenToLonLat / lonLatToScreen)\n * - on/off/when: typed event system\n * - destroy: cleanup\n *\n * Farklar:\n * - VerticalPerspectiveTransform kamera (pitch, bearing, fov)\n * - GlobeTileCovering tile seçimi (frustum + horizon culling)\n * - drawGlobeTile() ile unit sphere rendering\n * - GlobeProjection (globeness smooth transition)\n */\nimport type { ILayer, IRenderEngine } from '../interfaces/index';\nimport type { MapError } from '../errors';\nimport { GameMap } from './Map';\nimport { RenderLoop } from './RenderLoop';\nimport { VerticalPerspectiveTransform } from './projections/VerticalPerspectiveTransform';\nimport { GlobeProjection } from './projections/GlobeProjection';\nimport type { GlobeInteractionOptions } from './GlobeInteraction';\nexport interface GlobeViewEvents {\n    [key: string]: unknown;\n    'view-change': {\n        center: [number, number];\n        zoom: number;\n        pitch: number;\n        bearing: number;\n    };\n    'ready': void;\n    'error': MapError;\n    'layer-add': {\n        layer: ILayer;\n    };\n    'layer-remove': {\n        layer: ILayer;\n    };\n    'frame': {\n        frameNumber: number;\n        fps: number;\n    };\n    'destroy': void;\n}\nexport interface GlobeGoToTarget {\n    center?: [number, number];\n    zoom?: number;\n    pitch?: number;\n    bearing?: number;\n    duration?: number;\n}\nexport interface GlobeViewOptions {\n    container: HTMLElement | string | null;\n    center?: [number, number];\n    zoom?: number;\n    pitch?: number;\n    bearing?: number;\n    renderEngine?: IRenderEngine;\n    interaction?: GlobeInteractionOptions | false;\n    /** Vertical exaggeration for terrain layers (default: 50) */\n    exaggeration?: number;\n    /** Sun direction for terrain lighting [x, y, z] (normalized) */\n    sunDirection?: [number, number, number];\n}\nexport declare class GlobeView {\n    readonly map: GameMap;\n    private _events;\n    private _transform;\n    private _projection;\n    private _tileCovering;\n    private _layerManager;\n    private _renderLoop;\n    private _tileManager;\n    private _renderEngine;\n    private _container;\n    private _canvas;\n    private _interaction;\n    private _resizeObserver;\n    private _ready;\n    private _gpuReady;\n    private _destroyed;\n    private _readyResolve;\n    private _readyPromise;\n    private _bufferCache;\n    private _exaggeration;\n    private _sunDirection;\n    private _loggedTerrainTiles;\n    private _animating;\n    private _animationId;\n    constructor(options: GlobeViewOptions);\n    get center(): [number, number];\n    get zoom(): number;\n    get pitch(): number;\n    get bearing(): number;\n    get ready(): boolean;\n    get gpuReady(): boolean;\n    get canvas(): HTMLCanvasElement | null;\n    get transform(): VerticalPerspectiveTransform;\n    get projection(): GlobeProjection;\n    get renderLoop(): RenderLoop;\n    get exaggeration(): number;\n    set exaggeration(value: number);\n    get sunDirection(): [number, number, number];\n    set sunDirection(value: [number, number, number]);\n    goTo(target: GlobeGoToTarget): Promise<void>;\n    toMap(screenX: number, screenY: number): [number, number] | null;\n    toScreen(lon: number, lat: number): [number, number] | null;\n    on<K extends keyof GlobeViewEvents>(event: K, handler: (data: GlobeViewEvents[K]) => void): void;\n    off<K extends keyof GlobeViewEvents>(event: K, handler: (data: GlobeViewEvents[K]) => void): void;\n    when(): Promise<void>;\n    destroy(): void;\n    private _createCanvas;\n    private _setupResizeObserver;\n    private _initGpu;\n    /**\n     * Render one frame: globe tiles via GlobeTileCovering → drawGlobeTile(),\n     * then vector layers via drawGlobePoints/Lines/Polygons().\n     */\n    private _renderFrame;\n    /**\n     * Render a single vector layer on the globe.\n     */\n    private _renderVectorLayer;\n    /**\n     * Render a DTED terrain layer on the globe with radial displacement.\n     */\n    private _renderGlobeTerrainLayer;\n    /**\n     * Flat modda (zoom >= 6) viewport-tabanlı tile seçimi.\n     * Globe frustum + horizon culling yerine basit viewport coverage.\n     */\n    private _getTilesForFlat;\n    private _cancelAnimation;\n    private _emitViewChange;\n}\n//# sourceMappingURL=GlobeView.d.ts.map","engine/IViewMode.d.ts":"/**\n * IViewMode — Strategy interface for 2D/3D rendering modes.\n *\n * MapView delegates all mode-specific behavior (camera, interaction,\n * rendering, coordinate conversion) to the active IViewMode implementation.\n *\n * Implementations:\n * - Mode2D: CameraController2D + InteractionHandler + flat Mercator rendering\n * - Mode3D: GlobeProjection + GlobeInteraction + sphere/terrain rendering\n *\n * @see {@link Mode2D} for the 2D (flat Mercator) implementation\n * @see {@link Mode3D} for the 3D (globe/terrain) implementation\n * @see {@link ViewCore} for the shared infrastructure that persists across mode switches\n */\nimport type { CameraState, IRenderEngine, ResolvedGlobeEffects } from '../interfaces/index';\nimport type { LayerManager } from './LayerManager';\nimport type { TileManager } from './TileManager';\nimport type { TerrainTileManager } from './TerrainTileManager';\nimport type { TileScheduler } from './TileScheduler';\nimport type { VectorBufferCache } from './VectorBufferCache';\n/**\n * Serializable view state shared between modes during switchTo().\n * Each mode fills in what it supports and ignores the rest.\n *\n * When switching from 2D to 3D (or vice versa), the outgoing mode serializes\n * its state into a {@link ViewState}, and the incoming mode applies the fields\n * it understands. This ensures a smooth visual transition between modes.\n *\n * @see {@link IViewMode.getState} to read the current state from a mode\n * @see {@link IViewMode.setState} to apply a partial state to a mode\n */\nexport interface ViewState {\n    /**\n     * Center coordinate in EPSG:4326.\n     *\n     * @remarks Follows GeoJSON convention: [longitude, latitude].\n     */\n    center: [number, number];\n    /** Zoom level (0 = world view, higher = more detail). */\n    zoom: number;\n    /**\n     * Camera pitch angle in degrees.\n     *\n     * @remarks Used by 3D mode only. In 2D mode this is always 0.\n     * Range: 0 (looking straight down) to ~85 (near-horizon).\n     */\n    pitch: number;\n    /**\n     * Camera bearing (heading) in degrees, measured clockwise from north.\n     *\n     * @remarks Used by 3D mode only. In 2D mode this is always 0.\n     * Range: 0-360.\n     */\n    bearing: number;\n    /**\n     * Map rotation in degrees, measured clockwise.\n     *\n     * @remarks Used by 2D mode only. In 3D mode this is always 0.\n     */\n    rotation: number;\n}\n/**\n * Shared resources passed to {@link IViewMode.renderFrame}.\n *\n * These are owned by {@link ViewCore} and shared across modes. The render\n * context is assembled fresh each frame by the MapView orchestrator so that\n * modes never hold stale references.\n *\n * @see {@link ViewCore} for the owner of these resources\n * @see {@link IViewMode.renderFrame} for the consumer\n */\nexport interface RenderFrameContext {\n    /**\n     * The GPU render engine used to issue draw calls.\n     *\n     * @see {@link IRenderEngine}\n     */\n    readonly renderEngine: IRenderEngine;\n    /**\n     * Manages layer visibility, ordering, and zoom-range filtering.\n     *\n     * @see {@link LayerManager}\n     */\n    readonly layerManager: LayerManager;\n    /**\n     * Handles tile fetching, caching, and texture upload.\n     *\n     * @see {@link TileManager}\n     */\n    readonly tileManager: TileManager;\n    /**\n     * Handles terrain height/hillshade tile requests, GPU texture cache,\n     * and parent fallback for terrain-enabled rendering.\n     *\n     * @see {@link TerrainTileManager}\n     */\n    readonly terrainManager: TerrainTileManager;\n    /**\n     * Computes which tiles are needed for the current viewport extent and zoom.\n     *\n     * @see {@link TileScheduler}\n     */\n    readonly tileScheduler: TileScheduler;\n    /**\n     * Caches GPU buffers for vector layer geometry and terrain textures.\n     *\n     * @see {@link VectorBufferCache}\n     */\n    readonly bufferCache: VectorBufferCache;\n    /**\n     * Resolved globe effects configuration (lighting, atmosphere, pole caps, etc.).\n     *\n     * @see {@link ResolvedGlobeEffects}\n     */\n    readonly globeEffects: ResolvedGlobeEffects;\n}\n/**\n * Unified navigation target for animated camera transitions.\n *\n * Passed to {@link IViewMode.goTo}. Each mode uses only the fields it\n * supports and silently ignores the rest. All fields are optional so\n * callers can animate a single property (e.g., just zoom) without\n * affecting the others.\n *\n * @example\n * ```ts\n * // Fly to Istanbul at zoom 12 over 1 second\n * mapView.goTo({ center: [28.97, 41.01], zoom: 12, duration: 1000 });\n * ```\n *\n * @see {@link IViewMode.goTo}\n */\nexport interface GoToTarget {\n    /**\n     * Target center coordinate [longitude, latitude] in EPSG:4326.\n     */\n    center?: [number, number];\n    /** Target zoom level. */\n    zoom?: number;\n    /**\n     * Target pitch angle in degrees.\n     *\n     * @remarks Only effective in 3D mode.\n     */\n    pitch?: number;\n    /**\n     * Target bearing (heading) in degrees.\n     *\n     * @remarks Only effective in 3D mode.\n     */\n    bearing?: number;\n    /**\n     * Target rotation in degrees.\n     *\n     * @remarks Only effective in 2D mode.\n     */\n    rotation?: number;\n    /**\n     * Animation duration in milliseconds.\n     *\n     * @defaultValue 500\n     */\n    duration?: number;\n}\n/**\n * Strategy interface for 2D and 3D rendering modes.\n *\n * MapView owns a single `IViewMode` reference at any given time. When the\n * user calls `mapView.switchTo('3d')`, the current mode is disposed, state\n * is serialized via {@link getState}, a new mode is constructed, and state\n * is restored via {@link setState}.\n *\n * Implementors must handle:\n * - **State**: camera position, viewport sizing\n * - **Navigation**: animated fly-to transitions\n * - **Rendering**: issuing draw calls for tiles, vectors, and overlays\n * - **Interaction**: pointer/keyboard event handling\n * - **Coordinate conversion**: screen-to-geographic and back\n * - **Lifecycle**: resource cleanup on dispose\n *\n * @see {@link Mode2D} for the flat Mercator implementation\n * @see {@link Mode3D} for the globe/terrain implementation\n * @see {@link ViewCore} for shared infrastructure\n * @see {@link ViewState} for the serializable state exchanged during mode switches\n */\nexport interface IViewMode {\n    /**\n     * Mode identifier discriminator.\n     *\n     * Used by MapView to determine which mode is active and to dispatch\n     * mode-specific logic (e.g., interaction option types).\n     */\n    readonly type: '2d' | '3d';\n    /**\n     * Apply a partial view state to this mode.\n     *\n     * Called during mode switches (to restore state from the previous mode)\n     * and during {@link goTo} animations (to set intermediate values).\n     * The mode should apply the fields it understands and ignore unknown ones.\n     *\n     * @param state - Partial view state to merge into the current state.\n     *\n     * @see {@link getState} for the inverse operation\n     * @see {@link ViewState} for the full state shape\n     */\n    setState(state: Partial<ViewState>): void;\n    /**\n     * Read the current view state as a serializable object.\n     *\n     * The returned state captures center, zoom, and mode-specific properties\n     * (pitch/bearing for 3D, rotation for 2D). Used during mode switching\n     * to transfer state to the incoming mode.\n     *\n     * @returns The current view state snapshot.\n     *\n     * @see {@link setState} for the inverse operation\n     */\n    getState(): ViewState;\n    /**\n     * Produce a {@link CameraState} for the render engine's `beginFrame()` call.\n     *\n     * This translates the mode's internal camera representation into the\n     * engine-agnostic {@link CameraState} format that the render engine uses\n     * to set up view/projection matrices each frame.\n     *\n     * @returns Camera state including view matrix, projection matrix, and viewport dimensions.\n     *\n     * @see {@link CameraState}\n     */\n    getCameraState(): CameraState;\n    /**\n     * Notify the mode that the viewport has been resized.\n     *\n     * Called by the {@link ViewCore.setupResizeObserver | ResizeObserver} callback\n     * whenever the container dimensions change. The mode should update its\n     * camera aspect ratio and any viewport-dependent state.\n     *\n     * @param width  - New viewport width in CSS pixels.\n     * @param height - New viewport height in CSS pixels.\n     */\n    setViewport(width: number, height: number): void;\n    /**\n     * Animate the camera to a target state.\n     *\n     * The mode owns the animation loop: it interpolates from the current state\n     * to the target over the specified duration, calling `markDirty()` each\n     * frame to request a repaint and `onViewChange()` to emit view-change events.\n     *\n     * The returned promise resolves when the animation completes or is cancelled.\n     *\n     * @param target       - The navigation target (center, zoom, pitch, etc.).\n     * @param markDirty    - Callback to request a render frame from the RenderLoop.\n     * @param onViewChange - Callback to notify MapView that the view state changed\n     *                        (triggers 'view-change' event emission).\n     * @returns A promise that resolves when the animation finishes or is cancelled.\n     *\n     * @see {@link GoToTarget} for the target shape\n     * @see {@link cancelAnimation} to abort an in-progress animation\n     */\n    goTo(target: GoToTarget, markDirty: () => void, onViewChange: () => void): Promise<void>;\n    /**\n     * Cancel any in-progress {@link goTo} animation.\n     *\n     * The pending goTo promise should resolve (not reject) after cancellation.\n     * This is called when a new goTo is issued before the previous one completes,\n     * or when the user initiates manual interaction during an animation.\n     */\n    cancelAnimation(): void;\n    /**\n     * Render one frame using the shared resources in the provided context.\n     *\n     * Called by MapView inside the RenderLoop between `beginFrame()` and\n     * `endFrame()`. The mode is responsible for:\n     *\n     * 1. Computing tile coverage for the current viewport/zoom\n     * 2. Drawing raster imagery tiles via {@link TileManager}\n     * 3. Drawing vector layer features via {@link VectorBufferCache}\n     * 4. Drawing terrain layers (if applicable)\n     * 5. Any mode-specific extras (atmosphere, pole caps, grid lines, etc.)\n     *\n     * @param ctx - Shared render resources (engine, tile manager, layer manager, etc.).\n     *\n     * @see {@link RenderFrameContext} for the available resources\n     */\n    renderFrame(ctx: RenderFrameContext): void;\n    /**\n     * Attach pointer and keyboard interaction handlers to a DOM container.\n     *\n     * Called once after GPU initialization succeeds. The mode should register\n     * event listeners for pan, zoom, rotate, and other gestures appropriate\n     * for its projection type.\n     *\n     * @param container    - The DOM element to attach listeners to (usually the map container).\n     * @param markDirty    - Callback to request a render frame when interaction changes the view.\n     * @param onViewChange - Callback to notify MapView of view state changes.\n     * @param options      - Mode-specific interaction options, or `false` to disable interaction.\n     *                        For 2D mode: {@link InteractionHandlerOptions}.\n     *                        For 3D mode: {@link GlobeInteractionOptions}.\n     *\n     * @see {@link dispose} to clean up the registered listeners\n     */\n    attachInteraction(container: HTMLElement, markDirty: () => void, onViewChange: () => void, options?: Record<string, unknown> | false): void;\n    /**\n     * Convert a screen pixel coordinate to a geographic coordinate.\n     *\n     * @param screenX - Horizontal pixel offset from the container's left edge.\n     * @param screenY - Vertical pixel offset from the container's top edge.\n     * @returns A [longitude, latitude] tuple in EPSG:4326, or `null` if the\n     *          screen point does not intersect the map surface (e.g., clicking\n     *          the sky in 3D mode).\n     *\n     * @see {@link toScreen} for the inverse operation\n     */\n    toMap(screenX: number, screenY: number): [number, number] | null;\n    /**\n     * Convert a geographic coordinate to a screen pixel coordinate.\n     *\n     * @param lon - Longitude in degrees (EPSG:4326).\n     * @param lat - Latitude in degrees (EPSG:4326).\n     * @returns A [screenX, screenY] pixel tuple, or `null` if the geographic\n     *          point is not visible in the current viewport (e.g., behind the\n     *          globe in 3D mode).\n     *\n     * @see {@link toMap} for the inverse operation\n     */\n    toScreen(lon: number, lat: number): [number, number] | null;\n    /**\n     * Dispose all mode-specific resources.\n     *\n     * Called when switching modes or destroying the MapView. Implementations\n     * should clean up:\n     * - Interaction event listeners\n     * - Camera controllers\n     * - Mode-specific GPU resources (if any)\n     * - Animation timers\n     *\n     * After dispose, the mode instance must not be reused.\n     */\n    dispose(): void;\n}\n//# sourceMappingURL=IViewMode.d.ts.map","engine/InteractionHandler.d.ts":"/**\n * InteractionHandler — Mouse/touch etkileşim yönetimi\n *\n * Pointer Events API ile pan, zoom (wheel + pinch) ve rotate işlemleri.\n * Canvas/container üzerinde DOM eventlerini dinler, CameraController2D'ye yansıtır.\n *\n * Features:\n * - Pan: pointer drag (left button / single touch)\n * - Zoom: mouse wheel (zoom-to-cursor) + pinch gesture\n * - Double-click/tap: zoom in\n * - Keyboard: +/- zoom, arrow keys pan\n */\nimport type { CameraController2D } from './CameraController2D';\nexport interface InteractionHandlerOptions {\n    /** Enable pan (default: true) */\n    pan?: boolean;\n    /** Enable zoom (default: true) */\n    zoom?: boolean;\n    /** Enable keyboard shortcuts (default: true) */\n    keyboard?: boolean;\n    /** Enable double-click/tap zoom (default: true) */\n    doubleClickZoom?: boolean;\n    /** Zoom speed multiplier for wheel events (default: 1) */\n    zoomSpeed?: number;\n    /** Pan inertia duration in ms (0 = disabled, default: 300) */\n    inertiaDuration?: number;\n}\nexport declare class InteractionHandler {\n    private readonly _element;\n    private readonly _camera;\n    private readonly _onDirty;\n    private readonly _onViewChange;\n    private readonly _panEnabled;\n    private readonly _zoomEnabled;\n    private readonly _keyboardEnabled;\n    private readonly _doubleClickZoom;\n    private readonly _zoomSpeed;\n    private readonly _inertiaDuration;\n    private _dragging;\n    private _lastPointerX;\n    private _lastPointerY;\n    private _activePointerId;\n    private _pointers;\n    private _lastPinchDist;\n    private _lastPinchCenterX;\n    private _lastPinchCenterY;\n    private _velocityX;\n    private _velocityY;\n    private _lastMoveTime;\n    private _inertiaRafId;\n    private _lastClickTime;\n    private _lastClickX;\n    private _lastClickY;\n    private readonly _onPointerDown;\n    private readonly _onPointerMove;\n    private readonly _onPointerUp;\n    private readonly _onWheel;\n    private readonly _onKeyDown;\n    private readonly _onContextMenu;\n    private _destroyed;\n    constructor(element: HTMLElement, camera: CameraController2D, onDirty: () => void, onViewChange: () => void, options?: InteractionHandlerOptions);\n    destroy(): void;\n    private _attach;\n    private _detach;\n    private _handlePointerDown;\n    private _handlePointerMove;\n    private _handlePointerUp;\n    private _handleWheel;\n    private _handleKeyDown;\n    private _handleDoubleClick;\n    private _initPinch;\n    private _handlePinchMove;\n    private _startInertia;\n    private _stopInertia;\n    /**\n     * Zoom to a specific point on screen: the point under the cursor stays fixed.\n     */\n    private _zoomToPoint;\n    /**\n     * Get current camera resolution (meters per pixel).\n     */\n    private _getResolution;\n    /**\n     * Notify render loop + view change event.\n     */\n    private _notifyChange;\n}\n//# sourceMappingURL=InteractionHandler.d.ts.map","engine/InteractionHandler3D.d.ts":"/**\n * InteractionHandler3D — 3D globe interaction controls\n *\n * Pointer events → CameraController3D mutations.\n * Follows the same pattern as InteractionHandler (2D).\n *\n * Gesture mapping:\n * - Single pointer drag → Orbit (rotate camera lon/lat around Earth)\n * - Right-click / Shift+drag → Tilt + heading adjustment\n * - Wheel → Altitude zoom (closer/farther from Earth)\n * - Pinch → Altitude zoom\n * - Double-click → Zoom in at click point\n * - Keyboard arrows → Orbit\n * - +/- keys → Altitude zoom\n */\nimport type { CameraController3D } from './CameraController3D';\nexport interface InteractionHandler3DOptions {\n    /** Enable orbit (drag to rotate) — default: true */\n    orbit?: boolean;\n    /** Enable zoom (wheel/pinch to change altitude) — default: true */\n    zoom?: boolean;\n    /** Enable tilt (right-click/shift-drag) — default: true */\n    tilt?: boolean;\n    /** Enable keyboard navigation — default: true */\n    keyboard?: boolean;\n    /** Enable double-click zoom — default: true */\n    doubleClickZoom?: boolean;\n    /** Zoom speed multiplier — default: 1 */\n    zoomSpeed?: number;\n    /** Inertia duration in ms (0 = disabled) — default: 300 */\n    inertiaDuration?: number;\n}\nexport declare class InteractionHandler3D {\n    private readonly _element;\n    private readonly _camera;\n    private readonly _onDirty;\n    private readonly _onViewChange;\n    private readonly _orbitEnabled;\n    private readonly _zoomEnabled;\n    private readonly _tiltEnabled;\n    private readonly _keyboardEnabled;\n    private readonly _doubleClickZoomEnabled;\n    private readonly _zoomSpeed;\n    private readonly _inertiaDuration;\n    private _pointers;\n    private _dragging;\n    private _activePointerId;\n    private _lastPointerX;\n    private _lastPointerY;\n    private _shiftDrag;\n    private _rightButtonDrag;\n    private _velocityLon;\n    private _velocityLat;\n    private _lastMoveTime;\n    private _lastPinchDist;\n    private _lastClickTime;\n    private _lastClickX;\n    private _lastClickY;\n    private _inertiaRafId;\n    private _destroyed;\n    private readonly _boundPointerDown;\n    private readonly _boundPointerMove;\n    private readonly _boundPointerUp;\n    private readonly _boundWheel;\n    private readonly _boundKeyDown;\n    private readonly _boundContextMenu;\n    constructor(element: HTMLElement, camera: CameraController3D, onDirty: () => void, onViewChange: () => void, options?: InteractionHandler3DOptions);\n    destroy(): void;\n    private _handlePointerDown;\n    private _handlePointerMove;\n    private _handlePointerUp;\n    private _handleOrbitDrag;\n    private _handleTiltHeadingDrag;\n    private _initPinch;\n    private _handlePinch;\n    private _handleWheel;\n    private _handleKeyDown;\n    private _handleDoubleClick;\n    private _startInertia;\n    private _cancelInertia;\n}\n//# sourceMappingURL=InteractionHandler3D.d.ts.map","engine/LayerManager.d.ts":"/**\n * LayerManager — Layer lifecycle yönetimi\n *\n * Layer load orchestration (load() çağırma, hata yönetimi).\n * Visibility tracking (zoom bazlı min/maxScale kontrolü).\n * Layer'ın dirty state'ini takip et.\n */\nimport type { ILayer } from '../interfaces/index';\nimport type { MapError } from '../errors';\nexport interface LayerManagerEvents {\n    [key: string]: unknown;\n    /** A layer finished loading */\n    'layer-loaded': {\n        layerId: string;\n    };\n    /** A layer failed to load */\n    'layer-load-error': {\n        layerId: string;\n        error: MapError;\n    };\n    /** Layer visibility changed (by zoom or explicit toggle) */\n    'layer-visibility-change': {\n        layerId: string;\n        visible: boolean;\n    };\n    /** Layer dirty state changed */\n    'layer-dirty': {\n        layerId: string;\n    };\n}\nexport declare class LayerManager {\n    private _layers;\n    private _events;\n    private _currentZoom;\n    /**\n     * Register a layer and begin loading it.\n     * If the layer is already registered, this is a no-op.\n     */\n    addLayer(layer: ILayer): Promise<void>;\n    /**\n     * Unregister a layer. Calls destroy() on the layer.\n     */\n    removeLayer(layerId: string): void;\n    /**\n     * Remove all layers.\n     */\n    removeAll(): void;\n    /**\n     * Get a registered layer by id.\n     */\n    getLayer(layerId: string): ILayer | undefined;\n    /**\n     * Get all registered layer ids.\n     */\n    getLayerIds(): string[];\n    /**\n     * Update the current zoom level and re-evaluate visibility for all layers.\n     */\n    setCurrentZoom(zoom: number): void;\n    /**\n     * Check if a layer is effectively visible (considering zoom constraints).\n     */\n    isLayerVisible(layerId: string): boolean;\n    /**\n     * Mark a layer as dirty (needs re-render).\n     */\n    markDirty(layerId: string): void;\n    /**\n     * Get all dirty layer ids.\n     */\n    getDirtyLayers(): string[];\n    /**\n     * Clear dirty flag for a specific layer.\n     */\n    clearDirty(layerId: string): void;\n    /**\n     * Clear dirty flags for all layers.\n     */\n    clearAllDirty(): void;\n    /**\n     * Returns true if any registered layer is dirty.\n     */\n    hasAnyDirty(): boolean;\n    on<K extends keyof LayerManagerEvents>(event: K, handler: (data: LayerManagerEvents[K]) => void): void;\n    off<K extends keyof LayerManagerEvents>(event: K, handler: (data: LayerManagerEvents[K]) => void): void;\n    destroy(): void;\n    private _loadLayer;\n    /**\n     * Determine visibility based on zoom level and layer min/maxScale.\n     *\n     * Scale denominator at zoom level z = 559082264.028717 / 2^z.\n     * A layer is visible when: minScale >= currentScale >= maxScale.\n     * (minScale is a large number = zoomed out, maxScale is small = zoomed in)\n     */\n    private _isVisibleAtZoom;\n}\n//# sourceMappingURL=LayerManager.d.ts.map","engine/Map.d.ts":"/**\n * Map — Layer koleksiyonu\n *\n * Layer ekleme/çıkarma, z-order yönetimi.\n * Event'ler: layer-add, layer-remove, layer-reorder.\n */\nimport type { ILayer } from '../interfaces/index';\nexport interface MapEvents {\n    [key: string]: unknown;\n    'layer-add': {\n        layer: ILayer;\n        index: number;\n    };\n    'layer-remove': {\n        layer: ILayer;\n        index: number;\n    };\n    'layer-reorder': {\n        layer: ILayer;\n        fromIndex: number;\n        toIndex: number;\n    };\n}\nexport declare class GameMap {\n    private _layers;\n    private _events;\n    /**\n     * Read-only view of the layer stack (bottom-to-top draw order).\n     */\n    get layers(): readonly ILayer[];\n    /**\n     * Add a layer to the top of the layer stack.\n     */\n    add(layer: ILayer): void;\n    /**\n     * Remove a layer from the stack.\n     * Returns the removed layer, or undefined if not found.\n     */\n    remove(layer: ILayer): ILayer | undefined;\n    /**\n     * Find a layer by its id.\n     */\n    findLayerById(id: string): ILayer | undefined;\n    /**\n     * Reorder a layer to the given index (z-order).\n     * Index 0 = bottom (drawn first), length-1 = top (drawn last).\n     */\n    reorder(layer: ILayer, toIndex: number): void;\n    /**\n     * Remove all layers.\n     */\n    removeAll(): void;\n    on<K extends keyof MapEvents>(event: K, handler: (data: MapEvents[K]) => void): void;\n    off<K extends keyof MapEvents>(event: K, handler: (data: MapEvents[K]) => void): void;\n    destroy(): void;\n}\n//# sourceMappingURL=Map.d.ts.map","engine/MapView.d.ts":"/**\n * MapView — Unified public API for 2D and 3D map rendering.\n *\n * Replaces MapView2D, GlobeView, and TerrainView with a single entry point\n * that supports mode switching via switchTo('2d'|'3d').\n *\n * Architecture:\n * - ViewCore: shared infrastructure (canvas, GPU, tiles, layers, buffers)\n * - IViewMode: strategy interface (Mode2D or Mode3D)\n * - MapView: orchestrates lifecycle, events, and mode switching\n */\nimport type { ILayer, IRenderEngine, IView, Extent, Feature, GlobeEffectsConfig } from '../interfaces/index';\nimport type { MapError } from '../errors';\nimport type { ViewState, GoToTarget } from './IViewMode';\nimport type { InteractionHandlerOptions } from './InteractionHandler';\nimport type { GlobeInteractionOptions } from './GlobeInteraction';\nimport { ToolManager } from '../tools/ToolManager';\n/** Result of a hit-test query at a screen position. */\nexport interface HitTestResult {\n    /** The layer containing the hit feature. */\n    layer: ILayer;\n    /** The hit feature (from layer.getFeatures()). */\n    feature: Feature;\n    /** Geographic coordinates at the hit position [lon, lat]. */\n    mapPoint: [number, number] | null;\n}\n/**\n * Event map for all events emitted by {@link MapView}.\n *\n * Use with {@link MapView.on} and {@link MapView.off} for type-safe event handling.\n *\n * @example\n * ```ts\n * view.on('view-change', (e) => {\n *   console.log(`Center: ${e.center}, Zoom: ${e.zoom}, Mode: ${e.mode}`);\n * });\n *\n * view.on('error', (err) => {\n *   if (err.kind === 'webgpu-not-supported') {\n *     showFallbackUI();\n *   }\n * });\n * ```\n */\nexport interface MapViewEvents {\n    /** Catch-all index signature for custom event extensions. */\n    [key: string]: unknown;\n    /**\n     * Fired whenever the camera or viewport changes (pan, zoom, rotate, pitch).\n     * Includes the current mode so listeners can branch on 2D vs 3D state.\n     */\n    'view-change': {\n        /** Current center coordinate as [longitude, latitude] in EPSG:4326. */\n        center: [number, number];\n        /** Current zoom level. */\n        zoom: number;\n        /** Current pitch in degrees (always 0 in 2D mode). */\n        pitch: number;\n        /** Current bearing/heading in degrees (always 0 in 2D mode). */\n        bearing: number;\n        /** Current rotation in degrees (always 0 in 3D mode). */\n        rotation: number;\n        /** Active rendering mode. */\n        mode: '2d' | '3d';\n    };\n    /**\n     * Fired after a successful {@link MapView.switchTo} call completes.\n     * Contains the previous and new mode identifiers.\n     */\n    'mode-change': {\n        from: '2d' | '3d';\n        to: '2d' | '3d';\n    };\n    /**\n     * Fired once when the MapView finishes initialization (GPU init or headless setup).\n     * After this event, the view is ready to accept layers and render frames.\n     */\n    'ready': void;\n    /**\n     * Fired when a non-fatal error occurs (e.g. WebGPU not supported, device lost).\n     * The payload is a discriminated union — inspect the `kind` field to determine\n     * the specific error type.\n     */\n    'error': MapError;\n    /** Fired after a layer is added to the map via `map.add(layer)`. */\n    'layer-add': {\n        layer: ILayer;\n    };\n    /** Fired after a layer is removed from the map via `map.remove(layer)`. */\n    'layer-remove': {\n        layer: ILayer;\n    };\n    /**\n     * Fired at the end of each rendered frame.\n     * Useful for performance monitoring and HUD overlays.\n     */\n    'frame': {\n        /** Monotonically increasing frame counter. */\n        frameNumber: number;\n        /** Smoothed frames-per-second measurement. */\n        fps: number;\n    };\n    /** Fired when {@link MapView.destroy} is called, before resources are released. */\n    'destroy': void;\n    /** Fired on a pointer click on the map canvas. */\n    'click': {\n        /** Horizontal pixel coordinate relative to the canvas. */\n        screenX: number;\n        /** Vertical pixel coordinate relative to the canvas. */\n        screenY: number;\n        /** Geographic coordinates at the click position, or null if off-map (3D). */\n        mapPoint: [number, number] | null;\n    };\n    /** Fired on pointer movement over the map canvas (throttled ~60fps). */\n    'pointer-move': {\n        screenX: number;\n        screenY: number;\n        mapPoint: [number, number] | null;\n    };\n    /** Fired on double-click on the map canvas. */\n    'dblclick': {\n        screenX: number;\n        screenY: number;\n        mapPoint: [number, number] | null;\n    };\n    /** Fired on pointer button press on the map canvas. */\n    'mousedown': {\n        screenX: number;\n        screenY: number;\n        mapPoint: [number, number] | null;\n        button: number;\n    };\n    /** Fired on pointer button release on the map canvas. */\n    'mouseup': {\n        screenX: number;\n        screenY: number;\n        mapPoint: [number, number] | null;\n        button: number;\n    };\n    /** Fired on right-click (context menu) on the map canvas. */\n    'contextmenu': {\n        screenX: number;\n        screenY: number;\n        mapPoint: [number, number] | null;\n        originalEvent: Event;\n    };\n    /** Fired when a zoom animation starts (goTo, wheel, pinch, double-click). */\n    'zoomstart': {\n        zoom: number;\n    };\n    /** Fired when a zoom animation ends. */\n    'zoomend': {\n        zoom: number;\n    };\n    /** Fired when a move/pan animation starts. */\n    'movestart': {\n        center: [number, number];\n    };\n    /** Fired when a move/pan animation ends. */\n    'moveend': {\n        center: [number, number];\n    };\n}\n/**\n * Configuration options for creating a {@link MapView} instance.\n *\n * @example\n * ```ts\n * const view = new MapView({\n *   container: '#map',\n *   mode: '2d',\n *   center: [29.0, 41.0],\n *   zoom: 10,\n *   renderEngine: new WebGPURenderEngine(),\n * });\n * ```\n */\nexport interface MapViewOptions {\n    /**\n     * DOM container element or CSS selector string (e.g. `'#map'`).\n     * Pass `null` for headless or test mode where no DOM is available.\n     */\n    container: HTMLElement | string | null;\n    /**\n     * Initial rendering mode.\n     * - `'2d'` — flat Mercator map (default)\n     * - `'3d'` — globe/terrain rendering with perspective camera\n     * @defaultValue `'2d'`\n     */\n    mode?: '2d' | '3d';\n    /**\n     * Initial center coordinate as `[longitude, latitude]` in EPSG:4326.\n     * @defaultValue `[0, 0]` (set by the active mode)\n     */\n    center?: [number, number];\n    /**\n     * Initial zoom level. Higher values zoom in closer.\n     * @defaultValue `2` (set by the active mode)\n     */\n    zoom?: number;\n    /**\n     * Initial map rotation in degrees (2D mode only). Positive values rotate clockwise.\n     * @defaultValue `0`\n     */\n    rotation?: number;\n    /**\n     * Initial pitch (tilt) angle in degrees (3D mode only).\n     * `0` looks straight down; higher values tilt toward the horizon.\n     * @defaultValue `0`\n     */\n    pitch?: number;\n    /**\n     * Initial bearing (heading) in degrees (3D mode only).\n     * `0` points north; `90` points east.\n     * @defaultValue `0`\n     */\n    bearing?: number;\n    /**\n     * Minimum allowed zoom level (2D mode only).\n     * @defaultValue `0`\n     */\n    minZoom?: number;\n    /**\n     * Maximum allowed zoom level (2D mode only).\n     * @defaultValue `22`\n     */\n    maxZoom?: number;\n    /**\n     * Pre-created render engine instance. When provided, the MapView will\n     * initialize WebGPU using this engine. When omitted, the view operates\n     * in headless mode (useful for unit testing).\n     */\n    renderEngine?: IRenderEngine;\n    /**\n     * Background color used when clearing the canvas each frame.\n     * Accepts any CSS color string.\n     * @defaultValue `'black'`\n     */\n    backgroundColor?: string;\n    /**\n     * Interaction (pan/zoom/rotate) configuration. Pass an options object to\n     * customize behavior, or `false` to disable all pointer/keyboard interaction.\n     * @defaultValue `{}` (default interaction enabled)\n     */\n    interaction?: InteractionHandlerOptions | GlobeInteractionOptions | false;\n    /**\n     * Restrict the map view to within the given geographic bounds.\n     * The camera center will be clamped to stay within these bounds.\n     * `[minLon, minLat, maxLon, maxLat]` in EPSG:4326.\n     */\n    maxBounds?: [number, number, number, number];\n    /**\n     * Globe effects configuration for 3D mode: lighting, atmosphere, pole caps,\n     * background color. All properties are optional with sensible defaults.\n     *\n     * @example\n     * ```ts\n     * globeEffects: {\n     *   lighting: { ambient: 0.7, shadowStrength: 0.1 },\n     *   atmosphere: { strength: 1.2 },\n     *   backgroundColor: [0.02, 0.02, 0.08, 1.0],\n     * }\n     * ```\n     */\n    globeEffects?: GlobeEffectsConfig;\n}\n/**\n * Unified public API for 2D and 3D map rendering.\n *\n * `MapView` is the primary entry point for creating a mapgpu map. It manages\n * the full lifecycle — GPU initialization, layer management, interaction\n * handling, and render loop — and supports runtime mode switching between\n * flat Mercator (2D) and globe/terrain (3D) views via {@link switchTo}.\n *\n * @example\n * ```ts\n * import { MapView } from '@mapgpu/core';\n * import { WebGPURenderEngine } from '@mapgpu/render-webgpu';\n *\n * const view = new MapView({\n *   container: '#map',\n *   mode: '2d',\n *   center: [29.0, 41.0],\n *   zoom: 6,\n *   renderEngine: new WebGPURenderEngine(),\n * });\n *\n * await view.when();\n * console.log('Map is ready!');\n * ```\n */\nexport declare class MapView implements IView {\n    /**\n     * The underlying {@link GameMap} layer collection.\n     * Use `map.add(layer)` and `map.remove(layer)` to manage layers.\n     *\n     * @example\n     * ```ts\n     * view.map.add(myTileLayer);\n     * view.map.remove(myTileLayer);\n     * ```\n     */\n    get map(): import('./Map').GameMap;\n    /** Unique instance identifier (satisfies {@link IView}). */\n    readonly id: string;\n    /** Active rendering mode discriminant (satisfies {@link IView}). Alias for {@link mode}. */\n    get type(): '2d' | '3d';\n    private _core;\n    private _mode;\n    private _events;\n    private _ready;\n    private _destroyed;\n    private _readyResolve;\n    private _readyPromise;\n    private _interactionOptions;\n    private _animatedLayerCallbacks;\n    private _toolManager;\n    private _clickHandler;\n    private _pointerMoveHandler;\n    /**\n     * Creates a new MapView instance.\n     *\n     * The constructor sets up the canvas, creates the initial view mode (2D or 3D),\n     * wires layer management events, attaches interaction handlers, and begins\n     * asynchronous GPU initialization. Use {@link when} to wait for readiness.\n     *\n     * @param options - Configuration options for the view.\n     * @throws {Error} If `options.container` is a CSS selector that does not match any element.\n     *\n     * @example\n     * ```ts\n     * // Basic 2D map\n     * const view = new MapView({\n     *   container: document.getElementById('map'),\n     *   center: [32.85, 39.92],\n     *   zoom: 12,\n     *   renderEngine: engine,\n     * });\n     *\n     * // 3D globe\n     * const globe = new MapView({\n     *   container: '#globe',\n     *   mode: '3d',\n     *   center: [29.0, 41.0],\n     *   zoom: 4,\n     *   pitch: 45,\n     *   bearing: -30,\n     *   renderEngine: engine,\n     * });\n     *\n     * // Headless / test mode (no DOM, no GPU)\n     * const headless = new MapView({ container: null });\n     * ```\n     */\n    constructor(options: MapViewOptions);\n    /** The currently active rendering mode (`'2d'` or `'3d'`). */\n    get mode(): '2d' | '3d';\n    /**\n     * Current map center as `[longitude, latitude]` in EPSG:4326.\n     *\n     * @example\n     * ```ts\n     * const [lon, lat] = view.center;\n     * ```\n     */\n    get center(): [number, number];\n    /** Current zoom level. */\n    get zoom(): number;\n    /** Current pitch (tilt) angle in degrees. Always `0` in 2D mode. */\n    get pitch(): number;\n    /** Current bearing (heading) in degrees. Always `0` in 2D mode. */\n    get bearing(): number;\n    /** Current map rotation in degrees. Always `0` in 3D mode. */\n    get rotation(): number;\n    /**\n     * Whether the MapView has completed initialization.\n     * This becomes `true` after GPU init succeeds (or after the headless microtask).\n     */\n    get ready(): boolean;\n    /**\n     * Whether the WebGPU device and context are fully initialized and available.\n     * May be `false` even after {@link ready} is `true` if WebGPU is not supported.\n     */\n    get gpuReady(): boolean;\n    /** The underlying `<canvas>` element, or `null` in headless mode. */\n    get canvas(): HTMLCanvasElement | null;\n    /**\n     * Returns the full view state as a serializable {@link ViewState} object.\n     * Useful for persisting view state or passing it to other components.\n     *\n     * @returns The current view state including center, zoom, pitch, bearing, and rotation.\n     *\n     * @example\n     * ```ts\n     * const state = view.getViewState();\n     * localStorage.setItem('mapState', JSON.stringify(state));\n     * ```\n     */\n    getViewState(): ViewState;\n    /**\n     * Lazy-initialized tool manager for drawing/editing tools.\n     *\n     * The ToolManager provides an overlay-based event interception system,\n     * tool registry, undo/redo support via {@link CommandSystem}, and\n     * typed event dispatch. It is created on first access.\n     *\n     * @example\n     * ```ts\n     * const tm = view.toolManager;\n     * tm.registerTool(new DrawPointTool({ targetLayer }));\n     * tm.activateTool('draw-point');\n     * ```\n     */\n    get toolManager(): ToolManager;\n    /**\n     * Switch between 2D and 3D rendering modes at runtime.\n     *\n     * Preserves the current center and zoom across the transition. The previous\n     * mode's interaction handler is disposed and a new one is created for the\n     * target mode. All vector buffer caches are invalidated since GPU pipelines\n     * differ between modes. Emits a `'mode-change'` event on completion.\n     *\n     * If the view is already in the requested mode, this is a no-op.\n     *\n     * @param mode - The target rendering mode (`'2d'` or `'3d'`).\n     * @returns A promise that resolves when the mode switch is complete.\n     * @throws {Error} If the view has been destroyed.\n     *\n     * @example\n     * ```ts\n     * // Switch to globe view\n     * await view.switchTo('3d');\n     *\n     * // Switch back to flat map\n     * await view.switchTo('2d');\n     * ```\n     */\n    switchTo(mode: '2d' | '3d'): Promise<void>;\n    /**\n     * Animate the view to a new camera position.\n     *\n     * Accepts a partial {@link GoToTarget} — only the specified fields are\n     * animated; others remain unchanged. The active mode determines which\n     * fields are honored (e.g. `rotation` is 2D-only, `pitch`/`bearing` are\n     * 3D-only). The returned promise resolves when the animation completes.\n     *\n     * Calling `goTo` while a previous animation is in progress cancels the\n     * previous animation and starts the new one.\n     *\n     * @param target - The navigation target containing any combination of center,\n     *   zoom, pitch, bearing, rotation, and duration.\n     * @returns A promise that resolves when the animation finishes.\n     * @throws {Error} If the view has been destroyed (returned as a rejected promise).\n     *\n     * @example\n     * ```ts\n     * // Fly to Istanbul over 1 second\n     * await view.goTo({\n     *   center: [29.0, 41.0],\n     *   zoom: 12,\n     *   duration: 1000,\n     * });\n     *\n     * // Smoothly change only zoom\n     * await view.goTo({ zoom: 8 });\n     *\n     * // Instant jump (no animation)\n     * await view.goTo({ center: [0, 0], zoom: 2, duration: 0 });\n     * ```\n     */\n    goTo(target: GoToTarget): Promise<void>;\n    /**\n     * Zoom the view to fit the given geographic bounds.\n     *\n     * Works in both 2D (Mercator) and 3D (Globe) modes.\n     *\n     * @param bounds - Geographic extent as `[minLon, minLat, maxLon, maxLat]` in EPSG:4326.\n     * @param options - Optional padding (px) and animation duration.\n     */\n    fitBounds(bounds: [number, number, number, number], options?: {\n        padding?: number | {\n            top: number;\n            right: number;\n            bottom: number;\n            left: number;\n        };\n        duration?: number;\n    }): Promise<void>;\n    /**\n     * Pan to the given center coordinate, keeping the current zoom.\n     */\n    panTo(center: [number, number], options?: {\n        duration?: number;\n    }): Promise<void>;\n    /**\n     * Jump/animate to a specific view state (Leaflet setView equivalent).\n     */\n    setView(center: [number, number], zoom: number, options?: {\n        duration?: number;\n    }): Promise<void>;\n    /** Zoom in by one level, optionally at a specific point. */\n    zoomIn(options?: {\n        duration?: number;\n    }): Promise<void>;\n    /** Zoom out by one level. */\n    zoomOut(options?: {\n        duration?: number;\n    }): Promise<void>;\n    /**\n     * Fly to a target with arc animation (zoom out → pan → zoom in).\n     * Like goTo but with a more dramatic camera path.\n     */\n    flyTo(target: GoToTarget, options?: {\n        duration?: number;\n    }): Promise<void>;\n    private _maxBounds;\n    /**\n     * Restrict the map view to the given geographic bounds.\n     * Pass `null` to remove the constraint.\n     */\n    setMaxBounds(bounds: [number, number, number, number] | null): void;\n    /** Get the current max bounds constraint, or `null` if none. */\n    getMaxBounds(): [number, number, number, number] | null;\n    private _clampToMaxBounds;\n    /**\n     * Convert screen pixel coordinates to geographic coordinates.\n     *\n     * Returns `null` if the screen position does not intersect the map surface\n     * (e.g. clicking on empty space above the horizon in 3D mode).\n     *\n     * @param screenX - Horizontal pixel coordinate relative to the canvas.\n     * @param screenY - Vertical pixel coordinate relative to the canvas.\n     * @returns Geographic coordinates as `[longitude, latitude]` in EPSG:4326, or `null`.\n     *\n     * @example\n     * ```ts\n     * canvas.addEventListener('click', (e) => {\n     *   const coords = view.toMap(e.offsetX, e.offsetY);\n     *   if (coords) {\n     *     console.log(`Clicked at lon=${coords[0]}, lat=${coords[1]}`);\n     *   }\n     * });\n     * ```\n     */\n    toMap(screenX: number, screenY: number): [number, number] | null;\n    /**\n     * Convert geographic coordinates to screen pixel coordinates.\n     *\n     * Returns `null` if the geographic point is not visible on screen\n     * (e.g. on the far side of the globe in 3D mode).\n     *\n     * @param lon - Longitude in degrees (EPSG:4326).\n     * @param lat - Latitude in degrees (EPSG:4326).\n     * @returns Screen pixel coordinates as `[x, y]`, or `null`.\n     *\n     * @example\n     * ```ts\n     * const pixel = view.toScreen(29.0, 41.0);\n     * if (pixel) {\n     *   tooltip.style.left = `${pixel[0]}px`;\n     *   tooltip.style.top = `${pixel[1]}px`;\n     * }\n     * ```\n     */\n    toScreen(lon: number, lat: number): [number, number] | null;\n    /**\n     * Register an event listener for a specific event type.\n     *\n     * Event types and their payloads are defined in {@link MapViewEvents}.\n     * Multiple listeners can be registered for the same event.\n     *\n     * @param event - The event name to listen for.\n     * @param handler - Callback function invoked with the event payload.\n     *\n     * @example\n     * ```ts\n     * view.on('ready', () => {\n     *   console.log('Map is ready');\n     * });\n     *\n     * view.on('view-change', ({ center, zoom }) => {\n     *   updateURL(center, zoom);\n     * });\n     *\n     * view.on('frame', ({ fps }) => {\n     *   fpsCounter.textContent = `${fps.toFixed(0)} FPS`;\n     * });\n     * ```\n     */\n    on<K extends keyof MapViewEvents>(event: K, handler: (data: MapViewEvents[K]) => void): void;\n    /**\n     * Remove a previously registered event listener.\n     *\n     * The `handler` reference must be the same function passed to {@link on}.\n     *\n     * @param event - The event name to stop listening for.\n     * @param handler - The exact callback reference that was registered.\n     *\n     * @example\n     * ```ts\n     * const onViewChange = (e) => console.log(e);\n     * view.on('view-change', onViewChange);\n     *\n     * // Later, remove the listener\n     * view.off('view-change', onViewChange);\n     * ```\n     */\n    off<K extends keyof MapViewEvents>(event: K, handler: (data: MapViewEvents[K]) => void): void;\n    /**\n     * Returns a promise that resolves when the MapView is ready.\n     *\n     * This is equivalent to listening for the `'ready'` event, but in promise\n     * form for convenient `await` usage. If the view is already ready, the\n     * promise resolves immediately on the next microtask.\n     *\n     * @returns A promise that resolves when GPU initialization is complete.\n     *\n     * @example\n     * ```ts\n     * const view = new MapView({ container: '#map', renderEngine: engine });\n     * await view.when();\n     * // Safe to add layers and interact with the map\n     * view.map.add(myLayer);\n     * ```\n     */\n    when(): Promise<void>;\n    /**\n     * Destroy the MapView and release all associated resources.\n     *\n     * Cancels any in-progress animations, disposes the active mode's interaction\n     * handler, tears down the render loop and GPU resources, emits a `'destroy'`\n     * event, and removes all event listeners. After calling `destroy()`, the\n     * view instance must not be reused.\n     *\n     * Calling `destroy()` on an already-destroyed view is a safe no-op.\n     *\n     * @example\n     * ```ts\n     * // Clean up when component unmounts\n     * view.destroy();\n     * ```\n     */\n    destroy(): void;\n    /**\n     * Load an icon image for use with icon point symbols.\n     *\n     * Fetches the image from a URL (string) or accepts a pre-loaded ImageBitmap,\n     * then registers it in the render engine's sprite atlas. The returned `id`\n     * can be used as `PointSymbol.src` in renderers.\n     *\n     * @param id - Unique icon identifier (used as `symbol.src`)\n     * @param source - Image URL (string) or pre-loaded ImageBitmap\n     *\n     * @example\n     * ```ts\n     * // Load from URL\n     * await mapView.loadIcon('hospital', '/icons/hospital.png');\n     *\n     * // Use in renderer\n     * layer.renderer = new SimpleRenderer({\n     *   type: 'icon',\n     *   src: 'hospital',\n     *   size: 32,\n     *   color: [255, 255, 255, 255],\n     * });\n     * ```\n     */\n    loadIcon(id: string, source: string | ImageBitmap): Promise<void>;\n    /**\n     * Load a 3D model into the render engine for use with ModelSymbol.\n     * Supports GLB binary, glTF text format (URL), and raw ArrayBuffer.\n     *\n     * @example\n     * ```ts\n     * // Load GLB from URL\n     * await mapView.loadModel('missile', '/assets/missile.glb');\n     *\n     * // Load glTF from URL (.gltf + external .bin)\n     * await mapView.loadModel('building', '/assets/building.gltf');\n     *\n     * // Load from ArrayBuffer (GLB)\n     * const glb = await fetch('/assets/cube.glb').then(r => r.arrayBuffer());\n     * await mapView.loadModel('cube', glb);\n     *\n     * // Use in renderer\n     * layer.renderer = new SimpleRenderer({\n     *   type: 'model',\n     *   modelId: 'missile',\n     *   scale: 100,\n     * });\n     * ```\n     */\n    loadModel(id: string, source: string | ArrayBuffer): Promise<void>;\n    /**\n     * Compute the visible map extent as an AABB in EPSG:4326.\n     *\n     * Projects the four canvas corners via {@link toMap} and returns the\n     * axis-aligned bounding box. Returns `null` in headless mode or if\n     * no corners could be projected (e.g. looking at sky in 3D).\n     */\n    getBounds(): Extent | null;\n    /**\n     * Query features at a screen position via GPU picking.\n     *\n     * Returns an array of {@link HitTestResult} sorted by layer draw order\n     * (topmost first). Layers with `interactive === false` are skipped.\n     *\n     * @param screenX - Horizontal pixel coordinate relative to the canvas.\n     * @param screenY - Vertical pixel coordinate relative to the canvas.\n     */\n    hitTest(screenX: number, screenY: number): Promise<HitTestResult[]>;\n    /**\n     * Load an SVG icon by rasterizing it to an ImageBitmap and registering\n     * it in the sprite atlas. The returned `id` can be used as\n     * `PointSymbol.src` in renderers.\n     *\n     * @param id - Unique icon identifier\n     * @param svgMarkup - SVG source markup string\n     * @param width - Rasterization width in pixels\n     * @param height - Rasterization height in pixels\n     */\n    loadSvgIcon(id: string, svgMarkup: string, width: number, height: number): Promise<void>;\n    /**\n     * Toggle wireframe debug overlay on raster tiles.\n     * When enabled, a cyan wireframe grid is drawn over each tile\n     * to visualize the underlying vertex grid.\n     */\n    set debugTileVertices(enabled: boolean);\n    get debugTileVertices(): boolean;\n    /**\n     * Toggle extrusion debug mode.\n     * When enabled: shader shows clipZ/height/normal as color overlay,\n     * and CPU-side ExtrusionConverter logs geometry stats to console.\n     */\n    set extrusionDebug(enabled: boolean);\n    get extrusionDebug(): boolean;\n    /**\n     * Apply debug height brush at given mercator coordinates.\n     * Coordinate system must match the current mode's tile extents:\n     * - 2D: EPSG:3857 meters\n     * - 3D globe: normalized mercator (0..1)\n     */\n    applyDebugBrush(mercX: number, mercY: number, radius: number, strength: number, softness?: number): void;\n    /** Clear all debug height brush data */\n    clearDebugBrush(): void;\n    /** Set height exaggeration factor for debug overlay (default 1.0) */\n    setHeightExaggeration(factor: number): void;\n    /**\n     * Configure globe effects: lighting, atmosphere, pole caps, and background color.\n     * Partial updates are merged with existing config.\n     *\n     * @example\n     * ```ts\n     * view.setGlobeEffects({ lighting: { ambient: 0.7 } });\n     * view.setGlobeEffects({ atmosphere: { enabled: false } });\n     * view.setGlobeEffects({ backgroundColor: [0.02, 0.02, 0.08, 1.0] });\n     * ```\n     */\n    setGlobeEffects(config: GlobeEffectsConfig): void;\n    /** Configure scene lighting for extrusion and 3D geometry. */\n    setLighting(config: import('../interfaces/IRenderEngine').LightConfig): void;\n    /**\n     * Emit a `'view-change'` event with the current view state and mode.\n     * Called after any camera/viewport mutation (pan, zoom, resize, goTo, switchTo).\n     */\n    private _emitViewChange;\n}\n//# sourceMappingURL=MapView.d.ts.map","engine/MapView2D.d.ts":"/**\n * MapView2D — Ana public API sınıfı\n *\n * new MapView2D({ container, center, zoom }) ile oluşturulur.\n * map property: layer koleksiyonunu tutar.\n * goTo(target): animasyonlu kamera hareketi.\n * toMap/toScreen: koordinat dönüşümü.\n * on(event, handler): typed event system (EventBus kullanır).\n * when(): Promise — view hazır olduğunda resolve.\n * destroy(): cleanup.\n *\n * Render coordinate space: EPSG:3857 (Web Mercator)\n */\nimport type { ILayer, Extent, IRenderEngine } from '../interfaces/index';\nimport type { MapError } from '../errors';\nimport { GameMap } from './Map';\nimport { CameraController2D } from './CameraController2D';\nimport { LayerManager } from './LayerManager';\nimport { RenderLoop } from './RenderLoop';\nimport { TileScheduler } from './TileScheduler';\nimport type { InteractionHandlerOptions } from './InteractionHandler';\nexport interface MapView2DEvents {\n    [key: string]: unknown;\n    /** View state changed (center, zoom, rotation) */\n    'view-change': {\n        center: [number, number];\n        zoom: number;\n        rotation: number;\n        extent: Extent;\n    };\n    /** View is ready (initial setup complete) */\n    'ready': void;\n    /** Error occurred */\n    'error': MapError;\n    /** Layer added to map */\n    'layer-add': {\n        layer: ILayer;\n    };\n    /** Layer removed from map */\n    'layer-remove': {\n        layer: ILayer;\n    };\n    /** A frame was rendered */\n    'frame': {\n        frameNumber: number;\n        fps: number;\n    };\n    /** View was destroyed */\n    'destroy': void;\n}\nexport interface GoToTarget {\n    center?: [number, number];\n    zoom?: number;\n    rotation?: number;\n    /** Animation duration in ms (0 = instant, default: 500) */\n    duration?: number;\n}\nexport interface MapView2DOptions {\n    /**\n     * DOM container element or selector string.\n     * When running without DOM (e.g. tests), pass null.\n     */\n    container: HTMLElement | string | null;\n    /** Initial center [longitude, latitude] */\n    center?: [number, number];\n    /** Initial zoom level */\n    zoom?: number;\n    /** Initial rotation in degrees */\n    rotation?: number;\n    /** Min zoom level */\n    minZoom?: number;\n    /** Max zoom level */\n    maxZoom?: number;\n    /** Optional render engine — when provided with a container, auto-inits WebGPU */\n    renderEngine?: IRenderEngine;\n    /** Background color for the canvas clear (CSS color, default: '#000') */\n    backgroundColor?: string;\n    /** Interaction options (pass false to disable all interaction) */\n    interaction?: InteractionHandlerOptions | false;\n}\nexport declare class MapView2D {\n    readonly map: GameMap;\n    private _events;\n    private _camera;\n    private _layerManager;\n    private _renderLoop;\n    private _tileScheduler;\n    private _tileManager;\n    private _renderEngine;\n    private _container;\n    private _canvas;\n    private _interaction;\n    private _resizeObserver;\n    private _ready;\n    private _gpuReady;\n    private _destroyed;\n    private _readyResolve;\n    private _readyPromise;\n    private _bufferCache;\n    private _animating;\n    private _animationId;\n    constructor(options: MapView2DOptions);\n    /** Current center [longitude, latitude] */\n    get center(): [number, number];\n    /** Current zoom level */\n    get zoom(): number;\n    /** Current rotation in degrees */\n    get rotation(): number;\n    /** Current visible extent in EPSG:3857 */\n    get extent(): Extent;\n    /** Whether the view is ready */\n    get ready(): boolean;\n    /** Whether the GPU is initialized */\n    get gpuReady(): boolean;\n    /** The canvas element (null when running without DOM) */\n    get canvas(): HTMLCanvasElement | null;\n    get camera(): CameraController2D;\n    get layerManager(): LayerManager;\n    get tileScheduler(): TileScheduler;\n    get renderLoop(): RenderLoop;\n    /**\n     * Navigate to a target with optional animation.\n     * Center is specified as [longitude, latitude] (EPSG:4326).\n     */\n    goTo(target: GoToTarget): Promise<void>;\n    /**\n     * Convert screen coordinates to map coordinates [longitude, latitude].\n     */\n    toMap(screenX: number, screenY: number): [number, number];\n    /**\n     * Convert map coordinates [longitude, latitude] to screen coordinates.\n     */\n    toScreen(lon: number, lat: number): [number, number];\n    on<K extends keyof MapView2DEvents>(event: K, handler: (data: MapView2DEvents[K]) => void): void;\n    off<K extends keyof MapView2DEvents>(event: K, handler: (data: MapView2DEvents[K]) => void): void;\n    /**\n     * Returns a promise that resolves when the view is ready.\n     */\n    when(): Promise<void>;\n    destroy(): void;\n    /**\n     * Create a <canvas> element inside the container, sized to fill it.\n     */\n    private _createCanvas;\n    /**\n     * Observe container resize and update canvas dimensions + camera viewport.\n     */\n    private _setupResizeObserver;\n    /**\n     * Async GPU initialization — called when renderEngine + canvas are both available.\n     */\n    private _initGpu;\n    /**\n     * Render one frame: iterate visible layers, draw raster tiles + vector geometry.\n     * Called inside the RenderLoop frame callback between beginFrame() and endFrame().\n     */\n    private _debugFrameCount;\n    private _renderFrame;\n    /**\n     * Render a single vector layer. Builds GPU buffers on first call (or when dirty),\n     * then reuses cached buffers on subsequent frames.\n     */\n    private _renderVectorLayer;\n    /**\n     * Render a DTED terrain layer.\n     * Converts viewport extent from EPSG:3857 → WGS84, queries visible tiles,\n     * creates GPU textures on demand, and calls drawTerrain() for each tile.\n     */\n    private _renderTerrainLayer;\n    private _cancelAnimation;\n    private _emitViewChange;\n}\n//# sourceMappingURL=MapView2D.d.ts.map","engine/Mesh3DConverter.d.ts":"/**\n * Mesh3DConverter — Converts Point features + Mesh3DSymbol into GPU vertex/index data.\n *\n * Flow:\n * 1. Generate unit mesh for the symbol's meshType (box, cylinder, sphere, cone)\n * 2. Apply scale + rotation transforms\n * 3. Position in Mercator [0..1] + height (metres) space\n * 4. Pack into interleaved vertex buffer [pos3 + norm3] = 24 bytes/vertex\n */\nimport type { Feature, Mesh3DSymbol } from '../interfaces/index';\n/** Output from the converter — ready for GPU buffer creation. */\nexport interface Mesh3DVertexData {\n    /** Interleaved [posX, posY, posZ, normX, normY, normZ] per vertex */\n    vertices: Float32Array;\n    /** Triangle indices (uint32) */\n    indices: Uint32Array;\n    /** Number of indices */\n    indexCount: number;\n}\n/**\n * Convert Point features + Mesh3DSymbol into GPU-ready vertex data.\n *\n * Supports multiple features — each gets its own copy of the mesh\n * at its geographic position.\n */\nexport declare function convertMesh3DFeatures(features: readonly Feature[], symbol: Mesh3DSymbol): Mesh3DVertexData | null;\n//# sourceMappingURL=Mesh3DConverter.d.ts.map","engine/RenderLoop.d.ts":"/**\n * RenderLoop — requestAnimationFrame tabanlı render döngüsü\n *\n * Dirty checking: sadece değişiklik varsa render et.\n * Frame timing (fps tracking).\n * IRenderEngine mock ile çalışacak şekilde tasarlandı (gerçek WebGPU yok).\n * beginFrame/endFrame cycle.\n */\nimport type { IRenderEngine, CameraState } from '../interfaces/index';\nexport interface FrameCallback {\n    (deltaMs: number, frameNumber: number): void;\n}\nexport interface RenderLoopOptions {\n    /** Target FPS cap (0 = unlimited, default: 60) */\n    targetFps?: number;\n}\nexport interface FrameStats {\n    /** Current FPS (smoothed) */\n    fps: number;\n    /** Last frame duration in ms */\n    frameDurationMs: number;\n    /** Total frames rendered */\n    totalFrames: number;\n    /** Total frames skipped due to clean state */\n    skippedFrames: number;\n}\nexport declare class RenderLoop {\n    private _running;\n    private _dirty;\n    private _rafId;\n    private _firstFrame;\n    private _lastTimestamp;\n    private _frameNumber;\n    private _skippedFrames;\n    private _fps;\n    private _frameDurationMs;\n    private _targetFps;\n    private _minFrameInterval;\n    private _fpsAccumulator;\n    private _fpsFrameCount;\n    private _fpsLastUpdate;\n    private _renderEngine;\n    private _cameraStateProvider;\n    private _frameCallbacks;\n    private _preFrameCallbacks;\n    private _requestAnimationFrame;\n    private _cancelAnimationFrame;\n    constructor(options?: RenderLoopOptions, raf?: (cb: FrameRequestCallback) => number, caf?: (id: number) => void);\n    setRenderEngine(engine: IRenderEngine): void;\n    setCameraStateProvider(provider: () => CameraState): void;\n    onFrame(callback: FrameCallback): void;\n    offFrame(callback: FrameCallback): void;\n    /** Register a pre-frame callback (runs before beginFrame, for tile selection / near-far). */\n    onPreFrame(callback: FrameCallback): void;\n    offPreFrame(callback: FrameCallback): void;\n    markDirty(): void;\n    get isDirty(): boolean;\n    get running(): boolean;\n    start(): void;\n    stop(): void;\n    getStats(): FrameStats;\n    destroy(): void;\n    private _scheduleFrame;\n    private _tick;\n}\n//# sourceMappingURL=RenderLoop.d.ts.map","engine/ResourceManager.d.ts":"/**\n * ResourceManager — GPU ve CPU kaynakları lifecycle tracking\n *\n * Buffer ve texture register/release.\n * Memory usage tracking.\n * LRU eviction: max memory limiti aşıldığında en eski kaynağı sil.\n * Dispose pattern: destroy() ile tüm kaynakları temizle.\n */\nexport interface ResourceDescriptor {\n    /** Opaque resource data (e.g., GPUBuffer, GPUTexture reference) */\n    data: unknown;\n    /** Size of the resource in bytes */\n    byteSize: number;\n}\nexport interface MemoryUsage {\n    /** Total bytes tracked across all resources */\n    totalBytes: number;\n    /** Total buffer bytes */\n    bufferBytes: number;\n    /** Total texture bytes */\n    textureBytes: number;\n    /** Number of tracked buffers */\n    bufferCount: number;\n    /** Number of tracked textures */\n    textureCount: number;\n}\nexport interface ResourceManagerOptions {\n    /** Maximum memory in bytes before LRU eviction kicks in. Default: 512MB */\n    maxMemoryBytes?: number;\n    /** Custom time provider for testing */\n    now?: () => number;\n}\nexport declare class ResourceManager {\n    private _buffers;\n    private _textures;\n    private _maxMemoryBytes;\n    private _now;\n    private _totalBytes;\n    private _destroyed;\n    /** Callback invoked when a resource is evicted. For testing/monitoring. */\n    onEvict: ((id: string, type: 'buffer' | 'texture') => void) | null;\n    constructor(options?: ResourceManagerOptions);\n    registerBuffer(id: string, descriptor: ResourceDescriptor): void;\n    releaseBuffer(id: string): boolean;\n    getBuffer(id: string): ResourceDescriptor | undefined;\n    registerTexture(id: string, descriptor: ResourceDescriptor): void;\n    releaseTexture(id: string): boolean;\n    getTexture(id: string): ResourceDescriptor | undefined;\n    getMemoryUsage(): MemoryUsage;\n    destroy(): void;\n    get isDestroyed(): boolean;\n    private _checkDestroyed;\n    /**\n     * Evict oldest (LRU) resources until memory is under the limit.\n     */\n    private _evictIfNeeded;\n    /**\n     * Find the resource with the oldest lastAccess timestamp across all types.\n     */\n    private _findOldestResource;\n}\n//# sourceMappingURL=ResourceManager.d.ts.map","engine/SceneGraph.d.ts":"/**\n * SceneGraph — Hiyerarsik sahne yapisi\n *\n * Scene graph modeli (docs/01-ARCHITECTURE.md §10):\n * SceneRoot → LayerGroup → LayerNode → BatchNode\n *\n * Her node: id, type, visible, children, parent, transform.\n * Dirty tracking: node degistiginde parent'a kadar dirty isaretlenir.\n * DFS traversal ve gorunur node toplama.\n */\nexport type SceneNodeType = 'root' | 'terrain' | 'layer-group' | 'layer' | 'batch' | 'annotation-group' | 'effect-group' | 'custom';\nexport interface SceneNodeTransform {\n    /** Translation [x, y, z] */\n    position: [number, number, number];\n    /** Scale [sx, sy, sz] */\n    scale: [number, number, number];\n    /** Rotation quaternion [x, y, z, w] */\n    rotation: [number, number, number, number];\n}\nexport interface SceneNodeOptions {\n    id: string;\n    type: SceneNodeType;\n    visible?: boolean;\n    transform?: Partial<SceneNodeTransform>;\n    /** Arbitrary metadata attached to the node */\n    data?: Record<string, unknown>;\n}\n/** Callback for traverse operations */\nexport type TraverseCallback = (node: SceneNode) => boolean | void;\nexport declare class SceneNode {\n    readonly id: string;\n    readonly type: SceneNodeType;\n    private _visible;\n    private _dirty;\n    private _transform;\n    private _parent;\n    private _children;\n    private _data;\n    constructor(options: SceneNodeOptions);\n    get visible(): boolean;\n    get dirty(): boolean;\n    get transform(): Readonly<SceneNodeTransform>;\n    get parent(): SceneNode | null;\n    get children(): readonly SceneNode[];\n    get data(): Readonly<Record<string, unknown>>;\n    /** Whether this node and all ancestors are visible */\n    get effectiveVisible(): boolean;\n    setVisible(visible: boolean): void;\n    setTransform(transform: Partial<SceneNodeTransform>): void;\n    setData(key: string, value: unknown): void;\n    /** @internal */\n    _setParent(parent: SceneNode | null): void;\n    /** @internal */\n    _addChild(child: SceneNode): void;\n    /** @internal */\n    _removeChild(childId: string): SceneNode | undefined;\n    clearDirty(): void;\n    private _markDirty;\n    /** @internal — called by child nodes */\n    _markDirtyFromChild(): void;\n}\nexport declare class SceneGraph {\n    private _root;\n    private _nodeMap;\n    constructor();\n    /** The root node of the scene graph */\n    get root(): SceneNode;\n    /** Total number of nodes (including root) */\n    get nodeCount(): number;\n    /**\n     * Add a node as a child of the given parent.\n     * If parentId is null, adds to root.\n     * Returns the added node, or null if parent not found or duplicate id.\n     */\n    addNode(parentId: string | null, options: SceneNodeOptions): SceneNode | null;\n    /**\n     * Remove a node and all its descendants from the graph.\n     * Cannot remove root.\n     * Returns true if the node was found and removed.\n     */\n    removeNode(id: string): boolean;\n    /**\n     * Find a node by its id.\n     */\n    findNode(id: string): SceneNode | undefined;\n    /**\n     * Depth-first traversal of the scene graph.\n     * Callback receives each node. Return false from callback to skip children.\n     */\n    traverse(callback: TraverseCallback, startNode?: SceneNode): void;\n    /**\n     * Collect all visible nodes (nodes where effectiveVisible is true).\n     * Does not include the root node.\n     */\n    getVisibleNodes(): SceneNode[];\n    /**\n     * Collect all dirty nodes.\n     */\n    getDirtyNodes(): SceneNode[];\n    /**\n     * Clear dirty flags on all nodes.\n     */\n    clearAllDirty(): void;\n    destroy(): void;\n    private _dfs;\n    private _removeDescendants;\n}\n//# sourceMappingURL=SceneGraph.d.ts.map","engine/SceneView3D.d.ts":"/**\n * SceneView3D — 3D globe view\n *\n * MapView2D'nin 3D karsiligi.\n * CameraController3D ile perspective kamera.\n * Ray-sphere intersection ile screen↔world donusumleri.\n * Ayni GameMap sinifi ve layer paylasimi.\n * Typed event system (MapView2D pattern'ini takip eder).\n *\n * Canvas + WebGPU auto-init — MapView2D ile ayni akis:\n *   container → canvas → engine.init(canvas) → beginFrame/endFrame → render loop.\n */\nimport type { ILayer, IRenderEngine } from '../interfaces/index';\nimport type { MapError } from '../errors';\nimport { GameMap } from './Map';\nimport { CameraController3D } from './CameraController3D';\nimport type { CameraPosition3D } from './CameraController3D';\nimport { LayerManager } from './LayerManager';\nimport { RenderLoop } from './RenderLoop';\nimport type { InteractionHandler3DOptions } from './InteractionHandler3D';\nimport type { ImageryProvider } from './globe/ImageryProvider';\nexport interface SceneView3DEvents {\n    [key: string]: unknown;\n    /** Camera state changed */\n    'camera-change': {\n        position: CameraPosition3D;\n        heading: number;\n        tilt: number;\n        roll: number;\n    };\n    /** View is ready */\n    'ready': void;\n    /** Error occurred */\n    'error': MapError;\n    /** Layer added to map */\n    'layer-add': {\n        layer: ILayer;\n    };\n    /** Layer removed from map */\n    'layer-remove': {\n        layer: ILayer;\n    };\n    /** A frame was rendered */\n    'frame': {\n        frameNumber: number;\n        fps: number;\n    };\n    /** View was destroyed */\n    'destroy': void;\n}\nexport interface GoTo3DTarget {\n    /** Target position [longitude, latitude, altitude] */\n    position?: [number, number, number];\n    /** Heading in degrees */\n    heading?: number;\n    /** Tilt in degrees */\n    tilt?: number;\n    /** Roll in degrees */\n    roll?: number;\n    /** Animation duration in ms (0 = instant, default: 500) */\n    duration?: number;\n}\nexport interface SceneView3DOptions {\n    /**\n     * DOM container element or selector string.\n     * When running without DOM (e.g. tests), pass null.\n     */\n    container: HTMLElement | string | null;\n    /** Initial camera configuration */\n    camera?: {\n        /** Position [longitude, latitude, altitude] */\n        position?: [number, number, number];\n        /** Heading in degrees */\n        heading?: number;\n        /** Tilt in degrees */\n        tilt?: number;\n        /** Roll in degrees */\n        roll?: number;\n        /** FOV in degrees */\n        fov?: number;\n    };\n    /** Terrain configuration */\n    terrain?: {\n        enabled: boolean;\n    };\n    /** Optional render engine (for mock/test injection) */\n    renderEngine?: IRenderEngine;\n    /** Interaction options (false to disable, object to configure) */\n    interaction?: boolean | InteractionHandler3DOptions;\n    /** Base globe imagery provider (e.g. UrlTemplateImageryProvider for OSM) */\n    imagery?: ImageryProvider;\n}\nexport declare class SceneView3D {\n    readonly map: GameMap;\n    private _events;\n    private _camera;\n    private _layerManager;\n    private _renderLoop;\n    private _renderEngine;\n    private _container;\n    private _canvas;\n    private _ready;\n    private _gpuReady;\n    private _destroyed;\n    private _readyResolve;\n    private _readyPromise;\n    private _terrainEnabled;\n    private _resizeObserver;\n    private _interaction;\n    private _globeMesh;\n    private _globeTileManager;\n    private _vectorBuffers;\n    private _cachedTileSelections;\n    private _animating;\n    private _animationId;\n    constructor(options: SceneView3DOptions);\n    /** CameraController3D instance */\n    get camera(): CameraController3D;\n    /** Whether terrain is enabled */\n    get terrainEnabled(): boolean;\n    /** Whether the view is ready */\n    get ready(): boolean;\n    /** The canvas element (null when running without DOM) */\n    get canvas(): HTMLCanvasElement | null;\n    /** Layer manager instance */\n    get layerManager(): LayerManager;\n    /** Render loop instance */\n    get renderLoop(): RenderLoop;\n    /**\n     * Navigate to a target with optional animation.\n     * Animates camera position, heading, tilt, and roll.\n     */\n    goTo(target: GoTo3DTarget): Promise<void>;\n    /**\n     * Convert screen coordinates to map coordinates [longitude, latitude]\n     * using ray-sphere intersection against the Earth sphere.\n     * Returns null if the ray misses the Earth.\n     */\n    toMap(screenX: number, screenY: number): [number, number] | null;\n    /**\n     * Convert map coordinates [longitude, latitude, altitude] to screen coordinates.\n     * Returns null if the point is behind the camera.\n     */\n    toScreen(lon: number, lat: number, alt?: number): [number, number] | null;\n    on<K extends keyof SceneView3DEvents>(event: K, handler: (data: SceneView3DEvents[K]) => void): void;\n    off<K extends keyof SceneView3DEvents>(event: K, handler: (data: SceneView3DEvents[K]) => void): void;\n    /**\n     * Returns a promise that resolves when the view is ready.\n     */\n    when(): Promise<void>;\n    destroy(): void;\n    private _createCanvas;\n    private _setupResizeObserver;\n    private _initGpu;\n    /**\n     * Phase 3: Two-pass rendering.\n     * 1. Select visible tiles (quadtree traversal with frustum + horizon culling)\n     * 2. Compute tight near/far from visible tile bounding volumes\n     * 3. Override camera near/far → projection matrix updates before beginFrame\n     */\n    private _diagFrame;\n    private _preFrame;\n    private _renderFrame;\n    /**\n     * Render a single vector layer on the globe. Builds ECEF GPU buffers on first call\n     * (or when dirty), then reuses cached buffers on subsequent frames.\n     */\n    private _renderVectorLayer;\n    /**\n     * Build ECEF GPU buffers for a vector layer's features.\n     */\n    private _buildVectorBuffers;\n    /**\n     * Release GPU buffers for a vector layer.\n     */\n    private _releaseVectorBuffers;\n    /**\n     * Generate a UV sphere mesh in ECEF coordinates and upload to GPU.\n     * Vertex layout: interleaved position(vec3) + normal(vec3) = 24 bytes/vertex.\n     */\n    private _createGlobeMesh;\n    private _cancelAnimation;\n    private _emitCameraChange;\n}\n//# sourceMappingURL=SceneView3D.d.ts.map","engine/TerrainCameraController.d.ts":"/**\n * TerrainCameraController — 3D perspective camera for terrain viewing\n *\n * EPSG:3857 (Web Mercator) coordinate space, meters.\n * Provides perspective projection with pitch/bearing for 3D terrain visualization.\n *\n * Matrix chain (right-to-left application):\n *   Perspective(fov, aspect, near, far)\n *     × Translate(0, 0, -cameraDist)\n *     × RotateX(-pitch)\n *     × RotateZ(bearing)\n *     × Translate(-cx, -cy, 0)\n *\n * where:\n *   cameraDist = pixelDist × metersPerPixel\n *   pixelDist  = 0.5 × viewportHeight / tan(fov/2)\n *   metersPerPixel = 40075016.686 / (256 × 2^zoom)\n *   cx, cy = center in EPSG:3857 meters\n *\n * Coordinate system: Y-up, right-handed.\n */\nimport type { CameraState } from '../interfaces/index';\nexport interface TerrainCameraOptions {\n    /** Center in EPSG:3857 [x, y] meters */\n    center?: [number, number];\n    /** Zoom level (0–22) */\n    zoom?: number;\n    /** Pitch in degrees (0–85) */\n    pitch?: number;\n    /** Bearing in degrees (0–360) */\n    bearing?: number;\n    /** Field of view in degrees (default 60) */\n    fov?: number;\n    /** Viewport width in pixels */\n    viewportWidth?: number;\n    /** Viewport height in pixels */\n    viewportHeight?: number;\n}\nexport declare class TerrainCameraController {\n    private _center;\n    private _zoom;\n    private _pitch;\n    private _bearing;\n    private _fov;\n    private _viewportWidth;\n    private _viewportHeight;\n    private _viewMatrix;\n    private _projectionMatrix;\n    private _viewProjectionMatrix;\n    private _cameraPosition;\n    private _dirty;\n    constructor(options?: Partial<TerrainCameraOptions>);\n    get center(): [number, number];\n    get zoom(): number;\n    get pitch(): number;\n    get bearing(): number;\n    get fov(): number;\n    get viewportWidth(): number;\n    get viewportHeight(): number;\n    get viewMatrix(): Float32Array;\n    get projectionMatrix(): Float32Array;\n    get viewProjectionMatrix(): Float32Array;\n    get cameraPosition(): [number, number, number];\n    /**\n     * Meters per pixel at the current zoom level.\n     * At zoom 0 the entire world (40075016.686 m) fits into 256 pixels.\n     */\n    get metersPerPixel(): number;\n    /**\n     * Camera distance from the terrain surface in EPSG:3857 meters.\n     * Computed so that the viewport coverage matches the standard Web Mercator tile scheme.\n     */\n    get cameraDist(): number;\n    setCenter(x: number, y: number): void;\n    setZoom(zoom: number): void;\n    setPitch(pitch: number): void;\n    setBearing(bearing: number): void;\n    setFov(fov: number): void;\n    setViewport(width: number, height: number): void;\n    /**\n     * Returns a CameraState compatible with IRenderEngine.beginFrame().\n     */\n    getCameraState(): CameraState;\n    private _ensureClean;\n    private _clampValues;\n    private _updateMatrices;\n    /**\n     * Camera position in EPSG:3857 space.\n     * Back-trace through inverse view transforms starting from eye-space origin.\n     *\n     * Inverse chain (applied to [0, 0, 0]):\n     *   Translate(cx, cy, 0) × RotateZ(-bearing) × RotateX(pitch) × Translate(0, 0, dist) × [0,0,0]\n     */\n    private _computeCameraPosition;\n}\n//# sourceMappingURL=TerrainCameraController.d.ts.map","engine/TerrainInteraction.d.ts":"/**\n * TerrainInteraction — 3D terrain orbit-style mouse/touch interaction\n *\n * GlobeInteraction pattern'ini takip eder:\n * - Left drag: pan (EPSG:3857 center shift)\n * - Right drag: pitch + bearing change\n * - Scroll: zoom\n * - Keyboard: arrow keys (pan), +/- (zoom)\n *\n * TerrainCameraController'ı doğrudan kontrol eder.\n */\nimport type { TerrainCameraController } from './TerrainCameraController';\nexport interface TerrainInteractionOptions {\n    pan?: boolean;\n    zoom?: boolean;\n    keyboard?: boolean;\n    doubleClickZoom?: boolean;\n    pitchBearing?: boolean;\n    zoomSpeed?: number;\n}\nexport declare class TerrainInteraction {\n    private readonly _element;\n    private readonly _camera;\n    private readonly _onDirty;\n    private readonly _onViewChange;\n    private readonly _panEnabled;\n    private readonly _zoomEnabled;\n    private readonly _keyboardEnabled;\n    private readonly _doubleClickZoom;\n    private readonly _pitchBearingEnabled;\n    private readonly _zoomSpeed;\n    private _dragging;\n    private _lastPointerX;\n    private _lastPointerY;\n    private _activePointerId;\n    private _dragButton;\n    private _pointers;\n    private _lastPinchDist;\n    private _lastClickTime;\n    private readonly _onPointerDown;\n    private readonly _onPointerMove;\n    private readonly _onPointerUp;\n    private readonly _onWheel;\n    private readonly _onKeyDown;\n    private readonly _onContextMenu;\n    private _destroyed;\n    constructor(element: HTMLElement, camera: TerrainCameraController, onDirty: () => void, onViewChange: () => void, options?: TerrainInteractionOptions);\n    private _handlePointerDown;\n    private _handlePointerMove;\n    private _handlePointerUp;\n    private _handlePan;\n    private _handleWheel;\n    private _handleDoubleClick;\n    private _handleKeyDown;\n    private _getPinchDistance;\n    destroy(): void;\n}\n//# sourceMappingURL=TerrainInteraction.d.ts.map","engine/TerrainTileManager.d.ts":"/**\n * TerrainTileManager\n *\n * Caches terrain height/hillshade GPU textures per z/x/y, deduplicates\n * in-flight requests, and provides parent fallback for terrain tiles\n * when exact child tiles are not ready yet.\n */\nimport type { IRenderEngine, ITerrainLayer } from '../interfaces/index';\nexport interface TerrainTileCoord {\n    z: number;\n    x: number;\n    y: number;\n}\nexport interface TerrainReadyHeightTile {\n    texture: GPUTexture;\n    sourceCoord: TerrainTileCoord;\n    /** Tile UV remap into source texture UV: [offsetX, offsetY, scaleX, scaleY] */\n    uvOffsetScale: [number, number, number, number];\n}\nexport interface TerrainReadyHillshadeTile {\n    texture: GPUTexture;\n    sourceCoord: TerrainTileCoord;\n}\nexport interface TerrainTileManagerOptions {\n    maxHeightCacheEntries?: number;\n    maxHillshadeCacheEntries?: number;\n    maxConcurrent?: number;\n}\nexport declare class TerrainTileManager {\n    private readonly _maxHeightCacheEntries;\n    private readonly _maxHillshadeCacheEntries;\n    private readonly _maxConcurrent;\n    private readonly _heightCache;\n    private readonly _hillshadeCache;\n    private readonly _inFlight;\n    private _renderEngine;\n    private _destroyed;\n    private _activeLayerId;\n    onDirty: (() => void) | null;\n    constructor(options?: TerrainTileManagerOptions);\n    setRenderEngine(engine: IRenderEngine): void;\n    setActiveLayer(layerId: string | null): void;\n    requestTiles(layer: ITerrainLayer, coords: ReadonlyArray<TerrainTileCoord>): void;\n    getReadyHeightTile(layer: ITerrainLayer, targetZ: number, targetX: number, targetY: number): TerrainReadyHeightTile | null;\n    getReadyHillshadeTile(layer: ITerrainLayer, targetZ: number, targetX: number, targetY: number): TerrainReadyHillshadeTile | null;\n    invalidateLayer(layerId: string): void;\n    invalidateAll(): void;\n    destroy(): void;\n    get heightCacheSize(): number;\n    get hillshadeCacheSize(): number;\n    get inFlightCount(): number;\n    private _startRequest;\n    private _normalizeRequestCoord;\n    private _cacheKey;\n    private _layerIdFromKey;\n    private _materializeHeightIfReady;\n    private _materializeHillshadeIfReady;\n    private _getOrMaterializeHeight;\n    private _getOrMaterializeHillshade;\n    private _createHeightEntry;\n    private _createHillshadeEntry;\n    private _expandGrayToRgba;\n    private _computeUvOffsetScale;\n    private _evictIfNeeded;\n    private _pruneCachesForActiveLayer;\n    private _invalidateCachesForLayer;\n    private _releaseCache;\n}\n//# sourceMappingURL=TerrainTileManager.d.ts.map","engine/TerrainView.d.ts":"/**\n * TerrainView — 3D Terrain public API\n *\n * MapView2D ve GlobeView ile aynı lifecycle pattern:\n * - constructor: canvas oluştur, GPU init et, RenderLoop başlat\n * - goTo(): animasyonlu navigasyon\n * - toMap/toScreen: koordinat dönüşümü\n * - on/off/when: typed event system\n * - destroy: cleanup\n *\n * Farklar:\n * - TerrainCameraController (perspective kamera, pitch/bearing)\n * - drawTerrain3D() ile vertex displacement rendering\n * - exaggeration / sunDirection kontrolleri\n * - TerrainInteraction (orbit-style etkileşim)\n */\nimport type { ILayer, IRenderEngine } from '../interfaces/index';\nimport type { MapError } from '../errors';\nimport { GameMap } from './Map';\nimport type { TerrainInteractionOptions } from './TerrainInteraction';\nexport interface TerrainViewEvents {\n    [key: string]: unknown;\n    'view-change': {\n        center: [number, number];\n        zoom: number;\n        pitch: number;\n        bearing: number;\n    };\n    'ready': void;\n    'error': MapError;\n    'layer-add': {\n        layer: ILayer;\n    };\n    'layer-remove': {\n        layer: ILayer;\n    };\n    'frame': {\n        frameNumber: number;\n        fps: number;\n    };\n    'destroy': void;\n}\nexport interface TerrainGoToTarget {\n    center?: [number, number];\n    zoom?: number;\n    pitch?: number;\n    bearing?: number;\n    duration?: number;\n}\nexport interface TerrainViewOptions {\n    container: HTMLElement | string | null;\n    center?: [number, number];\n    zoom?: number;\n    pitch?: number;\n    bearing?: number;\n    exaggeration?: number;\n    sunDirection?: [number, number, number];\n    renderEngine?: IRenderEngine;\n    interaction?: TerrainInteractionOptions | false;\n}\nexport declare class TerrainView {\n    readonly map: GameMap;\n    private _events;\n    private _camera;\n    private _layerManager;\n    private _renderLoop;\n    private _tileScheduler;\n    private _tileManager;\n    private _renderEngine;\n    private _container;\n    private _canvas;\n    private _interaction;\n    private _resizeObserver;\n    private _ready;\n    private _gpuReady;\n    private _destroyed;\n    private _readyResolve;\n    private _readyPromise;\n    private _exaggeration;\n    private _sunDirection;\n    private _bufferCache;\n    private _animating;\n    private _animationId;\n    constructor(options: TerrainViewOptions);\n    get center(): [number, number];\n    get zoom(): number;\n    get pitch(): number;\n    get bearing(): number;\n    get ready(): boolean;\n    get gpuReady(): boolean;\n    get canvas(): HTMLCanvasElement | null;\n    get exaggeration(): number;\n    set exaggeration(value: number);\n    get sunDirection(): [number, number, number];\n    set sunDirection(value: [number, number, number]);\n    goTo(target: TerrainGoToTarget): Promise<void>;\n    toMap(screenX: number, screenY: number): [number, number];\n    toScreen(lon: number, lat: number): [number, number];\n    on<K extends keyof TerrainViewEvents>(event: K, handler: (data: TerrainViewEvents[K]) => void): void;\n    off<K extends keyof TerrainViewEvents>(event: K, handler: (data: TerrainViewEvents[K]) => void): void;\n    when(): Promise<void>;\n    destroy(): void;\n    private _createCanvas;\n    private _setupResizeObserver;\n    private _initGpu;\n    /**\n     * Render one frame: iterate visible layers, draw raster tiles + vector geometry + 3D terrain.\n     * Called inside the RenderLoop frame callback between beginFrame() and endFrame().\n     */\n    private _renderFrame;\n    /**\n     * Render a DTED terrain layer in 3D.\n     */\n    private _renderTerrainLayer;\n    /**\n     * Render a single vector layer. Builds GPU buffers on first call (or when dirty),\n     * then reuses cached buffers on subsequent frames.\n     */\n    private _renderVectorLayer;\n    private _cancelAnimation;\n    private _emitViewChange;\n}\n//# sourceMappingURL=TerrainView.d.ts.map","engine/TileManager.d.ts":"/**\n * TileManager — Tile fetch, cache ve texture lifecycle yönetimi\n *\n * Görünür tile'ları belirler, asenkron olarak fetch eder, LRU cache'te tutar\n * ve render engine üzerinden GPU texture'larını oluşturur/yok eder.\n *\n * Cache key formatı: \"sourceIdx/z/x/y\"\n */\nimport type { Extent, IRenderEngine, ImageryTile } from '../interfaces/index';\nimport type { TileScheduler } from './TileScheduler';\n/**\n * A function that fetches a tile image from a URL and returns an ImageBitmap.\n */\nexport type TileFetcher = (url: string) => Promise<ImageBitmap>;\n/**\n * Describes a tile source with URL generation, opacity, and zoom constraints.\n */\nexport interface TileSourceInfo {\n    /** Generate a tile URL for the given coordinates */\n    getTileUrl: (z: number, x: number, y: number) => string;\n    /** Layer opacity (0-1) */\n    opacity: number;\n    /** Minimum zoom level for this source */\n    minZoom: number;\n    /** Maximum zoom level for this source */\n    maxZoom: number;\n    /** Post-process color filters (read each frame, not cached). */\n    filters?: {\n        brightness?: number;\n        contrast?: number;\n        saturate?: number;\n    };\n}\n/**\n * Options for constructing a TileManager.\n */\nexport interface TileManagerOptions {\n    /** TileScheduler instance for coordinate math */\n    tileScheduler: TileScheduler;\n    /** Maximum number of cached tile entries (default: 256) */\n    maxCacheEntries?: number;\n    /** Maximum concurrent in-flight fetch requests (default: 6) */\n    maxConcurrent?: number;\n    /** Injectable tile fetcher for testing (default: fetch + createImageBitmap) */\n    fetcher?: TileFetcher;\n}\nexport declare class TileManager {\n    private readonly _tileScheduler;\n    private readonly _maxCacheEntries;\n    private readonly _maxConcurrent;\n    private readonly _fetcher;\n    /** GPU texture cache: key → CacheEntry */\n    private readonly _cache;\n    /** Şu anda devam eden fetch'ler (key → Promise) */\n    private readonly _inFlight;\n    /** Render engine referansı (texture oluşturmak için) */\n    private _renderEngine;\n    /** Yeni tile'lar hazır olduğunda çağrılacak callback */\n    onDirty: (() => void) | null;\n    /** Destroy edildi mi? */\n    private _destroyed;\n    constructor(options: TileManagerOptions);\n    /**\n     * Set the render engine used for GPU texture creation.\n     */\n    setRenderEngine(engine: IRenderEngine): void;\n    /**\n     * Get tiles that are ready (cached) for the given extent, zoom, and sources.\n     *\n     * Called each frame. For tiles not yet cached, background fetches are started\n     * (respecting the concurrency limit). Only already-cached tiles are returned.\n     *\n     * @param extent - Visible extent in EPSG:3857\n     * @param zoom   - Current integer zoom level\n     * @param sources - Tile source descriptors\n     * @returns Array of ImageryTile objects ready for rendering\n     */\n    getReadyTiles(extent: Extent, zoom: number, sources: TileSourceInfo[]): ImageryTile[];\n    /**\n     * Get tiles that are ready for a pre-computed list of tile coordinates.\n     *\n     * Used by GlobeView where tile selection is done by GlobeTileCovering\n     * rather than by TileScheduler.getTilesForExtent().\n     *\n     * Parent tile fallback: when a tile isn't cached yet, walks up the zoom tree\n     * to find a cached ancestor tile. This prevents black flashes during zoom changes.\n     */\n    getReadyTilesForCoords(coords: ReadonlyArray<{\n        z: number;\n        x: number;\n        y: number;\n    }>, sources: TileSourceInfo[]): ImageryTile[];\n    /**\n     * Drop all cached tiles and cancel in-flight requests.\n     * Useful when render resources are recreated (e.g. depth/pipeline reset).\n     */\n    invalidateAll(): void;\n    /**\n     * Release all cached textures and cancel pending fetches.\n     */\n    destroy(): void;\n    /** Number of entries currently in the cache */\n    get cacheSize(): number;\n    /** Number of currently in-flight fetch requests */\n    get inFlightCount(): number;\n    /**\n     * Belirtilen tile için arka planda fetch başlat.\n     * Eşzamanlılık limiti ve tekrarlı fetch'ler kontrol edilir.\n     */\n    private _startFetch;\n    /**\n     * Tek bir tile'ı fetch et, texture oluştur ve cache'e ekle.\n     */\n    private _fetchAndCache;\n    /**\n     * Cache boyutu maxCacheEntries'i aşıyorsa en az kullanılan girdileri sil.\n     */\n    private _evictIfNeeded;\n}\n//# sourceMappingURL=TileManager.d.ts.map","engine/TileScheduler.d.ts":"/**\n * TileScheduler — Tile bazlı katmanlar için tile hesaplama\n *\n * Slippy Map / XYZ tile coordinate system.\n * Render coordinate space: EPSG:3857 (Web Mercator).\n *\n * Tile BBOX (3857):\n *   tileSize = 20037508.342789244 * 2 / 2^z\n *   minX = x * tileSize - 20037508.342789244\n *   maxY = 20037508.342789244 - y * tileSize\n *   maxX = minX + tileSize\n *   minY = maxY - tileSize\n */\nimport type { Extent } from '../interfaces/index';\nexport interface TileCoord {\n    z: number;\n    x: number;\n    y: number;\n}\nexport interface TileWithPriority extends TileCoord {\n    /** Lower = higher priority (closer to center) */\n    priority: number;\n}\nexport interface TileSchedulerOptions {\n    /** Maximum concurrent tile requests (default: 6) */\n    maxConcurrent?: number;\n    /** Tile pixel size (default: 256) */\n    tileSize?: number;\n}\nexport declare class TileScheduler {\n    private _maxConcurrent;\n    private _tileSize;\n    constructor(options?: TileSchedulerOptions);\n    get maxConcurrent(): number;\n    get tileSize(): number;\n    /**\n     * Get visible tiles for the given extent and zoom level.\n     * Returns tiles sorted by priority (center-first).\n     *\n     * @param extent Visible extent in EPSG:3857\n     * @param zoom   Integer zoom level\n     */\n    getTilesForExtent(extent: Extent, zoom: number): TileWithPriority[];\n    /**\n     * Convert tile coordinate to EPSG:3857 extent.\n     */\n    tileToExtent(z: number, x: number, y: number): Extent;\n    /**\n     * Convert lon/lat (EPSG:4326) to tile coordinates at the given zoom.\n     */\n    lonLatToTile(lon: number, lat: number, zoom: number): TileCoord;\n    /**\n     * Clip a list of tiles to the max concurrent limit.\n     * Tiles should already be sorted by priority.\n     */\n    clipToConcurrentLimit(tiles: TileWithPriority[]): TileWithPriority[];\n}\n//# sourceMappingURL=TileScheduler.d.ts.map","engine/VectorBufferCache.d.ts":"/**\n * VectorBufferCache — Shared GPU buffer management for vector layers\n *\n * Builds per-symbol-group GPU buffers so that each unique symbol from a\n * renderer results in a separate draw call. This enables per-feature\n * styling via UniqueValueRenderer / ClassBreaksRenderer.\n *\n * When no renderer is set (or SimpleRenderer), a fast path produces a\n * single group per geometry type — identical to the old behavior.\n */\nimport type { IRenderEngine, IRenderer, Feature, VectorTileBinaryPayload, PointRenderBuffer, LineRenderBuffer, PolygonRenderBuffer, ModelRenderBuffer, ExtrusionRenderBuffer, Mesh3DRenderBuffer, PointSymbol, LineSymbol, PolygonSymbol, ModelSymbol, ExtrudedPolygonSymbol, Mesh3DSymbol } from '../interfaces/index';\nexport interface SymbolGroup<B, S> {\n    buffer: B;\n    symbol: S;\n}\nexport interface VectorBufferEntry {\n    pointGroups: SymbolGroup<PointRenderBuffer, PointSymbol>[];\n    lineGroups: SymbolGroup<LineRenderBuffer, LineSymbol>[];\n    polygonGroups: SymbolGroup<PolygonRenderBuffer, PolygonSymbol>[];\n    modelGroups: SymbolGroup<ModelRenderBuffer, ModelSymbol>[];\n    extrusionGroups: SymbolGroup<ExtrusionRenderBuffer, ExtrudedPolygonSymbol>[];\n    mesh3dGroups: SymbolGroup<Mesh3DRenderBuffer, Mesh3DSymbol>[];\n}\nexport interface VectorTileRenderEntry extends VectorBufferEntry {\n}\nexport interface VectorTileRenderKey {\n    layerId: string;\n    tileKey: string;\n    rendererKey: string;\n    zoomBucket: number;\n    mode: '2d' | '3d';\n    source: 'feature' | 'binary';\n    version: number;\n}\nexport interface VectorTileBuildOptions {\n    layerId: string;\n    tileKey: string;\n    version: number;\n    renderer?: IRenderer;\n    zoom?: number;\n    globe: boolean;\n}\ntype RenderFeature = Feature & {\n    geometry: Feature['geometry'] & {\n        spatialReference?: string;\n    };\n};\nexport declare const DEFAULT_POINT_SYMBOL: PointSymbol;\nexport declare const DEFAULT_LINE_SYMBOL: LineSymbol;\nexport declare const DEFAULT_POLYGON_SYMBOL: PolygonSymbol;\nexport declare class VectorBufferCache {\n    private _engine;\n    private _buffers;\n    private _tileBuffers;\n    private _tileSlotToRenderKey;\n    private _tileScopes;\n    /** Track renderer identity per layer for change detection. */\n    private _rendererKeys;\n    /** Track feature count per layer for change detection. */\n    private _featureCounts;\n    /** Track last zoom for zoom-sensitive renderer invalidation. */\n    private _lastZoomInt;\n    /** Layer IDs that use zoom-sensitive renderers. */\n    private _zoomSensitiveLayers;\n    /** Called when a cache entry is invalidated — used to trigger re-render. */\n    private _onInvalidate;\n    constructor(engine?: IRenderEngine | null);\n    /** Set a callback that fires when cache entries are invalidated. */\n    setOnInvalidate(cb: () => void): void;\n    /** Set or update the render engine reference (for late initialization) */\n    setRenderEngine(engine: IRenderEngine): void;\n    /**\n     * Get cached buffers for a layer, or build them from features.\n     * When a renderer is provided, features are grouped by resolved symbol\n     * so each group gets its own GPU buffer and draw call.\n     *\n     * When `zoom` is provided and the renderer has `zoomSensitive: true`,\n     * cached buffers are invalidated whenever the integer zoom level changes\n     * (since the renderer may produce different symbols at different zoom levels).\n     *\n     * Returns null if no render engine is available.\n     */\n    getOrBuild(layerId: string, features: readonly Feature[], renderer?: IRenderer, zoom?: number): VectorBufferEntry | null;\n    getOrBuildTile(options: VectorTileBuildOptions, features: readonly RenderFeature[]): VectorTileRenderEntry | null;\n    getOrBuildTileBinary(options: VectorTileBuildOptions, payload: VectorTileBinaryPayload): VectorTileRenderEntry | null;\n    /** Check if a layer has cached buffers */\n    has(layerId: string): boolean;\n    /** Invalidate (release) cached buffers for a specific layer */\n    invalidate(layerId: string): void;\n    pruneTileEntries(layerId: string, globe: boolean, visibleTileKeys: Iterable<string>): void;\n    /** Invalidate all cached buffers */\n    invalidateAll(): void;\n    /** Release all cached resources */\n    destroy(): void;\n    /** Stamp tileKey onto extrusion buffers for stable animation tracking. */\n    private _stampExtrusionIds;\n    private _buildFromBinaryPayload;\n    private _build;\n    /** Fast path: all features share one symbol per geometry type */\n    private _buildSingleGroup;\n    /** Multi-group path: group features by renderer symbol, build separate buffers */\n    private _buildMultiGroup;\n    /** Build point GPU buffer and push to entry.pointGroups */\n    private _buildPointGroup;\n    /** Build line GPU buffer and push to entry.lineGroups */\n    private _buildLineGroup;\n    /** Build polygon GPU buffer and push to entry.polygonGroups + outline to lineGroups */\n    private _buildPolygonGroup;\n    /** Build extrusion GPU buffers and push to entry.extrusionGroups */\n    private _buildExtrusionGroup;\n    /** Build mesh3D GPU buffers and push to entry.mesh3dGroups */\n    private _buildMesh3DGroup;\n    /** Build model instance buffer and push to entry.modelGroups */\n    private _buildModelGroup;\n    private _releaseEntry;\n    private _registerTileSlot;\n    private _invalidateTileEntry;\n}\nexport {};\n//# sourceMappingURL=VectorBufferCache.d.ts.map","engine/ViewCore.d.ts":"/**\n * ViewCore — Shared infrastructure for MapView's 2D/3D modes.\n *\n * Owns the resources that persist across mode switches:\n * - Canvas + container + ResizeObserver\n * - RenderEngine + RenderLoop\n * - TileManager + TileScheduler\n * - LayerManager + VectorBufferCache\n * - GameMap (layer collection)\n * - EventBus\n *\n * Each {@link IViewMode} receives a reference to ViewCore and uses these\n * shared resources during `renderFrame()` and lifecycle operations.\n *\n * ViewCore eliminates the duplicated setup logic that previously existed\n * in MapView2D, GlobeView, and TerrainView by centralizing canvas creation,\n * GPU initialization, resize observation, and resource teardown.\n *\n * @see {@link IViewMode} for the strategy interface that consumes these resources\n * @see {@link RenderFrameContext} for the per-frame resource bundle derived from ViewCore\n * @see {@link MapView} for the public API that orchestrates ViewCore and IViewMode\n */\nimport type { IRenderEngine, GpuCapabilities, DepthConfig, ResolvedGlobeEffects } from '../interfaces/index';\nimport { GameMap } from './Map';\nimport { LayerManager } from './LayerManager';\nimport { RenderLoop } from './RenderLoop';\nimport { TileScheduler } from './TileScheduler';\nimport { TileManager } from './TileManager';\nimport { TerrainTileManager } from './TerrainTileManager';\nimport { VectorBufferCache } from './VectorBufferCache';\n/**\n * Centralized owner of shared map infrastructure.\n *\n * ViewCore is instantiated once per MapView and survives mode switches.\n * It manages the full lifecycle of canvas creation, GPU initialization,\n * resize handling, and resource teardown.\n *\n * **Lifecycle stages:**\n * 1. **Construction** — instantiates GameMap, LayerManager, TileScheduler,\n *    TileManager, RenderLoop, and VectorBufferCache.\n * 2. **Canvas creation** ({@link createCanvas}) — creates and appends a\n *    `<canvas>` element to the container, sized for the device pixel ratio.\n * 3. **GPU init** ({@link initGpu}) — initializes the render engine, wires\n *    up tile-dirty callbacks, and sets the {@link gpuReady} flag.\n * 4. **Active** — modes call into the shared resources each frame.\n * 5. **Destroy** ({@link destroy}) — disconnects observers, destroys all\n *    sub-systems, and removes the canvas from the DOM.\n *\n * @see {@link IViewMode} for the mode strategy that uses these resources\n * @see {@link MapView} for the public API that owns the ViewCore instance\n */\nexport declare class ViewCore {\n    /**\n     * The layer collection exposed as the public API surface.\n     *\n     * Layers are added/removed via `gameMap.add(layer)` and `gameMap.remove(layer)`.\n     * The GameMap instance persists across mode switches.\n     *\n     * @see {@link GameMap}\n     */\n    readonly map: GameMap;\n    /**\n     * The GPU render engine (WebGPU or mock).\n     *\n     * Set to `null` until {@link initGpu} completes successfully. Once\n     * initialized, the engine is shared across all modes and sub-systems.\n     *\n     * @see {@link IRenderEngine}\n     */\n    renderEngine: IRenderEngine | null;\n    /**\n     * The render loop that drives frame callbacks.\n     *\n     * Schedules `requestAnimationFrame` ticks, calls `beginFrame()` /\n     * `endFrame()` on the render engine, and invokes registered frame\n     * callbacks in between.\n     *\n     * @see {@link RenderLoop}\n     */\n    readonly renderLoop: RenderLoop;\n    /**\n     * Tile scheduler that computes tile coverage for a given extent and zoom.\n     *\n     * Determines which tile coordinates (z/x/y) are needed to cover the\n     * current viewport. Used by both 2D and 3D modes.\n     *\n     * @see {@link TileScheduler}\n     */\n    readonly tileScheduler: TileScheduler;\n    /**\n     * Tile manager responsible for fetching, caching, and uploading tile imagery.\n     *\n     * Maintains a tile cache, handles network requests, and uploads decoded\n     * images to GPU textures via the render engine. Fires a dirty callback\n     * when new tiles become ready, triggering a repaint.\n     *\n     * @see {@link TileManager}\n     */\n    readonly tileManager: TileManager;\n    /**\n     * Terrain tile manager for height/hillshade requests and GPU caching.\n     *\n     * @see {@link TerrainTileManager}\n     */\n    readonly terrainManager: TerrainTileManager;\n    /**\n     * Layer manager that handles visibility, zoom-range filtering, and draw ordering.\n     *\n     * @see {@link LayerManager}\n     */\n    readonly layerManager: LayerManager;\n    /**\n     * GPU buffer cache for vector layer geometry and terrain textures.\n     *\n     * Caches vertex/index buffers so that vector features are not re-uploaded\n     * to the GPU every frame. Also caches terrain elevation textures.\n     *\n     * @see {@link VectorBufferCache}\n     */\n    readonly bufferCache: VectorBufferCache;\n    /**\n     * The `<canvas>` element used for WebGPU rendering.\n     *\n     * Created by {@link createCanvas} and removed from the DOM by {@link destroy}.\n     * May be `null` in headless/test mode or before canvas creation.\n     */\n    canvas: HTMLCanvasElement | null;\n    /**\n     * The DOM container element that hosts the canvas.\n     *\n     * Set by {@link createCanvas}. Cleared on {@link destroy}.\n     */\n    container: HTMLElement | null;\n    /**\n     * ResizeObserver watching the container for size changes.\n     *\n     * Created by {@link setupResizeObserver}. Disconnected and nulled on {@link destroy}.\n     */\n    resizeObserver: ResizeObserver | null;\n    /**\n     * Whether the view has been destroyed via {@link destroy}.\n     *\n     * Once `true`, all lifecycle methods become no-ops and async operations\n     * (such as GPU init) will throw.\n     */\n    destroyed: boolean;\n    /**\n     * Whether GPU initialization completed successfully.\n     *\n     * Set to `true` at the end of {@link initGpu}. Checked by MapView before\n     * attaching interaction handlers and starting the render loop.\n     */\n    gpuReady: boolean;\n    /**\n     * Resolved globe effects configuration (lighting, atmosphere, pole caps, etc.).\n     * Persists across mode switches. Updated via {@link MapView.setGlobeEffects}.\n     */\n    globeEffects: ResolvedGlobeEffects;\n    /**\n     * Create a new ViewCore instance with all sub-systems initialized.\n     *\n     * All sub-systems start in a lightweight, unconnected state. The render\n     * engine and canvas are wired up later via {@link createCanvas} and\n     * {@link initGpu}.\n     */\n    constructor();\n    /**\n     * Create a `<canvas>` element inside the given container, sized to fill it.\n     *\n     * The canvas is absolutely positioned within the container, covering its\n     * full area. The container's CSS `position` is set to `relative` if it is\n     * currently `static`, ensuring proper stacking context.\n     *\n     * The canvas backing store is sized to the container's CSS dimensions\n     * multiplied by `devicePixelRatio` for crisp rendering on HiDPI displays.\n     * Falls back to 800x600 if the container has no measurable size.\n     *\n     * This method consolidates the identical canvas creation logic that was\n     * previously duplicated in MapView2D, GlobeView, and TerrainView.\n     *\n     * @param container - The DOM element to host the canvas.\n     * @returns The created `<canvas>` element (also stored as {@link canvas}).\n     *\n     * @see {@link setupResizeObserver} to keep the canvas sized after creation\n     */\n    createCanvas(container: HTMLElement): HTMLCanvasElement;\n    /**\n     * Observe the container for resize events and keep the canvas sized correctly.\n     *\n     * Uses `ResizeObserver` (when available) to watch the container. On each\n     * resize, the canvas backing store is updated to match the new CSS\n     * dimensions times `devicePixelRatio`, and the `onResize` callback is\n     * invoked with the new CSS pixel dimensions so the active mode can update\n     * its camera/viewport.\n     *\n     * This method consolidates the identical resize logic that was previously\n     * duplicated across three view classes. The only varying part is the\n     * `onResize` callback, which each mode supplies to update its own camera.\n     *\n     * No-ops gracefully if `ResizeObserver` is not available in the environment\n     * (e.g., older test runners).\n     *\n     * @param container - The DOM container being observed for size changes.\n     * @param canvas    - The canvas element whose backing store is resized.\n     * @param onResize  - Callback invoked with the new CSS pixel dimensions\n     *                     (width, height) whenever the container changes size.\n     *                     Typically used by the active {@link IViewMode} to call\n     *                     {@link IViewMode.setViewport | setViewport()}.\n     *\n     * @see {@link createCanvas} which should be called before this method\n     * @see {@link destroy} which disconnects the observer\n     */\n    setupResizeObserver(container: HTMLElement, canvas: HTMLCanvasElement, onResize: (cssWidth: number, cssHeight: number) => void): void;\n    /**\n     * Initialize the GPU render engine and wire up dependent sub-systems.\n     *\n     * This is an async operation that:\n     * 1. Stores the render engine reference and passes it to the buffer cache,\n     *    tile manager, and render loop.\n     * 2. Calls `engine.init(canvas)` to request a GPU adapter/device and\n     *    configure the swap chain.\n     * 3. Sets {@link gpuReady} to `true` on success.\n     * 4. Wires the tile manager's dirty callback to the render loop so that\n     *    newly loaded tiles automatically trigger a repaint.\n     * 5. Logs a warning if the GPU is running in a degraded capability mode\n     *    (e.g., software fallback).\n     *\n     * If the view is {@link destroy | destroyed} while GPU init is in progress,\n     * this method throws an error to prevent further setup.\n     *\n     * @param engine - The render engine implementation (typically WebGPURenderEngine).\n     * @param canvas - The canvas element to bind the GPU context to.\n     * @returns The detected GPU capabilities (feature tier, max texture size, etc.).\n     * @throws Error if the view is destroyed during initialization.\n     *\n     * @see {@link GpuCapabilities} for the returned capability descriptor\n     * @see {@link IRenderEngine.init} for the underlying engine initialization\n     * @see {@link createCanvas} which should be called before this method\n     */\n    initGpu(engine: IRenderEngine, canvas: HTMLCanvasElement, depthConfig?: DepthConfig): Promise<GpuCapabilities>;\n    /**\n     * Release all shared resources and tear down the view infrastructure.\n     *\n     * This method is idempotent — calling it multiple times is safe.\n     * Once called, the ViewCore instance must not be reused.\n     *\n     * **Teardown sequence:**\n     * 1. Sets {@link destroyed} to `true` (prevents further operations).\n     * 2. Disconnects the {@link ResizeObserver} (if active).\n     * 3. Destroys the buffer cache (releases GPU buffers).\n     * 4. Destroys the tile manager (cancels pending fetches, releases textures).\n     * 5. Destroys the render loop (stops animation frame scheduling).\n     * 6. Destroys the layer manager (clears layer references).\n     * 7. Destroys the GameMap (removes all layers).\n     * 8. Removes the canvas element from the DOM.\n     *\n     * @remarks The active {@link IViewMode} should be disposed separately\n     * before calling this method. MapView handles this ordering automatically.\n     *\n     * @see {@link IViewMode.dispose} for mode-specific cleanup\n     */\n    destroy(): void;\n}\n//# sourceMappingURL=ViewCore.d.ts.map","engine/WorkerPool.d.ts":"/**\n * WorkerPool — Worker havuzu yönetimi\n *\n * Round-robin dağıtım ile worker'lara task gönderimi.\n * Abstraction layer: gerçek Worker yerine IWorker interface inject edilir.\n * Pending task tracking (Map<id, resolve/reject>).\n */\n/** Message sent from main thread to worker */\nexport interface WorkerRequest {\n    id: number;\n    type: string;\n    data: unknown;\n}\n/** Message sent from worker back to main thread */\nexport interface WorkerResponse {\n    id: number;\n    result?: unknown;\n    error?: string;\n}\n/**\n * Abstraction over a real Web Worker.\n * In production, wraps `new Worker(url)`.\n * In tests, inject a mock.\n */\nexport interface IWorker {\n    postMessage(message: WorkerRequest, transfer: Transferable[]): void;\n    onmessage: ((event: {\n        data: WorkerResponse;\n    }) => void) | null;\n    onerror: ((event: {\n        message: string;\n    }) => void) | null;\n    terminate(): void;\n}\n/** Factory function to create workers */\nexport type WorkerFactory = () => IWorker;\nexport interface WorkerPoolOptions {\n    /** Maximum number of workers. Default: Math.min(hardwareConcurrency / 2, 4) */\n    maxWorkers?: number;\n    /** Factory function to create IWorker instances */\n    workerFactory: WorkerFactory;\n}\nexport declare class WorkerPool {\n    private _workers;\n    private _maxWorkers;\n    private _workerFactory;\n    private _nextTaskId;\n    private _nextWorkerIndex;\n    private _pending;\n    private _initialized;\n    constructor(options: WorkerPoolOptions);\n    /**\n     * Initialize the worker pool — creates all worker instances.\n     */\n    init(): void;\n    /**\n     * Dispatch a task to the next available worker (round-robin).\n     */\n    dispatch<T>(taskType: string, data: unknown, transfer?: Transferable[]): Promise<T>;\n    /**\n     * Terminate all workers and reject all pending tasks.\n     */\n    terminate(): void;\n    get workerCount(): number;\n    get pendingCount(): number;\n    get initialized(): boolean;\n    private _setupWorker;\n}\n//# sourceMappingURL=WorkerPool.d.ts.map","engine/coordinates.d.ts":"/**\n * Coordinate Helpers — EPSG:4326 ↔ EPSG:3857 conversion\n *\n * Lightweight pure-math functions (no WASM dependency).\n * Single source of truth — previously duplicated in MapView2D and TerrainView.\n */\n/** WGS-84 semi-major axis in meters */\nexport declare const EARTH_RADIUS = 6378137;\n/** Maximum latitude for Web Mercator (avoids singularity at ±90°) */\nexport declare const MAX_LAT = 85.0511287798066;\n/**\n * Convert geographic coordinates to Web Mercator (EPSG:3857).\n *\n * @param lon - Longitude in degrees\n * @param lat - Latitude in degrees (clamped to ±85.051°)\n * @returns [x, y] in meters (EPSG:3857)\n */\nexport declare function lonLatToMercator(lon: number, lat: number): [number, number];\n/**\n * Convert Web Mercator coordinates to geographic (EPSG:4326).\n *\n * @param x - Easting in meters (EPSG:3857)\n * @param y - Northing in meters (EPSG:3857)\n * @returns [longitude, latitude] in degrees\n */\nexport declare function mercatorToLonLat(x: number, y: number): [number, number];\n//# sourceMappingURL=coordinates.d.ts.map","engine/earcut.d.ts":"/**\n * Earcut — Polygon triangulation via mapbox/earcut\n *\n * Thin wrapper around the battle-tested mapbox earcut library.\n * Handles complex concave polygons with holes, self-intersections,\n * and degenerate geometries via z-order indexing and 3-pass algorithm.\n *\n * Re-exports earcut() with the same signature used throughout the codebase,\n * plus earcutDeviation() for triangulation quality measurement.\n */\nexport declare function earcut(data: number[], holeIndices?: number[], dim?: number): number[];\n/**\n * Measure triangulation quality: ratio of triangulated area vs original polygon area.\n *\n * Returns 0 for a perfect triangulation. Values > 0 indicate area loss\n * (e.g. earcut failed to triangulate some regions). Typical threshold: > 0.01.\n *\n * @param data       - Flat coordinate array [x0, y0, x1, y1, ...]\n * @param holeIndices - Indices into data/dim marking hole starts\n * @param dim        - Number of coordinates per vertex (2 for 2D)\n * @param triangles  - Triangle indices returned by earcut()\n * @returns Deviation ratio (0 = perfect)\n */\nexport declare function earcutDeviation(data: number[], holeIndices: number[] | undefined, dim: number, triangles: number[]): number;\n//# sourceMappingURL=earcut.d.ts.map","engine/geo-utils.d.ts":"import type { Geometry, Feature } from '../interfaces/index';\n/**\n * Generate a circle polygon geometry via Haversine destination point calculation.\n * @param center - [longitude, latitude] in degrees\n * @param radiusMeters - radius in meters\n * @param segments - number of polygon segments (default 64)\n */\nexport declare function createCircleGeometry(center: [number, number], radiusMeters: number, segments?: number): Geometry;\n/**\n * Create range ring features for multiple radii around a center point.\n * @param center - [longitude, latitude] in degrees\n * @param radii - array of radius values in meters\n * @param segments - number of polygon segments per ring (default 64)\n */\nexport declare function createRangeRings(center: [number, number], radii: number[], segments?: number): Feature[];\n//# sourceMappingURL=geo-utils.d.ts.map","engine/globe/GlobeTile.d.ts":"/**\n * GlobeTile — 3D globe quadtree tile veri yapısı\n *\n * Her tile, küre yüzeyinde bir bölgeyi temsil eder.\n * Web Mercator tile adresleme (z/x/y) kullanır.\n * GPU kaynakları (mesh, texture) tile state'ine göre yönetilir.\n */\nexport type GlobeTileState = 'empty' | 'meshed' | 'loading' | 'ready' | 'error' | 'disposed';\nexport interface GlobeTileDescriptor {\n    /** Zoom level */\n    z: number;\n    /** Tile column */\n    x: number;\n    /** Tile row */\n    y: number;\n    west: number;\n    south: number;\n    east: number;\n    north: number;\n    vertexBuffer: GPUBuffer | null;\n    indexBuffer: GPUBuffer | null;\n    indexCount: number;\n    imageryTexture: GPUTexture | null;\n    state: GlobeTileState;\n    /**\n     * Geometric error of the tile (metres).\n     * Tile'ın grid aralığından kaynaklanan maksimum hata.\n     * SSE hesabında kullanılır.\n     */\n    geometricError: number;\n    /** LRU tracking — son kullanım zamanı (ms) */\n    lastUsed: number;\n}\n/** Tile cache key: \"z/x/y\" */\nexport declare function tileKey(z: number, x: number, y: number): string;\n/**\n * Convert Web Mercator tile coordinates to geographic bounds (WGS84 degrees).\n *\n * Standard slippy map tile scheme:\n *   tile2lon(x, z) = x / 2^z * 360 - 180\n *   tile2lat(y, z) = atan(sinh(π * (1 - 2*y / 2^z))) * 180/π\n */\nexport declare function tileToGeographic(z: number, x: number, y: number): {\n    west: number;\n    south: number;\n    east: number;\n    north: number;\n};\nexport interface TileBoundingSphere {\n    center: [number, number, number];\n    radius: number;\n}\n/**\n * Compute a bounding sphere for a geographic tile in ECEF.\n * Center = ECEF position of tile center on sphere surface.\n * Radius = distance from center to the farthest corner.\n */\nexport declare function computeTileBoundingSphere(west: number, south: number, east: number, north: number): TileBoundingSphere;\n/**\n * Compute the Horizon Occlusion Point for a tile.\n * CesiumJS-style: a point that, if visible from the camera, guarantees the tile\n * is potentially visible. This replaces acos-based horizon checks with a single dot product.\n *\n * The HOP is the tile center scaled outward by (centerDist + radius) / centerDist.\n */\nexport declare function computeHorizonOcclusionPoint(west: number, south: number, east: number, north: number): [number, number, number];\n/**\n * Compute geometric error for a tile.\n * The error is the approximate world-space size of one grid cell.\n *\n * @param gridSize Number of grid subdivisions per tile (e.g. 16)\n */\nexport declare function computeGeometricError(west: number, south: number, east: number, north: number, gridSize: number): number;\n//# sourceMappingURL=GlobeTile.d.ts.map","engine/globe/GlobeTileManager.d.ts":"/**\n * GlobeTileManager — Globe tile lifecycle yönetimi\n *\n * Her frame:\n * 1. GlobeTileScheduler'dan visible tile listesini al\n * 2. Her tile için mesh oluştur (CPU, hızlı)\n * 3. Imagery texture'ı cache'ten al veya async fetch başlat\n * 4. Renderable tile listesini döndür (TerrainMesh[])\n *\n * LRU cache ile eski tile'ların GPU kaynakları serbest bırakılır.\n * Parent-shows-while-children-load: yüklenen tile'ın parent'ı render edilir.\n */\nimport type { IRenderEngine, TerrainMesh } from '../../interfaces/index';\nimport type { CameraController3D } from '../CameraController3D';\nimport type { ImageryProvider } from './ImageryProvider';\nimport type { TerrainProvider } from './TerrainProvider';\nimport type { TileSelection, GlobeTileSchedulerOptions } from './GlobeTileScheduler';\nexport interface GlobeTileManagerOptions {\n    /** Imagery tile provider (e.g. UrlTemplateImageryProvider) */\n    imagery?: ImageryProvider | null;\n    /** Terrain/DEM provider (default: flat) */\n    terrain?: TerrainProvider | null;\n    /** Maximum cached tiles (default: 256) */\n    maxCacheEntries?: number;\n    /** Maximum concurrent imagery fetches (default: 6) */\n    maxConcurrentFetches?: number;\n    /** Grid subdivisions per tile mesh (default: 16) */\n    gridSize?: number;\n    /** Scheduler options */\n    scheduler?: GlobeTileSchedulerOptions;\n}\nexport declare class GlobeTileManager {\n    private readonly _scheduler;\n    private readonly _imagery;\n    private readonly _terrain;\n    private readonly _maxCacheEntries;\n    private readonly _maxConcurrent;\n    private readonly _gridSize;\n    private _renderEngine;\n    private _destroyed;\n    /** Tile cache: key → descriptor */\n    private readonly _cache;\n    /** In-flight imagery fetch promises */\n    private readonly _inFlight;\n    /** Callback: yeni tile hazır olduğunda render loop'u bilgilendir */\n    onDirty: (() => void) | null;\n    private _diagUpdateCount;\n    private _diagFetchStarted;\n    private _diagFetchSuccess;\n    private _diagFetchError;\n    private _diagFetchSkipMaxConcurrent;\n    private _diagFetchSkipNoUrl;\n    constructor(options?: GlobeTileManagerOptions);\n    /** Set the render engine (called after GPU init) */\n    setRenderEngine(engine: IRenderEngine): void;\n    /**\n     * Update: select visible tiles, generate meshes, return renderable list.\n     * Called each frame from SceneView3D._renderFrame().\n     */\n    update(camera: CameraController3D): TerrainMesh[];\n    /**\n     * Get visible tile selections without generating meshes.\n     * Used by pre-frame pass for near/far computation.\n     */\n    getVisibleTileSelections(camera: CameraController3D): TileSelection[];\n    /**\n     * Update from pre-computed tile selections (Phase 3: 2-pass render).\n     * Generates meshes, starts imagery fetches, returns renderable list.\n     */\n    updateFromSelections(selections: TileSelection[]): TerrainMesh[];\n    /** Terrain provider (reserved for future DEM integration) */\n    get terrain(): TerrainProvider | null;\n    /** Cache statistics */\n    get cacheSize(): number;\n    get inFlightCount(): number;\n    /** Destroy: release all GPU resources */\n    destroy(): void;\n    private _createTile;\n    private _startImageryFetch;\n    private _fetchImagery;\n    private _releaseTileResources;\n    private _evictIfNeeded;\n}\n//# sourceMappingURL=GlobeTileManager.d.ts.map","engine/globe/GlobeTileMesh.d.ts":"/**\n * GlobeTileMesh — Küre yüzeyi mesh üretimi\n *\n * Her tile için lon/lat grid → ECEF vertex mesh üretir.\n * Terrain pipeline vertex layout'u ile uyumlu:\n *   position(vec3) + uv(vec2) + normal(vec3) = 8 floats = 32 bytes/vertex\n *\n * DEM height displacement destekli: heights array verilirse vertex'ler\n * yüzey normali boyunca ötelenir.\n */\nexport interface TileMeshData {\n    /** Interleaved vertex data: position(3) + uv(2) + normal(3) */\n    vertices: Float32Array;\n    /** Triangle indices (CCW winding from outside) */\n    indices: Uint32Array;\n    /** Number of indices */\n    indexCount: number;\n}\n/**\n * Generate a sphere surface mesh for the given geographic tile region.\n *\n * Vertices are in ECEF coordinates (metres).\n * UV coordinates map [0,1]² for imagery texture drape.\n * Normals are the outward surface normals (unit sphere direction).\n *\n * @param west   Western longitude bound (degrees)\n * @param south  Southern latitude bound (degrees)\n * @param east   Eastern longitude bound (degrees)\n * @param north  Northern latitude bound (degrees)\n * @param gridSize Number of subdivisions per axis (default: 16)\n * @param heights  Optional DEM height data (gridSize+1)² array, row-major top-to-bottom\n */\n/**\n * Generate a sphere surface mesh for the given geographic tile region.\n *\n * @param skirtHeight Optional downward extrusion along normals at tile edges (metres).\n *   Skirts hide gaps between adjacent tiles at different LOD levels.\n */\nexport declare function generateTileMesh(west: number, south: number, east: number, north: number, gridSize?: number, heights?: Float32Array | null, skirtHeight?: number): TileMeshData;\n/**\n * Recompute normals from displaced mesh positions.\n * Call after height displacement when DEM is loaded.\n * Modifies vertices in-place.\n *\n * @param vertices Interleaved vertex array (8 floats per vertex)\n * @param indices  Triangle index array\n * @param gridSize Grid subdivision count\n */\nexport declare function recomputeNormals(vertices: Float32Array, indices: Uint32Array, gridSize: number): void;\n//# sourceMappingURL=GlobeTileMesh.d.ts.map","engine/globe/GlobeTileScheduler.d.ts":"/**\n * GlobeTileScheduler — Quadtree tabanlı tile seçimi\n *\n * CesiumJS mimarisini takip eder:\n * 1. Root tile'dan (zoom 0) başla\n * 2. Her tile için SSE (Screen-Space Error) hesapla\n * 3. SSE > threshold ise 4 child'a subdivide et, recurse\n * 4. Frustum + horizon culling ile görünmeyen tile'ları ele\n *\n * Sonuç: O(log n) traversal ile minimum tile set'i seçilir.\n */\nimport type { CameraController3D } from '../CameraController3D';\nexport interface TileSelection {\n    z: number;\n    x: number;\n    y: number;\n    west: number;\n    south: number;\n    east: number;\n    north: number;\n    geometricError: number;\n}\nexport interface GlobeTileSchedulerOptions {\n    /** Maximum zoom level (default: 19) */\n    maxZoom?: number;\n    /** Screen-space error threshold in pixels (default: 2.0) */\n    maxSSE?: number;\n    /** Grid subdivisions per tile — affects geometric error (default: 16) */\n    gridSize?: number;\n    /**\n     * Maximum tile count per frame (safety limit, default: 150).\n     * Prevents runaway subdivision.\n     */\n    maxTilesPerFrame?: number;\n}\nexport declare class GlobeTileScheduler {\n    private readonly _maxZoom;\n    private readonly _maxSSE;\n    private readonly _gridSize;\n    private readonly _maxTiles;\n    private _diagVisited;\n    private _diagFrustumCulled;\n    private _diagHopCulled;\n    private _diagLeaf;\n    private _diagSubdivided;\n    private _diagCallCount;\n    constructor(options?: GlobeTileSchedulerOptions);\n    /**\n     * Select visible tiles for the current camera state.\n     * Returns a list of tile coordinates sorted by distance (nearest first).\n     */\n    getVisibleTiles(camera: CameraController3D): TileSelection[];\n    private _visitTile;\n    /**\n     * CesiumJS-style Horizon Occlusion Point test.\n     * Uses dot product instead of acos — faster and more accurate.\n     *\n     * The HOP is the tile center scaled outward by its bounding sphere radius.\n     * If dot(HOP, camECEF) > R², the tile is potentially visible above the horizon.\n     */\n    private _isAboveHorizon;\n    /** Squared distance from camera to tile center (for sorting) */\n    private _tileDistanceSq;\n}\n/**\n * Compute tight near/far planes from the set of visible tiles.\n * This maximises depth precision by fitting the depth range to actual content.\n */\nexport declare function computeNearFarFromTiles(tiles: TileSelection[], camECEF: [number, number, number], globeRadius: number): {\n    near: number;\n    far: number;\n};\n//# sourceMappingURL=GlobeTileScheduler.d.ts.map","engine/globe/ImageryProvider.d.ts":"/**\n * ImageryProvider — Globe imagery tile kaynağı soyutlaması\n *\n * Provider pattern: farklı tile servisleri (OSM, Mapbox, Bing, WMS-C)\n * aynı interface üzerinden kullanılabilir.\n *\n * İlk implementasyon: UrlTemplateImageryProvider (XYZ/TMS tile servisleri)\n */\nexport interface ImageryProvider {\n    /** Provider hazır mı? (metadata yüklendi mi) */\n    readonly ready: boolean;\n    /** Minimum zoom level */\n    readonly minZoom: number;\n    /** Maximum zoom level */\n    readonly maxZoom: number;\n    /** Tile pixel boyutu (genellikle 256 veya 512) */\n    readonly tileSize: number;\n    /** Attribution metni */\n    readonly attribution: string;\n    /**\n     * Verilen tile koordinatları için URL üret.\n     * null dönerse bu tile için imagery yok demektir.\n     */\n    getTileUrl(z: number, x: number, y: number): string | null;\n}\nexport interface UrlTemplateImageryProviderOptions {\n    /**\n     * URL template. Placeholders:\n     *   {z} — zoom level\n     *   {x} — tile column\n     *   {y} — tile row\n     *   {s} — subdomain (round-robin)\n     *   {-y} — TMS y (flipped)\n     */\n    url: string;\n    /** Subdomains for load balancing (e.g. ['a','b','c']) */\n    subdomains?: string[];\n    /** Minimum zoom (default: 0) */\n    minZoom?: number;\n    /** Maximum zoom (default: 19) */\n    maxZoom?: number;\n    /** Tile pixel size (default: 256) */\n    tileSize?: number;\n    /** Attribution text (default: '') */\n    attribution?: string;\n    /** TMS mode: {-y} kullanımı (default: false) */\n    tms?: boolean;\n}\nexport declare class UrlTemplateImageryProvider implements ImageryProvider {\n    readonly ready = true;\n    readonly minZoom: number;\n    readonly maxZoom: number;\n    readonly tileSize: number;\n    readonly attribution: string;\n    private readonly _urlTemplate;\n    private readonly _subdomains;\n    private readonly _tms;\n    private _subdomainIndex;\n    constructor(options: UrlTemplateImageryProviderOptions);\n    getTileUrl(z: number, x: number, y: number): string | null;\n}\n//# sourceMappingURL=ImageryProvider.d.ts.map","engine/globe/TerrainProvider.d.ts":"/**\n * TerrainProvider — DEM / elevation veri kaynağı soyutlaması\n *\n * Globe tile mesh'inin height displacement'ı için yükseklik verisi sağlar.\n * Her tile (z/x/y) için bir height grid döner.\n *\n * Şu an stub — gelecekte Mapzen Terrarium, Cesium Terrain Server,\n * Mapbox Terrain RGB vb. implementasyonlar eklenecek.\n */\nexport interface TerrainTileData {\n    /**\n     * Height values in metres, row-major, top-to-bottom.\n     * Array size = (gridSize+1)²\n     */\n    heights: Float32Array;\n    /** Grid subdivision count (e.g. 16 → 17×17 = 289 values) */\n    gridSize: number;\n}\nexport interface TerrainProvider {\n    /** Provider hazır mı? */\n    readonly ready: boolean;\n    /** Minimum zoom level */\n    readonly minZoom: number;\n    /** Maximum zoom level */\n    readonly maxZoom: number;\n    /**\n     * Verilen tile için yükseklik verisini getir.\n     * null dönerse flat terrain kullanılır.\n     */\n    getTerrainTile(z: number, x: number, y: number): Promise<TerrainTileData | null>;\n}\n/**\n * Düz terrain — tüm yükseklikler 0.\n * DEM verisi olmadığında varsayılan provider.\n */\nexport declare class FlatTerrainProvider implements TerrainProvider {\n    readonly ready = true;\n    readonly minZoom = 0;\n    readonly maxZoom = 22;\n    getTerrainTile(): Promise<null>;\n}\n//# sourceMappingURL=TerrainProvider.d.ts.map","engine/globe/index.d.ts":"export { type GlobeTileState, type GlobeTileDescriptor, type TileBoundingSphere, tileKey, tileToGeographic, computeGeometricError, computeTileBoundingSphere, computeHorizonOcclusionPoint, } from './GlobeTile';\nexport { type TileMeshData, generateTileMesh, recomputeNormals, } from './GlobeTileMesh';\nexport { type ImageryProvider, type UrlTemplateImageryProviderOptions, UrlTemplateImageryProvider, } from './ImageryProvider';\nexport { type TerrainProvider, type TerrainTileData, FlatTerrainProvider, } from './TerrainProvider';\nexport { type TileSelection, type GlobeTileSchedulerOptions, GlobeTileScheduler, computeNearFarFromTiles, } from './GlobeTileScheduler';\nexport { type GlobeTileManagerOptions, GlobeTileManager, } from './GlobeTileManager';\n//# sourceMappingURL=index.d.ts.map","engine/index.d.ts":"export { lonLatToMercator, mercatorToLonLat, EARTH_RADIUS, MAX_LAT } from './coordinates';\nexport { GameMap } from './Map';\nexport type { MapEvents } from './Map';\nexport { CameraController2D } from './CameraController2D';\nexport type { CameraController2DOptions, ViewState } from './CameraController2D';\nexport { LayerManager } from './LayerManager';\nexport type { LayerManagerEvents } from './LayerManager';\nexport { TileScheduler } from './TileScheduler';\nexport type { TileCoord, TileWithPriority, TileSchedulerOptions, } from './TileScheduler';\nexport { RenderLoop } from './RenderLoop';\nexport type { FrameCallback, RenderLoopOptions, FrameStats, } from './RenderLoop';\nexport { WorkerPool } from './WorkerPool';\nexport type { WorkerRequest, WorkerResponse, IWorker, WorkerFactory, WorkerPoolOptions, } from './WorkerPool';\nexport { ResourceManager } from './ResourceManager';\nexport type { ResourceDescriptor, MemoryUsage, ResourceManagerOptions, } from './ResourceManager';\nexport { AnimationManager, Easing } from './AnimationManager';\nexport type { EasingFunction, EasingName, AnimationHandle, AnimateOptions, } from './AnimationManager';\nexport { CommandSystem } from './CommandSystem';\nexport type { ICommand, CommandSystemEvents, CommandSystemOptions, } from './CommandSystem';\nexport { Diagnostics } from './Diagnostics';\nexport type { DiagnosticStats, DiagnosticsOptions, } from './Diagnostics';\nexport { SceneGraph, SceneNode } from './SceneGraph';\nexport type { SceneNodeType, SceneNodeTransform, SceneNodeOptions, TraverseCallback, } from './SceneGraph';\nexport { FrustumCuller } from './FrustumCuller';\nexport type { FrustumPlane, FrustumPlanes, AABB, IntersectResult, } from './FrustumCuller';\nexport { extractFrustumPlanes, testAABBFrustum, testSphereFrustum } from './FrustumCuller';\nexport { TileManager } from './TileManager';\nexport type { TileManagerOptions, TileFetcher, TileSourceInfo, } from './TileManager';\nexport { TerrainTileManager } from './TerrainTileManager';\nexport type { TerrainTileCoord, TerrainReadyHeightTile, TerrainReadyHillshadeTile, TerrainTileManagerOptions, } from './TerrainTileManager';\nexport { InteractionHandler } from './InteractionHandler';\nexport type { InteractionHandlerOptions } from './InteractionHandler';\nexport { GeometryConverter } from './GeometryConverter';\nexport type { PointVertexData, LineVertexData, PolygonVertexData, ModelInstanceData, } from './GeometryConverter';\nexport { earcut } from './earcut';\nexport { VectorBufferCache } from './VectorBufferCache';\nexport type { VectorBufferEntry } from './VectorBufferCache';\nexport { DEFAULT_POINT_SYMBOL, DEFAULT_LINE_SYMBOL, DEFAULT_POLYGON_SYMBOL, } from './VectorBufferCache';\nexport { MapView } from './MapView';\nexport type { MapViewOptions, MapViewEvents, HitTestResult } from './MapView';\nexport type { IViewMode, ViewState as ModeViewState, RenderFrameContext, GoToTarget } from './IViewMode';\nexport { ViewCore } from './ViewCore';\nexport { Mode2D } from './modes/index';\nexport type { Mode2DOptions } from './modes/index';\nexport { Mode3D } from './modes/index';\nexport type { Mode3DOptions } from './modes/index';\nexport { GlobeInteraction } from './GlobeInteraction';\nexport type { GlobeInteractionOptions } from './GlobeInteraction';\nexport type { IProjection } from './projections/index';\nexport { MercatorProjection } from './projections/index';\nexport { GlobeProjection } from './projections/index';\nexport { VerticalPerspectiveTransform } from './projections/index';\nexport type { GlobeCameraParams } from './projections/index';\nexport { ConvexVolume } from './projections/index';\nexport type { ConvexVolumePlane } from './projections/index';\nexport { GlobeTileCovering } from './projections/index';\nexport type { GlobeTileCoord, GlobeTileCoveringOptions } from './projections/index';\nexport { createCircleGeometry, createRangeRings } from './geo-utils';\nexport { ToolManager } from '../tools/index';\nexport type { ITool, ToolState, ToolCursor, ToolContext, ToolPointerEvent, IPreviewLayer, ToolEvents, ToolManagerOptions, MeasurementResult, } from '../tools/index';\nexport { svgToImageBitmap } from './svg-utils';\n//# sourceMappingURL=index.d.ts.map","engine/mode-helpers.d.ts":"/**\n * mode-helpers — Shared logic extracted from Mode2D and Mode3D.\n *\n * Contains layer classification, vector rendering, and custom shader\n * rendering functions parameterized by a `globe` flag so they work\n * for both flat (2D) and globe (3D) render paths.\n *\n * @internal\n */\nimport type { ClusterViewCallbacks, CustomTextureBinding, ICustomShaderLayer, IRenderEngine } from '../interfaces/index';\n/**\n * Handle returned by {@link animateView} to control an in-progress animation.\n */\nexport interface AnimationHandle {\n    /** Promise that resolves when the animation completes or is cancelled. */\n    promise: Promise<void>;\n    /** Cancel the animation. The promise resolves on the next tick. */\n    cancel: () => void;\n}\n/**\n * Shared ease-in-out animation driver for goTo() in both Mode2D and Mode3D.\n *\n * Uses quadratic ease-in-out: `t < 0.5 ? 2t² : 1 - (-2t+2)²/2`\n * Schedules at ~60 fps via setTimeout(step, 16).\n *\n * @param duration - Animation duration in milliseconds.\n * @param onStep  - Called each frame with the eased progress (0..1).\n *                  The caller should interpolate mode-specific properties\n *                  and call markDirty()/onViewChange() inside this callback.\n * @param isAlive - Returns false if the mode has been destroyed,\n *                  which aborts the animation.\n * @returns An {@link AnimationHandle} with a promise and cancel function.\n */\nexport declare function animateView(duration: number, onStep: (ease: number) => void, isAlive: () => boolean): AnimationHandle;\nimport type { LayerManager } from './LayerManager';\nimport type { VectorBufferCache } from './VectorBufferCache';\nimport type { TileSourceInfo } from './TileManager';\n/**\n * Result of classifying visible layers by type.\n *\n * Produced by {@link classifyVisibleLayers} and consumed by the mode's\n * renderFrame() to dispatch draw calls.\n */\nexport interface ClassifiedLayers {\n    /** Tile (raster) layer sources suitable for TileManager. */\n    tileSources: TileSourceInfo[];\n    /** IDs of visible terrain layers (draw order bottom->top). */\n    terrainLayerIds: string[];\n    /** IDs of visible feature (vector) layers. */\n    vectorLayerIds: string[];\n    /** IDs of visible custom WGSL shader layers. */\n    customLayerIds: string[];\n    /** IDs of visible GPU cluster layers. */\n    clusterLayerIds: string[];\n    /** IDs of visible dynamic point layers (bulk GPU update). */\n    dynamicPointLayerIds: string[];\n    /** IDs of visible vector tile layers (PBF → Feature[], need updateVisibleTiles). */\n    vectorTileLayerIds: string[];\n}\n/**\n * Iterate all layers in the manager and classify them by type.\n *\n * A single layer may appear in multiple categories (e.g. a layer that\n * implements both ITileLayer and IFeatureLayer). Layers that are not\n * visible or not yet loaded are skipped.\n *\n * When `zoom` is provided, tile layers whose minZoom/maxZoom range\n * excludes the current zoom are skipped (Leaflet-compatible visibility).\n *\n * Results are stable-sorted by layer zIndex (lower values drawn first).\n *\n * @param layerManager - The layer manager to query.\n * @param zoom - Current map zoom level (used for tile layer visibility filtering).\n * @returns Classified layer groups.\n */\nexport declare function classifyVisibleLayers(layerManager: LayerManager, zoom?: number): ClassifiedLayers;\n/**\n * Render a single dynamic point layer.\n *\n * Dynamic point layers use pre-allocated GPU buffers that are updated\n * via writeBuffer() each frame — no allocation, ideal for real-time\n * simulations (e.g. missile tracks).\n *\n * @param layerId      - The unique identifier of the layer.\n * @param renderEngine - The WebGPU render engine for draw calls.\n * @param layerManager - Manager to look up layers by ID.\n * @param globe        - Whether to use globe projection draw calls.\n */\nexport declare function renderDynamicPointLayer(layerId: string, renderEngine: IRenderEngine, layerManager: LayerManager, globe: boolean): void;\n/**\n * Render a single vector (feature) layer.\n *\n * Retrieves the layer's features, builds or fetches cached GPU buffers\n * via VectorBufferCache, then issues draw calls for polygons, lines,\n * points, and models. When `globe` is true, the globe-specific draw\n * methods are used (drawGlobePolygons, etc.); otherwise the flat 2D\n * draw methods are used.\n *\n * @param layerId      - The unique identifier of the layer to render.\n * @param renderEngine - The WebGPU render engine for draw calls.\n * @param layerManager - Manager to look up layers by ID.\n * @param bufferCache  - Cache for GPU vertex/index buffers.\n * @param globe        - Whether to use globe projection draw calls.\n */\nexport declare function renderVectorLayer(layerId: string, renderEngine: IRenderEngine, layerManager: LayerManager, bufferCache: VectorBufferCache, globe: boolean, zoom?: number): void;\nexport declare function renderVectorTileLayer(layerId: string, renderEngine: IRenderEngine, layerManager: LayerManager, bufferCache: VectorBufferCache, globe: boolean, zoom?: number): void;\n/**\n * Render a single custom WGSL shader layer.\n *\n * Retrieves the layer's draw command, uniforms, and textures, builds the\n * full WGSL shader source via the provided `buildShaderSource` callback,\n * then issues a drawCustom() call on the render engine.\n *\n * @param layerId           - The unique identifier of the layer to render.\n * @param renderEngine      - The WebGPU render engine for draw calls.\n * @param layerManager      - Manager to look up layers by ID.\n * @param buildShaderSource - Mode-specific function that produces the full\n *                            WGSL source (preamble + user shaders).\n * @param globe             - Whether to use globe camera bindings. When true,\n *                            `:globe` is appended to the pipeline key and\n *                            `useGlobeCamera` is set on the draw call.\n */\nexport declare function renderCustomShaderLayer(layerId: string, renderEngine: IRenderEngine, layerManager: LayerManager, buildShaderSource: (layer: ICustomShaderLayer, customUniforms: ArrayBuffer | null, textures: CustomTextureBinding[]) => string, globe: boolean): void;\n/**\n * Render a single GPU cluster layer.\n *\n * Uploads source points if changed, then dispatches CPU clustering + GPU render\n * on the render engine. The extent is needed for grid sizing.\n *\n * @param layerId        - The unique identifier of the cluster layer.\n * @param renderEngine   - The WebGPU render engine.\n * @param layerManager   - Manager to look up layers by ID.\n * @param zoom           - Current zoom level (for cell size calculation).\n * @param extent         - Visible extent in EPSG:3857 [minX, minY, maxX, maxY].\n * @param globe          - Whether to use globe projection for rendering.\n * @param viewCallbacks  - Optional view callbacks for cluster click interaction.\n */\nexport declare function renderClusterLayer(layerId: string, renderEngine: IRenderEngine, layerManager: LayerManager, zoom: number, extent: [number, number, number, number], globe: boolean, viewCallbacks?: ClusterViewCallbacks): void;\n//# sourceMappingURL=mode-helpers.d.ts.map","engine/modes/Mode2D.d.ts":"/**\n * Mode2D — 2D (flat Mercator) strategy for MapView.\n *\n * Implements the {@link IViewMode} interface for flat, top-down map rendering\n * using Web Mercator (EPSG:3857) projection. Wraps {@link CameraController2D}\n * for camera math and {@link InteractionHandler} for pointer/keyboard input.\n *\n * In 2D mode, pitch and bearing are always 0. The only orientation control\n * is map rotation (heading) in degrees.\n *\n * @see IViewMode\n */\nimport type { CameraState } from '../../interfaces/index';\nimport type { IViewMode, ViewState, GoToTarget, RenderFrameContext } from '../IViewMode';\nimport { CameraController2D } from '../CameraController2D';\n/**\n * Configuration options for creating a {@link Mode2D} instance.\n *\n * All properties are optional and fall back to sensible defaults\n * (center at [0, 0], zoom 0, no rotation).\n */\nexport interface Mode2DOptions {\n    /** Initial map center as [longitude, latitude] in EPSG:4326. Defaults to [0, 0]. */\n    center?: [number, number];\n    /** Initial zoom level. Defaults to 0 (fully zoomed out). */\n    zoom?: number;\n    /** Initial map rotation in degrees (clockwise). Defaults to 0. */\n    rotation?: number;\n    /** Minimum allowed zoom level. */\n    minZoom?: number;\n    /** Maximum allowed zoom level. */\n    maxZoom?: number;\n    /** Initial viewport width in pixels. Defaults to 800. */\n    viewportWidth?: number;\n    /** Initial viewport height in pixels. Defaults to 600. */\n    viewportHeight?: number;\n}\n/**\n * 2D (flat Mercator) view mode for MapView.\n *\n * Renders the map as a flat plane using Web Mercator projection. Supports\n * raster tile layers, vector (feature) layers, and custom WGSL shader layers.\n *\n * @see IViewMode\n */\nexport declare class Mode2D implements IViewMode {\n    /** @see IViewMode.type */\n    readonly type: \"2d\";\n    /** Internal 2D camera controller (Mercator coordinates). */\n    private _camera;\n    /** Active interaction handler, or null if not attached. */\n    private _interaction;\n    /** Current animation handle, or null if idle. */\n    private _anim;\n    /** Whether this mode has been disposed. */\n    private _destroyed;\n    /** Stored markDirty callback for cluster click-triggered goTo. */\n    private _markDirty;\n    /** Stored onViewChange callback for cluster-triggered goTo. */\n    private _onViewChange;\n    /**\n     * Create a new Mode2D instance.\n     *\n     * The provided center [lon, lat] is converted to EPSG:3857 internally.\n     * Rotation is converted from degrees to radians for the underlying camera.\n     *\n     * @param options - Initial view configuration. Defaults to center [0,0], zoom 0.\n     */\n    constructor(options?: Mode2DOptions);\n    /**\n     * Apply a partial view state update to the camera.\n     *\n     * Center coordinates are converted from [lon, lat] to EPSG:3857.\n     * Rotation is converted from degrees to radians. Pitch and bearing\n     * fields are silently ignored in 2D mode.\n     *\n     * @param state - Partial view state to apply.\n     * @see IViewMode.setState\n     */\n    setState(state: Partial<ViewState>): void;\n    /**\n     * Read the current view state.\n     *\n     * Returns the center in [lon, lat] (EPSG:4326), the current zoom level,\n     * and the rotation in degrees. Pitch and bearing are always 0 in 2D mode.\n     *\n     * @returns The current serializable view state.\n     * @see IViewMode.getState\n     */\n    getState(): ViewState;\n    /**\n     * Build the CameraState for the render engine's beginFrame() call.\n     *\n     * Provides the orthographic view/projection matrices, camera position\n     * (center in EPSG:3857 at z=0), and viewport dimensions.\n     *\n     * @returns The camera state suitable for the WebGPU render engine.\n     * @see IViewMode.getCameraState\n     */\n    getCameraState(): CameraState;\n    /**\n     * Update the viewport dimensions (e.g. on canvas resize).\n     *\n     * Propagates the new size to the underlying camera controller,\n     * which recalculates the projection matrix.\n     *\n     * @param width - New viewport width in pixels.\n     * @param height - New viewport height in pixels.\n     * @see IViewMode.setViewport\n     */\n    setViewport(width: number, height: number): void;\n    /** Expose camera for advanced use (e.g. extent computation) */\n    get camera(): CameraController2D;\n    /**\n     * Animate the camera to a target view state.\n     *\n     * Uses ease-in-out (quadratic) interpolation over the specified duration.\n     * Interpolates center (in EPSG:3857), zoom, and rotation simultaneously.\n     * If duration is 0, the transition is applied immediately without animation.\n     *\n     * Only center, zoom, and rotation fields from the target are used;\n     * pitch and bearing are ignored in 2D mode.\n     *\n     * Any in-progress animation is cancelled before starting a new one.\n     *\n     * @param target - The navigation target describing the desired end state.\n     * @param markDirty - Callback to flag the view as needing a re-render.\n     * @param onViewChange - Callback to notify listeners of a view state change.\n     * @returns A promise that resolves when the animation completes or is cancelled.\n     * @throws Error if the mode has been disposed.\n     * @see IViewMode.goTo\n     */\n    goTo(target: GoToTarget, markDirty: () => void, onViewChange: () => void): Promise<void>;\n    /**\n     * Cancel any in-progress goTo animation.\n     *\n     * Clears the animation flag and cancels the pending setTimeout.\n     * The animation promise resolves on the next scheduled step.\n     *\n     * @see IViewMode.cancelAnimation\n     */\n    cancelAnimation(): void;\n    /**\n     * Render a single frame in 2D mode.\n     *\n     * Performs the following steps:\n     * 1. Computes the viewport extent in EPSG:3857 from the camera.\n     * 2. Classifies visible layers into tile (raster), feature (vector),\n     *    and custom shader categories.\n     * 3. Fetches and draws ready raster tiles via TileManager.\n     * 4. Draws vector features (polygons, lines, points) via VectorBufferCache.\n     * 5. Draws custom WGSL shader layers.\n     *\n     * Called by the RenderLoop between beginFrame() and endFrame().\n     *\n     * @param ctx - Shared rendering resources (render engine, managers, caches).\n     * @see IViewMode.renderFrame\n     */\n    renderFrame(ctx: RenderFrameContext): void;\n    private _resolveActiveTerrainLayer;\n    /**\n     * Build WGSL shader source with preamble for a custom shader layer.\n     */\n    private _buildCustomShaderSource;\n    /**\n     * Attach pointer and keyboard interaction handlers to a DOM container.\n     *\n     * Creates an {@link InteractionHandler} that translates mouse/touch/keyboard\n     * events into camera pan, zoom, and rotation operations. Pass `false` for\n     * the options parameter to skip attaching interaction entirely.\n     *\n     * @param container - The HTML element to listen for input events on.\n     * @param markDirty - Callback to flag the view as needing a re-render.\n     * @param onViewChange - Callback to notify listeners of a view state change.\n     * @param options - Interaction configuration, or `false` to disable.\n     * @see IViewMode.attachInteraction\n     */\n    attachInteraction(container: HTMLElement, markDirty: () => void, onViewChange: () => void, options?: Record<string, unknown> | false): void;\n    /**\n     * Convert screen pixel coordinates to geographic [longitude, latitude].\n     *\n     * Unprojects the screen position through the 2D camera to EPSG:3857,\n     * then converts to EPSG:4326 (lon/lat).\n     *\n     * @param screenX - Horizontal pixel position relative to the canvas.\n     * @param screenY - Vertical pixel position relative to the canvas.\n     * @returns Geographic coordinates as [longitude, latitude].\n     * @see IViewMode.toMap\n     */\n    toMap(screenX: number, screenY: number): [number, number];\n    /**\n     * Convert geographic coordinates to screen pixel position.\n     *\n     * Projects [longitude, latitude] through EPSG:3857 and the 2D camera\n     * to obtain canvas pixel coordinates.\n     *\n     * @param lon - Longitude in degrees.\n     * @param lat - Latitude in degrees.\n     * @returns Screen coordinates as [x, y] in pixels.\n     * @see IViewMode.toScreen\n     */\n    toScreen(lon: number, lat: number): [number, number];\n    /**\n     * Dispose of all mode-specific resources.\n     *\n     * Cancels any in-progress animation, destroys the interaction handler,\n     * and marks the mode as destroyed. Subsequent calls are no-ops.\n     *\n     * @see IViewMode.dispose\n     */\n    dispose(): void;\n}\n//# sourceMappingURL=Mode2D.d.ts.map","engine/modes/Mode3D.d.ts":"/**\n * Mode3D — Globe strategy for MapView.\n *\n * Implements the {@link IViewMode} interface for 3D globe rendering using\n * a MapLibre-inspired dual-projection model. Wraps\n * {@link VerticalPerspectiveTransform} for camera math,\n * {@link GlobeProjection} for the Mercator-to-globe transition, and\n * {@link GlobeInteraction} for orbit-style pointer/keyboard input.\n *\n * Supports raster tile layers (projected onto the unit sphere) and vector\n * (feature) layers rendered in globe space.\n *\n * The `globeness` factor (0 = flat Mercator, 1 = full globe) auto-transitions\n * around zoom level 5-6, allowing seamless switching between flat and globe\n * views.\n *\n * In 3D mode, rotation is always 0. Camera orientation is controlled via\n * pitch and bearing (both in degrees).\n *\n * @see IViewMode\n */\nimport type { CameraState } from '../../interfaces/index';\nimport type { IViewMode, ViewState, GoToTarget, RenderFrameContext } from '../IViewMode';\nimport { VerticalPerspectiveTransform } from '../projections/VerticalPerspectiveTransform';\nimport { GlobeProjection } from '../projections/GlobeProjection';\n/**\n * Configuration options for creating a {@link Mode3D} instance.\n *\n * All properties are optional and fall back to sensible defaults\n * (center at [0, 0], zoom 2, no pitch or bearing).\n */\nexport interface Mode3DOptions {\n    /** Initial map center as [longitude, latitude] in EPSG:4326. Defaults to [0, 0]. */\n    center?: [number, number];\n    /** Initial zoom level. Defaults to 2. */\n    zoom?: number;\n    /** Initial pitch (tilt) angle in degrees. Defaults to 0 (top-down). */\n    pitch?: number;\n    /** Initial bearing (heading) in degrees. Defaults to 0 (north-up). */\n    bearing?: number;\n    /** Initial viewport width in pixels. Defaults to 800. */\n    viewportWidth?: number;\n    /** Initial viewport height in pixels. Defaults to 600. */\n    viewportHeight?: number;\n}\n/**\n * 3D globe view mode for MapView.\n *\n * Renders the map on a unit sphere with vertical perspective projection.\n * Supports smooth Mercator-to-globe transition, atmosphere/pole cap\n * rendering, and orbit-style camera controls (pitch/bearing).\n *\n * @see IViewMode\n */\nexport declare class Mode3D implements IViewMode {\n    /** @see IViewMode.type */\n    readonly type: \"3d\";\n    /** Vertical perspective camera transform (manages view/projection matrices). */\n    private _transform;\n    /** Globe projection handling the Mercator-to-globe transition (globeness). */\n    private _projection;\n    /** Computes which tiles are visible on the globe at a given zoom. */\n    private _tileCovering;\n    /** Active globe interaction handler, or null if not attached. */\n    private _interaction;\n    /** Current animation handle, or null if idle. */\n    private _anim;\n    /** Whether this mode has been disposed. */\n    private _destroyed;\n    /** Stored markDirty callback for cluster click-triggered goTo. */\n    private _markDirty;\n    /** Stored onViewChange callback for cluster-triggered goTo. */\n    private _onViewChange;\n    /**\n     * Create a new Mode3D instance.\n     *\n     * Initializes the vertical perspective transform, globe projection,\n     * and tile covering. The projection's globeness is immediately synced\n     * to the initial zoom level.\n     *\n     * @param options - Initial view and terrain configuration.\n     */\n    constructor(options?: Mode3DOptions);\n    /**\n     * Expose the underlying vertical perspective transform for advanced use.\n     *\n     * Useful for computing extents, accessing raw view/projection matrices,\n     * or performing custom coordinate transformations.\n     */\n    get transform(): VerticalPerspectiveTransform;\n    /**\n     * Expose the underlying globe projection for advanced use.\n     *\n     * Provides access to the globeness factor and projection transition state.\n     */\n    get projection(): GlobeProjection;\n    /**\n     * Apply a partial view state update to the transform and projection.\n     *\n     * Center coordinates are in [lon, lat] (EPSG:4326). Zoom changes also\n     * update the globe projection's globeness factor. Pitch and bearing\n     * control the 3D camera orientation. The rotation field is silently\n     * ignored in 3D mode.\n     *\n     * @param state - Partial view state to apply.\n     * @see IViewMode.setState\n     */\n    setState(state: Partial<ViewState>): void;\n    /**\n     * Read the current view state.\n     *\n     * Returns the center in [lon, lat] (EPSG:4326), zoom, pitch, and bearing.\n     * Rotation is always 0 in 3D mode.\n     *\n     * @returns The current serializable view state.\n     * @see IViewMode.getState\n     */\n    getState(): ViewState;\n    /**\n     * Build the CameraState for the render engine's beginFrame() call.\n     *\n     * Provides the perspective view/projection matrices, camera position in\n     * 3D space, viewport dimensions, and globe-specific uniforms including\n     * the projection transition factor (globeness), clipping plane for\n     * back-face culling, globe radius (1.0 for unit sphere), and the flat\n     * view-projection matrix used for Mercator fallback.\n     *\n     * @returns The camera state suitable for the WebGPU render engine.\n     * @see IViewMode.getCameraState\n     */\n    getCameraState(): CameraState;\n    /**\n     * Update the viewport dimensions (e.g. on canvas resize).\n     *\n     * Propagates the new size to the underlying vertical perspective\n     * transform, which recalculates the projection matrix.\n     *\n     * @param width - New viewport width in pixels.\n     * @param height - New viewport height in pixels.\n     * @see IViewMode.setViewport\n     */\n    setViewport(width: number, height: number): void;\n    /**\n     * Animate the camera to a target view state.\n     *\n     * Uses ease-in-out (quadratic) interpolation over the specified duration.\n     * Interpolates center (lon/lat), zoom, pitch, and bearing simultaneously.\n     * The globe projection's globeness is updated on each animation frame\n     * to stay in sync with the interpolated zoom level.\n     *\n     * If duration is 0, the transition is applied immediately without animation.\n     * Only center, zoom, pitch, and bearing fields from the target are used;\n     * rotation is ignored in 3D mode.\n     *\n     * Any in-progress animation is cancelled before starting a new one.\n     *\n     * @param target - The navigation target describing the desired end state.\n     * @param markDirty - Callback to flag the view as needing a re-render.\n     * @param onViewChange - Callback to notify listeners of a view state change.\n     * @returns A promise that resolves when the animation completes or is cancelled.\n     * @throws Error if the mode has been disposed.\n     * @see IViewMode.goTo\n     */\n    goTo(target: GoToTarget, markDirty: () => void, onViewChange: () => void): Promise<void>;\n    /**\n     * Cancel any in-progress goTo animation.\n     *\n     * Clears the animation flag and cancels the pending setTimeout.\n     * The animation promise resolves on the next scheduled step.\n     *\n     * @see IViewMode.cancelAnimation\n     */\n    cancelAnimation(): void;\n    /**\n     * Render a single frame in 3D globe mode.\n     *\n     * Performs the following steps:\n     * 1. Updates the globe projection's globeness from the current zoom.\n     * 2. Classifies visible layers into tile (raster) and feature (vector) categories.\n     * 3. Computes tile coverage (globe-based or flat-based depending on globeness).\n     * 4. Draws atmosphere and pole caps behind tiles when in globe mode.\n     * 5. Converts raster tile extents from EPSG:3857 to Mercator [0..1] and\n     *    issues drawGlobeTile() calls.\n     * 6. Draws vector features (polygons, lines, points) in globe space.\n     *\n     * Called by the RenderLoop between beginFrame() and endFrame().\n     *\n     * @param ctx - Shared rendering resources (render engine, managers, caches).\n     * @see IViewMode.renderFrame\n     */\n    renderFrame(ctx: RenderFrameContext): void;\n    private _resolveActiveTerrainLayer;\n    private _computeClusterExtent3857;\n    private _fallbackClusterExtent3857;\n    /**\n     * Build WGSL shader source with globe-aware preamble for a custom shader layer.\n     *\n     * In 3D mode the preamble uses GlobeCameraUniforms (160 bytes) with both\n     * globe VP and flat VP matrices, projectionTransition, clippingPlane, and\n     * injects Mercator→sphere projection helper functions. The `projectMercator()`\n     * function is available for shaders to project raw EPSG:3857 positions onto\n     * the globe/flat surface depending on the current globeness.\n     */\n    private _buildCustomShaderSource;\n    /**\n     * Compute tile coverage for flat (Mercator) rendering at high zoom levels.\n     *\n     * Used when globeness < 0.5 (typically zoom >= 6). Calculates which\n     * XYZ tiles are visible by projecting the camera center to tile coordinates,\n     * then expanding by half the viewport in tile units. The pitch factor\n     * extends vertical coverage to account for the tilted perspective.\n     *\n     * @param targetZoom - The target zoom level (clamped to 0..22).\n     * @returns Array of `{ z, x, y }` tile coordinates that cover the viewport.\n     */\n    private _getTilesForFlat;\n    /**\n     * Attach orbit-style pointer and keyboard interaction to a DOM container.\n     *\n     * Creates a {@link GlobeInteraction} handler that translates mouse/touch/keyboard\n     * events into camera pan, zoom, pitch, and bearing operations. The onViewChange\n     * callback is wrapped to also update the globe projection's globeness on each\n     * interaction event. Pass `false` for the options parameter to skip attaching\n     * interaction entirely.\n     *\n     * @param container - The HTML element to listen for input events on.\n     * @param markDirty - Callback to flag the view as needing a re-render.\n     * @param onViewChange - Callback to notify listeners of a view state change.\n     * @param options - Interaction configuration, or `false` to disable.\n     * @see IViewMode.attachInteraction\n     */\n    attachInteraction(container: HTMLElement, markDirty: () => void, onViewChange: () => void, options?: Record<string, unknown> | false): void;\n    /**\n     * Convert screen pixel coordinates to geographic [longitude, latitude].\n     *\n     * Uses the current globeness factor to select the correct unprojection\n     * strategy:\n     * - globeness ≈ 1 (full globe): ray-sphere intersection via globe VP\n     * - globeness ≈ 0 (flat Mercator): inverse of flat VP matrix\n     * - in between: blends both results to match the shader's `mix()` blend\n     *\n     * The transition zone blend ensures that drawn features appear exactly\n     * where the user clicks, matching the forward projection used by the\n     * shader and by {@link toScreen}.\n     *\n     * @param screenX - Horizontal pixel position relative to the canvas.\n     * @param screenY - Vertical pixel position relative to the canvas.\n     * @returns Geographic coordinates as [longitude, latitude], or null if off-map.\n     * @see IViewMode.toMap\n     */\n    toMap(screenX: number, screenY: number): [number, number] | null;\n    /**\n     * Convert geographic coordinates to screen pixel position.\n     *\n     * Uses the current globeness factor to select the correct projection:\n     * - globeness ≈ 1: globe VP matrix (unit sphere)\n     * - globeness ≈ 0: flat Mercator VP matrix\n     * - in between: blend both clip-space positions (matching the shader)\n     *\n     * @param lon - Longitude in degrees.\n     * @param lat - Latitude in degrees.\n     * @returns Screen coordinates as [x, y] in pixels, or null if not visible.\n     * @see IViewMode.toScreen\n     */\n    toScreen(lon: number, lat: number): [number, number] | null;\n    /**\n     * Dispose of all mode-specific resources.\n     *\n     * Cancels any in-progress animation, destroys the globe interaction\n     * handler, and marks the mode as destroyed. Subsequent calls are no-ops.\n     *\n     * @see IViewMode.dispose\n     */\n    dispose(): void;\n}\n//# sourceMappingURL=Mode3D.d.ts.map","engine/modes/index.d.ts":"export { Mode2D } from './Mode2D';\nexport type { Mode2DOptions } from './Mode2D';\nexport { Mode3D } from './Mode3D';\nexport type { Mode3DOptions } from './Mode3D';\n//# sourceMappingURL=index.d.ts.map","engine/projections/ConvexVolume.d.ts":"/**\n * ConvexVolume — Globe tile bounding volume\n *\n * Küre üzerindeki bir tile yamacını saran convex polyhedron.\n * MapLibre'nin ConvexVolume yaklaşımı — AABB yerine 6-düzlem.\n *\n * Düzlemler:\n * - 4 kenar düzlemi: tile kenarları × küre merkezi (büyük daire kenarları)\n * - 1 dış düzlem: tile merkezinde küreye teğet\n * - 1 iç düzlem: tile'ın en yakın noktası (minZ)\n *\n * Hessian normal form: ax + by + cz + d ≥ 0 → inside.\n */\nimport type { FrustumPlane } from '../FrustumCuller';\nexport interface ConvexVolumePlane {\n    a: number;\n    b: number;\n    c: number;\n    d: number;\n}\nexport declare class ConvexVolume {\n    /** Bounding planes — inward-pointing normals. Point is inside if dot(n, p) + d ≥ 0 for all planes. */\n    readonly planes: readonly ConvexVolumePlane[];\n    /** Bounding sphere center (for quick rejection) */\n    readonly sphereCenter: [number, number, number];\n    /** Bounding sphere radius */\n    readonly sphereRadius: number;\n    constructor(planes: ConvexVolumePlane[], sphereCenter: [number, number, number], sphereRadius: number);\n    /**\n     * Tile'ın Mercator koordinatlarından ConvexVolume oluştur.\n     *\n     * @param z - zoom level\n     * @param x - tile column\n     * @param y - tile row\n     */\n    static fromTile(z: number, x: number, y: number): ConvexVolume;\n    /**\n     * Test: ConvexVolume frustum ile kesişiyor mu?\n     *\n     * Quick sphere test + detailed plane-vertex test.\n     * Returns true if potentially visible.\n     */\n    intersectsFrustum(frustumPlanes: readonly FrustumPlane[]): boolean;\n    /**\n     * Test: Tile horizon clipping plane ile görünür mü?\n     *\n     * Clipping plane format: [A, B, C, D] where Ax + By + Cz + D ≥ 0 → visible.\n     * Returns true if any part of the tile is on the visible side.\n     */\n    intersectsClippingPlane(plane: [number, number, number, number]): boolean;\n    /**\n     * Combined visibility test: frustum + clipping plane.\n     */\n    isVisible(frustumPlanes: readonly FrustumPlane[], clippingPlane: [number, number, number, number]): boolean;\n}\n//# sourceMappingURL=ConvexVolume.d.ts.map","engine/projections/GlobeProjection.d.ts":"/**\n * Globe Projection (Dual-Projection Wrapper)\n *\n * MercatorProjection + globe transition logic.\n * `globeness` faktörü (0=mercator, 1=globe) zoom seviyesine göre otomatik geçiş yapar.\n *\n * MapLibre'nin GlobeProjection yaklaşımını takip eder:\n * - zoom < TRANSITION_ZOOM_LOW  → globeness = 1 (tam globe)\n * - zoom > TRANSITION_ZOOM_HIGH → globeness = 0 (tam mercator)\n * - arada → smooth interpolation\n */\nimport type { IProjection } from './IProjection';\nexport declare class GlobeProjection implements IProjection {\n    readonly name = \"globe\";\n    /** Zoom < bu değer → tam globe */\n    static readonly TRANSITION_ZOOM_LOW = 5;\n    /** Zoom > bu değer → tam mercator */\n    static readonly TRANSITION_ZOOM_HIGH = 6;\n    private _globeness;\n    private readonly _mercator;\n    constructor();\n    /** Mevcut globeness değeri (0=mercator, 1=globe) */\n    get globeness(): number;\n    /** Globe modunda horizontal wrap yok */\n    get wrapsHorizontally(): boolean;\n    /**\n     * Zoom seviyesinden globeness hesapla.\n     * smooth interpolation: low=1, high=0, arada cosine easing.\n     */\n    static globenessFromZoom(zoom: number): number;\n    /** Globeness değerini doğrudan ayarla (0-1) */\n    setGlobeness(value: number): void;\n    /** Zoom seviyesinden globeness güncelle */\n    updateFromZoom(zoom: number): void;\n    /**\n     * lon/lat → normalized Mercator (0..1).\n     * Globe modunda da aynı Mercator coordinates — shader'da sphere'e dönüşüm yapılır.\n     */\n    project(lon: number, lat: number): [number, number];\n    /**\n     * Normalized Mercator (0..1) → lon/lat.\n     */\n    unproject(x: number, y: number): [number, number];\n    /**\n     * Mercator (0..1) → Angular coordinates (radians).\n     * Shader'daki mercatorToAngular ile aynı:\n     * - x → longitude: x * 2π - π → [-π, π] → shifted: x*2π+π (MapLibre convention)\n     * - y → latitude: 2*atan(exp(π - y*2π)) - π/2\n     */\n    static mercatorToAngular(mx: number, my: number): [number, number];\n    /**\n     * Angular (radians) → unit sphere (3D).\n     * Shader'daki angularToSphere ile aynı:\n     * - x = cos(lat) * sin(lon)\n     * - y = sin(lat)\n     * - z = cos(lat) * cos(lon)\n     */\n    static angularToSphere(lon: number, lat: number): [number, number, number];\n    /**\n     * lon/lat (derece) → unit sphere (3D).\n     * Convenience: project → angular → sphere.\n     */\n    static lonLatToSphere(lon: number, lat: number): [number, number, number];\n}\n//# sourceMappingURL=GlobeProjection.d.ts.map","engine/projections/GlobeTileCovering.d.ts":"/**\n * GlobeTileCovering — Globe view için tile seçimi\n *\n * MapLibre'nin GlobeCoveringTilesDetailsProvider yaklaşımı:\n * 1. Root tile'lardan başla (z=0 veya z=1)\n * 2. Her tile için visibility testi (frustum + clipping plane)\n * 3. Visible tile'ları hedef zoom'a kadar recursive subdivide et\n * 4. Antimeridian wrapping, kutup tile'ları, variable zoom\n *\n * İki katmanlı test:\n * - Frustum intersection (kamera görüş alanında mı?)\n * - Clipping plane intersection (ufuk çizgisinin bu tarafında mı?)\n */\nimport type { VerticalPerspectiveTransform } from './VerticalPerspectiveTransform';\n/** Tile coordinate with zoom/x/y */\nexport interface GlobeTileCoord {\n    z: number;\n    x: number;\n    y: number;\n}\nexport interface GlobeTileCoveringOptions {\n    /** Maximum zoom level to subdivide to (default: 22) */\n    maxZoom?: number;\n    /** Minimum zoom to start from (default: 0) */\n    minZoom?: number;\n}\nexport declare class GlobeTileCovering {\n    private _maxZoom;\n    private _minZoom;\n    constructor(options?: GlobeTileCoveringOptions);\n    /**\n     * Globe kamerası için gerekli tile'ları hesapla.\n     *\n     * @param transform - Globe kamerası (VP matris, clipping plane)\n     * @param targetZoom - Hedef zoom seviyesi (genellikle floor(transform.zoom))\n     * @returns Visible tile koordinatları\n     */\n    getTilesForGlobe(transform: VerticalPerspectiveTransform, targetZoom: number): GlobeTileCoord[];\n    /**\n     * Recursive subdivision.\n     * Tile'ı visible ise ve hedef zoom'a ulaşmadıysa 4 çocuğa böl.\n     */\n    private _subdivide;\n    /**\n     * Generate all tiles at a given zoom level.\n     */\n    private _tilesAtZoom;\n    /**\n     * Get the tile coordinate that contains a given lon/lat at the specified zoom.\n     */\n    static tileForLonLat(lon: number, lat: number, zoom: number): GlobeTileCoord;\n    /**\n     * Get the lon/lat bounds of a tile.\n     */\n    static tileBounds(z: number, x: number, y: number): {\n        west: number;\n        east: number;\n        north: number;\n        south: number;\n    };\n}\n//# sourceMappingURL=GlobeTileCovering.d.ts.map","engine/projections/IProjection.d.ts":"/**\n * Projection Interface\n *\n * Dual-projection model'in temel sözleşmesi.\n * MercatorProjection ve GlobeProjection bu interface'i implemente eder.\n */\nexport interface IProjection {\n    /** Projeksiyon adı */\n    readonly name: string;\n    /**\n     * lon/lat (derece) → normalized koordinatlar (0..1).\n     * Mercator: Web Mercator 0..1 space\n     * Globe: aynı (shader'da sphere'e dönüşüm yapılır)\n     */\n    project(lon: number, lat: number): [number, number];\n    /**\n     * Normalized koordinat (0..1) → lon/lat (derece).\n     */\n    unproject(x: number, y: number): [number, number];\n    /** Yatayda sarma (antimeridian wrapping) yapıyor mu? */\n    readonly wrapsHorizontally: boolean;\n}\n//# sourceMappingURL=IProjection.d.ts.map","engine/projections/MercatorProjection.d.ts":"/**\n * Mercator Projection\n *\n * WGS84 lon/lat → normalized Mercator (0..1) space.\n * MapLibre'nin MercatorCoordinate ile uyumlu.\n *\n * Formül: y = (180 - (180/π)*ln(tan(π/4 + lat*π/360))) / 360\n *       = 0.5 - ln(tan(π/4 + latRad/2)) / (2π)\n */\nimport type { IProjection } from './IProjection';\nexport declare class MercatorProjection implements IProjection {\n    readonly name = \"mercator\";\n    readonly wrapsHorizontally = true;\n    /**\n     * lon/lat (derece) → normalized Mercator (0..1).\n     * x: 0 = -180°, 0.5 = 0°, 1 = +180°\n     * y: 0 = +85.05°, 1 = -85.05° (y aşağı doğru artar — tile convention)\n     */\n    project(lon: number, lat: number): [number, number];\n    /**\n     * Normalized Mercator (0..1) → lon/lat (derece).\n     * Inverse: latRad = 2*atan(exp((0.5-y)*2π)) - π/2\n     */\n    unproject(x: number, y: number): [number, number];\n}\n//# sourceMappingURL=MercatorProjection.d.ts.map","engine/projections/VerticalPerspectiveTransform.d.ts":"/**\n * Vertical Perspective Transform\n *\n * MapLibre'nin VerticalPerspectiveTransform kamerasının TypeScript implementasyonu.\n * Unit sphere (radius=1) üzerinde globe render kamerası.\n *\n * Matris zinciri:\n *   perspective(fov, aspect, near, far)\n *     × translate(0, 0, -cameraToCenterDistance)\n *     × rotateX(-pitch)\n *     × rotateZ(bearing)\n *     × translate(0, 0, -1)        // globe center offset (radius=1)\n *     × rotateX(latitude)\n *     × rotateY(-longitude)\n *\n * Koordinat sistemi: Y-up, right-handed.\n */\nexport interface GlobeCameraParams {\n    center: [number, number];\n    zoom: number;\n    pitch: number;\n    bearing: number;\n    fov?: number;\n    viewportWidth: number;\n    viewportHeight: number;\n}\nexport declare class VerticalPerspectiveTransform {\n    private _center;\n    private _zoom;\n    private _pitch;\n    private _bearing;\n    private _fov;\n    private _viewportWidth;\n    private _viewportHeight;\n    private _viewMatrix;\n    private _projectionMatrix;\n    private _viewProjectionMatrix;\n    private _flatViewProjectionMatrix;\n    private _cameraPosition;\n    private _clippingPlane;\n    private _dirty;\n    constructor(params?: Partial<GlobeCameraParams>);\n    get center(): [number, number];\n    get zoom(): number;\n    get pitch(): number;\n    get bearing(): number;\n    get fov(): number;\n    get viewportWidth(): number;\n    get viewportHeight(): number;\n    get viewMatrix(): Float32Array;\n    get projectionMatrix(): Float32Array;\n    get viewProjectionMatrix(): Float32Array;\n    /** Flat Mercator VP matrix — maps Mercator [0..1] → NDC for zoom >= 6 rendering */\n    get flatViewProjectionMatrix(): Float32Array;\n    get cameraPosition(): [number, number, number];\n    /**\n     * Globe radius in pixels.\n     * MapLibre: worldSize / (2π) at equator.\n     * worldSize = tileSize * 2^zoom (tileSize=512)\n     */\n    get globeRadius(): number;\n    /**\n     * Camera-to-center distance in globe-radius units.\n     * Based on zoom/fov: distance = 1 / (2 * tan(fov/2) * globeScale)\n     * where globeScale relates zoom to sphere coverage.\n     */\n    get cameraToCenterDistance(): number;\n    /** Camera distance in Mercator [0..1] space — used for flat-path camera position. */\n    get mercatorCameraDistance(): number;\n    setCenter(lon: number, lat: number): void;\n    setZoom(zoom: number): void;\n    setPitch(pitch: number): void;\n    setBearing(bearing: number): void;\n    setViewport(width: number, height: number): void;\n    setFov(fov: number): void;\n    private _ensureClean;\n    updateMatrices(): void;\n    /**\n     * Near/far hesapla.\n     * dist = cameraToCenterDistance = kamera-yüzey mesafesi (unit sphere).\n     * Near: en yakın yüzey noktası dist kadar uzak → dist * 0.5 güvenli margin.\n     * Far: kamera → globe arkası = dist + 2 (çap).\n     */\n    computeNearFar(): [number, number];\n    /**\n     * Flat Mercator VP matrix hesapla — perspective + pitch + bearing.\n     * Maps Mercator [0..1] input to clip space.\n     * Used for zoom >= 6 where the globe VP camera asymptotically stalls.\n     *\n     * Matrix chain (right-to-left application):\n     *   Perspective(fov, aspect, near, far)\n     *     × Translate(0, 0, -cameraDist)    // camera distance in merc space\n     *     × RotateX(-pitch)                 // tilt view\n     *     × Scale(1, -1, 1)                 // flip Y (merc Y↓ → view Y↑)\n     *     × RotateZ(bearing)                // compass rotation\n     *     × Translate(-cx, -cy, 0)          // center to origin\n     *\n     * At pitch=0 this reduces to the equivalent orthographic projection:\n     *   ndcX = (2 * worldSize / vpWidth) * (mercX - cx)\n     *   ndcY = (2 * worldSize / vpHeight) * (cy - mercY)\n     */\n    private _computeFlatViewProjection;\n    /**\n     * Camera position hesapla (unit sphere space).\n     * Inverse of view transform, starting from (0,0,0) → apply inverse chain.\n     */\n    private _computeCameraPosition;\n    /**\n     * Clipping plane hesapla (horizon occlusion).\n     * Plane: dot(surfacePoint, normal) + d ≥ 0 → visible.\n     */\n    private _computeClippingPlane;\n    /**\n     * Clipping plane getter (horizon occlusion).\n     * [A, B, C, D] where Ax + By + Cz + D ≥ 0 → visible.\n     */\n    getClippingPlane(): [number, number, number, number];\n    /**\n     * Screen coordinates → lon/lat.\n     * Ray-sphere intersection.\n     * Returns null if ray misses globe.\n     */\n    screenToLonLat(sx: number, sy: number): [number, number] | null;\n    /**\n     * lon/lat → screen coordinates (globe projection).\n     * Returns null if point is on the back side of the globe.\n     */\n    lonLatToScreen(lon: number, lat: number): [number, number] | null;\n    /**\n     * lon/lat → screen coordinates using flat Mercator VP matrix.\n     * Used when globeness ≈ 0 (high zoom) where the shader renders with flatViewProjection.\n     * Maps lon/lat → Mercator [0..1] → flatVP → clip → NDC → screen.\n     */\n    lonLatToScreenFlat(lon: number, lat: number): [number, number] | null;\n    /**\n     * Screen coordinates → lon/lat using flat Mercator VP matrix inverse.\n     * Used when globeness ≈ 0 (high zoom) where the shader renders with flatViewProjection.\n     *\n     * Solves: flatVP * (mx, my, 0, 1) = (ndcX * w, ndcY * w, _, w)\n     * which gives a 2×2 linear system via Cramer's rule.\n     */\n    screenToLonLatFlat(sx: number, sy: number): [number, number] | null;\n}\n//# sourceMappingURL=VerticalPerspectiveTransform.d.ts.map","engine/projections/index.d.ts":"export type { IProjection } from './IProjection';\nexport { MercatorProjection } from './MercatorProjection';\nexport { GlobeProjection } from './GlobeProjection';\nexport { VerticalPerspectiveTransform } from './VerticalPerspectiveTransform';\nexport type { GlobeCameraParams } from './VerticalPerspectiveTransform';\nexport { ConvexVolume } from './ConvexVolume';\nexport type { ConvexVolumePlane } from './ConvexVolume';\nexport { GlobeTileCovering } from './GlobeTileCovering';\nexport type { GlobeTileCoord, GlobeTileCoveringOptions } from './GlobeTileCovering';\n//# sourceMappingURL=index.d.ts.map","engine/svg-utils.d.ts":"/**\n * svg-utils — SVG rasterization utilities for icon symbology.\n */\n/**\n * Rasterize SVG markup to an ImageBitmap.\n *\n * Uses Blob + createImageBitmap for browser-native SVG rendering.\n * Throws if the environment does not support createImageBitmap.\n *\n * @param svgMarkup - Raw SVG string (e.g. `<svg>...</svg>`)\n * @param width - Target width in pixels\n * @param height - Target height in pixels\n * @returns Rasterized ImageBitmap at the requested size\n */\nexport declare function svgToImageBitmap(svgMarkup: string, width: number, height: number): Promise<ImageBitmap>;\n//# sourceMappingURL=svg-utils.d.ts.map","entity/Entity.d.ts":"/**\n * Entity — High-level 3D scene object (CesiumJS Entity equivalent).\n *\n * An entity combines a position, orientation, and one or more graphics\n * components (billboard, label, polyline, polygon, model, box, etc.).\n * Properties can be static or time-dynamic via the IProperty system.\n */\nimport type { IProperty } from '../temporal/Property';\nimport type { JulianDate } from '../temporal/JulianDate';\nexport interface BillboardGraphics {\n    image?: string;\n    scale?: number | IProperty<number>;\n    color?: [number, number, number, number];\n    rotation?: number | IProperty<number>;\n    width?: number;\n    height?: number;\n    show?: boolean | IProperty<boolean>;\n}\nexport interface LabelGraphics {\n    text: string | IProperty<string>;\n    font?: string;\n    fillColor?: [number, number, number, number];\n    outlineColor?: [number, number, number, number];\n    outlineWidth?: number;\n    scale?: number;\n    show?: boolean | IProperty<boolean>;\n}\nexport interface PointGraphics {\n    pixelSize?: number;\n    color?: [number, number, number, number];\n    outlineColor?: [number, number, number, number];\n    outlineWidth?: number;\n    show?: boolean | IProperty<boolean>;\n}\nexport interface PolylineGraphics {\n    positions: [number, number, number][] | IProperty<[number, number, number][]>;\n    width?: number;\n    color?: [number, number, number, number];\n    dashPattern?: number[];\n    clampToGround?: boolean;\n    show?: boolean | IProperty<boolean>;\n}\nexport interface PolygonGraphics {\n    hierarchy: [number, number, number][] | IProperty<[number, number, number][]>;\n    color?: [number, number, number, number];\n    outlineColor?: [number, number, number, number];\n    outlineWidth?: number;\n    extrudedHeight?: number | IProperty<number>;\n    clampToGround?: boolean;\n    show?: boolean | IProperty<boolean>;\n}\nexport interface ModelGraphics {\n    uri: string;\n    scale?: number | IProperty<number>;\n    heading?: number | IProperty<number>;\n    pitch?: number | IProperty<number>;\n    roll?: number | IProperty<number>;\n    tintColor?: [number, number, number, number];\n    show?: boolean | IProperty<boolean>;\n}\nexport interface BoxGraphics {\n    dimensions: [number, number, number];\n    color?: [number, number, number, number];\n    outlineColor?: [number, number, number, number];\n    show?: boolean | IProperty<boolean>;\n}\nexport interface CylinderGraphics {\n    topRadius: number;\n    bottomRadius: number;\n    length: number;\n    color?: [number, number, number, number];\n    show?: boolean | IProperty<boolean>;\n}\nexport interface WallGraphics {\n    positions: [number, number][];\n    maximumHeights: number[];\n    minimumHeights?: number[];\n    color?: [number, number, number, number];\n    show?: boolean | IProperty<boolean>;\n}\nexport interface CorridorGraphics {\n    positions: [number, number][];\n    width: number;\n    height?: number;\n    color?: [number, number, number, number];\n    show?: boolean | IProperty<boolean>;\n}\nexport interface EllipsoidGraphics {\n    radii: [number, number, number];\n    color?: [number, number, number, number];\n    show?: boolean | IProperty<boolean>;\n}\nexport interface EntityOptions {\n    id?: string;\n    name?: string;\n    position?: [number, number, number] | IProperty<number[]>;\n    show?: boolean;\n    properties?: Record<string, unknown>;\n    /** Time availability as ISO 8601 interval. */\n    availability?: string;\n    billboard?: BillboardGraphics;\n    label?: LabelGraphics;\n    point?: PointGraphics;\n    polyline?: PolylineGraphics;\n    polygon?: PolygonGraphics;\n    model?: ModelGraphics;\n    box?: BoxGraphics;\n    cylinder?: CylinderGraphics;\n    wall?: WallGraphics;\n    corridor?: CorridorGraphics;\n    ellipsoid?: EllipsoidGraphics;\n}\nexport declare class Entity {\n    readonly id: string;\n    name: string;\n    show: boolean;\n    position: [number, number, number] | IProperty<number[]> | null;\n    properties: Record<string, unknown>;\n    availability?: string;\n    billboard?: BillboardGraphics;\n    label?: LabelGraphics;\n    point?: PointGraphics;\n    polyline?: PolylineGraphics;\n    polygon?: PolygonGraphics;\n    model?: ModelGraphics;\n    box?: BoxGraphics;\n    cylinder?: CylinderGraphics;\n    wall?: WallGraphics;\n    corridor?: CorridorGraphics;\n    ellipsoid?: EllipsoidGraphics;\n    constructor(options?: EntityOptions);\n    /**\n     * Resolve the position at a given time.\n     * Returns [lon, lat, alt] or null if no position.\n     */\n    getPosition(time?: JulianDate): [number, number, number] | null;\n    /** Check if the entity is visible at a given time (considering availability). */\n    isAvailable(time?: JulianDate): boolean;\n}\n//# sourceMappingURL=Entity.d.ts.map","entity/EntityCollection.d.ts":"/**\n * EntityCollection — Managed collection of entities with change events.\n *\n * CesiumJS EntityCollection equivalent.\n */\nimport { Entity } from './Entity';\nexport interface EntityCollectionEvents {\n    [key: string]: unknown;\n    'add': {\n        entity: Entity;\n    };\n    'remove': {\n        entity: Entity;\n    };\n    'change': {\n        entity: Entity;\n    };\n    'reset': void;\n}\nexport declare class EntityCollection {\n    private _entities;\n    private _events;\n    /** Add an entity to the collection. */\n    add(entityOrOptions: Entity | ConstructorParameters<typeof Entity>[0]): Entity;\n    /** Remove an entity by ID or reference. */\n    remove(entityOrId: Entity | string): boolean;\n    /** Get an entity by ID. */\n    getById(id: string): Entity | undefined;\n    /** Check if an entity exists. */\n    contains(entityOrId: Entity | string): boolean;\n    /** Get all entities as an array. */\n    get values(): Entity[];\n    /** Number of entities. */\n    get length(): number;\n    /** Remove all entities. */\n    removeAll(): void;\n    /** Subscribe to collection events. */\n    on<K extends keyof EntityCollectionEvents>(event: K, handler: (data: EntityCollectionEvents[K]) => void): void;\n    /** Unsubscribe. */\n    off<K extends keyof EntityCollectionEvents>(event: K, handler: (data: EntityCollectionEvents[K]) => void): void;\n    /** Iterate all entities. */\n    [Symbol.iterator](): IterableIterator<Entity>;\n}\n//# sourceMappingURL=EntityCollection.d.ts.map","entity/index.d.ts":"export { Entity } from './Entity';\nexport type { EntityOptions, BillboardGraphics, LabelGraphics, PointGraphics, PolylineGraphics, PolygonGraphics, ModelGraphics, BoxGraphics, CylinderGraphics, WallGraphics, CorridorGraphics, EllipsoidGraphics, } from './Entity';\nexport { EntityCollection } from './EntityCollection';\nexport type { EntityCollectionEvents } from './EntityCollection';\n//# sourceMappingURL=index.d.ts.map","errors.d.ts":"/**\n * mapgpu Error Model\n *\n * Tüm async işlemler typed error döndürür.\n * Discriminated union pattern ile hata tipi kontrolü yapılır.\n */\nexport type MapError = {\n    kind: 'layer-load-failed';\n    layerId: string;\n    cause: Error;\n} | {\n    kind: 'shader-compile-failed';\n    pipeline: string;\n    log: string;\n} | {\n    kind: 'wasm-init-failed';\n    cause: Error;\n} | {\n    kind: 'webgpu-not-supported';\n    userAgent: string;\n} | {\n    kind: 'webgpu-device-lost';\n    reason: string;\n    message: string;\n} | {\n    kind: 'service-unavailable';\n    url: string;\n    status: number;\n} | {\n    kind: 'cors-blocked';\n    url: string;\n} | {\n    kind: 'crs-unsupported';\n    crs: string;\n} | {\n    kind: 'terrain-unavailable';\n} | {\n    kind: 'los-out-of-bounds';\n} | {\n    kind: 'unknown';\n    cause: Error;\n};\nexport declare class MapGpuError extends Error {\n    readonly error: MapError;\n    constructor(error: MapError);\n}\n//# sourceMappingURL=errors.d.ts.map","events.d.ts":"/**\n * Typed Event Bus\n *\n * Harita, katman ve view olaylarını yönetir.\n * Tüm event'ler tiplenmiştir — runtime hata riski minimumdur.\n */\ntype EventHandler<T> = (data: T) => void;\nexport declare class EventBus<EventMap extends Record<string, unknown>> {\n    private listeners;\n    on<K extends keyof EventMap>(event: K, handler: EventHandler<EventMap[K]>): void;\n    off<K extends keyof EventMap>(event: K, handler: EventHandler<EventMap[K]>): void;\n    emit<K extends keyof EventMap>(event: K, data: EventMap[K]): void;\n    once<K extends keyof EventMap>(event: K, handler: EventHandler<EventMap[K]>): void;\n    removeAll(event?: keyof EventMap): void;\n}\nexport {};\n//# sourceMappingURL=events.d.ts.map","geometry/Geometry3D.d.ts":"/**\n * Named 3D Geometry Generators — Box, Cylinder, Sphere, Wall, Corridor.\n *\n * CesiumJS geometry type equivalents. Each generates position + normal + index\n * arrays ready for GPU upload via the ExtrusionPipeline or a dedicated 3D pipeline.\n */\nexport interface GeneratedMesh {\n    /** Vertex positions [x, y, z, ...]. */\n    positions: Float32Array;\n    /** Vertex normals [nx, ny, nz, ...]. */\n    normals: Float32Array;\n    /** Triangle indices. */\n    indices: Uint32Array;\n    /** Number of vertices. */\n    vertexCount: number;\n}\n/**\n * Generate a box (cuboid) mesh centered at origin.\n *\n * @param width - X dimension (meters).\n * @param height - Y dimension (meters).\n * @param depth - Z dimension (meters).\n */\nexport declare function createBoxGeometry(width?: number, height?: number, depth?: number): GeneratedMesh;\n/**\n * Generate a cylinder mesh centered at origin, axis along Y.\n *\n * @param radiusTop - Top radius (0 for cone).\n * @param radiusBottom - Bottom radius.\n * @param height - Height along Y axis.\n * @param segments - Number of radial segments (default 32).\n */\nexport declare function createCylinderGeometry(radiusTop?: number, radiusBottom?: number, height?: number, segments?: number): GeneratedMesh;\n/**\n * Generate a UV sphere mesh centered at origin.\n *\n * @param radius - Sphere radius.\n * @param widthSegments - Horizontal segments (default 32).\n * @param heightSegments - Vertical segments (default 16).\n */\nexport declare function createSphereGeometry(radius?: number, widthSegments?: number, heightSegments?: number): GeneratedMesh;\n/**\n * Generate a hemisphere mesh (upper half of sphere + flat bottom cap).\n * Y=0 at base, Y=radius at top. Smooth normals on dome, flat normal on base.\n *\n * @param radius - Hemisphere radius.\n * @param widthSegments - Horizontal segments (default 32).\n * @param heightSegments - Vertical segments for dome (default 12).\n */\nexport declare function createHemisphereGeometry(radius?: number, widthSegments?: number, heightSegments?: number): GeneratedMesh;\n/**\n * Generate a vertical wall from a polyline (ground positions + height).\n *\n * Creates a strip of quads along the polyline, from ground to the given height.\n *\n * @param positions - Ground-level [x, z] pairs (2D footprint).\n * @param height - Wall height.\n * @param minHeight - Optional base height (default 0).\n */\nexport declare function createWallGeometry(positions: [number, number][], height: number, minHeight?: number): GeneratedMesh;\n/**\n * Generate a corridor (swept path with width) from a polyline.\n *\n * Creates a flat ribbon following the polyline path, offset by half-width\n * on each side. Optionally extruded to a given height.\n *\n * @param positions - Center-line [x, z] pairs (2D path).\n * @param width - Corridor width in meters.\n * @param height - Optional extrusion height (0 = flat ribbon).\n */\nexport declare function createCorridorGeometry(positions: [number, number][], width: number, height?: number): GeneratedMesh;\n//# sourceMappingURL=Geometry3D.d.ts.map","geometry/LatLngBounds.d.ts":"/**\n * LatLngBounds — Immutable geographic bounds class (Leaflet L.LatLngBounds equivalent).\n *\n * Represents a rectangular area in geographic coordinates (EPSG:4326).\n */\nexport declare class LatLngBounds {\n    readonly minLon: number;\n    readonly minLat: number;\n    readonly maxLon: number;\n    readonly maxLat: number;\n    constructor(southwest: [number, number], northeast: [number, number]);\n    /** Create from a flat array [minLon, minLat, maxLon, maxLat]. */\n    static fromArray(arr: [number, number, number, number]): LatLngBounds;\n    /** Create bounds that contain all given points. */\n    static fromPoints(points: [number, number][]): LatLngBounds;\n    /** Southwest corner [lon, lat]. */\n    get southWest(): [number, number];\n    /** Northeast corner [lon, lat]. */\n    get northEast(): [number, number];\n    /** Center point [lon, lat]. */\n    get center(): [number, number];\n    /** Width in degrees. */\n    get width(): number;\n    /** Height in degrees. */\n    get height(): number;\n    /** Check if bounds are valid (non-zero area). */\n    get isValid(): boolean;\n    /** Check if a point is inside the bounds. */\n    contains(point: [number, number]): boolean;\n    /** Check if another bounds is fully inside this bounds. */\n    containsBounds(other: LatLngBounds): boolean;\n    /** Check if another bounds intersects this bounds. */\n    intersects(other: LatLngBounds): boolean;\n    /** Return a new bounds expanded to include the given point. */\n    extend(point: [number, number]): LatLngBounds;\n    /** Return a new bounds that is the union of this and another. */\n    union(other: LatLngBounds): LatLngBounds;\n    /** Return a new bounds padded by a ratio (0.1 = 10% padding). */\n    pad(ratio: number): LatLngBounds;\n    /** Check equality with another bounds. */\n    equals(other: LatLngBounds): boolean;\n    /** Convert to flat array [minLon, minLat, maxLon, maxLat]. */\n    toArray(): [number, number, number, number];\n    toString(): string;\n}\n//# sourceMappingURL=LatLngBounds.d.ts.map","geometry/generateGLB.d.ts":"/**\n * generateGLB — Create a minimal binary glTF 2.0 (GLB) from raw mesh data.\n *\n * Takes positions + normals + indices and produces a valid GLB ArrayBuffer\n * that can be loaded via `renderEngine.loadModel()`.\n *\n * GLB format:\n *   Header (12 bytes): magic(4) + version(4) + totalLength(4)\n *   JSON chunk: { asset, meshes, accessors, bufferViews, buffers }\n *   BIN chunk: interleaved vertices [pos3+norm3+uv2] + indices (uint32)\n */\nimport type { GeneratedMesh } from './Geometry3D';\n/**\n * Generate a GLB ArrayBuffer from raw mesh data.\n *\n * @param mesh - GeneratedMesh with positions, normals, and indices.\n * @param baseColor - Optional base color [r, g, b, a] 0-1. Default: white.\n * @returns GLB ArrayBuffer ready for `renderEngine.loadModel()`.\n */\nexport declare function generateGLB(mesh: GeneratedMesh, baseColor?: [number, number, number, number]): ArrayBuffer;\n//# sourceMappingURL=generateGLB.d.ts.map","index.d.ts":"export type * from './interfaces/index';\nexport { DEPTH_STANDARD, DEPTH_REVERSED_Z, isTileLayer, isFeatureLayer, isCustomShaderLayer, isTerrainLayer, isClusterLayer, resolveGlobeEffects, FOG_WGSL_SNIPPET, NIGHT_BLEND_WGSL_SNIPPET, WATER_SPECULAR_WGSL_SNIPPET, } from './interfaces/index';\nexport { SimpleRenderer, UniqueValueRenderer, ClassBreaksRenderer, CallbackRenderer, } from './renderers/index';\nexport type { UniqueValueInfo, UniqueValueRendererOptions, ClassBreakInfo, ClassBreaksRendererOptions, CallbackRendererFn, } from './renderers/index';\nexport { MapGpuError } from './errors';\nexport type { MapError } from './errors';\nexport { EventBus } from './events';\nexport { UnitManager } from './units/index';\nexport type { DistanceUnit, AreaUnit, CoordinateFormat, UnitManagerOptions, UnitManagerEvents, } from './units/index';\nexport { lonLatToMercator, mercatorToLonLat, EARTH_RADIUS, MAX_LAT, MapView, GameMap, CameraController2D, LayerManager, TileScheduler, RenderLoop, WorkerPool, ResourceManager, AnimationManager, Easing, CommandSystem, Diagnostics, SceneGraph, SceneNode, FrustumCuller, extractFrustumPlanes, testAABBFrustum, testSphereFrustum, TileManager, TerrainTileManager, InteractionHandler, GeometryConverter, earcut, ViewCore, Mode2D, Mode3D, VectorBufferCache, DEFAULT_POINT_SYMBOL, DEFAULT_LINE_SYMBOL, DEFAULT_POLYGON_SYMBOL, GlobeInteraction, ToolManager, MercatorProjection, GlobeProjection, VerticalPerspectiveTransform, ConvexVolume, GlobeTileCovering, createCircleGeometry, createRangeRings, } from './engine/index';\nexport type { MapViewOptions, MapViewEvents, GoToTarget, MapEvents, CameraController2DOptions, ViewState, LayerManagerEvents, TileCoord, TileWithPriority, TileSchedulerOptions, FrameCallback, RenderLoopOptions, FrameStats, WorkerRequest, WorkerResponse, IWorker, WorkerFactory, WorkerPoolOptions, ResourceDescriptor, MemoryUsage, ResourceManagerOptions, EasingFunction, EasingName, AnimationHandle, AnimateOptions, ICommand, CommandSystemEvents, CommandSystemOptions, DiagnosticStats, DiagnosticsOptions, SceneNodeType, SceneNodeTransform, SceneNodeOptions, TraverseCallback, FrustumPlane, FrustumPlanes, AABB, IntersectResult, TileManagerOptions, TileFetcher, TileSourceInfo, TerrainTileCoord, TerrainReadyHeightTile, TerrainReadyHillshadeTile, TerrainTileManagerOptions, InteractionHandlerOptions, PointVertexData, LineVertexData, PolygonVertexData, ModelInstanceData, IViewMode, ModeViewState, RenderFrameContext, Mode2DOptions, Mode3DOptions, VectorBufferEntry, GlobeInteractionOptions, IProjection, GlobeCameraParams, ConvexVolumePlane, GlobeTileCoord, GlobeTileCoveringOptions, ITool, ToolState, ToolCursor, ToolContext, ToolPointerEvent, IPreviewLayer, ToolEvents, ToolManagerOptions, MeasurementResult, } from './engine/index';\nexport { LatLngBounds } from './geometry/LatLngBounds';\nexport { createBoxGeometry, createCylinderGeometry, createSphereGeometry, createHemisphereGeometry, createWallGeometry, createCorridorGeometry, } from './geometry/Geometry3D';\nexport type { GeneratedMesh } from './geometry/Geometry3D';\nexport { generateGLB } from './geometry/generateGLB';\nexport { JulianDate, Clock, ConstantProperty, SampledProperty, CallbackProperty, TimeIntervalCollectionProperty, } from './temporal/index';\nexport type { ClockRange, ClockStep, ClockEvents, ClockOptions, IProperty, InterpolationType } from './temporal/index';\nexport { simplify } from './utils/simplify';\n//# sourceMappingURL=index.d.ts.map","interfaces/IAnalysis.d.ts":"/**\n * Analysis Interface Contract\n *\n * LOS, buffer, elevation query gibi analiz fonksiyonlarının sözleşmesi.\n * Analysis Agent bu interface'i implement eder.\n */\nimport type { LosResult } from './IWasmCore';\nexport interface LosParams {\n    /** Observer noktası: [lon, lat, elevation?] */\n    observer: [number, number, number?];\n    /** Hedef nokta: [lon, lat, elevation?] */\n    target: [number, number, number?];\n    /** Observer yükseklik ofseti (metre, terrain üstü) */\n    observerOffset?: number;\n    /** Target yükseklik ofseti (metre, terrain üstü) */\n    targetOffset?: number;\n    /** Terrain örnekleme sayısı (varsayılan: 512) */\n    sampleCount?: number;\n}\nexport interface LosAnalysisResult extends LosResult {\n    /** Sonuç geometrisi — görünür kısım [x0,y0,z0, x1,y1,z1, ...] */\n    visibleLine: Float64Array;\n    /** Sonuç geometrisi — engellenmiş kısım */\n    blockedLine: Float64Array | null;\n}\nexport interface ElevationQueryParams {\n    /** Sorgulanacak noktalar: [lon0, lat0, lon1, lat1, ...] */\n    points: Float64Array;\n}\nexport interface ElevationQueryResult {\n    /** Yükseklik değerleri (metre): [h0, h1, h2, ...] */\n    elevations: Float64Array;\n}\nexport interface BufferParams {\n    /** Merkez geometri (GeoJSON) */\n    geometry: {\n        type: string;\n        coordinates: unknown;\n    };\n    /** Buffer mesafesi (metre) */\n    distance: number;\n    /** Segment sayısı (daire yakınsaması, varsayılan: 64) */\n    segments?: number;\n}\nexport interface BufferResult {\n    /** Buffer polygon (GeoJSON Polygon) */\n    geometry: {\n        type: 'Polygon';\n        coordinates: number[][][];\n    };\n}\nexport interface RouteSampleParams {\n    /** Rota noktaları: [lon0, lat0, lon1, lat1, ...] */\n    route: Float64Array;\n    /** Örnekleme aralığı (metre) */\n    interval: number;\n}\nexport interface RouteSampleResult {\n    /** Örneklenmiş noktalar: [lon0, lat0, elev0, dist0, ...] */\n    samples: Float64Array;\n    /** Toplam mesafe (metre) */\n    totalDistance: number;\n}\nexport interface IAnalysis {\n    /** Line of Sight analizi */\n    runLos(params: LosParams): Promise<LosAnalysisResult>;\n    /** Yükseklik sorgusu */\n    queryElevation(params: ElevationQueryParams): Promise<ElevationQueryResult>;\n    /** Buffer (tampon bölge) oluştur */\n    buffer(params: BufferParams): Promise<BufferResult>;\n    /** Rota profili örnekleme */\n    sampleRoute(params: RouteSampleParams): Promise<RouteSampleResult>;\n}\n//# sourceMappingURL=IAnalysis.d.ts.map","interfaces/IGlobeEffects.d.ts":"/**\n * Globe Effects — Configuration interfaces for atmospheric, fog, night imagery, and water effects.\n *\n * CesiumJS Globe/SkyAtmosphere equivalent configurations.\n * These are passed to the render engine to enable/configure globe-specific visual effects.\n */\n/** Fog configuration for distance-based atmospheric haze. */\nexport interface FogConfig {\n    /** Enable fog rendering. Default: false. */\n    enabled: boolean;\n    /** Fog density factor. Higher = thicker fog. Default: 0.0003. */\n    density?: number;\n    /** Fog color [R, G, B, A] 0-1. Default: atmosphere blueish. */\n    color?: [number, number, number, number];\n    /** Start distance from camera (meters). Default: 0 (starts at camera). */\n    startDistance?: number;\n    /** Fog equation: 'linear' | 'exp' | 'exp2'. Default: 'exp'. */\n    equation?: 'linear' | 'exp' | 'exp2';\n}\n/** Night imagery configuration for Earth-at-night texture blending. */\nexport interface NightImageryConfig {\n    /** Enable night imagery rendering. Default: false. */\n    enabled: boolean;\n    /** URL to the night imagery texture (equirectangular or tile URL template). */\n    textureUrl?: string;\n    /** Intensity of night lights (0-1). Default: 1.0. */\n    intensity?: number;\n    /** Transition zone width (dot product range for day/night blend). Default: 0.1. */\n    transitionWidth?: number;\n}\n/** Water mask configuration for ocean/lake rendering effects. */\nexport interface WaterMaskConfig {\n    /** Enable water rendering effects. Default: false. */\n    enabled: boolean;\n    /** Water surface color [R, G, B, A] 0-1. Default: dark blue. */\n    color?: [number, number, number, number];\n    /** Specular highlight power (sun glint). Default: 64. */\n    specularPower?: number;\n    /** Fresnel bias for reflection intensity. Default: 0.02. */\n    fresnelBias?: number;\n    /** Animated wave perturbation frequency. Default: 0 (no waves). */\n    waveFrequency?: number;\n    /** Wave amplitude in normal perturbation. Default: 0.01. */\n    waveAmplitude?: number;\n}\n/** Globe terrain lighting defaults (used when no per-tile lighting3D is set). */\nexport interface GlobeLightingConfig {\n    /** Enable terrain lighting. Default: true. */\n    enabled?: boolean;\n    /** Ambient light factor: 0-1. Default 0.5. */\n    ambient?: number;\n    /** Diffuse light factor: 0-2. Default 0.85. */\n    diffuse?: number;\n    /** Pseudo-shadow strength: 0-1. Default 0.2. */\n    shadowStrength?: number;\n    /** Pseudo-shadow softness/distance: 0-1. Default 0.4. */\n    shadowSoftness?: number;\n    /** Sun azimuth in degrees, clockwise from north. Default 315. */\n    sunAzimuth?: number;\n    /** Sun altitude in degrees above horizon (0-90). Default 45. */\n    sunAltitude?: number;\n}\n/** Pole cap rendering configuration. */\nexport interface PoleCapConfig {\n    /** Enable pole cap rendering. Default: true. */\n    enabled?: boolean;\n    /** Pole cap color [R, G, B] 0-1. Default: [0.65, 0.78, 0.88]. */\n    color?: [number, number, number];\n}\n/** Atmosphere configuration (extends existing atmosphere pipeline). */\nexport interface AtmosphereConfig {\n    /** Enable atmosphere halo. Default: true in 3D mode. */\n    enabled: boolean;\n    /** Inner glow color [R, G, B, A] 0-1. */\n    colorInner?: [number, number, number, number];\n    /** Outer (limb) glow color [R, G, B, A] 0-1. */\n    colorOuter?: [number, number, number, number];\n    /** Glow strength multiplier. Default: 1.0. */\n    strength?: number;\n    /** Falloff exponent (higher = sharper edge). Default: 4.0. */\n    falloff?: number;\n}\n/** Combined globe effects configuration. */\nexport interface GlobeEffectsConfig {\n    fog?: FogConfig;\n    nightImagery?: NightImageryConfig;\n    waterMask?: WaterMaskConfig;\n    atmosphere?: AtmosphereConfig;\n    /** Default terrain lighting for globe tiles (when no per-tile lighting3D). */\n    lighting?: GlobeLightingConfig;\n    /** Pole cap rendering. */\n    poleCaps?: PoleCapConfig;\n    /** Background (clear) color for 3D mode [R, G, B, A] 0-1. Default: [0, 0, 0, 1]. */\n    backgroundColor?: [number, number, number, number];\n}\n/** Resolved globe effects with defaults. */\nexport interface ResolvedGlobeEffects {\n    fog: Required<FogConfig>;\n    nightImagery: Required<NightImageryConfig>;\n    waterMask: Required<WaterMaskConfig>;\n    atmosphere: Required<AtmosphereConfig>;\n    lighting: Required<GlobeLightingConfig>;\n    poleCaps: Required<PoleCapConfig>;\n    backgroundColor: [number, number, number, number];\n}\nexport declare function resolveGlobeEffects(cfg?: GlobeEffectsConfig): ResolvedGlobeEffects;\n/** WGSL fog calculation function — injected into globe fragment shaders. */\nexport declare const FOG_WGSL_SNIPPET = \"\\nfn applyFog(\\n  color: vec3<f32>,\\n  fogColor: vec3<f32>,\\n  distance: f32,\\n  density: f32,\\n  equation: u32, // 0=linear, 1=exp, 2=exp2\\n) -> vec3<f32> {\\n  var fogFactor: f32;\\n  if equation == 0u {\\n    fogFactor = clamp(1.0 - distance * density, 0.0, 1.0);\\n  } else if equation == 1u {\\n    fogFactor = exp(-distance * density);\\n  } else {\\n    fogFactor = exp(-pow(distance * density, 2.0));\\n  }\\n  return mix(fogColor, color, fogFactor);\\n}\\n\";\n/** WGSL night/day blend — injected into globe raster fragment shader. */\nexport declare const NIGHT_BLEND_WGSL_SNIPPET = \"\\nfn blendNightDay(\\n  dayColor: vec3<f32>,\\n  nightColor: vec3<f32>,\\n  lightFactor: f32,  // dot(normal, sunDir)\\n  transitionWidth: f32,\\n  nightIntensity: f32,\\n) -> vec3<f32> {\\n  let blend = smoothstep(-transitionWidth, transitionWidth, lightFactor);\\n  return mix(nightColor * nightIntensity, dayColor, blend);\\n}\\n\";\n/** WGSL water specular — injected into globe raster fragment shader. */\nexport declare const WATER_SPECULAR_WGSL_SNIPPET = \"\\nfn waterSpecular(\\n  normal: vec3<f32>,\\n  viewDir: vec3<f32>,\\n  lightDir: vec3<f32>,\\n  specularPower: f32,\\n  fresnelBias: f32,\\n) -> f32 {\\n  let halfVec = normalize(lightDir + viewDir);\\n  let spec = pow(max(dot(normal, halfVec), 0.0), specularPower);\\n  let fresnel = fresnelBias + (1.0 - fresnelBias) * pow(1.0 - max(dot(viewDir, normal), 0.0), 5.0);\\n  return spec * fresnel;\\n}\\n\";\n//# sourceMappingURL=IGlobeEffects.d.ts.map","interfaces/ILayer.d.ts":"/**\n * Layer Interface Contract\n *\n * Tüm katman türlerinin uyması gereken sözleşme.\n * Core-TS Agent bu interface'i tanımlar, ilgili agent'lar implement eder.\n */\nexport interface Extent {\n    minX: number;\n    minY: number;\n    maxX: number;\n    maxY: number;\n    spatialReference?: string;\n}\nexport interface Feature {\n    id: string | number;\n    geometry: Geometry;\n    attributes: Record<string, unknown>;\n}\nexport type GeometryType = 'Point' | 'MultiPoint' | 'LineString' | 'MultiLineString' | 'Polygon' | 'MultiPolygon';\nexport interface Geometry {\n    type: GeometryType;\n    coordinates: number[] | number[][] | number[][][] | number[][][][];\n}\nexport interface LayerEvents {\n    'load': void;\n    'error': LayerError;\n    'visibility-change': boolean;\n    'opacity-change': number;\n    'refresh': void;\n}\nexport interface LayerError {\n    code: string;\n    message: string;\n    cause?: Error;\n}\nexport interface ILayer {\n    /** Unique layer identifier */\n    readonly id: string;\n    /** Layer tipi: 'wms' | 'raster-tile' | 'feature' | 'geojson' | 'vector-tile' | 'terrain' | ... */\n    readonly type: string;\n    /** Görünürlük */\n    visible: boolean;\n    /** Opaklık (0-1) */\n    opacity: number;\n    /** Minimum görünür ölçek (opsiyonel) */\n    minScale?: number;\n    /** Maksimum görünür ölçek (opsiyonel) */\n    maxScale?: number;\n    /** Render ordering — higher values draw on top. Default 0. */\n    zIndex?: number;\n    /** Whether this layer responds to pointer interactions (hit-test, click). Default true. */\n    interactive?: boolean;\n    /** Layer'ın veri yükleme durumu */\n    readonly loaded: boolean;\n    /** Layer extent (yüklendikten sonra) */\n    readonly fullExtent?: Extent;\n    /** Layer'ı yükle (capabilities fetch, metadata parse vb.) */\n    load(): Promise<void>;\n    /** Layer'ı yenile (tile cache temizle, yeniden fetch et) */\n    refresh(): void;\n    /** Kaynakları serbest bırak */\n    destroy(): void;\n    on<K extends keyof LayerEvents>(event: K, handler: (data: LayerEvents[K]) => void): void;\n    off<K extends keyof LayerEvents>(event: K, handler: (data: LayerEvents[K]) => void): void;\n}\nexport interface IQueryableLayer extends ILayer {\n    /** Feature sorgusu (bbox veya attribute filter) */\n    queryFeatures(params: QueryParams): Promise<Feature[]>;\n    /** Extent sorgusu */\n    queryExtent(params?: QueryParams): Promise<Extent>;\n}\nexport interface QueryParams {\n    /** Geometry filtresi */\n    geometry?: Extent;\n    /** Attribute filtresi (SQL benzeri) */\n    where?: string;\n    /** Max sonuç sayısı */\n    maxResults?: number;\n    /** Dönecek alanlar */\n    outFields?: string[];\n}\n/** Tile-based layers — XYZ, TMS, VectorTile (z/x/y URL scheme) */\nexport interface ITileLayer extends ILayer {\n    getTileUrl(z: number, x: number, y: number): string;\n    readonly minZoom: number;\n    readonly maxZoom: number;\n}\n/** Feature-based layers — GeoJSON, Graphics, Feature */\nexport interface IFeatureLayer extends ILayer {\n    getFeatures(): readonly Feature[];\n    /** Optional renderer for feature-level symbology */\n    renderer?: import('./IRenderer').IRenderer;\n}\nexport interface TerrainHeightTileData {\n    z: number;\n    x: number;\n    y: number;\n    width: number;\n    height: number;\n    /** Height values in meters (row-major). */\n    data: Float32Array;\n}\nexport interface TerrainHillshadeTileData {\n    z: number;\n    x: number;\n    y: number;\n    width: number;\n    height: number;\n    /** RGBA8 hillshade pixels (row-major). */\n    data: Uint8Array;\n}\nexport interface TerrainLighting3DOptions {\n    enabled: boolean;\n    /** Sun direction azimuth in degrees, clockwise from north. */\n    sunAzimuth: number;\n    /** Sun altitude in degrees above horizon. */\n    sunAltitude: number;\n    /** 0..1 base ambient contribution. */\n    ambient: number;\n    /** 0..2 diffuse strength multiplier. */\n    diffuse: number;\n    /** 0..1 pseudo-shadow strength. */\n    shadowStrength: number;\n    /** 0..1 pseudo-shadow softness/distance. */\n    shadowSoftness: number;\n}\nexport interface ITerrainLayer extends ILayer {\n    readonly minZoom: number;\n    readonly maxZoom: number;\n    readonly exaggeration: number;\n    readonly lighting3D?: TerrainLighting3DOptions;\n    requestTile(z: number, x: number, y: number): Promise<void>;\n    getReadyHeightTile(z: number, x: number, y: number): TerrainHeightTileData | null;\n    getReadyHillshadeTile(z: number, x: number, y: number): TerrainHillshadeTileData | null;\n    setLighting3D?(options: Partial<TerrainLighting3DOptions>): void;\n}\nexport interface CustomVertexAttribute {\n    shaderLocation: number;\n    offset: number;\n    format: GPUVertexFormat;\n}\nexport interface CustomVertexBufferLayout {\n    arrayStride: number;\n    stepMode?: GPUVertexStepMode;\n    attributes: CustomVertexAttribute[];\n}\nexport interface CustomDrawCommand {\n    topology?: GPUPrimitiveTopology;\n    vertexCount?: number;\n    instanceCount?: number;\n    indexCount?: number;\n    indexFormat?: GPUIndexFormat;\n}\nexport interface CustomTextureBinding {\n    texture: GPUTexture;\n    sampler?: GPUSamplerDescriptor;\n}\n/** Custom WGSL shader layer — user-supplied vertex/fragment shaders with full GPU control. */\nexport interface ICustomShaderLayer extends ILayer {\n    readonly vertexShader: string;\n    readonly fragmentShader: string;\n    readonly vertexBufferLayouts: CustomVertexBufferLayout[];\n    getCustomUniforms(): ArrayBuffer | null;\n    getVertexBuffers(): GPUBuffer[];\n    getIndexBuffer(): GPUBuffer | null;\n    getTextures(): CustomTextureBinding[];\n    getDrawCommand(): CustomDrawCommand;\n    readonly animated: boolean;\n    readonly blendState?: GPUBlendState;\n    readonly rawMode?: boolean;\n}\nexport declare function isCustomShaderLayer(layer: ILayer): layer is ICustomShaderLayer;\nexport declare function isTileLayer(layer: ILayer): layer is ITileLayer;\nexport declare function isFeatureLayer(layer: ILayer): layer is IFeatureLayer;\nexport declare function isTerrainLayer(layer: ILayer): layer is ITerrainLayer;\nexport type ClusterThemePreset = 'ref-dark-cyan' | 'legacy-orange';\nexport interface ClusterStyleConfig {\n    clusterFillSmall: [number, number, number, number];\n    clusterFillMedium: [number, number, number, number];\n    clusterFillLarge: [number, number, number, number];\n    clusterStroke: [number, number, number, number];\n    clusterText: [number, number, number, number];\n    pointFill: [number, number, number, number];\n    pointStroke: [number, number, number, number];\n    pointSize: number;\n    pointStrokeWidth: number;\n    clusterBaseSize: number;\n    clusterGrowRate: number;\n    clusterStrokeWidth: number;\n}\nexport interface ClusterGoToTarget {\n    center?: [number, number];\n    zoom?: number;\n    duration?: number;\n}\n/** Callbacks provided by the view mode so cluster layers can interact with the map. */\nexport interface ClusterViewCallbacks {\n    toMap: (screenX: number, screenY: number) => [number, number] | null;\n    getZoom: () => number;\n    getExtent: () => [number, number, number, number];\n    toScreen?: (lon: number, lat: number) => [number, number] | null;\n    getViewportSize?: () => [number, number];\n    goTo?: (target: ClusterGoToTarget) => Promise<void> | void;\n}\nexport interface IClusterLayer extends ILayer {\n    sourceLayer: IFeatureLayer;\n    setSource(layer: IFeatureLayer): void;\n    clusterRadius: number;\n    clusterMinPoints: number;\n    getSourcePoints3857(): Float32Array | null;\n    readonly pointCount: number;\n    readonly sourceVersion: number;\n    handleClusterClick(screenX: number, screenY: number): void;\n    readonly clusterStyle: ClusterStyleConfig;\n    attachView(callbacks: ClusterViewCallbacks): void;\n}\nexport declare function isClusterLayer(layer: ILayer): layer is IClusterLayer;\nexport interface IDynamicPointLayer extends ILayer {\n    /** Number of active points */\n    readonly pointCount: number;\n    /** Pre-allocated GPU vertex buffer for positions */\n    readonly positionBuffer: GPUBuffer | null;\n    /** Bulk-update all positions via writeBuffer (no allocation) */\n    updatePositions(data: Float32Array): void;\n    /** Symbol used for rendering all points */\n    readonly pointSymbol: import('./IRenderEngine').PointSymbol;\n}\nexport declare function isDynamicPointLayer(layer: ILayer): layer is IDynamicPointLayer;\n//# sourceMappingURL=ILayer.d.ts.map","interfaces/IOgcAdapter.d.ts":"/**\n * OGC Adapter Interface Contract\n *\n * WMS, WFS, OGC API adapter'larının uyması gereken sözleşme.\n * Adapters-OGC Agent bu interface'leri implement eder.\n */\nexport interface ServiceCapabilities {\n    type: 'WMS' | 'WFS' | 'OGC-API-Features' | 'OGC-API-Maps' | 'XYZ';\n    version: string;\n    title?: string;\n    abstract?: string;\n}\nexport interface OgcError {\n    code: string;\n    message: string;\n    source: 'WMS' | 'WFS' | 'OGC-API';\n    status?: number;\n}\nexport interface MapImageryCapabilities extends ServiceCapabilities {\n    layers: MapImageryLayerInfo[];\n    formats: string[];\n}\nexport interface MapImageryLayerInfo {\n    name: string;\n    title: string;\n    abstract?: string;\n    crs: string[];\n    extent?: [number, number, number, number];\n    styles: {\n        name: string;\n        title?: string;\n        legendUrl?: string;\n    }[];\n    timeExtent?: string;\n    queryable: boolean;\n}\nexport interface MapImageryRequest {\n    layers: string[];\n    bbox: {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    };\n    width: number;\n    height: number;\n    crs?: string;\n    format?: string;\n    transparent?: boolean;\n    time?: string;\n    vendorParams?: Record<string, string>;\n}\nexport interface FeatureInfoRequest {\n    layers: string[];\n    bbox: {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    };\n    width: number;\n    height: number;\n    x: number;\n    y: number;\n    crs?: string;\n    featureCount?: number;\n}\nexport interface FeatureInfoResult {\n    features: Array<{\n        layerName: string;\n        attributes: Record<string, unknown>;\n    }>;\n}\nexport interface IMapImageryAdapter {\n    /** Service capabilities al */\n    getCapabilities(): Promise<MapImageryCapabilities>;\n    /** GetMap / map tile URL'i oluştur */\n    getMapUrl(params: MapImageryRequest): string;\n    /** Feature bilgisi sorgusu (opsiyonel — tüm servislerde yok) */\n    getFeatureInfo?(params: FeatureInfoRequest): Promise<FeatureInfoResult>;\n}\nexport interface FeatureCollectionInfo {\n    id: string;\n    title: string;\n    description?: string;\n    extent?: [number, number, number, number];\n    crs?: string[];\n}\nexport interface FeatureQueryParams {\n    bbox?: [number, number, number, number];\n    datetime?: string;\n    limit?: number;\n    offset?: number;\n    filter?: string;\n    properties?: string[];\n    sortBy?: string;\n}\nexport interface IFeatureAdapter {\n    /** Koleksiyon listesi */\n    getCollections(): Promise<FeatureCollectionInfo[]>;\n    /** Feature sorgusu — iterator (otomatik pagination) */\n    getFeatures(collectionId: string, params?: FeatureQueryParams): AsyncGenerator<GeoJsonFeature[], void, unknown>;\n}\nexport interface GeoJsonFeature {\n    type: 'Feature';\n    id?: string | number;\n    geometry: {\n        type: string;\n        coordinates: unknown;\n    };\n    properties: Record<string, unknown>;\n}\nexport type ServiceType = {\n    type: 'WMS';\n    version: string;\n} | {\n    type: 'WFS';\n    version: string;\n} | {\n    type: 'OGC-API-Features';\n} | {\n    type: 'OGC-API-Maps';\n} | {\n    type: 'XYZ';\n} | {\n    type: 'unknown';\n};\nexport interface IServiceDiscovery {\n    /** URL'den servis tipini otomatik algıla */\n    discover(url: string): Promise<ServiceType>;\n}\n//# sourceMappingURL=IOgcAdapter.d.ts.map","interfaces/IRenderEngine.d.ts":"/**\n * Render Engine Interface Contract\n *\n * WebGPU render core'un TypeScript sözleşmesi.\n * Engine orchestration katmanı bu interface üzerinden render core ile konuşur.\n */\nexport type RenderMode = 'full-gpu' | 'gpu-lite' | 'cpu-degraded';\nexport interface GpuFeatures {\n    timestampQuery: boolean;\n    float32Filterable: boolean;\n    indirectFirstInstance: boolean;\n    shaderF16: boolean;\n}\nexport interface GpuLimits {\n    maxTextureDimension2D: number;\n    maxBufferSize: number;\n    maxStorageBufferBindingSize: number;\n}\nexport interface GpuCapabilities {\n    mode: RenderMode;\n    features: GpuFeatures;\n    limits: GpuLimits;\n}\nexport interface CameraState {\n    /** View matrix (column-major, 16 floats) */\n    viewMatrix: Float32Array;\n    /** Projection matrix (column-major, 16 floats) */\n    projectionMatrix: Float32Array;\n    /** Kamera pozisyonu (world space) */\n    position: [number, number, number];\n    /** Viewport boyutları */\n    viewportWidth: number;\n    viewportHeight: number;\n    /** Projection transition: 0 = Mercator flat, 1 = globe sphere */\n    projectionTransition?: number;\n    /** Horizon clipping plane [A, B, C, D]: Ax + By + Cz + D ≥ 0 → visible */\n    clippingPlane?: [number, number, number, number];\n    /** Globe radius in unit-sphere space (typically 1.0) */\n    globeRadius?: number;\n    /** Flat Mercator VP matrix for zoom >= 6 rendering (column-major, 16 floats) */\n    flatViewProjectionMatrix?: Float32Array;\n    /** Camera position in Mercator [0..1] space [cx, cy, mercDist] for flat-path shading */\n    cameraMerc01?: [number, number, number];\n}\nexport interface PointSymbol {\n    type: 'simple-marker' | 'icon' | 'sdf-icon';\n    color: [number, number, number, number];\n    size: number;\n    outlineColor?: [number, number, number, number];\n    outlineWidth?: number;\n    /** Sprite atlas'taki ikon index'i (icon tipi için) */\n    iconIndex?: number;\n    /** Icon kaynak referansı — loadIcon() ile yüklenen ikon ID'si (type='icon' için) */\n    src?: string;\n    /** İkon dönüş açısı (derece, saat yönü) */\n    rotation?: number;\n    /** Glow effect color (RGBA 0-255). When set, a soft radial halo is drawn behind the point/icon. */\n    glowColor?: [number, number, number, number];\n    /** Glow spread in pixels beyond the point/icon size. Default 0 (no glow). */\n    glowSize?: number;\n    /** Background circle fill color (RGBA 0-255). Draws a filled circle behind the icon. */\n    backgroundColor?: [number, number, number, number];\n    /** Background circle diameter in pixels. 0 = no background. */\n    backgroundSize?: number;\n}\nexport interface LineSymbol {\n    type: 'simple-line';\n    color: [number, number, number, number];\n    width: number;\n    style: 'solid' | 'dash' | 'dot' | 'dash-dot';\n    /** Animated dash için hız (piksel/saniye, 0 = statik) */\n    dashAnimationSpeed?: number;\n    /** Custom dash pattern (pixel lengths: [dash, gap, dash, gap, ...]). Max 8 segments. */\n    dashArray?: number[];\n    /** Offset into the dash pattern (pixels). Default 0. */\n    dashOffset?: number;\n    /** Line end cap style. Default 'round'. */\n    lineCap?: 'butt' | 'round' | 'square';\n    /** Line corner join style. Default 'round'. */\n    lineJoin?: 'bevel' | 'round' | 'miter';\n    /** Glow effect color (RGBA 0-255). When set, a wider translucent pass is drawn behind. */\n    glowColor?: [number, number, number, number];\n    /** Glow spread in pixels beyond the line width. Default 0 (no glow). */\n    glowWidth?: number;\n}\nexport interface PolygonSymbol {\n    type: 'simple-fill';\n    color: [number, number, number, number];\n    outlineColor: [number, number, number, number];\n    outlineWidth: number;\n    /** Fill rule for complex polygons. Default 'nonzero'. */\n    fillRule?: 'evenodd' | 'nonzero';\n    /** Glow effect on polygon outline (RGBA 0-255). */\n    outlineGlowColor?: [number, number, number, number];\n    /** Glow spread in pixels beyond the outline width. Default 0. */\n    outlineGlowWidth?: number;\n}\n/** Structured glTF source — JSON descriptor + external binary buffers. */\nexport interface GltfSource {\n    json: unknown;\n    buffers: ArrayBuffer[];\n}\nexport interface ModelSymbol {\n    type: 'model';\n    /** loadModel() ile yüklenen model ID */\n    modelId: string;\n    /** Default 1.0, model units = meters */\n    scale?: number;\n    /** Yaw, derece, 0=kuzey, saat yönü */\n    heading?: number;\n    /** Derece */\n    pitch?: number;\n    /** Derece */\n    roll?: number;\n    /** RGBA 0-255 */\n    tintColor?: [number, number, number, number];\n    /** Dikey offset (metre) */\n    anchorZ?: number;\n}\nexport interface ExtrusionAnimationConfig {\n    /** Animation duration in milliseconds. Default: 800 */\n    duration?: number;\n    /** Delay factor: seconds per merc01 unit distance from wave origin. Default: 2.0\n     *  Higher = more pronounced wave effect. 0 = all buildings animate simultaneously. */\n    delayFactor?: number;\n    /** Easing function. Default: 'ease-out-cubic' */\n    easing?: 'ease-out-cubic' | 'linear';\n}\nexport interface ExtrudedPolygonSymbol {\n    type: 'fill-extrusion';\n    /** Fill color: RGBA 0-255 */\n    color: [number, number, number, number];\n    /** Attribute field name for building height in metres (e.g. 'render_height') */\n    heightField: string;\n    /** Attribute field name for minimum height in metres (e.g. 'render_min_height') */\n    minHeightField?: string;\n    /** Ambient light factor: 0-1, default 0.35 */\n    ambient?: number;\n    /** Blinn-Phong specular exponent. Higher = tighter highlight. Default 32. */\n    shininess?: number;\n    /** Blinn-Phong specular intensity: 0-1. Default 0.15. */\n    specularStrength?: number;\n    /** Optional grow animation. When set, buildings rise from ground when their tile loads. */\n    animation?: ExtrusionAnimationConfig;\n}\n/** 3D mesh geometry symbol — renders real Box/Cylinder/Sphere/Cone meshes. */\nexport interface Mesh3DSymbol {\n    type: 'mesh-3d';\n    /** Geometry type to generate. */\n    meshType: 'box' | 'cylinder' | 'sphere' | 'cone';\n    /** Fill color RGBA 0-255. */\n    color: [number, number, number, number];\n    /** Scale [x, y, z] in meters. Default [1,1,1]. */\n    scale?: [number, number, number];\n    /** Heading (yaw) in degrees. Default 0. */\n    heading?: number;\n    /** Pitch in degrees. Default 0. */\n    pitch?: number;\n    /** Roll in degrees. Default 0. */\n    roll?: number;\n    /** Ambient light factor 0-1. Default 0.35. */\n    ambient?: number;\n    /** Blinn-Phong specular exponent. Default 32. */\n    shininess?: number;\n    /** Specular intensity 0-1. Default 0.15. */\n    specularStrength?: number;\n}\nexport interface PointRenderBuffer {\n    /** GPU vertex buffer handle */\n    vertexBuffer: GPUBuffer;\n    /** Nokta sayısı */\n    count: number;\n    /** Instance data (opsiyonel — cluster/symbol variation) */\n    instanceBuffer?: GPUBuffer;\n    instanceCount?: number;\n}\nexport interface LineRenderBuffer {\n    vertexBuffer: GPUBuffer;\n    indexBuffer: GPUBuffer;\n    indexCount: number;\n}\nexport interface PolygonRenderBuffer {\n    vertexBuffer: GPUBuffer;\n    indexBuffer: GPUBuffer;\n    indexCount: number;\n}\nexport interface ModelRenderBuffer {\n    /** Per-feature instance data: [mercX, mercY, mercZ, scale, heading, pitch, roll, anchorZ] */\n    instanceBuffer: GPUBuffer;\n    instanceCount: number;\n}\nexport interface ExtrusionRenderBuffer {\n    /** GPU vertex buffer: [px,py,pz, nx,ny,nz, cx,cy] — 32 bytes/vertex */\n    vertexBuffer: GPUBuffer;\n    /** GPU index buffer */\n    indexBuffer: GPUBuffer;\n    /** Number of indices */\n    indexCount: number;\n    /** Stable identifier for animation tracking (e.g. tileKey \"z/x/y\") */\n    id?: string;\n}\nexport interface Mesh3DRenderBuffer {\n    /** GPU vertex buffer: [posX, posY, posZ, normX, normY, normZ] — 24 bytes/vertex */\n    vertexBuffer: GPUBuffer;\n    /** GPU index buffer (uint32) */\n    indexBuffer: GPUBuffer;\n    /** Number of indices */\n    indexCount: number;\n}\nexport interface ImageryTile {\n    texture: GPUTexture;\n    /** Tile extent (render coordinate space): [minX, minY, maxX, maxY] */\n    extent: [number, number, number, number];\n    opacity: number;\n    /** Depth bias for fallback parent tiles to prevent z-fighting with child tiles.\n     *  0.0 = exact tile (front), positive = pushed back. Default: 0. */\n    depthBias?: number;\n    /** Post-process color filters applied in the fragment shader. */\n    filters?: {\n        /** Brightness multiplier (1.0 = unchanged, 0 = black, 2 = double). */\n        brightness?: number;\n        /** Contrast multiplier (1.0 = unchanged). */\n        contrast?: number;\n        /** Saturation multiplier (1.0 = unchanged, 0 = grayscale). */\n        saturate?: number;\n    };\n}\n/** Globe imagery tile — rendered on unit sphere via subdivision mesh */\nexport interface GlobeTerrainLighting {\n    enabled?: boolean;\n    ambient?: number;\n    diffuse?: number;\n    shadowStrength?: number;\n    shadowSoftness?: number;\n    sunAzimuth?: number;\n    sunAltitude?: number;\n}\n/** Globe imagery tile — rendered on unit sphere via subdivision mesh */\nexport interface GlobeImageryTile {\n    texture: GPUTexture;\n    /** Tile Mercator extent (0..1): [minX, minY, maxX, maxY] */\n    mercatorExtent: [number, number, number, number];\n    opacity: number;\n    /** Depth bias for fallback parent tiles (0.0 = exact, positive = pushed back). */\n    depthBias?: number;\n    /** Optional terrain height texture for tile-local displacement mode. */\n    terrainHeightTexture?: GPUTexture;\n    /** Tile UV remap into terrain texture UV: [offsetX, offsetY, scaleX, scaleY]. */\n    terrainUvOffsetScale?: [number, number, number, number];\n    /** Height sampling mode: 0=world-space debug brush, 1=tile-local terrain. */\n    heightMode?: 0 | 1;\n    /** Exaggeration factor used by the active height mode. */\n    heightExaggeration?: number;\n    /** Optional terrain lighting controls for globe raster terrain shading. */\n    lighting3D?: GlobeTerrainLighting;\n    /** Post-process color filters applied in the fragment shader. */\n    filters?: {\n        brightness?: number;\n        contrast?: number;\n        saturate?: number;\n    };\n}\nexport interface TextSymbol {\n    type: 'simple-text';\n    fontFamily: string;\n    fontSize: number;\n    color: [number, number, number, number];\n    haloColor?: [number, number, number, number];\n    haloWidth?: number;\n    anchor: 'center' | 'left' | 'right' | 'top' | 'bottom';\n}\nexport interface TextRenderBuffer {\n    /** GPU vertex buffer handle (per-glyph quad instances) */\n    vertexBuffer: GPUBuffer;\n    /** Glyph sayısı */\n    count: number;\n}\nexport interface FeaturePickResult {\n    /** Layer ID */\n    layerId: string;\n    /** Feature ID */\n    featureId: number;\n    /** Ekran koordinatları */\n    screenX: number;\n    screenY: number;\n}\nexport interface GpuMemoryAccounting {\n    persistentBufferBytes: number;\n    transientBufferBytes: number;\n    textureBytes: number;\n    totalTrackedBytes: number;\n}\nexport interface DepthConfig {\n    /** Depth texture format */\n    format: GPUTextureFormat;\n    /** Depth comparison function */\n    compareFunc: GPUCompareFunction;\n    /** Depth clear value (1.0 for standard, 0.0 for reversed-Z) */\n    clearValue: number;\n}\n/** Standard depth: near=0, far=1. For 2D views. */\nexport declare const DEPTH_STANDARD: DepthConfig;\n/** Reversed-Z depth: near=1, far=0. Better precision for 3D/globe views. */\nexport declare const DEPTH_REVERSED_Z: DepthConfig;\nexport interface CustomDrawCall {\n    pipelineKey: string;\n    shaderSource: string;\n    vertexBufferLayouts: import('./ILayer').CustomVertexBufferLayout[];\n    vertexBuffers: GPUBuffer[];\n    indexBuffer: GPUBuffer | null;\n    indexFormat?: GPUIndexFormat;\n    frameUniforms: Float32Array;\n    customUniforms: ArrayBuffer | null;\n    textures: import('./ILayer').CustomTextureBinding[];\n    vertexCount?: number;\n    instanceCount?: number;\n    indexCount?: number;\n    topology?: GPUPrimitiveTopology;\n    blendState?: GPUBlendState;\n    /** When true, use the globe camera bind group (160 bytes: VP + flatVP +\n     *  viewport + projectionTransition + globeRadius + clippingPlane) instead\n     *  of the regular camera bind group. Used by Mode3D for Mercator→sphere\n     *  projection in custom shader preambles. */\n    useGlobeCamera?: boolean;\n}\n/** Ambient light component. */\nexport interface AmbientLight {\n    /** RGB color [0-1]. Default [1, 1, 1]. */\n    color?: [number, number, number];\n    /** Intensity multiplier. Default 0.35. */\n    intensity?: number;\n}\n/** Directional light component. */\nexport interface DirectionalLight {\n    /** RGB color [0-1]. Default [1, 1, 1]. */\n    color?: [number, number, number];\n    /** Intensity multiplier. Default 0.65. */\n    intensity?: number;\n    /** Normalized direction vector [x, y, z]. Default [0.3, -0.5, 0.8]. */\n    direction?: [number, number, number];\n}\n/**\n * Scene lighting configuration for extruded polygons and 3D geometry.\n * Combines ambient + directional lights in a Blinn-Phong model.\n */\nexport interface LightConfig {\n    ambient?: AmbientLight;\n    directional?: DirectionalLight[];\n}\nexport interface IRenderEngine {\n    /** WebGPU device init + capability detection */\n    init(canvas: HTMLCanvasElement, depthConfig?: DepthConfig): Promise<GpuCapabilities>;\n    /** Mevcut GPU yetenekleri */\n    readonly capabilities: GpuCapabilities;\n    /** Frame başlangıcı — camera uniform güncelle, transient buffer'ları sıfırla */\n    beginFrame(camera: CameraState): void;\n    /** Nokta çizimi */\n    drawPoints(buffer: PointRenderBuffer, symbol: PointSymbol): void;\n    /** Çizgi çizimi */\n    drawLines(buffer: LineRenderBuffer, symbol: LineSymbol): void;\n    /** Polygon çizimi */\n    drawPolygons(buffer: PolygonRenderBuffer, symbol: PolygonSymbol): void;\n    /** Raster imagery tile çizimi */\n    drawImagery(tile: ImageryTile): void;\n    /** Globe raster tile çizimi (unit sphere subdivision mesh) */\n    drawGlobeTile(tile: GlobeImageryTile): void;\n    /** Kutup kapakları çiz (±85.05° ötesindeki Mercator boşluklarını doldurur) */\n    drawPoleCaps(color: [number, number, number, number]): void;\n    /** Globe atmosfer halo efekti çiz */\n    drawAtmosphere(strength: number, config?: import('./IGlobeEffects').AtmosphereConfig): void;\n    /** Globe üzerinde nokta çizimi (EPSG:3857 → sphere projection) */\n    drawGlobePoints(buffer: PointRenderBuffer, symbol: PointSymbol): void;\n    /** Globe üzerinde çizgi çizimi (EPSG:3857 → sphere projection) */\n    drawGlobeLines(buffer: LineRenderBuffer, symbol: LineSymbol): void;\n    /** Globe üzerinde polygon çizimi (EPSG:3857 → sphere projection) */\n    drawGlobePolygons(buffer: PolygonRenderBuffer, symbol: PolygonSymbol): void;\n    /** Text çizimi (SDF glyph rendering) */\n    drawText(buffer: TextRenderBuffer, symbol: TextSymbol): void;\n    /** Custom WGSL shader draw call — user-supplied pipeline, buffers, uniforms */\n    drawCustom(call: CustomDrawCall): void;\n    /** Load a 3D model (GLB ArrayBuffer or structured glTF source) into the render engine */\n    loadModel(id: string, source: ArrayBuffer | GltfSource): Promise<void>;\n    /** Draw 3D model instances in 2D mode */\n    drawModels(buffer: ModelRenderBuffer, symbol: ModelSymbol): void;\n    /** Draw 3D model instances on the globe (3D mode) */\n    drawGlobeModels(buffer: ModelRenderBuffer, symbol: ModelSymbol): void;\n    /** Draw extruded polygons (3D buildings) in 2D mode */\n    drawExtrusion(buffer: ExtrusionRenderBuffer, symbol: ExtrudedPolygonSymbol): void;\n    /** Draw extruded polygons on the globe (3D mode) */\n    drawGlobeExtrusion(buffer: ExtrusionRenderBuffer, symbol: ExtrudedPolygonSymbol): void;\n    /** Draw 3D mesh geometry in 2D mode */\n    drawMesh3D(buffer: Mesh3DRenderBuffer, symbol: Mesh3DSymbol): void;\n    /** Draw 3D mesh geometry on the globe (3D mode) */\n    drawGlobeMesh3D(buffer: Mesh3DRenderBuffer, symbol: Mesh3DSymbol): void;\n    /** Upload source points for a cluster layer (EPSG:3857 xy pairs) */\n    setClusterSource(layerId: string, points: Float32Array, version: number): void;\n    /** Run compute + render for a cluster layer */\n    drawClusters(layerId: string, style: import('./ILayer').ClusterStyleConfig, clusterRadius: number, clusterMinPoints: number, zoom: number, extent: [number, number, number, number], globe: boolean): void;\n    /** Frame bitişi — command buffer submit */\n    endFrame(): void;\n    /** Whether any delegate requested continuous rendering (e.g., active grow animation) */\n    readonly needsContinuousRender: boolean;\n    /** Clear color ayarla (RGBA, 0-1 aralığı) */\n    setClearColor(r: number, g: number, b: number, a: number): void;\n    /** Toggle wireframe debug overlay on raster tiles (shows vertex grid) */\n    setDebugTileVertices(enabled: boolean): void;\n    /** Toggle extrusion debug coloring (clipZ/height/normal visualization) */\n    setExtrusionDebug(enabled: boolean): void;\n    /** Configure scene lighting for extrusion and 3D geometry. */\n    setLighting(config: LightConfig): void;\n    /** Apply debug height brush at the given mercator coordinates */\n    applyDebugBrush(mercX: number, mercY: number, radius: number, strength: number, softness?: number): void;\n    /** Clear all debug height brush data */\n    clearDebugBrush(): void;\n    /** Set height exaggeration factor for debug overlay (default 1.0) */\n    setHeightExaggeration(factor: number): void;\n    /** Set the current layer ID for picking attribution. Called before rendering each vector layer. */\n    setCurrentLayerId(id: string): void;\n    /** Enable or disable the GPU picking pass globally. Layers with interactive=false skip picking. */\n    setPickingEnabled(enabled: boolean): void;\n    /** GPU picking pass: verilen ekran koordinatında feature var mı? */\n    pick(x: number, y: number): Promise<FeaturePickResult | null>;\n    /** TypedArray'den GPU buffer oluştur */\n    createBuffer(data: ArrayBufferView, usage: GPUBufferUsageFlags): GPUBuffer;\n    /** ImageBitmap'den GPU texture oluştur */\n    createTexture(image: ImageBitmap): GPUTexture;\n    /** Sprite atlas'a ikon ekle (icon symbology için) */\n    loadIcon(id: string, image: ImageBitmap): void;\n    /** Write data into an existing GPU buffer at byte offset (no allocation). */\n    writeBuffer(buffer: GPUBuffer, offset: number, data: ArrayBufferView): void;\n    /** GPU buffer'ı serbest bırak */\n    releaseBuffer(buffer: GPUBuffer): void;\n    /** GPU texture'ı serbest bırak */\n    releaseTexture(texture: GPUTexture): void;\n    /** Float32 texture oluştur (r32float) */\n    createFloat32Texture(data: Float32Array, width: number, height: number): GPUTexture;\n    /** Uint8 texture oluştur (r8unorm) */\n    createUint8Texture(data: Uint8Array, width: number, height: number): GPUTexture;\n    /** RGBA8 texture oluştur (rgba8unorm) */\n    createRGBA8Texture(data: Uint8Array, width: number, height: number): GPUTexture;\n    /** Tracked GPU memory kullanımı */\n    getMemoryAccounting(): GpuMemoryAccounting;\n    /** Device lost durumunda recovery dene */\n    recover(depthConfig?: DepthConfig): Promise<void>;\n    /** Tüm GPU kaynaklarını serbest bırak */\n    destroy(): void;\n}\n//# sourceMappingURL=IRenderEngine.d.ts.map","interfaces/IRenderer.d.ts":"/**\n * IRenderer -- Symbology system for feature layers.\n *\n * A renderer determines which symbol to use for each feature based on\n * its attributes and/or the current render context. Renderers are the\n * primary mechanism for data-driven styling in mapgpu.\n *\n * Renderers are optional on {@link IFeatureLayer} -- when absent, the default\n * symbols from VectorBufferCache are used.\n *\n * Three built-in implementations are provided:\n * - {@link SimpleRenderer}: applies the same symbol to every feature\n * - {@link UniqueValueRenderer}: maps discrete attribute values to symbols\n * - {@link ClassBreaksRenderer}: maps numeric attribute ranges to symbols\n *\n * @module IRenderer\n */\nimport type { PointSymbol, LineSymbol, PolygonSymbol, ModelSymbol, ExtrudedPolygonSymbol, Mesh3DSymbol } from './IRenderEngine';\nimport type { Feature } from './ILayer';\n/**\n * Union type representing all supported symbol kinds.\n *\n * Each variant corresponds to a geometry type:\n * - {@link PointSymbol} -- rendered as markers, icons, or SDF icons\n * - {@link LineSymbol} -- rendered as stroked lines (solid, dash, dot, dash-dot)\n * - {@link PolygonSymbol} -- rendered as filled polygons with outlines\n * - {@link ModelSymbol} -- rendered as 3D GLB models placed at point locations\n *\n * The `type` discriminant field on each variant enables narrowing:\n *\n * @example\n * ```ts\n * function describeSymbol(sym: Symbol): string {\n *   switch (sym.type) {\n *     case 'simple-marker':\n *     case 'icon':\n *     case 'sdf-icon':\n *       return `Point symbol, size=${sym.size}`;\n *     case 'simple-line':\n *       return `Line symbol, width=${sym.width}, style=${sym.style}`;\n *     case 'simple-fill':\n *       return `Fill symbol, outlineWidth=${sym.outlineWidth}`;\n *   }\n * }\n * ```\n */\nexport type Symbol = PointSymbol | LineSymbol | PolygonSymbol | ModelSymbol | ExtrudedPolygonSymbol | Mesh3DSymbol;\n/**\n * Contextual information about the current map view, passed to\n * {@link IRenderer.getSymbol} so renderers can implement zoom-dependent\n * or resolution-dependent symbology.\n *\n * @example\n * ```ts\n * // A renderer could use context to vary symbol size by zoom:\n * getSymbol(feature: Feature, context?: SymbolRenderContext): Symbol {\n *   const size = context ? Math.max(4, context.zoom * 2) : 8;\n *   return { type: 'simple-marker', color: [255,0,0,255], size };\n * }\n * ```\n */\nexport interface SymbolRenderContext {\n    /**\n     * Current map zoom level.\n     *\n     * Typically ranges from 0 (world view) to ~22 (building level).\n     */\n    zoom: number;\n    /**\n     * Ground resolution at the current zoom level, expressed as meters per pixel.\n     *\n     * Useful for scale-dependent rendering decisions such as hiding detailed\n     * symbology at coarse zoom levels or adjusting line widths to maintain\n     * a consistent real-world width.\n     */\n    resolution: number;\n}\n/**\n * Core interface for the symbology/renderer system.\n *\n * A renderer maps features to symbols. It is set on a feature layer to\n * control how each feature is drawn. The render engine calls\n * {@link getSymbol} once per feature per frame to determine the visual\n * representation.\n *\n * Implementations must be stateless with respect to the feature being\n * symbolized -- the same feature and context should always produce the\n * same symbol.\n *\n * @example\n * ```ts\n * // Assign a renderer to a layer:\n * import { SimpleRenderer } from '@mapgpu/core';\n *\n * const renderer: IRenderer = new SimpleRenderer({\n *   type: 'simple-fill',\n *   color: [0, 120, 255, 200],\n *   outlineColor: [0, 0, 0, 255],\n *   outlineWidth: 1,\n * });\n *\n * featureLayer.renderer = renderer;\n * ```\n */\nexport interface IRenderer {\n    /**\n     * Discriminant string identifying the renderer type.\n     *\n     * Built-in values: `'simple'`, `'unique-value'`, `'class-breaks'`, `'callback'`.\n     * Custom renderer implementations should use a unique string.\n     */\n    readonly type: string;\n    /**\n     * When true, the renderer's symbols may vary with zoom level.\n     *\n     * This causes VectorBufferCache to pass the current zoom via\n     * {@link SymbolRenderContext} and to invalidate cached buffers\n     * when the zoom changes (since symbols may differ per zoom).\n     *\n     * Default: false (symbols are zoom-independent).\n     */\n    readonly zoomSensitive?: boolean;\n    /**\n     * Resolve the symbol for a given feature.\n     *\n     * Called by the render engine once per feature during each render pass.\n     * The implementation should inspect the feature's attributes (and\n     * optionally the render context) to determine which symbol to return.\n     *\n     * Returning `null` indicates the feature should not be drawn in this\n     * frame -- this can be used for attribute-based visibility filtering.\n     *\n     * @param feature - The feature to symbolize. The renderer typically\n     *   reads from `feature.attributes` to make symbology decisions.\n     * @param context - Optional render context providing the current zoom\n     *   level and resolution. May be `undefined` during unit tests or\n     *   when the view has not yet initialized.\n     * @returns The symbol to use for rendering, or `null` to hide the feature.\n     *\n     * @example\n     * ```ts\n     * const sym = renderer.getSymbol(feature, { zoom: 10, resolution: 152.87 });\n     * if (sym) {\n     *   // draw feature with sym\n     * }\n     * ```\n     */\n    getSymbol(feature: Feature, context?: SymbolRenderContext): Symbol | null;\n}\n//# sourceMappingURL=IRenderer.d.ts.map","interfaces/IVectorTileWorker.d.ts":"import type { ExtrudedPolygonSymbol, LineSymbol, ModelSymbol, PointSymbol, PolygonSymbol } from './IRenderEngine';\nimport type { GeometryType } from './ILayer';\nexport type VectorRenderableSymbol = PointSymbol | LineSymbol | PolygonSymbol | ModelSymbol | ExtrudedPolygonSymbol;\nexport type VectorTilePerformanceMode = 'auto' | 'legacy' | 'worker-wasm';\nexport interface VectorTilePerformanceOptions {\n    mode?: VectorTilePerformanceMode;\n    workerCount?: number;\n    maxInFlightTiles?: number;\n    minScreenAreaPx?: number;\n}\nexport interface SerializableSimpleRendererSnapshot {\n    type: 'simple';\n    symbol: VectorRenderableSymbol;\n    zoomSensitive?: boolean;\n}\nexport interface SerializableUniqueValueInfo {\n    value: string | number;\n    symbol: VectorRenderableSymbol;\n}\nexport interface SerializableUniqueValueRendererSnapshot {\n    type: 'unique-value';\n    field: string;\n    defaultSymbol: VectorRenderableSymbol;\n    uniqueValues: SerializableUniqueValueInfo[];\n    zoomSensitive?: boolean;\n}\nexport interface SerializableClassBreakInfo {\n    min: number;\n    max: number;\n    symbol: VectorRenderableSymbol;\n}\nexport interface SerializableClassBreaksRendererSnapshot {\n    type: 'class-breaks';\n    field: string;\n    defaultSymbol: VectorRenderableSymbol;\n    breaks: SerializableClassBreakInfo[];\n    zoomSensitive?: boolean;\n}\nexport type SerializableRendererSnapshot = SerializableSimpleRendererSnapshot | SerializableUniqueValueRendererSnapshot | SerializableClassBreaksRendererSnapshot;\nexport interface VectorTileBinaryPointGroup {\n    key: string;\n    symbol: PointSymbol;\n    vertices: Float32Array;\n    count: number;\n}\nexport interface VectorTileBinaryLineGroup {\n    key: string;\n    symbol: LineSymbol;\n    vertices: Float32Array;\n    indices: Uint32Array;\n    indexCount: number;\n}\nexport interface VectorTileBinaryPolygonGroup {\n    key: string;\n    symbol: PolygonSymbol;\n    vertices: Float32Array;\n    indices: Uint32Array;\n    indexCount: number;\n}\nexport interface VectorTileBinaryModelGroup {\n    key: string;\n    symbol: ModelSymbol;\n    instances: Float32Array;\n    count: number;\n}\nexport interface VectorTileBinaryExtrusionGroup {\n    key: string;\n    symbol: ExtrudedPolygonSymbol;\n    vertices: Float32Array;\n    indices: Uint32Array;\n    indexCount: number;\n}\nexport interface VectorTileBinaryPayload {\n    pointGroups: VectorTileBinaryPointGroup[];\n    lineGroups: VectorTileBinaryLineGroup[];\n    polygonGroups: VectorTileBinaryPolygonGroup[];\n    modelGroups: VectorTileBinaryModelGroup[];\n    extrusionGroups: VectorTileBinaryExtrusionGroup[];\n}\nexport interface VectorTileWorkerFeatureGeometry {\n    type: GeometryType;\n    coordinates: number[] | number[][] | number[][][] | number[][][][];\n    spatialReference: 'EPSG:3857';\n}\nexport interface VectorTileWorkerFeature {\n    id: string | number;\n    geometry: VectorTileWorkerFeatureGeometry;\n    attributes: Record<string, unknown>;\n}\nexport interface VectorTileWorkerRequest {\n    key: string;\n    z: number;\n    x: number;\n    y: number;\n    data: ArrayBuffer;\n    sourceLayer?: string;\n    rendererSnapshot: SerializableRendererSnapshot | null;\n    includeBinaryPayload: boolean;\n    zoom?: number;\n    minScreenAreaPx?: number;\n}\nexport interface VectorTileWorkerResponse {\n    key: string;\n    z: number;\n    x: number;\n    y: number;\n    sourceLayer?: string;\n    features: VectorTileWorkerFeature[];\n    binaryPayload: VectorTileBinaryPayload | null;\n    pipeline: 'worker-wasm' | 'worker-js';\n}\n//# sourceMappingURL=IVectorTileWorker.d.ts.map","interfaces/IWasmCore.d.ts":"/**\n * WASM Core Interface Contract\n *\n * Bu interface, Rust/WASM tarafında implement edilecek tüm spatial compute\n * fonksiyonlarının TypeScript sözleşmesidir.\n *\n * Kurallar:\n * - Tüm girdi/çıktı TypedArray tabanlıdır (zero-copy transfer için)\n * - Koordinatlar interleaved: [x0, y0, x1, y1, ...] veya [x0, y0, z0, x1, y1, z1, ...]\n * - Hata durumları string error fırlatır (wasm-bindgen JsValue)\n */\nexport interface TriangulateResult {\n    /** Vertex pozisyonları: [x0, y0, x1, y1, ...] */\n    vertices: Float64Array;\n    /** Triangle index'leri: [i0, i1, i2, ...] (her 3'lü bir üçgen) */\n    indices: Uint32Array;\n}\nexport interface ClusterOptions {\n    /** Piksel cinsinden kümeleme yarıçapı */\n    radius: number;\n    /** Minimum nokta sayısı */\n    minPoints: number;\n    /** Mevcut zoom seviyesi */\n    zoom: number;\n    /** Harita extent: [minX, minY, maxX, maxY] */\n    extent: Float64Array;\n}\nexport interface ClusterResult {\n    /** Küme merkez noktaları: [x0, y0, x1, y1, ...] */\n    centroids: Float64Array;\n    /** Her kümedeki nokta sayısı */\n    counts: Uint32Array;\n    /** Her orijinal noktanın ait olduğu küme index'i (-1 = kümelenmemiş) */\n    assignments: Int32Array;\n}\nexport interface SpatialIndexHandle {\n    /** Opaque handle — WASM tarafında tutulan index'e referans */\n    readonly _handle: number;\n}\nexport interface SpatialQueryResult {\n    /** Bulunan feature id'leri */\n    ids: Uint32Array;\n}\nexport interface LosResult {\n    /** Görüş var mı? */\n    visible: boolean;\n    /** Engel noktası (varsa): [x, y, z] */\n    blockingPoint: Float64Array | null;\n    /** Profil: [distance0, elevation0, distance1, elevation1, ...] */\n    profile: Float64Array;\n}\nexport interface BinaryFeatureBuffer {\n    /** Geometry tipi: 1=Point, 2=LineString, 3=Polygon, 4=Multi... */\n    geometryType: number;\n    /** Vertex pozisyonları: [x0, y0, x1, y1, ...] */\n    positions: Float64Array;\n    /** Geometry başlangıç index'leri (multi-part ve polygon ring sınırları) */\n    offsets: Uint32Array;\n    /** Feature id'leri */\n    featureIds: Uint32Array;\n    /** Feature sayısı */\n    featureCount: number;\n}\nexport interface IWasmCore {\n    /** WASM modülünü yükle ve initialize et */\n    init(): Promise<void>;\n    /** Koordinat dönüşümü (toplu) */\n    reprojectPoints(coords: Float64Array, fromEpsg: number, toEpsg: number): Float64Array;\n    /** Polygon triangulation (earcut) */\n    triangulate(vertices: Float64Array, holeIndices: Uint32Array): TriangulateResult;\n    /** Line tessellation — GPU'ya hazır vertex üretimi */\n    tessellateLines(positions: Float64Array, offsets: Uint32Array, lineWidth: number): Float64Array;\n    /** Nokta kümeleme */\n    clusterPoints(points: Float64Array, options: ClusterOptions): ClusterResult;\n    /** R-tree spatial index oluştur */\n    buildSpatialIndex(points: Float64Array): SpatialIndexHandle;\n    /** BBOX sorgusu */\n    querySpatialIndex(handle: SpatialIndexHandle, bbox: Float64Array): SpatialQueryResult;\n    /** LOS segment'leri üret (observer→target arası N sample) */\n    generateLosSegments(observer: Float64Array, target: Float64Array, sampleCount: number): Float64Array;\n    /** Yükseklik karşılaştırması ile LOS hesabı */\n    computeLos(segments: Float64Array, elevations: Float64Array, observerOffset: number, targetOffset: number): LosResult;\n    /** GeoJSON → binary feature buffer */\n    parseGeojson(json: string): BinaryFeatureBuffer;\n    /** Mapbox Vector Tile → binary feature buffer */\n    parseMvt(tile: Uint8Array): BinaryFeatureBuffer;\n    /** WGS84 geodetic → ECEF cartesian (toplu) */\n    geodeticToEcef(coords: Float64Array): Float64Array;\n    /** ECEF → double-encoded float32 pairs (globe rendering için) */\n    encodeEcefDouble(ecefCoords: Float64Array): Float32Array;\n    /** Kaynakları serbest bırak */\n    destroy(): void;\n}\n//# sourceMappingURL=IWasmCore.d.ts.map","interfaces/IWidget.d.ts":"/**\n * Widget Interface Contract\n *\n * Framework-agnostic widget sözleşmesi.\n * Widgets Agent bu interface'i implement eder.\n */\nexport interface IView {\n    readonly id: string;\n    readonly type: '2d' | '3d';\n    /** Canvas element for DOM attachment. */\n    readonly canvas: HTMLCanvasElement | null;\n    /** Convert geographic [lon, lat] to screen pixel [x, y]. */\n    toScreen(lon: number, lat: number): [number, number] | null;\n    /** Subscribe to view events. */\n    on(event: string, callback: (...args: unknown[]) => void): void;\n    /** Unsubscribe from view events. */\n    off(event: string, callback: (...args: unknown[]) => void): void;\n}\nexport interface IWidget {\n    /** Unique widget identifier */\n    readonly id: string;\n    /** Widget'ı DOM container'a bağla */\n    mount(container: HTMLElement): void;\n    /** Widget'ı DOM'dan kaldır */\n    unmount(): void;\n    /** Widget'ı bir view'a bağla (harita verisi/event akışı) */\n    bind(view: IView): void;\n    /** Widget'ı devre dışı bırak */\n    destroy(): void;\n}\nexport type WidgetPosition = 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'manual';\n//# sourceMappingURL=IWidget.d.ts.map","interfaces/index.d.ts":"export type { IWasmCore, TriangulateResult, ClusterOptions, ClusterResult, SpatialIndexHandle, SpatialQueryResult, LosResult, BinaryFeatureBuffer, } from './IWasmCore';\nexport type { VectorRenderableSymbol, VectorTilePerformanceMode, VectorTilePerformanceOptions, SerializableRendererSnapshot, SerializableSimpleRendererSnapshot, SerializableUniqueValueInfo, SerializableUniqueValueRendererSnapshot, SerializableClassBreakInfo, SerializableClassBreaksRendererSnapshot, VectorTileBinaryPointGroup, VectorTileBinaryLineGroup, VectorTileBinaryPolygonGroup, VectorTileBinaryModelGroup, VectorTileBinaryExtrusionGroup, VectorTileBinaryPayload, VectorTileWorkerFeatureGeometry, VectorTileWorkerFeature, VectorTileWorkerRequest, VectorTileWorkerResponse, } from './IVectorTileWorker';\nexport type { IRenderEngine, RenderMode, GpuCapabilities, GpuFeatures, GpuLimits, CameraState, CustomDrawCall, DepthConfig, PointSymbol, LineSymbol, PolygonSymbol, ModelSymbol, ExtrudedPolygonSymbol, Mesh3DSymbol, ExtrusionAnimationConfig, ExtrusionRenderBuffer, Mesh3DRenderBuffer, GltfSource, AmbientLight, DirectionalLight, LightConfig, TextSymbol, PointRenderBuffer, LineRenderBuffer, PolygonRenderBuffer, ModelRenderBuffer, TextRenderBuffer, ImageryTile, GlobeImageryTile, GlobeTerrainLighting, FeaturePickResult, GpuMemoryAccounting, } from './IRenderEngine';\nexport { DEPTH_STANDARD, DEPTH_REVERSED_Z, } from './IRenderEngine';\nexport type { ILayer, IQueryableLayer, ITileLayer, IFeatureLayer, ITerrainLayer, ICustomShaderLayer, IClusterLayer, IDynamicPointLayer, TerrainHeightTileData, TerrainHillshadeTileData, TerrainLighting3DOptions, ClusterThemePreset, ClusterStyleConfig, ClusterGoToTarget, ClusterViewCallbacks, CustomVertexAttribute, CustomVertexBufferLayout, CustomDrawCommand, CustomTextureBinding, Extent, Feature, Geometry, GeometryType, LayerEvents, LayerError, QueryParams, } from './ILayer';\nexport { isTileLayer, isFeatureLayer, isCustomShaderLayer, isTerrainLayer, isClusterLayer, isDynamicPointLayer, } from './ILayer';\nexport type { IRenderer, Symbol, SymbolRenderContext, } from './IRenderer';\nexport type { IWidget, IView, WidgetPosition } from './IWidget';\nexport type { IMapImageryAdapter, IFeatureAdapter, IServiceDiscovery, MapImageryCapabilities, MapImageryLayerInfo, MapImageryRequest, FeatureInfoRequest, FeatureInfoResult, FeatureCollectionInfo, FeatureQueryParams, GeoJsonFeature, ServiceType, ServiceCapabilities, OgcError, } from './IOgcAdapter';\nexport type { IAnalysis, LosParams, LosAnalysisResult, ElevationQueryParams, ElevationQueryResult, BufferParams, BufferResult, RouteSampleParams, RouteSampleResult, } from './IAnalysis';\nexport type { FogConfig, NightImageryConfig, WaterMaskConfig, AtmosphereConfig, GlobeLightingConfig, PoleCapConfig, GlobeEffectsConfig, ResolvedGlobeEffects, } from './IGlobeEffects';\nexport { resolveGlobeEffects, FOG_WGSL_SNIPPET, NIGHT_BLEND_WGSL_SNIPPET, WATER_SPECULAR_WGSL_SNIPPET, } from './IGlobeEffects';\n//# sourceMappingURL=index.d.ts.map","renderers/CallbackRenderer.d.ts":"/**\n * CallbackRenderer — Per-feature symbology via user-supplied callback.\n *\n * Enables fully dynamic, data-driven styling where the user provides\n * a function that receives each feature (and optional render context)\n * and returns the appropriate symbol.\n *\n * The multi-group path in VectorBufferCache handles grouping\n * automatically — no cache changes needed.\n */\nimport type { IRenderer, Symbol, SymbolRenderContext } from '../interfaces/IRenderer';\nimport type { Feature } from '../interfaces/ILayer';\nexport type CallbackRendererFn = (feature: Feature, context?: SymbolRenderContext) => Symbol | null;\nexport declare class CallbackRenderer implements IRenderer {\n    readonly type = \"callback\";\n    private _fn;\n    constructor(fn: CallbackRendererFn);\n    getSymbol(feature: Feature, context?: SymbolRenderContext): Symbol | null;\n}\n//# sourceMappingURL=CallbackRenderer.d.ts.map","renderers/ClassBreaksRenderer.d.ts":"/**\n * ClassBreaksRenderer -- maps numeric attribute ranges (class breaks) to symbols.\n *\n * Each break defines a half-open interval `[min, max)`. The feature's\n * numeric attribute value is compared against breaks in order; the first\n * matching range determines the symbol. If no break matches (or the\n * attribute is non-numeric), the default symbol is used.\n *\n * Breaks should be contiguous and non-overlapping for predictable results.\n * Use `Infinity` as the `max` of the last break to capture all values\n * above a threshold.\n *\n * @example\n * ```ts\n * import { ClassBreaksRenderer } from '@mapgpu/core';\n *\n * const renderer = new ClassBreaksRenderer({\n *   field: 'population',\n *   defaultSymbol: { type: 'simple-marker', color: [128,128,128,255], size: 4 },\n *   breaks: [\n *     { min: 0,        max: 10000,    symbol: { type: 'simple-marker', color: [200,230,200,255], size: 4 } },\n *     { min: 10000,    max: 100000,   symbol: { type: 'simple-marker', color: [100,200,100,255], size: 8 } },\n *     { min: 100000,   max: 1000000,  symbol: { type: 'simple-marker', color: [0,150,0,255],     size: 12 } },\n *     { min: 1000000,  max: Infinity, symbol: { type: 'simple-marker', color: [0,80,0,255],      size: 18 } },\n *   ],\n * });\n * layer.renderer = renderer;\n * ```\n *\n * @module ClassBreaksRenderer\n */\nimport type { IRenderer, Symbol, SymbolRenderContext } from '../interfaces/IRenderer';\nimport type { Feature } from '../interfaces/ILayer';\n/**\n * Describes a single numeric class break: a half-open interval `[min, max)`\n * mapped to a symbol.\n *\n * @example\n * ```ts\n * const breakInfo: ClassBreakInfo = {\n *   min: 0,\n *   max: 100,\n *   symbol: { type: 'simple-fill', color: [0,255,0,128], outlineColor: [0,0,0,255], outlineWidth: 1 },\n * };\n * ```\n */\nexport interface ClassBreakInfo {\n    /**\n     * Lower bound of the interval (inclusive).\n     *\n     * A feature's attribute value must be `>= min` to match this break.\n     */\n    min: number;\n    /**\n     * Upper bound of the interval (exclusive).\n     *\n     * A feature's attribute value must be `< max` to match this break.\n     * Use `Infinity` for the final break to capture all remaining values.\n     */\n    max: number;\n    /**\n     * The symbol to render when the feature's attribute value falls within\n     * the `[min, max)` range.\n     */\n    symbol: Symbol;\n}\n/**\n * Configuration options for constructing a {@link ClassBreaksRenderer}.\n *\n * @example\n * ```ts\n * const options: ClassBreaksRendererOptions = {\n *   field: 'elevation',\n *   defaultSymbol: { type: 'simple-fill', color: [128,128,128,255], outlineColor: [0,0,0,255], outlineWidth: 1 },\n *   breaks: [\n *     { min: 0,    max: 500,      symbol: { type: 'simple-fill', color: [0,150,0,255],   outlineColor: [0,0,0,255], outlineWidth: 1 } },\n *     { min: 500,  max: 1500,     symbol: { type: 'simple-fill', color: [200,200,0,255], outlineColor: [0,0,0,255], outlineWidth: 1 } },\n *     { min: 1500, max: Infinity, symbol: { type: 'simple-fill', color: [180,80,0,255],  outlineColor: [0,0,0,255], outlineWidth: 1 } },\n *   ],\n * };\n * ```\n */\nexport interface ClassBreaksRendererOptions {\n    /**\n     * The numeric attribute field name to read from each feature.\n     *\n     * The value at `feature.attributes[field]` is tested against each break.\n     * Non-numeric values cause the default symbol to be returned.\n     */\n    field: string;\n    /**\n     * Fallback symbol used when:\n     * - The attribute value is not a number\n     * - The attribute is missing (`undefined` or `null`)\n     * - The numeric value does not fall within any break range\n     */\n    defaultSymbol: Symbol;\n    /**\n     * Ordered array of class breaks.\n     *\n     * Breaks are evaluated in array order; the first matching range wins.\n     * For best results, keep breaks contiguous (each break's `min` equals\n     * the previous break's `max`) and non-overlapping.\n     */\n    breaks: ClassBreakInfo[];\n    /** Optional zoom-sensitivity hint for cache invalidation in vector pipelines. */\n    zoomSensitive?: boolean;\n}\n/**\n * A renderer that assigns symbols based on numeric attribute ranges.\n *\n * Ideal for quantitative data such as population density, elevation,\n * temperature, or any continuous numeric attribute that should be\n * classified into discrete visual categories.\n *\n * The matching rule for each break is: `min <= value < max` (half-open\n * interval, inclusive lower bound, exclusive upper bound).\n *\n * @example\n * ```ts\n * const renderer = new ClassBreaksRenderer({\n *   field: 'temperature',\n *   defaultSymbol: { type: 'simple-marker', color: [128,128,128,255], size: 6 },\n *   breaks: [\n *     { min: -40, max: 0,   symbol: { type: 'simple-marker', color: [0,0,255,255],   size: 8 } },\n *     { min: 0,   max: 20,  symbol: { type: 'simple-marker', color: [0,200,0,255],   size: 8 } },\n *     { min: 20,  max: 35,  symbol: { type: 'simple-marker', color: [255,200,0,255], size: 8 } },\n *     { min: 35,  max: 60,  symbol: { type: 'simple-marker', color: [255,0,0,255],   size: 8 } },\n *   ],\n * });\n *\n * // Feature with temperature=25 gets the yellow symbol (20 <= 25 < 35)\n * const sym = renderer.getSymbol({ id: 1, geometry: g, attributes: { temperature: 25 } });\n *\n * // Feature with temperature=-50 gets the default gray symbol (no break matches)\n * const sym2 = renderer.getSymbol({ id: 2, geometry: g, attributes: { temperature: -50 } });\n * ```\n */\nexport declare class ClassBreaksRenderer implements IRenderer {\n    /** Discriminant identifying this as a class-breaks renderer. Always `'class-breaks'`. */\n    readonly type = \"class-breaks\";\n    /**\n     * The numeric attribute field name used for classification.\n     *\n     * The renderer reads `feature.attributes[field]` and checks whether\n     * the value is a number before testing it against breaks.\n     */\n    readonly field: string;\n    /**\n     * Fallback symbol returned when the attribute is non-numeric, missing,\n     * or does not fall within any defined break range.\n     */\n    readonly defaultSymbol: Symbol;\n    /**\n     * Immutable array of class breaks, evaluated in order.\n     *\n     * The first break whose range `[min, max)` contains the attribute\n     * value is used. Subsequent breaks are not tested.\n     */\n    readonly breaks: readonly ClassBreakInfo[];\n    readonly zoomSensitive?: boolean;\n    /**\n     * Create a new ClassBreaksRenderer.\n     *\n     * @param options - Configuration specifying the field, default symbol,\n     *   and the ordered array of class breaks.\n     *\n     * @example\n     * ```ts\n     * const renderer = new ClassBreaksRenderer({\n     *   field: 'magnitude',\n     *   defaultSymbol: { type: 'simple-marker', color: [200,200,200,255], size: 4 },\n     *   breaks: [\n     *     { min: 0, max: 3,        symbol: { type: 'simple-marker', color: [0,200,0,255],   size: 5 } },\n     *     { min: 3, max: 5,        symbol: { type: 'simple-marker', color: [255,165,0,255], size: 9 } },\n     *     { min: 5, max: Infinity, symbol: { type: 'simple-marker', color: [255,0,0,255],   size: 14 } },\n     *   ],\n     * });\n     * ```\n     */\n    constructor(options: ClassBreaksRendererOptions);\n    /**\n     * Determine the symbol for a feature by classifying its numeric attribute.\n     *\n     * Reads `feature.attributes[this.field]` and returns:\n     * - The symbol from the first break where `min <= value < max`\n     * - {@link defaultSymbol} if the value is not a number\n     * - {@link defaultSymbol} if no break range matches\n     *\n     * This method never returns `null` -- all features are always drawn.\n     *\n     * @param feature - The feature whose numeric attribute is classified.\n     * @param _context - Optional render context (currently unused by this renderer).\n     * @returns The symbol for the matching class break, or the default symbol.\n     *\n     * @example\n     * ```ts\n     * const feature = { id: 7, geometry: geom, attributes: { population: 250000 } };\n     * const sym = renderer.getSymbol(feature);\n     * // Returns the symbol whose break range contains 250000\n     * ```\n     */\n    getSymbol(feature: Feature, _context?: SymbolRenderContext): Symbol;\n}\n//# sourceMappingURL=ClassBreaksRenderer.d.ts.map","renderers/SimpleRenderer.d.ts":"/**\n * SimpleRenderer -- applies the same symbol to every feature in a layer.\n *\n * This is the most basic renderer: all features share a single symbol\n * regardless of their attributes. Use it when uniform styling is desired\n * (e.g., all polygons the same color).\n *\n * @example\n * ```ts\n * import { SimpleRenderer } from '@mapgpu/core';\n *\n * // All features rendered as red filled polygons:\n * const renderer = new SimpleRenderer({\n *   type: 'simple-fill',\n *   color: [255, 0, 0, 180],\n *   outlineColor: [0, 0, 0, 255],\n *   outlineWidth: 2,\n * });\n * layer.renderer = renderer;\n * ```\n *\n * @example\n * ```ts\n * // All features rendered as blue markers:\n * const renderer = new SimpleRenderer({\n *   type: 'simple-marker',\n *   color: [0, 100, 255, 255],\n *   size: 10,\n *   outlineColor: [255, 255, 255, 255],\n *   outlineWidth: 1,\n * });\n * ```\n *\n * @module SimpleRenderer\n */\nimport type { IRenderer, Symbol, SymbolRenderContext } from '../interfaces/IRenderer';\nimport type { Feature } from '../interfaces/ILayer';\n/**\n * A renderer that applies a single, uniform symbol to every feature.\n *\n * The simplest renderer implementation -- `getSymbol()` always returns\n * the same symbol, ignoring both the feature's attributes and the\n * render context.\n *\n * @example\n * ```ts\n * const renderer = new SimpleRenderer({\n *   type: 'simple-line',\n *   color: [0, 0, 0, 255],\n *   width: 2,\n *   style: 'solid',\n * });\n *\n * const sym = renderer.getSymbol(anyFeature);\n * // sym is always the line symbol above\n * ```\n */\nexport declare class SimpleRenderer implements IRenderer {\n    /** Discriminant identifying this as a simple renderer. Always `'simple'`. */\n    readonly type = \"simple\";\n    /**\n     * The symbol applied to all features.\n     *\n     * Set once at construction time and never changes. Can be a\n     * {@link PointSymbol}, {@link LineSymbol}, or {@link PolygonSymbol}.\n     */\n    readonly symbol: Symbol;\n    /**\n     * Create a new SimpleRenderer.\n     *\n     * @param symbol - The symbol to apply to every feature. Must match the\n     *   geometry type of the layer (e.g., a PolygonSymbol for polygon layers).\n     *\n     * @example\n     * ```ts\n     * const renderer = new SimpleRenderer({\n     *   type: 'simple-fill',\n     *   color: [100, 149, 237, 200],\n     *   outlineColor: [25, 25, 112, 255],\n     *   outlineWidth: 1,\n     * });\n     * ```\n     */\n    constructor(symbol: Symbol);\n    /**\n     * Returns the same symbol for every feature.\n     *\n     * Both parameters are ignored -- the constructor-provided symbol is\n     * always returned. This method never returns `null`, so no features\n     * are hidden.\n     *\n     * @param _feature - The feature to symbolize (ignored).\n     * @param _context - Optional render context (ignored).\n     * @returns The single symbol assigned to this renderer.\n     *\n     * @example\n     * ```ts\n     * const sym = renderer.getSymbol(feature, { zoom: 5, resolution: 4891.97 });\n     * // Always returns the same symbol regardless of feature or zoom\n     * ```\n     */\n    getSymbol(_feature: Feature, _context?: SymbolRenderContext): Symbol;\n}\n//# sourceMappingURL=SimpleRenderer.d.ts.map","renderers/UniqueValueRenderer.d.ts":"/**\n * UniqueValueRenderer -- maps discrete attribute values to individual symbols.\n *\n * Each distinct value of a specified attribute field is associated with its\n * own symbol. Features whose attribute value is not in the map receive the\n * default symbol.\n *\n * Internally uses a `Map<string | number, Symbol>` for O(1) lookups.\n *\n * @example\n * ```ts\n * import { UniqueValueRenderer } from '@mapgpu/core';\n *\n * const renderer = new UniqueValueRenderer({\n *   field: 'landuse',\n *   defaultSymbol: {\n *     type: 'simple-fill',\n *     color: [128, 128, 128, 255],\n *     outlineColor: [0, 0, 0, 255],\n *     outlineWidth: 1,\n *   },\n *   uniqueValues: [\n *     {\n *       value: 'residential',\n *       symbol: { type: 'simple-fill', color: [255, 255, 150, 255], outlineColor: [0, 0, 0, 255], outlineWidth: 1 },\n *     },\n *     {\n *       value: 'commercial',\n *       symbol: { type: 'simple-fill', color: [150, 150, 255, 255], outlineColor: [0, 0, 0, 255], outlineWidth: 1 },\n *     },\n *     {\n *       value: 'industrial',\n *       symbol: { type: 'simple-fill', color: [200, 150, 200, 255], outlineColor: [0, 0, 0, 255], outlineWidth: 1 },\n *     },\n *   ],\n * });\n * layer.renderer = renderer;\n * ```\n *\n * @module UniqueValueRenderer\n */\nimport type { IRenderer, Symbol, SymbolRenderContext } from '../interfaces/IRenderer';\nimport type { Feature } from '../interfaces/ILayer';\n/**\n * Describes a mapping from a single attribute value to a symbol.\n *\n * Used as an entry in {@link UniqueValueRendererOptions.uniqueValues}.\n *\n * @example\n * ```ts\n * const info: UniqueValueInfo = {\n *   value: 'highway',\n *   symbol: { type: 'simple-line', color: [255, 100, 0, 255], width: 3, style: 'solid' },\n * };\n * ```\n */\nexport interface UniqueValueInfo {\n    /**\n     * The attribute value to match against.\n     *\n     * Compared using strict equality (`===`) with the feature's attribute\n     * value after a `Map.get()` lookup. Both strings and numbers are\n     * supported.\n     */\n    value: string | number;\n    /**\n     * The symbol to render when the feature's attribute matches {@link value}.\n     */\n    symbol: Symbol;\n}\n/**\n * Configuration options for constructing a {@link UniqueValueRenderer}.\n *\n * @example\n * ```ts\n * const options: UniqueValueRendererOptions = {\n *   field: 'status',\n *   defaultSymbol: { type: 'simple-marker', color: [128,128,128,255], size: 6 },\n *   uniqueValues: [\n *     { value: 'active',   symbol: { type: 'simple-marker', color: [0,255,0,255], size: 8 } },\n *     { value: 'inactive', symbol: { type: 'simple-marker', color: [255,0,0,255], size: 6 } },\n *   ],\n * };\n * ```\n */\nexport interface UniqueValueRendererOptions {\n    /**\n     * The attribute field name to read from each feature's `attributes` object.\n     *\n     * The value at `feature.attributes[field]` is used for the symbol lookup.\n     */\n    field: string;\n    /**\n     * Fallback symbol used when a feature's attribute value is `null`,\n     * `undefined`, or not present in the {@link uniqueValues} map.\n     */\n    defaultSymbol: Symbol;\n    /**\n     * Array of value-to-symbol mappings.\n     *\n     * Each entry associates a discrete attribute value with a symbol.\n     * If duplicate values are provided, the last one wins (standard\n     * `Map` constructor behavior).\n     */\n    uniqueValues: UniqueValueInfo[];\n    /** Optional zoom-sensitivity hint for cache invalidation in vector pipelines. */\n    zoomSensitive?: boolean;\n}\n/**\n * A renderer that assigns symbols based on discrete attribute values.\n *\n * Ideal for categorical data such as land-use types, road classifications,\n * or status codes where each distinct value should have its own visual style.\n *\n * @example\n * ```ts\n * const renderer = new UniqueValueRenderer({\n *   field: 'type',\n *   defaultSymbol: { type: 'simple-fill', color: [128,128,128,255], outlineColor: [0,0,0,255], outlineWidth: 1 },\n *   uniqueValues: [\n *     { value: 'park',   symbol: { type: 'simple-fill', color: [0,180,0,255], outlineColor: [0,0,0,255], outlineWidth: 1 } },\n *     { value: 'water',  symbol: { type: 'simple-fill', color: [0,100,255,255], outlineColor: [0,0,0,255], outlineWidth: 1 } },\n *   ],\n * });\n *\n * const sym = renderer.getSymbol({ id: 1, geometry: g, attributes: { type: 'park' } });\n * // Returns the green park symbol\n *\n * const sym2 = renderer.getSymbol({ id: 2, geometry: g, attributes: { type: 'unknown' } });\n * // Returns the gray default symbol\n * ```\n */\nexport declare class UniqueValueRenderer implements IRenderer {\n    /** Discriminant identifying this as a unique-value renderer. Always `'unique-value'`. */\n    readonly type = \"unique-value\";\n    /**\n     * The attribute field name used for symbol lookup.\n     *\n     * The renderer reads `feature.attributes[field]` to determine which\n     * symbol to return.\n     */\n    readonly field: string;\n    /**\n     * Fallback symbol returned when the feature's attribute value does not\n     * match any entry in the value map, or when the attribute is missing.\n     */\n    readonly defaultSymbol: Symbol;\n    /** Public copy of configured unique-value mapping (serializable). */\n    readonly uniqueValues: readonly UniqueValueInfo[];\n    readonly zoomSensitive?: boolean;\n    /**\n     * Internal lookup map from attribute value to symbol.\n     *\n     * Built once at construction time from the provided `uniqueValues` array.\n     * @internal\n     */\n    private _map;\n    /**\n     * Create a new UniqueValueRenderer.\n     *\n     * @param options - Configuration specifying the field, default symbol,\n     *   and the array of value-to-symbol mappings.\n     *\n     * @example\n     * ```ts\n     * const renderer = new UniqueValueRenderer({\n     *   field: 'category',\n     *   defaultSymbol: { type: 'simple-marker', color: [128,128,128,255], size: 6 },\n     *   uniqueValues: [\n     *     { value: 'A', symbol: { type: 'simple-marker', color: [255,0,0,255], size: 8 } },\n     *     { value: 'B', symbol: { type: 'simple-marker', color: [0,0,255,255], size: 8 } },\n     *   ],\n     * });\n     * ```\n     */\n    constructor(options: UniqueValueRendererOptions);\n    /**\n     * Look up the symbol for a feature based on its attribute value.\n     *\n     * Reads `feature.attributes[this.field]` and performs an O(1) map\n     * lookup. Returns {@link defaultSymbol} when:\n     * - The attribute value is `null` or `undefined`\n     * - The value is not present in the unique values map\n     *\n     * This method never returns `null` -- all features are always drawn.\n     *\n     * @param feature - The feature whose attribute value determines the symbol.\n     * @param _context - Optional render context (currently unused by this renderer).\n     * @returns The matched symbol, or the default symbol if no match is found.\n     *\n     * @example\n     * ```ts\n     * const feature = { id: 42, geometry: geom, attributes: { landuse: 'residential' } };\n     * const sym = renderer.getSymbol(feature);\n     * // Returns the symbol mapped to 'residential', or defaultSymbol if unmapped\n     * ```\n     */\n    getSymbol(feature: Feature, _context?: SymbolRenderContext): Symbol;\n}\n//# sourceMappingURL=UniqueValueRenderer.d.ts.map","renderers/index.d.ts":"export { SimpleRenderer } from './SimpleRenderer';\nexport { UniqueValueRenderer } from './UniqueValueRenderer';\nexport type { UniqueValueInfo, UniqueValueRendererOptions } from './UniqueValueRenderer';\nexport { ClassBreaksRenderer } from './ClassBreaksRenderer';\nexport type { ClassBreakInfo, ClassBreaksRendererOptions } from './ClassBreaksRenderer';\nexport { CallbackRenderer } from './CallbackRenderer';\nexport type { CallbackRendererFn } from './CallbackRenderer';\n//# sourceMappingURL=index.d.ts.map","temporal/Clock.d.ts":"/**\n * Clock — Simulation time controller for temporal visualization.\n *\n * CesiumJS Clock equivalent. Drives time-dynamic features, CZML playback,\n * and any animated layer that needs a shared notion of \"current time\".\n */\nimport { JulianDate } from './JulianDate';\nexport type ClockRange = 'UNBOUNDED' | 'CLAMPED' | 'LOOP_STOP';\nexport type ClockStep = 'SYSTEM_CLOCK' | 'SYSTEM_CLOCK_MULTIPLIER' | 'TICK_DEPENDENT';\nexport interface ClockEvents {\n    [key: string]: unknown;\n    /** Fired every tick with the current simulation time. */\n    tick: {\n        time: JulianDate;\n        deltaSeconds: number;\n    };\n    /** Fired when playback starts or stops. */\n    'playback-change': {\n        shouldAnimate: boolean;\n    };\n}\nexport interface ClockOptions {\n    /** Start of the simulation time range. */\n    startTime?: JulianDate;\n    /** End of the simulation time range. */\n    stopTime?: JulianDate;\n    /** Initial current time. Defaults to startTime. */\n    currentTime?: JulianDate;\n    /** Time multiplier. 1 = real-time, 60 = 1 minute per second, etc. */\n    multiplier?: number;\n    /** Whether the clock is animating. Default: false. */\n    shouldAnimate?: boolean;\n    /** Behavior when reaching the end of the range. Default: 'LOOP_STOP'. */\n    clockRange?: ClockRange;\n    /** Clock step mode. Default: 'SYSTEM_CLOCK_MULTIPLIER'. */\n    clockStep?: ClockStep;\n}\nexport declare class Clock {\n    startTime: JulianDate;\n    stopTime: JulianDate;\n    currentTime: JulianDate;\n    multiplier: number;\n    shouldAnimate: boolean;\n    clockRange: ClockRange;\n    clockStep: ClockStep;\n    private _events;\n    private _rafId;\n    private _lastRealTime;\n    constructor(options?: ClockOptions);\n    /** Subscribe to clock events. */\n    on<K extends keyof ClockEvents>(event: K, handler: (data: ClockEvents[K]) => void): void;\n    /** Unsubscribe from clock events. */\n    off<K extends keyof ClockEvents>(event: K, handler: (data: ClockEvents[K]) => void): void;\n    /**\n     * Advance the clock by the given real-world delta (milliseconds).\n     * Call this from the render loop or manually.\n     */\n    tick(realDeltaMs: number): JulianDate;\n    /** Start automatic ticking via requestAnimationFrame. */\n    start(): void;\n    /** Stop automatic ticking. */\n    stop(): void;\n    /** Reset to start time. */\n    reset(): void;\n    /** Set time range from ISO 8601 interval string (e.g., \"start/stop\"). */\n    setInterval(interval: string): void;\n    /** Progress ratio 0..1 within the time range. */\n    get progress(): number;\n    /** Destroy and stop all animation frames. */\n    destroy(): void;\n    private _applyRange;\n    private _scheduleFrame;\n}\n//# sourceMappingURL=Clock.d.ts.map","temporal/JulianDate.d.ts":"/**\n * JulianDate — High-precision date/time representation for temporal simulation.\n *\n * CesiumJS JulianDate equivalent. Uses day number + seconds-of-day to avoid\n * floating-point precision loss at large time ranges.\n */\nexport declare class JulianDate {\n    /** Integer Julian day number. */\n    readonly dayNumber: number;\n    /** Seconds within the day [0, 86400). */\n    readonly secondsOfDay: number;\n    constructor(dayNumber?: number, secondsOfDay?: number);\n    /** Create from a JavaScript Date. */\n    static fromDate(date: Date): JulianDate;\n    /** Create from an ISO 8601 string. */\n    static fromIso8601(iso: string): JulianDate;\n    /** Create from epoch milliseconds. */\n    static fromEpochMs(ms: number): JulianDate;\n    /** Current time. */\n    static now(): JulianDate;\n    /** Convert to JavaScript Date. */\n    toDate(): Date;\n    /** Convert to ISO 8601 string. */\n    toIso8601(): string;\n    /** Convert to epoch milliseconds. */\n    toEpochMs(): number;\n    /** Add seconds and return a new JulianDate. */\n    addSeconds(seconds: number): JulianDate;\n    /** Difference in seconds: this - other. */\n    secondsDifference(other: JulianDate): number;\n    /** Compare: -1 if this < other, 0 if equal, 1 if this > other. */\n    compare(other: JulianDate): -1 | 0 | 1;\n    /** Check equality. */\n    equals(other: JulianDate): boolean;\n    /** Linear interpolation between two dates. */\n    static lerp(a: JulianDate, b: JulianDate, t: number): JulianDate;\n    /** Clone. */\n    clone(): JulianDate;\n    toString(): string;\n}\n//# sourceMappingURL=JulianDate.d.ts.map","temporal/Property.d.ts":"/**\n * Property System — Time-dynamic property evaluation for entities.\n *\n * CesiumJS Property equivalent. Allows entity attributes (position, color, etc.)\n * to change over time with interpolation.\n */\nimport { JulianDate } from './JulianDate';\n/** A value that can be evaluated at a specific time. */\nexport interface IProperty<T> {\n    /** Get the value at the given time. */\n    getValue(time: JulianDate): T;\n    /** Whether this property changes over time. */\n    readonly isConstant: boolean;\n}\n/** A property with a fixed value that never changes. */\nexport declare class ConstantProperty<T> implements IProperty<T> {\n    readonly isConstant = true;\n    private _value;\n    constructor(value: T);\n    getValue(_time: JulianDate): T;\n    setValue(value: T): void;\n}\nexport type InterpolationType = 'linear' | 'step' | 'hermite';\n/**\n * A property with time-sampled values and interpolation.\n *\n * Samples are sorted by time. Between samples, values are interpolated.\n * Before the first sample or after the last, the boundary value is returned.\n */\nexport declare class SampledProperty implements IProperty<number[]> {\n    readonly isConstant = false;\n    private _samples;\n    private _interpolation;\n    private _dimension;\n    /**\n     * @param dimension Number of components per value (e.g., 3 for [lon, lat, alt]).\n     * @param interpolation Interpolation method between samples.\n     */\n    constructor(dimension?: number, interpolation?: InterpolationType);\n    get interpolation(): InterpolationType;\n    set interpolation(type: InterpolationType);\n    /** Number of samples. */\n    get length(): number;\n    /** Add a single sample. Maintains sorted order. */\n    addSample(time: JulianDate, value: number[]): void;\n    /**\n     * Add samples from a flat array with epoch.\n     * Format: [t0, v0_0, v0_1, ..., t1, v1_0, v1_1, ...]\n     * where t is seconds since epoch.\n     */\n    addSamplesFromEpoch(epoch: JulianDate, data: number[]): void;\n    getValue(time: JulianDate): number[];\n}\n/** A property whose value is computed on-demand by a callback function. */\nexport declare class CallbackProperty<T> implements IProperty<T> {\n    readonly isConstant: boolean;\n    private _callback;\n    constructor(callback: (time: JulianDate) => T, isConstant?: boolean);\n    getValue(time: JulianDate): T;\n    setCallback(callback: (time: JulianDate) => T): void;\n}\ninterface TimeInterval<T> {\n    start: JulianDate;\n    stop: JulianDate;\n    value: T;\n}\n/** A property with different constant values for different time intervals. */\nexport declare class TimeIntervalCollectionProperty<T> implements IProperty<T | undefined> {\n    readonly isConstant = false;\n    private _intervals;\n    addInterval(start: JulianDate, stop: JulianDate, value: T): void;\n    getValue(time: JulianDate): T | undefined;\n    get intervals(): readonly TimeInterval<T>[];\n}\nexport {};\n//# sourceMappingURL=Property.d.ts.map","temporal/index.d.ts":"export { JulianDate } from './JulianDate';\nexport { Clock } from './Clock';\nexport type { ClockRange, ClockStep, ClockEvents, ClockOptions } from './Clock';\nexport { ConstantProperty, SampledProperty, CallbackProperty, TimeIntervalCollectionProperty, } from './Property';\nexport type { IProperty, InterpolationType } from './Property';\n//# sourceMappingURL=index.d.ts.map","tools/ITool.d.ts":"/**\n * ITool — Drawing/editing tool interface contract.\n *\n * Strategy pattern: each tool implements this interface and is managed\n * by ToolManager. Tools receive pointer events, manage their own state\n * machine, and render previews via IPreviewLayer.\n */\nimport type { Feature, Geometry } from '../interfaces/ILayer';\nimport type { CommandSystem } from '../engine/CommandSystem';\nimport type { ToolEvents } from './ToolEvents';\nexport type ToolState = 'idle' | 'active' | 'drawing' | 'editing';\nexport type ToolCursor = 'default' | 'crosshair' | 'pointer' | 'grab' | 'move' | (string & {});\n/** Subset of GraphicsLayer used for tool preview rendering (rubber-band, vertex handles). */\nexport interface IPreviewLayer {\n    add(feature: Feature): void;\n    remove(id: string | number): boolean;\n    clear(): void;\n    readonly graphics: readonly Feature[];\n}\nexport interface ToolPointerEvent {\n    /** Pointer X position relative to the canvas. */\n    screenX: number;\n    /** Pointer Y position relative to the canvas. */\n    screenY: number;\n    /** Geographic [lon, lat] at the pointer position, or null if off-map. */\n    mapCoords: [number, number] | null;\n    /** The original DOM PointerEvent. */\n    originalEvent: PointerEvent;\n    /** Mouse button (0=left, 1=middle, 2=right). */\n    button: number;\n    /** Whether Shift key was held. */\n    shiftKey: boolean;\n    /** Whether Ctrl/Cmd key was held. */\n    ctrlKey: boolean;\n}\n/** Context provided to tools when activated. Contains view services. */\nexport interface ToolContext {\n    /** Convert screen coordinates to geographic [lon, lat]. */\n    toMap(screenX: number, screenY: number): [number, number] | null;\n    /** Convert geographic coordinates to screen [x, y]. */\n    toScreen(lon: number, lat: number): [number, number] | null;\n    /** The map canvas element. */\n    readonly canvas: HTMLCanvasElement;\n    /** Current rendering mode. */\n    readonly mode: '2d' | '3d';\n    /** Current zoom level. */\n    readonly zoom: number;\n    /** Preview layer for rubber-band / vertex handle rendering. */\n    readonly previewLayer: IPreviewLayer;\n    /** Command system for undo/redo. */\n    readonly commands: CommandSystem;\n    /** Mark the view as needing a re-render. */\n    markDirty(): void;\n    /** Emit a tool event. */\n    emitEvent<K extends keyof ToolEvents>(event: K, data: ToolEvents[K]): void;\n}\nexport interface ITool {\n    /** Unique tool identifier (e.g. 'draw-point', 'draw-polyline'). */\n    readonly id: string;\n    /** Human-readable tool name. */\n    readonly name: string;\n    /** Current tool state. */\n    readonly state: ToolState;\n    /** Current cursor to display. */\n    readonly cursor: ToolCursor;\n    /** Activate this tool with the given context. */\n    activate(context: ToolContext): void;\n    /** Deactivate this tool. */\n    deactivate(): void;\n    /** Handle pointer down. Return true if consumed. */\n    onPointerDown(e: ToolPointerEvent): boolean;\n    /** Handle pointer move. Return true if consumed. */\n    onPointerMove(e: ToolPointerEvent): boolean;\n    /** Handle pointer up. Return true if consumed. */\n    onPointerUp(e: ToolPointerEvent): boolean;\n    /** Handle double click. Return true if consumed. */\n    onDoubleClick(e: ToolPointerEvent): boolean;\n    /** Handle key down. Return true if consumed. */\n    onKeyDown(e: KeyboardEvent): boolean;\n    /** Cancel current in-progress operation (e.g. Escape). Tool stays active. */\n    cancel(): void;\n    /** Fully destroy the tool and release resources. */\n    destroy(): void;\n}\nexport type { Feature, Geometry };\n//# sourceMappingURL=ITool.d.ts.map","tools/ToolEvents.d.ts":"/**\n * ToolEvents — Typed event map for tool system.\n *\n * Used by ToolManager's EventBus for type-safe event dispatch.\n */\nimport type { Feature, Geometry } from '../interfaces/ILayer';\nexport interface ToolEvents {\n    [key: string]: unknown;\n    /** Fired when a tool is activated. */\n    'tool-activate': {\n        toolId: string;\n    };\n    /** Fired when a tool is deactivated. */\n    'tool-deactivate': {\n        toolId: string;\n    };\n    /** Fired when drawing starts (first vertex placed). */\n    'draw-start': {\n        toolId: string;\n        geometry: Geometry;\n    };\n    /** Fired when a vertex is added during drawing. */\n    'vertex-add': {\n        toolId: string;\n        coords: [number, number];\n        vertexIndex: number;\n    };\n    /** Fired when a vertex is removed during drawing. */\n    'vertex-remove': {\n        toolId: string;\n        vertexIndex: number;\n    };\n    /** Fired when drawing completes (feature finalized). */\n    'draw-complete': {\n        toolId: string;\n        feature: Feature;\n    };\n    /** Fired when drawing is cancelled (Escape). */\n    'draw-cancel': {\n        toolId: string;\n    };\n    /** Fired on cursor movement with map coordinates. */\n    'cursor-move': {\n        screenX: number;\n        screenY: number;\n        mapCoords: [number, number] | null;\n    };\n    /** Fired when a feature is selected in edit mode. */\n    'feature-select': {\n        feature: Feature;\n        layerId: string;\n    };\n    /** Fired when a feature is updated in edit mode. */\n    'feature-update': {\n        feature: Feature;\n        layerId: string;\n    };\n    /** Fired when undo/redo availability changes. */\n    'history-change': {\n        canUndo: boolean;\n        canRedo: boolean;\n    };\n    /** Fired when a measurement is completed. */\n    'measure-complete': {\n        toolId: string;\n        type: 'point' | 'distance' | 'area';\n        result: MeasurementResult;\n    };\n    /** Fired when measurements are cleared. */\n    'measure-clear': {\n        toolId: string;\n    };\n    /** Fired when LOS analysis completes (observer + target placed or updated). */\n    'los-update': {\n        toolId: string;\n        observer: [number, number];\n        target: [number, number];\n        observerOffset: number;\n        targetOffset: number;\n        result: import('../interfaces/IAnalysis').LosAnalysisResult;\n    };\n    /** Fired when LOS analysis is cleared. */\n    'los-clear': {\n        toolId: string;\n    };\n}\nexport interface MeasurementResult {\n    coordinates?: [number, number];\n    totalDistance?: number;\n    segmentDistances?: number[];\n    area?: number;\n    perimeter?: number;\n    vertices: [number, number][];\n}\n//# sourceMappingURL=ToolEvents.d.ts.map","tools/ToolManager.d.ts":"/**\n * ToolManager — Manages drawing/editing tool lifecycle.\n *\n * Responsibilities:\n * - Tool registry (register/unregister/activate/deactivate)\n * - Overlay div for pointer event interception\n * - ToolPointerEvent construction with coordinate conversion\n * - Double-click detection (timing-based)\n * - Keyboard dispatch (Escape→cancel, Ctrl+Z→undo, Ctrl+Shift+Z→redo)\n * - Wheel passthrough to map for zoom\n * - CommandSystem ownership for undo/redo\n * - EventBus<ToolEvents> for external listeners\n * - Cursor sync with active tool\n */\nimport { CommandSystem } from '../engine/CommandSystem';\nimport type { ITool, IPreviewLayer } from './ITool';\nimport type { ToolEvents } from './ToolEvents';\nexport interface ToolManagerOptions {\n    /** Optional pre-created preview layer. */\n    previewLayer?: IPreviewLayer;\n    /** Maximum undo history size. Default: 50. */\n    maxHistorySize?: number;\n    /** Allow wheel events to pass through for map zoom while drawing. Default: true. */\n    wheelPassthrough?: boolean;\n}\nexport declare class ToolManager {\n    private _tools;\n    private _activeTool;\n    private _overlay;\n    private _events;\n    private _commands;\n    private _previewLayer;\n    private _wheelPassthrough;\n    private _destroyed;\n    private _canvas;\n    private _container;\n    private _toMap;\n    private _toScreen;\n    private _getMode;\n    private _getZoom;\n    private _markDirty;\n    private _lastClickTime;\n    private _lastClickX;\n    private _lastClickY;\n    private _dblClickThreshold;\n    private _dblClickDistance;\n    private _boundPointerDown;\n    private _boundPointerMove;\n    private _boundPointerUp;\n    private _boundKeyDown;\n    private _boundWheel;\n    private _boundContextMenu;\n    constructor(options?: ToolManagerOptions);\n    /**\n     * Initialize with view references. Called by MapView's lazy getter.\n     */\n    init(refs: {\n        canvas: HTMLCanvasElement;\n        container: HTMLElement;\n        toMap: (sx: number, sy: number) => [number, number] | null;\n        toScreen: (lon: number, lat: number) => [number, number] | null;\n        getMode: () => '2d' | '3d';\n        getZoom: () => number;\n        markDirty: () => void;\n    }): void;\n    setPreviewLayer(layer: IPreviewLayer): void;\n    get previewLayer(): IPreviewLayer | null;\n    registerTool(tool: ITool): void;\n    unregisterTool(id: string): void;\n    getTool(id: string): ITool | undefined;\n    get tools(): ReadonlyMap<string, ITool>;\n    activateTool(id: string): void;\n    deactivateTool(): void;\n    get activeTool(): ITool | null;\n    get commands(): CommandSystem;\n    undo(): boolean;\n    redo(): boolean;\n    get canUndo(): boolean;\n    get canRedo(): boolean;\n    on<K extends keyof ToolEvents>(event: K, handler: (data: ToolEvents[K]) => void): void;\n    off<K extends keyof ToolEvents>(event: K, handler: (data: ToolEvents[K]) => void): void;\n    destroy(): void;\n    private _ensureOverlay;\n    private _removeOverlay;\n    private _onPointerDown;\n    private _onPointerMove;\n    private _onPointerUp;\n    private _onKeyDown;\n    private _onWheel;\n    private _buildPointerEvent;\n    private _buildContext;\n    private _syncCursor;\n    private _emitHistoryChange;\n}\n//# sourceMappingURL=ToolManager.d.ts.map","tools/index.d.ts":"/**\n * Tool system barrel exports.\n */\nexport type { ITool, ToolState, ToolCursor, ToolContext, ToolPointerEvent, IPreviewLayer, } from './ITool';\nexport type { ToolEvents, MeasurementResult } from './ToolEvents';\nexport { ToolManager } from './ToolManager';\nexport type { ToolManagerOptions } from './ToolManager';\n//# sourceMappingURL=index.d.ts.map","units/UnitManager.d.ts":"/**\n * UnitManager — Global unit/format management with reactive events.\n *\n * Manages distance, area, and coordinate format settings.\n * Emits 'units-change' whenever any setting changes so consumers\n * (e.g. measurement labels) can reactively re-format.\n */\nexport type DistanceUnit = 'metric' | 'imperial' | 'nautical';\nexport type AreaUnit = 'metric' | 'imperial';\nexport type CoordinateFormat = 'DD' | 'DMS' | 'MGRS';\nexport interface UnitManagerOptions {\n    distanceUnit?: DistanceUnit;\n    areaUnit?: AreaUnit;\n    coordinateFormat?: CoordinateFormat;\n}\nexport interface UnitManagerEvents {\n    [key: string]: unknown;\n    'units-change': {\n        distanceUnit: DistanceUnit;\n        areaUnit: AreaUnit;\n        coordinateFormat: CoordinateFormat;\n    };\n}\nexport declare class UnitManager {\n    private _distanceUnit;\n    private _areaUnit;\n    private _coordinateFormat;\n    private _events;\n    constructor(options?: UnitManagerOptions);\n    get distanceUnit(): DistanceUnit;\n    set distanceUnit(unit: DistanceUnit);\n    get areaUnit(): AreaUnit;\n    set areaUnit(unit: AreaUnit);\n    get coordinateFormat(): CoordinateFormat;\n    set coordinateFormat(format: CoordinateFormat);\n    /**\n     * Format a distance value in meters to the current distance unit.\n     * metric: < 1000m → \"X m\", >= 1000m → \"X.XX km\"\n     * imperial: < 5280ft → \"X ft\", >= 5280ft → \"X.XX mi\"\n     * nautical: < 1852m → \"X m\", >= 1852m → \"X.XX nmi\"\n     */\n    formatDistance(meters: number): string;\n    /**\n     * Format an area value in square meters to the current area unit.\n     * metric: < 1M m² → \"X m²\", >= 1M → \"X.XX km²\"\n     * imperial: < 43560 sqft → \"X sq ft\", >= → \"X.XX acres\"\n     */\n    formatArea(sqMeters: number): string;\n    /**\n     * Format a coordinate pair to the current coordinate format.\n     * DD: \"29.0784° E, 41.0082° N\"\n     * DMS: \"29° 04' 42.24\" E, 41° 0' 29.52\" N\"\n     * MGRS: \"35T LF 12345 67890\" (simplified)\n     */\n    formatCoordinate(lon: number, lat: number): string;\n    static formatDD(lon: number, lat: number): string;\n    static formatDMS(lon: number, lat: number): string;\n    static formatMGRS(lon: number, lat: number): string;\n    private static _toDMS;\n    on(event: 'units-change', handler: (data: UnitManagerEvents['units-change']) => void): void;\n    off(event: 'units-change', handler: (data: UnitManagerEvents['units-change']) => void): void;\n    destroy(): void;\n    private _emitChange;\n}\n//# sourceMappingURL=UnitManager.d.ts.map","units/index.d.ts":"/**\n * Unit management system barrel exports.\n */\nexport { UnitManager } from './UnitManager';\nexport type { DistanceUnit, AreaUnit, CoordinateFormat, UnitManagerOptions, UnitManagerEvents, } from './UnitManager';\n//# sourceMappingURL=index.d.ts.map","utils/simplify.d.ts":"/**\n * Douglas-Peucker polyline simplification algorithm.\n * Leaflet L.LineUtil.simplify() equivalent.\n *\n * Reduces the number of points in a polyline while preserving shape.\n * Works in any coordinate system (geographic, projected, or screen).\n */\n/**\n * Simplify a polyline using the Ramer-Douglas-Peucker algorithm.\n *\n * @param points - Array of [x, y] coordinate pairs.\n * @param tolerance - Maximum perpendicular distance threshold.\n *   Points closer than this to the simplified line are removed.\n * @returns Simplified array of [x, y] coordinate pairs.\n */\nexport declare function simplify(points: [number, number][], tolerance: number): [number, number][];\n//# sourceMappingURL=simplify.d.ts.map"},"@mapgpu/render-webgpu":{"bind-group-cache.d.ts":"/**\n * Bind Group Cache\n *\n * Pipeline + buffer + texture kombinasyonlarına göre GPUBindGroup cache.\n * getOrCreate pattern ile tekrar kullanım sağlar.\n * Buffer/texture release olduğunda invalidation yapar.\n * Memory accounting ile toplam cache boyutunu takip eder.\n */\nexport interface BindGroupCacheKey {\n    /** Pipeline label veya unique identifier */\n    pipelineId: string;\n    /** Bind group'taki buffer ve texture'ların unique ID'leri */\n    resourceIds: string[];\n}\nexport declare class BindGroupCache {\n    private readonly cache;\n    private totalEstimatedBytes;\n    /** Invalidation: resource ID -> cache key string set */\n    private readonly resourceToKeys;\n    /**\n     * Cache'ten var olan bind group'u al veya yeni oluştur.\n     *\n     * @param key Cache key\n     * @param create Factory: cache miss olduğunda bind group oluşturur\n     * @returns GPUBindGroup\n     */\n    getOrCreate(key: BindGroupCacheKey, create: () => GPUBindGroup): GPUBindGroup;\n    /**\n     * Belirli bir resource ID'si kullanılan tüm cache entry'lerini invalidate et.\n     * Buffer veya texture release/destroy olduğunda çağrılır.\n     *\n     * @param resourceId Release edilen resource'ın unique ID'si\n     */\n    invalidate(resourceId: string): void;\n    /**\n     * Cache'te bir key var mı kontrol et.\n     */\n    has(key: BindGroupCacheKey): boolean;\n    /**\n     * Tüm cache'i temizle.\n     */\n    clear(): void;\n    /**\n     * Cache'teki toplam entry sayısı.\n     */\n    get size(): number;\n    /**\n     * Tahmini cache memory overhead (bytes).\n     * Not: Bu GPU memory değil, cache metadata boyutudur.\n     */\n    get estimatedBytes(): number;\n}\n//# sourceMappingURL=bind-group-cache.d.ts.map","buffer-pool.d.ts":"/**\n * Buffer Pool Manager\n *\n * GPU buffer allocation, tracking, and release.\n * Persistent ve transient buffer ayrımı ile memory accounting sağlar.\n */\nimport type { GpuMemoryAccounting } from '@mapgpu/core';\nexport type BufferCategory = 'persistent' | 'transient';\nexport declare class BufferPool {\n    private readonly device;\n    private readonly tracked;\n    private persistentBytes;\n    private transientBytes;\n    constructor(device: GPUDevice);\n    /**\n     * Yeni GPU buffer oluştur ve takip et.\n     */\n    allocate(size: number, usage: GPUBufferUsageFlags, category?: BufferCategory): GPUBuffer;\n    /**\n     * Mevcut veriden GPU buffer oluştur (mapped at creation).\n     */\n    allocateWithData(data: ArrayBufferView, usage: GPUBufferUsageFlags, category?: BufferCategory): GPUBuffer;\n    /**\n     * GPU buffer'ı serbest bırak ve tracking'den çıkar.\n     */\n    release(buffer: GPUBuffer): void;\n    /**\n     * Tüm transient buffer'ları serbest bırak (her frame sonunda çağrılır).\n     */\n    releaseTransient(): void;\n    /**\n     * GPU memory accounting bilgisi döndür.\n     * textureBytes burada 0 döner;\n     * TextureManager tarafından ayrıca raporlanır.\n     */\n    getMemoryAccounting(): GpuMemoryAccounting;\n    /**\n     * Tüm buffer'ları serbest bırak.\n     */\n    destroy(): void;\n    /** Tracked buffer sayısı (diagnostics için). */\n    get trackedCount(): number;\n}\n//# sourceMappingURL=buffer-pool.d.ts.map","capabilities.d.ts":"/**\n * WebGPU Capability Detection\n *\n * Tarayıcı ve GPU özelliklerini algılar, render modunu belirler.\n */\nexport type RenderMode = 'full-gpu' | 'gpu-lite' | 'cpu-degraded';\nexport interface GpuFeatures {\n    timestampQuery: boolean;\n    float32Filterable: boolean;\n    indirectFirstInstance: boolean;\n    shaderF16: boolean;\n}\nexport interface GpuLimits {\n    maxTextureDimension2D: number;\n    maxBufferSize: number;\n    maxStorageBufferBindingSize: number;\n}\nexport interface GpuCapabilities {\n    mode: RenderMode;\n    features: GpuFeatures;\n    limits: GpuLimits;\n    adapter: GPUAdapter | null;\n    device: GPUDevice | null;\n}\nexport declare function detectCapabilities(): Promise<GpuCapabilities>;\n//# sourceMappingURL=capabilities.d.ts.map","cpu-cluster.d.ts":"/**\n * CPU Grid-Hash Clustering\n *\n * Pure TypeScript, zero GPU dependency, unit-testable.\n * Port of the GPU compute shader's two-pass algorithm (assignCells + finalizeClusters)\n * into a single-pass CPU implementation using a Map-based spatial hash.\n *\n * Output format matches ClusterOutput exactly (posX, posY, count, flags — 16 bytes),\n * so render pipelines consume the data identically.\n *\n * Additionally produces `membership` arrays for cluster-click fit-bounds:\n * membership[i] contains the source-point indices that belong to entries[i].\n */\nexport interface CpuClusterEntry {\n    posX: number;\n    posY: number;\n    count: number;\n    flags: number;\n}\nexport interface CpuClusterResult {\n    entries: CpuClusterEntry[];\n    membership: number[][];\n}\n/**\n * Grid-hash clustering in EPSG:3857 space.\n *\n * @param points         Flat [x,y,x,y,...] array of EPSG:3857 coordinates.\n * @param clusterRadius  Cluster radius in screen pixels.\n * @param zoom           Current map zoom level.\n * @param extent         Visible extent [minX, minY, maxX, maxY] in EPSG:3857.\n * @param minClusterCount Minimum point count to form a cluster (default 2).\n * @returns Cluster entries and membership arrays.\n */\nexport declare function gridCluster(points: Float32Array, clusterRadius: number, zoom: number, extent: [number, number, number, number], minClusterCount: number): CpuClusterResult;\n/**\n * Pack CpuClusterEntry[] into a Float32Array suitable for GPU storage buffer.\n * Layout matches ClusterOutput struct: posX(f32), posY(f32), count(u32), flags(u32).\n */\nexport declare function packClusterEntries(entries: CpuClusterEntry[]): Float32Array;\n//# sourceMappingURL=cpu-cluster.d.ts.map","draw-delegate-cluster.d.ts":"/**\n * Draw Delegate — Cluster (v2: CPU Clustering)\n *\n * Manages the cluster rendering lifecycle:\n * 1. Source buffer upload (persistent — survives across frames)\n * 2. CPU grid-hash clustering via gridCluster() — no GPU compute required\n * 3. Render dispatch (instanced billboard via drawIndirect)\n * 4. Canvas 2D digit atlas for readable count labels\n *\n * Key insight: render pipelines read `var<storage, read> clusters: array<ClusterOutput>`.\n * They don't know or care whether the data came from GPU compute or CPU.\n * Pipeline files (cluster-render-pipeline.ts, cluster-globe-render-pipeline.ts)\n * remain completely untouched.\n *\n * Eliminated:\n * - ClusterComputePipeline (no GPU compute dependency)\n * - cellsBuffer (no spatial hash on GPU)\n * - Render pass end/restart pattern (no compute pass interruption!)\n * - clearBuffer calls (no GPU-side state to reset)\n */\nimport type { ClusterStyleConfig } from '@mapgpu/core';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateCluster {\n    private readonly ctx;\n    private renderPipeline2D;\n    private renderPipelineGlobe;\n    private layerStates;\n    private digitAtlasTexture;\n    constructor(ctx: FrameContext);\n    setSource(layerId: string, points: Float32Array, version: number): void;\n    drawClusters(layerId: string, style: ClusterStyleConfig, clusterRadius: number, clusterMinPoints: number, zoom: number, extent: [number, number, number, number], globe: boolean): void;\n    private _draw2D;\n    private _drawGlobe;\n    /**\n     * Create a high-resolution RGBA texture containing digits 0-9.\n     * Each digit occupies one square cell. Rendered via Canvas 2D with stroked text\n     * for crisp, anti-aliased, readable text at all zoom levels.\n     */\n    private _createDigitAtlas;\n    /**\n     * Fallback bitmap digit atlas for environments without OffscreenCanvas.\n     * Scaled 5×7 bitmap font to fill dynamic cells.\n     */\n    private _createBitmapDigitAtlas;\n    destroy(): void;\n}\n//# sourceMappingURL=draw-delegate-cluster.d.ts.map","draw-delegate-custom.d.ts":"/**\n * Draw Delegate — Custom\n *\n * Custom WGSL shader draw calls with per-key pipeline caching.\n */\nimport type { CustomDrawCall } from '@mapgpu/core';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateCustom {\n    private readonly ctx;\n    private customPipelines;\n    /** Keys for which pipeline creation failed — prevents retrying every frame */\n    private customPipelineErrors;\n    private _customDrawDbgCount;\n    constructor(ctx: FrameContext);\n    drawCustom(call: CustomDrawCall): void;\n    destroy(): void;\n    reset(): void;\n}\n//# sourceMappingURL=draw-delegate-custom.d.ts.map","draw-delegate-extrusion.d.ts":"/**\n * Draw Delegate — Extrusion\n *\n * Handles extruded polygon (3D building) rendering for both 2D and globe modes.\n * Follows the same lazy-init delegate pattern as DrawDelegateVector.\n *\n * 2D material uniform layout (64 bytes, 16 floats):\n *   color: vec4<f32>       [0..15]\n *   ambient: f32           [16..19]\n *   debugMode: f32         [20..23]\n *   animProgress: f32      [24..27]\n *   animDuration: f32      [28..31]\n *   waveOrigin: vec2<f32>  [32..39]\n *   delayFactor: f32       [40..43]\n *   bearing: f32           [44..47]\n *   shininess: f32         [48..51]\n *   specularStrength: f32  [52..55]\n *   _pad1: f32             [56..59]\n *   _pad2: f32             [60..63]\n *\n * Globe material uniform layout (80 bytes, 20 floats):\n *   (same as 2D layout for first 16 floats)\n *   cameraPos: vec4<f32>   [64..79]  — xyz = Mercator [0..1] camera position\n */\nimport type { ExtrusionRenderBuffer, ExtrudedPolygonSymbol } from '@mapgpu/core';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateExtrusion {\n    private readonly ctx;\n    private extrusionPipeline;\n    private globeExtrusionPipeline;\n    private extrusionMaterials;\n    private globeExtrusionMaterials;\n    /** Tracks animation start per stable id (tileKey) or GPUBuffer fallback */\n    private animState;\n    /** Tile IDs whose grow animation has completed — never re-animate */\n    private animCompleted;\n    constructor(ctx: FrameContext);\n    private ensureExtrusionPipeline;\n    private ensureGlobeExtrusionPipeline;\n    drawExtrusion(buffer: ExtrusionRenderBuffer, symbol: ExtrudedPolygonSymbol): void;\n    drawGlobeExtrusion(buffer: ExtrusionRenderBuffer, symbol: ExtrudedPolygonSymbol): void;\n    private _createMaterialResource;\n    private getOrCreateMaterialResource;\n    private getOrCreateBindGroup;\n    private releaseMaterials;\n    destroy(): void;\n}\n//# sourceMappingURL=draw-delegate-extrusion.d.ts.map","draw-delegate-globe.d.ts":"/**\n * Draw Delegate — Globe\n *\n * Globe-specific vector rendering: drawGlobePoints, drawGlobeLines,\n * drawGlobePolygons, drawAtmosphere, drawPoleCaps.\n * Also handles globe icon pipeline routing.\n */\nimport type { LineRenderBuffer, LineSymbol, PointRenderBuffer, PointSymbol, PolygonRenderBuffer, PolygonSymbol, AtmosphereConfig } from '@mapgpu/core';\nimport { SpriteAtlas } from './sprite-atlas';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateGlobe {\n    private readonly ctx;\n    private readonly getIconAtlas;\n    private globePointPipeline;\n    private globeLinePipeline;\n    private globePolygonPipeline;\n    private poleCapPipeline;\n    private atmospherePipeline;\n    private globeIconPipeline;\n    private pointMaterials;\n    private lineMaterials;\n    private polygonMaterials;\n    private iconMaterials;\n    private atmosphereMaterials;\n    private poleCapMaterials;\n    private textureResourceIds;\n    private nextTextureResourceId;\n    constructor(ctx: FrameContext, getIconAtlas: () => SpriteAtlas);\n    private ensureGlobePointPipeline;\n    private ensureGlobeLinePipeline;\n    private ensureGlobePolygonPipeline;\n    private ensurePoleCapPipeline;\n    private ensureAtmospherePipeline;\n    private ensureGlobeIconPipeline;\n    private getOrCreateUniformResource;\n    private getOrCreateBindGroup;\n    private releaseUniformResources;\n    private getTextureResourceId;\n    drawGlobePoints(buffer: PointRenderBuffer, symbol: PointSymbol): void;\n    drawGlobeLines(buffer: LineRenderBuffer, symbol: LineSymbol): void;\n    drawGlobePolygons(buffer: PolygonRenderBuffer, symbol: PolygonSymbol): void;\n    drawPoleCaps(color: [number, number, number, number]): void;\n    drawAtmosphere(strength: number, config?: AtmosphereConfig): void;\n    /**\n     * Internal: Globe icon point rendering via sprite atlas.\n     */\n    private _drawGlobeIconPoints;\n    destroy(): void;\n    reset(): void;\n}\n//# sourceMappingURL=draw-delegate-globe.d.ts.map","draw-delegate-mesh3d.d.ts":"/**\n * Draw Delegate — Mesh3D\n *\n * Renders arbitrary 3D mesh geometry (Box, Cylinder, Sphere, Cone)\n * for both 2D and globe modes.\n *\n * Material uniform layout (32 bytes, 8 floats):\n *   color: vec4<f32>       [0..15]   — RGBA 0-1\n *   ambient: f32           [16..19]\n *   shininess: f32         [20..23]\n *   specularStrength: f32  [24..27]\n *   _pad: f32              [28..31]\n */\nimport type { Mesh3DRenderBuffer, Mesh3DSymbol } from '@mapgpu/core';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateMesh3D {\n    private readonly ctx;\n    private _pipeline;\n    private _globePipeline;\n    private _materials;\n    private _globeMaterials;\n    constructor(ctx: FrameContext);\n    private _ensurePipeline;\n    private _ensureGlobePipeline;\n    drawMesh3D(buffer: Mesh3DRenderBuffer, symbol: Mesh3DSymbol): void;\n    drawGlobeMesh3D(buffer: Mesh3DRenderBuffer, symbol: Mesh3DSymbol): void;\n    private _getOrCreateMaterial;\n    destroy(): void;\n}\n//# sourceMappingURL=draw-delegate-mesh3d.d.ts.map","draw-delegate-model.d.ts":"/**\n * Draw Delegate — Model\n *\n * 3D model rendering: drawModels (2D), drawGlobeModels (globe),\n * loadModel, and ModelManager integration.\n *\n * Iterates per-primitive for multi-mesh/multi-material models.\n */\nimport type { GltfSource, ModelRenderBuffer, ModelSymbol } from '@mapgpu/core';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateModel {\n    private readonly ctx;\n    private modelPipeline;\n    private globeModelPipeline;\n    private modelManager;\n    constructor(ctx: FrameContext);\n    private ensureModelPipeline;\n    private ensureGlobeModelPipeline;\n    private ensureModelManager;\n    loadModel(id: string, source: ArrayBuffer | GltfSource): Promise<void>;\n    drawModels(buffer: ModelRenderBuffer, symbol: ModelSymbol): void;\n    drawGlobeModels(buffer: ModelRenderBuffer, symbol: ModelSymbol): void;\n    destroy(): void;\n    reset(): void;\n    private _createMaterialBindGroup;\n}\n//# sourceMappingURL=draw-delegate-model.d.ts.map","draw-delegate-picking.d.ts":"/**\n * Draw Delegate — Picking\n *\n * GPU picking pass: renders feature IDs to an offscreen texture,\n * reads back the pixel at the given screen coordinate.\n */\nimport type { FeaturePickResult } from '@mapgpu/core';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegatePicking {\n    private readonly ctx;\n    private pickingPipeline;\n    constructor(ctx: FrameContext);\n    private ensurePickingPipeline;\n    pick(x: number, y: number): Promise<FeaturePickResult | null>;\n    destroy(): void;\n    reset(): void;\n}\n//# sourceMappingURL=draw-delegate-picking.d.ts.map","draw-delegate-raster.d.ts":"/**\n * Draw Delegate — Raster\n *\n * 2D raster tile rendering (drawImagery) and\n * globe raster tile rendering (drawGlobeTile).\n * Optional debug overlay: wireframe grid + vertex dots + tile borders.\n */\nimport type { GlobeImageryTile, ImageryTile } from '@mapgpu/core';\nimport { type GlobeRasterPipeline } from './pipelines/globe-raster-pipeline';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateRaster {\n    private readonly ctx;\n    private rasterPipeline;\n    private globeRasterPipeline;\n    private debugSuite2D;\n    private debugSuiteGlobe;\n    constructor(ctx: FrameContext);\n    initRasterPipeline(): void;\n    ensureGlobeRasterPipeline(): GlobeRasterPipeline;\n    private ensureDebugSuite2D;\n    private ensureDebugSuiteGlobe;\n    drawImagery(tile: ImageryTile): void;\n    drawGlobeTile(tile: GlobeImageryTile): void;\n    private _drawDebugOverlay;\n    destroy(): void;\n    reset(): void;\n}\n//# sourceMappingURL=draw-delegate-raster.d.ts.map","draw-delegate-vector.d.ts":"/**\n * Draw Delegate — Vector\n *\n * 2D vector rendering: drawPoints, drawLines, drawPolygons, drawText.\n * Also handles icon pipeline routing and post-process.\n */\nimport type { LineRenderBuffer, LineSymbol, PointRenderBuffer, PointSymbol, PolygonRenderBuffer, PolygonSymbol, TextRenderBuffer, TextSymbol } from '@mapgpu/core';\nimport { SpriteAtlas } from './sprite-atlas';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateVector {\n    private readonly ctx;\n    private readonly getIconAtlas;\n    private pointPipeline;\n    private linePipeline;\n    private polygonPipeline;\n    private textPipeline;\n    private postProcessPipeline;\n    private iconPipeline;\n    private pointMaterials;\n    private lineMaterials;\n    private polygonMaterials;\n    private textMaterials;\n    private iconMaterials;\n    private postProcessMaterials;\n    private textureResourceIds;\n    private nextTextureResourceId;\n    constructor(ctx: FrameContext, getIconAtlas: () => SpriteAtlas);\n    private ensurePointPipeline;\n    private ensureLinePipeline;\n    private ensurePolygonPipeline;\n    private ensureTextPipeline;\n    private ensurePostProcessPipeline;\n    private ensureIconPipeline;\n    private getOrCreateUniformResource;\n    private getOrCreateBindGroup;\n    private releaseUniformResources;\n    private getTextureResourceId;\n    drawPoints(buffer: PointRenderBuffer, symbol: PointSymbol): void;\n    drawLines(buffer: LineRenderBuffer, symbol: LineSymbol): void;\n    drawPolygons(buffer: PolygonRenderBuffer, symbol: PolygonSymbol): void;\n    drawText(buffer: TextRenderBuffer, symbol: TextSymbol): void;\n    drawPostProcess(sceneTexture: GPUTexture): void;\n    /**\n     * Internal: 2D icon point rendering via sprite atlas.\n     */\n    private _drawIconPoints;\n    /**\n     * Sprite atlas'a ikon ekle.\n     */\n    loadIcon(id: string, image: ImageBitmap): void;\n    destroy(): void;\n    reset(): void;\n}\n//# sourceMappingURL=draw-delegate-vector.d.ts.map","draw-delegate-water.d.ts":"/**\n * Draw Delegate — Water\n *\n * Globe ocean water rendering with mask-based geometry control.\n *\n * Two-pass approach:\n * 1. Mask pass: GeoJSON polygons → screen-sized R32Float texture\n *    (separate render pass, no depth, no MSAA)\n * 2. Ocean pass: Icosphere with Gerstner waves, samples mask in\n *    fragment shader to control where water is visible.\n *\n * Ocean renders ON TOP of tiles (depthCompare=always, depthWrite=false).\n * Mask mode determines interpretation:\n * - 'land': polygons = land → ocean renders OUTSIDE\n * - 'water': polygons = water → ocean renders INSIDE\n * - none: no mask → ocean covers full globe (fallback/no-tile mode)\n */\nimport type { GlobeWaterConfig } from '@mapgpu/core';\nimport type { FrameContext } from './frame-context';\nexport declare class DrawDelegateWater {\n    private readonly ctx;\n    private pipeline;\n    private maskPipeline;\n    private oceanMaterial;\n    private maskTexture;\n    private maskTextureView;\n    private maskBindGroup;\n    private maskVertexBuffer;\n    private maskIndexBuffer;\n    private maskIndexCount;\n    private maskWidth;\n    private maskHeight;\n    private fallbackMaskTexture;\n    private fallbackMaskBindGroup;\n    constructor(ctx: FrameContext);\n    private ensurePipeline;\n    private ensureMaskPipeline;\n    private ensureFallbackMask;\n    private ensureUniformResource;\n    private ensureMaskTexture;\n    private updateMaskBuffers;\n    drawGlobeWater(config: GlobeWaterConfig): void;\n    /**\n     * Render the mask pass if mask data is present and texture needs update.\n     *\n     * The mask geometry is static (GeoJSON doesn't change per frame),\n     * but the mask must be re-rendered when:\n     * - Canvas resizes (mask texture size changes)\n     * - First frame (mask not yet rendered)\n     * - Camera moves (globe projection changes the mask shape)\n     *\n     * Since camera moves every frame in interactive mode, we re-render\n     * the mask every frame. This is cheap: simple polygons, no MSAA, no depth.\n     *\n     * IMPORTANT: This must be called BEFORE the main render pass uses the mask.\n     * Since the main render pass is already open, we end it, do the mask pass,\n     * then the caller's subsequent drawGlobeWater runs in the already-open pass.\n     * Actually — we use a SEPARATE command encoder for the mask pass and\n     * submit it before the main frame's command buffer.\n     */\n    private _renderMaskIfNeeded;\n    destroy(): void;\n    reset(): void;\n}\n//# sourceMappingURL=draw-delegate-water.d.ts.map","frame-context.d.ts":"/**\n * Frame Context\n *\n * Shared GPU state and resources accessed by all draw delegates.\n * Contains device, queue, per-frame render pass, camera resources,\n * buffer pool, and depth/stencil configuration.\n */\nimport type { CameraState, DepthConfig, LightConfig } from '@mapgpu/core';\nimport { BufferPool } from './buffer-pool';\nimport type { BindGroupCache } from './bind-group-cache';\nimport type { HeightBrush } from './height-brush';\n/**\n * MSAA sample count for the main render pass.\n * All render pipelines (except picking) must use this value.\n * Picking has its own single-sample pass.\n */\nexport declare const MSAA_SAMPLE_COUNT = 4;\n/**\n * Camera uniform buffer layout (std140):\n * mat4x4<f32> viewProjection -> 64 bytes\n * vec2<f32> viewport         -> 8 bytes\n * padding                    -> 8 bytes (align to 16)\n * Total                      -> 80 bytes\n */\nexport declare const CAMERA_UNIFORM_SIZE = 80;\n/**\n * Globe camera uniform buffer layout (std140):\n * mat4x4<f32> viewProjection       ->  0-63  (64 bytes)\n * mat4x4<f32> flatViewProjection   -> 64-127 (64 bytes)\n * vec2<f32> viewport               -> 128-135 (8 bytes)\n * f32 projectionTransition         -> 136-139 (4 bytes)\n * f32 globeRadius                  -> 140-143 (4 bytes)\n * vec4<f32> clippingPlane          -> 144-159 (16 bytes)\n * Total                            -> 160 bytes\n */\nexport declare const GLOBE_CAMERA_UNIFORM_SIZE = 160;\n/**\n * Picking draw call record types — used by delegates to record draw calls\n * for the picking pass.\n */\nexport interface PickingDrawCallPoints {\n    type: 'points';\n    vertexBuffer: GPUBuffer;\n    vertexCount: number;\n    instanceCount: number;\n    layerId: string;\n}\nexport interface PickingDrawCallIndexed {\n    type: 'indexed';\n    vertexBuffer: GPUBuffer;\n    indexBuffer: GPUBuffer;\n    indexCount: number;\n    layerId: string;\n}\nexport type PickingDrawCall = PickingDrawCallPoints | PickingDrawCallIndexed;\n/**\n * Shared GPU state for a single render frame.\n * Created by RenderEngine, passed by reference to all delegates.\n */\nexport declare class FrameContext {\n    device: GPUDevice | null;\n    context: GPUCanvasContext | null;\n    colorFormat: GPUTextureFormat;\n    canvas: HTMLCanvasElement | null;\n    bufferPool: BufferPool | null;\n    bindGroupCache: BindGroupCache | null;\n    depthConfig: DepthConfig;\n    depthTexture: GPUTexture | null;\n    cameraBuffer: GPUBuffer | null;\n    cameraBindGroup: GPUBindGroup | null;\n    cameraBindGroupLayout: GPUBindGroupLayout | null;\n    globeCameraBuffer: GPUBuffer | null;\n    globeCameraBindGroup: GPUBindGroup | null;\n    globeCameraBindGroupLayout: GPUBindGroupLayout | null;\n    commandEncoder: GPUCommandEncoder | null;\n    renderPass: GPURenderPassEncoder | null;\n    currentCamera: CameraState | null;\n    frameTime: number;\n    pickingEnabled: boolean;\n    pickingDrawCalls: PickingDrawCall[];\n    currentLayerId: string;\n    placeholderTexture: GPUTexture | null;\n    sampleCount: number;\n    msaaColorTexture: GPUTexture | null;\n    lightConfig: LightConfig | null;\n    debugTileVertices: boolean;\n    extrusionDebugMode: boolean;\n    heightBrush: HeightBrush | null;\n    heightExaggeration: number;\n    /** Set by delegates when animations need continuous rendering (e.g. extrusion grow) */\n    needsContinuousRender: boolean;\n    deviceLost: boolean;\n    /**\n     * Lazy-init globe camera buffer, bind group, and bind group layout.\n     * Called from any delegate that needs globe rendering.\n     * Safe to call multiple times — only creates resources once.\n     */\n    ensureGlobeCameraResources(): void;\n    /**\n     * Write globe camera uniforms for the current frame.\n     * DRY helper — consolidates duplicate logic from globe draw methods.\n     */\n    ensureGlobeCameraWritten(): void;\n}\n//# sourceMappingURL=frame-context.d.ts.map","gltf-parser.d.ts":"/**\n * GLB / glTF 2.0 parser.\n *\n * Extracts ALL mesh primitives with per-primitive materials and\n * embedded base-color textures from a GLB ArrayBuffer.\n * Designed for loading 3-D models (buildings, vehicles, markers)\n * into the WebGPU pipeline.\n */\nexport interface ParsedGltfMesh {\n    positions: Float32Array;\n    normals: Float32Array;\n    texcoords: Float32Array;\n    indices: Uint16Array | Uint32Array;\n    vertexCount: number;\n    indexCount: number;\n}\nexport interface ParsedGltfMaterial {\n    baseColorFactor: [number, number, number, number];\n    metallicFactor: number;\n    roughnessFactor: number;\n    baseColorTextureIndex?: number;\n    normalTextureIndex?: number;\n    metallicRoughnessTextureIndex?: number;\n    occlusionTextureIndex?: number;\n    emissiveFactor: [number, number, number];\n    emissiveTextureIndex?: number;\n    alphaMode: 'OPAQUE' | 'MASK' | 'BLEND';\n    alphaCutoff: number;\n    doubleSided: boolean;\n    /** KHR_materials_unlit: skip all lighting, output base color directly */\n    unlit: boolean;\n}\n/** Embedded image data extracted from GLB binary chunk. */\nexport interface EmbeddedImageData {\n    data: Uint8Array;\n    mimeType: string;\n}\n/** A single draw-call unit: one mesh primitive + its material + optional texture data. */\nexport interface ParsedPrimitive {\n    mesh: ParsedGltfMesh;\n    material: ParsedGltfMaterial;\n    /** Embedded texture images, keyed by texture index. */\n    imageData: Map<number, EmbeddedImageData>;\n    name?: string;\n}\nexport interface ParsedGltf {\n    primitives: ParsedPrimitive[];\n}\nexport declare function parseGlb(data: ArrayBuffer): ParsedGltf;\n/**\n * Parse a glTF text-format model from pre-fetched JSON and buffers.\n *\n * @param jsonObj  - The parsed JSON descriptor (from .gltf file).\n * @param buffers  - External .bin buffers referenced by the JSON (in order).\n */\nexport declare function parseGltfJson(jsonObj: unknown, buffers: ArrayBuffer[]): ParsedGltf;\n//# sourceMappingURL=gltf-parser.d.ts.map","glyph-atlas.d.ts":"/**\n * Glyph Atlas\n *\n * SDF (Signed Distance Field) glyph atlas yonetimi.\n * Shelf-first-fit bin packing ile glyph'leri 2D texture'a yerlestirir.\n * Baslangic boyutu 512x512, ihtiyac halinde 4096'ya kadar buyur.\n */\nexport interface GlyphMetrics {\n    /** Glyph genisligi (piksel) */\n    width: number;\n    /** Glyph yuksekligi (piksel) */\n    height: number;\n    /** Sol bearing (piksel) */\n    bearingX: number;\n    /** Ust bearing (piksel) */\n    bearingY: number;\n    /** Advance width (piksel) */\n    advance: number;\n}\nexport interface GlyphEntry {\n    /** Atlas icerisindeki UV koordinatlari [u0, v0, u1, v1] */\n    uv: [number, number, number, number];\n    /** Glyph metrikleri */\n    metrics: GlyphMetrics;\n    /** Atlas icerisindeki piksel koordinatlari */\n    x: number;\n    y: number;\n}\nexport declare class GlyphAtlas {\n    private glyphs;\n    private shelves;\n    private _width;\n    private _height;\n    private data;\n    private dirty;\n    private device;\n    private texture;\n    constructor(device?: GPUDevice);\n    get width(): number;\n    get height(): number;\n    get glyphCount(): number;\n    get isDirty(): boolean;\n    /**\n     * SDF glyph ekle.\n     * @param charCode Unicode character code\n     * @param sdfData SDF bitmap verisi (width x height, single channel)\n     * @param metrics Glyph metrikleri\n     * @returns Eklenen GlyphEntry veya null (yer yoksa)\n     */\n    addGlyph(charCode: number, sdfData: Uint8Array, metrics: GlyphMetrics): GlyphEntry | null;\n    /**\n     * Glyph bilgilerini getir.\n     */\n    getGlyph(charCode: number): GlyphEntry | undefined;\n    /**\n     * GPU texture'i getir (lazy create + upload).\n     */\n    getTexture(): GPUTexture | null;\n    /**\n     * Atlas'in ham verisini dondur (test icin).\n     */\n    getData(): Uint8Array;\n    /**\n     * Atlas'i 2x buyut (maks MAX_SIZE).\n     * @returns Buyume basarili mi?\n     */\n    private grow;\n    /**\n     * Atlas verisini GPU texture'a yukle.\n     */\n    private uploadToGPU;\n    /**\n     * Tum kaynaklari serbest birak.\n     */\n    destroy(): void;\n}\n//# sourceMappingURL=glyph-atlas.d.ts.map","gpu-math.d.ts":"/**\n * GPU Math Utilities\n *\n * Matrix and vector operations shared across render engine delegates.\n */\n/**\n * 4x4 matrix multiplication (column-major Float32Array).\n * result = a * b\n */\nexport declare function multiplyMat4(a: Float32Array, b: Float32Array): Float32Array;\n//# sourceMappingURL=gpu-math.d.ts.map","height-brush.d.ts":"/**\n * Height Brush — Debug terrain editing tool (texture-based).\n *\n * Uses a world-space R32Float heightmap texture instead of per-vertex\n * storage buffers. This makes height data truly zoom-independent:\n * the texture resolution is fixed in world space, and the GPU vertex\n * shader samples it via textureSampleLevel() using Mercator UV coords.\n *\n * Coordinate system: normalized Mercator (0..1) for globe mode.\n */\nexport declare class HeightBrush {\n    private readonly resolution;\n    private readonly worldExtent;\n    private readonly cpuHeightmap;\n    private gpuTexture;\n    private gpuSampler;\n    private bindGroup;\n    private dirty;\n    private hasStrokes;\n    private addTexel;\n    private splatSubTexel;\n    constructor(device: GPUDevice, layout: GPUBindGroupLayout, options?: {\n        resolution?: number;\n        worldExtent?: [number, number, number, number];\n    });\n    /**\n     * Apply a brush stroke at world-space Mercator coordinates.\n     * Rasterizes the stroke into the CPU heightmap with adjustable falloff.\n     */\n    apply(mercX: number, mercY: number, radius: number, strength: number, softness?: number): void;\n    /**\n     * Upload CPU heightmap to GPU if dirty.\n     */\n    flush(device: GPUDevice): void;\n    /**\n     * Get the single bind group (texture + sampler).\n     * Returns null if no strokes have been applied.\n     * Flushes to GPU if dirty.\n     */\n    getBindGroup(device: GPUDevice): GPUBindGroup | null;\n    /** Clear all brush data (zero out heightmap) */\n    clear(): void;\n    /** Release GPU resources */\n    destroy(): void;\n}\n/**\n * Create the bind group layout for the height texture (group 2).\n * R32Float is unfilterable-float by default in WebGPU.\n */\nexport declare function createHeightTextureBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\n/**\n * Create a 1×1 zero-height texture and bind group (fallback when no brush).\n */\nexport declare function createZeroHeightTexture(device: GPUDevice, layout: GPUBindGroupLayout): {\n    texture: GPUTexture;\n    bindGroup: GPUBindGroup;\n};\n//# sourceMappingURL=height-brush.d.ts.map","index.d.ts":"/**\n * @mapgpu/render-webgpu\n *\n * WebGPU render core — GPU pipeline, buffer management, shader execution.\n * Phase 0: Capability detection + temel yapı.\n * Phase 2: Point, line, polygon, picking, terrain pipelines + bind group cache.\n * Phase 2+: Text, post-process pipelines + glyph/sprite atlas + label engine.\n */\nexport { detectCapabilities } from './capabilities';\nexport type { GpuCapabilities, RenderMode } from './capabilities';\nexport { BufferPool } from './buffer-pool';\nexport type { BufferCategory } from './buffer-pool';\nexport { TextureManager } from './texture-manager';\nexport { BindGroupCache } from './bind-group-cache';\nexport type { BindGroupCacheKey } from './bind-group-cache';\nexport { createRasterPipeline, createCameraBindGroupLayout, createRasterBindGroupLayout, RASTER_SHADER_SOURCE, } from './pipelines/raster-pipeline';\nexport type { RasterPipeline, RasterPipelineDescriptor, } from './pipelines/raster-pipeline';\nexport { createPointPipeline, createPointBindGroupLayout, POINT_SHADER_SOURCE, } from './pipelines/point-pipeline';\nexport type { PointPipeline, PointPipelineDescriptor, } from './pipelines/point-pipeline';\nexport { createLinePipeline, createLineBindGroupLayout, dashStyleToUniform, LINE_SHADER_SOURCE, } from './pipelines/line-pipeline';\nexport type { LinePipeline, LinePipelineDescriptor, } from './pipelines/line-pipeline';\nexport { createPolygonPipeline, createPolygonBindGroupLayout, POLYGON_SHADER_SOURCE, } from './pipelines/polygon-pipeline';\nexport type { PolygonPipeline, PolygonPipelineDescriptor, } from './pipelines/polygon-pipeline';\nexport { createPickingPipeline, createPickingBindGroupLayout, encodePickingId, decodePickingId, readPickingPixel, PICKING_SHADER_SOURCE, } from './pipelines/picking-pipeline';\nexport type { PickingPipeline, PickingPipelineDescriptor, } from './pipelines/picking-pipeline';\nexport { createTextPipeline, createTextBindGroupLayout, TEXT_SHADER_SOURCE, } from './pipelines/text-pipeline';\nexport type { TextPipeline, TextPipelineDescriptor, } from './pipelines/text-pipeline';\nexport { createPostProcessPipeline, createPostProcessBindGroupLayout, POST_PROCESS_SHADER_SOURCE, } from './pipelines/post-process-pipeline';\nexport type { PostProcessPipeline, PostProcessPipelineDescriptor, } from './pipelines/post-process-pipeline';\nexport { GlyphAtlas } from './glyph-atlas';\nexport type { GlyphMetrics, GlyphEntry } from './glyph-atlas';\nexport { SpriteAtlas } from './sprite-atlas';\nexport type { SpriteEntry } from './sprite-atlas';\nexport { LabelEngine } from './label-engine';\nexport type { LabelInput, LabelPlacement, Viewport } from './label-engine';\nexport { createGlobeRasterPipeline, createGlobeCameraBindGroupLayout, createGlobeTileBindGroupLayout, GLOBE_RASTER_SHADER_SOURCE, } from './pipelines/globe-raster-pipeline';\nexport type { GlobeRasterPipeline, GlobeRasterPipelineDescriptor, } from './pipelines/globe-raster-pipeline';\nexport { createGlobePointPipeline, createGlobePointBindGroupLayout, GLOBE_POINT_SHADER_SOURCE, } from './pipelines/globe-point-pipeline';\nexport type { GlobePointPipeline, GlobePointPipelineDescriptor, } from './pipelines/globe-point-pipeline';\nexport { createGlobeLinePipeline, createGlobeLineBindGroupLayout, GLOBE_LINE_SHADER_SOURCE, } from './pipelines/globe-line-pipeline';\nexport type { GlobeLinePipeline, GlobeLinePipelineDescriptor, } from './pipelines/globe-line-pipeline';\nexport { createGlobePolygonPipeline, createGlobePolygonBindGroupLayout, GLOBE_POLYGON_SHADER_SOURCE, } from './pipelines/globe-polygon-pipeline';\nexport type { GlobePolygonPipeline, GlobePolygonPipelineDescriptor, } from './pipelines/globe-polygon-pipeline';\nexport { createTileDebugSuite, createTileDebugMesh, } from './pipelines/tile-debug-pipeline';\nexport type { TileDebugSuite, TileDebugSuiteDescriptor, TileDebugMesh, } from './pipelines/tile-debug-pipeline';\nexport { createSubdivisionMesh } from './pipelines/subdivision-mesh';\nexport type { SubdivisionMesh } from './pipelines/subdivision-mesh';\nexport { createPoleCapPipeline, createPoleCapMesh, createPoleCapBindGroupLayout, POLE_CAP_SHADER_SOURCE, } from './pipelines/pole-cap-pipeline';\nexport type { PoleCapPipeline, PoleCapPipelineDescriptor, PoleCapMesh, } from './pipelines/pole-cap-pipeline';\nexport { createAtmospherePipeline, createAtmosphereMesh, createAtmosphereBindGroupLayout, ATMOSPHERE_SHADER_SOURCE, } from './pipelines/atmosphere-pipeline';\nexport type { AtmospherePipeline, AtmospherePipelineDescriptor, AtmosphereMesh, } from './pipelines/atmosphere-pipeline';\nexport { createIconPipeline, createIconBindGroupLayout, ICON_SHADER_SOURCE, } from './pipelines/icon-pipeline';\nexport type { IconPipeline, IconPipelineDescriptor, } from './pipelines/icon-pipeline';\nexport { createGlobeIconPipeline, createGlobeIconBindGroupLayout, GLOBE_ICON_SHADER_SOURCE, } from './pipelines/globe-icon-pipeline';\nexport type { GlobeIconPipeline, GlobeIconPipelineDescriptor, } from './pipelines/globe-icon-pipeline';\nexport { createCustomPipeline, buildShaderSource, } from './pipelines/custom-pipeline';\nexport type { CustomPipeline, CustomPipelineDescriptor, BuildShaderOptions, } from './pipelines/custom-pipeline';\nexport { createModelPipeline, createModelBindGroupLayout, MODEL_SHADER_SOURCE, } from './pipelines/model-pipeline';\nexport type { ModelPipeline, ModelPipelineDescriptor, } from './pipelines/model-pipeline';\nexport { createGlobeModelPipeline, createGlobeModelBindGroupLayout, GLOBE_MODEL_SHADER_SOURCE, } from './pipelines/globe-model-pipeline';\nexport type { GlobeModelPipeline, GlobeModelPipelineDescriptor, } from './pipelines/globe-model-pipeline';\nexport { ModelManager } from './model-manager';\nexport type { GpuModel, GpuModelPrimitive } from './model-manager';\nexport { parseGlb, parseGltfJson } from './gltf-parser';\nexport type { ParsedGltf, ParsedPrimitive, ParsedGltfMesh, ParsedGltfMaterial, EmbeddedImageData } from './gltf-parser';\nexport { gridCluster, packClusterEntries, } from './cpu-cluster';\nexport type { CpuClusterEntry, CpuClusterResult, } from './cpu-cluster';\nexport { createClusterRenderPipeline, createClusterRenderBindGroupLayout, CLUSTER_RENDER_SHADER_SOURCE, } from './pipelines/cluster-render-pipeline';\nexport type { ClusterRenderPipeline, ClusterRenderPipelineDescriptor, } from './pipelines/cluster-render-pipeline';\nexport { createClusterGlobeRenderPipeline, CLUSTER_GLOBE_RENDER_SHADER_SOURCE, } from './pipelines/cluster-globe-render-pipeline';\nexport type { ClusterGlobeRenderPipeline, ClusterGlobeRenderPipelineDescriptor, } from './pipelines/cluster-globe-render-pipeline';\nexport { HeightBrush, createHeightTextureBindGroupLayout, createZeroHeightTexture } from './height-brush';\nexport { createExtrusionPipeline, createExtrusionBindGroupLayout, EXTRUSION_SHADER_SOURCE, } from './pipelines/extrusion-pipeline';\nexport type { ExtrusionPipeline, ExtrusionPipelineDescriptor, } from './pipelines/extrusion-pipeline';\nexport { createGlobeExtrusionPipeline, GLOBE_EXTRUSION_SHADER_SOURCE, } from './pipelines/globe-extrusion-pipeline';\nexport type { GlobeExtrusionPipeline, GlobeExtrusionPipelineDescriptor, } from './pipelines/globe-extrusion-pipeline';\nexport { resolvePostProcessConfig, BLOOM_SHADER_SOURCE, createDefaultBloomState, HDR_SHADER_SOURCE, createDefaultHDRState, SSAO_SHADER_SOURCE, createDefaultSSAOState, } from './post-process/index';\nexport type { PostProcessConfig, ResolvedPostProcessConfig, BloomPassState, HDRPassState, SSAOPassState, } from './post-process/index';\nexport { resolveShadowConfig, computeCascadeSplits, SHADOW_SAMPLING_WGSL, } from './post-process/ShadowMapTypes';\nexport type { ShadowConfig, ResolvedShadowConfig, } from './post-process/ShadowMapTypes';\nexport { resolveParticleConfig, PARTICLE_STRIDE_BYTES, PARTICLE_LAYOUT, PARTICLE_UPDATE_WGSL, } from './particles/ParticleSystem';\nexport type { ParticleSystemConfig, ResolvedParticleConfig, ParticleEmitterConfig, EmitterType, } from './particles/ParticleSystem';\nexport { RenderEngine } from './render-engine';\n//# sourceMappingURL=index.d.ts.map","label-engine.d.ts":"/**\n * Label Engine\n *\n * Label yerlesim motoru — grid-based collision detection ile\n * cakisan label'lari gizler, priority sirasina gore yerlestirir.\n */\nexport interface LabelInput {\n    /** Benzersiz tanimlayici */\n    id: string;\n    /** Ekran konumu (piksel, sol-ust kose referansi) */\n    screenX: number;\n    screenY: number;\n    /** Label genisligi (piksel) */\n    width: number;\n    /** Label yuksekligi (piksel) */\n    height: number;\n    /** Oncelik (yuksek = daha onemli, once yerlestirilir) */\n    priority: number;\n}\nexport interface LabelPlacement {\n    /** Label ID */\n    id: string;\n    /** Yerlestirilen ekran konumu X */\n    screenX: number;\n    /** Yerlestirilen ekran konumu Y */\n    screenY: number;\n    /** Label gorunur mu? */\n    visible: boolean;\n}\nexport interface Viewport {\n    width: number;\n    height: number;\n}\nexport declare class LabelEngine {\n    private cellSize;\n    constructor(cellSize?: number);\n    /**\n     * Label'lari viewport icerisine yerlestir, cakisanlari gizle.\n     * @param labels Yerlestirilecek label'lar\n     * @param viewport Viewport boyutlari\n     * @returns Yerlestirme sonuclari (her label icin)\n     */\n    layoutLabels(labels: readonly LabelInput[], viewport: Viewport): LabelPlacement[];\n    /**\n     * Iki AABB'nin cakisip cakismadigini kontrol et.\n     */\n    private rectsOverlap;\n}\n//# sourceMappingURL=label-engine.d.ts.map","model-manager.d.ts":"import type { ParsedGltf, ParsedGltfMaterial } from './gltf-parser';\n/** Per-primitive GPU resources — one draw call unit. */\nexport interface GpuModelPrimitive {\n    /** Interleaved vertex buffer: [posX, posY, posZ, normX, normY, normZ, u, v] per vertex (32 bytes/vertex) */\n    vertexBuffer: GPUBuffer;\n    /** Index buffer (uint16 or uint32) */\n    indexBuffer: GPUBuffer;\n    /** Index format for setIndexBuffer call */\n    indexFormat: GPUIndexFormat;\n    /** Number of indices */\n    indexCount: number;\n    /** Number of vertices */\n    vertexCount: number;\n    /** PBR material data */\n    material: ParsedGltfMaterial;\n    /** PBR textures — null entries use the placeholder texture */\n    baseColorTexture: GPUTexture | null;\n    normalTexture: GPUTexture | null;\n    metallicRoughnessTexture: GPUTexture | null;\n    occlusionTexture: GPUTexture | null;\n    emissiveTexture: GPUTexture | null;\n}\nexport interface GpuModel {\n    primitives: GpuModelPrimitive[];\n}\nexport declare class ModelManager {\n    private _device;\n    private _models;\n    constructor(device: GPUDevice);\n    /**\n     * Upload a parsed GLTF model to GPU buffers (synchronous — no textures).\n     * Interleaves position/normal/texcoord into a single vertex buffer per primitive.\n     */\n    upload(id: string, parsed: ParsedGltf): void;\n    /**\n     * Upload a parsed GLTF model with async texture creation.\n     * Falls back to sync upload if no textures are present.\n     */\n    uploadAsync(id: string, parsed: ParsedGltf): Promise<void>;\n    /** Get a loaded model by ID */\n    get(id: string): GpuModel | undefined;\n    /** Check if a model is loaded */\n    has(id: string): boolean;\n    /** Release all GPU resources */\n    destroy(): void;\n    private _uploadPrimitive;\n    private _createTextureFromBitmap;\n}\n//# sourceMappingURL=model-manager.d.ts.map","particles/ParticleSystem.d.ts":"/**\n * ParticleSystem — GPU-accelerated particle simulation and rendering.\n *\n * CesiumJS ParticleSystem equivalent, implemented with WebGPU compute shaders.\n *\n * Lifecycle: spawn → update (position, velocity, lifetime) → render (point-list or billboard)\n * Storage: Single GPU buffer with per-particle data (52 bytes/particle).\n */\n/** Emitter shapes for particle spawning. */\nexport type EmitterType = 'box' | 'circle' | 'cone' | 'sphere';\nexport interface ParticleEmitterConfig {\n    type: EmitterType;\n    /** Emitter dimensions (interpretation depends on type). */\n    radius?: number;\n    width?: number;\n    height?: number;\n    depth?: number;\n    angle?: number;\n}\nexport interface ParticleSystemConfig {\n    /** Geographic position [lon, lat, alt]. */\n    position: [number, number, number];\n    /** Emitter configuration. */\n    emitter: ParticleEmitterConfig;\n    /** Particles emitted per second. Default: 100. */\n    emissionRate?: number;\n    /** Maximum particle count (buffer size). Default: 10000. */\n    maxParticles?: number;\n    /** Particle lifetime in seconds (randomized ± 20%). Default: 3.0. */\n    lifetime?: number;\n    /** Initial speed (m/s). Default: 1.0. */\n    speed?: number;\n    /** Speed variation factor (0-1). Default: 0.2. */\n    speedVariation?: number;\n    /** Start size (pixels or meters). Default: 5.0. */\n    startScale?: number;\n    /** End size. Default: 1.0. */\n    endScale?: number;\n    /** Start color [R, G, B, A] 0-1. Default: white. */\n    startColor?: [number, number, number, number];\n    /** End color [R, G, B, A] 0-1. Default: transparent white. */\n    endColor?: [number, number, number, number];\n    /** Gravity acceleration [x, y, z] m/s². Default: [0, -9.81, 0]. */\n    gravity?: [number, number, number];\n    /** Wind velocity [x, y, z] m/s. Default: [0, 0, 0]. */\n    wind?: [number, number, number];\n    /** Billboard texture URL (optional, uses point-list if not set). */\n    imageUrl?: string;\n    /** Master enable/disable. Default: true. */\n    enabled?: boolean;\n}\nexport interface ResolvedParticleConfig {\n    position: [number, number, number];\n    emitter: Required<ParticleEmitterConfig>;\n    emissionRate: number;\n    maxParticles: number;\n    lifetime: number;\n    speed: number;\n    speedVariation: number;\n    startScale: number;\n    endScale: number;\n    startColor: [number, number, number, number];\n    endColor: [number, number, number, number];\n    gravity: [number, number, number];\n    wind: [number, number, number];\n    imageUrl: string;\n    enabled: boolean;\n}\nexport declare function resolveParticleConfig(cfg: ParticleSystemConfig): ResolvedParticleConfig;\n/**\n * Per-particle data layout in the GPU storage buffer.\n * Total: 52 bytes per particle.\n */\nexport declare const PARTICLE_STRIDE_BYTES = 52;\nexport declare const PARTICLE_LAYOUT: {\n    readonly posX: 0;\n    readonly posY: 4;\n    readonly posZ: 8;\n    readonly velX: 12;\n    readonly velY: 16;\n    readonly velZ: 20;\n    readonly life: 24;\n    readonly maxLife: 28;\n    readonly scale: 32;\n    readonly colorR: 36;\n    readonly colorG: 40;\n    readonly colorB: 44;\n    readonly colorA: 48;\n};\n/** WGSL compute shader for particle update. */\nexport declare const PARTICLE_UPDATE_WGSL = \"\\n\\nstruct Particle {\\n  pos: vec3<f32>,\\n  vel: vec3<f32>,\\n  life: f32,\\n  maxLife: f32,\\n  scale: f32,\\n  color: vec4<f32>,\\n};\\n\\nstruct SimParams {\\n  deltaTime: f32,\\n  gravity: vec3<f32>,\\n  wind: vec3<f32>,\\n  startScale: f32,\\n  endScale: f32,\\n  startColor: vec4<f32>,\\n  endColor: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<storage, read_write> particles: array<Particle>;\\n@group(0) @binding(1) var<uniform> params: SimParams;\\n\\n@compute @workgroup_size(64)\\nfn main(@builtin(global_invocation_id) id: vec3<u32>) {\\n  let idx = id.x;\\n  if idx >= arrayLength(&particles) { return; }\\n\\n  var p = particles[idx];\\n\\n  // Skip dead particles\\n  if p.life <= 0.0 { return; }\\n\\n  // Update lifetime\\n  p.life -= params.deltaTime;\\n\\n  if p.life <= 0.0 {\\n    p.life = 0.0;\\n    p.color.a = 0.0;\\n    particles[idx] = p;\\n    return;\\n  }\\n\\n  // Progress 0..1\\n  let t = 1.0 - (p.life / p.maxLife);\\n\\n  // Physics\\n  p.vel += (params.gravity + params.wind) * params.deltaTime;\\n  p.pos += p.vel * params.deltaTime;\\n\\n  // Interpolate visual properties\\n  p.scale = mix(params.startScale, params.endScale, t);\\n  p.color = mix(params.startColor, params.endColor, t);\\n\\n  particles[idx] = p;\\n}\\n\";\n//# sourceMappingURL=ParticleSystem.d.ts.map","pipelines/atmosphere-pipeline.d.ts":"/**\n * Atmosphere Pipeline\n *\n * Globe etrafında atmosfer halo efekti — mesh tabanlı, per-fragment glow.\n *\n * Trick: Mesh, görünür glow bölgesinden BÜYÜK (1.15×). Polygonal silhouette\n * kenarları glow'un sıfıra düştüğü alanda kalır → görünmez.\n * Fragment shader'da worldPos normalize edilerek her piksel doğru küre\n * yönünü hesaplar — interpolasyon artefaktı yok.\n *\n * Depth: depthWriteEnabled=false — globe/tile'ların üzerini örtmez.\n */\nexport declare const ATMOSPHERE_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979323846;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nstruct AtmosphereUniforms {\\n  colorInner: vec4<f32>,\\n  colorOuter: vec4<f32>,\\n  strength: f32,\\n  falloff: f32,\\n  _pad0: f32,\\n  _pad1: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> atmosphere: AtmosphereUniforms;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) worldPos: vec3<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var out: VertexOutput;\\n  out.clipPosition = camera.viewProjection * vec4<f32>(input.position, 1.0);\\n  out.worldPos = input.position;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Normalize worldPos to project onto true sphere surface.\\n  // Linear interpolation between mesh vertices creates chords;\\n  // normalizing gives the exact spherical direction per-fragment.\\n  let sphereDir = normalize(input.worldPos);\\n\\n  // Compute clipDot per-fragment (not interpolated) for smooth horizon\\n  let clipDot = dot(sphereDir, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Discard back hemisphere\\n  if (clipDot < -0.15) {\\n    discard;\\n  }\\n\\n  // Fresnel-like edge glow: view direction vs sphere normal\\n  let cameraDir = normalize(camera.clippingPlane.xyz);\\n  let facing = abs(dot(sphereDir, cameraDir));\\n  let edgeFactor = 1.0 - facing;\\n\\n  // Glow intensity with configurable falloff\\n  let intensity = pow(edgeFactor, atmosphere.falloff);\\n\\n  // Boost and clamp\\n  let boosted = clamp(intensity * 2.0, 0.0, 1.0);\\n\\n  // Color gradient: inner (near globe edge) \\u2192 outer (limb)\\n  let color = mix(atmosphere.colorInner, atmosphere.colorOuter, edgeFactor);\\n\\n  // Final alpha: glow \\u00D7 strength \\u00D7 projectionTransition\\n  let alpha = boosted * atmosphere.strength * camera.projectionTransition;\\n\\n  // Smooth horizon fade (per-fragment, not interpolated \\u2192 no polygon edges)\\n  let horizonFade = smoothstep(-0.15, 0.05, clipDot);\\n  let finalAlpha = alpha * horizonFade;\\n\\n  if (finalAlpha < 0.002) {\\n    discard;\\n  }\\n\\n  return vec4<f32>(color.rgb, finalAlpha);\\n}\\n\";\nexport interface AtmosphereMesh {\n    vertexBuffer: GPUBuffer;\n    indexBuffer: GPUBuffer;\n    indexCount: number;\n    vertexCount: number;\n}\n/**\n * Icosphere mesh — octahedron subdivided and projected onto sphere.\n * Radius = ATMOSPHERE_MESH_SCALE (larger than visible glow region).\n *\n * @param device - GPUDevice\n * @param subdivisions - Detail level (default 4 → 2048 triangles)\n */\nexport declare function createAtmosphereMesh(device: GPUDevice, subdivisions?: number): AtmosphereMesh;\nexport declare function createAtmosphereBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface AtmospherePipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    subdivisions?: number;\n    sampleCount?: number;\n}\nexport interface AtmospherePipeline {\n    pipeline: GPURenderPipeline;\n    atmosphereBindGroupLayout: GPUBindGroupLayout;\n    mesh: AtmosphereMesh;\n}\n/**\n * Atmosphere pipeline — mesh-based glow with per-fragment sphere projection.\n * Mesh larger than visible glow → polygonal edges invisible.\n * depthWriteEnabled=false, depthCompare=always.\n */\nexport declare function createAtmospherePipeline(desc: AtmospherePipelineDescriptor): AtmospherePipeline;\n//# sourceMappingURL=atmosphere-pipeline.d.ts.map","pipelines/cluster-compute-pipeline.d.ts":"/**\n * Cluster Compute Pipeline\n *\n * First compute shader in the codebase!\n * Two-pass GPU spatial hash clustering:\n *   Pass 1 (assignCells): Atomic cell count + fixed-point position accumulation\n *   Pass 2 (finalizeClusters): Centroid computation + output buffer write\n *\n * Fixed-point trick: WGSL has no atomic<f32>, so positions relative to cell\n * origin are encoded as 14-bit fixed-point (×16384) and accumulated via\n * atomicAdd(&cell.sumX, i32(...)). Precision: cellSize/16384 ≈ 0.003m at zoom 10.\n *\n * The counters buffer doubles as GPUDrawIndirectArgs: { vertexCount: 6,\n * instanceCount: <atomic>, firstVertex: 0, firstInstance: 0 } — the GPU\n * self-determines instance count, no CPU readback needed.\n */\nexport declare const CLUSTER_COMPUTE_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Shared Types \\u2500\\u2500\\u2500\\n\\nstruct ClusterParams {\\n  gridOriginX: f32,\\n  gridOriginY: f32,\\n  cellSize: f32,\\n  gridWidth: u32,\\n  gridHeight: u32,\\n  pointCount: u32,\\n  minClusterCount: u32,\\n  _pad: u32,\\n};\\n\\nstruct CellData {\\n  count: atomic<u32>,\\n  sumX: atomic<i32>,\\n  sumY: atomic<i32>,\\n  _pad: u32,\\n};\\n\\nstruct ClusterOutput {\\n  posX: f32,\\n  posY: f32,\\n  count: u32,\\n  flags: u32,\\n};\\n\\nstruct IndirectArgs {\\n  vertexCount: u32,\\n  instanceCount: atomic<u32>,\\n  firstVertex: u32,\\n  firstInstance: u32,\\n};\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\n@group(0) @binding(0) var<storage, read> sourcePoints: array<vec2<f32>>;\\n@group(0) @binding(1) var<storage, read_write> cells: array<CellData>;\\n@group(0) @binding(2) var<storage, read_write> output: array<ClusterOutput>;\\n@group(0) @binding(3) var<storage, read_write> counters: IndirectArgs;\\n@group(0) @binding(4) var<uniform> params: ClusterParams;\\n\\n// \\u2500\\u2500\\u2500 Pass 1: Assign Points to Cells \\u2500\\u2500\\u2500\\n\\n@compute @workgroup_size(256)\\nfn assignCells(@builtin(global_invocation_id) gid: vec3<u32>) {\\n  let idx = gid.x;\\n  if (idx >= params.pointCount) { return; }\\n\\n  let point = sourcePoints[idx];\\n\\n  // Compute cell coordinates\\n  let relX = (point.x - params.gridOriginX) / params.cellSize;\\n  let relY = (point.y - params.gridOriginY) / params.cellSize;\\n\\n  // Clamp to grid bounds\\n  let cellX = min(u32(max(relX, 0.0)), params.gridWidth - 1u);\\n  let cellY = min(u32(max(relY, 0.0)), params.gridHeight - 1u);\\n  let cellIdx = cellY * params.gridWidth + cellX;\\n\\n  // Atomic accumulation\\n  atomicAdd(&cells[cellIdx].count, 1u);\\n\\n  // Fixed-point 14-bit accumulation of fractional position within cell\\n  let fracX = fract(max(relX, 0.0));\\n  let fracY = fract(max(relY, 0.0));\\n  atomicAdd(&cells[cellIdx].sumX, i32(fracX * 16384.0));\\n  atomicAdd(&cells[cellIdx].sumY, i32(fracY * 16384.0));\\n}\\n\\n// \\u2500\\u2500\\u2500 Pass 2: Finalize Clusters \\u2500\\u2500\\u2500\\n\\n@compute @workgroup_size(256)\\nfn finalizeClusters(@builtin(global_invocation_id) gid: vec3<u32>) {\\n  let cellIdx = gid.x;\\n  if (cellIdx >= params.gridWidth * params.gridHeight) { return; }\\n\\n  let count = atomicLoad(&cells[cellIdx].count);\\n  if (count == 0u) { return; }\\n\\n  let cellX = cellIdx % params.gridWidth;\\n  let cellY = cellIdx / params.gridWidth;\\n\\n  // Centroid from fixed-point accumulation\\n  let avgX = f32(atomicLoad(&cells[cellIdx].sumX)) / (f32(count) * 16384.0);\\n  let avgY = f32(atomicLoad(&cells[cellIdx].sumY)) / (f32(count) * 16384.0);\\n\\n  let cx = params.gridOriginX + (f32(cellX) + avgX) * params.cellSize;\\n  let cy = params.gridOriginY + (f32(cellY) + avgY) * params.cellSize;\\n\\n  // Flags: bit 0 = isCluster, bits 1-2 = tier (0=small, 1=medium, 2=large)\\n  var flags = 0u;\\n  if (count >= params.minClusterCount) {\\n    flags = 1u; // isCluster\\n    if (count >= 100u) {\\n      flags = flags | (2u << 1u); // large\\n    } else if (count >= 10u) {\\n      flags = flags | (1u << 1u); // medium\\n    }\\n    // else tier 0 (small) \\u2014 no extra bits\\n  }\\n\\n  let outIdx = atomicAdd(&counters.instanceCount, 1u);\\n  output[outIdx] = ClusterOutput(cx, cy, count, flags);\\n}\\n\";\nexport interface ClusterComputePipeline {\n    assignPipeline: GPUComputePipeline;\n    finalizePipeline: GPUComputePipeline;\n    bindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createClusterComputePipeline(device: GPUDevice): ClusterComputePipeline;\n//# sourceMappingURL=cluster-compute-pipeline.d.ts.map","pipelines/cluster-globe-render-pipeline.d.ts":"/**\n * Cluster Globe Render Pipeline\n *\n * Globe variant of cluster rendering. Same fragment shader (SDF circle +\n * digit atlas), different vertex shader using WGSL_GLOBE_PREAMBLE for\n * EPSG:3857 → Mercator [0..1] → Angular → Unit Sphere projection.\n *\n * Compute shader is shared — clustering operates in EPSG:3857 space;\n * projection to globe is render-time only.\n */\nexport declare const CLUSTER_GLOBE_RENDER_SHADER_SOURCE = \"\\n\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\nstruct ClusterOutput {\\n  posX: f32,\\n  posY: f32,\\n  count: u32,\\n  flags: u32,\\n};\\n\\nstruct ClusterMaterial {\\n  clusterFillSmall: vec4<f32>,\\n  clusterFillMedium: vec4<f32>,\\n  clusterFillLarge: vec4<f32>,\\n  clusterStroke: vec4<f32>,\\n  clusterText: vec4<f32>,\\n  pointFill: vec4<f32>,\\n  pointStroke: vec4<f32>,\\n  pointSize: f32,\\n  pointStrokeWidth: f32,\\n  clusterBaseSize: f32,\\n  clusterGrowRate: f32,\\n  clusterStrokeWidth: f32,\\n  _pad0: f32,\\n  _pad1: f32,\\n  _pad2: f32,\\n};\\n\\n@group(1) @binding(0) var<storage, read> clusters: array<ClusterOutput>;\\n@group(1) @binding(1) var<uniform> material: ClusterMaterial;\\n@group(1) @binding(2) var digitAtlasTex: texture_2d<f32>;\\n@group(1) @binding(3) var digitSampler: sampler;\\n\\n// Base depth offset. Large clusters need extra lift to avoid intersecting\\n// curved globe depth near the horizon (prevents \\\"half-circle\\\" clipping).\\nconst LAYER_DEPTH_OFFSET_BASE: f32 = 0.001;\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n  @location(1) @interpolate(flat) instanceIdx: u32,\\n  @location(2) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(\\n  @builtin(vertex_index) vid: u32,\\n  @builtin(instance_index) iid: u32,\\n) -> VertexOutput {\\n  var quadOffsets = array<vec2<f32>, 6>(\\n    vec2<f32>(-0.5, -0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>( 0.5,  0.5),\\n  );\\n\\n  let inst = clusters[iid];\\n  let isCluster = (inst.flags & 1u) != 0u;\\n\\n  var pixelSize: f32;\\n  if (isCluster) {\\n    let tier = min(f32((inst.flags >> 1u) & 3u), 2.0);\\n    pixelSize = material.clusterBaseSize + material.clusterGrowRate * tier;\\n  } else {\\n    pixelSize = material.pointSize;\\n  }\\n\\n  let offset = quadOffsets[vid];\\n  let uv = offset + vec2<f32>(0.5, 0.5);\\n\\n  // EPSG:3857 \\u2192 Mercator [0..1] \\u2192 angular \\u2192 sphere\\n  let merc01 = epsg3857ToMerc01(vec3<f32>(inst.posX, inst.posY, 0.0));\\n  let ang = mercatorToAngular(merc01);\\n  let spherePos = angularToSphere(ang.x, ang.y);\\n\\n  // Horizon dot product (passed to fragment for discard)\\n  let clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Globe clip position\\n  var globeClip = camera.viewProjection * vec4<f32>(spherePos, 1.0);\\n  let clipZ = globeClippingZ(spherePos);\\n  globeClip.z = clipZ * globeClip.w;\\n\\n  // Flat clip position (for transition blend)\\n  let flatClip = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, 0.0, 1.0);\\n\\n  // Blend based on projection transition\\n  var clipCenter: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipCenter = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    clipCenter = flatClip;\\n  } else {\\n    clipCenter = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n\\n  // Depth offset + clamping (match other globe pipelines)\\n  // Size-aware offset keeps bigger billboards fully in front of globe depth.\\n  let layerDepthOffset = LAYER_DEPTH_OFFSET_BASE + pixelSize * 0.00006;\\n  let adjustedZ = clipCenter.z - layerDepthOffset * clipCenter.w;\\n  let clampedZ = min(adjustedZ, clipCenter.w * 0.9999);\\n\\n  // Billboard offset in screen space\\n  let screenOffset = offset * pixelSize;\\n  let ndcOffset = vec2<f32>(\\n    screenOffset.x * 2.0 / camera.viewport.x,\\n    screenOffset.y * 2.0 / camera.viewport.y,\\n  );\\n\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(\\n    clipCenter.x + ndcOffset.x * clipCenter.w,\\n    clipCenter.y + ndcOffset.y * clipCenter.w,\\n    clampedZ,\\n    clipCenter.w,\\n  );\\n  out.uv = uv;\\n  out.instanceIdx = iid;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let inst = clusters[input.instanceIdx];\\n  let isCluster = (inst.flags & 1u) != 0u;\\n\\n  let centered = input.uv - vec2<f32>(0.5, 0.5);\\n  let dist = length(centered);\\n\\n  // \\u2500\\u2500 Uniform control flow zone \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\n  // fwidth + textureSample MUST be called before any non-uniform branch.\\n  let aa = fwidth(dist);\\n\\n  // Digit atlas UV (computed unconditionally; result ignored for points)\\n  let count = inst.count;\\n  let digitCount = getDigitCount(count);\\n  let digitCountF = f32(max(digitCount, 1u));\\n  let textHeight = 0.54;\\n  let maxTotalWidth = 0.78;\\n  let naturalDigitWidth = textHeight * 0.52;\\n  let digitWidth = min(naturalDigitWidth, maxTotalWidth / digitCountF);\\n  let totalWidth = digitCountF * digitWidth;\\n  let startU = 0.5 - totalWidth * 0.5;\\n  let localU = input.uv.x;\\n  let localV = input.uv.y;\\n  let rawDigitIdx = (localU - startU) / digitWidth;\\n  let digitIdx = u32(max(rawDigitIdx, 0.0));\\n  let safeDigitIdx = min(digitIdx, max(digitCount, 1u) - 1u);\\n  let digit = getDigitAt(count, digitCount, safeDigitIdx);\\n  let withinU = fract(max(rawDigitIdx, 0.0));\\n  let vMin = 0.5 - textHeight * 0.5;\\n  let vMax = 0.5 + textHeight * 0.5;\\n  let withinV = clamp((localV - vMin) / textHeight, 0.0, 1.0);\\n  // Remove side-bearings inside each digit cell to tighten inter-digit spacing.\\n  let glyphCropMin = 0.18;\\n  let glyphCropMax = 0.82;\\n  let atlasDigitU = glyphCropMin + withinU * (glyphCropMax - glyphCropMin);\\n  let atlasU = (f32(digit) + atlasDigitU) / 10.0;\\n  let atlasV = withinV;\\n  let texColor = textureSample(digitAtlasTex, digitSampler, vec2<f32>(atlasU, atlasV));\\n  // \\u2500\\u2500 End uniform zone \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\n\\n  // Horizon culling \\u2014 fragment discard (matching other globe pipelines)\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  if (dist > 0.5) {\\n    discard;\\n  }\\n\\n  // \\u2500\\u2500 Non-uniform branching (safe \\u2014 special ops already computed) \\u2500\\u2500\\n  if (!isCluster) {\\n    let strokeFrac = clamp(material.pointStrokeWidth / max(material.pointSize, 1.0), 0.0, 0.49);\\n    let inner = 0.5 - strokeFrac;\\n    let fillMix = 1.0 - smoothstep(inner - aa, inner, dist);\\n    let edgeAlpha = 1.0 - smoothstep(0.5 - aa, 0.5, dist);\\n    let color = mix(material.pointStroke, material.pointFill, fillMix);\\n    return vec4<f32>(color.rgb, color.a * edgeAlpha);\\n  }\\n\\n  let tier = (inst.flags >> 1u) & 3u;\\n  var fillColor: vec4<f32>;\\n  if (tier >= 2u) {\\n    fillColor = material.clusterFillLarge;\\n  } else if (tier >= 1u) {\\n    fillColor = material.clusterFillMedium;\\n  } else {\\n    fillColor = material.clusterFillSmall;\\n  }\\n\\n  let clusterTier = min(f32((inst.flags >> 1u) & 3u), 2.0);\\n  let clusterPixelSize = material.clusterBaseSize + material.clusterGrowRate * clusterTier;\\n  let strokeFrac = clamp(material.clusterStrokeWidth / max(clusterPixelSize, 1.0), 0.0, 0.49);\\n  let inner = 0.5 - strokeFrac;\\n  let fillMix = 1.0 - smoothstep(inner - aa, inner, dist);\\n  let edgeAlpha = 1.0 - smoothstep(0.5 - aa, 0.5, dist);\\n  let circleColor = mix(material.clusterStroke, fillColor, fillMix);\\n\\n  let inDigitRegion = step(vMin, localV) * step(localV, vMax)\\n                    * step(startU, localU) * step(localU, startU + totalWidth)\\n                    * step(f32(digitIdx), f32(digitCount) - 0.5);\\n  let textAlpha = texColor.a * inDigitRegion * material.clusterText.a;\\n\\n  let finalColor = mix(circleColor.rgb, material.clusterText.rgb, textAlpha);\\n  return vec4<f32>(finalColor, circleColor.a * edgeAlpha);\\n}\\n\\nfn getDigitCount(n: u32) -> u32 {\\n  if (n >= 100000u) { return 6u; }\\n  if (n >= 10000u) { return 5u; }\\n  if (n >= 1000u) { return 4u; }\\n  if (n >= 100u) { return 3u; }\\n  if (n >= 10u) { return 2u; }\\n  return 1u;\\n}\\n\\nfn getDigitAt(n: u32, digitCount: u32, idx: u32) -> u32 {\\n  var divisor = 1u;\\n  for (var i = 0u; i < digitCount - 1u - idx; i = i + 1u) {\\n    divisor = divisor * 10u;\\n  }\\n  return (n / divisor) % 10u;\\n}\\n\";\nexport interface ClusterGlobeRenderPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface ClusterGlobeRenderPipeline {\n    pipeline: GPURenderPipeline;\n    renderBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\nexport declare function createClusterGlobeRenderPipeline(desc: ClusterGlobeRenderPipelineDescriptor): ClusterGlobeRenderPipeline;\n//# sourceMappingURL=cluster-globe-render-pipeline.d.ts.map","pipelines/cluster-render-pipeline.d.ts":"/**\n * Cluster Render Pipeline (2D)\n *\n * Instanced billboard rendering for cluster results.\n * Reads ClusterOutput[] from storage buffer (produced by compute pass).\n * SDF circle in fragment shader + tier coloring + digit atlas count label.\n *\n * 6 vertices × N instances (N determined by indirect draw args from compute).\n */\nexport declare const CLUSTER_RENDER_SHADER_SOURCE = \"\\n\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct ClusterOutput {\\n  posX: f32,\\n  posY: f32,\\n  count: u32,\\n  flags: u32,\\n};\\n\\nstruct ClusterMaterial {\\n  clusterFillSmall: vec4<f32>,\\n  clusterFillMedium: vec4<f32>,\\n  clusterFillLarge: vec4<f32>,\\n  clusterStroke: vec4<f32>,\\n  clusterText: vec4<f32>,\\n  pointFill: vec4<f32>,\\n  pointStroke: vec4<f32>,\\n  pointSize: f32,\\n  pointStrokeWidth: f32,\\n  clusterBaseSize: f32,\\n  clusterGrowRate: f32,\\n  clusterStrokeWidth: f32,\\n  _pad0: f32,\\n  _pad1: f32,\\n  _pad2: f32,\\n};\\n\\n@group(1) @binding(0) var<storage, read> clusters: array<ClusterOutput>;\\n@group(1) @binding(1) var<uniform> material: ClusterMaterial;\\n@group(1) @binding(2) var digitAtlasTex: texture_2d<f32>;\\n@group(1) @binding(3) var digitSampler: sampler;\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n  @location(1) @interpolate(flat) instanceIdx: u32,\\n};\\n\\n@vertex\\nfn vs_main(\\n  @builtin(vertex_index) vid: u32,\\n  @builtin(instance_index) iid: u32,\\n) -> VertexOutput {\\n  var quadOffsets = array<vec2<f32>, 6>(\\n    vec2<f32>(-0.5, -0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>( 0.5,  0.5),\\n  );\\n\\n  let inst = clusters[iid];\\n  let isCluster = (inst.flags & 1u) != 0u;\\n\\n  var pixelSize: f32;\\n  if (isCluster) {\\n    let tier = min(f32((inst.flags >> 1u) & 3u), 2.0);\\n    pixelSize = material.clusterBaseSize + material.clusterGrowRate * tier;\\n  } else {\\n    pixelSize = material.pointSize;\\n  }\\n\\n  let offset = quadOffsets[vid];\\n  let uv = offset + vec2<f32>(0.5, 0.5);\\n\\n  let clipCenter = camera.viewProjection * vec4<f32>(inst.posX, inst.posY, 0.0, 1.0);\\n\\n  let screenOffset = offset * pixelSize;\\n  let ndcOffset = vec2<f32>(\\n    screenOffset.x * 2.0 / camera.viewport.x,\\n    screenOffset.y * 2.0 / camera.viewport.y,\\n  );\\n\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(\\n    clipCenter.x + ndcOffset.x * clipCenter.w,\\n    clipCenter.y + ndcOffset.y * clipCenter.w,\\n    clipCenter.z,\\n    clipCenter.w,\\n  );\\n  out.uv = uv;\\n  out.instanceIdx = iid;\\n  return out;\\n}\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let inst = clusters[input.instanceIdx];\\n  let isCluster = (inst.flags & 1u) != 0u;\\n\\n  let centered = input.uv - vec2<f32>(0.5, 0.5);\\n  let dist = length(centered);\\n\\n  // \\u2500\\u2500 Uniform control flow zone \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\n  // fwidth + textureSample MUST be called before any non-uniform branch.\\n  let aa = fwidth(dist);\\n\\n  // Digit atlas UV (computed unconditionally; result ignored for points)\\n  let count = inst.count;\\n  let digitCount = getDigitCount(count);\\n  let digitCountF = f32(max(digitCount, 1u));\\n  let textHeight = 0.54;\\n  let maxTotalWidth = 0.78;\\n  let naturalDigitWidth = textHeight * 0.52;\\n  let digitWidth = min(naturalDigitWidth, maxTotalWidth / digitCountF);\\n  let totalWidth = digitCountF * digitWidth;\\n  let startU = 0.5 - totalWidth * 0.5;\\n  let localU = input.uv.x;\\n  let localV = input.uv.y;\\n  let rawDigitIdx = (localU - startU) / digitWidth;\\n  let digitIdx = u32(max(rawDigitIdx, 0.0));\\n  let safeDigitIdx = min(digitIdx, max(digitCount, 1u) - 1u);\\n  let digit = getDigitAt(count, digitCount, safeDigitIdx);\\n  let withinU = fract(max(rawDigitIdx, 0.0));\\n  let vMin = 0.5 - textHeight * 0.5;\\n  let vMax = 0.5 + textHeight * 0.5;\\n  let withinV = clamp((localV - vMin) / textHeight, 0.0, 1.0);\\n  // Remove side-bearings inside each digit cell to tighten inter-digit spacing.\\n  let glyphCropMin = 0.18;\\n  let glyphCropMax = 0.82;\\n  let atlasDigitU = glyphCropMin + withinU * (glyphCropMax - glyphCropMin);\\n  let atlasU = (f32(digit) + atlasDigitU) / 10.0;\\n  let atlasV = withinV;\\n  let texColor = textureSample(digitAtlasTex, digitSampler, vec2<f32>(atlasU, atlasV));\\n  // \\u2500\\u2500 End uniform zone \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\n\\n  // SDF circle \\u2014 discard outside radius (uniform: depends only on UV)\\n  if (dist > 0.5) {\\n    discard;\\n  }\\n\\n  // \\u2500\\u2500 Non-uniform branching (safe \\u2014 special ops already computed) \\u2500\\u2500\\n  if (!isCluster) {\\n    let strokeFrac = clamp(material.pointStrokeWidth / max(material.pointSize, 1.0), 0.0, 0.49);\\n    let inner = 0.5 - strokeFrac;\\n    let fillMix = 1.0 - smoothstep(inner - aa, inner, dist);\\n    let edgeAlpha = 1.0 - smoothstep(0.5 - aa, 0.5, dist);\\n    let color = mix(material.pointStroke, material.pointFill, fillMix);\\n    return vec4<f32>(color.rgb, color.a * edgeAlpha);\\n  }\\n\\n  // Cluster circle \\u2014 tier fill + stroke\\n  let tier = (inst.flags >> 1u) & 3u;\\n  var fillColor: vec4<f32>;\\n  if (tier >= 2u) {\\n    fillColor = material.clusterFillLarge;\\n  } else if (tier >= 1u) {\\n    fillColor = material.clusterFillMedium;\\n  } else {\\n    fillColor = material.clusterFillSmall;\\n  }\\n\\n  let clusterTier = min(f32((inst.flags >> 1u) & 3u), 2.0);\\n  let clusterPixelSize = material.clusterBaseSize + material.clusterGrowRate * clusterTier;\\n  let strokeFrac = clamp(material.clusterStrokeWidth / max(clusterPixelSize, 1.0), 0.0, 0.49);\\n  let inner = 0.5 - strokeFrac;\\n  let fillMix = 1.0 - smoothstep(inner - aa, inner, dist);\\n  let edgeAlpha = 1.0 - smoothstep(0.5 - aa, 0.5, dist);\\n  let circleColor = mix(material.clusterStroke, fillColor, fillMix);\\n\\n  let inDigitRegion = step(vMin, localV) * step(localV, vMax)\\n                    * step(startU, localU) * step(localU, startU + totalWidth)\\n                    * step(f32(digitIdx), f32(digitCount) - 0.5);\\n  let textAlpha = texColor.a * inDigitRegion * material.clusterText.a;\\n\\n  let finalColor = mix(circleColor.rgb, material.clusterText.rgb, textAlpha);\\n  return vec4<f32>(finalColor, circleColor.a * edgeAlpha);\\n}\\n\\n// \\u2500\\u2500\\u2500 Digit Helpers \\u2500\\u2500\\u2500\\n\\nfn getDigitCount(n: u32) -> u32 {\\n  if (n >= 100000u) { return 6u; }\\n  if (n >= 10000u) { return 5u; }\\n  if (n >= 1000u) { return 4u; }\\n  if (n >= 100u) { return 3u; }\\n  if (n >= 10u) { return 2u; }\\n  return 1u;\\n}\\n\\nfn getDigitAt(n: u32, digitCount: u32, idx: u32) -> u32 {\\n  // idx 0 = most significant digit\\n  var divisor = 1u;\\n  for (var i = 0u; i < digitCount - 1u - idx; i = i + 1u) {\\n    divisor = divisor * 10u;\\n  }\\n  return (n / divisor) % 10u;\\n}\\n\";\nexport declare function createClusterRenderBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface ClusterRenderPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface ClusterRenderPipeline {\n    pipeline: GPURenderPipeline;\n    renderBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\nexport declare function createClusterRenderPipeline(desc: ClusterRenderPipelineDescriptor): ClusterRenderPipeline;\n//# sourceMappingURL=cluster-render-pipeline.d.ts.map","pipelines/custom-pipeline.d.ts":"/**\n * Custom Pipeline Factory\n *\n * Builds WebGPU render pipelines from user-supplied WGSL shaders.\n * Handles preamble injection (camera + frame uniforms), bind group\n * layouts, and pipeline caching.\n *\n * Bind group layout:\n *   @group(0) @binding(0) — CameraUniforms (viewProjection, viewport)\n *   @group(1) @binding(0) — FrameUniforms { time, deltaTime, frameNumber, opacity }\n *   @group(2) @binding(0) — CustomUniforms (user-defined, optional)\n *   @group(3) @binding(0) — Sampler (optional)\n *   @group(3) @binding(1) — Texture (optional)\n */\nimport type { CustomVertexBufferLayout } from '@mapgpu/core';\nexport interface BuildShaderOptions {\n    hasCustomUniforms: boolean;\n    hasTexture: boolean;\n    rawMode: boolean;\n}\n/**\n * Build complete WGSL source by prepending camera/frame/custom/texture\n * preambles to the user's vertex and fragment shader code.\n *\n * When rawMode is true, vertex + fragment are concatenated as-is.\n */\nexport declare function buildShaderSource(vertexShader: string, fragmentShader: string, opts: BuildShaderOptions): string;\nexport interface CustomPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    depthFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    shaderSource: string;\n    vertexBufferLayouts: CustomVertexBufferLayout[];\n    topology: GPUPrimitiveTopology;\n    hasCustomUniforms: boolean;\n    hasTexture: boolean;\n    blendState?: GPUBlendState;\n    sampleCount?: number;\n}\nexport interface CustomPipeline {\n    pipeline: GPURenderPipeline;\n    frameBindGroupLayout: GPUBindGroupLayout;\n    customBindGroupLayout: GPUBindGroupLayout | null;\n    textureBindGroupLayout: GPUBindGroupLayout | null;\n}\n/**\n * Create a custom render pipeline from a descriptor.\n */\nexport declare function createCustomPipeline(desc: CustomPipelineDescriptor): CustomPipeline;\n//# sourceMappingURL=custom-pipeline.d.ts.map","pipelines/extrusion-pipeline.d.ts":"/**\n * Extrusion Pipeline — 2D fill-extrusion (3D buildings)\n *\n * Renders extruded polygon geometry with Blinn-Phong directional lighting.\n * Uses rotation-aware oblique projection to create visible 2.5D buildings\n * from the top-down orthographic camera — the oblique offset direction\n * rotates with the camera bearing so buildings lean consistently on screen.\n *\n * Vertex layout: [position: vec3<f32>, normal: vec3<f32>, centroid: vec2<f32>] — stride 32 bytes.\n * Material uniform: 64 bytes (16 floats) — see ExtrusionMaterial struct.\n */\nexport declare const EXTRUSION_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\nconst ROOF_DEPTH_BIAS: f32 = 1e-4;\\nconst WALL_DEPTH_BIAS: f32 = 2e-5;\\n\\nstruct ExtrusionMaterial {\\n  color: vec4<f32>,\\n  ambient: f32,\\n  debugMode: f32,\\n  animProgress: f32,\\n  animDuration: f32,\\n  waveOrigin: vec2<f32>,\\n  delayFactor: f32,\\n  bearing: f32,\\n  shininess: f32,\\n  specularStrength: f32,\\n  _pad1: f32,\\n  _pad2: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: ExtrusionMaterial;\\n\\nfn easeOutCubic(t: f32) -> f32 {\\n  let inv = 1.0 - t;\\n  return 1.0 - inv * inv * inv;\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n  @location(1) normal: vec3<f32>,\\n  @location(2) centroid: vec2<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) vNormal: vec3<f32>,\\n  @location(1) debugData: vec3<f32>,\\n  @location(2) worldPos: vec3<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var out: VertexOutput;\\n  out.debugData = vec3<f32>(0.0, 0.0, 0.0);\\n\\n  // XY comes in Mercator [0..1], convert back to EPSG:3857 for 2D camera\\n  let HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n  let epsg = vec2<f32>(\\n    input.position.x * 2.0 * HALF_CIRCUMFERENCE - HALF_CIRCUMFERENCE,\\n    (1.0 - input.position.y) * 2.0 * HALF_CIRCUMFERENCE - HALF_CIRCUMFERENCE\\n  );\\n\\n  // Oblique offset in EPSG:3857: shift roof by height to create 2.5D appearance.\\n  // Direction rotates with camera bearing so buildings lean consistently on screen.\\n  var h = input.position.z;\\n\\n  // Grow animation: scale height by eased progress\\n  if (material.animDuration > 0.0) {\\n    let dist = distance(input.centroid, material.waveOrigin);\\n    let delay = dist * material.delayFactor;\\n    let rawT = clamp((material.animProgress - delay) / material.animDuration, 0.0, 1.0);\\n    let progress = easeOutCubic(rawT);\\n    h = h * progress;\\n  }\\n\\n  // Rotation-aware oblique: offset direction follows camera bearing\\n  let obliqueMag: f32 = 0.5;\\n  let offsetDir = vec2<f32>(-sin(material.bearing), cos(material.bearing));\\n  let obliquePos = vec3<f32>(\\n    epsg.x + h * offsetDir.x * obliqueMag,\\n    epsg.y + h * offsetDir.y * obliqueMag,\\n    h,\\n  );\\n\\n  out.clipPosition = camera.viewProjection * vec4<f32>(obliquePos, 1.0);\\n  out.worldPos = obliquePos;\\n\\n  // Logarithmic depth remap: better distribution across height range.\\n  // Maps [0..1000+m] \\u2192 [0.5..0.01] with log2 distribution so both\\n  // low (1-5m) and tall (500m+) buildings have adequate depth separation.\\n  let logH = log2(max(h, 0.1) + 1.0);\\n  let logMax = log2(1001.0);\\n  let normalizedZ = clamp(0.5 - logH / (2.0 * logMax), 0.01, 0.99);\\n  out.clipPosition.z = normalizedZ * out.clipPosition.w;\\n\\n  // Roof triangles share their top edge positions with wall quads.\\n  // Bias them slightly toward the camera to avoid wall-vs-roof depth acne.\\n  if (input.normal.z > 0.5) {\\n    out.clipPosition.z -= ROOF_DEPTH_BIAS * out.clipPosition.w;\\n  }\\n\\n  // Shared building edges can still generate coplanar wall depth ties.\\n  // Split ties deterministically by wall normal orientation.\\n  if (abs(input.normal.z) < 0.5) {\\n    let wallDot = input.normal.x * 0.70710677 + input.normal.y * 0.70710677;\\n    let wallDir = select(-1.0, 1.0, wallDot >= 0.0);\\n    out.clipPosition.z -= wallDir * WALL_DEPTH_BIAS * out.clipPosition.w;\\n  }\\n\\n  // Debug data: normalizedZ, height in km, face type (0=wall, 0.5=floor, 1=roof)\\n  let faceType2d = select(0.0, select(0.5, 1.0, input.normal.z > 0.5), abs(input.normal.z) > 0.1);\\n  out.debugData = vec3<f32>(normalizedZ, h * 0.001, faceType2d);\\n\\n  out.vNormal = input.normal;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Debug mode: visualize depth / height / face type as color\\n  if (material.debugMode > 0.5) {\\n    let depth = input.debugData.x;\\n    let faceType = input.debugData.z;\\n    // Depth gradient: green(far) \\u2192 yellow \\u2192 red(near camera)\\n    let t = clamp(1.0 - depth, 0.0, 1.0);\\n    var c = mix(vec3<f32>(0.0, 1.0, 0.0), vec3<f32>(1.0, 1.0, 0.0), clamp(t * 2.0, 0.0, 1.0));\\n    c = mix(c, vec3<f32>(1.0, 0.0, 0.0), clamp(t * 2.0 - 1.0, 0.0, 1.0));\\n    if (faceType > 0.75) { c = mix(c, vec3<f32>(0.3, 0.3, 1.0), 0.4); }\\n    if (faceType > 0.25 && faceType < 0.75) { c = mix(c, vec3<f32>(0.8, 0.2, 0.8), 0.4); }\\n    return vec4<f32>(c, 0.9);\\n  }\\n\\n  // Blinn-Phong directional lighting\\n  let lightDir = normalize(vec3<f32>(0.3, -0.5, 0.8));\\n  let normal = normalize(input.vNormal);\\n  let NdotL = max(dot(normal, lightDir), 0.0);\\n\\n  // View direction: from above, rotated with camera bearing for consistent specular\\n  let viewDir = normalize(vec3<f32>(-sin(material.bearing), cos(material.bearing), 1.5));\\n  let halfDir = normalize(lightDir + viewDir);\\n  let NdotH = max(dot(normal, halfDir), 0.0);\\n  let specular = pow(NdotH, material.shininess) * material.specularStrength;\\n\\n  let diffuse = (1.0 - material.ambient) * NdotL;\\n  let lit = material.ambient + diffuse + specular;\\n  let color = material.color.rgb * min(lit, 1.0);\\n\\n  // Premultiplied alpha output\\n  return vec4<f32>(color * material.color.a, material.color.a);\\n}\\n\";\nexport declare function createExtrusionBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface ExtrusionPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface ExtrusionPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createExtrusionPipeline(desc: ExtrusionPipelineDescriptor): ExtrusionPipeline;\n//# sourceMappingURL=extrusion-pipeline.d.ts.map","pipelines/globe-extrusion-pipeline.d.ts":"/**\n * Globe Extrusion Pipeline — 3D buildings on globe\n *\n * Same vertex layout as extrusion-pipeline but projects through\n * EPSG:3857 → Mercator [0..1] → angular → unit sphere with\n * radial height offset, normal transform to globe tangent space,\n * Blinn-Phong lighting, and horizon clipping.\n */\nexport declare function createGlobeExtrusionShaderSource(depthCompare?: GPUCompareFunction): string;\nexport declare const GLOBE_EXTRUSION_SHADER_SOURCE: string;\nexport interface GlobeExtrusionPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface GlobeExtrusionPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createGlobeExtrusionPipeline(desc: GlobeExtrusionPipelineDescriptor): GlobeExtrusionPipeline;\n//# sourceMappingURL=globe-extrusion-pipeline.d.ts.map","pipelines/globe-icon-pipeline.d.ts":"/**\n * Globe Icon Pipeline\n *\n * WGSL shader: EPSG:3857 position → Mercator [0..1] → Angular → Unit Sphere.\n * Billboard quad per point with sprite atlas texture sampling.\n * GlobeCameraUniforms (160 byte) ile dual projection + horizon occlusion.\n */\nexport declare const GLOBE_ICON_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\nconst EARTH_RADIUS_M: f32 = 6371000.0;\\nconst ALTITUDE_EXAG: f32 = 5.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct IconMaterial {\\n  tintColor: vec4<f32>,\\n  uvRect: vec4<f32>,\\n  size: f32,\\n  rotation: f32,\\n  bgRadius: f32,\\n  outlineWidth: f32,\\n  bgColor: vec4<f32>,\\n  outlineColor: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: IconMaterial;\\n@group(1) @binding(1) var iconSampler: sampler;\\n@group(1) @binding(2) var iconTexture: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Helpers \\u2500\\u2500\\u2500\\n\\nfn altitudeOffset(altMeters: f32) -> f32 {\\n  return altMeters / EARTH_RADIUS_M * ALTITUDE_EXAG;\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n  @location(1) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(\\n  input: VertexInput,\\n  @builtin(vertex_index) vid: u32,\\n) -> VertexOutput {\\n  var quadOffsets = array<vec2<f32>, 6>(\\n    vec2<f32>(-0.5, -0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>( 0.5,  0.5),\\n  );\\n\\n  let offset = quadOffsets[vid];\\n\\n  // Apply rotation\\n  let rad = material.rotation * 3.14159265 / 180.0;\\n  let cosR = cos(rad);\\n  let sinR = sin(rad);\\n  let rotatedOffset = vec2<f32>(\\n    offset.x * cosR - offset.y * sinR,\\n    offset.x * sinR + offset.y * cosR,\\n  );\\n\\n  // Raw UV for fragment shader (0-1 across quad)\\n  let uv = offset + vec2<f32>(0.5, 0.5);\\n\\n  // EPSG:3857 \\u2192 Mercator [0..1] \\u2192 Angular \\u2192 Sphere\\n  let merc01 = epsg3857ToMerc01(input.position);\\n  let angular = mercatorToAngular(merc01);\\n  let baseSphere = angularToSphere(angular.x, angular.y);\\n\\n  // Altitude: Z is meters above sea level \\u2192 convert to unit sphere fraction\\n  let altFrac = altitudeOffset(input.position.z);\\n  let spherePos = baseSphere * (1.0 + altFrac);\\n\\n  // Globe clip space\\n  var globeClip = camera.viewProjection * vec4<f32>(spherePos, 1.0);\\n  globeClip.z = globeClippingZ(baseSphere) * globeClip.w;\\n\\n  var clipCenter: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipCenter = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    clipCenter = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n  } else {\\n    let flatClip = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n    clipCenter = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n  let clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Billboard offset: expand for background circle if present\\n  var pixelSize = material.size;\\n  if (material.bgRadius > 0.0) {\\n    pixelSize = max(material.size, material.bgRadius * 2.0 + material.outlineWidth * 2.0);\\n  }\\n  let screenOffset = rotatedOffset * pixelSize;\\n  let ndcOffset = vec2<f32>(\\n    screenOffset.x * 2.0 / camera.viewport.x,\\n    screenOffset.y * 2.0 / camera.viewport.y,\\n  );\\n\\n  // Shader-level depth offset: icons render in front of lines\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0008;\\n  let adjustedZ = clipCenter.z - LAYER_DEPTH_OFFSET * clipCenter.w;\\n  let clampedZ = min(adjustedZ, clipCenter.w * 0.9999);\\n\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(\\n    clipCenter.x + ndcOffset.x * clipCenter.w,\\n    clipCenter.y + ndcOffset.y * clipCenter.w,\\n    clampedZ,\\n    clipCenter.w,\\n  );\\n  out.uv = uv;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon discard\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  let centered = input.uv - vec2<f32>(0.5, 0.5);\\n\\n  // \\u2500\\u2500 No background: original icon-only path \\u2500\\u2500\\n  if (material.bgRadius <= 0.0) {\\n    let atlasUV = vec2<f32>(\\n      mix(material.uvRect.x, material.uvRect.z, input.uv.x),\\n      mix(material.uvRect.y, material.uvRect.w, input.uv.y),\\n    );\\n    let texColor = textureSample(iconTexture, iconSampler, atlasUV);\\n    if (texColor.a < 0.01) { discard; }\\n    return vec4<f32>(texColor.rgb * material.tintColor.rgb, texColor.a * material.tintColor.a);\\n  }\\n\\n  // \\u2500\\u2500 Background circle mode \\u2500\\u2500\\n  let totalSize = max(material.size, material.bgRadius * 2.0 + material.outlineWidth * 2.0);\\n  let pixelDist = length(centered) * totalSize;\\n  let outerEdge = material.bgRadius + material.outlineWidth;\\n  let aa = fwidth(pixelDist);\\n\\n  // Sample icon texture unconditionally (uniform control flow)\\n  let iconLocalUV = clamp(centered * totalSize / material.size + vec2<f32>(0.5, 0.5), vec2<f32>(0.0), vec2<f32>(1.0));\\n  let atlasUV = vec2<f32>(\\n    mix(material.uvRect.x, material.uvRect.z, iconLocalUV.x),\\n    mix(material.uvRect.y, material.uvRect.w, iconLocalUV.y),\\n  );\\n  let texColor = textureSample(iconTexture, iconSampler, atlasUV);\\n\\n  if (pixelDist > outerEdge + aa) { discard; }\\n\\n  if (pixelDist > material.bgRadius) {\\n    let outerAlpha = 1.0 - smoothstep(outerEdge - aa, outerEdge + aa, pixelDist);\\n    return vec4<f32>(material.outlineColor.rgb, material.outlineColor.a * outerAlpha);\\n  }\\n\\n  var result = material.bgColor;\\n  let iconHalf = material.size * 0.5;\\n  let pixelPos = centered * totalSize;\\n  let inIcon = step(abs(pixelPos.x), iconHalf) * step(abs(pixelPos.y), iconHalf);\\n  let tinted = vec4<f32>(\\n    texColor.rgb * material.tintColor.rgb,\\n    texColor.a * material.tintColor.a * inIcon,\\n  );\\n  result = vec4<f32>(\\n    mix(result.rgb, tinted.rgb, tinted.a),\\n    result.a + tinted.a * (1.0 - result.a),\\n  );\\n\\n  return result;\\n}\\n\";\nexport declare function createGlobeIconBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobeIconPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface GlobeIconPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\nexport declare function createGlobeIconPipeline(desc: GlobeIconPipelineDescriptor): GlobeIconPipeline;\n//# sourceMappingURL=globe-icon-pipeline.d.ts.map","pipelines/globe-line-pipeline.d.ts":"/**\n * Globe Line Pipeline\n *\n * WGSL shader: EPSG:3857 → Mercator [0..1] → Angular → Unit Sphere.\n * Screen-space miter join on globe-projected positions.\n * Dash patterns + horizon occlusion.\n */\nexport declare const GLOBE_LINE_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\nconst EARTH_RADIUS_M: f32 = 6371000.0;\\nconst ALTITUDE_EXAG: f32 = 5.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct LineMaterial {\\n  color: vec4<f32>,\\n  width: f32,\\n  dashStyle: f32,\\n  dashAnimationSpeed: f32,\\n  time: f32,\\n  dashSegments0: vec4<f32>,\\n  dashSegments1: vec4<f32>,\\n  dashMeta: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: LineMaterial;\\n\\n// \\u2500\\u2500\\u2500 Helpers \\u2500\\u2500\\u2500\\n\\nfn altitudeOffset(altMeters: f32) -> f32 {\\n  return altMeters / EARTH_RADIUS_M * ALTITUDE_EXAG;\\n}\\n\\nfn projectToClip(pos: vec3<f32>) -> vec4<f32> {\\n  let merc01 = epsg3857ToMerc01(pos);\\n  let angular = mercatorToAngular(merc01);\\n  let baseSphere = angularToSphere(angular.x, angular.y);\\n  let altFrac = altitudeOffset(pos.z);\\n  let spherePos = baseSphere * (1.0 + altFrac);\\n\\n  var globeClip = camera.viewProjection * vec4<f32>(spherePos, 1.0);\\n  globeClip.z = globeClippingZ(baseSphere) * globeClip.w;\\n\\n  if (camera.projectionTransition >= 0.999) {\\n    return globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    return camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n  }\\n  let flatClip = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n  return mix(flatClip, globeClip, camera.projectionTransition);\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) prevPos: vec3<f32>,\\n  @location(1) currPos: vec3<f32>,\\n  @location(2) nextPos: vec3<f32>,\\n  @location(3) side: f32,\\n  @location(4) cumulDist: f32,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) lineDistance: f32,\\n  @location(1) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let clipCurr = projectToClip(input.currPos);\\n  let clipPrev = projectToClip(input.prevPos);\\n  let clipNext = projectToClip(input.nextPos);\\n\\n  // Convert to screen space\\n  let screenCurr = clipCurr.xy / clipCurr.w * camera.viewport * 0.5;\\n  let screenPrev = clipPrev.xy / clipPrev.w * camera.viewport * 0.5;\\n  let screenNext = clipNext.xy / clipNext.w * camera.viewport * 0.5;\\n\\n  // Direction vectors\\n  let dirPrev = normalize(screenCurr - screenPrev);\\n  let dirNext = normalize(screenNext - screenCurr);\\n\\n  // Miter direction\\n  let normalPrev = vec2<f32>(-dirPrev.y, dirPrev.x);\\n  let normalNext = vec2<f32>(-dirNext.y, dirNext.x);\\n\\n  var miter: vec2<f32>;\\n  let hasPrev = length(input.currPos - input.prevPos) > 0.0001;\\n  let hasNext = length(input.nextPos - input.currPos) > 0.0001;\\n\\n  if (hasPrev && hasNext) {\\n    miter = normalize(normalPrev + normalNext);\\n    let miterLen = 1.0 / max(dot(miter, normalPrev), 0.1);\\n    miter = miter * min(miterLen, 3.0);\\n  } else if (hasPrev) {\\n    miter = normalPrev;\\n  } else {\\n    miter = normalNext;\\n  }\\n\\n  let halfWidth = material.width * 0.5;\\n  let offset = miter * halfWidth * input.side;\\n  let screenPos = screenCurr + offset;\\n  let ndcPos = screenPos / (camera.viewport * 0.5);\\n\\n  // Cumulative arc-length \\u2192 screen-space via pixels-per-unit ratio\\n  let mercLen = length(input.currPos.xy - input.prevPos.xy);\\n  let screenLen = length(screenCurr - screenPrev);\\n  let ppu = select(1.0, screenLen / mercLen, mercLen > 0.0001);\\n\\n  // Horizon dot for current position\\n  let merc01 = epsg3857ToMerc01(input.currPos);\\n  let angular = mercatorToAngular(merc01);\\n  let spherePos = angularToSphere(angular.x, angular.y);\\n  let clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  var out: VertexOutput;\\n  // Shader-level depth offset: lines render in front of polygons\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0005;\\n  let adjustedZ = clipCurr.z - LAYER_DEPTH_OFFSET * clipCurr.w;\\n  let clampedZ = min(adjustedZ, clipCurr.w * 0.9999);\\n  out.clipPosition = vec4<f32>(ndcPos * clipCurr.w, clampedZ, clipCurr.w);\\n  out.lineDistance = input.cumulDist * ppu;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\nfn getDashSegment(i: i32) -> f32 {\\n  if (i < 4) { return material.dashSegments0[i]; }\\n  return material.dashSegments1[i - 4];\\n}\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon discard\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  let dist = input.lineDistance + material.dashAnimationSpeed * material.time;\\n\\n  // Custom dashArray takes priority\\n  let segCount = i32(material.dashMeta.x);\\n  if (segCount > 0) {\\n    let total = material.dashMeta.y;\\n    let d = ((dist % total) + total) % total;\\n    var cumul = 0.0;\\n    for (var i = 0; i < 8; i++) {\\n      if (i >= segCount) { break; }\\n      cumul += getDashSegment(i);\\n      if (d < cumul) {\\n        if (i % 2 == 1) { discard; }\\n        break;\\n      }\\n    }\\n    return material.color;\\n  }\\n\\n  // Built-in dash patterns\\n  if (material.dashStyle > 0.5 && material.dashStyle < 1.5) {\\n    let pattern = dist % 16.0;\\n    if (pattern > 10.0) { discard; }\\n  } else if (material.dashStyle > 1.5 && material.dashStyle < 2.5) {\\n    let pattern = dist % 6.0;\\n    if (pattern > 3.0) { discard; }\\n  } else if (material.dashStyle > 2.5) {\\n    let pattern = dist % 21.0;\\n    if ((pattern > 10.0 && pattern < 14.0) || pattern > 17.0) { discard; }\\n  }\\n\\n  return material.color;\\n}\\n\";\nexport declare function createGlobeLineBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobeLinePipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface GlobeLinePipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createGlobeLinePipeline(desc: GlobeLinePipelineDescriptor): GlobeLinePipeline;\n//# sourceMappingURL=globe-line-pipeline.d.ts.map","pipelines/globe-mesh3d-pipeline.d.ts":"/**\n * Globe Mesh3D Pipeline — 3D mesh rendering on globe surface.\n *\n * Same vertex layout as mesh3d-pipeline but projects to unit sphere.\n * Height displacement: radius = 1 + h / EARTH_RADIUS_M.\n */\nexport declare const GLOBE_MESH3D_SHADER_SOURCE = \"\\n\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\nconst EARTH_RADIUS_M: f32 = 6378137.0;\\n\\nstruct Mesh3DMaterial {\\n  color: vec4<f32>,\\n  ambient: f32,\\n  shininess: f32,\\n  specularStrength: f32,\\n  _pad: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: Mesh3DMaterial;\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n  @location(1) normal: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) vNormal: vec3<f32>,\\n  @location(1) worldPos: vec3<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var out: VertexOutput;\\n\\n  // XY in Mercator [0..1], Z = height (metres)\\n  let merc01 = input.position.xy;\\n  let ang = mercatorToAngular(merc01);\\n  let sphereBase = angularToSphere(ang.x, ang.y);\\n\\n  // Radial height: position on sphere at (1 + h/R)\\n  let h = input.position.z;\\n  let radius = 1.0 + h / EARTH_RADIUS_M;\\n  let worldPos = sphereBase * radius;\\n\\n  // Horizon clipping\\n  let clipZ = globeClippingZ(sphereBase);\\n  if clipZ < -0.01 {\\n    out.clipPosition = vec4<f32>(0.0, 0.0, -2.0, 1.0); // behind camera\\n    return out;\\n  }\\n\\n  var globeClip = camera.viewProjection * vec4<f32>(worldPos, 1.0);\\n\\n  // Depth: use globe clipping Z + height bias (matches extrusion pattern)\\n  let heightBias = abs(h) / EARTH_RADIUS_M;\\n  let effectiveClipZ = select(clipZ, min(clipZ + 0.0001 + heightBias, 0.9999), clipZ <= 1.0);\\n  globeClip.z = effectiveClipZ * globeClip.w;\\n\\n  // Flat path for transition zone\\n  let heightScale = h / EARTH_RADIUS_M;\\n  let flatPos = vec4<f32>(merc01.x, merc01.y, heightScale, 1.0);\\n\\n  if (camera.projectionTransition >= 0.999) {\\n    out.clipPosition = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    out.clipPosition = camera.flatViewProjection * flatPos;\\n  } else {\\n    var flatClip = camera.flatViewProjection * flatPos;\\n    out.clipPosition = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n\\n  out.worldPos = worldPos;\\n\\n  // Transform normals to globe tangent space\\n  let cosLat = cos(ang.y);\\n  let sinLat = sin(ang.y);\\n  let cosLon = cos(ang.x);\\n  let sinLon = sin(ang.x);\\n  let east = vec3<f32>(cosLon, 0.0, -sinLon);\\n  let north = vec3<f32>(-sinLat * sinLon, cosLat, -sinLat * cosLon);\\n  let up = sphereBase;\\n\\n  out.vNormal = normalize(\\n    input.normal.x * east +\\n    input.normal.y * up +\\n    input.normal.z * north\\n  );\\n\\n  return out;\\n}\\n\\n@fragment\\nfn fs_main(in: VertexOutput) -> @location(0) vec4<f32> {\\n  let N = normalize(in.vNormal);\\n  let L = normalize(vec3<f32>(0.3, 0.8, 0.5));\\n  let V = normalize(-in.worldPos);\\n  let H = normalize(L + V);\\n\\n  let diff = max(dot(N, L), 0.0);\\n  let spec = pow(max(dot(N, H), 0.0), material.shininess) * material.specularStrength;\\n  let lighting = material.ambient + diff * (1.0 - material.ambient) + spec;\\n  let baseColor = material.color.rgb * lighting;\\n  let alpha = material.color.a;\\n\\n  return vec4<f32>(baseColor * alpha, alpha);\\n}\\n\";\nexport interface GlobeMesh3DPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface GlobeMesh3DPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createGlobeMesh3DPipeline(desc: GlobeMesh3DPipelineDescriptor): GlobeMesh3DPipeline;\n//# sourceMappingURL=globe-mesh3d-pipeline.d.ts.map","pipelines/globe-model-pipeline.d.ts":"/**\n * Globe Model Pipeline — 3D instanced mesh rendering on globe\n *\n * Projects GLTF meshes onto the unit sphere with tangent-frame orientation.\n */\nexport declare const GLOBE_MODEL_SHADER_SOURCE = \"\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct ModelMaterial {\\n  baseColorFactor: vec4<f32>,\\n  tintColor: vec4<f32>,\\n  emissiveFactor: vec3<f32>,\\n  metallic: f32,\\n  roughness: f32,\\n  hasBaseColorTex: f32,\\n  hasNormalTex: f32,\\n  hasMetallicRoughnessTex: f32,\\n  hasOcclusionTex: f32,\\n  hasEmissiveTex: f32,\\n  alphaCutoff: f32,\\n  isUnlit: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: ModelMaterial;\\n@group(1) @binding(1) var texSampler: sampler;\\n@group(1) @binding(2) var baseColorTex: texture_2d<f32>;\\n@group(1) @binding(3) var normalTex: texture_2d<f32>;\\n@group(1) @binding(4) var metallicRoughnessTex: texture_2d<f32>;\\n@group(1) @binding(5) var occlusionTex: texture_2d<f32>;\\n@group(1) @binding(6) var emissiveTex: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Helpers \\u2500\\u2500\\u2500\\n\\nfn degreesToRadians(deg: f32) -> f32 {\\n  return deg * PI / 180.0;\\n}\\n\\nfn eulerToRotationMatrix(heading: f32, pitch: f32, roll: f32) -> mat3x3<f32> {\\n  let h = degreesToRadians(heading);\\n  let p = degreesToRadians(pitch);\\n  let r = degreesToRadians(roll);\\n\\n  let ch = cos(h); let sh = sin(h);\\n  let cp = cos(p); let sp = sin(p);\\n  let cr = cos(r); let sr = sin(r);\\n\\n  return mat3x3<f32>(\\n    vec3<f32>(ch*cp, sh*cp, -sp),\\n    vec3<f32>(ch*sp*sr - sh*cr, sh*sp*sr + ch*cr, cp*sr),\\n    vec3<f32>(ch*sp*cr + sh*sr, sh*sp*cr - ch*sr, cp*cr),\\n  );\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n  @location(1) normal: vec3<f32>,\\n  @location(2) texcoord: vec2<f32>,\\n  @location(3) worldPos: vec3<f32>,\\n  @location(4) scaleHeading: vec2<f32>,\\n  @location(5) pitchRollAnchor: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) vNormal: vec3<f32>,\\n  @location(1) vTexcoord: vec2<f32>,\\n  @location(2) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var output: VertexOutput;\\n\\n  let scale = input.scaleHeading.x;\\n  let heading = input.scaleHeading.y;\\n  let pitch = input.pitchRollAnchor.x;\\n  let roll = input.pitchRollAnchor.y;\\n  let anchorZ = input.pitchRollAnchor.z;\\n\\n  // Convert instance position to sphere\\n  let merc01 = epsg3857ToMerc01(input.worldPos);\\n  let angular = mercatorToAngular(merc01);\\n  let spherePos = angularToSphere(angular.x, angular.y);\\n\\n  // Build LOCAL TANGENT FRAME at sphere position\\n  let up = normalize(spherePos);\\n  // Pole guard: if near pole, use X-axis instead of Y\\n  var refDir = vec3<f32>(0.0, 1.0, 0.0);\\n  if (abs(up.y) > 0.999) {\\n    refDir = vec3<f32>(1.0, 0.0, 0.0);\\n  }\\n  let east = normalize(cross(refDir, up));\\n  let north = cross(up, east);\\n  let tangentMatrix = mat3x3<f32>(east, up, north);\\n\\n  // Rotation matrix (shared by globe and flat paths)\\n  let rotMat = eulerToRotationMatrix(heading, pitch, roll);\\n\\n  // \\u2500\\u2500\\u2500 Globe path: tangent frame on unit sphere \\u2500\\u2500\\u2500\\n  let globeScale = scale / 6378137.0;\\n  let localOffset = tangentMatrix * (rotMat * (input.position * globeScale));\\n  // worldPos.z carries altitude in metres from Point coordinates[2]\\n  // Apply same 5\\u00D7 exaggeration as globe-line/point/icon pipelines\\n  let totalAlt = input.worldPos.z + anchorZ;\\n  let altFrac = totalAlt / 6378137.0 * 5.0;\\n  let altOffset = up * altFrac;\\n  let globeFinal = spherePos * (1.0 + altFrac) + localOffset;\\n\\n  var globeClip = camera.viewProjection * vec4<f32>(globeFinal, 1.0);\\n  // Use horizon-aware base depth + local offset for model self-occlusion\\n  let baseDepth = globeClippingZ(spherePos);\\n  let modelUpOffset = dot(localOffset, up) * 10.0;\\n  globeClip.z = (baseDepth - modelUpOffset) * globeClip.w;\\n\\n  // \\u2500\\u2500\\u2500 Flat path: model vertex offset in Mercator [0..1] space \\u2500\\u2500\\u2500\\n  let flatRotated = rotMat * (input.position * scale);\\n  let merc01Scale = 1.0 / (2.0 * HALF_CIRCUMFERENCE);\\n  let flatMerc = vec3<f32>(\\n    merc01.x + flatRotated.x * merc01Scale,\\n    merc01.y - flatRotated.y * merc01Scale,\\n    (flatRotated.z + input.worldPos.z + anchorZ) * merc01Scale\\n  );\\n  var flatClip = camera.flatViewProjection * vec4<f32>(flatMerc, 1.0);\\n  // Remap depth for model self-occlusion: higher Z = closer to camera = lower clip Z\\n  let localZ = flatRotated.z + input.worldPos.z + anchorZ;\\n  let normalizedZ = clamp(0.5 - localZ / (scale * 10.0), 0.01, 0.99);\\n  flatClip.z = normalizedZ * flatClip.w;\\n\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0003;\\n\\n  // Blend based on projection transition\\n  var clipPos: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipPos = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    clipPos = flatClip;\\n  } else {\\n    clipPos = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n  clipPos.z -= LAYER_DEPTH_OFFSET * clipPos.w;\\n  clipPos.z = min(clipPos.z, clipPos.w * 0.9999);\\n\\n  output.clipPosition = clipPos;\\n\\n  // Normal: globe tangent frame vs flat (match 2D mode in flat path)\\n  let globeNormal = normalize(tangentMatrix * (rotMat * input.normal));\\n  let flatNormal = normalize(rotMat * input.normal);\\n  if (camera.projectionTransition >= 0.999) {\\n    output.vNormal = globeNormal;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    output.vNormal = flatNormal;\\n  } else {\\n    output.vNormal = normalize(mix(flatNormal, globeNormal, camera.projectionTransition));\\n  }\\n  output.vTexcoord = input.texcoord;\\n  output.clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  return output;\\n}\\n\\n// \\u2500\\u2500\\u2500 PBR Helpers \\u2500\\u2500\\u2500\\n\\nfn distributionGGX(NdotH: f32, roughness: f32) -> f32 {\\n  let a = roughness * roughness;\\n  let a2 = a * a;\\n  let d = NdotH * NdotH * (a2 - 1.0) + 1.0;\\n  return a2 / (PI * d * d + 0.0001);\\n}\\n\\nfn geometrySchlickGGX(NdotV: f32, roughness: f32) -> f32 {\\n  let r = roughness + 1.0;\\n  let k = (r * r) / 8.0;\\n  return NdotV / (NdotV * (1.0 - k) + k);\\n}\\n\\nfn geometrySmith(NdotV: f32, NdotL: f32, roughness: f32) -> f32 {\\n  return geometrySchlickGGX(NdotV, roughness) * geometrySchlickGGX(NdotL, roughness);\\n}\\n\\nfn fresnelSchlick(cosTheta: f32, F0: vec3<f32>) -> vec3<f32> {\\n  return F0 + (1.0 - F0) * pow(clamp(1.0 - cosTheta, 0.0, 1.0), 5.0);\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment: PBR \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput, @builtin(front_facing) frontFacing: bool) -> @location(0) vec4<f32> {\\n  // Horizon occlusion\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) { discard; }\\n\\n  let uv = input.vTexcoord;\\n\\n  // Base Color\\n  var baseColor = material.baseColorFactor;\\n  if (material.hasBaseColorTex > 0.5) {\\n    baseColor = baseColor * textureSample(baseColorTex, texSampler, uv);\\n  }\\n  baseColor = vec4<f32>(baseColor.rgb * material.tintColor.rgb, baseColor.a * material.tintColor.a);\\n\\n  if (material.alphaCutoff > 0.0 && baseColor.a < material.alphaCutoff) { discard; }\\n\\n  // KHR_materials_unlit: skip all lighting\\n  if (material.isUnlit > 0.5) { return baseColor; }\\n\\n  // Normal (flip for back-faces on double-sided materials)\\n  var N = normalize(input.vNormal);\\n  if (!frontFacing) { N = -N; }\\n\\n  // Metallic / Roughness\\n  var metallic = material.metallic;\\n  var roughness = material.roughness;\\n  if (material.hasMetallicRoughnessTex > 0.5) {\\n    let mrSample = textureSample(metallicRoughnessTex, texSampler, uv);\\n    roughness = roughness * mrSample.g;\\n    metallic = metallic * mrSample.b;\\n  }\\n  roughness = clamp(roughness, 0.04, 1.0);\\n\\n  // PBR Lighting\\n  let lightDir = normalize(vec3<f32>(0.5, 0.8, 0.6));\\n  let viewDir = normalize(vec3<f32>(0.0, 0.0, 1.0));\\n  let H = normalize(lightDir + viewDir);\\n\\n  let NdotL = max(dot(N, lightDir), 0.0);\\n  let NdotV = max(dot(N, viewDir), 0.001);\\n  let NdotH = max(dot(N, H), 0.0);\\n  let HdotV = max(dot(H, viewDir), 0.0);\\n\\n  let F0 = mix(vec3<f32>(0.04), baseColor.rgb, metallic);\\n  let D = distributionGGX(NdotH, roughness);\\n  let G = geometrySmith(NdotV, NdotL, roughness);\\n  let F = fresnelSchlick(HdotV, F0);\\n\\n  let specular = (D * G * F) / (4.0 * NdotV * NdotL + 0.0001);\\n  let kD = (vec3<f32>(1.0) - F) * (1.0 - metallic);\\n  let diffuse = kD * baseColor.rgb / PI;\\n\\n  var color = (diffuse + specular) * NdotL;\\n  color += 0.15 * baseColor.rgb;\\n\\n  if (material.hasOcclusionTex > 0.5) {\\n    color = color * textureSample(occlusionTex, texSampler, uv).r;\\n  }\\n\\n  var emissive = material.emissiveFactor;\\n  if (material.hasEmissiveTex > 0.5) {\\n    emissive = emissive * textureSample(emissiveTex, texSampler, uv).rgb;\\n  }\\n  color += emissive;\\n\\n  return vec4<f32>(color, baseColor.a);\\n}\\n\";\nexport interface GlobeModelPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\nexport interface GlobeModelPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport declare function createGlobeModelBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport declare function createGlobeModelPipeline(desc: GlobeModelPipelineDescriptor): GlobeModelPipeline;\n//# sourceMappingURL=globe-model-pipeline.d.ts.map","pipelines/globe-pipeline.d.ts":"/**\n * Globe Pipeline\n *\n * Lit sphere rendering for 3D globe view.\n * Position + normal vertex layout, Lambertian lighting.\n * Vertex positions are in ECEF coordinates (metres).\n */\nexport declare const GLOBE_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\nstruct GlobeUniforms {\\n  lightDirection: vec3<f32>,\\n  _pad0: f32,\\n  baseColor: vec4<f32>,\\n  ambientIntensity: f32,\\n  diffuseIntensity: f32,\\n  _pad1: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n@group(1) @binding(0) var<uniform> globe: GlobeUniforms;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n  @location(1) normal: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) worldNormal: vec3<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var out: VertexOutput;\\n  out.clipPosition = camera.viewProjection * vec4<f32>(input.position, 1.0);\\n  out.worldNormal = input.normal;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let normal = normalize(input.worldNormal);\\n  let lightDir = normalize(globe.lightDirection);\\n  let ndotl = max(dot(normal, lightDir), 0.0);\\n\\n  let ambient = globe.ambientIntensity;\\n  let diffuse = globe.diffuseIntensity * ndotl;\\n  let lit = globe.baseColor.rgb * (ambient + diffuse);\\n\\n  return vec4<f32>(lit, globe.baseColor.a);\\n}\\n\";\nexport declare function createGlobeBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobePipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n}\nexport interface GlobePipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createGlobePipeline(desc: GlobePipelineDescriptor): GlobePipeline;\n//# sourceMappingURL=globe-pipeline.d.ts.map","pipelines/globe-point-pipeline.d.ts":"/**\n * Globe Point Pipeline\n *\n * WGSL shader: EPSG:3857 position → Mercator [0..1] → Angular → Unit Sphere.\n * Billboard quad per point with SDF circle/square.\n * GlobeCameraUniforms (160 byte) ile dual projection + horizon occlusion.\n */\nexport declare const GLOBE_POINT_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\nconst EARTH_RADIUS_M: f32 = 6371000.0;\\nconst ALTITUDE_EXAG: f32 = 5.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct PointMaterial {\\n  color: vec4<f32>,\\n  outlineColor: vec4<f32>,\\n  size: f32,\\n  outlineWidth: f32,\\n  // 0 = circle, 1 = square\\n  shape: f32,\\n  _pad: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: PointMaterial;\\n\\n// \\u2500\\u2500\\u2500 Helpers \\u2500\\u2500\\u2500\\n\\nfn altitudeOffset(altMeters: f32) -> f32 {\\n  return altMeters / EARTH_RADIUS_M * ALTITUDE_EXAG;\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n  @location(1) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(\\n  input: VertexInput,\\n  @builtin(vertex_index) vid: u32,\\n) -> VertexOutput {\\n  var quadOffsets = array<vec2<f32>, 6>(\\n    vec2<f32>(-0.5, -0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>( 0.5,  0.5),\\n  );\\n\\n  let offset = quadOffsets[vid];\\n  let uv = offset + vec2<f32>(0.5, 0.5);\\n\\n  // EPSG:3857 \\u2192 Mercator [0..1] \\u2192 Angular \\u2192 Sphere\\n  let merc01 = epsg3857ToMerc01(input.position);\\n  let angular = mercatorToAngular(merc01);\\n  let baseSphere = angularToSphere(angular.x, angular.y);\\n  let altFrac = altitudeOffset(input.position.z);\\n  let spherePos = baseSphere * (1.0 + altFrac);\\n\\n  // Globe clip space\\n  var globeClip = camera.viewProjection * vec4<f32>(spherePos, 1.0);\\n  globeClip.z = globeClippingZ(baseSphere) * globeClip.w;\\n\\n  var clipCenter: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipCenter = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    clipCenter = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n  } else {\\n    let flatClip = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n    clipCenter = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n  let clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Billboard offset in screen space\\n  let pixelSize = material.size + material.outlineWidth * 2.0;\\n  let screenOffset = offset * pixelSize;\\n  let ndcOffset = vec2<f32>(\\n    screenOffset.x * 2.0 / camera.viewport.x,\\n    screenOffset.y * 2.0 / camera.viewport.y,\\n  );\\n\\n  // Shader-level depth offset: points render in front of lines\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0008;\\n  let adjustedZ = clipCenter.z - LAYER_DEPTH_OFFSET * clipCenter.w;\\n  let clampedZ = min(adjustedZ, clipCenter.w * 0.9999);\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(\\n    clipCenter.x + ndcOffset.x * clipCenter.w,\\n    clipCenter.y + ndcOffset.y * clipCenter.w,\\n    clampedZ,\\n    clipCenter.w,\\n  );\\n  out.uv = uv;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon discard\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  let centered = input.uv - vec2<f32>(0.5, 0.5);\\n  let totalRadius = 0.5;\\n  let outlineFraction = material.outlineWidth / (material.size + material.outlineWidth * 2.0);\\n  let innerRadius = totalRadius - outlineFraction;\\n\\n  let dist = length(centered);\\n  let aa = fwidth(dist);\\n  let squareDist = max(abs(centered.x), abs(centered.y));\\n\\n  if (material.shape < 0.5) {\\n    // Circle SDF\\n    if (dist > totalRadius) {\\n      discard;\\n    }\\n    let alpha = 1.0 - smoothstep(innerRadius - aa, innerRadius, dist);\\n    return mix(material.outlineColor, material.color, alpha);\\n  } else {\\n    // Square SDF\\n    if (squareDist > totalRadius) {\\n      discard;\\n    }\\n    if (squareDist > innerRadius) {\\n      return material.outlineColor;\\n    }\\n    return material.color;\\n  }\\n}\\n\";\nexport declare function createGlobePointBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobePointPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface GlobePointPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createGlobePointPipeline(desc: GlobePointPipelineDescriptor): GlobePointPipeline;\n//# sourceMappingURL=globe-point-pipeline.d.ts.map","pipelines/globe-polygon-pipeline.d.ts":"/**\n * Globe Polygon Pipeline\n *\n * WGSL shader: EPSG:3857 → Mercator [0..1] → Angular → Unit Sphere.\n * Triangulated mesh rendering with solid fill + horizon occlusion.\n */\nexport declare const GLOBE_POLYGON_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\\n\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\nconst EARTH_RADIUS_M: f32 = 6371000.0;\\nconst ALTITUDE_EXAG: f32 = 5.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct PolygonMaterial {\\n  color: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: PolygonMaterial;\\n\\n// \\u2500\\u2500\\u2500 Helpers \\u2500\\u2500\\u2500\\n\\nfn altitudeOffset(altMeters: f32) -> f32 {\\n  return altMeters / EARTH_RADIUS_M * ALTITUDE_EXAG;\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let merc01 = epsg3857ToMerc01(input.position);\\n  let angular = mercatorToAngular(merc01);\\n  let baseSphere = angularToSphere(angular.x, angular.y);\\n  let altFrac = altitudeOffset(input.position.z);\\n  let spherePos = baseSphere * (1.0 + altFrac);\\n\\n  // Globe clip space\\n  var globeClip = camera.viewProjection * vec4<f32>(spherePos, 1.0);\\n  globeClip.z = globeClippingZ(baseSphere) * globeClip.w;\\n\\n  let clipDot = dot(baseSphere, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Shader-level depth offset: polygons render in front of tiles\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0003;\\n  var clipPos: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipPos = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    clipPos = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n  } else {\\n    let flatClip = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, altFrac, 1.0);\\n    clipPos = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n  clipPos.z -= LAYER_DEPTH_OFFSET * clipPos.w;\\n  clipPos.z = min(clipPos.z, clipPos.w * 0.9999);\\n\\n  var out: VertexOutput;\\n  out.clipPosition = clipPos;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon discard\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  return material.color;\\n}\\n\";\nexport declare function createGlobePolygonBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobePolygonPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface GlobePolygonPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createGlobePolygonPipeline(desc: GlobePolygonPipelineDescriptor): GlobePolygonPipeline;\n//# sourceMappingURL=globe-polygon-pipeline.d.ts.map","pipelines/globe-raster-dem-pipeline.d.ts":"/**\n * Globe Raster DEM Pipeline\n *\n * MapLibre-style DEM displacement: raster basemap tile'lar DEM yükseklik\n * verisine göre vertex-displaced olarak küre üzerinde render edilir.\n *\n * Fark: globe-terrain-pipeline kendi color ramp'ını çizer,\n * bu pipeline ise raster tile texture'ını drape eder (OSM basemap vb.).\n *\n * Vertex: terrain mesh (128×128 + skirt) → UV → DEM sample → radial displacement\n * Fragment: raster tile texture sample (basemap renkleri korunur)\n *\n * Bind groups:\n *   group(0): Globe camera uniforms (shared)\n *   group(1): Tile uniforms + raster sampler + raster texture + DEM texture\n */\nexport declare const GLOBE_RASTER_DEM_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst EARTH_RADIUS: f32 = 6378137.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\nstruct TileDEMUniforms {\\n  // Tile Mercator extent: minX, minY, maxX, maxY (0..1 range)\\n  mercatorExtent: vec4<f32>,\\n  // opacity, exaggeration, demTexWidth, demTexHeight\\n  opacity: f32,\\n  exaggeration: f32,\\n  demTexWidth: f32,\\n  demTexHeight: f32,\\n  // DEM\\u2192Tile UV transform: scaleX, scaleY, offsetX, offsetY\\n  // Maps tile UV to DEM UV space: demUV = tileUV * scale + offset\\n  demTransform: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n@group(1) @binding(0) var<uniform> tile: TileDEMUniforms;\\n@group(1) @binding(1) var tileSampler: sampler;\\n@group(1) @binding(2) var tileTexture: texture_2d<f32>;\\n@group(1) @binding(3) var demTexture: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Bilinear DEM Sampling \\u2500\\u2500\\u2500\\n\\nfn bilinearSampleDEM(uv: vec2<f32>, texSize: vec2<f32>) -> f32 {\\n  let texCoord = uv * texSize - vec2<f32>(0.5);\\n  let x0 = i32(floor(texCoord.x));\\n  let y0 = i32(floor(texCoord.y));\\n  let x1 = x0 + 1;\\n  let y1 = y0 + 1;\\n  let fx = fract(texCoord.x);\\n  let fy = fract(texCoord.y);\\n\\n  let w = i32(texSize.x);\\n  let h = i32(texSize.y);\\n  let cx0 = clamp(x0, 0, w - 1);\\n  let cx1 = clamp(x1, 0, w - 1);\\n  let cy0 = clamp(y0, 0, h - 1);\\n  let cy1 = clamp(y1, 0, h - 1);\\n\\n  let v00 = textureLoad(demTexture, vec2<i32>(cx0, cy0), 0).r;\\n  let v10 = textureLoad(demTexture, vec2<i32>(cx1, cy0), 0).r;\\n  let v01 = textureLoad(demTexture, vec2<i32>(cx0, cy1), 0).r;\\n  let v11 = textureLoad(demTexture, vec2<i32>(cx1, cy1), 0).r;\\n\\n  // NaN guard (NODATA regions \\u2192 zero displacement)\\n  let s00 = select(0.0, v00, v00 == v00);\\n  let s10 = select(0.0, v10, v10 == v10);\\n  let s01 = select(0.0, v01, v01 == v01);\\n  let s11 = select(0.0, v11, v11 == v11);\\n\\n  let top = mix(s00, s10, fx);\\n  let bot = mix(s01, s11, fx);\\n  return mix(top, bot, fy);\\n}\\n\\n// \\u2500\\u2500\\u2500 Globe Projection \\u2500\\u2500\\u2500\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) uvSkirt: vec3<f32>,  // u, v, isSkirt (from terrain mesh)\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) texCoord: vec2<f32>,\\n  @location(1) clipDot: f32,\\n  @location(2) worldPos: vec3<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let u = input.uvSkirt.x;\\n  let v = input.uvSkirt.y;\\n  let isSkirt = input.uvSkirt.z;\\n\\n  // Map UV to tile Mercator coordinates [0..1]\\n  let mercX = mix(tile.mercatorExtent.x, tile.mercatorExtent.z, u);\\n  let mercY = mix(tile.mercatorExtent.y, tile.mercatorExtent.w, v);\\n\\n  // \\u2500\\u2500\\u2500 DEM Sampling \\u2500\\u2500\\u2500\\n  // Transform tile UV to DEM UV space\\n  let demUV = vec2<f32>(\\n    u * tile.demTransform.x + tile.demTransform.z,\\n    (1.0 - v) * tile.demTransform.y + tile.demTransform.w\\n  );\\n  let demTexSize = vec2<f32>(tile.demTexWidth, tile.demTexHeight);\\n  let elevation = bilinearSampleDEM(demUV, demTexSize);\\n\\n  // \\u2500\\u2500\\u2500 Globe projection with radial displacement \\u2500\\u2500\\u2500\\n  let angular = mercatorToAngular(vec2<f32>(mercX, mercY));\\n  let spherePos = angularToSphere(angular.x, angular.y);\\n\\n  // Radial displacement: push vertex along sphere normal\\n  let radialDisplacement = elevation * tile.exaggeration / EARTH_RADIUS;\\n  var displacedPos = spherePos * (1.0 + radialDisplacement);\\n\\n  // Skirt: drop below surface to hide tile edge gaps\\n  if (isSkirt > 0.5) {\\n    let dropAmount = max(abs(elevation) * tile.exaggeration, 200.0 * tile.exaggeration) / EARTH_RADIUS;\\n    displacedPos = spherePos * (1.0 + radialDisplacement - dropAmount);\\n  }\\n\\n  // Globe clip space\\n  var globeClip = camera.viewProjection * vec4<f32>(displacedPos, 1.0);\\n  globeClip.z = globeClippingZ(displacedPos) * globeClip.w;\\n\\n  let clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Depth offset (render in front of non-displaced tiles)\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0002;\\n  var clipPos: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipPos = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    clipPos = camera.flatViewProjection * vec4<f32>(mercX, mercY, 0.0, 1.0);\\n  } else {\\n    let flatClip = camera.flatViewProjection * vec4<f32>(mercX, mercY, 0.0, 1.0);\\n    clipPos = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n  clipPos.z -= LAYER_DEPTH_OFFSET * clipPos.w;\\n  clipPos.z = min(clipPos.z, clipPos.w * 0.9999);\\n\\n  var out: VertexOutput;\\n  out.position = clipPos;\\n  out.texCoord = vec2<f32>(u, v);\\n  out.clipDot = clipDot;\\n  out.worldPos = displacedPos;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon occlusion\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  // Sample the raster tile texture (basemap colors)\\n  let color = textureSample(tileTexture, tileSampler, input.texCoord);\\n\\n  // Simple terrain shading from screen-space derivatives\\n  let ddx = dpdx(input.worldPos);\\n  let ddy = dpdy(input.worldPos);\\n  var normal = normalize(cross(ddx, ddy));\\n  let sphereNormal = normalize(input.worldPos);\\n  if (dot(normal, sphereNormal) < 0.0) {\\n    normal = -normal;\\n  }\\n\\n  // Subtle directional lighting (preserves basemap colors)\\n  let sunDir = normalize(vec3<f32>(0.5, 0.5, 0.707));\\n  let diffuse = max(dot(normal, sunDir), 0.0);\\n  let lighting = 0.6 + 0.4 * diffuse;\\n\\n  return vec4<f32>(color.rgb * lighting, color.a * tile.opacity);\\n}\\n\";\n/**\n * Globe raster DEM tile bind group layout (group 1).\n * 4 entries: uniform + sampler + raster texture + DEM texture (r32float, unfilterable)\n */\nexport declare function createGlobeRasterDEMBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobeRasterDEMPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n}\nexport interface GlobeRasterDEMPipeline {\n    pipeline: GPURenderPipeline;\n    tileBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Create globe raster DEM pipeline.\n * Uses terrain mesh (128×128 + skirt) for vertex displacement,\n * raster tile texture for fragment color (basemap draping).\n */\nexport declare function createGlobeRasterDEMPipeline(desc: GlobeRasterDEMPipelineDescriptor): GlobeRasterDEMPipeline;\n//# sourceMappingURL=globe-raster-dem-pipeline.d.ts.map","pipelines/globe-raster-elevated-pipeline.d.ts":"/**\n * Globe Raster Elevated Pipeline\n *\n * Hybrid pipeline: vertex displacement from elevation texture (like globe-terrain)\n * + raster imagery texturing (like globe-raster). Renders OSM/WMS tiles draped\n * over DTED terrain on the unit sphere.\n *\n * Uses 128×128 terrain mesh with skirt geometry (vec3: u, v, isSkirt).\n *\n * Bind group layout:\n *   @group(0) @binding(0) — GlobeCameraUniforms (VP, flatVP, viewport, transition, radius, clippingPlane)\n *   @group(1) @binding(0) — ElevatedTileUniforms\n *   @group(1) @binding(1) — tileSampler (linear filtering for raster imagery)\n *   @group(1) @binding(2) — tileTexture (raster imagery — rgba8unorm/bgra8unorm)\n *   @group(1) @binding(3) — elevationTex (r32float — unfilterable, manual bilinear)\n */\nexport declare const GLOBE_RASTER_ELEVATED_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst EARTH_RADIUS: f32 = 6378137.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\nstruct ElevatedTileUniforms {\\n  // Tile Mercator extent: minX, minY, maxX, maxY (0..1 range)\\n  mercatorExtent: vec4<f32>,\\n  // opacity, exaggeration, elevDim (core dim, 0=legacy), padding\\n  opacity: f32,\\n  exaggeration: f32,\\n  elevDim: f32,\\n  padding1: f32,\\n  // minElev, maxElev, skirtDrop (0=compute from range), elevTexWidth\\n  minElev: f32,\\n  maxElev: f32,\\n  skirtDrop: f32,\\n  elevTexWidth: f32,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n@group(1) @binding(0) var<uniform> tile: ElevatedTileUniforms;\\n@group(1) @binding(1) var tileSampler: sampler;\\n@group(1) @binding(2) var tileTexture: texture_2d<f32>;\\n@group(1) @binding(3) var elevationTex: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Bilinear Sampling Helper (r32float is unfilterable in WebGPU) \\u2500\\u2500\\u2500\\n\\n// Legacy mode: direct UV\\u2192texel mapping (no padding)\\nfn bilinearSampleLegacy(uv: vec2<f32>, texSize: vec2<f32>) -> f32 {\\n  let texCoord = uv * texSize - vec2<f32>(0.5);\\n  let x0 = i32(floor(texCoord.x));\\n  let y0 = i32(floor(texCoord.y));\\n  let x1 = x0 + 1;\\n  let y1 = y0 + 1;\\n  let fx = fract(texCoord.x);\\n  let fy = fract(texCoord.y);\\n\\n  let w = i32(texSize.x);\\n  let h = i32(texSize.y);\\n  let cx0 = clamp(x0, 0, w - 1);\\n  let cx1 = clamp(x1, 0, w - 1);\\n  let cy0 = clamp(y0, 0, h - 1);\\n  let cy1 = clamp(y1, 0, h - 1);\\n\\n  let v00 = textureLoad(elevationTex, vec2<i32>(cx0, cy0), 0).r;\\n  let v10 = textureLoad(elevationTex, vec2<i32>(cx1, cy0), 0).r;\\n  let v01 = textureLoad(elevationTex, vec2<i32>(cx0, cy1), 0).r;\\n  let v11 = textureLoad(elevationTex, vec2<i32>(cx1, cy1), 0).r;\\n\\n  let s00 = select(0.0, v00, v00 == v00);\\n  let s10 = select(0.0, v10, v10 == v10);\\n  let s01 = select(0.0, v01, v01 == v01);\\n  let s11 = select(0.0, v11, v11 == v11);\\n\\n  let top = mix(s00, s10, fx);\\n  let bot = mix(s01, s11, fx);\\n  return mix(top, bot, fy);\\n}\\n\\n// MapLibre-style padded bilinear sampling\\n// Texture is (dim+2)\\u00D7(dim+2), core data lives in pixels [1..dim].\\n// UV [0,1] maps to core data \\u2192 offset by +1 into the padded texture.\\n// Bilinear tap naturally reads border pixels for seamless tile edges.\\nfn bilinearSamplePadded(uv: vec2<f32>, dim: f32) -> f32 {\\n  let stride = dim + 2.0;\\n  // UV \\u2192 core pixel coordinate, then shift by +1 for padding\\n  let coord = uv * dim + 1.0;\\n  let f = fract(coord - 0.5);\\n  let base = floor(coord - 0.5);\\n\\n  let ix0 = i32(base.x);\\n  let iy0 = i32(base.y);\\n  let ix1 = ix0 + 1;\\n  let iy1 = iy0 + 1;\\n\\n  // Clamp to padded texture bounds [0, stride-1]\\n  let iStride = i32(stride);\\n  let cx0 = clamp(ix0, 0, iStride - 1);\\n  let cx1 = clamp(ix1, 0, iStride - 1);\\n  let cy0 = clamp(iy0, 0, iStride - 1);\\n  let cy1 = clamp(iy1, 0, iStride - 1);\\n\\n  let v00 = textureLoad(elevationTex, vec2<i32>(cx0, cy0), 0).r;\\n  let v10 = textureLoad(elevationTex, vec2<i32>(cx1, cy0), 0).r;\\n  let v01 = textureLoad(elevationTex, vec2<i32>(cx0, cy1), 0).r;\\n  let v11 = textureLoad(elevationTex, vec2<i32>(cx1, cy1), 0).r;\\n\\n  // NaN guard\\n  let s00 = select(0.0, v00, v00 == v00);\\n  let s10 = select(0.0, v10, v10 == v10);\\n  let s01 = select(0.0, v01, v01 == v01);\\n  let s11 = select(0.0, v11, v11 == v11);\\n\\n  let top = mix(s00, s10, f.x);\\n  let bot = mix(s01, s11, f.x);\\n  return mix(top, bot, f.y);\\n}\\n\\n// Dispatch: use padded sampling when elevDim > 0, legacy otherwise\\nfn bilinearSample(uv: vec2<f32>) -> f32 {\\n  if (tile.elevDim > 0.0) {\\n    return bilinearSamplePadded(uv, tile.elevDim);\\n  }\\n  return bilinearSampleLegacy(uv, vec2<f32>(tile.elevTexWidth, tile.elevTexWidth));\\n}\\n\\n// \\u2500\\u2500\\u2500 Globe Projection Helpers \\u2500\\u2500\\u2500\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) uvSkirt: vec3<f32>,  // u, v, isSkirt (from terrain-mesh.ts)\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) texCoord: vec2<f32>,  // For raster imagery sampling\\n  @location(1) clipDot: f32,         // For horizon discard\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let u = input.uvSkirt.x;\\n  let v = input.uvSkirt.y;\\n  let isSkirt = input.uvSkirt.z;\\n\\n  // Map UV to tile Mercator coordinates [0..1]\\n  let mercX = mix(tile.mercatorExtent.x, tile.mercatorExtent.z, u);\\n  let mercY = mix(tile.mercatorExtent.y, tile.mercatorExtent.w, v);\\n\\n  // Sample elevation at this UV\\n  // getElevationGrid produces row 0 = north, matching mesh v=0 = north\\n  // Clamp to >= 0: Terrarium encoding uses -32768 for ocean/NODATA.\\n  // With high exaggeration (50x), unclamped ocean pushes vertices deep underground\\n  // (radius 0.74 instead of 1.0), causing the \\\"everything goes dark\\\" effect.\\n  let elevation = max(bilinearSample(vec2<f32>(u, v)), 0.0);\\n\\n  // Mercator \\u2192 Angular \\u2192 Unit Sphere\\n  let angular = mercatorToAngular(vec2<f32>(mercX, mercY));\\n  var spherePos = angularToSphere(angular.x, angular.y);\\n\\n  // Radial displacement along sphere normal\\n  let radialDisplacement = elevation * tile.exaggeration / EARTH_RADIUS;\\n  var displacedPos = spherePos * (1.0 + radialDisplacement);\\n\\n  // Skirt: displace inward (toward center) to hide tile edge gaps\\n  if (isSkirt > 0.5) {\\n    var dropAmount: f32;\\n    if (tile.skirtDrop > 0.0) {\\n      // Zoom-dependent skirt (MapLibre-style): pre-computed by Mode3D\\n      dropAmount = tile.skirtDrop / EARTH_RADIUS;\\n    } else {\\n      // Legacy: compute from clamped elevation range.\\n      // Since surface vertices clamp elevation to >= 0, the range used for\\n      // skirt depth must also use clamped min/max. Without this, Terrarium\\n      // ocean tiles (min=-32768) produce range=33768 \\u2192 skirt drops 26% of R.\\n      let clampedRange = max(tile.maxElev, 0.0) - max(tile.minElev, 0.0);\\n      dropAmount = max(clampedRange * tile.exaggeration, 500.0 * tile.exaggeration) / EARTH_RADIUS;\\n    }\\n    displacedPos = spherePos * (1.0 + radialDisplacement - dropAmount);\\n  }\\n\\n  // Globe clip space \\u2014 use displaced position for projection AND depth\\n  var globeClip = camera.viewProjection * vec4<f32>(displacedPos, 1.0);\\n  globeClip.z = globeClippingZ(displacedPos) * globeClip.w;\\n\\n  // Clipping plane dot for horizon occlusion (use undisplaced for stable occlusion)\\n  let clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // 3-way projectionTransition blend \\u2014 must match flat raster pipeline\\n  // to prevent seam/gap between elevated and flat tiles during transition\\n  var clipPos: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipPos = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    // Flat mode: Mercator projection, no elevation displacement\\n    clipPos = camera.flatViewProjection * vec4<f32>(mercX, mercY, 0.0, 1.0);\\n  } else {\\n    // Transition: blend flat (no displacement) with globe (displaced)\\n    let flatClip = camera.flatViewProjection * vec4<f32>(mercX, mercY, 0.0, 1.0);\\n    clipPos = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n\\n  // Depth offset: elevated tiles render in front of flat tiles\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0005;\\n  clipPos.z -= LAYER_DEPTH_OFFSET * clipPos.w;\\n  clipPos.z = min(clipPos.z, clipPos.w * 0.9999);\\n\\n  var out: VertexOutput;\\n  out.position = clipPos;\\n  out.texCoord = vec2<f32>(u, v);  // Use mesh UV directly for raster imagery\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon occlusion \\u2014 discard back-hemisphere fragments\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  // Sample raster imagery (OSM/WMS tile texture)\\n  let color = textureSample(tileTexture, tileSampler, input.texCoord);\\n  return vec4<f32>(color.rgb, color.a * tile.opacity);\\n}\\n\";\n/**\n * Elevated tile bind group layout (group 1).\n * 4 entries: uniform + sampler + raster texture + elevation texture (unfilterable)\n */\nexport declare function createElevatedTileBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobeRasterElevatedPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n}\nexport interface GlobeRasterElevatedPipeline {\n    pipeline: GPURenderPipeline;\n    elevatedTileBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Create globe raster elevated pipeline.\n * Uses terrain mesh (128×128 + skirt) with elevation displacement + raster imagery.\n */\nexport declare function createGlobeRasterElevatedPipeline(desc: GlobeRasterElevatedPipelineDescriptor): GlobeRasterElevatedPipeline;\n//# sourceMappingURL=globe-raster-elevated-pipeline.d.ts.map","pipelines/globe-raster-pipeline.d.ts":"/**\n * Globe Raster Pipeline\n *\n * WGSL shader: tile UV → Mercator → Angular → Unit Sphere dönüşüm zinciri.\n * projectionTransition (0-1) ile Mercator flat ↔ globe sphere smooth blend.\n * Horizon occlusion: clipping plane dot product testi.\n * Height displacement:\n * - mode 0: world-space debug brush height texture\n * - mode 1: tile-local terrain height texture (meters -> world unit conversion)\n *\n * MapLibre'nin _projection_globe.vertex.glsl adaptasyonu — WebGPU/WGSL.\n */\nimport type { SubdivisionMesh } from './subdivision-mesh';\nexport declare const GLOBE_RASTER_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst EARTH_RADIUS_METERS: f32 = 6378137.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  // View-Projection matrix (column-major 4x4) \\u2014 globe (unit sphere)\\n  viewProjection: mat4x4<f32>,\\n  // Flat Mercator VP matrix (column-major 4x4) \\u2014 Mercator [0..1] ortho\\n  flatViewProjection: mat4x4<f32>,\\n  // Viewport dimensions\\n  viewport: vec2<f32>,\\n  // Projection transition: 0 = Mercator flat, 1 = globe sphere\\n  projectionTransition: f32,\\n  // Globe radius (unit sphere = 1.0)\\n  globeRadius: f32,\\n  // Clipping plane for horizon occlusion: Ax + By + Cz + D\\n  clippingPlane: vec4<f32>,\\n};\\n\\nstruct TileUniforms {\\n  // Tile Mercator extent: minX, minY, maxX, maxY (0..1 range)\\n  mercatorExtent: vec4<f32>,\\n  // Tile opacity\\n  opacity: f32,\\n  // Height exaggeration factor\\n  heightExaggeration: f32,\\n  // Height mode: 0 = world-space debug brush, 1 = tile-local terrain\\n  heightMode: f32,\\n  // Depth bias: fallback parent tiles pushed back to prevent z-fighting\\n  depthBias: f32,\\n  // Tile UV remap into terrain texture UV: [offsetX, offsetY, scaleX, scaleY]\\n  terrainUv: vec4<f32>,\\n  // Terrain lighting controls: [ambient, diffuse, shadowStrength, shadowSoftness]\\n  lightParams: vec4<f32>,\\n  // Sun controls: [azimuthDeg, altitudeDeg, enabled(0|1), _pad]\\n  sunParams: vec4<f32>,\\n  // Post-process filters: [brightness, contrast, saturate, _pad]\\n  filters: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n@group(1) @binding(0) var<uniform> tile: TileUniforms;\\n@group(1) @binding(1) var tileSampler: sampler;\\n@group(1) @binding(2) var tileTexture: texture_2d<f32>;\\n@group(2) @binding(0) var heightMap: texture_2d<f32>;\\n@group(2) @binding(1) var heightSampler: sampler;\\n\\n// \\u2500\\u2500\\u2500 Manual bilinear height sampling \\u2500\\u2500\\u2500\\n// r32float doesn't support filtering sampler without float32-filterable feature.\\n// We use textureLoad + manual bilinear to get smooth interpolation.\\n\\nfn sampleHeight(uv: vec2<f32>) -> f32 {\\n  let dims = vec2<f32>(textureDimensions(heightMap, 0));\\n  let tc = uv * max(dims - vec2(1.0), vec2(0.0));\\n  let tc0 = vec2<i32>(floor(tc));\\n  let f = fract(tc);\\n  let maxC = vec2<i32>(dims) - 1;\\n  let h00 = textureLoad(heightMap, clamp(tc0, vec2(0), maxC), 0).r;\\n  let h10 = textureLoad(heightMap, clamp(tc0 + vec2(1, 0), vec2(0), maxC), 0).r;\\n  let h01 = textureLoad(heightMap, clamp(tc0 + vec2(0, 1), vec2(0), maxC), 0).r;\\n  let h11 = textureLoad(heightMap, clamp(tc0 + vec2(1, 1), vec2(0), maxC), 0).r;\\n  return mix(mix(h00, h10, f.x), mix(h01, h11, f.x), f.y);\\n}\\n\\nfn terrainUvForTileUv(tileUv: vec2<f32>) -> vec2<f32> {\\n  return tile.terrainUv.xy + tileUv * tile.terrainUv.zw;\\n}\\n\\nfn heightUvForTileUv(tileUv: vec2<f32>, mercUv: vec2<f32>) -> vec2<f32> {\\n  if (tile.heightMode >= 0.5) {\\n    return terrainUvForTileUv(tileUv);\\n  }\\n  return mercUv;\\n}\\n\\nfn heightToWorldUnit(heightValue: f32) -> f32 {\\n  var hUnit = heightValue;\\n  if (tile.heightMode >= 0.5) {\\n    hUnit = heightValue / EARTH_RADIUS_METERS;\\n  }\\n  return hUnit * tile.heightExaggeration;\\n}\\n\\nfn sunDirection(azimuthDeg: f32, altitudeDeg: f32) -> vec3<f32> {\\n  let az = azimuthDeg * PI / 180.0;\\n  let alt = altitudeDeg * PI / 180.0;\\n  let cosAlt = cos(alt);\\n  return normalize(vec3<f32>(\\n    sin(az) * cosAlt,  // east\\n    cos(az) * cosAlt,  // north\\n    sin(alt),          // up\\n  ));\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) uv: vec2<f32>,  // Grid UV (0..1)\\n  @location(1) skirt: f32,      // 0 = surface vertex, 1 = skirt vertex\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) texCoord: vec2<f32>,\\n  @location(1) clipDot: f32,  // For fragment discard (horizon)\\n};\\n\\n// Mercator (0..1) \\u2192 Angular (radians)\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\n// Angular (radians) \\u2192 Unit Sphere (3D)\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\n// MapLibre custom Z: geometry-aware depth from clipping plane\\n// Replaces perspective Z \\u2014 handles horizon occlusion + depth in one step\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  // Map grid UV to tile Mercator coordinates\\n  let mercX = mix(tile.mercatorExtent.x, tile.mercatorExtent.z, input.uv.x);\\n  let mercY = mix(tile.mercatorExtent.y, tile.mercatorExtent.w, input.uv.y);\\n\\n  // \\u2500\\u2500\\u2500 Globe path: Mercator \\u2192 Angular \\u2192 Sphere \\u2192 globe clip space \\u2500\\u2500\\u2500\\n  let angular = mercatorToAngular(vec2<f32>(mercX, mercY));\\n  let sphereBase = angularToSphere(angular.x, angular.y);\\n\\n  // Height sampling: world-space brush (mode=0) or tile-local terrain (mode=1).\\n  var h = 0.0;\\n  if (tile.heightMode >= 0.5) {\\n    h = sampleHeight(terrainUvForTileUv(input.uv));\\n  } else {\\n    h = sampleHeight(vec2<f32>(mercX, mercY));\\n  }\\n  var displacement = heightToWorldUnit(h);\\n\\n  // Skirts hide tile cracks when adjacent tiles have different sampled heights.\\n  if (tile.heightMode >= 0.5 && input.skirt >= 0.5) {\\n    let skirtDepth = max(0.0015, abs(displacement) * 0.35 + 0.0006);\\n    displacement -= skirtDepth;\\n  }\\n  let spherePos = sphereBase * (1.0 + displacement);\\n\\n  var globeClip = camera.viewProjection * vec4<f32>(spherePos, 1.0);\\n  // Replace globe Z with horizon-aware depth (use base for stable clipping)\\n  globeClip.z = globeClippingZ(sphereBase) * globeClip.w;\\n\\n  // \\u2500\\u2500\\u2500 Blend clip-space positions \\u2500\\u2500\\u2500\\n  let clipDot = dot(sphereBase, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Shader-level depth offset: tiles render in front of pole caps.\\n  // depthBias > 0 for fallback parent tiles, 0 for exact tiles \\u2014 prevents z-fighting.\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0001;\\n  var clipPos: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipPos = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    var flatClip = camera.flatViewProjection * vec4<f32>(mercX, mercY, displacement, 1.0);\\n    clipPos = flatClip;\\n  } else {\\n    var flatClip = camera.flatViewProjection * vec4<f32>(mercX, mercY, displacement, 1.0);\\n    clipPos = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n  clipPos.z -= LAYER_DEPTH_OFFSET * clipPos.w;\\n  clipPos.z += tile.depthBias * clipPos.w;\\n  clipPos.z = min(clipPos.z, clipPos.w * 0.9999);\\n\\n  var out: VertexOutput;\\n  out.position = clipPos;\\n  out.texCoord = vec2<f32>(input.uv.x, input.uv.y);\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\nfn applyFilters(c: vec3<f32>) -> vec3<f32> {\\n  var rgb = c * tile.filters.x;\\n  rgb = (rgb - 0.5) * tile.filters.y + 0.5;\\n  let gray = dot(rgb, vec3<f32>(0.2126, 0.7152, 0.0722));\\n  return mix(vec3<f32>(gray), rgb, tile.filters.z);\\n}\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Discard back-hemisphere fragments for clean horizon edge\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  var color = textureSample(tileTexture, tileSampler, input.texCoord);\\n  if (tile.sunParams.z < 0.5) {\\n    var rgb = applyFilters(color.rgb);\\n    return vec4<f32>(rgb, color.a * tile.opacity);\\n  }\\n\\n  let mercX = mix(tile.mercatorExtent.x, tile.mercatorExtent.z, input.texCoord.x);\\n  let mercY = mix(tile.mercatorExtent.y, tile.mercatorExtent.w, input.texCoord.y);\\n  let mercUv = vec2<f32>(mercX, mercY);\\n  let heightUv = heightUvForTileUv(input.texCoord, mercUv);\\n\\n  let dims = vec2<f32>(textureDimensions(heightMap, 0));\\n  let texel = vec2<f32>(1.0) / max(dims, vec2<f32>(1.0));\\n  let stepX = vec2<f32>(texel.x, 0.0);\\n  let stepY = vec2<f32>(0.0, texel.y);\\n\\n  let hW = heightToWorldUnit(sampleHeight(heightUv - stepX));\\n  let hE = heightToWorldUnit(sampleHeight(heightUv + stepX));\\n  let hN = heightToWorldUnit(sampleHeight(heightUv - stepY));\\n  let hS = heightToWorldUnit(sampleHeight(heightUv + stepY));\\n\\n  let dhdx = (hE - hW) / max(1e-6, 2.0 * texel.x);\\n  let dhdy = (hN - hS) / max(1e-6, 2.0 * texel.y);\\n\\n  let angular = mercatorToAngular(mercUv);\\n  let up = normalize(angularToSphere(angular.x, angular.y));\\n  let refDir = select(vec3<f32>(0.0, 0.0, 1.0), vec3<f32>(1.0, 0.0, 0.0), abs(up.z) > 0.99);\\n  let east = normalize(cross(refDir, up));\\n  let north = normalize(cross(up, east));\\n  let normal = normalize(up - east * dhdx - north * dhdy);\\n\\n  let lightDir = sunDirection(tile.sunParams.x, tile.sunParams.y);\\n  let ambient = clamp(tile.lightParams.x, 0.0, 1.0);\\n  let diffuse = clamp(tile.lightParams.y, 0.0, 2.0);\\n  let ndotl = max(dot(normal, lightDir), 0.0);\\n  var lightTerm = ambient + diffuse * ndotl;\\n\\n  // Pseudo-shadow: compare height toward sun direction in local tangent frame.\\n  var lightUvDir = vec2<f32>(dot(lightDir, east), -dot(lightDir, north));\\n  let dirLen = max(length(lightUvDir), 1e-6);\\n  lightUvDir = lightUvDir / dirLen;\\n\\n  let softness = clamp(tile.lightParams.w, 0.0, 1.0);\\n  let sampleDist = mix(1.5, 7.0, softness);\\n  let shadowStep = vec2<f32>(lightUvDir.x * texel.x, lightUvDir.y * texel.y) * sampleDist;\\n\\n  let hCenter = heightToWorldUnit(sampleHeight(heightUv));\\n  let hTowardSun = heightToWorldUnit(sampleHeight(heightUv + shadowStep));\\n  let rise = max(0.0, hTowardSun - hCenter);\\n\\n  let sunAltRad = tile.sunParams.y * PI / 180.0;\\n  let altitudeFactor = max(0.15, sin(sunAltRad));\\n  let riseScale = select(800.0, 22000.0, tile.heightMode >= 0.5);\\n  let occlusion = clamp(rise * riseScale / altitudeFactor, 0.0, 1.0);\\n  // Disable pseudo-shadow when view is strongly globe-like (zoomed out).\\n  // Shadows fade in only as we transition toward the flatter close-zoom view.\\n  let transitionToFlat = clamp(1.0 - camera.projectionTransition, 0.0, 1.0);\\n  let zoomShadowFade = smoothstep(0.08, 0.32, transitionToFlat);\\n  let shadowStrength = clamp(tile.lightParams.z, 0.0, 1.0) * zoomShadowFade;\\n  let pseudoShadow = 1.0 - occlusion * shadowStrength;\\n  lightTerm *= pseudoShadow;\\n\\n  let litRgb = color.rgb * clamp(lightTerm, 0.0, 2.0);\\n  var finalRgb = applyFilters(litRgb);\\n  return vec4<f32>(finalRgb, color.a * tile.opacity);\\n}\\n\";\n/**\n * Globe camera bind group layout (group 0).\n * Extended with projectionTransition, globeRadius, clippingPlane.\n */\nexport declare function createGlobeCameraBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\n/**\n * Globe tile bind group layout (group 1).\n * Same as raster: uniform + sampler + texture.\n */\nexport declare function createGlobeTileBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobeRasterPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    subdivisions?: number;\n    sampleCount?: number;\n}\nexport interface GlobeRasterPipeline {\n    pipeline: GPURenderPipeline;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    globeTileBindGroupLayout: GPUBindGroupLayout;\n    heightBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n    heightSampler: GPUSampler;\n    subdivisionMesh: SubdivisionMesh;\n    zeroHeightTexture: GPUTexture;\n    zeroHeightBindGroup: GPUBindGroup;\n}\n/**\n * Globe raster pipeline oluştur.\n * Subdivision mesh + WGSL shader + WebGPU pipeline.\n */\nexport declare function createGlobeRasterPipeline(desc: GlobeRasterPipelineDescriptor): GlobeRasterPipeline;\n//# sourceMappingURL=globe-raster-pipeline.d.ts.map","pipelines/globe-terrain-pipeline.d.ts":"/**\n * Globe Terrain Pipeline\n *\n * WGSL shader for 3D DTED terrain rendering on the globe.\n * Combines globe projection (UV → Mercator → Angular → Sphere) with\n * vertex displacement along the sphere normal from elevation texture.\n *\n * Uses the same globe camera bind group (group 0) as globe-raster-pipeline.\n * Terrain tile bind group (group 1) has elevation/hillshade/colorRamp textures.\n *\n * Key differences from terrain-3d-pipeline (flat):\n * - Radial displacement: spherePos *= (1 + elev * exaggeration / EARTH_RADIUS)\n * - Globe projection: Mercator [0..1] → Angular → Unit Sphere\n * - Horizon occlusion: clipping plane dot product\n * - Screen-space normals: dpdx/dpdy derivatives for correct spherical normals\n */\nexport declare const GLOBE_TERRAIN_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst EARTH_RADIUS: f32 = 6378137.0;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\nstruct GlobeTerrainTileUniforms {\\n  // Tile Mercator extent: minX, minY, maxX, maxY (0..1 range)\\n  mercatorExtent: vec4<f32>,\\n  // opacity, minElev, maxElev, hillshadeStrength\\n  opacity: f32,\\n  minElev: f32,\\n  maxElev: f32,\\n  hillshadeStrength: f32,\\n  // exaggeration, elevTexWidth, elevTexHeight, skirtDrop\\n  exaggeration: f32,\\n  elevTexWidth: f32,\\n  elevTexHeight: f32,\\n  skirtDrop: f32,\\n  // sunDirection (normalized) + padding\\n  sunDirection: vec3<f32>,\\n  padding0: f32,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n@group(1) @binding(0) var<uniform> tile: GlobeTerrainTileUniforms;\\n@group(1) @binding(1) var terrainSampler: sampler;\\n@group(1) @binding(2) var elevationTex: texture_2d<f32>;\\n@group(1) @binding(3) var hillshadeTex: texture_2d<f32>;\\n@group(1) @binding(4) var colorRampTex: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Bilinear Sampling Helper \\u2500\\u2500\\u2500\\n\\nfn bilinearSample(uv: vec2<f32>, texSize: vec2<f32>) -> f32 {\\n  let texCoord = uv * texSize - vec2<f32>(0.5);\\n  let x0 = i32(floor(texCoord.x));\\n  let y0 = i32(floor(texCoord.y));\\n  let x1 = x0 + 1;\\n  let y1 = y0 + 1;\\n  let fx = fract(texCoord.x);\\n  let fy = fract(texCoord.y);\\n\\n  let w = i32(texSize.x);\\n  let h = i32(texSize.y);\\n  let cx0 = clamp(x0, 0, w - 1);\\n  let cx1 = clamp(x1, 0, w - 1);\\n  let cy0 = clamp(y0, 0, h - 1);\\n  let cy1 = clamp(y1, 0, h - 1);\\n\\n  let v00 = textureLoad(elevationTex, vec2<i32>(cx0, cy0), 0).r;\\n  let v10 = textureLoad(elevationTex, vec2<i32>(cx1, cy0), 0).r;\\n  let v01 = textureLoad(elevationTex, vec2<i32>(cx0, cy1), 0).r;\\n  let v11 = textureLoad(elevationTex, vec2<i32>(cx1, cy1), 0).r;\\n\\n  let s00 = select(0.0, v00, v00 == v00);\\n  let s10 = select(0.0, v10, v10 == v10);\\n  let s01 = select(0.0, v01, v01 == v01);\\n  let s11 = select(0.0, v11, v11 == v11);\\n\\n  let top = mix(s00, s10, fx);\\n  let bot = mix(s01, s11, fx);\\n  return mix(top, bot, fy);\\n}\\n\\n// \\u2500\\u2500\\u2500 Globe Projection Helpers \\u2500\\u2500\\u2500\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) uvSkirt: vec3<f32>,  // u, v, isSkirt\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n  @location(1) worldPos: vec3<f32>,\\n  @location(2) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let u = input.uvSkirt.x;\\n  let v = input.uvSkirt.y;\\n  let isSkirt = input.uvSkirt.z;\\n\\n  // Map UV to tile Mercator coordinates [0..1]\\n  let mercX = mix(tile.mercatorExtent.x, tile.mercatorExtent.z, u);\\n  let mercY = mix(tile.mercatorExtent.y, tile.mercatorExtent.w, v);\\n\\n  // Sample elevation at this UV\\n  let texSize = vec2<f32>(tile.elevTexWidth, tile.elevTexHeight);\\n  // Flip V: texture (0,0) = top-left (north), UV v=0 = south\\n  let texUV = vec2<f32>(u, 1.0 - v);\\n  let elevation = bilinearSample(texUV, texSize);\\n\\n  // Mercator \\u2192 Angular \\u2192 Unit Sphere\\n  let angular = mercatorToAngular(vec2<f32>(mercX, mercY));\\n  var spherePos = angularToSphere(angular.x, angular.y);\\n\\n  // Radial displacement along sphere normal\\n  let radialDisplacement = elevation * tile.exaggeration / EARTH_RADIUS;\\n  var displacedPos = spherePos * (1.0 + radialDisplacement);\\n\\n  // Skirt: displace inward (toward center) to hide tile edge gaps\\n  if (isSkirt > 0.5) {\\n    let range = tile.maxElev - tile.minElev;\\n    let dropAmount = max(range * tile.exaggeration, 500.0 * tile.exaggeration) / EARTH_RADIUS;\\n    displacedPos = spherePos * (1.0 + radialDisplacement - dropAmount);\\n  }\\n\\n  // Globe clip space \\u2014 use DISPLACED position for both projection AND depth\\n  var globeClip = camera.viewProjection * vec4<f32>(displacedPos, 1.0);\\n  // Use displacedPos for depth: displaced terrain is naturally closer to camera,\\n  // so its dot product with clipping plane is larger \\u2192 smaller z \\u2192 passes depth test\\n  globeClip.z = globeClippingZ(displacedPos) * globeClip.w;\\n\\n  // Clipping plane dot product for horizon occlusion (use undisplaced for stable occlusion)\\n  let clipDot = dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // Terrain ALWAYS uses globe projection \\u2014 flat Mercator has no Z dimension for terrain.\\n  // At high zoom (projectionTransition \\u2248 0), raster tiles go flat but terrain stays on sphere.\\n  const LAYER_DEPTH_OFFSET: f32 = 0.0005;\\n  var clipPos = globeClip;\\n  clipPos.z -= LAYER_DEPTH_OFFSET * clipPos.w;\\n  clipPos.z = min(clipPos.z, clipPos.w * 0.9999);\\n\\n  var out: VertexOutput;\\n  out.position = clipPos;\\n  out.uv = texUV;\\n  out.worldPos = displacedPos;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon occlusion \\u2014 discard back-hemisphere fragments\\n  if (camera.projectionTransition > 0.01 && input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  let texSize = vec2<f32>(tile.elevTexWidth, tile.elevTexHeight);\\n\\n  // Elevation for color ramp lookup\\n  let elevation = bilinearSample(input.uv, texSize);\\n\\n  // NaN check (NODATA regions)\\n  if (elevation != elevation) {\\n    discard;\\n  }\\n\\n  // Screen-space normal from world position derivatives\\n  let ddx = dpdx(input.worldPos);\\n  let ddy = dpdy(input.worldPos);\\n  var normal = normalize(cross(ddx, ddy));\\n\\n  // Ensure normal points outward (away from sphere center)\\n  let sphereNormal = normalize(input.worldPos);\\n  if (dot(normal, sphereNormal) < 0.0) {\\n    normal = -normal;\\n  }\\n\\n  // Lambertian directional lighting\\n  let sunDir = normalize(tile.sunDirection);\\n  let diffuse = max(dot(normal, sunDir), 0.0);\\n  let lighting = 0.3 + 0.7 * diffuse;\\n\\n  // Normalize elevation to [0, 1] for color ramp lookup\\n  let range = tile.maxElev - tile.minElev;\\n  var normalized: f32;\\n  if (range > 0.0) {\\n    normalized = clamp((elevation - tile.minElev) / range, 0.0, 1.0);\\n  } else {\\n    normalized = 0.5;\\n  }\\n\\n  // Sample color ramp LUT (rgba8unorm 256\\u00D71)\\n  let color = textureSample(colorRampTex, terrainSampler, vec2<f32>(normalized, 0.5));\\n\\n  // Sample pre-computed hillshade (r8unorm)\\n  let shade = textureSample(hillshadeTex, terrainSampler, input.uv).r;\\n\\n  // Blend: color * lighting * hillshade\\n  let shadeFactor = mix(1.0, shade, tile.hillshadeStrength);\\n  let lit = color.rgb * lighting * shadeFactor;\\n\\n  return vec4<f32>(lit, color.a * tile.opacity);\\n}\\n\";\n/**\n * Globe terrain tile bind group layout (group 1).\n * 5 entries: uniform + sampler + elevation(r32float) + hillshade(r8unorm) + colorRamp(rgba8unorm)\n */\nexport declare function createGlobeTerrainBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface GlobeTerrainPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n}\nexport interface GlobeTerrainPipeline {\n    pipeline: GPURenderPipeline;\n    terrainBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Create globe terrain render pipeline.\n * Uses terrain mesh (128×128 + skirt), globe camera, depth write.\n */\nexport declare function createGlobeTerrainPipeline(desc: GlobeTerrainPipelineDescriptor): GlobeTerrainPipeline;\n//# sourceMappingURL=globe-terrain-pipeline.d.ts.map","pipelines/icon-pipeline.d.ts":"/**\n * Icon Pipeline\n *\n * Billboard quad per point, instanced rendering.\n * WGSL shader: sprite atlas texture sampling + tint color.\n * PointSymbol (type='icon') ile sprite atlas'tan ikon render eder.\n */\nexport declare const ICON_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct IconMaterial {\\n  tintColor: vec4<f32>,\\n  uvRect: vec4<f32>,\\n  size: f32,\\n  rotation: f32,\\n  bgRadius: f32,\\n  outlineWidth: f32,\\n  bgColor: vec4<f32>,\\n  outlineColor: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: IconMaterial;\\n@group(1) @binding(1) var iconSampler: sampler;\\n@group(1) @binding(2) var iconTexture: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  // Per-instance: point center position (x, y, z)\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n};\\n\\n// Billboard quad: 6 vertices (2 triangles) per point instance\\n@vertex\\nfn vs_main(\\n  input: VertexInput,\\n  @builtin(vertex_index) vid: u32,\\n) -> VertexOutput {\\n  // Quad corners: 2 triangles (0,1,2) and (2,1,3)\\n  var quadOffsets = array<vec2<f32>, 6>(\\n    vec2<f32>(-0.5, -0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>( 0.5,  0.5),\\n  );\\n\\n  let offset = quadOffsets[vid];\\n\\n  // Apply rotation\\n  let rad = material.rotation * 3.14159265 / 180.0;\\n  let cosR = cos(rad);\\n  let sinR = sin(rad);\\n  let rotatedOffset = vec2<f32>(\\n    offset.x * cosR - offset.y * sinR,\\n    offset.x * sinR + offset.y * cosR,\\n  );\\n\\n  // Raw UV for fragment shader (0-1 across quad)\\n  let uv = offset + vec2<f32>(0.5, 0.5);\\n\\n  // Project center to clip space\\n  let clipCenter = camera.viewProjection * vec4<f32>(input.position.xy, 0.0, 1.0);\\n\\n  // Billboard: expand quad for background circle if present\\n  var pixelSize = material.size;\\n  if (material.bgRadius > 0.0) {\\n    pixelSize = max(material.size, material.bgRadius * 2.0 + material.outlineWidth * 2.0);\\n  }\\n  let screenOffset = rotatedOffset * pixelSize;\\n  let ndcOffset = vec2<f32>(\\n    screenOffset.x * 2.0 / camera.viewport.x,\\n    screenOffset.y * 2.0 / camera.viewport.y,\\n  );\\n\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(\\n    clipCenter.x + ndcOffset.x * clipCenter.w,\\n    clipCenter.y + ndcOffset.y * clipCenter.w,\\n    clipCenter.z,\\n    clipCenter.w,\\n  );\\n  out.uv = uv;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let centered = input.uv - vec2<f32>(0.5, 0.5);\\n\\n  // \\u2500\\u2500 No background: original icon-only path \\u2500\\u2500\\n  if (material.bgRadius <= 0.0) {\\n    let atlasUV = vec2<f32>(\\n      mix(material.uvRect.x, material.uvRect.z, input.uv.x),\\n      mix(material.uvRect.y, material.uvRect.w, input.uv.y),\\n    );\\n    let texColor = textureSample(iconTexture, iconSampler, atlasUV);\\n    if (texColor.a < 0.01) { discard; }\\n    return vec4<f32>(texColor.rgb * material.tintColor.rgb, texColor.a * material.tintColor.a);\\n  }\\n\\n  // \\u2500\\u2500 Background circle mode \\u2500\\u2500\\n  let totalSize = max(material.size, material.bgRadius * 2.0 + material.outlineWidth * 2.0);\\n  let pixelDist = length(centered) * totalSize;\\n  let outerEdge = material.bgRadius + material.outlineWidth;\\n  let aa = fwidth(pixelDist);\\n\\n  // Sample icon texture unconditionally (uniform control flow)\\n  let iconLocalUV = clamp(centered * totalSize / material.size + vec2<f32>(0.5, 0.5), vec2<f32>(0.0), vec2<f32>(1.0));\\n  let atlasUV = vec2<f32>(\\n    mix(material.uvRect.x, material.uvRect.z, iconLocalUV.x),\\n    mix(material.uvRect.y, material.uvRect.w, iconLocalUV.y),\\n  );\\n  let texColor = textureSample(iconTexture, iconSampler, atlasUV);\\n\\n  // Outside everything: discard\\n  if (pixelDist > outerEdge + aa) { discard; }\\n\\n  // Outline ring (anti-aliased)\\n  if (pixelDist > material.bgRadius) {\\n    let outerAlpha = 1.0 - smoothstep(outerEdge - aa, outerEdge + aa, pixelDist);\\n    return vec4<f32>(material.outlineColor.rgb, material.outlineColor.a * outerAlpha);\\n  }\\n\\n  // Background fill\\n  var result = material.bgColor;\\n\\n  // Blend icon over background (only within icon bounds)\\n  let iconHalf = material.size * 0.5;\\n  let pixelPos = centered * totalSize;\\n  let inIcon = step(abs(pixelPos.x), iconHalf) * step(abs(pixelPos.y), iconHalf);\\n  let tinted = vec4<f32>(\\n    texColor.rgb * material.tintColor.rgb,\\n    texColor.a * material.tintColor.a * inIcon,\\n  );\\n  result = vec4<f32>(\\n    mix(result.rgb, tinted.rgb, tinted.a),\\n    result.a + tinted.a * (1.0 - result.a),\\n  );\\n\\n  return result;\\n}\\n\";\n/**\n * Icon material bind group layout (group 1).\n * binding 0: material uniform buffer\n * binding 1: sampler\n * binding 2: sprite atlas texture\n */\nexport declare function createIconBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface IconPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface IconPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Icon render pipeline oluştur.\n * Instanced rendering: her point bir instance, 6 vertex (billboard quad).\n */\nexport declare function createIconPipeline(desc: IconPipelineDescriptor): IconPipeline;\n//# sourceMappingURL=icon-pipeline.d.ts.map","pipelines/line-pipeline.d.ts":"/**\n * Line Pipeline\n *\n * Screen-space line width with miter join.\n * Supports solid, dash, dot, dash-dot patterns.\n * LineSymbol'den renk, kalınlık, stil okur.\n */\nexport declare const LINE_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct LineMaterial {\\n  color: vec4<f32>,\\n  width: f32,\\n  // Dash pattern: 0=solid, 1=dash, 2=dot, 3=dash-dot\\n  dashStyle: f32,\\n  dashAnimationSpeed: f32,\\n  time: f32,\\n  // Custom dashArray \\u2014 packed into 2 vec4s (up to 8 segments)\\n  dashSegments0: vec4<f32>,\\n  dashSegments1: vec4<f32>,\\n  // x=segment count (0=use dashStyle), y=total pattern length\\n  dashMeta: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: LineMaterial;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\n// Each line segment uses 6 vertices (2 triangles forming a screen-space quad).\\n// Vertex buffer layout: [prevX, prevY, prevZ, currX, currY, currZ, nextX, nextY, nextZ, side, cumulDist]\\n// side: -1.0 or 1.0 (which side of the line)\\n// cumulDist: cumulative Mercator arc-length from polyline start\\nstruct VertexInput {\\n  @location(0) prevPos: vec3<f32>,\\n  @location(1) currPos: vec3<f32>,\\n  @location(2) nextPos: vec3<f32>,\\n  @location(3) side: f32,\\n  @location(4) cumulDist: f32,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) lineDistance: f32,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let clipCurr = camera.viewProjection * vec4<f32>(input.currPos.xy, 0.0, 1.0);\\n  let clipPrev = camera.viewProjection * vec4<f32>(input.prevPos.xy, 0.0, 1.0);\\n  let clipNext = camera.viewProjection * vec4<f32>(input.nextPos.xy, 0.0, 1.0);\\n\\n  // Convert to screen space\\n  let screenCurr = clipCurr.xy / clipCurr.w * camera.viewport * 0.5;\\n  let screenPrev = clipPrev.xy / clipPrev.w * camera.viewport * 0.5;\\n  let screenNext = clipNext.xy / clipNext.w * camera.viewport * 0.5;\\n\\n  // Direction vectors\\n  let dirPrev = normalize(screenCurr - screenPrev);\\n  let dirNext = normalize(screenNext - screenCurr);\\n\\n  // Miter direction (average of two normals)\\n  let normalPrev = vec2<f32>(-dirPrev.y, dirPrev.x);\\n  let normalNext = vec2<f32>(-dirNext.y, dirNext.x);\\n\\n  var miter: vec2<f32>;\\n  let hasPrev = length(input.currPos - input.prevPos) > 0.0001;\\n  let hasNext = length(input.nextPos - input.currPos) > 0.0001;\\n\\n  if (hasPrev && hasNext) {\\n    miter = normalize(normalPrev + normalNext);\\n    // Miter length correction\\n    let miterLen = 1.0 / max(dot(miter, normalPrev), 0.1);\\n    miter = miter * min(miterLen, 3.0); // Cap miter to prevent spikes\\n  } else if (hasPrev) {\\n    miter = normalPrev;\\n  } else {\\n    miter = normalNext;\\n  }\\n\\n  // Offset in screen space\\n  let halfWidth = material.width * 0.5;\\n  let offset = miter * halfWidth * input.side;\\n\\n  // Back to clip space\\n  let screenPos = screenCurr + offset;\\n  let ndcPos = screenPos / (camera.viewport * 0.5);\\n\\n  // Cumulative arc-length \\u2192 screen-space via pixels-per-unit ratio\\n  let mercLen = length(input.currPos.xy - input.prevPos.xy);\\n  let screenLen = length(screenCurr - screenPrev);\\n  let ppu = select(1.0, screenLen / mercLen, mercLen > 0.0001);\\n\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(ndcPos * clipCurr.w, clipCurr.z, clipCurr.w);\\n  out.lineDistance = input.cumulDist * ppu;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\nfn getDashSegment(i: i32) -> f32 {\\n  if (i < 4) { return material.dashSegments0[i]; }\\n  return material.dashSegments1[i - 4];\\n}\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let dist = input.lineDistance + material.dashAnimationSpeed * material.time;\\n\\n  // Custom dashArray takes priority when dashMeta.x > 0\\n  let segCount = i32(material.dashMeta.x);\\n  if (segCount > 0) {\\n    let total = material.dashMeta.y;\\n    let d = ((dist % total) + total) % total; // wrap positive\\n    var cumul = 0.0;\\n    for (var i = 0; i < 8; i++) {\\n      if (i >= segCount) { break; }\\n      cumul += getDashSegment(i);\\n      if (d < cumul) {\\n        if (i % 2 == 1) { discard; } // odd index = gap\\n        break;\\n      }\\n    }\\n    return material.color;\\n  }\\n\\n  // Built-in dash patterns (screen-space units)\\n  if (material.dashStyle > 0.5 && material.dashStyle < 1.5) {\\n    // Dash: 10px on, 6px off\\n    let pattern = dist % 16.0;\\n    if (pattern > 10.0) { discard; }\\n  } else if (material.dashStyle > 1.5 && material.dashStyle < 2.5) {\\n    // Dot: 3px on, 3px off\\n    let pattern = dist % 6.0;\\n    if (pattern > 3.0) { discard; }\\n  } else if (material.dashStyle > 2.5) {\\n    // Dash-dot: 10px on, 4px off, 3px on, 4px off\\n    let pattern = dist % 21.0;\\n    if ((pattern > 10.0 && pattern < 14.0) || pattern > 17.0) { discard; }\\n  }\\n\\n  return material.color;\\n}\\n\";\n/**\n * Line material bind group layout (group 1).\n */\nexport declare function createLineBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface LinePipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface LinePipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\n/**\n * Line render pipeline oluştur.\n * Screen-space line width with miter joins and dash patterns.\n */\nexport declare function createLinePipeline(desc: LinePipelineDescriptor): LinePipeline;\n/**\n * LineSymbol.style string'ini shader dashStyle uniform değerine çevir.\n */\nexport declare function dashStyleToUniform(style: 'solid' | 'dash' | 'dot' | 'dash-dot'): number;\n//# sourceMappingURL=line-pipeline.d.ts.map","pipelines/mesh3d-pipeline.d.ts":"/**\n * Mesh3D Pipeline — 2D rendering of arbitrary 3D mesh geometry.\n *\n * Renders Box, Cylinder, Sphere, Cone as real 3D meshes with Blinn-Phong lighting.\n * Uses oblique 2.5D projection (same as extrusion pipeline) for visible height in 2D mode.\n *\n * Vertex layout: [position: vec3<f32>, normal: vec3<f32>] — stride 24 bytes.\n * Material uniform: 32 bytes — [color(16) + ambient(4) + shininess(4) + specularStrength(4) + pad(4)].\n */\nexport declare const MESH3D_SHADER_SOURCE = \"\\n\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct Mesh3DMaterial {\\n  color: vec4<f32>,         // RGBA 0-1 (premultiplied in fragment)\\n  ambient: f32,\\n  shininess: f32,\\n  specularStrength: f32,\\n  _pad: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: Mesh3DMaterial;\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n  @location(1) normal: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) vNormal: vec3<f32>,\\n  @location(1) worldPos: vec3<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var out: VertexOutput;\\n\\n  // XY comes in Mercator [0..1], convert to EPSG:3857 for 2D camera\\n  let HALF: f32 = 20037508.34;\\n  let epsg = vec2<f32>(\\n    input.position.x * 2.0 * HALF - HALF,\\n    (1.0 - input.position.y) * 2.0 * HALF - HALF\\n  );\\n\\n  // Height (metres) + oblique 2.5D offset for visible 3D effect in top-down view\\n  let h = input.position.z;\\n  let obliqueMag: f32 = 0.5;\\n  let worldPos = vec3<f32>(\\n    epsg.x + h * obliqueMag,\\n    epsg.y + h * obliqueMag,\\n    h,\\n  );\\n\\n  out.clipPosition = camera.viewProjection * vec4<f32>(worldPos, 1.0);\\n  out.worldPos = worldPos;\\n  out.vNormal = input.normal;\\n\\n  // Logarithmic depth remap \\u2014 matches extrusion pipeline.\\n  let absH = abs(h);\\n  let logH = log2(max(absH, 0.1) + 1.0);\\n  let logMax = log2(1001.0);\\n  let normalizedZ = clamp(0.5 - logH / (2.0 * logMax), 0.01, 0.99);\\n  out.clipPosition.z = normalizedZ * out.clipPosition.w;\\n\\n  return out;\\n}\\n\\n@fragment\\nfn fs_main(in: VertexOutput) -> @location(0) vec4<f32> {\\n  let N = normalize(in.vNormal);\\n  let L = normalize(vec3<f32>(0.3, 0.8, 0.5)); // fixed directional light\\n  let V = normalize(vec3<f32>(0.0, 0.0, 1.0)); // approximate view direction\\n  let H = normalize(L + V); // Blinn half-vector\\n\\n  // Diffuse\\n  let diff = max(dot(N, L), 0.0);\\n\\n  // Specular (Blinn-Phong)\\n  let spec = pow(max(dot(N, H), 0.0), material.shininess) * material.specularStrength;\\n\\n  let lighting = material.ambient + diff * (1.0 - material.ambient) + spec;\\n  let baseColor = material.color.rgb * lighting;\\n  let alpha = material.color.a;\\n\\n  // Premultiplied alpha output\\n  return vec4<f32>(baseColor * alpha, alpha);\\n}\\n\";\nexport declare function createMesh3DBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface Mesh3DPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface Mesh3DPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\nexport declare function createMesh3DPipeline(desc: Mesh3DPipelineDescriptor): Mesh3DPipeline;\n//# sourceMappingURL=mesh3d-pipeline.d.ts.map","pipelines/model-pipeline.d.ts":"/**\n * Model Pipeline — 2D instanced 3D mesh rendering\n *\n * Renders GLTF meshes at map positions with PBR shading.\n * Uses instanced rendering: mesh vertices × feature instances.\n * Supports base color, normal, metallic-roughness, occlusion, and emissive maps.\n */\nexport declare const MODEL_SHADER_SOURCE = \"\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct ModelMaterial {\\n  baseColorFactor: vec4<f32>,       // 0-15\\n  tintColor: vec4<f32>,             // 16-31\\n  emissiveFactor: vec3<f32>,        // 32-43\\n  metallic: f32,                    // 44-47\\n  roughness: f32,                   // 48-51\\n  hasBaseColorTex: f32,             // 52-55\\n  hasNormalTex: f32,                // 56-59\\n  hasMetallicRoughnessTex: f32,     // 60-63\\n  hasOcclusionTex: f32,             // 64-67\\n  hasEmissiveTex: f32,              // 68-71\\n  alphaCutoff: f32,                 // 72-75\\n  isUnlit: f32,                     // 76-79\\n};\\n\\n@group(1) @binding(0) var<uniform> material: ModelMaterial;\\n@group(1) @binding(1) var texSampler: sampler;\\n@group(1) @binding(2) var baseColorTex: texture_2d<f32>;\\n@group(1) @binding(3) var normalTex: texture_2d<f32>;\\n@group(1) @binding(4) var metallicRoughnessTex: texture_2d<f32>;\\n@group(1) @binding(5) var occlusionTex: texture_2d<f32>;\\n@group(1) @binding(6) var emissiveTex: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Vertex Input \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  // Per-vertex (slot 0)\\n  @location(0) position: vec3<f32>,\\n  @location(1) normal: vec3<f32>,\\n  @location(2) texcoord: vec2<f32>,\\n  // Per-instance (slot 1)\\n  @location(3) worldPos: vec3<f32>,\\n  @location(4) scaleHeading: vec2<f32>,   // scale, heading\\n  @location(5) pitchRollAnchor: vec3<f32>, // pitch, roll, anchorZ\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) vNormal: vec3<f32>,\\n  @location(1) vTexcoord: vec2<f32>,\\n  @location(2) vWorldPos: vec3<f32>,\\n};\\n\\n// \\u2500\\u2500\\u2500 Rotation Matrix from Euler angles (heading/pitch/roll in degrees) \\u2500\\u2500\\u2500\\n\\nfn degreesToRadians(deg: f32) -> f32 {\\n  return deg * 3.14159265358979 / 180.0;\\n}\\n\\nfn eulerToRotationMatrix(heading: f32, pitch: f32, roll: f32) -> mat3x3<f32> {\\n  let h = degreesToRadians(heading);\\n  let p = degreesToRadians(pitch);\\n  let r = degreesToRadians(roll);\\n\\n  let ch = cos(h); let sh = sin(h);\\n  let cp = cos(p); let sp = sin(p);\\n  let cr = cos(r); let sr = sin(r);\\n\\n  // ZYX rotation order: heading(Z) * pitch(Y) * roll(X)\\n  return mat3x3<f32>(\\n    vec3<f32>(ch*cp, sh*cp, -sp),\\n    vec3<f32>(ch*sp*sr - sh*cr, sh*sp*sr + ch*cr, cp*sr),\\n    vec3<f32>(ch*sp*cr + sh*sr, sh*sp*cr - ch*sr, cp*cr),\\n  );\\n}\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var output: VertexOutput;\\n\\n  let scale = input.scaleHeading.x;\\n  let heading = input.scaleHeading.y;\\n  let pitch = input.pitchRollAnchor.x;\\n  let roll = input.pitchRollAnchor.y;\\n  let anchorZ = input.pitchRollAnchor.z;\\n\\n  let rotMat = eulerToRotationMatrix(heading, pitch, roll);\\n  let rotated = rotMat * (input.position * scale);\\n\\n  // Preserve Z for 3D model structure (visible when camera has pitch)\\n  let worldPos = input.worldPos + vec3<f32>(rotated.x, rotated.y, rotated.z + anchorZ);\\n\\n  output.clipPosition = camera.viewProjection * vec4<f32>(worldPos, 1.0);\\n  // Remap depth for model self-occlusion: higher local Z = closer to top-down camera = lower clip Z\\n  let localZ = rotated.z + anchorZ;\\n  let normalizedZ = clamp(0.5 - localZ / (scale * 10.0), 0.01, 0.99);\\n  output.clipPosition.z = normalizedZ * output.clipPosition.w;\\n\\n  output.vNormal = normalize(rotMat * input.normal);\\n  output.vTexcoord = input.texcoord;\\n  output.vWorldPos = worldPos;\\n\\n  return output;\\n}\\n\\n// \\u2500\\u2500\\u2500 PBR Helpers \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979;\\n\\n// GGX/Trowbridge-Reitz Normal Distribution Function\\nfn distributionGGX(NdotH: f32, roughness: f32) -> f32 {\\n  let a = roughness * roughness;\\n  let a2 = a * a;\\n  let d = NdotH * NdotH * (a2 - 1.0) + 1.0;\\n  return a2 / (PI * d * d + 0.0001);\\n}\\n\\n// Schlick-GGX Geometry function\\nfn geometrySchlickGGX(NdotV: f32, roughness: f32) -> f32 {\\n  let r = roughness + 1.0;\\n  let k = (r * r) / 8.0;\\n  return NdotV / (NdotV * (1.0 - k) + k);\\n}\\n\\n// Smith's method for combined geometry obstruction\\nfn geometrySmith(NdotV: f32, NdotL: f32, roughness: f32) -> f32 {\\n  return geometrySchlickGGX(NdotV, roughness) * geometrySchlickGGX(NdotL, roughness);\\n}\\n\\n// Schlick Fresnel approximation\\nfn fresnelSchlick(cosTheta: f32, F0: vec3<f32>) -> vec3<f32> {\\n  return F0 + (1.0 - F0) * pow(clamp(1.0 - cosTheta, 0.0, 1.0), 5.0);\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment: PBR \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput, @builtin(front_facing) frontFacing: bool) -> @location(0) vec4<f32> {\\n  let uv = input.vTexcoord;\\n\\n  // \\u2500\\u2500 Base Color \\u2500\\u2500\\n  var baseColor = material.baseColorFactor;\\n  if (material.hasBaseColorTex > 0.5) {\\n    baseColor = baseColor * textureSample(baseColorTex, texSampler, uv);\\n  }\\n  baseColor = vec4<f32>(baseColor.rgb * material.tintColor.rgb, baseColor.a * material.tintColor.a);\\n\\n  // Alpha test (MASK mode)\\n  if (material.alphaCutoff > 0.0 && baseColor.a < material.alphaCutoff) {\\n    discard;\\n  }\\n\\n  // KHR_materials_unlit: skip all lighting\\n  if (material.isUnlit > 0.5) {\\n    return baseColor;\\n  }\\n\\n  // \\u2500\\u2500 Normal (flip for back-faces on double-sided materials) \\u2500\\u2500\\n  var N = normalize(input.vNormal);\\n  if (!frontFacing) { N = -N; }\\n  if (material.hasNormalTex > 0.5) {\\n    let tangentNormal = textureSample(normalTex, texSampler, uv).rgb * 2.0 - 1.0;\\n    // Cotangent frame from screen-space derivatives\\n    let dpdx_val = dpdx(input.vWorldPos);\\n    let dpdy_val = dpdy(input.vWorldPos);\\n    let dudx = dpdx(uv);\\n    let dvdy = dpdy(uv);\\n    let T = normalize(dpdx_val * dvdy.y - dpdy_val * dudx.y);\\n    let B = normalize(cross(N, T));\\n    let TBN = mat3x3<f32>(T, B, N);\\n    N = normalize(TBN * tangentNormal);\\n  }\\n\\n  // \\u2500\\u2500 Metallic / Roughness \\u2500\\u2500\\n  var metallic = material.metallic;\\n  var roughness = material.roughness;\\n  if (material.hasMetallicRoughnessTex > 0.5) {\\n    let mrSample = textureSample(metallicRoughnessTex, texSampler, uv);\\n    roughness = roughness * mrSample.g; // green channel = roughness\\n    metallic = metallic * mrSample.b;   // blue channel = metallic\\n  }\\n  roughness = clamp(roughness, 0.04, 1.0);\\n\\n  // \\u2500\\u2500 PBR Lighting \\u2500\\u2500\\n  let lightDir = normalize(vec3<f32>(0.5, 0.8, 0.6));\\n  let viewDir = normalize(vec3<f32>(0.0, 0.0, 1.0));\\n  let H = normalize(lightDir + viewDir);\\n\\n  let NdotL = max(dot(N, lightDir), 0.0);\\n  let NdotV = max(dot(N, viewDir), 0.001);\\n  let NdotH = max(dot(N, H), 0.0);\\n  let HdotV = max(dot(H, viewDir), 0.0);\\n\\n  // Dielectric/metallic F0\\n  let F0 = mix(vec3<f32>(0.04), baseColor.rgb, metallic);\\n\\n  // Cook-Torrance BRDF\\n  let D = distributionGGX(NdotH, roughness);\\n  let G = geometrySmith(NdotV, NdotL, roughness);\\n  let F = fresnelSchlick(HdotV, F0);\\n\\n  let specular = (D * G * F) / (4.0 * NdotV * NdotL + 0.0001);\\n  let kD = (vec3<f32>(1.0) - F) * (1.0 - metallic);\\n  let diffuse = kD * baseColor.rgb / PI;\\n\\n  let radiance = vec3<f32>(1.0); // directional light color\\n  var color = (diffuse + specular) * radiance * NdotL;\\n\\n  // Ambient\\n  color += 0.15 * baseColor.rgb;\\n\\n  // \\u2500\\u2500 Ambient Occlusion \\u2500\\u2500\\n  if (material.hasOcclusionTex > 0.5) {\\n    let ao = textureSample(occlusionTex, texSampler, uv).r;\\n    color = color * ao;\\n  }\\n\\n  // \\u2500\\u2500 Emissive \\u2500\\u2500\\n  var emissive = material.emissiveFactor;\\n  if (material.hasEmissiveTex > 0.5) {\\n    emissive = emissive * textureSample(emissiveTex, texSampler, uv).rgb;\\n  }\\n  color += emissive;\\n\\n  return vec4<f32>(color, baseColor.a);\\n}\\n\";\nexport interface ModelPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\nexport interface ModelPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport declare function createModelBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport declare function createModelPipeline(desc: ModelPipelineDescriptor): ModelPipeline;\n//# sourceMappingURL=model-pipeline.d.ts.map","pipelines/ocean-globe-mesh.d.ts":"/**\n * Ocean Globe Mesh\n *\n * Icosphere mesh for ocean water rendering on the globe.\n * Follows the atmosphere mesh pattern (octahedron → subdivide → sphere).\n *\n * Key differences from atmosphere mesh:\n * - Radius = 1.0 (sits on globe surface, not 1.15× larger)\n * - Higher subdivision (5 → 8192 triangles) for wave displacement detail\n * - CullMode is 'front' (camera outside, render inner faces)\n */\nexport interface OceanGlobeMesh {\n    vertexBuffer: GPUBuffer;\n    indexBuffer: GPUBuffer;\n    indexCount: number;\n    vertexCount: number;\n}\n/**\n * Create an icosphere mesh for ocean rendering.\n *\n * Octahedron base → subdivide → project onto unit sphere (radius=1.0).\n * Uses higher subdivision than atmosphere for smoother wave displacement.\n *\n * @param device - GPUDevice\n * @param subdivisions - Detail level (default 5 → 8192 triangles)\n */\nexport declare function createOceanGlobeMesh(device: GPUDevice, subdivisions?: number): OceanGlobeMesh;\n//# sourceMappingURL=ocean-globe-mesh.d.ts.map","pipelines/ocean-globe-pipeline.d.ts":"/**\n * Ocean Globe Pipeline\n *\n * WGSL shader + GPU pipeline for realistic ocean water on the globe.\n * Uses Gerstner wave displacement, Fresnel reflection, Blinn-Phong specular,\n * and foam highlights. Renders on the unit sphere icosphere mesh.\n *\n * Mask-based rendering: Ocean renders ON TOP of tiles with alpha blending.\n * A screen-space mask texture (rendered from GeoJSON land/water polygons)\n * controls where the ocean is visible. This works with any basemap style\n * (light, dark, satellite) — no color detection needed.\n *\n * Visibility: Fades out via projectionTransition (globeness) — invisible\n * in full 2D Mercator mode.\n */\nimport { type OceanGlobeMesh } from './ocean-globe-mesh';\nexport declare const OCEAN_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979323846;\\nconst TAU: f32 = 6.28318530717958647692;\\nconst HALF_PI: f32 = 1.57079632679489661923;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nstruct OceanUniforms {\\n  deepColor: vec4<f32>,\\n  shallowColor: vec4<f32>,\\n  specularColor: vec4<f32>,\\n  sunDirection: vec4<f32>,\\n  time: f32,\\n  waveAmplitude: f32,\\n  waveFrequency: f32,\\n  fresnelPower: f32,\\n  foamThreshold: f32,\\n  opacity: f32,\\n  maskMode: f32,\\n  _pad1: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> ocean: OceanUniforms;\\n@group(2) @binding(0) var maskTexture: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Per-pixel wave normal (fragment shader detail) \\u2500\\u2500\\u2500\\n// Computes wave normals analytically per-pixel on the sphere surface.\\n// This is the key to visible waves \\u2014 vertex displacement alone is too coarse.\\n\\nfn waveNormal(\\n  uv: vec2<f32>,\\n  waveDir: vec2<f32>,\\n  freq: f32,\\n  speed: f32,\\n  amp: f32,\\n  t: f32,\\n) -> vec2<f32> {\\n  let phase = dot(uv, waveDir) * freq + t * speed;\\n  let d = amp * freq * cos(phase);\\n  return waveDir * d;\\n}\\n\\nfn computeWaveNormals(surfaceUV: vec2<f32>, t: f32, amp: f32, freq: f32) -> vec3<f32> {\\n  // 6 wave layers at different scales for natural look\\n  var grad = vec2<f32>(0.0);\\n\\n  // Large swells\\n  grad += waveNormal(surfaceUV, vec2<f32>(0.8, 0.6),   freq * 1.0,  0.8,  amp * 1.0,   t);\\n  grad += waveNormal(surfaceUV, vec2<f32>(-0.6, 0.8),  freq * 1.2,  0.6,  amp * 0.8,   t);\\n  // Medium waves\\n  grad += waveNormal(surfaceUV, vec2<f32>(0.3, -0.95),  freq * 2.3,  1.2,  amp * 0.5,   t);\\n  grad += waveNormal(surfaceUV, vec2<f32>(-0.9, -0.4), freq * 3.1,  1.5,  amp * 0.35,  t);\\n  // Small ripples\\n  grad += waveNormal(surfaceUV, vec2<f32>(0.95, 0.3),  freq * 5.7,  2.0,  amp * 0.15,  t);\\n  grad += waveNormal(surfaceUV, vec2<f32>(-0.4, 0.9),  freq * 7.3,  2.5,  amp * 0.08,  t);\\n\\n  return normalize(vec3<f32>(-grad.x, 1.0, -grad.y));\\n}\\n\\n// Foam from wave crest detection\\nfn computeFoam(surfaceUV: vec2<f32>, t: f32, amp: f32, freq: f32) -> f32 {\\n  let p1 = dot(surfaceUV, vec2<f32>(0.8, 0.6)) * freq + t * 0.8;\\n  let p2 = dot(surfaceUV, vec2<f32>(-0.6, 0.8)) * freq * 1.2 + t * 0.6;\\n  let crest = sin(p1) * 0.6 + sin(p2) * 0.4;\\n  return max(0.0, crest);\\n}\\n\\n// \\u2500\\u2500\\u2500 Sphere \\u2192 Mercator helpers \\u2500\\u2500\\u2500\\n\\nfn sphereToMerc01(sd: vec3<f32>) -> vec2<f32> {\\n  let lon = atan2(sd.x, sd.z);                   // [-PI, PI]\\n  let lat = asin(clamp(sd.y, -1.0, 1.0));        // [-PI/2, PI/2]\\n  let mx = (lon + PI) / TAU;                      // [0, 1]\\n  let my = (1.0 - log(tan(HALF_PI * 0.5 + lat * 0.5)) / PI) * 0.5;\\n  return vec2<f32>(mx, clamp(my, 0.0, 1.0));\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) worldPos: vec3<f32>,\\n  @location(1) clipDot: f32,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let sphereDir = normalize(input.position);\\n\\n  // Vertex displacement (Gerstner-style, visible at close zoom silhouettes)\\n  let upRef = select(vec3<f32>(0.0, 1.0, 0.0), vec3<f32>(1.0, 0.0, 0.0), abs(sphereDir.y) > 0.99);\\n  let tX = normalize(cross(upRef, sphereDir));\\n  let tZ = normalize(cross(sphereDir, tX));\\n  let vuv = vec2<f32>(dot(sphereDir, tX), dot(sphereDir, tZ));\\n\\n  let p0 = sin(dot(vuv, vec2<f32>(0.8, 0.6))  * ocean.waveFrequency       + ocean.time * 0.8);\\n  let p1 = sin(dot(vuv, vec2<f32>(-0.6, 0.8)) * ocean.waveFrequency * 1.3 + ocean.time * 0.6) * 0.7;\\n  let vertDisp = (p0 + p1) * ocean.waveAmplitude;\\n\\n  let displaced = sphereDir * (1.0 + vertDisp);\\n  let clipDot = dot(sphereDir, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  // \\u2500\\u2500 Dual projection: globe \\u2194 flat blend \\u2500\\u2500\\n  var globeClip = camera.viewProjection * vec4<f32>(displaced, 1.0);\\n  // Custom depth for horizon occlusion (same as globe-polygon-pipeline)\\n  let baseClipZ = 1.0 - (dot(sphereDir, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n  globeClip.z = baseClipZ * globeClip.w;\\n\\n  var clipPos: vec4<f32>;\\n  if (camera.projectionTransition >= 0.999) {\\n    clipPos = globeClip;\\n  } else if (camera.projectionTransition <= 0.001) {\\n    let merc01 = sphereToMerc01(sphereDir);\\n    clipPos = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, vertDisp, 1.0);\\n  } else {\\n    let merc01 = sphereToMerc01(sphereDir);\\n    let flatClip = camera.flatViewProjection * vec4<f32>(merc01.x, merc01.y, vertDisp, 1.0);\\n    clipPos = mix(flatClip, globeClip, camera.projectionTransition);\\n  }\\n\\n  var out: VertexOutput;\\n  out.clipPosition = clipPos;\\n  out.worldPos = displaced;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // Horizon culling\\n  if (input.clipDot < -0.01) {\\n    discard;\\n  }\\n\\n  // \\u2500\\u2500 Mask check \\u2500\\u2500\\n  if (ocean.maskMode > 0.5) {\\n    let screenUV = input.clipPosition.xy;\\n    let maskDims = vec2<f32>(textureDimensions(maskTexture, 0));\\n    let maskCoord = vec2<i32>(clamp(screenUV, vec2<f32>(0.0), maskDims - 1.0));\\n    let maskVal = textureLoad(maskTexture, maskCoord, 0).r;\\n\\n    if (ocean.maskMode < 1.5) {\\n      if (maskVal > 0.5) { discard; }\\n    } else {\\n      if (maskVal < 0.5) { discard; }\\n    }\\n  }\\n\\n  let sphereDir = normalize(input.worldPos);\\n\\n  // \\u2500\\u2500 Per-pixel tangent frame \\u2500\\u2500\\n  // Stable tangent basis (handle pole singularity)\\n  let upRef = select(vec3<f32>(0.0, 1.0, 0.0), vec3<f32>(1.0, 0.0, 0.0), abs(sphereDir.y) > 0.99);\\n  let tangentX = normalize(cross(upRef, sphereDir));\\n  let tangentZ = normalize(cross(sphereDir, tangentX));\\n\\n  // Surface UV for wave computation (angular coordinates on sphere)\\n  let surfaceUV = vec2<f32>(\\n    atan2(sphereDir.x, sphereDir.z) / PI,       // longitude [-1, 1]\\n    asin(clamp(sphereDir.y, -1.0, 1.0)) / PI    // latitude [-0.5, 0.5]\\n  ) * ocean.waveFrequency;\\n\\n  // \\u2500\\u2500 Per-pixel wave normal (the key to visible waves) \\u2500\\u2500\\n  let waveN = computeWaveNormals(surfaceUV, ocean.time, ocean.waveAmplitude * 80.0, 1.0);\\n\\n  // Transform wave normal from tangent space to world space\\n  let N = normalize(\\n    tangentX * waveN.x +\\n    sphereDir * waveN.y +\\n    tangentZ * waveN.z\\n  );\\n\\n  // View direction\\n  let V = normalize(-input.worldPos);\\n\\n  // Fresnel\\n  let R0: f32 = 0.02;\\n  let NdotV = max(dot(N, V), 0.0);\\n  let fresnel = R0 + (1.0 - R0) * pow(1.0 - NdotV, ocean.fresnelPower);\\n\\n  // Sun specular (Blinn-Phong)\\n  let L = normalize(ocean.sunDirection.xyz);\\n  let H = normalize(L + V);\\n  let NdotH = max(dot(N, H), 0.0);\\n  let specular = pow(NdotH, 256.0) * fresnel * 1.5;\\n\\n  // Diffuse color blend (deep \\u2194 shallow based on facing angle)\\n  let facing = max(dot(N, V), 0.0);\\n  let waterColor = mix(ocean.deepColor.rgb, ocean.shallowColor.rgb, facing * facing);\\n\\n  // Subtle subsurface scattering approximation\\n  let NdotL = max(dot(sphereDir, L), 0.0);\\n  let sss = pow(NdotL, 3.0) * 0.15 * ocean.shallowColor.rgb;\\n\\n  // Foam\\n  let foamVal = computeFoam(surfaceUV, ocean.time, ocean.waveAmplitude * 80.0, 1.0);\\n  var foam = vec3<f32>(0.0);\\n  if (foamVal > ocean.foamThreshold) {\\n    let foamIntensity = smoothstep(ocean.foamThreshold, 1.0, foamVal) * 0.4;\\n    foam = vec3<f32>(foamIntensity);\\n  }\\n\\n  // Combine\\n  var color = waterColor + sss + ocean.specularColor.rgb * specular + foam;\\n\\n  // Horizon fade\\n  let horizonFade = smoothstep(-0.01, 0.1, input.clipDot);\\n\\n  // Ocean visible at ALL projection states \\u2014 no globeness fade\\n  let globenessFade: f32 = 1.0;\\n\\n  let alpha = ocean.opacity * horizonFade * globenessFade;\\n\\n  if (alpha < 0.002) {\\n    discard;\\n  }\\n\\n  return vec4<f32>(color, alpha);\\n}\\n\";\nexport declare function createOceanBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport declare function createOceanMaskBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface OceanGlobePipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n    subdivisions?: number;\n}\nexport interface OceanGlobePipeline {\n    pipeline: GPURenderPipeline;\n    oceanBindGroupLayout: GPUBindGroupLayout;\n    maskBindGroupLayout: GPUBindGroupLayout;\n    mesh: OceanGlobeMesh;\n}\n/**\n * Ocean globe pipeline — Gerstner wave displacement + Fresnel + specular.\n *\n * Key config:\n * - cullMode: 'front' — camera outside sphere, render inner faces\n * - depthWriteEnabled: false — ocean draws ON TOP of tiles (overlay)\n * - depthCompare: 'always' — ignores depth (mask controls visibility)\n * - blend: src-alpha for transparency + globeness fade\n */\nexport declare function createOceanGlobePipeline(desc: OceanGlobePipelineDescriptor): OceanGlobePipeline;\n//# sourceMappingURL=ocean-globe-pipeline.d.ts.map","pipelines/ocean-mask-pipeline.d.ts":"/**\n * Ocean Mask Pipeline\n *\n * Renders GeoJSON land/water polygon geometry into a screen-sized R32Float\n * mask texture. The ocean pipeline samples this texture to control where\n * water is visible.\n *\n * Polygons are projected from EPSG:3857 to the globe (same as globe-polygon-pipeline)\n * and rasterized as solid white (1.0) onto a black (0.0) background.\n *\n * This pipeline runs in a separate render pass (no depth, no MSAA)\n * before the main render pass each frame.\n */\nexport interface OceanMaskPipeline {\n    pipeline: GPURenderPipeline;\n}\nexport declare function createOceanMaskPipeline(device: GPUDevice, globeCameraBindGroupLayout: GPUBindGroupLayout): OceanMaskPipeline;\n//# sourceMappingURL=ocean-mask-pipeline.d.ts.map","pipelines/picking-pipeline.d.ts":"/**\n * Picking Pipeline\n *\n * Offscreen render pass: her feature'a unique ID rengi atar.\n * Feature ID -> RGBA encoding (24-bit ID + 8-bit layer index).\n * readback: mapAsync ile piksel okuma.\n */\nexport declare const PICKING_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct PickingUniforms {\\n  // Feature ID encoded as color: R = id & 0xFF, G = (id >> 8) & 0xFF, B = (id >> 16) & 0xFF\\n  // A = layer index\\n  featureColor: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> picking: PickingUniforms;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var out: VertexOutput;\\n  out.clipPosition = camera.viewProjection * vec4<f32>(input.position, 1.0);\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  return picking.featureColor;\\n}\\n\";\n/**\n * Picking uniform bind group layout (group 1).\n */\nexport declare function createPickingBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface PickingPipelineDescriptor {\n    device: GPUDevice;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n}\nexport interface PickingPipeline {\n    pipeline: GPURenderPipeline;\n    pickingBindGroupLayout: GPUBindGroupLayout;\n    pickingTexture: GPUTexture;\n    depthTexture: GPUTexture;\n    readbackBuffer: GPUBuffer;\n    width: number;\n    height: number;\n}\n/**\n * Picking render pipeline oluştur.\n * Offscreen rgba8unorm texture'a render eder.\n */\nexport declare function createPickingPipeline(desc: PickingPipelineDescriptor & {\n    width: number;\n    height: number;\n}): PickingPipeline;\n/**\n * Feature ID ve layer index'i RGBA float renk değerlerine encode et.\n * 24-bit feature ID + 8-bit layer index.\n *\n * @param featureId 0..16777215 (24-bit)\n * @param layerIndex 0..255 (8-bit)\n * @returns [r, g, b, a] normalized 0..1\n */\nexport declare function encodePickingId(featureId: number, layerIndex: number): [number, number, number, number];\n/**\n * RGBA uint8 piksel değerlerinden feature ID ve layer index'i decode et.\n *\n * @param r 0..255\n * @param g 0..255\n * @param b 0..255\n * @param a 0..255\n * @returns { featureId, layerIndex } veya null (boş piksel)\n */\nexport declare function decodePickingId(r: number, g: number, b: number, a: number): {\n    featureId: number;\n    layerIndex: number;\n} | null;\n/**\n * Picking pass sonrası piksel oku.\n * mapAsync ile asenkron GPU readback yapar.\n *\n * @param device GPUDevice\n * @param pickingPipeline Picking pipeline state\n * @param x Ekran X koordinatı\n * @param y Ekran Y koordinatı\n * @returns Decoded feature ID veya null\n */\nexport declare function readPickingPixel(device: GPUDevice, pickingPipeline: PickingPipeline, x: number, y: number): Promise<{\n    featureId: number;\n    layerIndex: number;\n} | null>;\n//# sourceMappingURL=picking-pipeline.d.ts.map","pipelines/point-pipeline.d.ts":"/**\n * Point Pipeline\n *\n * Billboard quad per point, instanced rendering.\n * WGSL shader: circle/square SDF in fragment shader.\n * PointSymbol'den renk, boyut, outline okur.\n */\nexport declare const POINT_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct PointMaterial {\\n  color: vec4<f32>,\\n  outlineColor: vec4<f32>,\\n  size: f32,\\n  outlineWidth: f32,\\n  // 0 = circle, 1 = square\\n  shape: f32,\\n  // 0 = solid (normal), >0 = soft radial glow falloff\\n  glowFalloff: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: PointMaterial;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  // Per-instance: point center position (x, y, z)\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n};\\n\\n// Billboard quad: 6 vertices (2 triangles) per point instance\\n@vertex\\nfn vs_main(\\n  input: VertexInput,\\n  @builtin(vertex_index) vid: u32,\\n) -> VertexOutput {\\n  // Quad corners: 2 triangles (0,1,2) and (2,1,3)\\n  var quadOffsets = array<vec2<f32>, 6>(\\n    vec2<f32>(-0.5, -0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>(-0.5,  0.5),\\n    vec2<f32>( 0.5, -0.5),\\n    vec2<f32>( 0.5,  0.5),\\n  );\\n\\n  let offset = quadOffsets[vid];\\n  let uv = offset + vec2<f32>(0.5, 0.5);\\n\\n  // Project center to clip space\\n  let clipCenter = camera.viewProjection * vec4<f32>(input.position.xy, 0.0, 1.0);\\n\\n  // Billboard: offset in screen space then back to clip\\n  let pixelSize = material.size + material.outlineWidth * 2.0;\\n  let screenOffset = offset * pixelSize;\\n  let ndcOffset = vec2<f32>(\\n    screenOffset.x * 2.0 / camera.viewport.x,\\n    screenOffset.y * 2.0 / camera.viewport.y,\\n  );\\n\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(\\n    clipCenter.x + ndcOffset.x * clipCenter.w,\\n    clipCenter.y + ndcOffset.y * clipCenter.w,\\n    clipCenter.z,\\n    clipCenter.w,\\n  );\\n  out.uv = uv;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let centered = input.uv - vec2<f32>(0.5, 0.5);\\n  let totalRadius = 0.5;\\n  let dist = length(centered);\\n\\n  // \\u2500\\u2500 Soft glow mode: radial gradient falloff \\u2500\\u2500\\n  if (material.glowFalloff > 0.0) {\\n    if (dist > totalRadius) {\\n      discard;\\n    }\\n    // Quadratic falloff from center to edge \\u2192 soft halo\\n    let t = dist / totalRadius;\\n    let alpha = (1.0 - t * t) * material.color.a;\\n    return vec4<f32>(material.color.rgb, alpha);\\n  }\\n\\n  // \\u2500\\u2500 Normal mode: solid circle/square with outline \\u2500\\u2500\\n  let outlineFraction = material.outlineWidth / (material.size + material.outlineWidth * 2.0);\\n  let innerRadius = totalRadius - outlineFraction;\\n\\n  // Compute derivatives in uniform control flow (before any discard/branch)\\n  let aa = fwidth(dist);\\n  let squareDist = max(abs(centered.x), abs(centered.y));\\n\\n  if (material.shape < 0.5) {\\n    // Circle SDF\\n    if (dist > totalRadius) {\\n      discard;\\n    }\\n    // Anti-alias edge with smooth transition\\n    let alpha = 1.0 - smoothstep(innerRadius - aa, innerRadius, dist);\\n    return mix(material.outlineColor, material.color, alpha);\\n  } else {\\n    // Square SDF\\n    if (squareDist > totalRadius) {\\n      discard;\\n    }\\n    if (squareDist > innerRadius) {\\n      return material.outlineColor;\\n    }\\n    return material.color;\\n  }\\n}\\n\";\n/**\n * Point material bind group layout (group 1).\n */\nexport declare function createPointBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface PointPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface PointPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\n/**\n * Point render pipeline oluştur.\n * Instanced rendering: her point bir instance, 6 vertex (billboard quad).\n */\nexport declare function createPointPipeline(desc: PointPipelineDescriptor): PointPipeline;\n//# sourceMappingURL=point-pipeline.d.ts.map","pipelines/pole-cap-pipeline.d.ts":"/**\n * Pole Cap Pipeline\n *\n * Mercator tile coverage ±85.05° ile sınırlıdır — kutuplarda dairesel delikler kalır.\n * Bu pipeline, her iki kutbu basit renkli triangle-fan geometrisiyle kapatır.\n *\n * Mesh: center vertex (pole noktası) + ring vertices (85.05° enleminde)\n * Shader: sphere position → viewProjection → clip space, solid color output.\n * Horizon occlusion: aynı globeClippingZ + clipDot mekanizması.\n */\nexport declare const POLE_CAP_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Constants \\u2500\\u2500\\u2500\\n\\nconst PI: f32 = 3.14159265358979323846;\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\\n\\nstruct PoleCapUniforms {\\n  color: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> poleCap: PoleCapUniforms;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) clipDot: f32,\\n};\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var globeClip = camera.viewProjection * vec4<f32>(input.position, 1.0);\\n  globeClip.z = globeClippingZ(input.position) * globeClip.w;\\n\\n  let clipDot = dot(input.position, camera.clippingPlane.xyz) + camera.clippingPlane.w;\\n\\n  var out: VertexOutput;\\n  out.position = globeClip;\\n  out.clipDot = clipDot;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  if (input.clipDot < -0.01) {\\n    discard;\\n  }\\n  return poleCap.color;\\n}\\n\";\nexport interface PoleCapMesh {\n    vertexBuffer: GPUBuffer;\n    indexBuffer: GPUBuffer;\n    indexCount: number;\n    vertexCount: number;\n}\n/**\n * Kutup kapağı mesh'i oluştur — iki triangle fan (kuzey + güney).\n *\n * Her cap: 1 center vertex + `segments` ring vertices = segments + 1 vertex\n * Toplam: 2 × (segments + 1) vertex, 2 × segments × 3 index\n *\n * @param device - GPUDevice\n * @param segments - Fan dilim sayısı (default 64)\n */\nexport declare function createPoleCapMesh(device: GPUDevice, segments?: number): PoleCapMesh;\n/**\n * Pole cap bind group layout (group 1) — sadece color uniform.\n */\nexport declare function createPoleCapBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface PoleCapPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    /** Globe camera bind group layout (group 0) — shared with globe-raster pipeline */\n    globeCameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    segments?: number;\n    sampleCount?: number;\n}\nexport interface PoleCapPipeline {\n    pipeline: GPURenderPipeline;\n    poleCapBindGroupLayout: GPUBindGroupLayout;\n    mesh: PoleCapMesh;\n}\n/**\n * Pole cap pipeline oluştur.\n * Globe camera bind group layout'u globe-raster pipeline ile paylaşır.\n */\nexport declare function createPoleCapPipeline(desc: PoleCapPipelineDescriptor): PoleCapPipeline;\n//# sourceMappingURL=pole-cap-pipeline.d.ts.map","pipelines/polygon-pipeline.d.ts":"/**\n * Polygon Pipeline\n *\n * Triangulated mesh rendering for polygon fill.\n * PolygonSymbol'den dolgu rengi, outline okur.\n * Outline pass line pipeline'ı kullanılarak ayrıca çizilir.\n */\nexport declare const POLYGON_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct PolygonMaterial {\\n  color: vec4<f32>,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: PolygonMaterial;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) position: vec3<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  var out: VertexOutput;\\n  out.clipPosition = camera.viewProjection * vec4<f32>(input.position, 1.0);\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  return material.color;\\n}\\n\";\n/**\n * Polygon material bind group layout (group 1).\n */\nexport declare function createPolygonBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface PolygonPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface PolygonPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n}\n/**\n * Polygon fill render pipeline oluştur.\n * Triangulated mesh rendering. Outline ayrı pass'ta line pipeline ile çizilir.\n */\nexport declare function createPolygonPipeline(desc: PolygonPipelineDescriptor): PolygonPipeline;\n//# sourceMappingURL=polygon-pipeline.d.ts.map","pipelines/post-process-pipeline.d.ts":"/**\n * Post-Process Pipeline\n *\n * FXAA (Fast Approximate Anti-Aliasing) post-process pipeline.\n * Full-screen quad, luma-based edge detection.\n * Scene texture'i okur, anti-aliased sonucu yazar.\n */\nexport declare const POST_PROCESS_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct PostProcessUniforms {\\n  // Reciprocal of screen size (1/width, 1/height)\\n  rcpScreenSize: vec2<f32>,\\n  // FXAA quality: subpixel aliasing removal (0.0 = off, 1.0 = full)\\n  fxaaQuality: f32,\\n  _pad: f32,\\n};\\n\\n@group(0) @binding(0) var<uniform> params: PostProcessUniforms;\\n@group(0) @binding(1) var sceneSampler: sampler;\\n@group(0) @binding(2) var sceneTexture: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n};\\n\\n// Full-screen quad: 4 vertices, triangle-strip\\n@vertex\\nfn vs_main(@builtin(vertex_index) vid: u32) -> VertexOutput {\\n  var positions = array<vec2<f32>, 4>(\\n    vec2<f32>(-1.0, -1.0),\\n    vec2<f32>( 1.0, -1.0),\\n    vec2<f32>(-1.0,  1.0),\\n    vec2<f32>( 1.0,  1.0),\\n  );\\n\\n  let pos = positions[vid];\\n  let uv = pos * 0.5 + vec2<f32>(0.5, 0.5);\\n\\n  var out: VertexOutput;\\n  out.position = vec4<f32>(pos, 0.0, 1.0);\\n  // Flip Y for texture sampling (UV origin = top-left)\\n  out.uv = vec2<f32>(uv.x, 1.0 - uv.y);\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 FXAA Fragment \\u2500\\u2500\\u2500\\n\\n// Luma hesapla (Rec. 709)\\nfn luminance(color: vec3<f32>) -> f32 {\\n  return dot(color, vec3<f32>(0.299, 0.587, 0.114));\\n}\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let uv = input.uv;\\n  let rcpFrame = params.rcpScreenSize;\\n\\n  // Center pixel\\n  let colorM = textureSample(sceneTexture, sceneSampler, uv);\\n  let lumaM = luminance(colorM.rgb);\\n\\n  // Neighbor luma samples (NSWE)\\n  let lumaN = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>(0.0, -rcpFrame.y)).rgb);\\n  let lumaS = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>(0.0,  rcpFrame.y)).rgb);\\n  let lumaW = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>(-rcpFrame.x, 0.0)).rgb);\\n  let lumaE = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>( rcpFrame.x, 0.0)).rgb);\\n\\n  // Edge detection: max contrast\\n  let lumaMin = min(lumaM, min(min(lumaN, lumaS), min(lumaW, lumaE)));\\n  let lumaMax = max(lumaM, max(max(lumaN, lumaS), max(lumaW, lumaE)));\\n  let lumaRange = lumaMax - lumaMin;\\n\\n  // Skip low contrast areas\\n  let edgeThreshold = 0.0625;\\n  let edgeThresholdMin = 0.0312;\\n  if (lumaRange < max(edgeThresholdMin, lumaMax * edgeThreshold)) {\\n    return colorM;\\n  }\\n\\n  // Corner samples for better edge direction estimation\\n  let lumaNW = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>(-rcpFrame.x, -rcpFrame.y)).rgb);\\n  let lumaNE = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>( rcpFrame.x, -rcpFrame.y)).rgb);\\n  let lumaSW = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>(-rcpFrame.x,  rcpFrame.y)).rgb);\\n  let lumaSE = luminance(textureSample(sceneTexture, sceneSampler, uv + vec2<f32>( rcpFrame.x,  rcpFrame.y)).rgb);\\n\\n  // Subpixel aliasing test\\n  let lumaAvg = (lumaN + lumaS + lumaW + lumaE) * 0.25;\\n  let subpixelBlend = clamp(abs(lumaAvg - lumaM) / lumaRange, 0.0, 1.0);\\n  let subpixelAmount = smoothstep(0.0, 1.0, subpixelBlend) * smoothstep(0.0, 1.0, subpixelBlend) * params.fxaaQuality;\\n\\n  // Determine edge direction (horizontal vs vertical)\\n  let edgeH = abs(lumaN + lumaS - 2.0 * lumaM) * 2.0 +\\n              abs(lumaNE + lumaSE - 2.0 * lumaE) +\\n              abs(lumaNW + lumaSW - 2.0 * lumaW);\\n  let edgeV = abs(lumaE + lumaW - 2.0 * lumaM) * 2.0 +\\n              abs(lumaNE + lumaNW - 2.0 * lumaN) +\\n              abs(lumaSE + lumaSW - 2.0 * lumaS);\\n  let isHorizontal = edgeH >= edgeV;\\n\\n  // Blend direction\\n  var blendDir: vec2<f32>;\\n  if (isHorizontal) {\\n    let gradN = abs(lumaN - lumaM);\\n    let gradS = abs(lumaS - lumaM);\\n    if (gradN >= gradS) {\\n      blendDir = vec2<f32>(0.0, -rcpFrame.y);\\n    } else {\\n      blendDir = vec2<f32>(0.0, rcpFrame.y);\\n    }\\n  } else {\\n    let gradW = abs(lumaW - lumaM);\\n    let gradE = abs(lumaE - lumaM);\\n    if (gradW >= gradE) {\\n      blendDir = vec2<f32>(-rcpFrame.x, 0.0);\\n    } else {\\n      blendDir = vec2<f32>(rcpFrame.x, 0.0);\\n    }\\n  }\\n\\n  // Simple 2-tap blend along edge\\n  let blendedColor = textureSample(sceneTexture, sceneSampler, uv + blendDir * 0.5);\\n\\n  // Mix with subpixel amount\\n  return mix(colorM, blendedColor, subpixelAmount);\\n}\\n\";\n/**\n * Post-process bind group layout (group 0).\n */\nexport declare function createPostProcessBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface PostProcessPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    sampleCount?: number;\n}\nexport interface PostProcessPipeline {\n    pipeline: GPURenderPipeline;\n    bindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Post-process (FXAA) render pipeline olustur.\n */\nexport declare function createPostProcessPipeline(desc: PostProcessPipelineDescriptor): PostProcessPipeline;\n//# sourceMappingURL=post-process-pipeline.d.ts.map","pipelines/raster-pipeline.d.ts":"/**\n * Raster Imagery Pipeline\n *\n * Basit bir raster tile çizim pipeline'ı.\n * Vertex shader: quad (4 vertex) → tile extent'e map\n * Fragment shader: texture sample * opacity\n */\nexport declare const RASTER_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n};\\n\\nstruct TileUniforms {\\n  // Tile extent: minX, minY, maxX, maxY\\n  extent: vec4<f32>,\\n  // Opacity (0..1)\\n  opacity: f32,\\n  // Post-process filters (default 1.0 = no change)\\n  brightness: f32,\\n  contrast: f32,\\n  saturate: f32,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n@group(1) @binding(0) var<uniform> tile: TileUniforms;\\n@group(1) @binding(1) var tileSampler: sampler;\\n@group(1) @binding(2) var tileTexture: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n};\\n\\n// Full-screen quad: 4 vertex, 2 triangle (triangle-strip)\\n// vertex_index 0..3 \\u2192 quad corners\\n@vertex\\nfn vs_main(@builtin(vertex_index) vid: u32) -> VertexOutput {\\n  // Quad corners: BL, BR, TL, TR\\n  var positions = array<vec2<f32>, 4>(\\n    vec2<f32>(0.0, 0.0),\\n    vec2<f32>(1.0, 0.0),\\n    vec2<f32>(0.0, 1.0),\\n    vec2<f32>(1.0, 1.0),\\n  );\\n\\n  let uv = positions[vid];\\n\\n  // RTE (Relative-to-Eye): tile extent is camera-relative (CPU subtracted center\\n  // in f64).  Using w=0 treats the position as a direction vector so that\\n  // viewProjection applies rotation + scale but NOT the camera translation\\n  // (which is already baked into the relative coordinates).  Adding (0,0,0,1)\\n  // restores the homogeneous point.  This keeps full f32 precision at any zoom.\\n  let relX = mix(tile.extent.x, tile.extent.z, uv.x);\\n  let relY = mix(tile.extent.y, tile.extent.w, uv.y);\\n  let clipOffset = camera.viewProjection * vec4<f32>(relX, relY, 0.0, 0.0);\\n\\n  var out: VertexOutput;\\n  out.position = clipOffset + vec4<f32>(0.0, 0.0, 0.0, 1.0);\\n  // Flip UV Y: texture (0,0) = top-left (north), but world minY = south\\n  out.uv = vec2<f32>(uv.x, 1.0 - uv.y);\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  var color = textureSample(tileTexture, tileSampler, input.uv);\\n  // Brightness\\n  var rgb = color.rgb * tile.brightness;\\n  // Contrast: (c - 0.5) * contrast + 0.5\\n  rgb = (rgb - 0.5) * tile.contrast + 0.5;\\n  // Saturation: mix grayscale \\u2194 color\\n  let gray = dot(rgb, vec3<f32>(0.2126, 0.7152, 0.0722));\\n  rgb = mix(vec3<f32>(gray), rgb, tile.saturate);\\n  return vec4<f32>(rgb, color.a * tile.opacity);\\n}\\n\";\n/**\n * Camera bind group layout (group 0): shared across all pipelines.\n */\nexport declare function createCameraBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\n/**\n * Raster tile bind group layout (group 1).\n */\nexport declare function createRasterBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface RasterPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    sampleCount?: number;\n}\nexport interface RasterPipeline {\n    pipeline: GPURenderPipeline;\n    rasterBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Raster imagery render pipeline oluştur.\n */\nexport declare function createRasterPipeline(desc: RasterPipelineDescriptor): RasterPipeline;\n//# sourceMappingURL=raster-pipeline.d.ts.map","pipelines/subdivision-mesh.d.ts":"/**\n * Subdivision Mesh — Shared grid for globe tile rendering\n *\n * Her tile aynı subdivided quad mesh'i paylaşır.\n * Grid boyutu (subdivisions × subdivisions) → vertex shader'da\n * tile UV → Mercator → Angular → Sphere dönüşümü yapılır.\n *\n * Default: 64×64 = 4,225 vertex, 2×64×64 = 8,192 triangle.\n * Vertex layout: vec2<f32> position (UV: 0..1)\n */\nexport interface SubdivisionMesh {\n    /** Vertex buffer: vec2<f32> positions, UV (0..1) */\n    vertexBuffer: GPUBuffer;\n    /** Index buffer: uint16 indices */\n    indexBuffer: GPUBuffer;\n    /** Number of indices to draw */\n    indexCount: number;\n    /** Number of vertices */\n    vertexCount: number;\n    /** Grid subdivisions per side */\n    subdivisions: number;\n}\n/**\n * Shared subdivision grid mesh oluştur.\n * Tüm globe tile'lar bu mesh'i paylaşır — per-tile uniform ile farklı pozisyonlara map edilir.\n *\n * @param device - GPUDevice\n * @param subdivisions - Grid bölümleri (default 32)\n */\nexport declare function createSubdivisionMesh(device: GPUDevice, subdivisions?: number): SubdivisionMesh;\n//# sourceMappingURL=subdivision-mesh.d.ts.map","pipelines/terrain-3d-pipeline.d.ts":"/**\n * Terrain 3D Pipeline\n *\n * WGSL shader for 3D DTED elevation rendering with vertex displacement.\n * 128×128 grid mesh + GPU vertex displacement from r32float elevation texture.\n *\n * Key differences from 2D terrain-pipeline:\n * - Vertex buffer input (grid mesh) instead of 4-vertex quad\n * - Vertex shader reads elevation texture → Z displacement\n * - Fragment shader computes surface normals via finite differences\n * - Directional lighting (Lambertian)\n * - depth write enabled, back-face culling\n * - Skirt geometry support (isSkirt vertex flag)\n *\n * Bind Groups:\n *   group(0): CameraUniforms (shared, from raster-pipeline)\n *   group(1): TerrainTile3DUniforms + sampler + elevation(r32float) + hillshade(r8unorm) + colorRamp(rgba8unorm 256×1)\n */\nexport declare const TERRAIN_3D_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n};\\n\\nstruct TerrainTile3DUniforms {\\n  // Tile extent: minX, minY, maxX, maxY (EPSG:3857)\\n  extent: vec4<f32>,\\n  // opacity, minElev, maxElev, hillshadeStrength\\n  opacity: f32,\\n  minElev: f32,\\n  maxElev: f32,\\n  hillshadeStrength: f32,\\n  // exaggeration, elevTexWidth, elevTexHeight, skirtDrop\\n  exaggeration: f32,\\n  elevTexWidth: f32,\\n  elevTexHeight: f32,\\n  skirtDrop: f32,\\n  // sunDirection (normalized)\\n  sunDirection: vec3<f32>,\\n  padding0: f32,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n@group(1) @binding(0) var<uniform> tile: TerrainTile3DUniforms;\\n@group(1) @binding(1) var terrainSampler: sampler;\\n@group(1) @binding(2) var elevationTex: texture_2d<f32>;\\n@group(1) @binding(3) var hillshadeTex: texture_2d<f32>;\\n@group(1) @binding(4) var colorRampTex: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Bilinear Sampling Helper \\u2500\\u2500\\u2500\\n\\nfn bilinearSample(uv: vec2<f32>, texSize: vec2<f32>) -> f32 {\\n  // Manual bilinear interpolation for r32float (unfilterable)\\n  let texCoord = uv * texSize - vec2<f32>(0.5);\\n  let x0 = i32(floor(texCoord.x));\\n  let y0 = i32(floor(texCoord.y));\\n  let x1 = x0 + 1;\\n  let y1 = y0 + 1;\\n  let fx = fract(texCoord.x);\\n  let fy = fract(texCoord.y);\\n\\n  let w = i32(texSize.x);\\n  let h = i32(texSize.y);\\n  let cx0 = clamp(x0, 0, w - 1);\\n  let cx1 = clamp(x1, 0, w - 1);\\n  let cy0 = clamp(y0, 0, h - 1);\\n  let cy1 = clamp(y1, 0, h - 1);\\n\\n  let v00 = textureLoad(elevationTex, vec2<i32>(cx0, cy0), 0).r;\\n  let v10 = textureLoad(elevationTex, vec2<i32>(cx1, cy0), 0).r;\\n  let v01 = textureLoad(elevationTex, vec2<i32>(cx0, cy1), 0).r;\\n  let v11 = textureLoad(elevationTex, vec2<i32>(cx1, cy1), 0).r;\\n\\n  // Handle NaN (NODATA)\\n  let s00 = select(0.0, v00, v00 == v00);\\n  let s10 = select(0.0, v10, v10 == v10);\\n  let s01 = select(0.0, v01, v01 == v01);\\n  let s11 = select(0.0, v11, v11 == v11);\\n\\n  let top = mix(s00, s10, fx);\\n  let bot = mix(s01, s11, fx);\\n  return mix(top, bot, fy);\\n}\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  @location(0) uvSkirt: vec3<f32>,  // u, v, isSkirt\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n  @location(1) worldPos: vec3<f32>,\\n};\\n\\n@vertex\\nfn vs_main(input: VertexInput) -> VertexOutput {\\n  let u = input.uvSkirt.x;\\n  let v = input.uvSkirt.y;\\n  let isSkirt = input.uvSkirt.z;\\n\\n  // Map UV to tile extent world position (EPSG:3857 meters)\\n  let worldX = mix(tile.extent.x, tile.extent.z, u);\\n  let worldY = mix(tile.extent.y, tile.extent.w, v);\\n\\n  // Sample elevation at this UV\\n  let texSize = vec2<f32>(tile.elevTexWidth, tile.elevTexHeight);\\n  // Flip V for texture: texture (0,0) = top-left (north), UV v=0 = south\\n  let texUV = vec2<f32>(u, 1.0 - v);\\n  let elevation = bilinearSample(texUV, texSize);\\n\\n  // Compute world Z with exaggeration\\n  var worldZ = elevation * tile.exaggeration;\\n\\n  // Skirt vertices drop below the terrain edge\\n  if (isSkirt > 0.5) {\\n    let range = tile.maxElev - tile.minElev;\\n    let dropAmount = max(range * tile.exaggeration * 0.3, 1000.0 * tile.exaggeration);\\n    worldZ = worldZ - dropAmount;\\n  }\\n\\n  let worldPos = vec4<f32>(worldX, worldY, worldZ, 1.0);\\n\\n  var out: VertexOutput;\\n  out.position = camera.viewProjection * worldPos;\\n  out.uv = texUV;\\n  out.worldPos = vec3<f32>(worldX, worldY, worldZ);\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  let texSize = vec2<f32>(tile.elevTexWidth, tile.elevTexHeight);\\n\\n  // Compute surface normal via finite differences\\n  let texelSize = vec2<f32>(1.0 / texSize.x, 1.0 / texSize.y);\\n  let uv = input.uv;\\n\\n  let hL = bilinearSample(uv - vec2<f32>(texelSize.x, 0.0), texSize);\\n  let hR = bilinearSample(uv + vec2<f32>(texelSize.x, 0.0), texSize);\\n  let hD = bilinearSample(uv - vec2<f32>(0.0, texelSize.y), texSize);\\n  let hU = bilinearSample(uv + vec2<f32>(0.0, texelSize.y), texSize);\\n\\n  // World-space deltas\\n  let tileWidth = tile.extent.z - tile.extent.x;\\n  let tileHeight = tile.extent.w - tile.extent.y;\\n  let dx = tileWidth / texSize.x;\\n  let dy = tileHeight / texSize.y;\\n\\n  let dzdx = (hR - hL) * tile.exaggeration / (2.0 * dx);\\n  let dzdy = (hU - hD) * tile.exaggeration / (2.0 * dy);\\n\\n  // Normal from cross product of tangent vectors\\n  var normal = normalize(vec3<f32>(-dzdx, -dzdy, 1.0));\\n\\n  // Lambertian directional lighting\\n  let sunDir = normalize(tile.sunDirection);\\n  let diffuse = max(dot(normal, sunDir), 0.0);\\n  // Ambient + diffuse blend\\n  let lighting = 0.3 + 0.7 * diffuse;\\n\\n  // Elevation for color ramp lookup\\n  let elevation = bilinearSample(uv, texSize);\\n\\n  // NaN check (NODATA regions)\\n  if (elevation != elevation) {\\n    discard;\\n  }\\n\\n  // Normalize elevation to [0, 1] for color ramp lookup\\n  let range = tile.maxElev - tile.minElev;\\n  var normalized: f32;\\n  if (range > 0.0) {\\n    normalized = clamp((elevation - tile.minElev) / range, 0.0, 1.0);\\n  } else {\\n    normalized = 0.5;\\n  }\\n\\n  // Sample color ramp LUT (rgba8unorm 256\\u00D71 \\u2014 filterable)\\n  let color = textureSample(colorRampTex, terrainSampler, vec2<f32>(normalized, 0.5));\\n\\n  // Sample pre-computed hillshade (r8unorm \\u2014 filterable)\\n  let shade = textureSample(hillshadeTex, terrainSampler, uv).r;\\n\\n  // Blend: color * lighting * hillshade\\n  let shadeFactor = mix(1.0, shade, tile.hillshadeStrength);\\n  let lit = color.rgb * lighting * shadeFactor;\\n\\n  return vec4<f32>(lit, color.a * tile.opacity);\\n}\\n\";\n/**\n * Terrain 3D tile bind group layout (group 1).\n * 5 entries: uniform + sampler + elevation + hillshade + color ramp\n */\nexport declare function createTerrain3DBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface Terrain3DPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n}\nexport interface Terrain3DPipeline {\n    pipeline: GPURenderPipeline;\n    terrainBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Create terrain 3D render pipeline.\n * Uses triangle-list topology, depth write, back-face culling.\n */\nexport declare function createTerrain3DPipeline(desc: Terrain3DPipelineDescriptor): Terrain3DPipeline;\n//# sourceMappingURL=terrain-3d-pipeline.d.ts.map","pipelines/terrain-mesh.d.ts":"/**\n * Terrain Mesh — Grid mesh with skirt geometry for terrain tile rendering\n *\n * Subdivision-mesh'in terrain-özelleştirilmiş versiyonu.\n * Normal grid vertex'lerine ek olarak, kenar boyunca \"skirt\" vertex'leri\n * eklenir. Skirt'ler, komşu tile'lar arasındaki yükseklik farkından\n * kaynaklanan boşlukları (crack/gap) kapatır.\n *\n * Default: 128×128 = 16,641 grid vertex + 4×129 = 516 skirt vertex\n * Vertex layout: vec3<f32> (u, v, isSkirt)\n *   - u, v: UV koordinatları (0..1)\n *   - isSkirt: 0.0 = normal vertex, 1.0 = skirt vertex\n *\n * Shader, isSkirt=1 olan vertex'leri terrain kenarının altına iter.\n */\n/**\n * Terrain mesh — grid + skirt geometry for seamless terrain tile rendering.\n */\nexport interface TerrainMesh {\n    /** Vertex buffer: vec3<f32> (u, v, isSkirt) */\n    vertexBuffer: GPUBuffer;\n    /** Index buffer: uint32 indices */\n    indexBuffer: GPUBuffer;\n    /** Total number of indices to draw (grid + skirt) */\n    indexCount: number;\n    /** Total number of vertices (grid + skirt) */\n    vertexCount: number;\n    /** Grid subdivisions per side */\n    subdivisions: number;\n    /** First index where skirt triangles begin in the index buffer */\n    skirtIndexStart: number;\n}\n/**\n * Terrain grid mesh + skirt geometry oluştur.\n * Her terrain tile bu mesh'i paylaşır — per-tile uniform ile yükseklik\n * verisi ve pozisyon belirlenir.\n *\n * Grid: subdivisions × subdivisions hücre → (subdivisions+1)^2 vertex\n * Skirt: 4 kenar × (subdivisions+1) skirt vertex, kenar segment'leri\n * boyunca üçgen çiftleri ile bağlanır.\n *\n * @param device - GPUDevice\n * @param subdivisions - Grid bölümleri (default 128)\n * @returns TerrainMesh — vertex/index buffer'lar ve metadata\n */\nexport declare function createTerrainMesh(device: GPUDevice, subdivisions?: number): TerrainMesh;\n//# sourceMappingURL=terrain-mesh.d.ts.map","pipelines/terrain-pipeline.d.ts":"/**\n * Terrain Pipeline\n *\n * WGSL shader for DTED elevation rendering.\n * Elevation r32float texture → color ramp LUT lookup → hillshade blending.\n *\n * Bind Groups:\n *   group(0): CameraUniforms (shared, from raster-pipeline)\n *   group(1): TerrainTileUniforms + sampler + elevation(r32float) + hillshade(r8unorm) + colorRamp(rgba8unorm 256×1)\n */\nexport declare const TERRAIN_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n};\\n\\nstruct TerrainTileUniforms {\\n  // Tile extent: minX, minY, maxX, maxY (EPSG:3857)\\n  extent: vec4<f32>,\\n  // Opacity (0..1)\\n  opacity: f32,\\n  // Min elevation (meters)\\n  minElev: f32,\\n  // Max elevation (meters)\\n  maxElev: f32,\\n  // Hillshade blending strength (0..1)\\n  hillshadeStrength: f32,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n@group(1) @binding(0) var<uniform> tile: TerrainTileUniforms;\\n@group(1) @binding(1) var terrainSampler: sampler;\\n@group(1) @binding(2) var elevationTex: texture_2d<f32>;\\n@group(1) @binding(3) var hillshadeTex: texture_2d<f32>;\\n@group(1) @binding(4) var colorRampTex: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexOutput {\\n  @builtin(position) position: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n};\\n\\n@vertex\\nfn vs_main(@builtin(vertex_index) vid: u32) -> VertexOutput {\\n  // Quad corners: BL, BR, TL, TR (triangle-strip)\\n  var positions = array<vec2<f32>, 4>(\\n    vec2<f32>(0.0, 0.0),\\n    vec2<f32>(1.0, 0.0),\\n    vec2<f32>(0.0, 1.0),\\n    vec2<f32>(1.0, 1.0),\\n  );\\n\\n  let uv = positions[vid];\\n\\n  // Map UV to tile extent world position\\n  let worldX = mix(tile.extent.x, tile.extent.z, uv.x);\\n  let worldY = mix(tile.extent.y, tile.extent.w, uv.y);\\n  let worldPos = vec4<f32>(worldX, worldY, 0.0, 1.0);\\n\\n  var out: VertexOutput;\\n  out.position = camera.viewProjection * worldPos;\\n  // Flip UV Y: texture (0,0) = top-left (north), but world minY = south\\n  out.uv = vec2<f32>(uv.x, 1.0 - uv.y);\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // textureLoad for elevation (r32float \\u2014 unfilterable, no sampler needed)\\n  let texSize = textureDimensions(elevationTex);\\n  let texCoord = vec2<i32>(\\n    clamp(i32(input.uv.x * f32(texSize.x)), 0, i32(texSize.x) - 1),\\n    clamp(i32(input.uv.y * f32(texSize.y)), 0, i32(texSize.y) - 1),\\n  );\\n  let elevation = textureLoad(elevationTex, texCoord, 0).r;\\n\\n  // NaN check (NODATA regions) \\u2014 discard transparent\\n  if (elevation != elevation) {\\n    discard;\\n  }\\n\\n  // Normalize elevation to [0, 1] for color ramp lookup\\n  let range = tile.maxElev - tile.minElev;\\n  var normalized: f32;\\n  if (range > 0.0) {\\n    normalized = clamp((elevation - tile.minElev) / range, 0.0, 1.0);\\n  } else {\\n    normalized = 0.5;\\n  }\\n\\n  // Sample color ramp LUT (rgba8unorm 256\\u00D71 \\u2014 filterable)\\n  let color = textureSample(colorRampTex, terrainSampler, vec2<f32>(normalized, 0.5));\\n\\n  // Sample hillshade (r8unorm \\u2014 filterable)\\n  let shade = textureSample(hillshadeTex, terrainSampler, input.uv).r;\\n\\n  // Blend: color * lerp(1.0, shade, hillshadeStrength)\\n  let shadeFactor = mix(1.0, shade, tile.hillshadeStrength);\\n\\n  return vec4<f32>(color.rgb * shadeFactor, color.a * tile.opacity);\\n}\\n\";\n/**\n * Terrain tile bind group layout (group 1).\n * 5 entries: uniform + sampler + elevation + hillshade + color ramp\n */\nexport declare function createTerrainBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\n/**\n * Alternative terrain bind group layout for devices with float32Filterable.\n * Uses 'float' sample type for elevation/hillshade textures (hardware filtering).\n */\nexport declare function createTerrainBindGroupLayoutFilterable(device: GPUDevice): GPUBindGroupLayout;\nexport interface TerrainPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    /** Set true if device supports float32Filterable */\n    float32Filterable?: boolean;\n}\nexport interface TerrainPipeline {\n    pipeline: GPURenderPipeline;\n    terrainBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Create terrain render pipeline.\n */\nexport declare function createTerrainPipeline(desc: TerrainPipelineDescriptor): TerrainPipeline;\n//# sourceMappingURL=terrain-pipeline.d.ts.map","pipelines/text-pipeline.d.ts":"/**\n * Text Pipeline\n *\n * SDF text rendering pipeline.\n * WGSL shader: distance field tabanlı glyph rendering + halo desteği.\n * Billboard quad per glyph, instanced rendering.\n */\nexport declare const TEXT_SHADER_SOURCE = \"\\n\\n// \\u2500\\u2500\\u2500 Bindings \\u2500\\u2500\\u2500\\n\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\\n\\nstruct TextMaterial {\\n  color: vec4<f32>,\\n  haloColor: vec4<f32>,\\n  fontSize: f32,\\n  haloWidth: f32,\\n  // 0=center, 1=left, 2=right, 3=top, 4=bottom\\n  anchor: f32,\\n  _pad: f32,\\n};\\n\\n@group(1) @binding(0) var<uniform> material: TextMaterial;\\n@group(1) @binding(1) var atlasSampler: sampler;\\n@group(1) @binding(2) var atlasTexture: texture_2d<f32>;\\n\\n// \\u2500\\u2500\\u2500 Vertex \\u2500\\u2500\\u2500\\n\\nstruct VertexInput {\\n  // Per-instance: glyph position (world x, y, z)\\n  @location(0) position: vec3<f32>,\\n  // Per-instance: glyph UV rect in atlas (u0, v0, u1, v1)\\n  @location(1) uvRect: vec4<f32>,\\n  // Per-instance: glyph offset from anchor + size (offsetX, offsetY, width, height)\\n  @location(2) glyphOffset: vec4<f32>,\\n};\\n\\nstruct VertexOutput {\\n  @builtin(position) clipPosition: vec4<f32>,\\n  @location(0) uv: vec2<f32>,\\n};\\n\\n// Billboard quad: 6 vertices (2 triangles) per glyph instance\\n@vertex\\nfn vs_main(\\n  input: VertexInput,\\n  @builtin(vertex_index) vid: u32,\\n) -> VertexOutput {\\n  // Quad corners: 2 triangles (0,1,2) and (2,1,3)\\n  var quadOffsets = array<vec2<f32>, 6>(\\n    vec2<f32>(0.0, 0.0),\\n    vec2<f32>(1.0, 0.0),\\n    vec2<f32>(0.0, 1.0),\\n    vec2<f32>(0.0, 1.0),\\n    vec2<f32>(1.0, 0.0),\\n    vec2<f32>(1.0, 1.0),\\n  );\\n\\n  let corner = quadOffsets[vid];\\n\\n  // Interpolate UV from atlas rect\\n  let uv = vec2<f32>(\\n    mix(input.uvRect.x, input.uvRect.z, corner.x),\\n    mix(input.uvRect.y, input.uvRect.w, corner.y),\\n  );\\n\\n  // Project center to clip space\\n  let clipCenter = camera.viewProjection * vec4<f32>(input.position, 1.0);\\n\\n  // Screen-space offset for this glyph quad\\n  let pixelOffset = vec2<f32>(\\n    input.glyphOffset.x + corner.x * input.glyphOffset.z,\\n    input.glyphOffset.y + corner.y * input.glyphOffset.w,\\n  );\\n\\n  let ndcOffset = vec2<f32>(\\n    pixelOffset.x * 2.0 / camera.viewport.x,\\n    pixelOffset.y * 2.0 / camera.viewport.y,\\n  );\\n\\n  var out: VertexOutput;\\n  out.clipPosition = vec4<f32>(\\n    clipCenter.x + ndcOffset.x * clipCenter.w,\\n    clipCenter.y - ndcOffset.y * clipCenter.w,\\n    clipCenter.z,\\n    clipCenter.w,\\n  );\\n  out.uv = uv;\\n  return out;\\n}\\n\\n// \\u2500\\u2500\\u2500 Fragment \\u2500\\u2500\\u2500\\n\\n@fragment\\nfn fs_main(input: VertexOutput) -> @location(0) vec4<f32> {\\n  // SDF distance sample (r channel, 0.5 = edge)\\n  let dist = textureSample(atlasTexture, atlasSampler, input.uv).r;\\n\\n  // SDF threshold: inside glyph\\n  let edgeThreshold = 0.5;\\n  let aa = fwidth(dist) * 0.75;\\n\\n  // Halo rendering\\n  let haloThreshold = edgeThreshold - material.haloWidth * 0.05;\\n  let haloAlpha = smoothstep(haloThreshold - aa, haloThreshold + aa, dist);\\n  let fillAlpha = smoothstep(edgeThreshold - aa, edgeThreshold + aa, dist);\\n\\n  // Composite: halo behind fill\\n  let haloResult = vec4<f32>(material.haloColor.rgb, material.haloColor.a * haloAlpha);\\n  let fillResult = vec4<f32>(material.color.rgb, material.color.a * fillAlpha);\\n\\n  // Alpha blend: fill over halo\\n  let alpha = fillResult.a + haloResult.a * (1.0 - fillResult.a);\\n  if (alpha < 0.01) {\\n    discard;\\n  }\\n\\n  let rgb = (fillResult.rgb * fillResult.a + haloResult.rgb * haloResult.a * (1.0 - fillResult.a)) / alpha;\\n  return vec4<f32>(rgb, alpha);\\n}\\n\";\n/**\n * Text material bind group layout (group 1).\n */\nexport declare function createTextBindGroupLayout(device: GPUDevice): GPUBindGroupLayout;\nexport interface TextPipelineDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    sampleCount?: number;\n}\nexport interface TextPipeline {\n    pipeline: GPURenderPipeline;\n    materialBindGroupLayout: GPUBindGroupLayout;\n    sampler: GPUSampler;\n}\n/**\n * Text render pipeline olustur.\n * Instanced rendering: her glyph bir instance, 6 vertex (billboard quad).\n */\nexport declare function createTextPipeline(desc: TextPipelineDescriptor): TextPipeline;\n//# sourceMappingURL=text-pipeline.d.ts.map","pipelines/tile-debug-pipeline.d.ts":"/**\n * Tile Debug Pipeline — Wireframe + vertex dots + tile border overlay.\n *\n * Three visual layers per tile:\n *   1. Wireframe grid (line-list) — subtle cyan\n *   2. Vertex dots (instanced quads, circle-masked) — bright yellow\n *   3. Tile border (screen-space expanded quads) — thick orange\n *\n * All three layers support per-vertex height displacement via a height\n * texture at @group(2). Tiles without height data bind a shared zero texture.\n *\n * Works in both 2D (flat Mercator) and 3D (globe) modes.\n */\nexport interface TileDebugMesh {\n    vertexBuffer: GPUBuffer;\n    wireframeIndexBuffer: GPUBuffer;\n    wireframeIndexCount: number;\n    vertexCount: number;\n    subdivisions: number;\n}\nexport interface TileDebugSuiteDescriptor {\n    device: GPUDevice;\n    colorFormat: GPUTextureFormat;\n    cameraBindGroupLayout: GPUBindGroupLayout;\n    depthFormat?: GPUTextureFormat;\n    depthCompare?: GPUCompareFunction;\n    globe?: boolean;\n    sampleCount?: number;\n}\nexport interface TileDebugSuite {\n    wireframePipeline: GPURenderPipeline;\n    dotPipeline: GPURenderPipeline;\n    borderPipeline: GPURenderPipeline;\n    /** Uniform bind group layout (group 1) */\n    bindGroupLayout: GPUBindGroupLayout;\n    /** Height texture bind group layout (group 2) */\n    heightBindGroupLayout: GPUBindGroupLayout;\n    /** Shared sampler for dynamically bound terrain textures */\n    heightSampler: GPUSampler;\n    mesh: TileDebugMesh;\n    quadBuffer: GPUBuffer;\n    /** Shared zero-height texture for tiles without brush data */\n    zeroHeightTexture: GPUTexture;\n    /** Pre-built bind group for zero heights */\n    zeroHeightBindGroup: GPUBindGroup;\n}\n/** Uniform layout: 112 bytes = 28 floats */\nexport declare const DEBUG_UNIFORM_SIZE = 112;\nexport declare const GRID_COLOR: [number, number, number, number];\nexport declare const DOT_COLOR: [number, number, number, number];\nexport declare const BORDER_COLOR: [number, number, number, number];\nexport declare const DOT_SIZE = 3;\nexport declare const BORDER_WIDTH = 2;\nexport declare function createTileDebugMesh(device: GPUDevice, subdivisions?: number): TileDebugMesh;\nexport declare function createTileDebugSuite(desc: TileDebugSuiteDescriptor): TileDebugSuite;\n//# sourceMappingURL=tile-debug-pipeline.d.ts.map","pipelines/wgsl-preambles.d.ts":"/**\n * Shared WGSL Preambles\n *\n * Extracts duplicated WGSL shader snippets (structs, constants, helpers)\n * into reusable string constants. Each pipeline imports what it needs\n * and interpolates via template literals.\n *\n * IMPORTANT: These strings must stay character-for-character identical\n * to the inline WGSL they replace — do NOT reformat or add/remove whitespace.\n */\n/**\n * CameraUniforms struct + @group(0) @binding(0) declaration.\n * Used by 2D pipelines: point, line, polygon, text, icon, model, picking.\n *\n * Note: raster-pipeline uses a smaller CameraUniforms (no viewport).\n * Note: custom-pipeline has its own preamble with different group layout.\n */\nexport declare const WGSL_CAMERA_UNIFORMS = \"\\nstruct CameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: CameraUniforms;\";\n/**\n * GlobeCameraUniforms struct + @group(0) @binding(0) declaration.\n * Used by all globe pipelines: globe-raster, globe-point, globe-line,\n * globe-polygon, globe-icon, globe-model, pole-cap, atmosphere.\n */\nexport declare const WGSL_GLOBE_CAMERA_UNIFORMS = \"\\nstruct GlobeCameraUniforms {\\n  viewProjection: mat4x4<f32>,\\n  flatViewProjection: mat4x4<f32>,\\n  viewport: vec2<f32>,\\n  projectionTransition: f32,\\n  globeRadius: f32,\\n  clippingPlane: vec4<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> camera: GlobeCameraUniforms;\";\n/**\n * Core globe constants: PI, TWO_PI, HALF_CIRCUMFERENCE.\n * Used by globe-point, globe-line, globe-polygon, globe-icon, globe-model, globe-raster.\n *\n * Note: Some pipelines also define EARTH_RADIUS_M and ALTITUDE_EXAG\n * inline (globe-point, globe-line, globe-polygon, globe-icon).\n */\nexport declare const WGSL_GLOBE_CONSTANTS = \"\\nconst PI: f32 = 3.14159265358979323846;\\nconst TWO_PI: f32 = 6.28318530717958647692;\\nconst HALF_CIRCUMFERENCE: f32 = 20037508.34;\";\n/**\n * Shared globe projection helpers:\n *   epsg3857ToMerc01  — EPSG:3857 → Mercator [0..1] with Y-flip\n *   mercatorToAngular — Mercator [0..1] → Angular (radians)\n *   angularToSphere   — Angular → Unit Sphere (3D)\n *   globeClippingZ    — Horizon-aware depth from clipping plane\n *\n * Used by globe-point, globe-line, globe-polygon, globe-icon, globe-model.\n *\n * Note: globe-raster only uses mercatorToAngular, angularToSphere, globeClippingZ\n *       (no epsg3857ToMerc01) — those are kept inline.\n */\nexport declare const WGSL_GLOBE_HELPERS = \"\\nfn epsg3857ToMerc01(pos: vec3<f32>) -> vec2<f32> {\\n  return vec2<f32>(\\n    (pos.x + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE),\\n    1.0 - (pos.y + HALF_CIRCUMFERENCE) / (2.0 * HALF_CIRCUMFERENCE)\\n  );\\n}\\n\\nfn mercatorToAngular(merc: vec2<f32>) -> vec2<f32> {\\n  let lon = merc.x * TWO_PI - PI;\\n  let lat = atan(exp(PI - merc.y * TWO_PI)) * 2.0 - PI * 0.5;\\n  return vec2<f32>(lon, lat);\\n}\\n\\nfn angularToSphere(lon: f32, lat: f32) -> vec3<f32> {\\n  let cosLat = cos(lat);\\n  return vec3<f32>(\\n    cosLat * sin(lon),\\n    sin(lat),\\n    cosLat * cos(lon),\\n  );\\n}\\n\\nfn globeClippingZ(spherePos: vec3<f32>) -> f32 {\\n  return 1.0 - (dot(spherePos, camera.clippingPlane.xyz) + camera.clippingPlane.w);\\n}\";\n/**\n * Full globe preamble: camera uniforms + constants + helpers.\n * Convenience constant for pipelines that use all three parts.\n */\nexport declare const WGSL_GLOBE_PREAMBLE: string;\n//# sourceMappingURL=wgsl-preambles.d.ts.map","post-process/BloomPass.d.ts":"/**\n * BloomPass — GPU bloom/glow post-processing effect.\n *\n * Algorithm:\n * 1. Bright-pass: extract pixels above threshold\n * 2. Gaussian blur: separable 2-pass (horizontal + vertical)\n * 3. Additive blend: combine blur result with original scene\n */\nexport declare const BLOOM_SHADER_SOURCE = \"\\n\\nstruct BloomUniforms {\\n  threshold: f32,\\n  intensity: f32,\\n  rcpSize: vec2<f32>,  // 1/width, 1/height\\n  direction: vec2<f32>, // (1,0) for H-blur, (0,1) for V-blur\\n  _pad: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> params: BloomUniforms;\\n@group(0) @binding(1) var inputSampler: sampler;\\n@group(0) @binding(2) var inputTexture: texture_2d<f32>;\\n\\n// Full-screen triangle\\n@vertex\\nfn vsMain(@builtin(vertex_index) idx: u32) -> @builtin(position) vec4<f32> {\\n  let uv = vec2<f32>(f32((idx << 1u) & 2u), f32(idx & 2u));\\n  return vec4<f32>(uv * 2.0 - 1.0, 0.0, 1.0);\\n}\\n\\n// Gaussian weights (9-tap kernel)\\nconst KERNEL_SIZE: i32 = 4;\\nconst WEIGHTS: array<f32, 5> = array<f32, 5>(\\n  0.227027, 0.1945946, 0.1216216, 0.054054, 0.016216\\n);\\n\\n@fragment\\nfn fsBrightPass(@builtin(position) pos: vec4<f32>) -> @location(0) vec4<f32> {\\n  let uv = pos.xy * params.rcpSize;\\n  let color = textureSample(inputTexture, inputSampler, uv);\\n  let brightness = dot(color.rgb, vec3<f32>(0.2126, 0.7152, 0.0722));\\n  if brightness > params.threshold {\\n    return vec4<f32>(color.rgb * params.intensity, 1.0);\\n  }\\n  return vec4<f32>(0.0, 0.0, 0.0, 1.0);\\n}\\n\\n@fragment\\nfn fsBlur(@builtin(position) pos: vec4<f32>) -> @location(0) vec4<f32> {\\n  let uv = pos.xy * params.rcpSize;\\n  var result = textureSample(inputTexture, inputSampler, uv).rgb * WEIGHTS[0];\\n\\n  let texelOffset = params.direction * params.rcpSize;\\n  for (var i: i32 = 1; i <= KERNEL_SIZE; i = i + 1) {\\n    let offset = texelOffset * f32(i);\\n    result += textureSample(inputTexture, inputSampler, uv + offset).rgb * WEIGHTS[i];\\n    result += textureSample(inputTexture, inputSampler, uv - offset).rgb * WEIGHTS[i];\\n  }\\n\\n  return vec4<f32>(result, 1.0);\\n}\\n\\n@fragment\\nfn fsComposite(@builtin(position) pos: vec4<f32>) -> @location(0) vec4<f32> {\\n  let uv = pos.xy * params.rcpSize;\\n  let scene = textureSample(inputTexture, inputSampler, uv);\\n  // Bloom texture would be bound as a second texture in a real implementation\\n  // For now, this is the additive pass placeholder\\n  return scene;\\n}\\n\";\n/** Bloom pass configuration (runtime). */\nexport interface BloomPassState {\n    threshold: number;\n    intensity: number;\n    radius: number;\n    enabled: boolean;\n}\nexport declare function createDefaultBloomState(): BloomPassState;\n//# sourceMappingURL=BloomPass.d.ts.map","post-process/HDRPass.d.ts":"/**\n * HDRPass — Tone mapping post-processing effect.\n *\n * Supports Reinhard and ACES Filmic tone mapping operators.\n */\nexport declare const HDR_SHADER_SOURCE = \"\\n\\nstruct HDRUniforms {\\n  exposure: f32,\\n  toneMapper: f32,  // 0 = Reinhard, 1 = ACES\\n  rcpSize: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> params: HDRUniforms;\\n@group(0) @binding(1) var inputSampler: sampler;\\n@group(0) @binding(2) var inputTexture: texture_2d<f32>;\\n\\n@vertex\\nfn vsMain(@builtin(vertex_index) idx: u32) -> @builtin(position) vec4<f32> {\\n  let uv = vec2<f32>(f32((idx << 1u) & 2u), f32(idx & 2u));\\n  return vec4<f32>(uv * 2.0 - 1.0, 0.0, 1.0);\\n}\\n\\nfn reinhardToneMap(color: vec3<f32>) -> vec3<f32> {\\n  return color / (color + vec3<f32>(1.0));\\n}\\n\\nfn acesToneMap(color: vec3<f32>) -> vec3<f32> {\\n  // ACES Filmic approximation (Narkowicz 2015)\\n  let a = 2.51;\\n  let b = 0.03;\\n  let c = 2.43;\\n  let d = 0.59;\\n  let e = 0.14;\\n  return saturate((color * (a * color + b)) / (color * (c * color + d) + e));\\n}\\n\\n@fragment\\nfn fsMain(@builtin(position) pos: vec4<f32>) -> @location(0) vec4<f32> {\\n  let uv = pos.xy * params.rcpSize;\\n  var color = textureSample(inputTexture, inputSampler, uv).rgb;\\n\\n  // Apply exposure\\n  color = color * params.exposure;\\n\\n  // Tone mapping\\n  if params.toneMapper < 0.5 {\\n    color = reinhardToneMap(color);\\n  } else {\\n    color = acesToneMap(color);\\n  }\\n\\n  // Gamma correction (linear \\u2192 sRGB)\\n  color = pow(color, vec3<f32>(1.0 / 2.2));\\n\\n  return vec4<f32>(color, 1.0);\\n}\\n\";\nexport interface HDRPassState {\n    exposure: number;\n    toneMapping: 'reinhard' | 'aces';\n    enabled: boolean;\n}\nexport declare function createDefaultHDRState(): HDRPassState;\n//# sourceMappingURL=HDRPass.d.ts.map","post-process/PostProcessTypes.d.ts":"/**\n * Post-Processing Types — Shared interfaces for composable effect chain.\n */\n/** Configuration for a single post-process pass. */\nexport interface PostProcessPassConfig {\n    /** Unique name for this pass (e.g., 'bloom', 'ssao', 'fxaa'). */\n    name: string;\n    /** Whether this pass is enabled. */\n    enabled: boolean;\n}\n/** Configuration for the full post-processing chain. */\nexport interface PostProcessConfig {\n    /** Master enable/disable for all post-processing. Default: false. */\n    enabled: boolean;\n    /** FXAA anti-aliasing. */\n    fxaa?: {\n        enabled: boolean;\n        quality?: number;\n    };\n    /** Bloom glow effect. */\n    bloom?: {\n        enabled: boolean;\n        threshold?: number;\n        intensity?: number;\n        radius?: number;\n    };\n    /** Screen-space ambient occlusion. */\n    ssao?: {\n        enabled: boolean;\n        radius?: number;\n        intensity?: number;\n        bias?: number;\n    };\n    /** HDR tone mapping. */\n    hdr?: {\n        enabled: boolean;\n        exposure?: number;\n        toneMapping?: 'reinhard' | 'aces';\n    };\n}\n/** Resolved config with all defaults applied. */\nexport interface ResolvedPostProcessConfig {\n    enabled: boolean;\n    fxaa: {\n        enabled: boolean;\n        quality: number;\n    };\n    bloom: {\n        enabled: boolean;\n        threshold: number;\n        intensity: number;\n        radius: number;\n    };\n    ssao: {\n        enabled: boolean;\n        radius: number;\n        intensity: number;\n        bias: number;\n    };\n    hdr: {\n        enabled: boolean;\n        exposure: number;\n        toneMapping: 'reinhard' | 'aces';\n    };\n}\nexport declare function resolvePostProcessConfig(cfg?: PostProcessConfig): ResolvedPostProcessConfig;\n//# sourceMappingURL=PostProcessTypes.d.ts.map","post-process/SSAOPass.d.ts":"/**\n * SSAOPass — Screen-Space Ambient Occlusion post-processing effect.\n *\n * Algorithm:\n * 1. Sample depth buffer in a hemisphere around each fragment\n * 2. Count occluded samples → occlusion factor\n * 3. Blur the AO texture to reduce noise\n * 4. Multiply with scene color\n */\nexport declare const SSAO_SHADER_SOURCE = \"\\n\\nstruct SSAOUniforms {\\n  radius: f32,\\n  intensity: f32,\\n  bias: f32,\\n  sampleCount: f32,\\n  rcpSize: vec2<f32>,\\n  _pad: vec2<f32>,\\n};\\n\\n@group(0) @binding(0) var<uniform> params: SSAOUniforms;\\n@group(0) @binding(1) var depthSampler: sampler;\\n@group(0) @binding(2) var depthTexture: texture_2d<f32>;\\n\\n@vertex\\nfn vsMain(@builtin(vertex_index) idx: u32) -> @builtin(position) vec4<f32> {\\n  let uv = vec2<f32>(f32((idx << 1u) & 2u), f32(idx & 2u));\\n  return vec4<f32>(uv * 2.0 - 1.0, 0.0, 1.0);\\n}\\n\\n// Simple hash for pseudo-random sampling directions\\nfn hash(p: vec2<f32>) -> f32 {\\n  var p3 = fract(vec3<f32>(p.x, p.y, p.x) * 0.1031);\\n  p3 = p3 + dot(p3, p3.yzx + 33.33);\\n  return fract((p3.x + p3.y) * p3.z);\\n}\\n\\n@fragment\\nfn fsMain(@builtin(position) pos: vec4<f32>) -> @location(0) vec4<f32> {\\n  let uv = pos.xy * params.rcpSize;\\n  let centerDepth = textureSample(depthTexture, depthSampler, uv).r;\\n\\n  if centerDepth >= 1.0 {\\n    return vec4<f32>(1.0); // Sky \\u2014 no occlusion\\n  }\\n\\n  var occlusion: f32 = 0.0;\\n  let samples = i32(params.sampleCount);\\n\\n  for (var i: i32 = 0; i < samples; i = i + 1) {\\n    let angle = f32(i) * 2.399963 + hash(pos.xy); // golden angle\\n    let r = params.radius * (f32(i) + 0.5) / f32(samples);\\n    let offset = vec2<f32>(cos(angle), sin(angle)) * r * params.rcpSize;\\n\\n    let sampleDepth = textureSample(depthTexture, depthSampler, uv + offset).r;\\n    let diff = centerDepth - sampleDepth;\\n\\n    if diff > params.bias && diff < params.radius {\\n      occlusion += 1.0;\\n    }\\n  }\\n\\n  occlusion = 1.0 - (occlusion / f32(samples)) * params.intensity;\\n  return vec4<f32>(occlusion, occlusion, occlusion, 1.0);\\n}\\n\";\nexport interface SSAOPassState {\n    radius: number;\n    intensity: number;\n    bias: number;\n    sampleCount: number;\n    enabled: boolean;\n}\nexport declare function createDefaultSSAOState(): SSAOPassState;\n//# sourceMappingURL=SSAOPass.d.ts.map","post-process/ShadowMapTypes.d.ts":"/**\n * Shadow Map Types — Configuration for cascade shadow mapping.\n *\n * Directional light (sun) shadow with Percentage Closer Filtering (PCF).\n */\nexport interface ShadowConfig {\n    /** Master enable/disable. */\n    enabled: boolean;\n    /** Shadow map resolution per cascade. Default: 2048. */\n    resolution?: number;\n    /** Number of cascades (1-4). Default: 3. */\n    cascadeCount?: number;\n    /** PCF kernel size (1 = hard, 3 = soft, 5 = very soft). Default: 3. */\n    pcfKernelSize?: number;\n    /** Shadow darkness (0 = no shadow, 1 = pitch black). Default: 0.5. */\n    darkness?: number;\n    /** Maximum shadow distance from camera (meters). Default: 5000. */\n    maxDistance?: number;\n    /** Shadow bias to prevent acne. Default: 0.005. */\n    bias?: number;\n    /** Normal bias to prevent peter-panning. Default: 0.02. */\n    normalBias?: number;\n}\nexport interface ResolvedShadowConfig {\n    enabled: boolean;\n    resolution: number;\n    cascadeCount: number;\n    pcfKernelSize: number;\n    darkness: number;\n    maxDistance: number;\n    bias: number;\n    normalBias: number;\n}\nexport declare function resolveShadowConfig(cfg?: ShadowConfig): ResolvedShadowConfig;\n/** Cascade split distances (logarithmic). */\nexport declare function computeCascadeSplits(nearPlane: number, maxDistance: number, cascadeCount: number, lambda?: number): number[];\nexport declare const SHADOW_SAMPLING_WGSL = \"\\n// PCF shadow sampling function (injected into scene shaders)\\nfn sampleShadow(\\n  shadowMap: texture_depth_2d,\\n  shadowSampler: sampler_comparison,\\n  shadowCoord: vec3<f32>,\\n  bias: f32,\\n  texelSize: f32,\\n) -> f32 {\\n  let adjustedZ = shadowCoord.z - bias;\\n  var shadow: f32 = 0.0;\\n\\n  // 3x3 PCF kernel\\n  for (var x: i32 = -1; x <= 1; x = x + 1) {\\n    for (var y: i32 = -1; y <= 1; y = y + 1) {\\n      let offset = vec2<f32>(f32(x), f32(y)) * texelSize;\\n      shadow += textureSampleCompare(\\n        shadowMap, shadowSampler,\\n        shadowCoord.xy + offset, adjustedZ\\n      );\\n    }\\n  }\\n\\n  return shadow / 9.0;\\n}\\n\";\n//# sourceMappingURL=ShadowMapTypes.d.ts.map","post-process/index.d.ts":"/**\n * Post-Processing Effects Module\n */\nexport { resolvePostProcessConfig, } from './PostProcessTypes';\nexport type { PostProcessConfig, PostProcessPassConfig, ResolvedPostProcessConfig, } from './PostProcessTypes';\nexport { BLOOM_SHADER_SOURCE, createDefaultBloomState } from './BloomPass';\nexport type { BloomPassState } from './BloomPass';\nexport { HDR_SHADER_SOURCE, createDefaultHDRState } from './HDRPass';\nexport type { HDRPassState } from './HDRPass';\nexport { SSAO_SHADER_SOURCE, createDefaultSSAOState } from './SSAOPass';\nexport type { SSAOPassState } from './SSAOPass';\n//# sourceMappingURL=index.d.ts.map","render-engine.d.ts":"/**\n * Render Engine — Facade\n *\n * WebGPU render core — IRenderEngine interface implementation.\n * Delegates all draw operations to focused delegate classes.\n * Owns lifecycle (init, beginFrame, endFrame, destroy, recover)\n * and shared GPU state (FrameContext).\n */\nimport type { CameraState, ClusterStyleConfig, CustomDrawCall, DepthConfig, ExtrudedPolygonSymbol, ExtrusionRenderBuffer, Mesh3DRenderBuffer, Mesh3DSymbol, FeaturePickResult, GlobeImageryTile, GpuCapabilities, GpuMemoryAccounting, ImageryTile, LightConfig, LineRenderBuffer, LineSymbol, ModelRenderBuffer, ModelSymbol, PointRenderBuffer, PointSymbol, PolygonRenderBuffer, PolygonSymbol, TextRenderBuffer, TextSymbol } from '@mapgpu/core';\nexport declare class RenderEngine {\n    private readonly ctx;\n    private _capabilities;\n    private textureManager;\n    private bindGroupCache;\n    private iconAtlas;\n    private pickingDelegate;\n    private rasterDelegate;\n    private globeDelegate;\n    private vectorDelegate;\n    private modelDelegate;\n    private customDelegate;\n    private clusterDelegate;\n    private extrusionDelegate;\n    private mesh3dDelegate;\n    private _clearColor;\n    /**\n     * Mevcut GPU yetenekleri.\n     */\n    get capabilities(): GpuCapabilities;\n    /**\n     * Expose depth config for pipeline creation.\n     */\n    get depthConfig(): DepthConfig;\n    /**\n     * Whether any delegate requested continuous rendering (e.g., active animation).\n     * Checked by the render loop after endFrame to keep rendering.\n     */\n    get needsContinuousRender(): boolean;\n    private ensureIconAtlas;\n    /**\n     * WebGPU device init + capability detection.\n     */\n    init(canvas: HTMLCanvasElement, depthConfig?: DepthConfig): Promise<GpuCapabilities>;\n    /**\n     * Clear color ayarla (RGBA, 0-1 araligi).\n     */\n    setClearColor(r: number, g: number, b: number, a: number): void;\n    /**\n     * Toggle wireframe debug overlay on raster tiles.\n     */\n    setDebugTileVertices(enabled: boolean): void;\n    setExtrusionDebug(enabled: boolean): void;\n    setLighting(config: LightConfig): void;\n    applyDebugBrush(mercX: number, mercY: number, radius: number, strength: number, softness?: number): void;\n    clearDebugBrush(): void;\n    setHeightExaggeration(factor: number): void;\n    /**\n     * Frame baslangici — camera uniform guncelle, render pass baslat.\n     */\n    beginFrame(camera: CameraState): void;\n    drawImagery(tile: ImageryTile): void;\n    drawGlobeTile(tile: GlobeImageryTile): void;\n    drawPoleCaps(color: [number, number, number, number]): void;\n    drawAtmosphere(strength: number, config?: import('@mapgpu/core').AtmosphereConfig): void;\n    drawGlobePoints(buffer: PointRenderBuffer, symbol: PointSymbol): void;\n    drawGlobeLines(buffer: LineRenderBuffer, symbol: LineSymbol): void;\n    drawGlobePolygons(buffer: PolygonRenderBuffer, symbol: PolygonSymbol): void;\n    drawPoints(buffer: PointRenderBuffer, symbol: PointSymbol): void;\n    drawLines(buffer: LineRenderBuffer, symbol: LineSymbol): void;\n    drawPolygons(buffer: PolygonRenderBuffer, symbol: PolygonSymbol): void;\n    drawText(buffer: TextRenderBuffer, symbol: TextSymbol): void;\n    drawPostProcess(sceneTexture: GPUTexture): void;\n    drawCustom(call: CustomDrawCall): void;\n    loadModel(id: string, source: ArrayBuffer | import('@mapgpu/core').GltfSource): Promise<void>;\n    drawModels(buffer: ModelRenderBuffer, symbol: ModelSymbol): void;\n    drawGlobeModels(buffer: ModelRenderBuffer, symbol: ModelSymbol): void;\n    drawExtrusion(buffer: ExtrusionRenderBuffer, symbol: ExtrudedPolygonSymbol): void;\n    drawGlobeExtrusion(buffer: ExtrusionRenderBuffer, symbol: ExtrudedPolygonSymbol): void;\n    drawMesh3D(buffer: Mesh3DRenderBuffer, symbol: Mesh3DSymbol): void;\n    drawGlobeMesh3D(buffer: Mesh3DRenderBuffer, symbol: Mesh3DSymbol): void;\n    setClusterSource(layerId: string, points: Float32Array, version: number): void;\n    drawClusters(layerId: string, style: ClusterStyleConfig, clusterRadius: number, clusterMinPoints: number, zoom: number, extent: [number, number, number, number], globe: boolean): void;\n    loadIcon(id: string, image: ImageBitmap): void;\n    setCurrentLayerId(id: string): void;\n    setPickingEnabled(enabled: boolean): void;\n    pick(x: number, y: number): Promise<FeaturePickResult | null>;\n    /**\n     * Frame bitisi — command buffer submit, transient buffer cleanup.\n     */\n    endFrame(): void;\n    createTexture(image: ImageBitmap): GPUTexture;\n    createBuffer(data: ArrayBufferView, usage: GPUBufferUsageFlags): GPUBuffer;\n    writeBuffer(buffer: GPUBuffer, offset: number, data: ArrayBufferView): void;\n    releaseBuffer(buffer: GPUBuffer): void;\n    releaseTexture(texture: GPUTexture): void;\n    createFloat32Texture(data: Float32Array, width: number, height: number): GPUTexture;\n    createUint8Texture(data: Uint8Array, width: number, height: number): GPUTexture;\n    createRGBA8Texture(data: Uint8Array, width: number, height: number): GPUTexture;\n    getMemoryAccounting(): GpuMemoryAccounting;\n    recover(depthConfig?: DepthConfig): Promise<void>;\n    destroy(): void;\n}\n//# sourceMappingURL=render-engine.d.ts.map","sprite-atlas.d.ts":"/**\n * Sprite Atlas\n *\n * Ikon sprite atlas yonetimi.\n * Shelf-first-fit bin packing ile sprite'lari 2D RGBA texture'a yerlestirir.\n * Baslangic boyutu 512x512, ihtiyac halinde 4096'ya kadar buyur.\n */\nexport interface SpriteEntry {\n    /** Atlas icerisindeki UV koordinatlari [u0, v0, u1, v1] */\n    uv: [number, number, number, number];\n    /** Sprite genisligi (piksel) */\n    width: number;\n    /** Sprite yuksekligi (piksel) */\n    height: number;\n    /** Atlas icerisindeki piksel koordinatlari */\n    x: number;\n    y: number;\n}\nexport declare class SpriteAtlas {\n    private sprites;\n    private shelves;\n    private _width;\n    private _height;\n    private data;\n    private dirty;\n    private device;\n    private texture;\n    constructor(device?: GPUDevice);\n    get width(): number;\n    get height(): number;\n    get spriteCount(): number;\n    get isDirty(): boolean;\n    /**\n     * Sprite ekle.\n     * @param id Sprite tanimlayicisi\n     * @param data RGBA piksel verisi (width * height * 4 bytes)\n     * @param width Sprite genisligi\n     * @param height Sprite yuksekligi\n     * @returns Eklenen SpriteEntry veya null (yer yoksa)\n     */\n    addSprite(id: string, data: Uint8Array, width: number, height: number): SpriteEntry | null;\n    /**\n     * Sprite bilgilerini getir.\n     */\n    getSprite(id: string): SpriteEntry | undefined;\n    /**\n     * GPU texture'i getir (lazy create + upload).\n     */\n    getTexture(): GPUTexture | null;\n    /**\n     * Atlas'in ham verisini dondur (test icin).\n     */\n    getData(): Uint8Array;\n    /**\n     * Atlas'i 2x buyut (maks MAX_SIZE).\n     */\n    private grow;\n    /**\n     * Atlas verisini GPU texture'a yukle.\n     */\n    private uploadToGPU;\n    /**\n     * Tum kaynaklari serbest birak.\n     */\n    destroy(): void;\n}\n//# sourceMappingURL=sprite-atlas.d.ts.map","test-setup.d.ts":"/**\n * Test Setup\n *\n * WebGPU global'leri Node.js test ortamında mevcut olmadığı için polyfill sağlar.\n */\n//# sourceMappingURL=test-setup.d.ts.map","texture-manager.d.ts":"/**\n * Texture Manager\n *\n * GPU texture oluşturma, takip etme ve LRU cache temeli.\n */\nexport declare class TextureManager {\n    private readonly device;\n    private readonly tracked;\n    private totalTextureBytes;\n    constructor(device: GPUDevice);\n    /**\n     * ImageBitmap'den GPU texture oluştur.\n     * Format: rgba8unorm (4 bytes per pixel).\n     */\n    createFromImageBitmap(image: ImageBitmap): GPUTexture;\n    /**\n     * Texture erişim zamanını güncelle (LRU tracking).\n     */\n    touch(texture: GPUTexture): void;\n    /**\n     * GPU texture'ı serbest bırak.\n     */\n    release(texture: GPUTexture): void;\n    /**\n     * LRU eviction: en eski texture'ları serbest bırak,\n     * toplam byte sayısı maxBytes altına düşene kadar.\n     */\n    evict(maxBytes: number): void;\n    /**\n     * Float32Array'den r32float GPU texture oluştur.\n     * Tek kanallı elevation data için.\n     */\n    createFromFloat32(data: Float32Array, width: number, height: number): GPUTexture;\n    /**\n     * Uint8Array'den r8unorm GPU texture oluştur.\n     * Tek kanallı hillshade data için.\n     */\n    createFromUint8(data: Uint8Array, width: number, height: number): GPUTexture;\n    /**\n     * Uint8Array'den rgba8unorm GPU texture oluştur.\n     * Color ramp LUT (256×1) için.\n     */\n    createFromRGBA8(data: Uint8Array, width: number, height: number): GPUTexture;\n    /**\n     * Toplam texture byte kullanımı.\n     */\n    get textureBytes(): number;\n    /**\n     * Tracked texture sayısı.\n     */\n    get trackedCount(): number;\n    /**\n     * Tüm texture'ları serbest bırak.\n     */\n    destroy(): void;\n}\n//# sourceMappingURL=texture-manager.d.ts.map"},"@mapgpu/layers":{"AnimatedLayer.d.ts":"/**\n * AnimatedLayer\n *\n * A time-aware layer that animates features based on a temporal attribute.\n * Provides playback controls (play, pause, stop) and time-based feature filtering.\n *\n * NOTE: No WebGPU/WASM/network calls. Pure CPU-side time management.\n * Uses a tick-based approach rather than real timers for testability.\n */\nimport type { Feature } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\n/** Playback state of the animated layer */\nexport type PlaybackState = 'stopped' | 'playing' | 'paused';\nexport interface AnimatedLayerOptions extends LayerBaseOptions {\n    /** Source features with temporal attributes */\n    source: Feature[];\n    /** Attribute field name containing the time value (ISO string or epoch ms) */\n    timeField: string;\n    /** Playback speed multiplier. Defaults to 1.0 */\n    speed?: number;\n}\nexport declare class AnimatedLayer extends LayerBase {\n    readonly type: \"animated\";\n    private _source;\n    private readonly _timeField;\n    private _speed;\n    private _currentTime;\n    private _state;\n    private _timeRange;\n    constructor(options: AnimatedLayerOptions);\n    get timeField(): string;\n    get speed(): number;\n    get currentTime(): number;\n    get state(): PlaybackState;\n    get source(): readonly Feature[];\n    get timeRange(): {\n        min: number;\n        max: number;\n    } | null;\n    protected onLoad(): Promise<void>;\n    refresh(): void;\n    destroy(): void;\n    /**\n     * Set the current time to a specific value.\n     * @param date Date object or epoch milliseconds\n     */\n    setTime(date: Date | number): void;\n    /**\n     * Start playback from the current time.\n     */\n    play(): void;\n    /**\n     * Pause playback at the current time.\n     */\n    pause(): void;\n    /**\n     * Stop playback and reset to the start time.\n     */\n    stop(): void;\n    /**\n     * Set playback speed multiplier.\n     * @param multiplier Speed multiplier (1.0 = normal, 2.0 = double speed)\n     */\n    setSpeed(multiplier: number): void;\n    /**\n     * Advance the current time by a given delta (in real milliseconds).\n     * The actual time advance is delta * speed.\n     * Only advances when in 'playing' state.\n     */\n    tick(deltaMs: number): void;\n    /**\n     * Get features that are active at or before the current time.\n     * A feature is active if its time value <= currentTime.\n     */\n    getCurrentFeatures(): Feature[];\n    /**\n     * Get features within a specific time window.\n     */\n    getFeaturesInRange(startTime: number, endTime: number): Feature[];\n    /**\n     * Set the source features.\n     */\n    setSource(features: Feature[]): void;\n    private getFeatureTime;\n    private computeTimeRange;\n    private updateExtent;\n    private expandExtent;\n}\n//# sourceMappingURL=AnimatedLayer.d.ts.map","ClusterLayer.d.ts":"/**\n * ClusterLayer\n *\n * Groups nearby point features into clusters based on spatial proximity.\n * Supports aggregation fields (sum, avg, min, max, count) and provides\n * IQueryableLayer interface for querying clusters and individual points.\n *\n * NOTE: No WebGPU/WASM/network calls. Pure CPU-side clustering.\n */\nimport type { IQueryableLayer, Feature, Extent, QueryParams } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\n/** Aggregation operation type */\nexport type AggregationType = 'sum' | 'avg' | 'min' | 'max' | 'count';\n/** Aggregation field specification: { operation: fieldName } */\nexport type AggregationFields = Record<AggregationType, string>;\n/** A cluster or individual point result */\nexport interface ClusterFeature {\n    /** Cluster id (negative for clusters, original id for singles) */\n    id: string | number;\n    /** True if this represents a cluster */\n    isCluster: boolean;\n    /** Number of points in the cluster (1 for single points) */\n    pointCount: number;\n    /** Center coordinates [lon, lat] */\n    coordinates: [number, number];\n    /** Aggregated attribute values (for clusters) */\n    properties: Record<string, unknown>;\n}\nexport interface ClusterLayerOptions extends LayerBaseOptions {\n    /** Source point features */\n    source: Feature[];\n    /** Cluster radius in coordinate units. Defaults to 50. */\n    clusterRadius?: number;\n    /** Minimum number of points to form a cluster. Defaults to 2. */\n    clusterMinPoints?: number;\n    /** Aggregation fields: { sum: 'population', avg: 'score' } */\n    fields?: Partial<AggregationFields>;\n}\nexport declare class ClusterLayer extends LayerBase implements IQueryableLayer {\n    readonly type: \"cluster\";\n    private _source;\n    private _clusterRadius;\n    private _clusterMinPoints;\n    private _fields;\n    constructor(options: ClusterLayerOptions);\n    get clusterRadius(): number;\n    set clusterRadius(value: number);\n    get clusterMinPoints(): number;\n    get source(): readonly Feature[];\n    get fields(): Partial<AggregationFields>;\n    protected onLoad(): Promise<void>;\n    refresh(): void;\n    setSource(features: Feature[]): void;\n    /**\n     * Get clusters and unclustered points for a given extent and zoom level.\n     *\n     * Uses a simple grid-based clustering approach:\n     * 1. Divide space into cells of size = clusterRadius\n     * 2. Group points by cell\n     * 3. Cells with >= clusterMinPoints become clusters\n     * 4. Remaining points returned as singles\n     */\n    getClusters(extent: Extent, _zoom: number): ClusterFeature[];\n    queryFeatures(params: QueryParams): Promise<Feature[]>;\n    queryExtent(params?: QueryParams): Promise<Extent>;\n    private getPointCoords;\n    private computeCentroid;\n    private aggregateFields;\n    private updateExtent;\n    private expandExtent;\n    private applyWhereFilter;\n}\n//# sourceMappingURL=ClusterLayer.d.ts.map","DynamicPointLayer.d.ts":"/**\n * DynamicPointLayer — Pre-allocated GPU buffer for bulk position updates.\n *\n * Designed for real-time simulation scenarios (e.g. missile tracks,\n * aircraft positions) where thousands of points update every frame.\n * Uses writeBuffer() for zero-allocation GPU updates.\n */\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nimport type { PointSymbol, IRenderEngine } from '@mapgpu/core';\nexport interface DynamicPointLayerOptions extends LayerBaseOptions {\n    /** Maximum number of points this layer can hold. Default 10000. */\n    maxPoints?: number;\n    /** Symbol used for rendering all points. */\n    symbol?: PointSymbol;\n}\nexport declare class DynamicPointLayer extends LayerBase {\n    readonly type = \"dynamic-point\";\n    private _maxPoints;\n    private _pointCount;\n    private _positionBuffer;\n    private _renderEngine;\n    private _pointSymbol;\n    constructor(options?: DynamicPointLayerOptions);\n    /** Number of active points. */\n    get pointCount(): number;\n    /** Pre-allocated GPU vertex buffer for positions. */\n    get positionBuffer(): GPUBuffer | null;\n    /** Symbol used for rendering all points. */\n    get pointSymbol(): PointSymbol;\n    set pointSymbol(sym: PointSymbol);\n    /** Maximum number of points this layer can hold. */\n    get maxPoints(): number;\n    /**\n     * Attach render engine and allocate the GPU buffer.\n     * Must be called before updatePositions().\n     */\n    attachRenderEngine(engine: IRenderEngine): void;\n    /**\n     * Bulk-update all positions via writeBuffer (no allocation).\n     *\n     * @param data - Float32Array of [x, y, z, x, y, z, ...] in EPSG:3857.\n     *               Length must be a multiple of 3.\n     *               The number of points is data.length / 3.\n     */\n    updatePositions(data: Float32Array): void;\n    protected onLoad(): Promise<void>;\n    destroy(): void;\n}\n//# sourceMappingURL=DynamicPointLayer.d.ts.map","FeatureLayer.d.ts":"/**\n * FeatureLayer\n *\n * Remote feature layer backed by an IFeatureAdapter (WFS or OGC API Features).\n * Supports remote querying with automatic pagination, and optional local caching.\n * Implements IQueryableLayer.\n */\nimport type { IQueryableLayer, IFeatureLayer, IFeatureAdapter, IRenderer, FeatureCollectionInfo, Feature, Extent, QueryParams } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\n/** Factory function type for creating a feature adapter from a URL */\nexport type FeatureAdapterFactory = (url: string) => IFeatureAdapter;\nexport interface FeatureLayerOptions extends LayerBaseOptions {\n    /** Service base URL (used with adapterFactory to create an adapter) */\n    url?: string;\n    /** Collection / feature type id to query. If omitted, first collection is used. */\n    collectionId?: string;\n    /** Injected adapter instance for dependency injection */\n    adapter?: IFeatureAdapter;\n    /** Factory to create adapter from url (alternative to providing adapter directly) */\n    adapterFactory?: FeatureAdapterFactory;\n    /** Enable local caching of fetched features. Defaults to false. */\n    enableCache?: boolean;\n}\nexport declare class FeatureLayer extends LayerBase implements IQueryableLayer, IFeatureLayer {\n    readonly type: \"feature\";\n    /** Optional renderer for feature-level symbology. */\n    private _renderer?;\n    get renderer(): IRenderer | undefined;\n    set renderer(value: IRenderer | undefined);\n    private readonly url?;\n    private collectionId?;\n    private adapter;\n    private readonly adapterFactory?;\n    private collections;\n    private cachedFeatures;\n    private readonly enableCache;\n    constructor(options: FeatureLayerOptions);\n    protected onLoad(): Promise<void>;\n    queryFeatures(params: QueryParams): Promise<Feature[]>;\n    queryExtent(params?: QueryParams): Promise<Extent>;\n    /**\n     * Get cached features (synchronous, for render-frame consumption).\n     * Returns empty array if cache is disabled or no data has been fetched yet.\n     */\n    getFeatures(): readonly Feature[];\n    /**\n     * Get the available collections (after load).\n     */\n    getCollections(): readonly FeatureCollectionInfo[];\n    /**\n     * Get the active collection id.\n     */\n    getCollectionId(): string | undefined;\n    refresh(): void;\n    destroy(): void;\n    private toFeature;\n    private filterLocally;\n    private expandExtent;\n}\n//# sourceMappingURL=FeatureLayer.d.ts.map","GeoJSONLayer.d.ts":"/**\n * GeoJSONLayer\n *\n * Loads GeoJSON data from a URL or inline data object and provides\n * in-memory feature querying (bbox and attribute filter).\n * Implements IQueryableLayer.\n */\nimport type { IQueryableLayer, IFeatureLayer, IRenderer, Feature, Extent, QueryParams } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\n/** GeoJSON types for parsing */\ninterface GeoJsonFeatureCollection {\n    type: 'FeatureCollection';\n    features: GeoJsonFeature[];\n}\ninterface GeoJsonFeature {\n    type: 'Feature';\n    id?: string | number;\n    geometry: {\n        type: string;\n        coordinates: unknown;\n    };\n    properties?: Record<string, unknown>;\n}\nexport interface GeoJSONLayerOptions extends LayerBaseOptions {\n    /** URL to a GeoJSON file */\n    url?: string;\n    /** Inline GeoJSON data (FeatureCollection or Feature array) */\n    data?: GeoJsonFeatureCollection;\n    /** Custom fetch function for dependency injection (testing) */\n    fetchFn?: typeof fetch;\n    /** Callback invoked for each loaded feature. Use to bind popups, tooltips, or extra attributes. */\n    onEachFeature?: (feature: Feature) => void;\n}\nexport declare class GeoJSONLayer extends LayerBase implements IQueryableLayer, IFeatureLayer {\n    readonly type: \"geojson\";\n    /** Optional renderer for feature-level symbology. */\n    private _renderer?;\n    get renderer(): IRenderer | undefined;\n    set renderer(value: IRenderer | undefined);\n    private readonly url?;\n    private readonly initialData?;\n    private readonly fetchFn;\n    private features;\n    private readonly _onEachFeature?;\n    constructor(options: GeoJSONLayerOptions);\n    protected onLoad(): Promise<void>;\n    /**\n     * Dynamically add GeoJSON data to the layer (Leaflet L.geoJSON.addData equivalent).\n     * Appends features without clearing existing ones.\n     */\n    addData(data: GeoJsonFeatureCollection): void;\n    /**\n     * Parse a GeoJSON FeatureCollection into internal Feature objects.\n     */\n    private parseFeatureCollection;\n    /**\n     * Compute the bounding box of all features.\n     */\n    private computeExtent;\n    /**\n     * Recursively walk nested coordinate arrays and invoke cb for each [x,y].\n     */\n    private expandExtent;\n    queryFeatures(params: QueryParams): Promise<Feature[]>;\n    queryExtent(params?: QueryParams): Promise<Extent>;\n    refresh(): void;\n    /**\n     * Get all features in memory (after load).\n     */\n    getFeatures(): readonly Feature[];\n    private featureIntersectsBbox;\n    /**\n     * Apply a simple where clause. Supports:\n     *   \"field = 'value'\"\n     *   \"field = number\"\n     *   \"field > number\"\n     *   \"field < number\"\n     *   \"field >= number\"\n     *   \"field <= number\"\n     *   \"field != 'value'\"\n     */\n    private applyWhereFilter;\n}\nexport {};\n//# sourceMappingURL=GeoJSONLayer.d.ts.map","GpuClusterLayer.d.ts":"/**\n * GpuClusterLayer — GPU-rendered clustering via CPU grid-hash.\n *\n * Wraps an IFeatureLayer (GeoJSONLayer, GraphicsLayer, WFS etc.) and\n * extracts point geometries into a flat Float32Array of EPSG:3857 coordinates.\n * Clustering runs on CPU (pure grid-hash) and the render pipeline\n * consumes the output identically.\n *\n * Cluster interaction: click a cluster -> fit-bounds zoom.\n */\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nimport type { ClusterStyleConfig, ClusterThemePreset, ClusterViewCallbacks, IClusterLayer, IFeatureLayer } from '@mapgpu/core';\nexport interface GpuClusterLayerOptions extends LayerBaseOptions {\n    source: IFeatureLayer;\n    clusterRadius?: number;\n    clusterMinPoints?: number;\n    clusterMaxZoom?: number;\n    themePreset?: ClusterThemePreset;\n    style?: Partial<ClusterStyleConfig>;\n}\nexport declare class GpuClusterLayer extends LayerBase implements IClusterLayer {\n    readonly type = \"gpu-cluster\";\n    private _sourceLayer;\n    private _points3857;\n    private _sourceVersion;\n    private _removeRefreshListener;\n    clusterRadius: number;\n    clusterMinPoints: number;\n    clusterMaxZoom: number;\n    private _themePreset;\n    private _clusterStyle;\n    private _viewCallbacks;\n    constructor(options: GpuClusterLayerOptions);\n    get sourceLayer(): IFeatureLayer;\n    set sourceLayer(layer: IFeatureLayer);\n    setSource(layer: IFeatureLayer): void;\n    get sourceVersion(): number;\n    get pointCount(): number;\n    getSourcePoints3857(): Float32Array | null;\n    get clusterStyle(): ClusterStyleConfig;\n    get themePreset(): ClusterThemePreset;\n    setThemePreset(preset: ClusterThemePreset, style?: Partial<ClusterStyleConfig>): void;\n    setStyle(style: Partial<ClusterStyleConfig>): void;\n    attachView(callbacks: ClusterViewCallbacks): void;\n    handleClusterClick(screenX: number, screenY: number): void;\n    protected onLoad(): Promise<void>;\n    destroy(): void;\n    private _clusterHitRadiusPx;\n    private _pickHitCluster;\n    private _pickHitClusterScreen;\n    private _pickHitClusterMap;\n    private _computeFitTarget;\n    private _convertToMercator;\n    private _bindSourceListener;\n    private _unbindSourceListener;\n}\n//# sourceMappingURL=GpuClusterLayer.d.ts.map","GraphicsLayer.d.ts":"/**\n * GraphicsLayer\n *\n * A client-side layer for programmatically adding, removing,\n * and querying features. Does not fetch from any remote service.\n * Implements IQueryableLayer.\n */\nimport type { IQueryableLayer, IFeatureLayer, IRenderer, Feature, Extent, QueryParams } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nexport type GraphicsLayerOptions = LayerBaseOptions;\nexport declare class GraphicsLayer extends LayerBase implements IQueryableLayer, IFeatureLayer {\n    readonly type: \"graphics\";\n    /** Optional renderer for feature-level symbology. */\n    private _renderer?;\n    get renderer(): IRenderer | undefined;\n    set renderer(value: IRenderer | undefined);\n    private _graphics;\n    private graphicsMap;\n    constructor(options?: GraphicsLayerOptions);\n    protected onLoad(): Promise<void>;\n    /**\n     * Add a feature to the layer.\n     */\n    add(feature: Feature): void;\n    /**\n     * Add multiple features at once.\n     */\n    addMany(features: Feature[]): void;\n    /**\n     * Replace all features atomically with a single refresh.\n     * Much more efficient than clear() + addMany() for animation loops.\n     */\n    replaceAll(features: Feature[]): void;\n    /**\n     * Remove a feature by id.\n     * Returns true if the feature was found and removed.\n     */\n    remove(id: string | number): boolean;\n    /**\n     * Remove all features.\n     */\n    clear(): void;\n    /**\n     * Get a readonly view of all current features.\n     */\n    get graphics(): readonly Feature[];\n    /**\n     * Get the number of features.\n     */\n    get count(): number;\n    /**\n     * Get all features in this layer.\n     */\n    getFeatures(): readonly Feature[];\n    queryFeatures(params: QueryParams): Promise<Feature[]>;\n    queryExtent(params?: QueryParams): Promise<Extent>;\n    private updateExtent;\n    private expandExtent;\n    private featureIntersectsBbox;\n    private applyWhereFilter;\n}\n//# sourceMappingURL=GraphicsLayer.d.ts.map","HeatmapLayer.d.ts":"/**\n * HeatmapLayer\n *\n * Generates heatmap grid data from a set of point features using\n * Gaussian kernel density estimation. Supports configurable radius,\n * intensity, weight field, and gradient color palettes.\n *\n * NOTE: No WebGPU/WASM/network calls. Pure CPU-side computation.\n */\nimport type { Feature } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\n/** RGBA color stop for heatmap gradient */\nexport interface GradientStop {\n    /** Position in 0-1 range */\n    offset: number;\n    /** RGBA color: [r, g, b, a] each in 0-255 */\n    color: [number, number, number, number];\n}\nexport interface HeatmapLayerOptions extends LayerBaseOptions {\n    /** Source point features */\n    source: Feature[];\n    /** Kernel radius in pixels. Defaults to 25. */\n    radius?: number;\n    /** Intensity multiplier. Defaults to 1.0. */\n    intensity?: number;\n    /** Gradient color palette. Defaults to a blue-to-red ramp. */\n    gradient?: GradientStop[];\n    /** Attribute field name to use as weight. If omitted, weight = 1.0 for all. */\n    weightField?: string;\n}\nexport declare class HeatmapLayer extends LayerBase {\n    readonly type: \"heatmap\";\n    private _source;\n    private _radius;\n    private _intensity;\n    private _gradient;\n    private _weightField?;\n    constructor(options: HeatmapLayerOptions);\n    get radius(): number;\n    get intensity(): number;\n    get gradient(): readonly GradientStop[];\n    get weightField(): string | undefined;\n    get source(): readonly Feature[];\n    setSource(features: Feature[]): void;\n    setRadius(px: number): void;\n    setIntensity(value: number): void;\n    setGradient(stops: GradientStop[]): void;\n    protected onLoad(): Promise<void>;\n    refresh(): void;\n    /**\n     * Compute heatmap grid data using Gaussian kernel density estimation.\n     *\n     * The grid dimensions are derived from the source extent with 1-unit cells.\n     * Returns a Float32Array of density values (row-major) with metadata encoded\n     * in the first 2 elements: [width, height, ...values].\n     *\n     * Returns null if not loaded or no features.\n     */\n    getGridData(): {\n        width: number;\n        height: number;\n        data: Float32Array;\n    } | null;\n    private getPointCoords;\n    private getWeight;\n    private updateExtent;\n}\n//# sourceMappingURL=HeatmapLayer.d.ts.map","ImageOverlay.d.ts":"/**\n * ImageOverlay — Display a static image at geographic bounds (Leaflet L.imageOverlay equivalent).\n */\nimport type { Extent } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nexport interface ImageOverlayOptions extends LayerBaseOptions {\n    /** URL of the image to display. */\n    url: string;\n    /** Geographic bounds as [minLon, minLat, maxLon, maxLat] in EPSG:4326. */\n    bounds: [number, number, number, number];\n}\nexport declare class ImageOverlay extends LayerBase {\n    readonly type = \"image-overlay\";\n    readonly url: string;\n    readonly bounds: [number, number, number, number];\n    private _imageData;\n    constructor(options: ImageOverlayOptions);\n    protected onLoad(): Promise<void>;\n    /** Get the loaded image bitmap for rendering. */\n    get imageData(): ImageBitmap | null;\n    /** Get the extent for this overlay. */\n    get fullExtent(): Extent | undefined;\n    /** Update the image URL and reload. */\n    setUrl(url: string): void;\n    /** Update the geographic bounds. */\n    setBounds(bounds: [number, number, number, number]): void;\n}\n//# sourceMappingURL=ImageOverlay.d.ts.map","LayerBase.d.ts":"/**\n * LayerBase — Abstract base class for all layers.\n *\n * Implements the ILayer interface, providing common lifecycle,\n * visibility, opacity, scale management, and event delegation.\n */\nimport { EventBus } from '@mapgpu/core';\nimport type { ILayer, LayerEvents, Extent } from '@mapgpu/core';\n/**\n * Map void → undefined so that EventBus's `Record<string, unknown>` constraint\n * is satisfied (void is not assignable to unknown in strict mode).\n */\ntype NormalizedLayerEvents = {\n    [K in keyof LayerEvents]: LayerEvents[K] extends void ? undefined : LayerEvents[K];\n};\n/** Reset counter (for testing only) */\nexport declare function _resetLayerIdCounter(): void;\nexport interface LayerBaseOptions {\n    /** Custom layer id (auto-generated if omitted) */\n    id?: string;\n    /** Initial visibility. Defaults to true. */\n    visible?: boolean;\n    /** Initial opacity (0-1). Defaults to 1. */\n    opacity?: number;\n    /** Minimum visible scale */\n    minScale?: number;\n    /** Maximum visible scale */\n    maxScale?: number;\n    /** Render ordering — higher values draw on top. Default 0. */\n    zIndex?: number;\n    /** Whether this layer responds to pointer interactions. Default true. */\n    interactive?: boolean;\n    /** Raster blend mode. Default 'normal'. */\n    blendMode?: 'normal' | 'screen' | 'multiply' | 'overlay';\n    /** Post-process color filters for raster tiles. */\n    filters?: {\n        brightness?: number;\n        contrast?: number;\n        saturate?: number;\n    };\n}\nexport declare abstract class LayerBase implements ILayer {\n    readonly id: string;\n    abstract readonly type: string;\n    private _visible;\n    private _opacity;\n    private _loaded;\n    private _destroyed;\n    minScale?: number;\n    maxScale?: number;\n    zIndex?: number;\n    interactive: boolean;\n    blendMode: 'normal' | 'screen' | 'multiply' | 'overlay';\n    filters?: {\n        brightness?: number;\n        contrast?: number;\n        saturate?: number;\n    };\n    protected _fullExtent?: Extent;\n    protected readonly eventBus: EventBus<NormalizedLayerEvents>;\n    constructor(options?: LayerBaseOptions);\n    get visible(): boolean;\n    set visible(value: boolean);\n    get opacity(): number;\n    set opacity(value: number);\n    get loaded(): boolean;\n    protected setLoaded(value: boolean): void;\n    get fullExtent(): Extent | undefined;\n    load(): Promise<void>;\n    /** Subclass must implement actual loading logic */\n    protected abstract onLoad(): Promise<void>;\n    refresh(): void;\n    /**\n     * Signal that the layer's visual style has changed (e.g. renderer update)\n     * without clearing data or resetting load state.\n     *\n     * Emits the same 'refresh' event so that any listening render cache\n     * (e.g. VectorBufferCache) is invalidated and the map repaints.\n     * Unlike {@link refresh}, this does NOT clear features or mark the\n     * layer as unloaded.\n     */\n    redraw(): void;\n    destroy(): void;\n    on<K extends keyof LayerEvents>(event: K, handler: (data: LayerEvents[K]) => void): void;\n    off<K extends keyof LayerEvents>(event: K, handler: (data: LayerEvents[K]) => void): void;\n}\nexport {};\n//# sourceMappingURL=LayerBase.d.ts.map","LayerGroup.d.ts":"/**\n * LayerGroup — A container for managing multiple layers as a unit (Leaflet L.layerGroup equivalent).\n *\n * Operations on the group (visibility, opacity) cascade to all child layers.\n */\nimport type { ILayer } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nexport interface LayerGroupOptions extends LayerBaseOptions {\n    /** Initial layers to include in the group. */\n    layers?: ILayer[];\n}\nexport declare class LayerGroup extends LayerBase {\n    readonly type = \"layer-group\";\n    private _layers;\n    constructor(options?: LayerGroupOptions);\n    protected onLoad(): Promise<void>;\n    /** Add a layer to the group. */\n    addLayer(layer: ILayer): this;\n    /** Remove a layer from the group. */\n    removeLayer(layerOrId: ILayer | string): this;\n    /** Check if a layer is in the group. */\n    hasLayer(layerOrId: ILayer | string): boolean;\n    /** Get a layer by ID. */\n    getLayer(id: string): ILayer | undefined;\n    /** Get all layers in the group. */\n    getLayers(): readonly ILayer[];\n    /** Number of layers in the group. */\n    get count(): number;\n    /** Cascade visibility to all child layers. */\n    set visible(v: boolean);\n    get visible(): boolean;\n    /** Cascade opacity to all child layers. */\n    set opacity(o: number);\n    get opacity(): number;\n    /** Remove all layers from the group. */\n    clearLayers(): this;\n    /** Iterate over all layers. */\n    eachLayer(fn: (layer: ILayer) => void): this;\n    destroy(): void;\n}\n//# sourceMappingURL=LayerGroup.d.ts.map","OceanLayer.d.ts":"/**\n * OceanLayer — Globe ocean water effect layer.\n *\n * Provides realistic 3D water rendering on the globe with Gerstner wave\n * displacement, Fresnel reflection, Blinn-Phong specular, and foam.\n *\n * Supports mask-based rendering: provide a GeoJSON URL containing land\n * or water polygons. The ocean effect only renders in the appropriate areas.\n *\n * Usage:\n * ```ts\n * // Land mask — ocean renders everywhere EXCEPT inside land polygons\n * const ocean = new OceanLayer({\n *   mask: {\n *     url: 'https://cdn.jsdelivr.net/npm/world-atlas@2/land-50m.json',\n *     mode: 'land',\n *     format: 'topojson',\n *     objectName: 'land',\n *   },\n * });\n *\n * // Water mask — ocean renders INSIDE water polygons\n * const ocean = new OceanLayer({\n *   mask: { url: '/data/oceans.geojson', mode: 'water' },\n * });\n *\n * // No mask — ocean covers full globe (useful without raster tiles)\n * const ocean = new OceanLayer();\n *\n * mapView.map.add(ocean);\n * ```\n */\nimport type { GlobeWaterConfig, IOceanLayer } from '@mapgpu/core';\nimport { LayerBase, type LayerBaseOptions } from './LayerBase';\nexport interface OceanMaskConfig {\n    /** URL to GeoJSON or TopoJSON file containing land/water polygons. */\n    url: string;\n    /**\n     * How to interpret the polygons:\n     * - 'land': polygons = land areas → ocean renders OUTSIDE\n     * - 'water': polygons = water areas → ocean renders INSIDE\n     */\n    mode: 'land' | 'water';\n    /** Data format. Default: 'geojson'. */\n    format?: 'geojson' | 'topojson';\n    /** For TopoJSON: name of the objects key to use (e.g. 'land', 'countries'). */\n    objectName?: string;\n}\nexport interface OceanLayerOptions extends LayerBaseOptions {\n    /** Deep ocean color (RGBA 0-1). Default [0, 0.05, 0.15, 1]. */\n    deepColor?: [number, number, number, number];\n    /** Shallow water color (RGBA 0-1). Default [0, 0.4, 0.6, 1]. */\n    shallowColor?: [number, number, number, number];\n    /** Gerstner wave amplitude in unit-sphere scale. Default 0.002. */\n    waveAmplitude?: number;\n    /** Gerstner wave frequency. Default 12.0. */\n    waveFrequency?: number;\n    /** Wind speed (affects wave visuals). Default 10. */\n    windSpeed?: number;\n    /** Wind direction in degrees. Default 0 (north). */\n    windDirection?: number;\n    /** Enable foam highlights on wave crests. Default true. */\n    foamEnabled?: boolean;\n    /** Sun direction vector [x, y, z] (will be normalized). Default [0.3, 0.8, 0.5]. */\n    sunDirection?: [number, number, number];\n    /**\n     * Mask configuration for land/water area control.\n     * When provided, ocean only renders in appropriate areas.\n     * When omitted, ocean covers the full globe.\n     */\n    mask?: OceanMaskConfig;\n}\nexport declare class OceanLayer extends LayerBase implements IOceanLayer {\n    readonly type = \"ocean\";\n    readonly needsContinuousRender = true;\n    private _deepColor;\n    private _shallowColor;\n    private _waveAmplitude;\n    private _waveFrequency;\n    private _windSpeed;\n    private _windDirection;\n    private _foamEnabled;\n    private _sunDirection;\n    private _maskConfig?;\n    private _maskVertices?;\n    private _maskIndices?;\n    constructor(options?: OceanLayerOptions);\n    protected onLoad(): Promise<void>;\n    getWaterConfig(): GlobeWaterConfig;\n    set deepColor(color: [number, number, number, number]);\n    set shallowColor(color: [number, number, number, number]);\n    set waveAmplitude(value: number);\n    set waveFrequency(value: number);\n    set sunDirection(dir: [number, number, number]);\n}\n//# sourceMappingURL=OceanLayer.d.ts.map","RasterTileLayer.d.ts":"/**\n * RasterTileLayer\n *\n * XYZ/TMS raster tile layer. Generates tile URLs from a URL template\n * with {z}, {x}, {y}, and {s} (subdomain) placeholders.\n * TMS y-flip is supported via a tms option.\n */\nimport type { Extent, ITileLayer } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nexport interface RasterTileLayerOptions extends LayerBaseOptions {\n    /** URL template with {z}, {x}, {y}, {s} placeholders */\n    urlTemplate: string;\n    /** Use TMS y-flip convention. Defaults to false. */\n    tms?: boolean;\n    /** Subdomains for load balancing (e.g. ['a','b','c']). Rotated via {s}. */\n    subdomains?: string[];\n    /** Minimum zoom level. Defaults to 0. */\n    minZoom?: number;\n    /** Maximum zoom level. Defaults to 22. */\n    maxZoom?: number;\n    /** Attribution text */\n    attribution?: string;\n}\nexport declare class RasterTileLayer extends LayerBase implements ITileLayer {\n    readonly type: \"raster-tile\";\n    readonly urlTemplate: string;\n    readonly tms: boolean;\n    readonly subdomains: readonly string[];\n    readonly minZoom: number;\n    readonly maxZoom: number;\n    readonly attribution?: string;\n    private subdomainIndex;\n    constructor(options: RasterTileLayerOptions);\n    protected onLoad(): Promise<void>;\n    /**\n     * Validate that the URL template contains required placeholders.\n     */\n    private validateTemplate;\n    /**\n     * Generate a tile URL for the given tile coordinates.\n     *\n     * @param z - Zoom level\n     * @param x - Tile column\n     * @param y - Tile row (standard XYZ convention: 0 = top)\n     */\n    getTileUrl(z: number, x: number, y: number): string;\n    /**\n     * Check if a zoom level is within the valid range.\n     */\n    isZoomValid(z: number): boolean;\n    /**\n     * Get the full extent as an Extent object.\n     */\n    get fullExtent(): Extent | undefined;\n}\n//# sourceMappingURL=RasterTileLayer.d.ts.map","TacticalGraphicLayer.d.ts":"/**\n * TacticalGraphicLayer — Renders MIL-STD-2525C tactical graphics.\n *\n * Accepts features with SIDC + controlPoints attributes.\n * During getFeatures(), it resolves each feature's SIDC to a TacticalGraphicDefinition,\n * calls buildTacticalGraphic() to get RenderInstruction[], then converts these to\n * standard Feature[] with appropriate LineSymbol/PolygonSymbol styling.\n *\n * The layer stores raw tactical graphic features (with SIDC + control points)\n * and lazily produces renderable features by:\n *   1. Parsing controlPoints (JSON string or number[][])\n *   2. Looking up TacticalGraphicDefinition by SIDC functionId\n *   3. Calling buildTacticalGraphic(definition, controlPoints)\n *   4. Converting each RenderInstruction to a Feature with appropriate geometry\n *   5. Mapping RenderInstruction.style to internal _tg_style attributes\n */\nimport type { IFeatureLayer, IRenderer, Feature } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nimport type { TacticalGraphicDefinition } from '@mapgpu/symbols';\n/** Internal raw tactical graphic feature before resolution */\ninterface TacticalFeature {\n    id: string | number;\n    sidc: string;\n    controlPoints: number[][];\n    attributes: Record<string, unknown>;\n}\nexport interface TacticalGraphicLayerOptions extends LayerBaseOptions {\n    /** Initial features to add */\n    features?: TacticalFeature[];\n    /** Feature attribute field containing the SIDC string. Default: 'sidc' */\n    sidcField?: string;\n    /** Feature attribute field containing control points. Default: 'controlPoints' */\n    controlPointsField?: string;\n    /** Tactical graphic definitions for lookup */\n    definitions?: TacticalGraphicDefinition[];\n}\nexport declare class TacticalGraphicLayer extends LayerBase implements IFeatureLayer {\n    readonly type: \"tactical-graphic\";\n    /** Optional renderer for feature-level symbology. */\n    private _renderer?;\n    get renderer(): IRenderer | undefined;\n    set renderer(value: IRenderer | undefined);\n    /** Feature attribute field containing the SIDC. Stored for external access. */\n    readonly sidcField: string;\n    /** Feature attribute field containing control points. Stored for external access. */\n    readonly controlPointsField: string;\n    private _rawFeatures;\n    private readonly _featureMap;\n    private readonly _definitions;\n    /** Cached resolved features */\n    private _resolvedFeatures;\n    private _cacheValid;\n    private _nextAutoId;\n    constructor(options?: TacticalGraphicLayerOptions);\n    protected onLoad(): Promise<void>;\n    /**\n     * Register tactical graphic definitions for resolving SIDCs.\n     */\n    registerDefinitions(defs: TacticalGraphicDefinition[]): void;\n    /**\n     * Get a definition by function ID.\n     */\n    getDefinition(functionId: string): TacticalGraphicDefinition | undefined;\n    /**\n     * Add a tactical graphic feature.\n     * The feature must have sidc and controlPoints properties.\n     */\n    addFeature(feature: {\n        id?: string | number;\n        sidc: string;\n        controlPoints: number[][] | string;\n        attributes?: Record<string, unknown>;\n    }): void;\n    /**\n     * Remove a tactical graphic feature by ID.\n     * Returns true if the feature was found and removed.\n     */\n    removeFeature(id: string | number): boolean;\n    /**\n     * Remove all tactical graphic features.\n     */\n    clear(): void;\n    /**\n     * Get the number of raw tactical graphic features.\n     */\n    get featureCount(): number;\n    /**\n     * Get resolved renderable features.\n     *\n     * For each stored tactical graphic feature, this:\n     *   1. Looks up the TacticalGraphicDefinition by SIDC functionId\n     *   2. Calls buildTacticalGraphic() to produce RenderInstruction[]\n     *   3. Converts each instruction to a standard Feature\n     */\n    getFeatures(): readonly Feature[];\n    private _addFeatureInternal;\n    /**\n     * Resolve a single raw tactical feature into renderable Feature[].\n     */\n    private _resolveFeature;\n    /**\n     * Extract the function ID from a 15-character SIDC.\n     * For tactical graphics (coding scheme 'G'), the function ID\n     * is derived from positions 4-10.\n     */\n    private _extractFunctionId;\n    /**\n     * Convert a RenderInstruction to a standard Feature.\n     */\n    private _instructionToFeature;\n    /**\n     * Recompute the full extent from resolved features.\n     */\n    private _updateExtent;\n    /**\n     * Recursively walk nested coordinate arrays and invoke cb for each [x,y].\n     */\n    private _expandExtent;\n}\nexport {};\n//# sourceMappingURL=TacticalGraphicLayer.d.ts.map","TerrainLayer.d.ts":"/**\n * TerrainLayer\n *\n * Terrain mesh loading and management layer.\n * Provides elevation queries and tile mesh data.\n *\n * NOTE: No real terrain tile fetch/decode. Interface and mock implementation.\n */\nimport type { Extent } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\n/** Supported DEM data formats */\nexport type DemFormat = 'mapbox-terrain' | 'quantized-mesh' | 'heightmap-png';\n/** Lightweight terrain mesh data (mirrors TerrainMesh from IRenderEngine but CPU-side) */\nexport interface TerrainTileMesh {\n    /** Vertex positions: [x0, y0, z0, x1, y1, z1, ...] */\n    vertices: Float32Array;\n    /** Triangle indices */\n    indices: Uint32Array;\n    /** Vertex count */\n    vertexCount: number;\n    /** Index count */\n    indexCount: number;\n    /** Tile extent */\n    extent: Extent;\n}\nexport interface TerrainLayerOptions extends LayerBaseOptions {\n    /** Terrain tile service URL template */\n    url?: string;\n    /** DEM data format */\n    demFormat?: DemFormat;\n    /** Height exaggeration factor (1.0 = normal, 2.0 = 2x) */\n    exaggeration?: number;\n    /** Minimum zoom level for terrain */\n    minZoom?: number;\n    /** Maximum zoom level for terrain */\n    maxZoom?: number;\n}\nexport declare class TerrainLayer extends LayerBase {\n    readonly type: \"terrain\";\n    private readonly _url?;\n    private _demFormat;\n    private _exaggeration;\n    private _minZoom;\n    private _maxZoom;\n    private _metadata;\n    constructor(options?: TerrainLayerOptions);\n    get demFormat(): DemFormat;\n    set demFormat(value: DemFormat);\n    get exaggeration(): number;\n    set exaggeration(value: number);\n    get minZoom(): number;\n    get maxZoom(): number;\n    get url(): string | undefined;\n    protected onLoad(): Promise<void>;\n    /**\n     * Get terrain metadata (available after load).\n     */\n    getMetadata(): {\n        tileSize: number;\n        availableZooms: number[];\n        bounds: Extent;\n    } | null;\n    /**\n     * Query elevation at a given lon/lat.\n     * Mock implementation — returns a deterministic value based on coordinates.\n     *\n     * @returns Elevation in meters, or null if outside terrain bounds\n     */\n    getElevation(lon: number, lat: number): number | null;\n    /**\n     * Get terrain tile mesh data for a specific tile coordinate.\n     * Mock implementation — generates a simple grid mesh.\n     *\n     * @param z Zoom level\n     * @param x Tile X coordinate\n     * @param y Tile Y coordinate\n     * @returns TerrainTileMesh or null if out of range\n     */\n    getTileMesh(z: number, x: number, y: number): TerrainTileMesh | null;\n    refresh(): void;\n}\n//# sourceMappingURL=TerrainLayer.d.ts.map","VectorTileLayer.d.ts":"/**\n * VectorTileLayer\n *\n * Vector tile layer that fetches MVT (PBF) tiles, parses them into\n * GeoJSON-style features, and exposes them via the IFeatureLayer interface.\n *\n * Implements both ITileLayer (for tile URL generation and zoom range) and\n * IFeatureLayer (for feature access by the vector rendering pipeline).\n *\n * The mode calls updateVisibleTiles() each frame with the current tile\n * coordinates; VectorTileManager handles background fetch + cache.\n */\nimport type { Extent, Feature, IFeatureLayer, IRenderer, ITileLayer, VectorTilePerformanceOptions } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nimport { type ParsedVectorTile } from './mvt-parser';\n/** Render symbology for vector tile features */\nexport interface VectorTileStyle {\n    /** Fill color for polygons: [r, g, b, a] each 0-255 */\n    fillColor?: [number, number, number, number];\n    /** Stroke color for lines/polygon outlines: [r, g, b, a] each 0-255 */\n    strokeColor?: [number, number, number, number];\n    /** Stroke width in pixels */\n    strokeWidth?: number;\n    /** Point radius in pixels */\n    pointRadius?: number;\n    /** Point color: [r, g, b, a] each 0-255 */\n    pointColor?: [number, number, number, number];\n    /** Label field name */\n    labelField?: string;\n    /** Font size in pixels */\n    fontSize?: number;\n}\nexport interface VectorTileLayerOptions extends LayerBaseOptions {\n    /** URL template with {z}/{x}/{y} placeholders (e.g. \"https://tiles.example.com/{z}/{x}/{y}.pbf\") */\n    url: string;\n    /** Source layer name within the vector tile */\n    sourceLayer?: string;\n    /** Minimum zoom level. Defaults to 0. */\n    minZoom?: number;\n    /** Maximum zoom level. Defaults to 22. */\n    maxZoom?: number;\n    /** Render style */\n    style?: VectorTileStyle;\n    /** IRenderer for vector symbology (SimpleRenderer, etc.) */\n    renderer?: IRenderer;\n    /** Optional tile processing performance mode configuration. */\n    performance?: VectorTilePerformanceOptions;\n}\nexport interface VectorTileUpdateContext {\n    renderMode?: '2d' | '3d';\n    zoom?: number;\n}\nexport declare class VectorTileLayer extends LayerBase implements ITileLayer, IFeatureLayer {\n    readonly type: \"vector-tile\";\n    private _url;\n    private readonly _sourceLayer?;\n    private readonly _minZoom;\n    private readonly _maxZoom;\n    private _style;\n    private _renderer?;\n    private _performance;\n    private _vtManager;\n    private _visibleTiles;\n    private _publicFeatures;\n    private _publicFeaturesDirty;\n    /** Sorted tile key string for skip-if-unchanged optimization. */\n    private _lastTileKeySet;\n    /** Set when a background tile fetch completes — forces re-check even if tile coords unchanged. */\n    private _tilesDirty;\n    constructor(options: VectorTileLayerOptions);\n    get url(): string;\n    get sourceLayer(): string | undefined;\n    get minZoom(): number;\n    get maxZoom(): number;\n    get style(): VectorTileStyle;\n    set style(value: VectorTileStyle);\n    /** IRenderer for data-driven symbology */\n    get renderer(): IRenderer | undefined;\n    set renderer(value: IRenderer | undefined);\n    get performance(): VectorTilePerformanceOptions;\n    set performance(value: VectorTilePerformanceOptions);\n    getFeatures(): readonly Feature[];\n    getVisibleRenderTiles(): readonly ParsedVectorTile[];\n    /** Update cached features for the current visible tile coordinates. */\n    updateVisibleTiles(coords: {\n        z: number;\n        x: number;\n        y: number;\n    }[], context?: VectorTileUpdateContext): void;\n    protected onLoad(): Promise<void>;\n    /** Fetch TileJSON and extract the actual tile URL template */\n    private _resolveTileJson;\n    refresh(): void;\n    destroy(): void;\n    getTileUrl(z: number, x: number, y: number): string;\n    isZoomValid(z: number): boolean;\n    get fullExtent(): Extent | undefined;\n    private validateUrl;\n}\n//# sourceMappingURL=VectorTileLayer.d.ts.map","VectorTileManager.d.ts":"/**\n * VectorTileManager — PBF tile fetch + parse + optional worker/WASM pipeline\n *\n * Supports:\n * - Legacy JS parse path (main thread)\n * - Worker-driven parse + binary payload generation (3D path)\n * - Auto fallback to legacy when worker/WASM is unavailable\n * - Priority queue + back-pressure + stale result discard\n */\nimport { type IRenderer, type VectorTilePerformanceOptions } from '@mapgpu/core';\nimport { type ParsedVectorTile } from './mvt-parser';\ninterface TileCoord {\n    z: number;\n    x: number;\n    y: number;\n}\nexport interface VectorTileGetReadyContext {\n    renderMode?: '2d' | '3d';\n    zoom?: number;\n    renderer?: IRenderer;\n    performance?: VectorTilePerformanceOptions;\n}\nexport interface VectorTileManagerOptions {\n    performance?: VectorTilePerformanceOptions;\n}\nexport declare class VectorTileManager {\n    private _cache;\n    private _maxEntries;\n    private _tileVersion;\n    private _queue;\n    private _queuedKeys;\n    private _inFlightKeys;\n    private _activeTasks;\n    private _effectiveMaxInFlightTiles;\n    private _visibleKeys;\n    private _performance;\n    private _workerPool;\n    private _workerDisabled;\n    private _warnedFallback;\n    /** Debounce timer for batching multiple tile load notifications. */\n    private _notifyTimer;\n    /** Callback fired when a background tile fetch completes. Triggers re-render. */\n    onTileLoaded: ((tileKey: string) => void) | null;\n    constructor(options?: VectorTileManagerOptions);\n    setPerformance(options?: VectorTilePerformanceOptions): void;\n    /**\n     * Get ready parsed tiles for the given tile coordinates.\n     *\n     * Returns immediately available (cached) tiles. Missing tiles are queued\n     * for background fetch/parse and become visible after `onTileLoaded`.\n     */\n    getReadyTiles(tileCoords: TileCoord[], urlTemplate: string, sourceLayer?: string, context?: VectorTileGetReadyContext): ParsedVectorTile[];\n    /** Clear all cached and queued tiles. */\n    clear(): void;\n    /** Release worker resources permanently for this manager instance. */\n    destroy(): void;\n    private _drainQueue;\n    private _processTask;\n    private _parseTileInWorker;\n    private _shouldUseWorkerPath;\n    private _ensureWorkerPool;\n    private _disableWorkerPath;\n    private _computeEffectiveMaxInFlight;\n    private _pruneQueuedInvisible;\n    private _debouncedNotify;\n    /** LRU eviction: remove oldest entries when cache exceeds max size. */\n    private _evict;\n}\nexport {};\n//# sourceMappingURL=VectorTileManager.d.ts.map","VideoOverlay.d.ts":"/**\n * VideoOverlay — Display a video element at geographic bounds (Leaflet L.videoOverlay equivalent).\n */\nimport type { Extent } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nexport interface VideoOverlayOptions extends LayerBaseOptions {\n    /** URL(s) of the video. Multiple for format fallback. */\n    url: string | string[];\n    /** Geographic bounds as [minLon, minLat, maxLon, maxLat] in EPSG:4326. */\n    bounds: [number, number, number, number];\n    /** Autoplay the video. Default: true. */\n    autoplay?: boolean;\n    /** Loop the video. Default: true. */\n    loop?: boolean;\n    /** Mute the video. Default: true. */\n    muted?: boolean;\n}\nexport declare class VideoOverlay extends LayerBase {\n    readonly type = \"video-overlay\";\n    readonly bounds: [number, number, number, number];\n    private _video;\n    private _urls;\n    private _autoplay;\n    private _loop;\n    private _muted;\n    constructor(options: VideoOverlayOptions);\n    protected onLoad(): Promise<void>;\n    /** Get the underlying HTMLVideoElement. */\n    get videoElement(): HTMLVideoElement | null;\n    /** Play the video. */\n    play(): void;\n    /** Pause the video. */\n    pause(): void;\n    get fullExtent(): Extent | undefined;\n}\n//# sourceMappingURL=VideoOverlay.d.ts.map","WGSLLayer.d.ts":"/**\n * WGSLLayer — Parametric custom WGSL shader layer.\n *\n * Allows users to supply their own vertex/fragment WGSL shaders,\n * vertex buffers, index buffers, custom uniforms, and textures.\n * Camera and frame uniforms (time, deltaTime, frameNumber, opacity)\n * are automatically injected via preamble.\n *\n * @example\n * ```ts\n * const layer = new WGSLLayer({\n *   vertexShader: `\n *     @vertex fn vs_main(@location(0) pos: vec2<f32>) -> @builtin(position) vec4<f32> {\n *       return camera.viewProjection * vec4<f32>(pos, 0.0, 1.0);\n *     }\n *   `,\n *   fragmentShader: `\n *     @fragment fn fs_main() -> @location(0) vec4<f32> {\n *       return vec4<f32>(1.0, 0.0, 0.0, frame.opacity);\n *     }\n *   `,\n *   vertexBufferLayouts: [{\n *     arrayStride: 8,\n *     attributes: [{ shaderLocation: 0, offset: 0, format: 'float32x2' }],\n *   }],\n *   animated: true,\n * });\n * ```\n */\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nimport type { ICustomShaderLayer, CustomVertexBufferLayout, CustomDrawCommand, CustomTextureBinding } from '@mapgpu/core';\nexport interface WGSLLayerOptions extends LayerBaseOptions {\n    /** WGSL vertex shader body (preamble auto-injected unless rawMode) */\n    vertexShader: string;\n    /** WGSL fragment shader body (preamble auto-injected unless rawMode) */\n    fragmentShader: string;\n    /** Vertex buffer layout descriptors */\n    vertexBufferLayouts: CustomVertexBufferLayout[];\n    /** Whether the layer animates continuously (forces re-render every frame) */\n    animated?: boolean;\n    /** If true, the shader source is used as-is without preamble injection */\n    rawMode?: boolean;\n    /** GPU blend state for the render pipeline */\n    blendState?: GPUBlendState;\n    /** Primitive topology. Default: 'triangle-list' */\n    topology?: GPUPrimitiveTopology;\n}\nexport declare class WGSLLayer extends LayerBase implements ICustomShaderLayer {\n    readonly type = \"custom-shader\";\n    readonly vertexShader: string;\n    readonly fragmentShader: string;\n    readonly vertexBufferLayouts: CustomVertexBufferLayout[];\n    readonly animated: boolean;\n    readonly rawMode: boolean;\n    readonly blendState?: GPUBlendState;\n    private _vertexBuffers;\n    private _indexBuffer;\n    private _indexFormat;\n    private _customUniforms;\n    private _textures;\n    private _drawParams;\n    private _topology;\n    constructor(options: WGSLLayerOptions);\n    setVertexBuffer(index: number, buffer: GPUBuffer): void;\n    setIndexBuffer(buffer: GPUBuffer, format?: GPUIndexFormat): void;\n    setCustomUniforms(data: ArrayBuffer | Float32Array): void;\n    setTexture(texture: GPUTexture, samplerDesc?: GPUSamplerDescriptor): void;\n    setDrawParams(params: {\n        vertexCount?: number;\n        instanceCount?: number;\n        indexCount?: number;\n    }): void;\n    requestRender(): void;\n    getVertexBuffers(): GPUBuffer[];\n    getIndexBuffer(): GPUBuffer | null;\n    getCustomUniforms(): ArrayBuffer | null;\n    getTextures(): CustomTextureBinding[];\n    getDrawCommand(): CustomDrawCommand;\n    protected onLoad(): Promise<void>;\n}\n//# sourceMappingURL=WGSLLayer.d.ts.map","WMSLayer.d.ts":"/**\n * WMSLayer\n *\n * Wraps a WMS service as a map layer. Delegates to WmsAdapter from\n * @mapgpu/adapters-ogc for capabilities fetching and URL building.\n */\nimport type { IMapImageryAdapter, MapImageryLayerInfo, FeatureInfoResult, Extent } from '@mapgpu/core';\nimport { LayerBase } from './LayerBase';\nimport type { LayerBaseOptions } from './LayerBase';\nexport interface WMSLayerOptions extends LayerBaseOptions {\n    /** WMS service base URL */\n    url: string;\n    /** Layer name(s) to display */\n    layers: string[];\n    /** Image format. Defaults to 'image/png'. */\n    format?: string;\n    /** Request transparent tiles. Defaults to true. */\n    transparent?: boolean;\n    /** Coordinate reference system. Auto-negotiated from capabilities if omitted. */\n    crs?: string;\n    /** Optional proxy URL prefix */\n    proxyUrl?: string;\n    /** Vendor-specific WMS parameters */\n    vendorParams?: Record<string, string>;\n    /** Injected adapter (for testing or custom implementations) */\n    adapter?: IMapImageryAdapter;\n}\nexport declare class WMSLayer extends LayerBase {\n    readonly type: \"wms\";\n    readonly url: string;\n    readonly layerNames: string[];\n    readonly format: string;\n    readonly transparent: boolean;\n    readonly crs?: string;\n    readonly proxyUrl?: string;\n    readonly vendorParams?: Record<string, string>;\n    private adapter;\n    private capabilities;\n    private layerInfos;\n    constructor(options: WMSLayerOptions);\n    protected onLoad(): Promise<void>;\n    /**\n     * Compute the combined extent of all configured layer names\n     * from capabilities metadata.\n     */\n    private computeExtent;\n    /**\n     * Build a GetMap tile URL for the given extent and pixel dimensions.\n     */\n    getTileUrl(extent: Extent, width: number, height: number): string;\n    /**\n     * Execute a GetFeatureInfo query at the given pixel position.\n     */\n    getFeatureInfo(x: number, y: number, extent: Extent, width: number, height: number): Promise<FeatureInfoResult>;\n    /**\n     * Get the layer metadata from capabilities.\n     */\n    getLayerInfos(): readonly MapImageryLayerInfo[];\n}\n//# sourceMappingURL=WMSLayer.d.ts.map","circle-marker.d.ts":"/**\n * circle-marker — Leaflet CircleMarker → mapgpu PointSymbol factory.\n */\nimport type { PointSymbol } from '@mapgpu/core';\nexport interface CircleMarkerOptions {\n    fillColor?: [number, number, number, number];\n    fillOpacity?: number;\n    strokeColor?: [number, number, number, number];\n    strokeWeight?: number;\n    radius?: number;\n}\nexport declare function createCircleMarkerSymbol(options?: CircleMarkerOptions): PointSymbol;\n//# sourceMappingURL=circle-marker.d.ts.map","index.d.ts":"/**\n * @mapgpu/layers\n *\n * Layer implementations — WMSLayer, GeoJSONLayer, RasterTileLayer,\n * GraphicsLayer, FeatureLayer, HeatmapLayer,\n * ClusterLayer, VectorTileLayer, AnimatedLayer.\n */\nexport { LayerBase } from './LayerBase';\nexport type { LayerBaseOptions } from './LayerBase';\nexport { WMSLayer } from './WMSLayer';\nexport type { WMSLayerOptions } from './WMSLayer';\nexport { GeoJSONLayer } from './GeoJSONLayer';\nexport type { GeoJSONLayerOptions } from './GeoJSONLayer';\nexport { RasterTileLayer } from './RasterTileLayer';\nexport type { RasterTileLayerOptions } from './RasterTileLayer';\nexport { GraphicsLayer } from './GraphicsLayer';\nexport type { GraphicsLayerOptions } from './GraphicsLayer';\nexport { FeatureLayer } from './FeatureLayer';\nexport type { FeatureLayerOptions, FeatureAdapterFactory } from './FeatureLayer';\nexport { HeatmapLayer } from './HeatmapLayer';\nexport type { HeatmapLayerOptions, GradientStop } from './HeatmapLayer';\nexport { ClusterLayer } from './ClusterLayer';\nexport type { ClusterLayerOptions, ClusterFeature, AggregationType, AggregationFields, } from './ClusterLayer';\nexport { VectorTileLayer } from './VectorTileLayer';\nexport type { VectorTileLayerOptions, VectorTileStyle } from './VectorTileLayer';\nexport { parseMvt } from './mvt-parser';\nexport { AnimatedLayer } from './AnimatedLayer';\nexport type { AnimatedLayerOptions, PlaybackState } from './AnimatedLayer';\nexport { GpuClusterLayer } from './GpuClusterLayer';\nexport type { GpuClusterLayerOptions } from './GpuClusterLayer';\nexport { WGSLLayer } from './WGSLLayer';\nexport type { WGSLLayerOptions } from './WGSLLayer';\nexport { createCircleMarkerSymbol } from './circle-marker';\nexport type { CircleMarkerOptions } from './circle-marker';\nexport { DynamicPointLayer } from './DynamicPointLayer';\nexport type { DynamicPointLayerOptions } from './DynamicPointLayer';\nexport { ImageOverlay } from './ImageOverlay';\nexport type { ImageOverlayOptions } from './ImageOverlay';\nexport { VideoOverlay } from './VideoOverlay';\nexport type { VideoOverlayOptions } from './VideoOverlay';\nexport { LayerGroup } from './LayerGroup';\nexport type { LayerGroupOptions } from './LayerGroup';\n//# sourceMappingURL=index.d.ts.map","mvt-parser.d.ts":"/**\n * MVT Parser — Mapbox Vector Tile (MVT) protobuf decoder\n *\n * Implements MVT spec 2.1 from scratch using only the `pbf` library\n * for protobuf wire format decoding (varint, string, bytes).\n * All MVT semantic parsing (layers, features, geometry commands) is our code.\n *\n * Protobuf wire format (pbf library):\n *   ArrayBuffer → varint, string, float, double, packed arrays\n *\n * MVT semantic parsing (our code):\n *   Tile { layers[] } → Layer { name, keys[], values[], features[], extent }\n *   Feature { id, tags[], type, geometry[] } → decoded rings/points\n *   Geometry commands → lon/lat GeoJSON coordinates\n */\nimport type { Feature, Geometry, GeometryType, VectorTileBinaryPayload } from '@mapgpu/core';\nexport interface ProjectedGeometry {\n    type: GeometryType;\n    coordinates: number[] | number[][] | number[][][] | number[][][][];\n    spatialReference: 'EPSG:3857';\n}\nexport interface ProjectedFeature {\n    id: string | number;\n    geometry: ProjectedGeometry;\n    attributes: Record<string, unknown>;\n}\nexport interface ParsedVectorTile {\n    key: string;\n    z: number;\n    x: number;\n    y: number;\n    sourceLayer?: string;\n    features: ProjectedFeature[];\n    binaryPayload?: VectorTileBinaryPayload | null;\n    version: number;\n}\n/**\n * Decode MVT geometry commands into rings of [x, y] tile-local coordinates.\n *\n * Commands are encoded as: commandInteger = (count << 3) | cmdId\n * - id=1 → MoveTo (starts a new ring/point)\n * - id=2 → LineTo (adds points to current ring)\n * - id=7 → ClosePath (closes current ring)\n *\n * Coordinates use zigzag encoding with delta accumulation.\n * Zigzag decode: (value >>> 1) ^ -(value & 1)\n */\ndeclare function decodeGeometry(geometry: number[]): number[][][];\n/** Zigzag decode: (n >>> 1) ^ -(n & 1) */\ndeclare function zigzag(n: number): number;\n/**\n * Convert tile-local coordinates to [longitude, latitude].\n *\n * In MVT, coordinates are in tile-local space where:\n * - (0, 0) is top-left corner of the tile\n * - (extent, extent) is bottom-right corner\n *\n * Conversion to lon/lat:\n *   lon = (tileX + px / extent) / 2^z * 360 - 180\n *   lat = atan(sinh(PI * (1 - 2 * (tileY + py / extent) / 2^z))) * 180 / PI\n */\ndeclare function tileToLonLat(px: number, py: number, z: number, x: number, y: number, extent: number): [number, number];\ndeclare function tileToMercator(px: number, py: number, z: number, x: number, y: number, extent: number): [number, number];\ndeclare function mercatorToLonLat(mx: number, my: number): [number, number];\nexport declare function projectedFeatureToPublicFeature(feature: ProjectedFeature): Feature;\nexport declare function projectedGeometryToPublicGeometry(geometry: ProjectedGeometry): Geometry;\n/**\n * Signed area of a ring in tile coordinates.\n * Positive = CW (outer ring in MVT), Negative = CCW (hole).\n */\ndeclare function signedArea(ring: number[][]): number;\n/**\n * Parse an MVT (Mapbox Vector Tile) protobuf binary into GeoJSON-style features.\n *\n * @param data - Raw PBF binary data (ArrayBuffer)\n * @param z - Tile zoom level\n * @param x - Tile column\n * @param y - Tile row\n * @param sourceLayer - Optional: only parse features from this layer name\n * @returns Array of Feature objects with lon/lat coordinates\n */\nexport declare function parseMvtTile(data: ArrayBuffer, z: number, x: number, y: number, sourceLayer?: string): ParsedVectorTile;\nexport declare function parseMvt(data: ArrayBuffer, z: number, x: number, y: number, sourceLayer?: string): Feature[];\nexport { decodeGeometry, zigzag, signedArea, tileToLonLat, tileToMercator, mercatorToLonLat };\n//# sourceMappingURL=mvt-parser.d.ts.map","spider-layout.d.ts":"/**\n * Spider Layout — Radial position calculator for cluster expansion.\n *\n * Given a cluster center and member count, computes radial offsets\n * for each member so they fan out from the center without overlapping.\n *\n * Ring 1: up to 10 members at radius = legLength\n * Ring 2: next 20 members at radius = legLength × 1.8\n * Max 50 members displayed (performance cap).\n */\nexport interface SpiderOffset {\n    dx: number;\n    dy: number;\n}\n/**\n * Compute radial spider offsets from a center point.\n *\n * @param centerX     Center X coordinate (not used in offset calc — for context).\n * @param centerY     Center Y coordinate (not used in offset calc — for context).\n * @param memberCount Number of members to place.\n * @param legLength   Radius for the first ring (in same units as center coords).\n * @returns Array of {dx, dy} offsets relative to (0, 0).\n */\nexport declare function computeSpiderPositions(_centerX: number, _centerY: number, memberCount: number, legLength: number): SpiderOffset[];\n//# sourceMappingURL=spider-layout.d.ts.map","vector-tile-renderer-snapshot.d.ts":"import type { IRenderer, SerializableRendererSnapshot } from '@mapgpu/core';\nexport declare function createSerializableRendererSnapshot(renderer?: IRenderer): SerializableRendererSnapshot | null;\n//# sourceMappingURL=vector-tile-renderer-snapshot.d.ts.map","vector-tile-worker-protocol.d.ts":"import type { VectorTileBinaryPayload, VectorTileWorkerRequest, VectorTileWorkerResponse } from '@mapgpu/core';\nexport declare const VECTOR_TILE_WORKER_TASK = \"vector-tile:parse-build\";\nexport type { VectorTileWorkerRequest, VectorTileWorkerResponse };\nexport declare function collectVectorTilePayloadTransferables(payload: VectorTileBinaryPayload | null): Transferable[];\n//# sourceMappingURL=vector-tile-worker-protocol.d.ts.map","vector-tile.worker.d.ts":"export {};\n//# sourceMappingURL=vector-tile.worker.d.ts.map"},"@mapgpu/widgets":{"AttributionWidget.d.ts":"/**\n * AttributionWidget — Displays data source attribution text (Leaflet L.control.attribution equivalent).\n */\nimport type { IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface AttributionWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    prefix?: string | false;\n}\nexport declare class AttributionWidget extends WidgetBase {\n    private _prefix;\n    private _attributions;\n    private _contentEl;\n    constructor(options?: AttributionWidgetOptions);\n    addAttribution(text: string): this;\n    removeAttribution(text: string): this;\n    setPrefix(prefix: string | false): this;\n    protected render(root: HTMLElement): void;\n    private _updateContent;\n    protected onViewBound(_view: IView): void;\n}\n//# sourceMappingURL=AttributionWidget.d.ts.map","BasemapGalleryWidget.d.ts":"/**\n * BasemapGalleryWidget — Displays a gallery of basemap options with thumbnails.\n *\n * Users can click to switch the active basemap.\n */\nimport type { IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface BasemapItem {\n    /** Unique basemap identifier */\n    id: string;\n    /** Display title */\n    title: string;\n    /** Thumbnail data URL or placeholder color */\n    thumbnailUrl?: string;\n}\nexport interface BasemapGalleryWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    basemaps?: BasemapItem[];\n    activeBasemapId?: string;\n}\nexport declare class BasemapGalleryWidget extends WidgetBase {\n    private _basemaps;\n    private _activeBasemapId;\n    private _galleryEl;\n    private _selectionHandlers;\n    constructor(options?: BasemapGalleryWidgetOptions);\n    get basemaps(): ReadonlyArray<BasemapItem>;\n    get activeBasemapId(): string | null;\n    setBasemaps(basemaps: BasemapItem[]): void;\n    selectBasemap(basemapId: string): void;\n    onSelect(handler: (basemap: BasemapItem) => void): void;\n    offSelect(handler: (basemap: BasemapItem) => void): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _rebuildGallery;\n}\n//# sourceMappingURL=BasemapGalleryWidget.d.ts.map","CoordinatesWidget.d.ts":"/**\n * CoordinatesWidget — Displays cursor coordinates on the map.\n *\n * Supports DD (decimal degrees), DMS (degrees-minutes-seconds), and MGRS formats.\n * Listens to mouse move events on the bound container.\n */\nimport type { IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport type CoordinateFormat = 'DD' | 'DMS' | 'MGRS';\nexport interface CoordinatesWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    format?: CoordinateFormat;\n}\nexport declare class CoordinatesWidget extends WidgetBase {\n    private _format;\n    private _spanEl;\n    private _mouseMoveHandler;\n    private _mouseTarget;\n    /** Current longitude in decimal degrees */\n    private _lon;\n    /** Current latitude in decimal degrees */\n    private _lat;\n    /**\n     * Optional function to convert pixel coordinates to map coordinates.\n     * If not provided, the widget displays pixel coordinates.\n     * Signature: (pixelX, pixelY) => [longitude, latitude]\n     */\n    screenToMap: ((x: number, y: number) => [number, number]) | null;\n    constructor(options?: CoordinatesWidgetOptions);\n    get format(): CoordinateFormat;\n    set format(value: CoordinateFormat);\n    get longitude(): number;\n    get latitude(): number;\n    /**\n     * Manually set coordinates (useful for programmatic updates).\n     */\n    setCoordinates(lon: number, lat: number): void;\n    /**\n     * Start listening to mouse move events on a target element.\n     */\n    listenTo(target: HTMLElement): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(view: IView): void;\n    protected onDestroy(): void;\n    private _removeMouseListener;\n    private _updateDisplay;\n    private _formatCoordinates;\n    /**\n     * Format as Decimal Degrees: \"28.9784° E, 41.0082° N\"\n     */\n    static formatDD(lon: number, lat: number): string;\n    /**\n     * Format as Degrees-Minutes-Seconds: \"28° 58' 42.24\" E, 41° 0' 29.52\" N\"\n     */\n    static formatDMS(lon: number, lat: number): string;\n    /**\n     * Simplified MGRS-like format.\n     * Full MGRS requires UTM zone + grid square calculation.\n     * This provides a simplified representation for display purposes.\n     */\n    static formatMGRS(lon: number, lat: number): string;\n    private static _toDMS;\n}\n//# sourceMappingURL=CoordinatesWidget.d.ts.map","DTEDUploadWidget.d.ts":"/**\n * DTEDUploadWidget — Drag-Drop Elevation Data Upload\n *\n * WidgetBase extension for uploading .dt0/.dt1/.dt2 files via drag-drop.\n * Parses DTED binary → emits events for DTEDStore integration.\n *\n * ┌─────────────────────────────┐\n * │ ◈ Elevation Data            │\n * ├─────────────────────────────┤\n * │ ┌─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐ │\n * │ │  .dt1/.dt2 dosyaları    │ │  ← Drop zone\n * │ │   sürükle-bırak        │ │\n * │ └─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘ │\n * │  [Dosya Seç]                │\n * ├─────────────────────────────┤\n * │  N39E032  0-2450m    [×]   │  ← Yüklenen tile listesi\n * │  N39E033  120-3200m  [×]   │\n * ├─────────────────────────────┤\n * │  2 tile | 50 MB             │\n * └─────────────────────────────┘\n */\nimport type { IView } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface DTEDUploadWidgetOptions {\n    id?: string;\n    position?: 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'manual';\n    /** Maximum file size in bytes (default: 100MB) */\n    maxFileSize?: number;\n    /** Callback when a file is parsed. Receives ArrayBuffer + fileName. */\n    onFileLoaded?: (buffer: ArrayBuffer, fileName: string) => void;\n    /** Callback when a tile is removed by ID. */\n    onTileRemoved?: (tileId: string) => void;\n}\ninterface UploadedTileInfo {\n    id: string;\n    fileName: string;\n    minElev: number;\n    maxElev: number;\n    sizeBytes: number;\n}\nexport declare class DTEDUploadWidget extends WidgetBase {\n    private _maxFileSize;\n    private _onFileLoaded?;\n    private _onTileRemoved?;\n    private _uploadedTiles;\n    private _dropZone;\n    private _fileInput;\n    private _tileList;\n    private _statsEl;\n    constructor(options?: DTEDUploadWidgetOptions);\n    protected render(root: HTMLElement): void;\n    /**\n     * Add a tile entry to the displayed list.\n     * Called externally after parsing succeeds.\n     */\n    addTileEntry(info: UploadedTileInfo): void;\n    /**\n     * Remove a tile entry by ID.\n     */\n    removeTileEntry(tileId: string): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _handleFiles;\n    private _renderTileList;\n    private _updateStats;\n}\nexport {};\n//# sourceMappingURL=DTEDUploadWidget.d.ts.map","DockPanel.d.ts":"/**\n * DockPanel — Organizes widgets in a panel layout with four dock positions.\n *\n * Supports collapsible panels (left, right, top, bottom).\n */\nimport type { IWidget, IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport type DockPosition = 'left' | 'right' | 'top' | 'bottom';\nexport interface DockPanelOptions {\n    id?: string;\n    position?: WidgetPosition;\n}\nexport declare class DockPanel extends WidgetBase {\n    private _panels;\n    private _collapsed;\n    private _panelEls;\n    private _toggleBtns;\n    constructor(options?: DockPanelOptions);\n    get widgets(): ReadonlyArray<{\n        widget: IWidget;\n        dockPosition: DockPosition;\n    }>;\n    addWidget(widget: IWidget, dockPosition: DockPosition): void;\n    removeWidget(widget: IWidget | string): void;\n    isCollapsed(dockPosition: DockPosition): boolean;\n    togglePanel(dockPosition: DockPosition): void;\n    collapsePanel(dockPosition: DockPosition): void;\n    expandPanel(dockPosition: DockPosition): void;\n    getWidgetsAt(dockPosition: DockPosition): IWidget[];\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _rebuildPanelContent;\n    private _getToggleLabel;\n    private _updateToggleBtn;\n}\n//# sourceMappingURL=DockPanel.d.ts.map","DrawToolbarWidget.d.ts":"/**\n * DrawToolbarWidget — UI toolbar for drawing/editing tools.\n *\n * Displays tool buttons (Point, Polyline, Polygon, Edit) with active state.\n * Binds to ToolManager for activation/deactivation and undo/redo.\n */\nimport type { ToolManager, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface DrawToolbarWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n}\nexport declare class DrawToolbarWidget extends WidgetBase {\n    private _toolManager;\n    private _toolButtons;\n    private _undoBtn;\n    private _redoBtn;\n    private _onToolActivate;\n    private _onToolDeactivate;\n    private _onHistoryChange;\n    constructor(options?: DrawToolbarWidgetOptions);\n    /**\n     * Bind the toolbar to a ToolManager instance.\n     */\n    bindToolManager(tm: ToolManager): void;\n    protected render(root: HTMLElement): void;\n    protected onDestroy(): void;\n    private _createToolButton;\n    private _createActionButton;\n    private _updateActiveState;\n    private _updateUndoRedo;\n}\n//# sourceMappingURL=DrawToolbarWidget.d.ts.map","LOSWidget.d.ts":"/**\n * LOSWidget — Line of Sight analysis control.\n *\n * Dark-themed, compact widget with:\n * - Observer/target coordinate display\n * - Height offset sliders with number inputs\n * - Visible/Blocked status badge\n * - Canvas2D elevation profile chart\n * - LosTool bidirectional binding\n */\nimport type { LosAnalysisResult, IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface LOSWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n}\nexport interface LOSObserverTarget {\n    observer: [number, number];\n    target: [number, number];\n    observerOffset: number;\n    targetOffset: number;\n}\n/** Minimal LosTool interface for binding (avoids hard dependency on @mapgpu/tools). */\ninterface ILosTool {\n    readonly observer: [number, number] | null;\n    readonly target: [number, number] | null;\n    readonly observerOffset: number;\n    readonly targetOffset: number;\n    setObserverOffset(meters: number): void;\n    setTargetOffset(meters: number): void;\n}\n/** Minimal event source for tool events. */\ninterface IToolEventSource {\n    on(event: 'los-update', handler: (data: {\n        observer: [number, number];\n        target: [number, number];\n        observerOffset: number;\n        targetOffset: number;\n        result: LosAnalysisResult;\n    }) => void): void;\n    on(event: 'los-clear', handler: (data: {\n        toolId: string;\n    }) => void): void;\n}\nexport declare class LOSWidget extends WidgetBase {\n    private _observer;\n    private _target;\n    private _observerOffset;\n    private _targetOffset;\n    private _result;\n    private _observerInput;\n    private _targetInput;\n    private _observerOffsetInput;\n    private _targetOffsetInput;\n    private _observerSlider;\n    private _targetSlider;\n    private _resultEl;\n    private _profileCanvas;\n    private _pickBtn;\n    private _runHandlers;\n    private _boundTool;\n    private _losUpdateHandler;\n    private _losClearHandler;\n    constructor(options?: LOSWidgetOptions);\n    get observer(): [number, number] | null;\n    get target(): [number, number] | null;\n    get observerOffset(): number;\n    get targetOffset(): number;\n    get result(): LosAnalysisResult | null;\n    setObserver(lon: number, lat: number): void;\n    setTarget(lon: number, lat: number): void;\n    setObserverOffset(meters: number): void;\n    setTargetOffset(meters: number): void;\n    setResult(result: LosAnalysisResult): void;\n    clearResult(): void;\n    onRunLos(handler: (params: LOSObserverTarget) => void): void;\n    offRunLos(handler: (params: LOSObserverTarget) => void): void;\n    private _pickHandlers;\n    /** Register a callback for the \"Pick Points\" button. */\n    onPick(handler: () => void): void;\n    offPick(handler: () => void): void;\n    bindLosTool(tool: ILosTool, eventSource: IToolEventSource): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _createCoordSection;\n    private _createHeightRow;\n    private _onOffsetChange;\n    private _renderResult;\n    private _renderProfile;\n    private _clearProfileCanvas;\n    private _emitRun;\n}\nexport {};\n//# sourceMappingURL=LOSWidget.d.ts.map","LayerListWidget.d.ts":"/**\n * LayerListWidget — Displays layers with visibility toggle, opacity slider, and drag-reorder.\n *\n * Subscribes to layer add/remove events via the EventBus pattern.\n */\nimport type { ILayer, IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface LayerListEvents {\n    'layer-add': ILayer;\n    'layer-remove': ILayer;\n    'layer-reorder': {\n        layer: ILayer;\n        newIndex: number;\n    };\n}\ntype LayerListEventHandler<K extends keyof LayerListEvents> = (data: LayerListEvents[K]) => void;\nexport interface LayerListWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n}\nexport declare class LayerListWidget extends WidgetBase {\n    private _layers;\n    private _listEl;\n    private _layerEventCleanups;\n    private _listeners;\n    constructor(options?: LayerListWidgetOptions);\n    get layers(): ReadonlyArray<ILayer>;\n    addLayer(layer: ILayer): void;\n    removeLayer(layerOrId: ILayer | string): void;\n    reorderLayer(layerId: string, newIndex: number): void;\n    on<K extends keyof LayerListEvents>(event: K, handler: LayerListEventHandler<K>): void;\n    off<K extends keyof LayerListEvents>(event: K, handler: LayerListEventHandler<K>): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _subscribeLayerEvents;\n    private _updateLayerItem;\n    private _rebuildList;\n    private emitEvent;\n}\nexport {};\n//# sourceMappingURL=LayerListWidget.d.ts.map","MeasureToolbarWidget.d.ts":"/**\n * MeasureToolbarWidget — Ready-made toolbar for measurement tools.\n *\n * Provides tool buttons (Point, Distance, Area), unit selector dropdowns,\n * and Clear Last / Clear All buttons. Self-contained convenience wrapper\n * that internally creates UnitManager and MeasureLabelManager.\n *\n * Can also accept an externally provided UnitManager for shared control.\n */\nimport type { IView, ToolManager, WidgetPosition } from '@mapgpu/core';\nimport { UnitManager } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface MeasureToolbarWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    unitManager?: UnitManager;\n}\nexport declare class MeasureToolbarWidget extends WidgetBase {\n    private _toolManager;\n    private _unitManager;\n    private _ownsUnitManager;\n    private _toolButtons;\n    private _distUnitSelect;\n    private _coordFormatSelect;\n    private _onToolActivate;\n    private _onToolDeactivate;\n    constructor(options?: MeasureToolbarWidgetOptions);\n    get unitManager(): UnitManager;\n    /**\n     * Bind to a ToolManager instance.\n     * Listens for tool-activate/deactivate to sync button states.\n     */\n    bindToolManager(tm: ToolManager): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _createToolButton;\n    private _createActionButton;\n    private _createLabeledRow;\n    private _createSeparator;\n    private _updateActiveState;\n    private _clearLast;\n    private _clearAll;\n}\n//# sourceMappingURL=MeasureToolbarWidget.d.ts.map","MeasurementWidget.d.ts":"/**\n * MeasurementWidget — Distance and area measurement tool.\n *\n * Supports Haversine distance and Shoelace (geodesic) area calculations.\n * Unit conversion: m/km, ft/mi.\n */\nimport type { IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport type MeasurementMode = 'distance' | 'area' | 'none';\nexport type MeasurementUnit = 'metric' | 'imperial';\nexport interface MeasurementResult {\n    distance?: number;\n    area?: number;\n    unit: string;\n}\nexport interface MeasurementWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    mode?: MeasurementMode;\n    unit?: MeasurementUnit;\n}\n/**\n * Haversine distance between two points on Earth.\n * @returns distance in meters\n */\nexport declare function haversineDistance(lon1: number, lat1: number, lon2: number, lat2: number): number;\n/**\n * Shoelace formula for geodesic polygon area.\n * Uses spherical excess for approximate geodesic area.\n * @returns area in square meters\n */\nexport declare function sphericalPolygonArea(points: Array<[number, number]>): number;\nexport declare class MeasurementWidget extends WidgetBase {\n    private _mode;\n    private _unit;\n    private _points;\n    private _modeLabel;\n    private _resultLabel;\n    private _distBtn;\n    private _areaBtn;\n    constructor(options?: MeasurementWidgetOptions);\n    get mode(): MeasurementMode;\n    get unit(): MeasurementUnit;\n    set unit(value: MeasurementUnit);\n    get points(): ReadonlyArray<[number, number]>;\n    setMode(mode: MeasurementMode): void;\n    addPoint(lon: number, lat: number): void;\n    getResult(): MeasurementResult;\n    clear(): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _calculateTotalDistance;\n    private _updateModeHighlight;\n    private _updateDisplay;\n}\n//# sourceMappingURL=MeasurementWidget.d.ts.map","PopupWidget.d.ts":"/**\n * PopupWidget — DOM overlay popup anchored to map coordinates.\n *\n * Positions itself over the map canvas at a given [lon, lat] using\n * the view's toScreen() coordinate conversion. Repositions on each\n * 'view-change' event.\n */\nimport type { IView } from '@mapgpu/core';\nexport interface PopupOptions {\n    /** Anchor position as [longitude, latitude]. */\n    position: [number, number];\n    /** HTML content string or DOM element. */\n    content: string | HTMLElement;\n    /** Pixel offset from anchor [x, y]. Default [0, -12]. */\n    offset?: [number, number];\n    /** Maximum popup width in pixels. Default 300. */\n    maxWidth?: number;\n}\nexport declare class PopupWidget {\n    private _container;\n    private _view;\n    private _position;\n    private _offset;\n    private _isOpen;\n    private _viewChangeHandler;\n    constructor();\n    /** Whether the popup is currently visible. */\n    get isOpen(): boolean;\n    /** The popup DOM container element. */\n    get container(): HTMLElement;\n    /**\n     * Attach to a view (IView must have on/off/toScreen).\n     * The popup appends itself to the view's container parent.\n     */\n    attachTo(view: IView): void;\n    /** Open the popup at the given position with content. */\n    open(options: PopupOptions): void;\n    /** Close and hide the popup. */\n    close(): void;\n    /** Clean up DOM and event listeners. */\n    destroy(): void;\n    private _reposition;\n}\n//# sourceMappingURL=PopupWidget.d.ts.map","ScaleBarWidget.d.ts":"/**\n * ScaleBarWidget — Displays a scale bar based on current view extent/zoom.\n *\n * Supports metric (m/km) and imperial (ft/mi) units.\n */\nimport type { IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport type ScaleBarUnit = 'metric' | 'imperial' | 'dual';\nexport interface ScaleBarWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    unit?: ScaleBarUnit;\n    maxWidthPx?: number;\n}\nexport declare class ScaleBarWidget extends WidgetBase {\n    private _unit;\n    private _maxWidthPx;\n    private _barEl;\n    private _labelEl;\n    private _groundResolution;\n    private _viewChangeHandler;\n    constructor(options?: ScaleBarWidgetOptions);\n    get unit(): ScaleBarUnit;\n    set unit(value: ScaleBarUnit);\n    /**\n     * Update the scale bar based on ground resolution (meters per pixel).\n     * Should be called whenever view changes (zoom, extent, etc.).\n     */\n    setGroundResolution(metersPerPixel: number): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(view: IView): void;\n    protected onDestroy(): void;\n    private _removeViewListener;\n    private _updateDisplay;\n    private _renderMetric;\n    private _renderImperial;\n    /**\n     * Find the largest \"nice\" number that is <= maxValue.\n     * Exported as static for testability.\n     */\n    static findNiceNumber(maxValue: number): number;\n}\n//# sourceMappingURL=ScaleBarWidget.d.ts.map","SearchWidget.d.ts":"/**\n * SearchWidget — Text input with configurable search sources and debounced search.\n *\n * Displays results in a dropdown list. Emits select event when a result is chosen.\n */\nimport type { Extent, IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface SearchResult {\n    text: string;\n    location?: [number, number];\n    extent?: Extent;\n}\nexport interface SearchSource {\n    name: string;\n    search(query: string): Promise<SearchResult[]>;\n}\nexport interface SearchWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    placeholder?: string;\n    sources: SearchSource[];\n}\nexport declare class SearchWidget extends WidgetBase {\n    private _sources;\n    private _placeholder;\n    private _inputEl;\n    private _dropdownEl;\n    private _results;\n    private _debounceTimer;\n    private _selectHandlers;\n    constructor(options: SearchWidgetOptions);\n    get results(): ReadonlyArray<SearchResult>;\n    onSelect(handler: (result: SearchResult) => void): void;\n    offSelect(handler: (result: SearchResult) => void): void;\n    /**\n     * Programmatically trigger a search.\n     */\n    search(query: string): Promise<SearchResult[]>;\n    /**\n     * Clear search results and input.\n     */\n    clear(): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _onInput;\n    private _renderDropdown;\n    private _emitSelect;\n}\n//# sourceMappingURL=SearchWidget.d.ts.map","SelectionInspectorWidget.d.ts":"/**\n * SelectionInspectorWidget — Displays selected feature attributes in a table format.\n *\n * Supports pagination (max 10 per page), clear selection, and feature count display.\n */\nimport type { Feature, IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface SelectionInspectorWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    pageSize?: number;\n}\nexport declare class SelectionInspectorWidget extends WidgetBase {\n    private _features;\n    private _pageSize;\n    private _currentPage;\n    private _tableEl;\n    private _paginationEl;\n    private _countEl;\n    private _clearHandlers;\n    constructor(options?: SelectionInspectorWidgetOptions);\n    get features(): ReadonlyArray<Feature>;\n    get currentPage(): number;\n    get totalPages(): number;\n    get pageSize(): number;\n    setFeatures(features: Feature[]): void;\n    clearSelection(): void;\n    goToPage(page: number): void;\n    nextPage(): void;\n    prevPage(): void;\n    onClear(handler: () => void): void;\n    offClear(handler: () => void): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _rebuild;\n    private _updateCount;\n    private _rebuildTable;\n    private _rebuildPagination;\n}\n//# sourceMappingURL=SelectionInspectorWidget.d.ts.map","TimeSliderWidget.d.ts":"/**\n * TimeSliderWidget — Temporal dimension control with play/pause/stop and speed settings.\n *\n * Uses HTML range input for time navigation.\n * Supports autoplay with configurable speed multiplier.\n */\nimport type { IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface TimeSliderWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    min: Date;\n    max: Date;\n    value?: Date;\n    step?: number;\n}\nexport type PlaybackSpeed = 1 | 2 | 4;\nexport declare class TimeSliderWidget extends WidgetBase {\n    private _min;\n    private _max;\n    private _value;\n    private _step;\n    private _speed;\n    private _playing;\n    private _playTimer;\n    private _sliderEl;\n    private _labelEl;\n    private _playBtn;\n    private _speedLabel;\n    private _timeChangeHandlers;\n    constructor(options: TimeSliderWidgetOptions);\n    get min(): Date;\n    get max(): Date;\n    get value(): Date;\n    get speed(): PlaybackSpeed;\n    get playing(): boolean;\n    setValue(date: Date): void;\n    setSpeed(speed: PlaybackSpeed): void;\n    play(): void;\n    pause(): void;\n    stop(): void;\n    onTimeChange(handler: (date: Date) => void): void;\n    offTimeChange(handler: (date: Date) => void): void;\n    protected render(root: HTMLElement): void;\n    protected onViewBound(_view: IView): void;\n    protected onDestroy(): void;\n    private _startTimer;\n    private _stopTimer;\n    private _tick;\n    private _updateSlider;\n    private _updateLabel;\n    private _emitTimeChange;\n}\n//# sourceMappingURL=TimeSliderWidget.d.ts.map","TooltipWidget.d.ts":"/**\n * TooltipWidget — Lightweight tooltip overlays.\n *\n * Supports two modes:\n * - Permanent: pinned at a geographic coordinate, repositioned on view-change\n * - Sticky: follows the mouse cursor at a fixed pixel offset\n */\nimport type { IView } from '@mapgpu/core';\nexport interface TooltipOptions {\n    /** Pixel offset from anchor [x, y]. Default [10, 0]. */\n    offset?: [number, number];\n    /** CSS class to apply to the tooltip element. */\n    className?: string;\n}\nexport declare class TooltipWidget {\n    private _parent;\n    private _view;\n    private _permanents;\n    private _stickyEl;\n    /** Whether the sticky tooltip is currently visible. */\n    stickyVisible: boolean;\n    private _viewChangeHandler;\n    constructor();\n    /** Attach to a view for coordinate conversion and view-change events. */\n    attachTo(view: IView): void;\n    /** Add a permanent tooltip pinned at a geographic coordinate. */\n    addPermanent(id: string, content: string, coordinate: [number, number], options?: TooltipOptions): void;\n    /** Remove a permanent tooltip by id. */\n    removePermanent(id: string): void;\n    /** Show a sticky tooltip at screen coordinates (follows mouse). */\n    showSticky(content: string, screenX: number, screenY: number): void;\n    /** Hide the sticky tooltip. */\n    hideSticky(): void;\n    /** Clean up all tooltips and event listeners. */\n    destroy(): void;\n    private _repositionAll;\n    private _repositionOne;\n}\n//# sourceMappingURL=TooltipWidget.d.ts.map","WidgetBase.d.ts":"/**\n * WidgetBase — All widgets extend this base class.\n *\n * Implements IWidget interface with common lifecycle:\n * mount → bind → unmount → destroy\n */\nimport type { IWidget, IView, WidgetPosition } from '@mapgpu/core';\nexport declare abstract class WidgetBase implements IWidget {\n    protected readonly widgetName: string;\n    readonly id: string;\n    position: WidgetPosition;\n    protected _view: IView | null;\n    protected _container: HTMLElement | null;\n    protected _root: HTMLElement | null;\n    protected _destroyed: boolean;\n    constructor(widgetName: string, options?: {\n        id?: string;\n        position?: WidgetPosition;\n    });\n    mount(container: HTMLElement): void;\n    unmount(): void;\n    bind(view: IView): void;\n    destroy(): void;\n    /** Subclass should render its content into the root element. */\n    protected abstract render(root: HTMLElement): void;\n    /** Called when a view is bound. Subclasses can override. */\n    protected onViewBound(_view: IView): void;\n    /** Called before destroy. Subclasses can override for cleanup. */\n    protected onDestroy(): void;\n    private applyPositionStyles;\n}\n//# sourceMappingURL=WidgetBase.d.ts.map","ZoomControlWidget.d.ts":"/**\n * ZoomControlWidget — Zoom in/out buttons (Leaflet L.control.zoom equivalent).\n */\nimport type { IView, WidgetPosition } from '@mapgpu/core';\nimport { WidgetBase } from './WidgetBase';\nexport interface ZoomControlWidgetOptions {\n    id?: string;\n    position?: WidgetPosition;\n    zoomInTitle?: string;\n    zoomOutTitle?: string;\n}\nexport declare class ZoomControlWidget extends WidgetBase {\n    private _zoomInTitle;\n    private _zoomOutTitle;\n    private _btnIn;\n    private _btnOut;\n    constructor(options?: ZoomControlWidgetOptions);\n    protected render(root: HTMLElement): void;\n    private _createButton;\n    protected onViewBound(_view: IView): void;\n}\n//# sourceMappingURL=ZoomControlWidget.d.ts.map","index.d.ts":"/**\n * @mapgpu/widgets\n *\n * Framework-agnostic UI widgets for mapgpu.\n */\nexport { WidgetBase } from './WidgetBase';\nexport { LayerListWidget } from './LayerListWidget';\nexport type { LayerListEvents, LayerListWidgetOptions } from './LayerListWidget';\nexport { ScaleBarWidget } from './ScaleBarWidget';\nexport type { ScaleBarUnit, ScaleBarWidgetOptions } from './ScaleBarWidget';\nexport { CoordinatesWidget } from './CoordinatesWidget';\nexport type { CoordinateFormat, CoordinatesWidgetOptions } from './CoordinatesWidget';\nexport { BasemapGalleryWidget } from './BasemapGalleryWidget';\nexport type { BasemapItem, BasemapGalleryWidgetOptions } from './BasemapGalleryWidget';\nexport { SearchWidget } from './SearchWidget';\nexport type { SearchResult, SearchSource, SearchWidgetOptions } from './SearchWidget';\nexport { MeasurementWidget, haversineDistance, sphericalPolygonArea } from './MeasurementWidget';\nexport type { MeasurementMode, MeasurementUnit, MeasurementResult, MeasurementWidgetOptions } from './MeasurementWidget';\nexport { TimeSliderWidget } from './TimeSliderWidget';\nexport type { TimeSliderWidgetOptions, PlaybackSpeed } from './TimeSliderWidget';\nexport { LOSWidget } from './LOSWidget';\nexport type { LOSWidgetOptions, LOSObserverTarget } from './LOSWidget';\nexport { SelectionInspectorWidget } from './SelectionInspectorWidget';\nexport type { SelectionInspectorWidgetOptions } from './SelectionInspectorWidget';\nexport { DockPanel } from './DockPanel';\nexport type { DockPosition, DockPanelOptions } from './DockPanel';\nexport { DrawToolbarWidget } from './DrawToolbarWidget';\nexport type { DrawToolbarWidgetOptions } from './DrawToolbarWidget';\nexport { MeasureToolbarWidget } from './MeasureToolbarWidget';\nexport type { MeasureToolbarWidgetOptions } from './MeasureToolbarWidget';\nexport { ZoomControlWidget } from './ZoomControlWidget';\nexport type { ZoomControlWidgetOptions } from './ZoomControlWidget';\nexport { AttributionWidget } from './AttributionWidget';\nexport type { AttributionWidgetOptions } from './AttributionWidget';\nexport { PopupWidget } from './PopupWidget';\nexport type { PopupOptions } from './PopupWidget';\nexport { TooltipWidget } from './TooltipWidget';\nexport type { TooltipOptions } from './TooltipWidget';\n//# sourceMappingURL=index.d.ts.map"},"@mapgpu/tools":{"DrawPointTool.d.ts":"/**\n * DrawPointTool — Single-click point placement tool.\n *\n * State machine:\n *   active ──[left click]──→ (create feature + emit draw-complete) ──→ active\n *\n * Preview: ghost point at cursor position.\n */\nimport type { ToolPointerEvent, ToolContext } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { ITargetLayer } from './commands/CreateFeatureCommand';\nimport type { SnapEngine } from './snap/SnapEngine';\nexport interface DrawPointToolOptions {\n    targetLayer: ITargetLayer;\n    snapEngine?: SnapEngine;\n}\nexport declare class DrawPointTool extends ToolBase {\n    readonly id = \"draw-point\";\n    readonly name = \"Draw Point\";\n    private _targetLayer;\n    private _snapEngine;\n    private _lastSnap;\n    private _cursorPreviewId;\n    constructor(options: DrawPointToolOptions);\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(_e: ToolPointerEvent): boolean;\n    private _resolveSnap;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(_e: KeyboardEvent): boolean;\n    cancel(): void;\n}\n//# sourceMappingURL=DrawPointTool.d.ts.map","DrawPolygonTool.d.ts":"/**\n * DrawPolygonTool — Multi-click polygon drawing tool.\n *\n * State machine:\n *   active ──[click]──→ drawing ──[click]──→ drawing (add vertex)\n *                                   ├──[dblclick/Enter]──→ active (draw-complete, close ring)\n *                                   ├──[Escape]──→ active (draw-cancel)\n *                                   └──[Backspace]──→ drawing (remove last vertex)\n *\n * Preview: polygon fill + closing edge to cursor + vertex dots.\n * Minimum 3 vertices to finish. Auto-closes ring on complete.\n */\nimport type { ToolPointerEvent, ToolContext } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { ITargetLayer } from './commands/CreateFeatureCommand';\nimport type { SnapEngine } from './snap/SnapEngine';\nexport interface DrawPolygonToolOptions {\n    targetLayer: ITargetLayer;\n    snapEngine?: SnapEngine;\n}\nexport declare class DrawPolygonTool extends ToolBase {\n    readonly id = \"draw-polygon\";\n    readonly name = \"Draw Polygon\";\n    private _targetLayer;\n    private _snapEngine;\n    private _lastSnap;\n    private _vertices;\n    private _cursorPos;\n    constructor(options: DrawPolygonToolOptions);\n    private _resolveSnap;\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(_e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    private _finishDrawing;\n    private _updatePreview;\n}\n//# sourceMappingURL=DrawPolygonTool.d.ts.map","DrawPolylineTool.d.ts":"/**\n * DrawPolylineTool — Multi-click polyline drawing tool.\n *\n * State machine:\n *   active ──[click]──→ drawing ──[click]──→ drawing (add vertex)\n *                                   ├──[dblclick/Enter]──→ active (draw-complete)\n *                                   ├──[Escape]──→ active (draw-cancel)\n *                                   └──[Backspace]──→ drawing (remove last vertex)\n *\n * Preview: current polyline + rubber-band segment to cursor + vertex dots.\n * Minimum 2 vertices to finish.\n */\nimport type { ToolPointerEvent, ToolContext } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { ITargetLayer } from './commands/CreateFeatureCommand';\nimport type { SnapEngine } from './snap/SnapEngine';\nexport interface DrawPolylineToolOptions {\n    targetLayer: ITargetLayer;\n    snapEngine?: SnapEngine;\n}\nexport declare class DrawPolylineTool extends ToolBase {\n    readonly id = \"draw-polyline\";\n    readonly name = \"Draw Polyline\";\n    private _targetLayer;\n    private _snapEngine;\n    private _lastSnap;\n    private _vertices;\n    private _cursorPos;\n    constructor(options: DrawPolylineToolOptions);\n    private _resolveSnap;\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(_e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    private _finishDrawing;\n    private _updatePreview;\n}\n//# sourceMappingURL=DrawPolylineTool.d.ts.map","EditTool.d.ts":"/**\n * EditTool — Select and reshape existing features.\n *\n * State machine:\n *   active ──[click on feature]──→ editing (show vertex handles)\n *   editing ──[drag vertex handle]──→ editing (geometry snapshot + undo)\n *   editing ──[dblclick on edge]──→ editing (insert vertex)\n *   editing ──[Delete on selected vertex]──→ editing (remove vertex)\n *   editing ──[click empty]──→ active (deselect)\n *   editing ──[Escape]──→ active (deselect)\n *\n * Hit-testing: vertex proximity + edge proximity + point-in-polygon.\n */\nimport type { ToolPointerEvent, ToolContext, Feature } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { ITargetLayer } from './commands/CreateFeatureCommand';\nexport interface EditToolOptions {\n    editableLayers: (ITargetLayer & {\n        getFeatures(): readonly Feature[];\n        readonly id: string;\n    })[];\n    hitTolerance?: number;\n}\nexport declare class EditTool extends ToolBase {\n    readonly id = \"edit\";\n    readonly name = \"Edit\";\n    private _editableLayers;\n    private _hitTolerance;\n    private _selectedFeature;\n    private _selectedLayerId;\n    private _editableVertices;\n    private _isClosedRing;\n    private _draggingVertexIndex;\n    private _dragGeomSnapshot;\n    private _isDragging;\n    private _draggingFeature;\n    private _dragLastMapCoords;\n    private _hoveredVertexIndex;\n    constructor(options: EditToolOptions);\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    private _selectFeature;\n    private _clearSelection;\n    /**\n     * Read editable vertices from the selected feature's geometry.\n     * For closed polygon rings, the duplicate closing vertex is omitted.\n     */\n    private _readVerticesFromGeometry;\n    /**\n     * Write editable vertices back into the selected feature's geometry.\n     * For closed polygon rings, the closing vertex is auto-appended.\n     */\n    private _writeVerticesToGeometry;\n    /**\n     * Hit-test all editable features using vertex proximity + edge proximity.\n     * For polygons, also uses point-in-polygon.\n     */\n    private _hitTestFeatures;\n    /**\n     * Compute screen distance from a point to a feature.\n     * Uses vertex + edge proximity for lines/points.\n     * For polygons, also checks point-in-polygon (returns 0 if inside).\n     */\n    private _featureScreenDistance;\n    /**\n     * Extract polygon outer rings as screen-space coordinate arrays.\n     */\n    private _extractScreenRings;\n    /**\n     * Ray-casting point-in-polygon test (screen space).\n     */\n    private _pointInRing;\n    private _pointToSegDist;\n    /**\n     * Hit-test midpoint handles. Returns the insert index and midpoint coords if hit.\n     */\n    private _hitTestMidpoints;\n    private _findLayer;\n    private _minVertexCount;\n    private _flattenCoords;\n    private _updateEditPreview;\n}\n//# sourceMappingURL=EditTool.d.ts.map","LosTool.d.ts":"/**\n * LosTool — Interactive Line of Sight analysis tool.\n *\n * State machine:\n *   active ──[click]──→ observer-placed (blue dot)\n *          ──[click]──→ target-placed → auto-run LOS → showing-result\n *          ──[drag handle]──→ dragging → [release] → re-run LOS\n *          ──[Escape]──→ active (clear)\n *          ──[Shift+drag vertical]──→ height-adjust (3D mode)\n *\n * Preview features:\n *   observer-point: blue circle (draggable)\n *   target-point: red circle (draggable)\n *   visible-line: green solid line\n *   blocked-line: red dashed line\n *   blocking-point: red X marker\n */\nimport type { ToolPointerEvent, ToolContext, LosAnalysisResult } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { LosAnalysis } from '@mapgpu/analysis';\nexport interface LosToolOptions {\n    analysis: LosAnalysis;\n    /** Number of sample points along the LOS line (default 256) */\n    sampleCount?: number;\n    /** Debounce interval in ms for drag updates (default 50) */\n    debounceMs?: number;\n}\nexport declare class LosTool extends ToolBase {\n    readonly id = \"los\";\n    readonly name = \"Line of Sight\";\n    private _analysis;\n    private _sampleCount;\n    private _debounceMs;\n    private _observer;\n    private _target;\n    private _observerOffset;\n    private _targetOffset;\n    private _result;\n    private _losState;\n    private _dragTarget;\n    private _isDragging;\n    private _debounceTimer;\n    private _cursorPos;\n    private _shiftDragStartY;\n    private _shiftDragStartOffset;\n    constructor(options: LosToolOptions);\n    get observer(): [number, number] | null;\n    get target(): [number, number] | null;\n    get observerOffset(): number;\n    get targetOffset(): number;\n    get result(): LosAnalysisResult | null;\n    setObserverOffset(meters: number): void;\n    setTargetOffset(meters: number): void;\n    setObserver(lon: number, lat: number): void;\n    setTarget(lon: number, lat: number): void;\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    private _reset;\n    private _clearAndEmit;\n    private _clearDebounce;\n    private _runLosDebounced;\n    private _runLos;\n    private _hitTestHandle;\n    private _updatePreview;\n}\n//# sourceMappingURL=LosTool.d.ts.map","MeasureLineTool.d.ts":"/**\n * MeasureLineTool — Distance measurement along a polyline.\n *\n * State machine:\n *   active ──[click]──→ drawing ──[click]──→ drawing (add vertex)\n *                                   ├──[dblclick/Enter]──→ active (complete, ≥2 vertices)\n *                                   ├──[Escape]──→ active (cancel current)\n *                                   └──[Backspace]──→ drawing (remove last vertex)\n *\n * Preview: dashed rubber-band line + vertex dots + segment distance labels + total.\n * Multiple measurements coexist on screen.\n */\nimport type { ToolPointerEvent, ToolContext } from '@mapgpu/core';\nimport { MeasureToolBase } from './MeasureToolBase';\nimport type { MeasureToolBaseOptions } from './MeasureToolBase';\nexport declare class MeasureLineTool extends MeasureToolBase {\n    readonly id = \"measure-line\";\n    readonly name = \"Measure Distance\";\n    private _vertices;\n    private _cursorPos;\n    constructor(options: MeasureToolBaseOptions);\n    protected onActivate(context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(_e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    protected _onUnitsChange(): void;\n    private _finishMeasurement;\n    private _updatePreview;\n}\n//# sourceMappingURL=MeasureLineTool.d.ts.map","MeasurePointTool.d.ts":"/**\n * MeasurePointTool — Coordinate measurement at click location.\n *\n * State machine:\n *   active ──[click]──→ place point + coordinate label ──→ active\n *\n * Multiple point measurements accumulate on screen.\n */\nimport type { ToolPointerEvent } from '@mapgpu/core';\nimport { MeasureToolBase } from './MeasureToolBase';\nimport type { MeasureToolBaseOptions } from './MeasureToolBase';\nexport declare class MeasurePointTool extends MeasureToolBase {\n    readonly id = \"measure-point\";\n    readonly name = \"Measure Point\";\n    private _cursorPos;\n    constructor(options: MeasureToolBaseOptions);\n    protected onActivate(context: import('@mapgpu/core').ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(_e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(_e: KeyboardEvent): boolean;\n    cancel(): void;\n    protected _onUnitsChange(): void;\n    private _updatePreview;\n}\n//# sourceMappingURL=MeasurePointTool.d.ts.map","MeasurePolygonTool.d.ts":"/**\n * MeasurePolygonTool — Area measurement via polygon drawing.\n *\n * State machine:\n *   active ──[click]──→ drawing ──[click]──→ drawing (add vertex)\n *                                   ├──[dblclick/Enter]──→ active (complete, ≥3 vertices)\n *                                   ├──[Escape]──→ active (cancel current)\n *                                   └──[Backspace]──→ drawing (remove last vertex)\n *\n * Preview: polygon fill + dashed outline + edge distance labels + area at centroid.\n * Multiple measurements coexist on screen.\n */\nimport type { ToolPointerEvent, ToolContext } from '@mapgpu/core';\nimport { MeasureToolBase } from './MeasureToolBase';\nimport type { MeasureToolBaseOptions } from './MeasureToolBase';\nexport declare class MeasurePolygonTool extends MeasureToolBase {\n    readonly id = \"measure-area\";\n    readonly name = \"Measure Area\";\n    private _vertices;\n    private _cursorPos;\n    constructor(options: MeasureToolBaseOptions);\n    protected onActivate(context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(_e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    protected _onUnitsChange(): void;\n    private _finishMeasurement;\n    private _updatePreview;\n}\n//# sourceMappingURL=MeasurePolygonTool.d.ts.map","MeasureToolBase.d.ts":"/**\n * MeasureToolBase — Shared base class for measurement tools.\n *\n * Extends ToolBase with:\n * - UnitManager integration (subscribe/unsubscribe on activate/deactivate)\n * - MeasureLabelManager reference\n * - Persistent measurementLayer for completed measurements\n * - Clear Last / Clear All API\n */\nimport type { ToolContext, IPreviewLayer, MeasurementResult } from '@mapgpu/core';\nimport { UnitManager } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { MeasureLabelManager } from './helpers/MeasureLabelManager';\nexport interface MeasurementRecord {\n    id: string;\n    type: 'point' | 'distance' | 'area';\n    result: MeasurementResult;\n    featureIds: string[];\n    labelIds: string[];\n}\nexport interface MeasureToolBaseOptions {\n    unitManager: UnitManager;\n    labelManager: MeasureLabelManager;\n    measurementLayer: IPreviewLayer;\n}\nexport declare abstract class MeasureToolBase extends ToolBase {\n    protected _unitManager: UnitManager;\n    protected _labelManager: MeasureLabelManager;\n    protected _measurementLayer: IPreviewLayer;\n    protected _completedMeasurements: MeasurementRecord[];\n    private _unitsChangeHandler;\n    constructor(options: MeasureToolBaseOptions);\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    /**\n     * Re-format all persistent labels when units change.\n     * Subclasses must implement to recalculate labels.\n     */\n    protected abstract _onUnitsChange(): void;\n    /**\n     * Remove the last completed measurement.\n     */\n    clearLastMeasurement(): void;\n    /**\n     * Remove all completed measurements.\n     */\n    clearAllMeasurements(): void;\n    /**\n     * Get all completed measurements.\n     */\n    getMeasurements(): readonly MeasurementRecord[];\n}\n//# sourceMappingURL=MeasureToolBase.d.ts.map","MilStdDrawTool.d.ts":"/**\n * MilStdDrawTool — Schema-driven MIL-STD-2525C symbol placement.\n *\n * For point symbols: single click placement (like DrawPointTool)\n * For tactical graphics: multi-click control point placement with\n * rubber-band preview, driven by the schema's minControlPoints/maxControlPoints.\n *\n * State machine:\n *   active ──[click, point mode]──→ (create feature) ──→ active\n *   active ──[click, tactical mode]──→ drawing ──[click]──→ drawing (add CP)\n *                                       ├──[dblclick/Enter]──→ active (finish)\n *                                       ├──[Escape]──→ active (cancel)\n *                                       ├──[Backspace]──→ drawing (remove last CP)\n *                                       └──[maxCPs reached]──→ active (auto-finish)\n */\nimport type { ToolPointerEvent } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { ITargetLayer } from './commands/CreateFeatureCommand';\nexport interface MilStdDrawToolOptions {\n    /** Target layer to add completed features to */\n    targetLayer: ITargetLayer;\n    /** The 15-char SIDC to place */\n    sidc: string;\n    /** Whether this is a point symbol or tactical graphic */\n    mode: 'point' | 'tactical';\n    /** For tactical graphics: minimum control points required */\n    minControlPoints?: number;\n    /** For tactical graphics: maximum control points (-1 = unlimited). Default: -1 */\n    maxControlPoints?: number;\n}\nexport declare class MilStdDrawTool extends ToolBase {\n    readonly id = \"milstd-draw\";\n    readonly name = \"MIL-STD Draw\";\n    private _targetLayer;\n    private _sidc;\n    private _mode;\n    private _minControlPoints;\n    private _maxControlPoints;\n    private _vertices;\n    private _cursorPos;\n    constructor(options: MilStdDrawToolOptions);\n    /** The current SIDC being drawn. */\n    get sidc(): string;\n    /** Update the SIDC being drawn (allows switching symbol mid-session). */\n    setSidc(sidc: string, mode: 'point' | 'tactical', minCp?: number, maxCp?: number): void;\n    protected onActivate(): void;\n    protected onDeactivate(): void;\n    onPointerDown(_e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    private _placePointSymbol;\n    private _finishTacticalGraphic;\n    private _updatePreview;\n    private _reset;\n}\n//# sourceMappingURL=MilStdDrawTool.d.ts.map","MilStdEditTool.d.ts":"/**\n * MilStdEditTool — Edit control points of placed MIL-STD tactical graphics.\n *\n * Allows:\n * - Selecting a feature for editing\n * - Dragging control points to reposition them\n * - Changing SIDC on selected feature\n *\n * State machine:\n *   active ──[selectFeature()]──→ editing (show CP handles)\n *   editing ──[drag CP]──→ editing (update position)\n *   editing ──[Escape]──→ active (deselect)\n *   editing ──[deselectFeature()]──→ active\n */\nimport type { ToolPointerEvent, Feature } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nexport interface MilStdEditToolOptions {\n    /** Pixel tolerance for control point hit detection. Default: 12 */\n    tolerance?: number;\n}\nexport declare class MilStdEditTool extends ToolBase {\n    readonly id = \"milstd-edit\";\n    readonly name = \"MIL-STD Edit\";\n    private _tolerance;\n    private _selectedFeature;\n    private _controlPoints;\n    private _draggingIndex;\n    constructor(options?: MilStdEditToolOptions);\n    /** The currently selected feature, or null. */\n    get selectedFeature(): Feature | null;\n    /** The current control points (read-only view). */\n    get controlPoints(): readonly [number, number][];\n    /** Select a feature for editing. */\n    selectFeature(feature: Feature): void;\n    /** Deselect current feature. */\n    deselectFeature(): void;\n    /** Change the SIDC of the selected feature. */\n    changeSidc(newSidc: string): void;\n    protected onActivate(): void;\n    protected onDeactivate(): void;\n    onPointerDown(e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(_e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    private _showControlPoints;\n}\n//# sourceMappingURL=MilStdEditTool.d.ts.map","PlaceGeometryTool.d.ts":"/**\n * PlaceGeometryTool — Interactive 3D geometry placement (Blender-style).\n *\n * Phase 1 (sizing):    Click + drag → radius (footprint preview)\n * Phase 2 (extruding): Release → move mouse up/down → real-time 3D mesh height\n * Confirm:             Click or Enter → finalize geometry\n * Cancel:              Escape at any phase\n *\n * During extrusion phase, a live 3D mesh preview is rendered via a temporary\n * GraphicsLayer with Mesh3DSymbol — updates every mouse move.\n */\nimport type { ToolPointerEvent, ToolContext, Feature } from '@mapgpu/core';\nimport { ToolBase } from './ToolBase';\nimport type { ITargetLayer } from './commands/CreateFeatureCommand';\nexport type PlaceableGeometryType = 'box' | 'cylinder' | 'sphere' | 'cone';\nexport interface PlaceGeometryToolOptions {\n    targetLayer: ITargetLayer;\n    geometryType?: PlaceableGeometryType;\n    color?: [number, number, number, number];\n    /** Pixels-to-meters ratio for height extrusion. Default: 2 (2m per pixel). */\n    heightSensitivity?: number;\n}\nexport declare const GEOMETRY_MODEL_IDS: Record<PlaceableGeometryType, string>;\ntype Phase = 'idle' | 'sizing' | 'extruding';\n/**\n * Callback fired during extrusion phase on every mouse move.\n * Consumers should update a temporary layer's renderer with the new scale.\n */\nexport type ExtrusionPreviewCallback = (center: [number, number], radius: number, height: number, geometryType: PlaceableGeometryType) => void;\nexport declare class PlaceGeometryTool extends ToolBase {\n    readonly id = \"place-geometry\";\n    readonly name = \"Place Geometry\";\n    private _targetLayer;\n    private _geometryType;\n    color: [number, number, number, number];\n    private _heightSensitivity;\n    private _phase;\n    private _center;\n    private _radiusM;\n    private _heightM;\n    private _extrudeAnchorY;\n    /** External callback for live 3D preview during extrusion. */\n    onExtrusionPreview: ExtrusionPreviewCallback | null;\n    constructor(options: PlaceGeometryToolOptions);\n    setGeometryType(type: PlaceableGeometryType): void;\n    setColor(color: [number, number, number, number]): void;\n    get geometryType(): PlaceableGeometryType;\n    get phase(): Phase;\n    get currentRadius(): number;\n    get currentHeight(): number;\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    onPointerDown(e: ToolPointerEvent): boolean;\n    onPointerMove(e: ToolPointerEvent): boolean;\n    onPointerUp(e: ToolPointerEvent): boolean;\n    onDoubleClick(_e: ToolPointerEvent): boolean;\n    onKeyDown(e: KeyboardEvent): boolean;\n    cancel(): void;\n    private _reset;\n    private _createFeature;\n    private _updatePreview;\n}\n/**\n * Generate extrusion slice features for a geometry type.\n *\n * - **Box/Cylinder**: 1 slice — full radius, constant walls\n * - **Cone**: 12 stacked slices with linearly decreasing radius (wide at bottom, point at top)\n * - **Sphere**: 12 stacked slices with hemisphere profile: r(h) = R × sqrt(1 - (h/R)²)\n *\n * Each slice is a Polygon feature with `height` and `minHeight` attributes\n * for use with `ExtrudedPolygonSymbol`.\n */\nexport declare function generateGeometrySlices(center: [number, number], radius: number, height: number, geoType: PlaceableGeometryType): Feature[];\nexport {};\n//# sourceMappingURL=PlaceGeometryTool.d.ts.map","SnapEngine.d.ts":"/**\n * SnapEngine — Backward-compatible wrapper around the new snap system.\n *\n * Preserves the original API surface (SnapOptions, SnapResult with 'vertex'|'edge'|'none')\n * while delegating to the new multi-type SnapEngine under the hood.\n *\n * @deprecated Import from `./snap/index.js` for the full-featured snap engine.\n */\nimport type { Feature } from '@mapgpu/core';\nexport interface SnapOptions {\n    /** Enable vertex snapping. Default: true */\n    vertex?: boolean;\n    /** Enable edge snapping. Default: false */\n    edge?: boolean;\n    /** Snap tolerance in screen pixels. Default: 10 */\n    tolerance?: number;\n}\nexport interface SnapResult {\n    /** Snapped geographic coordinates. */\n    coords: [number, number];\n    /** Type of snap that occurred. */\n    type: 'vertex' | 'edge' | 'none';\n    /** Source feature ID, if snapped to a feature. */\n    sourceFeatureId?: string | number;\n}\nexport interface SnapSourceLayer {\n    getFeatures(): readonly Feature[];\n}\nexport declare class SnapEngine {\n    private _inner;\n    constructor(options?: SnapOptions);\n    addSourceLayer(layer: SnapSourceLayer): void;\n    removeSourceLayer(layer: SnapSourceLayer): void;\n    snap(screenX: number, screenY: number, mapCoords: [number, number], toScreen: (lon: number, lat: number) => [number, number] | null): SnapResult;\n    get options(): {\n        vertex: boolean;\n        edge: boolean;\n        tolerance: number;\n    };\n    setTolerance(px: number): void;\n}\n//# sourceMappingURL=SnapEngine.d.ts.map","ToolBase.d.ts":"/**\n * ToolBase — Abstract base class for all tools.\n *\n * Provides shared lifecycle management and state machine transitions.\n * Concrete tools extend this and implement specific event handlers.\n */\nimport type { ITool, ToolState, ToolCursor, ToolContext, ToolPointerEvent } from '@mapgpu/core';\nexport declare abstract class ToolBase implements ITool {\n    abstract readonly id: string;\n    abstract readonly name: string;\n    protected _state: ToolState;\n    protected _cursor: ToolCursor;\n    protected _context: ToolContext | null;\n    get state(): ToolState;\n    get cursor(): ToolCursor;\n    activate(context: ToolContext): void;\n    deactivate(): void;\n    abstract onPointerDown(e: ToolPointerEvent): boolean;\n    abstract onPointerMove(e: ToolPointerEvent): boolean;\n    abstract onPointerUp(e: ToolPointerEvent): boolean;\n    abstract onDoubleClick(e: ToolPointerEvent): boolean;\n    abstract onKeyDown(e: KeyboardEvent): boolean;\n    abstract cancel(): void;\n    destroy(): void;\n    protected onActivate(_context: ToolContext): void;\n    protected onDeactivate(): void;\n    protected onDestroy(): void;\n    protected markDirty(): void;\n}\n//# sourceMappingURL=ToolBase.d.ts.map","commands/AddVertexCommand.d.ts":"import type { ICommand } from '@mapgpu/core';\nexport declare class AddVertexCommand implements ICommand {\n    private _vertices;\n    private _coords;\n    private _index?;\n    readonly description: string;\n    constructor(_vertices: [number, number][], _coords: [number, number], _index?: number | undefined);\n    execute(): void;\n    undo(): void;\n}\n//# sourceMappingURL=AddVertexCommand.d.ts.map","commands/CreateFeatureCommand.d.ts":"import type { ICommand, Feature } from '@mapgpu/core';\n/** Target layer interface — subset of GraphicsLayer. */\nexport interface ITargetLayer {\n    add(feature: Feature): void;\n    remove(id: string | number): boolean;\n}\nexport declare class CreateFeatureCommand implements ICommand {\n    private _targetLayer;\n    private _feature;\n    readonly description: string;\n    constructor(_targetLayer: ITargetLayer, _feature: Feature);\n    execute(): void;\n    undo(): void;\n}\n//# sourceMappingURL=CreateFeatureCommand.d.ts.map","commands/DeleteVertexCommand.d.ts":"import type { ICommand } from '@mapgpu/core';\nexport declare class DeleteVertexCommand implements ICommand {\n    private _vertices;\n    private _index;\n    readonly description: string;\n    private _removedCoords;\n    constructor(_vertices: [number, number][], _index: number);\n    execute(): void;\n    undo(): void;\n}\n//# sourceMappingURL=DeleteVertexCommand.d.ts.map","commands/MoveFeatureCommand.d.ts":"import type { ICommand, Feature } from '@mapgpu/core';\nimport type { ITargetLayer } from './CreateFeatureCommand';\nexport declare class MoveFeatureCommand implements ICommand {\n    private _targetLayer;\n    private _feature;\n    private _deltaLon;\n    private _deltaLat;\n    readonly description: string;\n    private _oldGeometry;\n    constructor(_targetLayer: ITargetLayer, _feature: Feature, _deltaLon: number, _deltaLat: number);\n    execute(): void;\n    undo(): void;\n    private _offsetCoordinates;\n}\n//# sourceMappingURL=MoveFeatureCommand.d.ts.map","commands/MoveVertexCommand.d.ts":"import type { ICommand } from '@mapgpu/core';\nexport declare class MoveVertexCommand implements ICommand {\n    private _vertices;\n    private _index;\n    private _newCoords;\n    readonly description: string;\n    private _oldCoords;\n    constructor(_vertices: [number, number][], _index: number, _newCoords: [number, number]);\n    execute(): void;\n    undo(): void;\n}\n//# sourceMappingURL=MoveVertexCommand.d.ts.map","commands/RemoveVertexCommand.d.ts":"import type { ICommand } from '@mapgpu/core';\nexport declare class RemoveVertexCommand implements ICommand {\n    private _vertices;\n    private _index;\n    readonly description: string;\n    private _removedCoords;\n    constructor(_vertices: [number, number][], _index: number);\n    execute(): void;\n    undo(): void;\n}\n//# sourceMappingURL=RemoveVertexCommand.d.ts.map","commands/index.d.ts":"export { AddVertexCommand } from './AddVertexCommand';\nexport { RemoveVertexCommand } from './RemoveVertexCommand';\nexport { CreateFeatureCommand } from './CreateFeatureCommand';\nexport type { ITargetLayer } from './CreateFeatureCommand';\nexport { MoveVertexCommand } from './MoveVertexCommand';\nexport { DeleteVertexCommand } from './DeleteVertexCommand';\nexport { MoveFeatureCommand } from './MoveFeatureCommand';\n//# sourceMappingURL=index.d.ts.map","helpers/MeasureLabelManager.d.ts":"/**\n * MeasureLabelManager — HTML overlay labels for measurement tools.\n *\n * Creates positioned HTML elements above geographic coordinates,\n * updated via toScreen() projection on view-change events.\n * Labels are pointer-events:none so they don't interfere with map interaction.\n */\nexport interface MeasureLabel {\n    id: string;\n    geoPosition: [number, number];\n    text: string;\n    type: 'distance' | 'area' | 'coordinate' | 'total';\n    persistent: boolean;\n}\nexport interface MeasureLabelManagerOptions {\n    container: HTMLElement;\n    toScreen: (lon: number, lat: number) => [number, number] | null;\n}\nexport declare class MeasureLabelManager {\n    private _container;\n    private _toScreen;\n    private _labels;\n    private _elements;\n    constructor(options: MeasureLabelManagerOptions);\n    addLabel(label: MeasureLabel): void;\n    removeLabel(id: string): void;\n    updateLabel(id: string, text: string): void;\n    /** Remove all non-persistent labels. */\n    clearTransient(): void;\n    /** Remove all labels. */\n    clearAll(): void;\n    /** Reproject all labels via toScreen. Call on view-change. */\n    updatePositions(): void;\n    /** Get all current labels (readonly). */\n    get labels(): ReadonlyMap<string, MeasureLabel>;\n    destroy(): void;\n    private _positionElement;\n}\n//# sourceMappingURL=MeasureLabelManager.d.ts.map","helpers/footprintGenerators.d.ts":"/**\n * Footprint Generators — Create geographic polygon rings from metric dimensions.\n *\n * Used by PlaceGeometryTool to generate footprint polygons for 3D geometry extrusion.\n * All functions return closed rings as [[lon, lat], ...] arrays.\n */\n/**\n * Rectangular footprint (Box-like shapes).\n * @param centerLon - Center longitude.\n * @param centerLat - Center latitude.\n * @param halfWidthM - Half-width in meters (east-west).\n * @param halfDepthM - Half-depth in meters (north-south).\n * @returns Closed polygon ring [[lon, lat], ...].\n */\nexport declare function makeRectFootprint(centerLon: number, centerLat: number, halfWidthM: number, halfDepthM: number): number[][];\n/**\n * Circular footprint (Cylinder, Sphere, Cone shapes).\n * @param centerLon - Center longitude.\n * @param centerLat - Center latitude.\n * @param radiusM - Radius in meters.\n * @param segments - Number of polygon segments (default 32).\n * @returns Closed polygon ring [[lon, lat], ...].\n */\nexport declare function makeCircleFootprint(centerLon: number, centerLat: number, radiusM: number, segments?: number): number[][];\n/**\n * Compute distance in meters between two lon/lat points (Haversine, simplified).\n */\nexport declare function distanceMeters(lon1: number, lat1: number, lon2: number, lat2: number): number;\n//# sourceMappingURL=footprintGenerators.d.ts.map","helpers/geodesic.d.ts":"/**\n * Geodesic calculation helpers for measurement tools.\n *\n * Haversine distance, spherical polygon area, midpoint, and centroid.\n * Duplicated from @mapgpu/widgets to avoid WASM/heavy dependencies.\n */\nexport declare const EARTH_RADIUS = 6371000;\n/**\n * Haversine great-circle distance between two WGS84 points.\n * @returns distance in meters\n */\nexport declare function geodesicDistance(lon1: number, lat1: number, lon2: number, lat2: number): number;\n/**\n * Per-segment distances for a polyline.\n * @returns array of distances in meters (length = vertices.length - 1)\n */\nexport declare function geodesicSegmentDistances(vertices: [number, number][]): number[];\n/**\n * Total distance along a polyline.\n * @returns total distance in meters\n */\nexport declare function geodesicTotalDistance(vertices: [number, number][]): number;\n/**\n * Spherical polygon area using the shoelace-like formula on a sphere.\n * @returns area in square meters (always positive)\n */\nexport declare function sphericalPolygonArea(vertices: [number, number][]): number;\n/**\n * Perimeter of a polygon (closing edge included).\n * @returns perimeter in meters\n */\nexport declare function geodesicPerimeter(vertices: [number, number][]): number;\n/**\n * Geographic midpoint between two WGS84 points.\n * Uses simple averaging (sufficient for short distances).\n */\nexport declare function geodesicMidpoint(lon1: number, lat1: number, lon2: number, lat2: number): [number, number];\n/**\n * Centroid of a polygon (simple arithmetic mean of vertices).\n */\nexport declare function polygonCentroid(vertices: [number, number][]): [number, number];\n//# sourceMappingURL=geodesic.d.ts.map","helpers/geometryHelpers.d.ts":"/**\n * Geometry helper utilities for drawing/editing tools.\n *\n * Hit-testing, midpoint calculation, and distance utilities.\n */\n/** Euclidean distance between two screen-space points. */\nexport declare function screenDistance(ax: number, ay: number, bx: number, by: number): number;\n/** Midpoint between two coordinate pairs. */\nexport declare function midpoint(a: [number, number], b: [number, number]): [number, number];\n/**\n * Find the nearest vertex index to a screen position.\n * Returns the index and screen distance, or null if no vertex within tolerance.\n */\nexport declare function findNearestVertex(vertices: [number, number][], screenX: number, screenY: number, toScreen: (lon: number, lat: number) => [number, number] | null, tolerance?: number): {\n    index: number;\n    distance: number;\n} | null;\n/**\n * Find the nearest edge (segment) to a screen position.\n * Returns the edge index (segment between vertices[i] and vertices[i+1]),\n * the closest point on the edge, and the screen distance.\n */\nexport declare function findNearestEdge(vertices: [number, number][], screenX: number, screenY: number, toScreen: (lon: number, lat: number) => [number, number] | null, tolerance?: number): {\n    edgeIndex: number;\n    t: number;\n    distance: number;\n} | null;\n/**\n * Distance from point (px, py) to segment (ax, ay)-(bx, by).\n * Returns distance and parametric position t ∈ [0, 1].\n */\nexport declare function pointToSegmentDistance(px: number, py: number, ax: number, ay: number, bx: number, by: number): {\n    distance: number;\n    t: number;\n};\nexport declare function generateFeatureId(): string;\nexport declare function generatePreviewId(prefix?: string): string;\n//# sourceMappingURL=geometryHelpers.d.ts.map","index.d.ts":"/**\n * @mapgpu/tools\n *\n * Drawing and editing tools for mapgpu.\n * Point, Polyline, Polygon draw tools + Edit tool + SnapEngine.\n */\nexport { ToolBase } from './ToolBase';\nexport { DrawPointTool } from './DrawPointTool';\nexport type { DrawPointToolOptions } from './DrawPointTool';\nexport { DrawPolylineTool } from './DrawPolylineTool';\nexport type { DrawPolylineToolOptions } from './DrawPolylineTool';\nexport { DrawPolygonTool } from './DrawPolygonTool';\nexport type { DrawPolygonToolOptions } from './DrawPolygonTool';\nexport { EditTool } from './EditTool';\nexport type { EditToolOptions } from './EditTool';\nexport { PlaceGeometryTool } from './PlaceGeometryTool';\nexport type { PlaceGeometryToolOptions, PlaceableGeometryType, ExtrusionPreviewCallback } from './PlaceGeometryTool';\nexport { makeRectFootprint, makeCircleFootprint, distanceMeters } from './helpers/footprintGenerators';\nexport { SnapEngine } from './SnapEngine';\nexport type { SnapOptions, SnapResult, SnapSourceLayer } from './SnapEngine';\nexport { SnapEngine as AdvancedSnapEngine, AngleGuideManager, SnapVisualizer, SnapType, SNAP_PRIORITY, ENTITY_SNAP_TYPES, resolveSnapConfig, compareSnapCandidates, cross2D, lerpCoords, edgeMidpoint, segmentSegmentIntersection, extractEdges, extractVertices, nearestPointOnSegment, generateAngleGuides, snapToAngleGuide, } from './snap/index';\nexport type { SnapCandidate, SnapResult as AdvancedSnapResult, SnapConfig, SnapSourceLayer as AdvancedSnapSourceLayer, ResolvedSnapConfig, } from './snap/index';\nexport { AddVertexCommand, RemoveVertexCommand, CreateFeatureCommand, MoveVertexCommand, DeleteVertexCommand, MoveFeatureCommand, } from './commands/index';\nexport type { ITargetLayer } from './commands/index';\nexport { CURSOR_POINT_SYMBOL, VERTEX_SYMBOL, MIDPOINT_SYMBOL, PREVIEW_LINE_SYMBOL, PREVIEW_POLYGON_SYMBOL, OUTPUT_POINT_SYMBOL, OUTPUT_LINE_SYMBOL, OUTPUT_POLYGON_SYMBOL, SELECTED_LINE_SYMBOL, SELECTED_POLYGON_SYMBOL, SNAP_INDICATOR_SYMBOL, ANGLE_GUIDE_LINE_SYMBOL, } from './symbols/drawSymbols';\nexport { MeasureToolBase } from './MeasureToolBase';\nexport type { MeasureToolBaseOptions, MeasurementRecord } from './MeasureToolBase';\nexport { MeasurePointTool } from './MeasurePointTool';\nexport { MeasureLineTool } from './MeasureLineTool';\nexport { MeasurePolygonTool } from './MeasurePolygonTool';\nexport { EARTH_RADIUS as GEODESIC_EARTH_RADIUS, geodesicDistance, geodesicSegmentDistances, geodesicTotalDistance, sphericalPolygonArea, geodesicPerimeter, geodesicMidpoint, polygonCentroid, } from './helpers/geodesic';\nexport { MeasureLabelManager } from './helpers/MeasureLabelManager';\nexport type { MeasureLabel, MeasureLabelManagerOptions } from './helpers/MeasureLabelManager';\nexport { MEASURE_LINE_SYMBOL, MEASURE_VERTEX_SYMBOL, MEASURE_CURSOR_SYMBOL, MEASURE_POLYGON_SYMBOL, MEASURE_POINT_SYMBOL, } from './symbols/measureSymbols';\nexport { setupMeasurementTools } from './setupMeasurementTools';\nexport type { SetupMeasurementToolsOptions } from './setupMeasurementTools';\nexport { MilStdDrawTool } from './MilStdDrawTool';\nexport type { MilStdDrawToolOptions } from './MilStdDrawTool';\nexport { MilStdEditTool } from './MilStdEditTool';\nexport type { MilStdEditToolOptions } from './MilStdEditTool';\nexport { LosTool } from './LosTool';\nexport type { LosToolOptions } from './LosTool';\nexport { screenDistance, midpoint, findNearestVertex, findNearestEdge, pointToSegmentDistance, generateFeatureId, generatePreviewId, } from './helpers/geometryHelpers';\nimport type { ToolManager, IPreviewLayer } from '@mapgpu/core';\nimport type { SnapOptions } from './SnapEngine';\nimport type { SnapConfig } from './snap/SnapTypes';\nimport type { ITargetLayer } from './commands/index';\nimport type { Feature } from '@mapgpu/core';\n/** Options for the drawing tool manager factory. */\nexport interface CreateDrawingToolManagerOptions {\n    /** Target layer where completed features are placed. */\n    targetLayer: ITargetLayer & {\n        getFeatures(): readonly Feature[];\n        readonly id: string;\n    };\n    /** Preview layer for rubber-band rendering. If not provided, one must be set on the ToolManager. */\n    previewLayer?: IPreviewLayer;\n    /** Legacy snap configuration. */\n    snap?: SnapOptions;\n    /** Advanced snap configuration (takes precedence over `snap`). */\n    snapConfig?: SnapConfig;\n}\n/**\n * Factory function to set up a ToolManager with all drawing tools registered.\n *\n * @param toolManager - The ToolManager instance (typically from view.toolManager).\n * @param options - Configuration including target layer and optional snap settings.\n * @returns The configured ToolManager.\n *\n * @example\n * ```ts\n * const tm = setupDrawingTools(view.toolManager, {\n *   targetLayer: outputLayer,\n *   previewLayer: previewLayer,\n *   snapConfig: { enabled: true },\n * });\n * tm.activateTool('draw-polygon');\n * ```\n */\nexport declare function setupDrawingTools(toolManager: ToolManager, options: CreateDrawingToolManagerOptions): ToolManager;\n//# sourceMappingURL=index.d.ts.map","setupMeasurementTools.d.ts":"/**\n * setupMeasurementTools — Factory function to register measurement tools.\n *\n * Creates a MeasureLabelManager and registers all three measurement tools\n * (point, line, polygon) on the given ToolManager.\n *\n * @example\n * ```ts\n * const { labelManager } = setupMeasurementTools(view.toolManager, {\n *   measurementLayer,\n *   labelContainer: view.canvas!.parentElement!,\n *   unitManager,\n *   toScreen: (lon, lat) => view.toScreen(lon, lat),\n * });\n * view.on('view-change', () => labelManager.updatePositions());\n * view.toolManager.activateTool('measure-line');\n * ```\n */\nimport type { ToolManager, IPreviewLayer } from '@mapgpu/core';\nimport { UnitManager } from '@mapgpu/core';\nimport { MeasureLabelManager } from './helpers/MeasureLabelManager';\nexport interface SetupMeasurementToolsOptions {\n    measurementLayer: IPreviewLayer;\n    labelContainer: HTMLElement;\n    unitManager: UnitManager;\n    toScreen: (lon: number, lat: number) => [number, number] | null;\n}\nexport declare function setupMeasurementTools(toolManager: ToolManager, options: SetupMeasurementToolsOptions): {\n    labelManager: MeasureLabelManager;\n};\n//# sourceMappingURL=setupMeasurementTools.d.ts.map","snap/AngleGuideManager.d.ts":"/**\n * AngleGuideManager — Tracks hover origins and generates angle guide snaps.\n *\n * Origins are registered when:\n * 1. User hovers near a snap point for longer than the threshold (auto-mark)\n * 2. User places a vertex during drawing (explicit add)\n *\n * Guide rays are emitted from each origin at configured angle intervals.\n */\nexport declare class AngleGuideManager {\n    private _origins;\n    private _hoverThreshold;\n    private _hover;\n    constructor(hoverThresholdMs?: number);\n    /** Explicitly add an origin (e.g., when user places a vertex). */\n    addOrigin(coords: [number, number]): void;\n    /** Update hover tracking. Call on every pointer move. */\n    updateHover(snapCoords: [number, number] | null, now?: number): void;\n    /**\n     * Find the best angle guide snap for the current cursor position.\n     * Returns a partial SnapCandidate (coords + screenDistance) or null.\n     */\n    findGuideSnap(screenX: number, screenY: number, toScreen: (lon: number, lat: number) => [number, number] | null, tolerance: number, angleIntervals: number[]): {\n        coords: [number, number];\n        screenDistance: number;\n    } | null;\n    /** Get current origins (read-only). */\n    get origins(): readonly [number, number][];\n    /** Clear all origins and hover state. */\n    reset(): void;\n}\n//# sourceMappingURL=AngleGuideManager.d.ts.map","snap/SnapEngine.d.ts":"/**\n * SnapEngine — Multi-type snap engine with priority-based candidate selection.\n *\n * Supports: endpoint, midpoint, intersection, nearest-on-edge, point, and angle guide snaps.\n * Follows openwebcad's two-phase pattern: entity snaps first, angle guides as fallback.\n */\nimport { SnapType, type SnapResult, type SnapConfig, type SnapSourceLayer, type ResolvedSnapConfig } from './SnapTypes';\nimport { AngleGuideManager } from './AngleGuideManager';\nexport declare class SnapEngine {\n    private _config;\n    private _sourceLayers;\n    readonly angleGuideManager: AngleGuideManager;\n    constructor(config?: SnapConfig);\n    addSourceLayer(layer: SnapSourceLayer): void;\n    removeSourceLayer(layer: SnapSourceLayer): void;\n    get config(): Readonly<ResolvedSnapConfig>;\n    setTolerance(px: number): void;\n    setEnabled(enabled: boolean): void;\n    enableType(type: SnapType): void;\n    disableType(type: SnapType): void;\n    /**\n     * Extra vertices to include in endpoint snapping (e.g. in-progress drawing vertices).\n     * Set by drawing tools before calling snap(), cleared after draw-complete.\n     */\n    activeVertices: [number, number][];\n    snap(screenX: number, screenY: number, mapCoords: [number, number], toScreen: (lon: number, lat: number) => [number, number] | null): SnapResult;\n    private _findIntersections;\n}\n//# sourceMappingURL=SnapEngine.d.ts.map","snap/SnapTypes.d.ts":"/**\n * Snap type definitions, candidate interfaces, and configuration.\n *\n * Inspired by openwebcad's snap system — adapted for GIS map coordinates\n * with screen-space tolerance matching.\n */\nimport type { Feature } from '@mapgpu/core';\n/** All supported snap point types. */\nexport declare enum SnapType {\n    /** Line/polygon vertex endpoints. */\n    EndPoint = \"endpoint\",\n    /** Point geometry features. */\n    Point = \"point\",\n    /** Midpoint of each edge segment. */\n    MidPoint = \"midpoint\",\n    /** Intersection of edges from different features. */\n    Intersection = \"intersection\",\n    /** Perpendicular nearest point on an edge. */\n    Nearest = \"nearest\",\n    /** Snap to angle guide lines (45°, 90° etc.). */\n    AngleGuide = \"angle-guide\"\n}\n/**\n * Default priority for each snap type (lower number = higher priority).\n * Entity snaps (0–49) always beat guide snaps (50+).\n */\nexport declare const SNAP_PRIORITY: Record<SnapType, number>;\n/** All entity-based snap types (excludes angle guides). */\nexport declare const ENTITY_SNAP_TYPES: ReadonlySet<SnapType>;\n/** A single snap candidate produced during the snap search. */\nexport interface SnapCandidate {\n    /** Snapped geographic [lon, lat]. */\n    coords: [number, number];\n    /** Snap type that produced this candidate. */\n    type: SnapType;\n    /** Screen-space pixel distance from cursor. */\n    screenDistance: number;\n    /** Source feature ID, if snap came from a feature. */\n    sourceFeatureId?: string | number;\n    /** Priority (lower wins). Derived from SNAP_PRIORITY unless overridden. */\n    priority: number;\n}\n/** Result returned by the snap engine. */\nexport interface SnapResult {\n    /** Final snapped coordinates (best candidate, or raw mapCoords if no snap). */\n    coords: [number, number];\n    /** Type of snap that occurred. `'none'` if no candidate within tolerance. */\n    type: SnapType | 'none';\n    /** Source feature ID, if snapped to a feature. */\n    sourceFeatureId?: string | number;\n    /** All candidates within tolerance, sorted by priority then distance. */\n    candidates: SnapCandidate[];\n}\n/** Configuration for the snap engine. */\nexport interface SnapConfig {\n    /** Master on/off switch. Default: `true`. */\n    enabled?: boolean;\n    /** Active snap types. Default: all entity snaps enabled, angle guide disabled. */\n    enabledTypes?: Set<SnapType>;\n    /** Screen-pixel tolerance for proximity. Default: `10`. */\n    tolerance?: number;\n    /** Angle guide intervals in degrees. Default: `[0, 45, 90, 135]`. */\n    angleGuideIntervals?: number[];\n    /** Hover duration (ms) before a point becomes an angle guide origin. Default: `500`. */\n    angleGuideHoverThreshold?: number;\n    /** Custom priority overrides (SnapType → priority number). */\n    priorityOverrides?: Partial<Record<SnapType, number>>;\n}\n/** Resolved (non-optional) snap configuration. */\nexport interface ResolvedSnapConfig {\n    enabled: boolean;\n    enabledTypes: Set<SnapType>;\n    tolerance: number;\n    angleGuideIntervals: number[];\n    angleGuideHoverThreshold: number;\n    priorities: Record<SnapType, number>;\n}\n/** A layer that provides features for snapping. */\nexport interface SnapSourceLayer {\n    getFeatures(): readonly Feature[];\n}\n/** Default snap configuration values. */\nexport declare function resolveSnapConfig(cfg?: SnapConfig): ResolvedSnapConfig;\n/** Compare two candidates: lower priority wins, ties broken by closer distance. */\nexport declare function compareSnapCandidates(a: SnapCandidate, b: SnapCandidate): number;\n//# sourceMappingURL=SnapTypes.d.ts.map","snap/SnapVisualizer.d.ts":"/**\n * SnapVisualizer — Renders snap indicator features to the preview layer\n * and updates the cursor to reflect the active snap type.\n *\n * Stateless utility. Call `render()` from tool's `_updatePreview()`.\n */\nimport type { IPreviewLayer } from '@mapgpu/core';\nimport { type SnapResult } from './SnapTypes';\nexport declare class SnapVisualizer {\n    /**\n     * Update snap visualization on the preview layer and cursor.\n     *\n     * - Removes previous indicators.\n     * - Adds snap point marker if snap is active.\n     * - Adds guide line for angle guide snaps.\n     * - Updates cursor to reflect active snap type.\n     */\n    static render(previewLayer: IPreviewLayer, snapResult: SnapResult | null, guideOrigin?: [number, number]): void;\n    /** Remove all snap indicators. */\n    static clear(previewLayer: IPreviewLayer): void;\n    /** Get the cursor string for a snap result. Returns `null` if no snap. */\n    static getCursor(snapResult: SnapResult | null): string | null;\n}\n//# sourceMappingURL=SnapVisualizer.d.ts.map","snap/index.d.ts":"/**\n * Snap module — Advanced multi-type snapping system.\n */\nexport { SnapEngine } from './SnapEngine';\nexport { AngleGuideManager } from './AngleGuideManager';\nexport { SnapVisualizer } from './SnapVisualizer';\nexport { SnapType, SNAP_PRIORITY, ENTITY_SNAP_TYPES, resolveSnapConfig, compareSnapCandidates, } from './SnapTypes';\nexport type { SnapCandidate, SnapResult, SnapConfig, SnapSourceLayer, ResolvedSnapConfig, } from './SnapTypes';\nexport { cross2D, lerpCoords, edgeMidpoint, segmentSegmentIntersection, extractEdges, extractVertices, nearestPointOnSegment, generateAngleGuides, snapToAngleGuide, } from './snapGeometry';\n//# sourceMappingURL=index.d.ts.map","snap/snapGeometry.d.ts":"/**\n * Pure geometry functions for the snap system.\n *\n * All functions are stateless — no side effects, no DOM, no GPU.\n * Screen-space helpers reuse `screenDistance` and `pointToSegmentDistance`\n * from `../helpers/geometryHelpers.ts`.\n */\nimport type { Geometry } from '@mapgpu/core';\n/** 2D cross product of vectors (ax,ay) × (bx,by). */\nexport declare function cross2D(ax: number, ay: number, bx: number, by: number): number;\n/** Linear interpolation between two coordinate pairs. */\nexport declare function lerpCoords(a: [number, number], b: [number, number], t: number): [number, number];\n/** Midpoint of two coordinate pairs. */\nexport declare function edgeMidpoint(a: [number, number], b: [number, number]): [number, number];\n/**\n * Find intersection point of two line segments in 2D.\n *\n * Uses parametric form:\n *   P = p1 + t*(p2 - p1)\n *   Q = p3 + u*(p4 - p3)\n *\n * Returns [lon, lat] or null if no intersection.\n */\nexport declare function segmentSegmentIntersection(p1: [number, number], p2: [number, number], p3: [number, number], p4: [number, number]): [number, number] | null;\ntype Edge = [[number, number], [number, number]];\n/**\n * Extract edges as pairs of coordinate tuples from any GeoJSON geometry.\n *\n * - Point / MultiPoint → empty\n * - LineString → N-1 edges\n * - Polygon → ring edges (including closing segment)\n * - Multi* → recursive flatten\n */\nexport declare function extractEdges(geometry: Geometry): Edge[];\n/**\n * Extract all vertex coordinates from any GeoJSON geometry.\n */\nexport declare function extractVertices(geometry: Geometry): [number, number][];\nexport interface NearestOnSegmentResult {\n    coords: [number, number];\n    screenDistance: number;\n    t: number;\n}\n/**\n * Find the nearest point on a map-space segment from a screen cursor.\n *\n * 1. Projects segment endpoints to screen space.\n * 2. Computes perpendicular projection in screen space (reuses pointToSegmentDistance).\n * 3. Interpolates in map coordinates using the parametric t.\n */\nexport declare function nearestPointOnSegment(segA: [number, number], segB: [number, number], screenX: number, screenY: number, toScreen: (lon: number, lat: number) => [number, number] | null): NearestOnSegmentResult | null;\nexport interface AngleGuide {\n    origin: [number, number];\n    direction: [number, number];\n    angleDeg: number;\n}\n/**\n * Generate angle guide rays from origin points.\n *\n * Each origin emits rays at the specified angle intervals.\n * Angles are measured from geographic north (0°=N, 90°=E).\n * Only half-circle (0–179°) is needed since each line extends both ways.\n */\nexport declare function generateAngleGuides(origins: [number, number][], anglesDeg: number[]): AngleGuide[];\n/**\n * Snap cursor to an angle guide ray (infinite line through origin).\n *\n * Projects the ray to screen space, finds the perpendicular projection\n * of the cursor onto the line, and returns the map-space coordinate.\n */\nexport declare function snapToAngleGuide(guide: AngleGuide, screenX: number, screenY: number, toScreen: (lon: number, lat: number) => [number, number] | null, tolerance: number): {\n    coords: [number, number];\n    screenDistance: number;\n} | null;\nexport {};\n//# sourceMappingURL=snapGeometry.d.ts.map","symbols/drawSymbols.d.ts":"/**\n * Default preview/feedback symbols for drawing tools.\n */\nimport type { PointSymbol, LineSymbol, PolygonSymbol } from '@mapgpu/core';\n/** Ghost vertex at cursor position. */\nexport declare const CURSOR_POINT_SYMBOL: PointSymbol;\n/** Placed vertex handle. */\nexport declare const VERTEX_SYMBOL: PointSymbol;\n/** Midpoint handle for edit tool. */\nexport declare const MIDPOINT_SYMBOL: PointSymbol;\n/** Rubber-band line during drawing. */\nexport declare const PREVIEW_LINE_SYMBOL: LineSymbol;\n/** Preview polygon fill during drawing. */\nexport declare const PREVIEW_POLYGON_SYMBOL: PolygonSymbol;\n/** Completed feature point style. */\nexport declare const OUTPUT_POINT_SYMBOL: PointSymbol;\nexport declare const OUTPUT_LINE_SYMBOL: LineSymbol;\nexport declare const OUTPUT_POLYGON_SYMBOL: PolygonSymbol;\n/** Selected feature highlight for edit tool. */\nexport declare const SELECTED_LINE_SYMBOL: LineSymbol;\nexport declare const SELECTED_POLYGON_SYMBOL: PolygonSymbol;\n/** Snap indicator point. */\nexport declare const SNAP_INDICATOR_SYMBOL: PointSymbol;\n/** Angle guide dashed line. */\nexport declare const ANGLE_GUIDE_LINE_SYMBOL: LineSymbol;\n//# sourceMappingURL=drawSymbols.d.ts.map","symbols/measureSymbols.d.ts":"/**\n * Default symbols for measurement tools.\n *\n * Uses an orange theme to visually distinguish from drawing tools (blue).\n * RGB: 255, 87, 34 — Material Design Deep Orange 500.\n */\nimport type { PointSymbol, LineSymbol, PolygonSymbol } from '@mapgpu/core';\n/** Dashed measurement line. */\nexport declare const MEASURE_LINE_SYMBOL: LineSymbol;\n/** Placed vertex handle (white fill, orange outline). */\nexport declare const MEASURE_VERTEX_SYMBOL: PointSymbol;\n/** Ghost cursor point during measurement. */\nexport declare const MEASURE_CURSOR_SYMBOL: PointSymbol;\n/** Measurement polygon fill (translucent orange). */\nexport declare const MEASURE_POLYGON_SYMBOL: PolygonSymbol;\n/** Completed measurement point. */\nexport declare const MEASURE_POINT_SYMBOL: PointSymbol;\n//# sourceMappingURL=measureSymbols.d.ts.map"},"@mapgpu/analysis":{"AnalysisService.d.ts":"/**\n * AnalysisService — Facade / orchestration.\n *\n * Implements IAnalysis interface. Delegates each analysis type to the\n * corresponding specialised class.\n * IWasmCore is injected via constructor (dependency injection).\n */\nimport type { IAnalysis, IWasmCore, LosParams, LosAnalysisResult, ElevationQueryParams, ElevationQueryResult, BufferParams, BufferResult, RouteSampleParams, RouteSampleResult } from '@mapgpu/core';\nexport declare class AnalysisService implements IAnalysis {\n    private readonly losAnalysis;\n    private readonly elevationQuery;\n    private readonly bufferAnalysis;\n    private readonly routeSampler;\n    constructor(wasmCore: IWasmCore);\n    runLos(params: LosParams): Promise<LosAnalysisResult>;\n    queryElevation(params: ElevationQueryParams): Promise<ElevationQueryResult>;\n    buffer(params: BufferParams): Promise<BufferResult>;\n    sampleRoute(params: RouteSampleParams): Promise<RouteSampleResult>;\n}\n//# sourceMappingURL=AnalysisService.d.ts.map","BufferAnalysis.d.ts":"/**\n * Buffer Analysis — generates buffer polygons around geometries.\n *\n * Point buffer: circular polygon via haversine bearing/distance.\n * No external geo library dependency.\n */\nimport type { BufferParams, BufferResult } from '@mapgpu/core';\nexport declare class BufferAnalysis {\n    buffer(params: BufferParams): Promise<BufferResult>;\n    private pointBuffer;\n}\n//# sourceMappingURL=BufferAnalysis.d.ts.map","BuildingObstacleProvider.d.ts":"/**\n * BuildingObstacleProvider — Treats building footprints as elevation obstacles.\n *\n * Uses two detection methods:\n * 1. Point-in-polygon: sample point lands inside a building → return building height\n * 2. Line-segment intersection: LOS line crosses a building edge → return building height\n *\n * Method 2 is critical because sample points can skip over narrow buildings\n * or clip building corners without landing inside the footprint.\n */\nimport type { Feature } from '@mapgpu/core';\nimport type { IElevationProvider } from './IElevationProvider';\nexport interface BuildingObstacleProviderOptions {\n    /** Function that returns current building features */\n    getFeatures: () => readonly Feature[];\n    /** Attribute name for building height (e.g., 'render_height') */\n    heightField: string;\n    /** Optional attribute name for min height (e.g., 'render_min_height') */\n    minHeightField?: string;\n    /** Optional base terrain provider for ground elevation */\n    baseProvider?: IElevationProvider;\n}\nexport declare class BuildingObstacleProvider implements IElevationProvider {\n    private readonly _getFeatures;\n    private readonly _heightField;\n    private readonly _minHeightField?;\n    private readonly _baseProvider?;\n    private _cachedBuildings;\n    private _cachedFeaturesRef;\n    constructor(options: BuildingObstacleProviderOptions);\n    sampleElevation(lon: number, lat: number): number | null;\n    /**\n     * Batch elevation query with line-segment intersection.\n     *\n     * For each consecutive pair of sample points, tests if the segment\n     * intersects any building edge. This catches buildings that fall\n     * between sample points (narrow buildings, corner clips).\n     */\n    sampleElevationBatch(points: Float64Array): Float64Array;\n    /** Lazily build/cache building data from features. */\n    private _getBuildings;\n}\n//# sourceMappingURL=BuildingObstacleProvider.d.ts.map","CompositeElevationProvider.d.ts":"/**\n * CompositeElevationProvider — chains multiple IElevationProviders.\n *\n * Returns the maximum elevation from all providers at each point.\n * Useful for combining terrain + building obstacle providers.\n */\nimport type { IElevationProvider } from './IElevationProvider';\nexport declare class CompositeElevationProvider implements IElevationProvider {\n    private readonly _providers;\n    constructor(providers: IElevationProvider[]);\n    sampleElevation(lon: number, lat: number): number | null;\n    sampleElevationBatch(points: Float64Array): Float64Array;\n}\n//# sourceMappingURL=CompositeElevationProvider.d.ts.map","ElevationQuery.d.ts":"/**\n * Elevation Query — returns elevation values for a list of geographic points.\n *\n * Uses mock terrain data (no real terrain service connection).\n */\nimport type { ElevationQueryParams, ElevationQueryResult } from '@mapgpu/core';\nexport declare class ElevationQuery {\n    queryElevation(params: ElevationQueryParams): Promise<ElevationQueryResult>;\n}\n//# sourceMappingURL=ElevationQuery.d.ts.map","IElevationProvider.d.ts":"/**\n * IElevationProvider — Terrain elevation data source interface.\n *\n * Abstracts away the source of elevation data (DTED, TerrainRGB, buildings, etc.)\n * so that analysis algorithms can query heights without knowing the data origin.\n */\nexport interface IElevationProvider {\n    /** Single-point elevation query (metres). Returns null if no data available. */\n    sampleElevation(lon: number, lat: number): number | null;\n    /**\n     * Batch elevation query for performance.\n     * Input: Float64Array of interleaved [lon0, lat0, lon1, lat1, ...]\n     * Output: Float64Array of heights [h0, h1, ...] — NaN where data unavailable.\n     */\n    sampleElevationBatch(points: Float64Array): Float64Array;\n}\n//# sourceMappingURL=IElevationProvider.d.ts.map","LosAnalysis.d.ts":"/**\n * Line of Sight analysis.\n *\n * Delegates segment generation and LOS computation to IWasmCore.\n */\nimport type { IWasmCore, LosParams, LosAnalysisResult } from '@mapgpu/core';\nimport type { IElevationProvider } from './IElevationProvider';\nexport declare class LosAnalysis {\n    private readonly wasm;\n    private _elevationProvider;\n    constructor(wasm: IWasmCore, elevationProvider?: IElevationProvider);\n    /** Set or replace the elevation provider at runtime. */\n    setElevationProvider(provider: IElevationProvider | null): void;\n    runLos(params: LosParams): Promise<LosAnalysisResult>;\n}\n//# sourceMappingURL=LosAnalysis.d.ts.map","RouteSampler.d.ts":"/**\n * Route Sampler — samples points along a route at regular intervals.\n *\n * Each sample: [lon, lat, elevation, cumulativeDistance].\n * Uses haversine for distance calculation.\n */\nimport type { RouteSampleParams, RouteSampleResult } from '@mapgpu/core';\nexport declare class RouteSampler {\n    sampleRoute(params: RouteSampleParams): Promise<RouteSampleResult>;\n}\n//# sourceMappingURL=RouteSampler.d.ts.map","TerrainElevationProvider.d.ts":"/**\n * TerrainElevationProvider — wraps ITerrainLayer(s) as an IElevationProvider.\n *\n * Queries height tiles from DTED or TerrainRGB layers using bilinear interpolation.\n * Tries each layer in order and returns the first valid elevation.\n */\nimport type { ITerrainLayer } from '@mapgpu/core';\nimport type { IElevationProvider } from './IElevationProvider';\nexport declare class TerrainElevationProvider implements IElevationProvider {\n    private readonly _layers;\n    constructor(layers: ITerrainLayer[]);\n    sampleElevation(lon: number, lat: number): number | null;\n    sampleElevationBatch(points: Float64Array): Float64Array;\n}\n//# sourceMappingURL=TerrainElevationProvider.d.ts.map","__mocks__/MockWasmCore.d.ts":"/**\n * Mock IWasmCore for testing analysis classes.\n *\n * Implements only the methods used by the analysis package.\n */\nimport type { IWasmCore, TriangulateResult, ClusterOptions, ClusterResult, SpatialIndexHandle, SpatialQueryResult, LosResult, BinaryFeatureBuffer } from '@mapgpu/core';\nexport declare class MockWasmCore implements IWasmCore {\n    init(): Promise<void>;\n    reprojectPoints(coords: Float64Array, _fromEpsg: number, _toEpsg: number): Float64Array;\n    triangulate(vertices: Float64Array, _holeIndices: Uint32Array): TriangulateResult;\n    tessellateLines(positions: Float64Array, _offsets: Uint32Array, _lineWidth: number): Float64Array;\n    clusterPoints(_points: Float64Array, _options: ClusterOptions): ClusterResult;\n    buildSpatialIndex(_points: Float64Array): SpatialIndexHandle;\n    querySpatialIndex(_handle: SpatialIndexHandle, _bbox: Float64Array): SpatialQueryResult;\n    /**\n     * Generate N evenly-spaced 3D sample points along observer→target line.\n     * Returns interleaved [x0,y0,z0, x1,y1,z1, ...].\n     */\n    generateLosSegments(observer: Float64Array, target: Float64Array, sampleCount: number): Float64Array;\n    /**\n     * Mock LOS computation.\n     * If all elevations are 0 (flat terrain) and observer has positive offset,\n     * the line is visible. Otherwise detect intersection.\n     */\n    computeLos(segments: Float64Array, elevations: Float64Array, observerOffset: number, targetOffset: number): LosResult;\n    parseGeojson(_json: string): BinaryFeatureBuffer;\n    parseMvt(_tile: Uint8Array): BinaryFeatureBuffer;\n    geodeticToEcef(coords: Float64Array): Float64Array;\n    encodeEcefDouble(_ecefCoords: Float64Array): Float32Array;\n    destroy(): void;\n}\n//# sourceMappingURL=MockWasmCore.d.ts.map","geometry/line-intersects-polygon.d.ts":"/**\n * Line-segment vs polygon intersection test.\n *\n * Tests if a line segment intersects a polygon (with optional holes).\n * Returns the first intersection point with its fractional distance along the segment,\n * or null if no intersection.\n */\nexport interface IntersectionResult {\n    /** Fractional distance along segment (0 = start, 1 = end) */\n    t: number;\n    /** Intersection point [lon, lat] */\n    point: [number, number];\n}\n/**\n * Test if a line segment (p0→p1) intersects any edge of a polygon ring.\n * Returns the closest intersection (smallest t) or null.\n */\nexport declare function segmentIntersectsRing(p0x: number, p0y: number, p1x: number, p1y: number, ring: readonly (readonly number[])[]): IntersectionResult | null;\n/**\n * Test if a line segment intersects a polygon with optional holes.\n * Returns the closest intersection or null.\n */\nexport declare function segmentIntersectsPolygon(p0x: number, p0y: number, p1x: number, p1y: number, rings: readonly (readonly (readonly number[])[])[]): IntersectionResult | null;\n//# sourceMappingURL=line-intersects-polygon.d.ts.map","geometry/point-in-polygon.d.ts":"/**\n * Point-in-polygon test using ray-casting algorithm.\n *\n * Works with GeoJSON-style coordinate arrays.\n * Handles simple polygons and polygons with holes.\n */\n/**\n * Test if a point is inside a polygon ring (array of [x, y] coordinates).\n * Uses the ray-casting (crossing number) algorithm.\n */\nexport declare function pointInRing(px: number, py: number, ring: readonly (readonly number[])[]): boolean;\n/**\n * Test if a point is inside a polygon with optional holes.\n * rings[0] = exterior ring, rings[1..N] = holes.\n */\nexport declare function pointInPolygon(px: number, py: number, rings: readonly (readonly (readonly number[])[])[]): boolean;\n//# sourceMappingURL=point-in-polygon.d.ts.map","haversine.d.ts":"/**\n * Haversine distance calculation utilities.\n *\n * All coordinates are WGS84 (EPSG:4326), [longitude, latitude] order.\n * No external dependencies.\n */\ndeclare const EARTH_RADIUS = 6371000;\n/**\n * Haversine distance between two geographic points.\n * @returns distance in metres\n */\nexport declare function haversineDistance(lon1: number, lat1: number, lon2: number, lat2: number): number;\n/**\n * Compute a destination point given start, bearing and distance.\n * @param lon   start longitude (degrees)\n * @param lat   start latitude  (degrees)\n * @param bearing  initial bearing (radians, clockwise from north)\n * @param distance distance in metres\n * @returns [lon, lat] in degrees\n */\nexport declare function destinationPoint(lon: number, lat: number, bearing: number, distance: number): [number, number];\n/**\n * Interpolate linearly between two geographic points by fraction t ∈ [0,1].\n * Uses great-circle interpolation (spherical).\n * @returns [lon, lat] in degrees\n */\nexport declare function interpolateGreatCircle(lon1: number, lat1: number, lon2: number, lat2: number, t: number): [number, number];\nexport { EARTH_RADIUS };\n//# sourceMappingURL=haversine.d.ts.map","index.d.ts":"/**\n * @mapgpu/analysis\n *\n * Spatial analysis modules — LOS, buffer, elevation query, route sampling.\n */\nexport { LosAnalysis } from './LosAnalysis';\nexport { ElevationQuery } from './ElevationQuery';\nexport { BufferAnalysis } from './BufferAnalysis';\nexport { RouteSampler } from './RouteSampler';\nexport { AnalysisService } from './AnalysisService';\nexport { haversineDistance, destinationPoint, interpolateGreatCircle, EARTH_RADIUS, } from './haversine';\nexport type { IElevationProvider } from './IElevationProvider';\nexport { TerrainElevationProvider } from './TerrainElevationProvider';\nexport { BuildingObstacleProvider } from './BuildingObstacleProvider';\nexport type { BuildingObstacleProviderOptions } from './BuildingObstacleProvider';\nexport { CompositeElevationProvider } from './CompositeElevationProvider';\nexport { pointInRing, pointInPolygon } from './geometry/point-in-polygon';\nexport { segmentIntersectsRing, segmentIntersectsPolygon } from './geometry/line-intersects-polygon';\nexport type { IntersectionResult } from './geometry/line-intersects-polygon';\n//# sourceMappingURL=index.d.ts.map"},"@mapgpu/terrain":{"DEMData.d.ts":"/**\n * DEMData — Padded Elevation Tile Data\n *\n * MapLibre GL JS'nin dem_data.ts'inden adapte edildi.\n * Her tile (dim+2)×(dim+2) Float32Array tutar: 1px padding her yonde.\n * backfillBorder() ile komsu tile'lardan kenar pikselleri kopyalanir\n * → bilinear sampling tile sinilarinda seamless calisir.\n *\n * MapLibre'den farklar:\n * - RGBA Uint32 yerine Float32Array (hem RGB decode hem DTED float destegi)\n * - Static factory methods: fromRGBA(), fromFloat32()\n */\nexport type DEMEncoding = 'mapbox' | 'terrarium' | 'custom';\nexport interface DEMCustomFactors {\n    r: number;\n    g: number;\n    b: number;\n    base: number;\n}\nexport declare class DEMData {\n    /** Core data dimension (e.g. 256) — padding haric */\n    readonly dim: number;\n    /** Stride = dim + 2 (1px padding each side) */\n    readonly stride: number;\n    /** Padded elevation data: stride x stride */\n    readonly data: Float32Array;\n    /** Minimum elevation in this tile */\n    min: number;\n    /** Maximum elevation in this tile */\n    max: number;\n    private constructor();\n    /**\n     * Pixel index accounting for 1px border padding.\n     * Valid range: x,y in [-1 .. dim]\n     */\n    private _idx;\n    /**\n     * Get elevation at pixel coordinate (border-aware: -1..dim).\n     */\n    get(x: number, y: number): number;\n    /**\n     * Set elevation at pixel coordinate (border-aware: -1..dim).\n     */\n    set(x: number, y: number, value: number): void;\n    /**\n     * Clone edge pixels into the 1px border padding.\n     * Called on construction before any backfillBorder.\n     * This gives usable (though not perfect) border values\n     * until real neighbor data arrives.\n     */\n    private _initBorder;\n    /**\n     * Copy border pixels from a neighbor tile.\n     *\n     * dx/dy encode the neighbor direction:\n     *   dx=-1 → neighbor is to the LEFT   → copy its right edge to our left border\n     *   dx=+1 → neighbor is to the RIGHT  → copy its left edge to our right border\n     *   dy=-1 → neighbor is ABOVE (north)  → copy its bottom edge to our top border\n     *   dy=+1 → neighbor is BELOW (south)  → copy its top edge to our bottom border\n     *   Both non-zero → corner neighbor (single pixel copy)\n     *\n     * MapLibre-compatible neighbor convention.\n     */\n    backfillBorder(neighbor: DEMData, dx: number, dy: number): void;\n    /**\n     * Decode an RGBA-encoded DEM tile (Mapbox RGB or Terrarium encoding)\n     * into a padded Float32Array.\n     *\n     * @param rgba - Raw RGBA pixel data (4 bytes per pixel, row-major)\n     * @param width - Image width (should equal desired dim)\n     * @param height - Image height (should equal desired dim)\n     * @param encoding - 'mapbox' | 'terrarium' | 'custom'\n     * @param factors - Custom decode factors (only for 'custom' encoding)\n     */\n    static fromRGBA(rgba: Uint8Array, width: number, height: number, encoding: DEMEncoding, factors?: DEMCustomFactors): DEMData;\n    /**\n     * Create DEMData from an existing Float32Array elevation grid.\n     * Used for DTED compatibility — takes a flat dim×dim grid and wraps\n     * it in (dim+2)×(dim+2) padded storage.\n     *\n     * @param grid - Elevation values (row-major, dim×dim)\n     * @param width - Grid width\n     * @param height - Grid height\n     */\n    static fromFloat32(grid: Float32Array, width: number, height: number): DEMData;\n    /**\n     * Create DEMData from an already-padded Float32Array (stride×stride).\n     * No border init needed — caller is responsible for providing correct padding.\n     * Used when the source data already includes the 1px border (e.g. DTED extended extent).\n     *\n     * @param data - Padded elevation data (stride×stride where stride = dim+2)\n     * @param dim - Core data dimension (padding excluded)\n     */\n    static fromPaddedFloat32(data: Float32Array, dim: number): DEMData;\n}\n//# sourceMappingURL=DEMData.d.ts.map","DTEDLayer.d.ts":"import type { ITerrainLayer, TerrainHeightTileData, TerrainHillshadeTileData, TerrainLighting3DOptions } from '@mapgpu/core';\nimport { LayerBase } from '@mapgpu/layers';\nimport type { DTEDLayerOptions, DTEDLevelName, DTEDLocalFile, DTEDMode, Hillshade2DOptions } from './types';\nexport declare class DTEDLayer extends LayerBase implements ITerrainLayer {\n    readonly type: \"terrain\";\n    readonly mode: DTEDMode;\n    readonly levels: readonly DTEDLevelName[];\n    readonly exaggeration: number;\n    readonly minZoom: number;\n    readonly maxZoom: number;\n    private readonly _store;\n    private readonly _tileSize;\n    private readonly _maxReadyTiles;\n    private readonly _urlForCell?;\n    private readonly _localFiles;\n    private _hillshade2D;\n    private _lighting3D;\n    private readonly _readyHeight;\n    private readonly _readyHillshade;\n    private readonly _requestInFlight;\n    private readonly _remoteCellInFlight;\n    private _hillshadeFn;\n    private _wasmInitPromise;\n    constructor(options?: DTEDLayerOptions);\n    addLocalFile(file: DTEDLocalFile): Promise<void>;\n    protected onLoad(): Promise<void>;\n    refresh(): void;\n    destroy(): void;\n    /** Return diagnostic info about the tile store and ready caches. */\n    getStoreInfo(): {\n        cells: {\n            level: string;\n            origin: [number, number];\n            extent: {\n                minX: number;\n                minY: number;\n                maxX: number;\n                maxY: number;\n            };\n        }[];\n        readyHeightCount: number;\n        readyHillshadeCount: number;\n        fullExtent: {\n            minX: number;\n            minY: number;\n            maxX: number;\n            maxY: number;\n        } | null;\n    };\n    private _emitDebug;\n    requestTile(z: number, x: number, y: number): Promise<void>;\n    get hillshade2D(): Readonly<Hillshade2DOptions>;\n    get lighting3D(): Readonly<TerrainLighting3DOptions>;\n    setHillshade2D(options: Partial<Hillshade2DOptions>): void;\n    setLighting3D(options: Partial<TerrainLighting3DOptions>): void;\n    getReadyHeightTile(z: number, x: number, y: number): TerrainHeightTileData | null;\n    getReadyHillshadeTile(z: number, x: number, y: number): TerrainHillshadeTileData | null;\n    private _buildReadyTile;\n    private _sampleHeightGrid;\n    private _ensureRemoteCoverage;\n    private _ensureRemoteCell;\n    private _evictReadyTiles;\n    private _updateFullExtentFromStore;\n    private _tryInitWasmHillshade;\n}\n//# sourceMappingURL=DTEDLayer.d.ts.map","DTEDStore.d.ts":"/**\n * DTEDStore — In-Memory Terrain Tile Storage\n *\n * Spatial-indexed DTED tile storage with bbox intersection queries.\n * Uses a simple Map + AABB intersection for efficient spatial queries.\n */\nimport type { Extent } from '@mapgpu/core';\nimport type { DTEDTile, DTEDTileMetadata, StorageStats } from './types';\nexport declare class DTEDStore {\n    private tiles;\n    private _initialized;\n    init(): Promise<void>;\n    get initialized(): boolean;\n    /**\n     * Store a parsed DTED tile.\n     */\n    storeTile(tile: DTEDTile): Promise<void>;\n    /**\n     * Query tiles whose extent intersects the given bbox.\n     */\n    queryTilesForExtent(extent: Extent): DTEDTileMetadata[];\n    /**\n     * Load full tile data (elevations) for a given tile ID.\n     */\n    loadTileData(tileId: string): DTEDTile | null;\n    /**\n     * Get elevation at a specific geographic point.\n     * Returns null if no tile covers the point.\n     */\n    getElevationAtPoint(lon: number, lat: number): number | null;\n    /**\n     * Sample elevation data over a regular grid for an arbitrary geographic extent.\n     * Returns a Float32Array (width × height) suitable for creating a GPU r32float texture.\n     * Cells outside DTED coverage are set to NaN.\n     *\n     * @param extent - Geographic extent in EPSG:4326 (lon/lat degrees)\n     * @param width - Output grid width (columns)\n     * @param height - Output grid height (rows)\n     * @returns Float32Array of elevation values (row-major, top-left = north-west)\n     */\n    getElevationGrid(extent: Extent, width: number, height: number): Float32Array;\n    /**\n     * Check whether stored tiles have data near the center of the given extent.\n     * Uses center-point test only: far-zoom tiles are large (~60°×60°) and a\n     * strict 5-point full-coverage check causes all tiles to fail when DTED\n     * covers only a small region. Partial-coverage edges produce NaN→0 (sea\n     * level) which is visually acceptable.\n     */\n    hasDataForExtent(extent: Extent): boolean;\n    /**\n     * List all stored tiles.\n     */\n    listTiles(): DTEDTileMetadata[];\n    /**\n     * Delete a tile by ID.\n     */\n    deleteTile(tileId: string): boolean;\n    /**\n     * Get storage statistics.\n     */\n    getStorageStats(): StorageStats;\n    /**\n     * Compute the union extent of all stored tiles.\n     */\n    getFullExtent(): Extent | null;\n    /**\n     * Get global min/max elevation across all tiles.\n     */\n    getElevationRange(): [number, number];\n    /**\n     * Destroy store and free memory.\n     */\n    destroy(): void;\n    private _tileToMetadata;\n}\n//# sourceMappingURL=DTEDStore.d.ts.map","DTEDTileStore.d.ts":"import type { DTEDLevelName, DTEDTile } from './types';\nexport declare class DTEDTileStore {\n    private readonly _local;\n    private readonly _remote;\n    constructor();\n    addLocal(tile: DTEDTile): void;\n    addRemote(tile: DTEDTile, forcedLevel?: DTEDLevelName): void;\n    hasTile(level: DTEDLevelName, lon: number, lat: number): boolean;\n    getTile(level: DTEDLevelName, lon: number, lat: number): DTEDTile | null;\n    sampleElevation(lon: number, lat: number, levels: readonly DTEDLevelName[]): number | null;\n    getFullExtent(): {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    } | null;\n    private _stitchWithNeighbors;\n}\n//# sourceMappingURL=DTEDTileStore.d.ts.map","DuckDBDTEDStore.d.ts":"/**\n * DuckDBDTEDStore — DuckDB-WASM Terrain Tile Storage\n *\n * DuckDB-WASM powered spatial tile storage.\n * Metadata lives in a SQL table with spatial index.\n * Elevation data (Int16Array) stays in JS Map for sync per-frame access.\n *\n * Same public API as DTEDStore — drop-in replacement.\n */\nimport type { Extent } from '@mapgpu/core';\nimport type { DTEDTile, DTEDTileMetadata, StorageStats } from './types';\nexport declare class DuckDBDTEDStore {\n    private _db;\n    private _conn;\n    private _initialized;\n    /** In-memory elevation data — sync access for per-frame render loop */\n    private _elevations;\n    /** Cached metadata for sync queryTilesForExtent() — rebuilt on mutations */\n    private _metadataCache;\n    private _cacheDirty;\n    get initialized(): boolean;\n    /**\n     * Initialize DuckDB-WASM and create metadata table.\n     */\n    init(): Promise<void>;\n    /**\n     * Store a parsed DTED tile.\n     * Metadata goes to DuckDB, elevation data stays in JS Map.\n     */\n    storeTile(tile: DTEDTile): Promise<void>;\n    /**\n     * Query tiles whose extent intersects the given bbox.\n     * SYNC — uses cached metadata for per-frame render loop compatibility.\n     */\n    queryTilesForExtent(extent: Extent): DTEDTileMetadata[];\n    /**\n     * Async spatial query via DuckDB SQL.\n     * Use this for non-render-loop queries (e.g., UI, analysis).\n     */\n    queryTilesForExtentAsync(extent: Extent): Promise<DTEDTileMetadata[]>;\n    /**\n     * Load full tile data (elevations) for a given tile ID.\n     * SYNC — reads from in-memory Map.\n     */\n    loadTileData(tileId: string): DTEDTile | null;\n    /**\n     * Get elevation at a specific geographic point.\n     */\n    getElevationAtPoint(lon: number, lat: number): number | null;\n    /**\n     * List all stored tiles.\n     */\n    listTiles(): DTEDTileMetadata[];\n    /**\n     * List tiles via DuckDB SQL (async, supports ORDER BY, LIMIT etc.).\n     */\n    listTilesAsync(options?: {\n        orderBy?: string;\n        limit?: number;\n    }): Promise<DTEDTileMetadata[]>;\n    /**\n     * Delete a tile by ID.\n     */\n    deleteTile(tileId: string): boolean;\n    /**\n     * Get storage statistics.\n     */\n    getStorageStats(): StorageStats;\n    /**\n     * Get storage statistics via DuckDB aggregation (async).\n     */\n    getStorageStatsAsync(): Promise<StorageStats & {\n        avgElevRange: number;\n    }>;\n    /**\n     * Compute the union extent of all stored tiles.\n     */\n    getFullExtent(): Extent | null;\n    /**\n     * Get full extent via DuckDB aggregation (async).\n     */\n    getFullExtentAsync(): Promise<Extent | null>;\n    /**\n     * Get global min/max elevation across all tiles.\n     */\n    getElevationRange(): [number, number];\n    /**\n     * Custom SQL query on the metadata table.\n     * Returns raw Arrow table for advanced analysis.\n     */\n    query(sql: string): Promise<unknown>;\n    /**\n     * Destroy store and free all resources.\n     */\n    destroy(): void;\n    private _ensureMetadataCache;\n    /** Convert DuckDB Arrow result to DTEDTileMetadata array */\n    private _arrowToMetadata;\n}\n//# sourceMappingURL=DuckDBDTEDStore.d.ts.map","RasterDEMLayer.d.ts":"/**\n * RasterDEMLayer — Terrain Layer for raster-dem tile servers\n *\n * Extends LayerBase and implements ITerrainLayer for integration with\n * Mode3D's terrain rendering pipeline. Fetches DEM tiles from standard\n * raster-dem servers (Mapbox Terrain-RGB, Terrarium), decodes via DEMData,\n * and provides padded (dim+2) elevation textures for seamless sampling.\n *\n * Flow:\n *   RasterDEMSource.loadTile(z,x,y) → DEMData (padded, backfilled)\n *   → DEMData.data (Float32Array stride×stride) → GPU r32float texture\n *   → shader samples with +1 padding offset for seamless borders\n */\nimport { LayerBase } from '@mapgpu/layers';\nimport type { Extent, ITerrainLayer, TextureFactory, GpuTerrainTileData } from '@mapgpu/core';\nimport { RasterDEMSource } from './RasterDEMSource';\nimport { DEMData } from './DEMData';\nimport type { DEMEncoding } from './DEMData';\nimport type { ColorRampStop, HillshadeFunction } from './types';\nexport interface RasterDEMLayerOptions {\n    /** Layer ID */\n    id?: string;\n    /** Tile URL template or TileJSON URL */\n    url: string;\n    /** DEM encoding format. Default: 'mapbox' */\n    encoding?: DEMEncoding;\n    /** Tile size. Default: 256 */\n    tileSize?: number;\n    /** Minimum zoom. Default: 0 */\n    minZoom?: number;\n    /** Maximum zoom. Default: 15 */\n    maxZoom?: number;\n    /** Vertical exaggeration. Default: 50 */\n    exaggeration?: number;\n    /** Hillshade strength 0-1. Default: 0.5 */\n    hillshadeStrength?: number;\n    /** Color ramp stops. Default: HYPSOMETRIC_RAMP */\n    colorRamp?: ColorRampStop[];\n    /** Visibility */\n    visible?: boolean;\n    /** Opacity */\n    opacity?: number;\n    /** WASM hillshade function (optional, faster) */\n    wasmHillshade?: HillshadeFunction;\n}\nexport declare class RasterDEMLayer extends LayerBase implements ITerrainLayer {\n    readonly type: \"raster-dem\";\n    private _source;\n    private _gpuTileCache;\n    private _colorRampStops;\n    private _colorRampLUT;\n    private _lastMinElev;\n    private _lastMaxElev;\n    private _hillshadeStrength;\n    private _hillshadeFn;\n    private _exaggeration;\n    constructor(options: RasterDEMLayerOptions);\n    get source(): RasterDEMSource;\n    get exaggeration(): number;\n    set exaggeration(v: number);\n    get hillshadeStrength(): number;\n    set hillshadeStrength(v: number);\n    protected onLoad(): Promise<void>;\n    destroy(): void;\n    /**\n     * Get visible tile IDs for the given viewport extent.\n     * Extent is in EPSG:4326 (lon/lat degrees).\n     * Returns tile keys in \"z/x/y\" format.\n     */\n    getVisibleTiles(extent: Extent): string[];\n    /**\n     * Create GPU-ready tile data from a loaded DEM tile.\n     * Uses the padded DEMData (dim+2) for elevation texture.\n     */\n    createGpuTile(tileId: string, textureManager: TextureFactory): GpuTerrainTileData | null;\n    /**\n     * Get color ramp LUT data.\n     */\n    getColorRampLUTData(): Uint8Array;\n    /**\n     * Get the DEMData for a specific tile.\n     * If the exact tile isn't available, tries parent tiles (overzooming)\n     * and extracts the sub-tile region via bilinear resampling.\n     */\n    getDEMForTile(z: number, x: number, y: number): DEMData | null;\n    /**\n     * Extract a sub-tile region from a parent DEM tile via bilinear resampling.\n     * Used for overzooming: when DEM server maxzoom < raster tile zoom.\n     */\n    private _extractSubTile;\n    /**\n     * Check whether DEM data covers any part of the given extent.\n     */\n    hasDataForExtent(extent: Extent): boolean;\n    /**\n     * Sample elevation grid from loaded DEM tiles.\n     * This is used for the elevation-only terrain visualization (no raster imagery).\n     */\n    getElevationGrid(extent: Extent, width: number, height: number): Float32Array;\n    private _toGpuTerrainTileData;\n}\n//# sourceMappingURL=RasterDEMLayer.d.ts.map","RasterDEMSource.d.ts":"/**\n * RasterDEMSource — Terrain Tile Fetch + Decode + Neighbor Backfill\n *\n * Fetches DEM tiles from raster-dem servers (Mapbox Terrain-RGB, Terrarium, etc.),\n * decodes RGBA PNG to Float32 elevation via DEMData, and orchestrates\n * backfillBorder() across neighboring tiles for seamless sampling.\n *\n * Tile URL template uses {z}/{x}/{y} placeholders.\n */\nimport { DEMData } from './DEMData';\nimport type { DEMEncoding } from './DEMData';\nexport interface RasterDEMSourceOptions {\n    /** Tile URL template with {z}, {x}, {y} placeholders, OR a TileJSON URL */\n    url: string;\n    /** Encoding format. Default: 'mapbox' */\n    encoding?: DEMEncoding;\n    /** Tile size in pixels. Default: 256 */\n    tileSize?: number;\n    /** Minimum zoom level. Default: 0 */\n    minZoom?: number;\n    /** Maximum zoom level. Default: 15 */\n    maxZoom?: number;\n}\nexport declare class RasterDEMSource {\n    private _tiles;\n    private _pending;\n    private _tileUrlTemplate;\n    private _encoding;\n    private _tileSize;\n    private _minZoom;\n    private _maxZoom;\n    private _initialized;\n    private static readonly MAX_CACHED_TILES;\n    constructor(options: RasterDEMSourceOptions);\n    get encoding(): DEMEncoding;\n    get tileSize(): number;\n    get minZoom(): number;\n    get maxZoom(): number;\n    /**\n     * Initialize the source. If the URL is a TileJSON endpoint,\n     * fetch it and extract the tile URL template.\n     */\n    init(): Promise<void>;\n    /**\n     * Build the tile URL from z/x/y coordinates.\n     */\n    private _buildUrl;\n    /**\n     * Get a cached tile, or null if not loaded.\n     */\n    getTile(z: number, x: number, y: number): DEMData | null;\n    /**\n     * Check if a tile is loaded.\n     */\n    hasTile(z: number, x: number, y: number): boolean;\n    /**\n     * Load a DEM tile: fetch PNG → decode RGBA → DEMData → backfill neighbors.\n     * Returns the cached DEMData if already loaded.\n     * Uses a pending-request dedup to avoid double-fetching.\n     */\n    loadTile(z: number, x: number, y: number): Promise<DEMData | null>;\n    /**\n     * Ensure a tile is loaded (fire-and-forget, non-blocking).\n     * Clamps zoom to the source's min/maxZoom range to avoid 404s\n     * and compute the correct parent tile coordinates for overzooming.\n     */\n    ensureTileLoaded(z: number, x: number, y: number): void;\n    /**\n     * Remove a tile from the cache.\n     */\n    removeTile(z: number, x: number, y: number): void;\n    /**\n     * Clear all cached tiles.\n     */\n    clear(): void;\n    /**\n     * Number of cached tiles.\n     */\n    get tileCount(): number;\n    private _fetchAndDecode;\n    /**\n     * After a tile is loaded, backfill borders with all available neighbors,\n     * AND update those neighbors' borders with this tile's data.\n     */\n    private _backfillNeighbors;\n}\n//# sourceMappingURL=RasterDEMSource.d.ts.map","TerrainRGBLayer.d.ts":"import type { ITerrainLayer, TerrainHeightTileData, TerrainHillshadeTileData, TerrainLighting3DOptions } from '@mapgpu/core';\nimport { LayerBase } from '@mapgpu/layers';\nimport type { Hillshade2DOptions, TerrainRGBLayerOptions } from './types';\nexport declare class TerrainRGBLayer extends LayerBase implements ITerrainLayer {\n    readonly type: \"terrain\";\n    readonly exaggeration: number;\n    get minZoom(): number;\n    get maxZoom(): number;\n    private _minZoom;\n    private _maxZoom;\n    private readonly _minZoomLocked;\n    private readonly _maxZoomLocked;\n    private readonly _encodingLocked;\n    private readonly _tileJsonUrl;\n    private readonly _fetchInit;\n    private readonly _maxReadyTiles;\n    private _hillshade2D;\n    private _lighting3D;\n    private readonly _pixelFetcher;\n    private _tileUrls;\n    private _encoding;\n    private _bounds;\n    private readonly _readyHeight;\n    private readonly _readyHillshade;\n    private readonly _requestInFlight;\n    private _hillshadeFn;\n    private _wasmInitPromise;\n    constructor(options?: TerrainRGBLayerOptions);\n    protected onLoad(): Promise<void>;\n    refresh(): void;\n    destroy(): void;\n    requestTile(z: number, x: number, y: number): Promise<void>;\n    get hillshade2D(): Readonly<Hillshade2DOptions>;\n    get lighting3D(): Readonly<TerrainLighting3DOptions>;\n    setHillshade2D(options: Partial<Hillshade2DOptions>): void;\n    setLighting3D(options: Partial<TerrainLighting3DOptions>): void;\n    getReadyHeightTile(z: number, x: number, y: number): TerrainHeightTileData | null;\n    getReadyHillshadeTile(z: number, x: number, y: number): TerrainHillshadeTileData | null;\n    private _buildReadyTile;\n    private _decodeHeightPixels;\n    private _fetchTileJson;\n    private _applyTileJson;\n    private _validateTileTemplates;\n    private _intersectsLayerBounds;\n    private _evictReadyTiles;\n    private _tryInitWasmHillshade;\n}\n//# sourceMappingURL=TerrainRGBLayer.d.ts.map","color-ramp.d.ts":"/**\n * Color Ramp — Hypsometric Tint\n *\n * Topographic elevation coloring: green (lowland) → brown (highland) → white (peaks).\n * Generates a 256×1 RGBA lookup texture for GPU-side color mapping.\n */\nimport type { ColorRampStop } from './types';\n/**\n * Classic hypsometric tint — topographic map standard.\n * Elevation values in meters.\n */\nexport declare const HYPSOMETRIC_RAMP: ColorRampStop[];\n/**\n * Generate a 256×1 RGBA8 color ramp LUT texture data.\n *\n * Index 0 = minElevation, index 255 = maxElevation.\n * Used in the terrain shader to map normalized elevation → color.\n */\nexport declare function generateColorRampLUT(stops: ColorRampStop[], minElevation: number, maxElevation: number): Uint8Array;\n/**\n * Generate a NODATA-aware color ramp.\n * Index 0 is reserved for NODATA (transparent).\n * Indices 1-255 map to minElevation..maxElevation.\n */\nexport declare function generateColorRampLUTWithNodata(stops: ColorRampStop[], minElevation: number, maxElevation: number): Uint8Array;\n//# sourceMappingURL=color-ramp.d.ts.map","hillshade.d.ts":"import type { HillshadeComputeFn } from './types';\n/**\n * CPU hillshade fallback (Horn's method).\n */\nexport declare const computeHillshadeTS: HillshadeComputeFn;\nexport declare function estimateCellSizeMeters(bounds: {\n    west: number;\n    east: number;\n    north: number;\n    south: number;\n}, size: number): {\n    cellSizeX: number;\n    cellSizeY: number;\n};\n//# sourceMappingURL=hillshade.d.ts.map","index.d.ts":"export { DTEDLayer } from './DTEDLayer';\nexport { TerrainRGBLayer } from './TerrainRGBLayer';\nexport type { DTEDLayerOptions, DTEDLevelName, DTEDMode, DTEDLocalFile, DTEDTile, DTEDHeader, Hillshade2DOptions, HillshadeComputeFn, TerrainRGBLayerOptions, TerrainRGBTileJSON, TerrainRGBEncoding, TerrainRGBPixelFetcher, TerrainRGBImageData, } from './types';\nexport { parseDTED, detectDTEDLevel, extractCoordsFromFilename } from './parsers/dted-parser';\nexport { DTEDTileStore } from './DTEDTileStore';\nexport { computeHillshadeTS, estimateCellSizeMeters } from './hillshade';\n//# sourceMappingURL=index.d.ts.map","parsers/dted-parser.d.ts":"/**\n * DTED binary parser (MIL-PRF-89020B).\n *\n * Parses DT0/DT1/DT2 files into row-major, north-up grids.\n */\nimport type { DTEDHeader, DTEDLevelName, DTEDTile } from '../types';\nexport declare function detectDTEDLevel(fileName: string, header: DTEDHeader): DTEDLevelName;\n/**\n * Extract cell origin from filename patterns:\n * - \"e032/n39.dt2\"   (directory structure)\n * - \"E032/N39.dt2\"\n * - \"n39e032.dt2\"    (lat-first flat)\n * - \"e032n39.dt2\"    (lon-first flat)\n */\nexport declare function extractCoordsFromFilename(fileName: string): [number, number] | null;\nexport interface ParseDTEDOptions {\n    fileName?: string;\n}\nexport declare function parseDTED(buffer: ArrayBuffer, options?: ParseDTEDOptions): DTEDTile;\n//# sourceMappingURL=dted-parser.d.ts.map","types.d.ts":"import type { LayerBaseOptions } from '@mapgpu/layers';\nimport type { TerrainLighting3DOptions } from '@mapgpu/core';\nexport declare const DTED_NODATA = -32767;\nexport type DTEDLevelName = 'dt0' | 'dt1' | 'dt2';\nexport type DTEDMode = 'local' | 'remote' | 'hybrid';\nexport interface DTEDHeader {\n    originLon: number;\n    originLat: number;\n    lonInterval: number;\n    latInterval: number;\n    numLonLines: number;\n    numLatPoints: number;\n}\nexport interface DTEDTile {\n    id: string;\n    level: DTEDLevelName;\n    origin: [number, number];\n    width: number;\n    height: number;\n    elevations: Int16Array;\n    minElevation: number;\n    maxElevation: number;\n    extent: {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    };\n}\nexport type DTEDLocalFile = File | {\n    name: string;\n    buffer: ArrayBuffer;\n};\nexport interface Hillshade2DOptions {\n    enabled: boolean;\n    opacity: number;\n    azimuth: number;\n    altitude: number;\n    /**\n     * 0..1 range.\n     * 0 = crisp/strong relief, 1 = softer relief (flat areas become more transparent).\n     */\n    softness?: number;\n}\nexport type HillshadeComputeFn = (elevations: Int16Array, width: number, height: number, cellSizeX: number, cellSizeY: number, azimuth: number, altitude: number) => Uint8Array;\nexport interface DTEDLayerOptions extends LayerBaseOptions {\n    mode?: DTEDMode;\n    localFiles?: DTEDLocalFile[];\n    urlForCell?: (args: {\n        lat: number;\n        lon: number;\n        level: DTEDLevelName;\n    }) => string | null | undefined;\n    levels?: DTEDLevelName[];\n    hillshade2D?: Partial<Hillshade2DOptions>;\n    lighting3D?: Partial<TerrainLighting3DOptions>;\n    exaggeration?: number;\n    minZoom?: number;\n    maxZoom?: number;\n    tileSize?: number;\n    maxReadyTiles?: number;\n    wasmHillshade?: HillshadeComputeFn;\n}\nexport interface TileLonLatBounds {\n    west: number;\n    east: number;\n    north: number;\n    south: number;\n}\nexport type TerrainRGBEncoding = 'terrain-rgb' | 'terrarium';\nexport interface TerrainRGBTileJSON {\n    tiles?: string[];\n    minzoom?: number;\n    maxzoom?: number;\n    bounds?: [number, number, number, number];\n    encoding?: TerrainRGBEncoding;\n    attribution?: string;\n}\nexport interface TerrainRGBImageData {\n    width: number;\n    height: number;\n    /** RGBA8 pixels (row-major). */\n    data: Uint8Array | Uint8ClampedArray;\n}\nexport type TerrainRGBPixelFetcher = (url: string) => Promise<TerrainRGBImageData>;\nexport interface TerrainRGBLayerOptions extends LayerBaseOptions {\n    /** TileJSON endpoint URL (e.g. .../tiles.json). */\n    tileJsonUrl?: string;\n    /** Inline TileJSON object (takes precedence over tileJsonUrl). */\n    tileJson?: TerrainRGBTileJSON;\n    /** Direct tile URL templates (e.g. .../{z}/{x}/{y}.png). */\n    tileUrls?: string[];\n    /** Height encoding. Defaults to 'terrain-rgb'. */\n    encoding?: TerrainRGBEncoding;\n    hillshade2D?: Partial<Hillshade2DOptions>;\n    lighting3D?: Partial<TerrainLighting3DOptions>;\n    exaggeration?: number;\n    minZoom?: number;\n    maxZoom?: number;\n    maxReadyTiles?: number;\n    wasmHillshade?: HillshadeComputeFn;\n    /** Optional injectable RGBA fetch/decode hook for tests/custom pipelines. */\n    pixelFetcher?: TerrainRGBPixelFetcher;\n    /** Optional fetch init applied to TileJSON/tile requests. */\n    fetchInit?: RequestInit;\n}\n//# sourceMappingURL=types.d.ts.map"},"@mapgpu/adapters-ogc":{"czml/CzmlParser.d.ts":"/**\n * CZML Parser — Converts Cesium Language (CZML) JSON to Features with temporal metadata.\n *\n * CZML is a JSON schema for describing time-dynamic graphical scenes.\n * This parser extracts static and time-sampled positions, styles, and availability.\n *\n * Reference: https://github.com/AnalyticalGraphicsInc/czml-writer/wiki/CZML-Guide\n */\nexport interface CzmlFeature {\n    id: string;\n    geometry: CzmlGeometry;\n    attributes: Record<string, unknown>;\n    /** Time availability as ISO 8601 interval (e.g., \"2024-01-01T00:00:00Z/2024-12-31T23:59:59Z\"). */\n    availability?: string;\n    /** Sampled positions: array of { epoch, positions } for temporal interpolation. */\n    sampledPositions?: CzmlSampledPosition[];\n}\nexport interface CzmlGeometry {\n    type: 'Point' | 'LineString' | 'Polygon';\n    coordinates: number[] | number[][] | number[][][];\n}\nexport interface CzmlSampledPosition {\n    epoch: string;\n    /** Flat array: [t0, lon0, lat0, alt0, t1, lon1, lat1, alt1, ...] */\n    cartographicDegrees: number[];\n}\nexport interface CzmlParseResult {\n    id?: string;\n    name?: string;\n    description?: string;\n    clock?: CzmlClock;\n    features: CzmlFeature[];\n}\nexport interface CzmlClock {\n    interval: string;\n    currentTime: string;\n    multiplier: number;\n    range: 'UNBOUNDED' | 'CLAMPED' | 'LOOP_STOP';\n    step: 'SYSTEM_CLOCK' | 'SYSTEM_CLOCK_MULTIPLIER' | 'TICK_DEPENDENT';\n}\n/**\n * Parse a CZML JSON array into features with temporal metadata.\n */\nexport declare function parseCzml(czml: unknown[]): CzmlParseResult;\n//# sourceMappingURL=CzmlParser.d.ts.map","discovery/service-discovery.d.ts":"/**\n * Service Discovery\n *\n * Implements IServiceDiscovery — auto-detects OGC service type from a URL.\n *\n * Strategy:\n * 1. URL pattern analysis (service=WMS, /wfs?, /collections, /{z}/{x}/{y})\n * 2. HEAD request → Content-Type check\n * 3. GetCapabilities probe (WMS/WFS)\n * 4. Landing page probe (OGC API)\n * 5. Fallback: { type: 'unknown' }\n */\nimport type { IServiceDiscovery, ServiceType } from '../types';\nexport interface ServiceDiscoveryOptions {\n    /** Request timeout in milliseconds. Defaults to 10000 (10s). */\n    timeout?: number;\n    /** Optional proxy URL prefix */\n    proxyUrl?: string;\n    /** Custom fetch function for dependency injection (testing) */\n    fetchFn?: typeof fetch;\n}\nexport declare class ServiceDiscovery implements IServiceDiscovery {\n    private readonly timeout;\n    private readonly proxyUrl?;\n    private readonly fetchFn;\n    constructor(options?: ServiceDiscoveryOptions);\n    /**\n     * Build a fetch-ready URL, optionally routing through a proxy.\n     */\n    private buildFetchUrl;\n    /**\n     * Fetch text content with timeout. Returns null on failure.\n     */\n    private tryFetchText;\n    /**\n     * Fetch JSON content with timeout. Returns null on failure.\n     */\n    private tryFetchJson;\n    /**\n     * Auto-detect service type from a URL.\n     */\n    discover(url: string): Promise<ServiceType>;\n    /**\n     * Probe for OGC API (Features or Maps) by fetching landing page.\n     */\n    private probeOgcApi;\n    /**\n     * Determine OGC API sub-type based on links.\n     */\n    private detectOgcApiType;\n    /**\n     * Probe for WMS service by sending GetCapabilities request.\n     */\n    private probeWms;\n    /**\n     * Probe for WFS service by sending GetCapabilities request.\n     */\n    private probeWfs;\n}\n/**\n * Detect service type from URL patterns alone (no network requests).\n */\nexport declare function detectFromUrlPattern(url: string): ServiceType | null;\n//# sourceMappingURL=service-discovery.d.ts.map","gpx/GpxParser.d.ts":"/**\n * GPX Parser — Converts GPS Exchange Format to GeoJSON Features.\n *\n * Supports: waypoints (wpt), tracks (trk/trkseg/trkpt), routes (rte/rtept).\n * Extracts elevation, timestamps, name, description, and custom extensions.\n */\nexport interface GpxFeature {\n    id: string | number;\n    geometry: GpxGeometry;\n    attributes: Record<string, unknown>;\n}\nexport interface GpxGeometry {\n    type: 'Point' | 'LineString' | 'MultiLineString';\n    coordinates: number[] | number[][] | number[][][];\n}\nexport interface GpxParseResult {\n    waypoints: GpxFeature[];\n    tracks: GpxFeature[];\n    routes: GpxFeature[];\n    metadata?: {\n        name?: string;\n        description?: string;\n        author?: string;\n        time?: string;\n    };\n}\n/**\n * Parse a GPX XML string into waypoints, tracks, and routes.\n */\nexport declare function parseGpx(gpxString: string): GpxParseResult;\n/** Convenience: merge all GPX features into a single array. */\nexport declare function gpxToFeatures(result: GpxParseResult): GpxFeature[];\n//# sourceMappingURL=GpxParser.d.ts.map","imagery/ArcGISProvider.d.ts":"/**\n * ArcGISProvider — ArcGIS Online / Server tile imagery.\n */\nimport type { IImageryProvider, ImageryProviderMetadata } from './ImageryProviderTypes';\nexport interface ArcGISProviderOptions {\n    /** Base URL of the ArcGIS map service. */\n    url?: string;\n    /** Optional API token for secured services. */\n    token?: string;\n}\nexport declare class ArcGISProvider implements IImageryProvider {\n    readonly type = \"arcgis\";\n    private _url;\n    private _token;\n    constructor(options?: ArcGISProviderOptions);\n    getMetadata(): Promise<ImageryProviderMetadata>;\n    getTileUrl(z: number, x: number, y: number): string;\n}\n//# sourceMappingURL=ArcGISProvider.d.ts.map","imagery/BingMapsProvider.d.ts":"/**\n * BingMapsProvider — Bing Maps tile imagery.\n *\n * Uses the Bing Maps REST API for metadata and quadkey tile addressing.\n */\nimport type { IImageryProvider, ImageryProviderMetadata } from './ImageryProviderTypes';\nexport interface BingMapsProviderOptions {\n    /** Bing Maps API key. */\n    key: string;\n    /** Imagery set: 'Aerial' | 'AerialWithLabels' | 'Road' | 'CanvasDark' | 'CanvasLight'. Default: 'Aerial'. */\n    imagerySet?: string;\n    /** Culture code (e.g., 'en-US', 'tr-TR'). Default: 'en-US'. */\n    culture?: string;\n}\nexport declare class BingMapsProvider implements IImageryProvider {\n    readonly type = \"bing-maps\";\n    private _key;\n    private _imagerySet;\n    private _culture;\n    private _templateUrl;\n    private _subdomains;\n    constructor(options: BingMapsProviderOptions);\n    getMetadata(): Promise<ImageryProviderMetadata>;\n    getTileUrl(z: number, x: number, y: number): string;\n}\n//# sourceMappingURL=BingMapsProvider.d.ts.map","imagery/ImageryProviderTypes.d.ts":"/**\n * Imagery Provider Types — Shared interfaces for tile imagery sources.\n *\n * Each provider implements IImageryProvider, which extends IMapImageryAdapter\n * with auto-discovery of metadata (attribution, zoom bounds, tile scheme).\n */\nexport interface ImageryProviderMetadata {\n    /** Human-readable name. */\n    name: string;\n    /** Attribution text (HTML allowed). */\n    attribution: string;\n    /** Minimum zoom level available. */\n    minZoom: number;\n    /** Maximum zoom level available. */\n    maxZoom: number;\n    /** Tile pixel size (usually 256 or 512). */\n    tileSize: number;\n}\nexport interface IImageryProvider {\n    /** Provider identifier. */\n    readonly type: string;\n    /** Fetch provider metadata (capabilities, attribution, zoom range). */\n    getMetadata(): Promise<ImageryProviderMetadata>;\n    /** Build a tile URL for the given coordinates. */\n    getTileUrl(z: number, x: number, y: number): string;\n}\n//# sourceMappingURL=ImageryProviderTypes.d.ts.map","imagery/MapboxProvider.d.ts":"/**\n * MapboxProvider — Mapbox raster tile imagery.\n */\nimport type { IImageryProvider, ImageryProviderMetadata } from './ImageryProviderTypes';\nexport interface MapboxProviderOptions {\n    /** Mapbox access token. */\n    accessToken: string;\n    /** Tileset ID (e.g., 'mapbox.satellite', 'mapbox.streets-v12'). */\n    tilesetId?: string;\n    /** Tile format: 'png' | 'jpg' | 'webp'. Default: 'webp'. */\n    format?: string;\n    /** High-DPI tiles (@2x). Default: true. */\n    highDpi?: boolean;\n}\nexport declare class MapboxProvider implements IImageryProvider {\n    readonly type = \"mapbox\";\n    private _token;\n    private _tilesetId;\n    private _format;\n    private _highDpi;\n    constructor(options: MapboxProviderOptions);\n    getMetadata(): Promise<ImageryProviderMetadata>;\n    getTileUrl(z: number, x: number, y: number): string;\n}\n//# sourceMappingURL=MapboxProvider.d.ts.map","imagery/WMTSProvider.d.ts":"/**\n * WMTSProvider — OGC Web Map Tile Service provider.\n *\n * Supports RESTful and KVP GetTile encoding.\n */\nimport type { IImageryProvider, ImageryProviderMetadata } from './ImageryProviderTypes';\nexport interface WMTSProviderOptions {\n    /** WMTS service base URL. */\n    url: string;\n    /** Layer identifier. */\n    layer: string;\n    /** Tile matrix set identifier. Default: 'WebMercatorQuad'. */\n    tileMatrixSet?: string;\n    /** Output format. Default: 'image/png'. */\n    format?: string;\n    /** Style identifier. Default: 'default'. */\n    style?: string;\n    /** Encoding: 'RESTful' | 'KVP'. Default: 'RESTful'. */\n    encoding?: 'RESTful' | 'KVP';\n    /** RESTful URL template (e.g., '/tile/{TileMatrix}/{TileRow}/{TileCol}'). */\n    resourceUrl?: string;\n}\nexport declare class WMTSProvider implements IImageryProvider {\n    readonly type = \"wmts\";\n    private _url;\n    private _layer;\n    private _tileMatrixSet;\n    private _format;\n    private _style;\n    private _encoding;\n    private _resourceUrl;\n    constructor(options: WMTSProviderOptions);\n    getMetadata(): Promise<ImageryProviderMetadata>;\n    getTileUrl(z: number, x: number, y: number): string;\n}\n//# sourceMappingURL=WMTSProvider.d.ts.map","imagery/index.d.ts":"export type { IImageryProvider, ImageryProviderMetadata } from './ImageryProviderTypes';\nexport { BingMapsProvider } from './BingMapsProvider';\nexport type { BingMapsProviderOptions } from './BingMapsProvider';\nexport { MapboxProvider } from './MapboxProvider';\nexport type { MapboxProviderOptions } from './MapboxProvider';\nexport { ArcGISProvider } from './ArcGISProvider';\nexport type { ArcGISProviderOptions } from './ArcGISProvider';\nexport { WMTSProvider } from './WMTSProvider';\nexport type { WMTSProviderOptions } from './WMTSProvider';\n//# sourceMappingURL=index.d.ts.map","index.d.ts":"/**\n * @mapgpu/adapters-ogc\n *\n * OGC service adapters — WMS, WFS, OGC API Features, OGC API Maps,\n * XYZ tile, Service Discovery, and shared types.\n */\nexport type { IMapImageryAdapter, MapImageryCapabilities, MapImageryLayerInfo, MapImageryRequest, FeatureInfoRequest, FeatureInfoResult, ServiceCapabilities, IFeatureAdapter, FeatureCollectionInfo, FeatureQueryParams, GeoJsonFeature, OgcError, ServiceType, IServiceDiscovery, } from './types';\nexport { parseWmsCapabilities } from './wms/capabilities-parser';\nexport { buildGetMapUrl, buildGetFeatureInfoUrl } from './wms/url-builder';\nexport { WmsAdapter } from './wms/wms-adapter';\nexport type { WmsAdapterOptions } from './wms/wms-adapter';\nexport type { WmsCapabilities, WmsLayerInfo, WmsStyle, WmsBoundingBox, WmsTimeDimension, GetMapParams, GetFeatureInfoParams, } from './wms/types';\nexport { parseWfsCapabilities } from './wfs/capabilities-parser';\nexport { buildGetFeatureUrl, buildDescribeFeatureTypeUrl } from './wfs/url-builder';\nexport { WfsAdapter, parseGmlFeatures } from './wfs/wfs-adapter';\nexport type { WfsAdapterOptions } from './wfs/wfs-adapter';\nexport type { WfsCapabilities, WfsFeatureType, WfsBoundingBox, WfsQueryParams, } from './wfs/types';\nexport { OgcApiFeaturesAdapter } from './ogc-api-features/ogc-api-features-adapter';\nexport type { OgcApiFeaturesAdapterOptions } from './ogc-api-features/ogc-api-features-adapter';\nexport type { OgcApiCollection, OgcApiLink, OgcApiCollectionsResponse, OgcApiItemsResponse, } from './ogc-api-features/types';\nexport { OgcApiMapsAdapter } from './ogc-api-maps/ogc-api-maps-adapter';\nexport type { OgcApiMapsAdapterOptions } from './ogc-api-maps/ogc-api-maps-adapter';\nexport { XyzAdapter } from './xyz/xyz-adapter';\nexport type { XyzAdapterOptions } from './xyz/xyz-adapter';\nexport { ServiceDiscovery, detectFromUrlPattern } from './discovery/service-discovery';\nexport type { ServiceDiscoveryOptions } from './discovery/service-discovery';\nexport { parseKml } from './kml/KmlParser';\nexport type { KmlFeature, KmlGeometry, KmlStyle, KmlParseResult } from './kml/KmlParser';\nexport { parseGpx, gpxToFeatures } from './gpx/GpxParser';\nexport type { GpxFeature, GpxGeometry, GpxParseResult } from './gpx/GpxParser';\nexport { parseCzml } from './czml/CzmlParser';\nexport type { CzmlFeature, CzmlGeometry, CzmlSampledPosition, CzmlParseResult, CzmlClock } from './czml/CzmlParser';\nexport { BingMapsProvider } from './imagery/BingMapsProvider';\nexport type { BingMapsProviderOptions } from './imagery/BingMapsProvider';\nexport { MapboxProvider } from './imagery/MapboxProvider';\nexport type { MapboxProviderOptions } from './imagery/MapboxProvider';\nexport { ArcGISProvider } from './imagery/ArcGISProvider';\nexport type { ArcGISProviderOptions } from './imagery/ArcGISProvider';\nexport { WMTSProvider } from './imagery/WMTSProvider';\nexport type { WMTSProviderOptions } from './imagery/WMTSProvider';\nexport type { IImageryProvider, ImageryProviderMetadata } from './imagery/ImageryProviderTypes';\n//# sourceMappingURL=index.d.ts.map","kml/KmlParser.d.ts":"/**\n * KML/KMZ Parser — Converts KML geographic data to GeoJSON Features.\n *\n * Supports: Placemark, Point, LineString, LinearRing, Polygon,\n * MultiGeometry, Style, StyleMap, ExtendedData, coordinates with altitude.\n *\n * Based on OGC KML 2.2 specification.\n */\nexport interface KmlFeature {\n    id: string | number;\n    geometry: KmlGeometry;\n    attributes: Record<string, unknown>;\n}\nexport interface KmlGeometry {\n    type: 'Point' | 'LineString' | 'Polygon' | 'MultiPoint' | 'MultiLineString' | 'MultiPolygon';\n    coordinates: number[] | number[][] | number[][][] | number[][][][];\n}\nexport interface KmlStyle {\n    lineColor?: [number, number, number, number];\n    lineWidth?: number;\n    fillColor?: [number, number, number, number];\n    iconUrl?: string;\n    iconScale?: number;\n    labelColor?: [number, number, number, number];\n}\nexport interface KmlParseResult {\n    features: KmlFeature[];\n    styles: Map<string, KmlStyle>;\n    name?: string;\n    description?: string;\n}\n/**\n * Parse a KML XML string into features and styles.\n */\nexport declare function parseKml(kmlString: string): KmlParseResult;\n//# sourceMappingURL=KmlParser.d.ts.map","ogc-api-features/ogc-api-features-adapter.d.ts":"/**\n * OGC API Features Adapter\n *\n * Implements the IFeatureAdapter interface for OGC API - Features services.\n * Supports collections listing, items pagination via Link header or\n * response links[rel=next], bbox/datetime/limit/offset/filter query params.\n */\nimport type { IFeatureAdapter, FeatureCollectionInfo, FeatureQueryParams, GeoJsonFeature } from '../types';\nexport interface OgcApiFeaturesAdapterOptions {\n    /** OGC API base URL (landing page URL) */\n    url: string;\n    /** Optional proxy URL prefix */\n    proxyUrl?: string;\n    /** Request timeout in milliseconds. Defaults to 30000 (30s). */\n    timeout?: number;\n    /** Page size for pagination. Defaults to 1000. */\n    pageSize?: number;\n    /** Custom fetch function for dependency injection (testing) */\n    fetchFn?: typeof fetch;\n}\nexport declare class OgcApiFeaturesAdapter implements IFeatureAdapter {\n    private readonly url;\n    private readonly proxyUrl?;\n    private readonly timeout;\n    private readonly pageSize;\n    private readonly fetchFn;\n    constructor(options: OgcApiFeaturesAdapterOptions);\n    /**\n     * Build a fetch-ready URL, optionally routing through a proxy.\n     */\n    private buildFetchUrl;\n    /**\n     * Fetch with timeout and error handling.\n     */\n    private fetchWithTimeout;\n    /**\n     * Get available collections from the /collections endpoint.\n     */\n    getCollections(): Promise<FeatureCollectionInfo[]>;\n    /**\n     * Fetch features from a collection with automatic pagination.\n     * Yields batches of GeoJsonFeature arrays.\n     *\n     * Pagination strategy:\n     * 1. Follow links[rel=next] in the response body\n     * 2. Fall back to Link header parsing\n     * 3. Fall back to offset-based pagination\n     */\n    getFeatures(collectionId: string, params?: FeatureQueryParams): AsyncGenerator<GeoJsonFeature[], void, unknown>;\n    /**\n     * Build the initial items URL with query parameters.\n     */\n    private buildItemsUrl;\n    /**\n     * Find the next page URL from response body links or Link header.\n     */\n    private findNextUrl;\n}\n//# sourceMappingURL=ogc-api-features-adapter.d.ts.map","ogc-api-features/types.d.ts":"/**\n * OGC API Features types.\n */\nexport interface OgcApiLink {\n    href: string;\n    rel: string;\n    type?: string;\n    title?: string;\n}\nexport interface OgcApiCollection {\n    id: string;\n    title: string;\n    description?: string;\n    links: OgcApiLink[];\n    extent?: {\n        spatial?: {\n            bbox?: number[][];\n            crs?: string;\n        };\n        temporal?: {\n            interval?: (string | null)[][];\n            trs?: string;\n        };\n    };\n    crs?: string[];\n    itemType?: string;\n}\nexport interface OgcApiCollectionsResponse {\n    collections: OgcApiCollection[];\n    links: OgcApiLink[];\n}\nexport interface OgcApiItemsResponse {\n    type: 'FeatureCollection';\n    features: OgcApiFeature[];\n    links?: OgcApiLink[];\n    numberMatched?: number;\n    numberReturned?: number;\n    timeStamp?: string;\n}\nexport interface OgcApiFeature {\n    type: 'Feature';\n    id?: string | number;\n    geometry: {\n        type: string;\n        coordinates: unknown;\n    };\n    properties: Record<string, unknown>;\n}\n//# sourceMappingURL=types.d.ts.map","ogc-api-maps/ogc-api-maps-adapter.d.ts":"/**\n * OGC API Maps Adapter\n *\n * Implements the IMapImageryAdapter interface for OGC API - Maps services.\n * Provides RESTful access to rendered map images from\n * /collections/{id}/map endpoints.\n */\nimport type { IMapImageryAdapter, MapImageryCapabilities, MapImageryRequest } from '../types';\nexport interface OgcApiMapsAdapterOptions {\n    /** OGC API base URL (landing page URL) */\n    url: string;\n    /** Optional proxy URL prefix */\n    proxyUrl?: string;\n    /** Request timeout in milliseconds. Defaults to 30000 (30s). */\n    timeout?: number;\n    /** Custom fetch function for dependency injection (testing) */\n    fetchFn?: typeof fetch;\n}\nexport declare class OgcApiMapsAdapter implements IMapImageryAdapter {\n    private readonly url;\n    private readonly proxyUrl?;\n    private readonly timeout;\n    private readonly fetchFn;\n    constructor(options: OgcApiMapsAdapterOptions);\n    /**\n     * Build a fetch-ready URL, optionally routing through a proxy.\n     */\n    private buildFetchUrl;\n    /**\n     * Fetch with timeout and error handling.\n     */\n    private fetchWithTimeout;\n    /**\n     * Get capabilities by fetching /collections metadata.\n     */\n    getCapabilities(): Promise<MapImageryCapabilities>;\n    /**\n     * Build a map image URL for the given parameters.\n     *\n     * URL format: /collections/{collectionId}/map?bbox=...&width=...&height=...\n     */\n    getMapUrl(params: MapImageryRequest): string;\n}\n//# sourceMappingURL=ogc-api-maps-adapter.d.ts.map","types.d.ts":"/**\n * Core adapter interface types for adapters-ogc package.\n *\n * These types mirror the @mapgpu/core IOgcAdapter contract.\n * Defined locally to avoid cross-package build dependencies in Faz 0.\n */\nexport interface ServiceCapabilities {\n    type: 'WMS' | 'WFS' | 'OGC-API-Features' | 'OGC-API-Maps' | 'XYZ';\n    version: string;\n    title?: string;\n    abstract?: string;\n}\nexport interface MapImageryCapabilities extends ServiceCapabilities {\n    layers: MapImageryLayerInfo[];\n    formats: string[];\n}\nexport interface MapImageryLayerInfo {\n    name: string;\n    title: string;\n    abstract?: string;\n    crs: string[];\n    extent?: [number, number, number, number];\n    styles: {\n        name: string;\n        title?: string;\n        legendUrl?: string;\n    }[];\n    timeExtent?: string;\n    queryable: boolean;\n}\nexport interface MapImageryRequest {\n    layers: string[];\n    bbox: {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    };\n    width: number;\n    height: number;\n    crs?: string;\n    format?: string;\n    transparent?: boolean;\n    time?: string;\n    vendorParams?: Record<string, string>;\n}\nexport interface FeatureInfoRequest {\n    layers: string[];\n    bbox: {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    };\n    width: number;\n    height: number;\n    x: number;\n    y: number;\n    crs?: string;\n    featureCount?: number;\n}\nexport interface FeatureInfoResult {\n    features: Array<{\n        layerName: string;\n        attributes: Record<string, unknown>;\n    }>;\n}\nexport interface IMapImageryAdapter {\n    getCapabilities(): Promise<MapImageryCapabilities>;\n    getMapUrl(params: MapImageryRequest): string;\n    getFeatureInfo?(params: FeatureInfoRequest): Promise<FeatureInfoResult>;\n}\nexport interface FeatureCollectionInfo {\n    id: string;\n    title: string;\n    description?: string;\n    extent?: [number, number, number, number];\n    crs?: string[];\n}\nexport interface FeatureQueryParams {\n    bbox?: [number, number, number, number];\n    datetime?: string;\n    limit?: number;\n    offset?: number;\n    filter?: string;\n    properties?: string[];\n    sortBy?: string;\n}\nexport interface GeoJsonFeature {\n    type: 'Feature';\n    id?: string | number;\n    geometry: {\n        type: string;\n        coordinates: unknown;\n    };\n    properties: Record<string, unknown>;\n}\nexport interface IFeatureAdapter {\n    /** Collection listing */\n    getCollections(): Promise<FeatureCollectionInfo[]>;\n    /** Feature query — iterator (automatic pagination) */\n    getFeatures(collectionId: string, params?: FeatureQueryParams): AsyncGenerator<GeoJsonFeature[], void, unknown>;\n}\nexport interface OgcError {\n    code: string;\n    message: string;\n    source: 'WMS' | 'WFS' | 'OGC-API';\n    status?: number;\n}\nexport type ServiceType = {\n    type: 'WMS';\n    version: string;\n} | {\n    type: 'WFS';\n    version: string;\n} | {\n    type: 'OGC-API-Features';\n} | {\n    type: 'OGC-API-Maps';\n} | {\n    type: 'XYZ';\n} | {\n    type: 'unknown';\n};\nexport interface IServiceDiscovery {\n    /** Auto-detect service type from a URL */\n    discover(url: string): Promise<ServiceType>;\n}\n//# sourceMappingURL=types.d.ts.map","wfs/capabilities-parser.d.ts":"/**\n * WFS Capabilities XML Parser\n *\n * Supports WFS 2.0.0 GetCapabilities response parsing.\n * Uses DOMParser (browser API / happy-dom in tests).\n */\nimport type { WfsCapabilities } from './types';\n/**\n * Parse a WFS GetCapabilities XML string into a structured WfsCapabilities object.\n *\n * Handles WFS 2.0.0 format.\n */\nexport declare function parseWfsCapabilities(xml: string): WfsCapabilities;\n//# sourceMappingURL=capabilities-parser.d.ts.map","wfs/types.d.ts":"/**\n * WFS-specific types used by the capabilities parser, URL builder, and adapter.\n */\nexport interface WfsBoundingBox {\n    crs: string;\n    lowerCorner: [number, number];\n    upperCorner: [number, number];\n}\nexport interface WfsFeatureType {\n    name: string;\n    title: string;\n    abstract?: string;\n    defaultCrs: string;\n    otherCrs: string[];\n    boundingBox?: WfsBoundingBox;\n    outputFormats: string[];\n}\nexport interface WfsCapabilities {\n    version: string;\n    title: string;\n    abstract?: string;\n    featureTypes: WfsFeatureType[];\n    outputFormats: string[];\n    supportsStartIndex: boolean;\n    getFeatureUrl?: string;\n    describeFeatureTypeUrl?: string;\n}\nexport interface WfsQueryParams {\n    baseUrl: string;\n    version: string;\n    typeName: string;\n    outputFormat?: string;\n    srsName?: string;\n    count?: number;\n    startIndex?: number;\n    bbox?: [number, number, number, number];\n    bboxCrs?: string;\n    filter?: string;\n    propertyName?: string[];\n    sortBy?: string;\n}\n//# sourceMappingURL=types.d.ts.map","wfs/url-builder.d.ts":"/**\n * WFS URL Builder\n *\n * Builds GetFeature and DescribeFeatureType URLs for WFS 2.0.0.\n */\nimport type { WfsQueryParams } from './types';\n/**\n * Build a WFS GetFeature URL.\n */\nexport declare function buildGetFeatureUrl(params: WfsQueryParams): string;\n/**\n * Build a WFS DescribeFeatureType URL.\n */\nexport declare function buildDescribeFeatureTypeUrl(baseUrl: string, version: string, typeName: string, outputFormat?: string): string;\n//# sourceMappingURL=url-builder.d.ts.map","wfs/wfs-adapter.d.ts":"/**\n * WFS Adapter\n *\n * Implements the IFeatureAdapter interface for WFS 2.0.0 services.\n * Supports GetCapabilities, GetFeature with pagination (startIndex/count),\n * GML to GeoJSON conversion, and filter encoding (bbox + property filter).\n */\nimport type { IFeatureAdapter, FeatureCollectionInfo, FeatureQueryParams, GeoJsonFeature } from '../types';\nexport interface WfsAdapterOptions {\n    /** WFS service base URL */\n    url: string;\n    /** Preferred WFS version. Defaults to '2.0.0'. */\n    version?: string;\n    /** Optional proxy URL prefix */\n    proxyUrl?: string;\n    /** Request timeout in milliseconds. Defaults to 30000 (30s). */\n    timeout?: number;\n    /** Page size for pagination. Defaults to 1000. */\n    pageSize?: number;\n    /** Custom fetch function for dependency injection (testing) */\n    fetchFn?: typeof fetch;\n}\nexport declare class WfsAdapter implements IFeatureAdapter {\n    private readonly url;\n    private readonly version;\n    private readonly proxyUrl?;\n    private readonly timeout;\n    private readonly pageSize;\n    private readonly fetchFn;\n    private capabilities;\n    constructor(options: WfsAdapterOptions);\n    /**\n     * Build a fetch-ready URL, optionally routing through a proxy.\n     */\n    private buildFetchUrl;\n    /**\n     * Fetch with timeout and error handling.\n     */\n    private fetchWithTimeout;\n    /**\n     * Fetch and parse WFS GetCapabilities.\n     */\n    private loadCapabilities;\n    /**\n     * Get available feature type collections.\n     */\n    getCollections(): Promise<FeatureCollectionInfo[]>;\n    /**\n     * Fetch features from a WFS service with automatic pagination.\n     * Yields batches of GeoJsonFeature arrays.\n     */\n    getFeatures(collectionId: string, params?: FeatureQueryParams): AsyncGenerator<GeoJsonFeature[], void, unknown>;\n}\n/**\n * Parse GML features from a WFS GetFeature XML response.\n * Supports basic geometry types: Point, LineString, Polygon, MultiPoint,\n * MultiLineString, MultiPolygon.\n */\nexport declare function parseGmlFeatures(xml: string): GeoJsonFeature[];\n//# sourceMappingURL=wfs-adapter.d.ts.map","wms/adapter-types.d.ts":"/**\n * Re-export of core interface types used by the WMS adapter.\n *\n * These types mirror @mapgpu/core IOgcAdapter contract.\n * We re-export from a local definition to avoid circular dependencies\n * between packages during Faz 0.\n */\nexport type { IMapImageryAdapter, MapImageryCapabilities, MapImageryLayerInfo, MapImageryRequest, FeatureInfoRequest, FeatureInfoResult, } from '../types';\n//# sourceMappingURL=adapter-types.d.ts.map","wms/capabilities-parser.d.ts":"/**\n * WMS Capabilities XML Parser\n *\n * Supports WMS 1.1.1 and 1.3.0 GetCapabilities response parsing.\n * Uses DOMParser (browser API).\n *\n * Note: Avoids :scope pseudo-class in querySelector as it is not\n * supported by all DOM implementations (e.g., happy-dom).\n * Uses direct child element traversal instead.\n */\nimport type { WmsCapabilities } from './types';\n/**\n * Parse a WMS GetCapabilities XML string into a structured WmsCapabilities object.\n *\n * Handles both WMS 1.1.1 (WMT_MS_Capabilities root) and WMS 1.3.0 (WMS_Capabilities root).\n * Nested layers inherit CRS/SRS from their parent.\n */\nexport declare function parseWmsCapabilities(xml: string): WmsCapabilities;\n//# sourceMappingURL=capabilities-parser.d.ts.map","wms/types.d.ts":"/**\n * WMS-specific types used by the capabilities parser, URL builder, and adapter.\n */\nexport interface WmsBoundingBox {\n    crs: string;\n    minX: number;\n    minY: number;\n    maxX: number;\n    maxY: number;\n}\nexport interface WmsStyle {\n    name: string;\n    title?: string;\n    legendUrl?: string;\n}\nexport interface WmsTimeDimension {\n    name: 'time';\n    units: string;\n    default?: string;\n    values: string;\n}\nexport interface WmsLayerInfo {\n    name: string;\n    title: string;\n    abstract?: string;\n    crs: string[];\n    boundingBoxes: WmsBoundingBox[];\n    styles: WmsStyle[];\n    queryable: boolean;\n    timeDimension?: WmsTimeDimension;\n    layers?: WmsLayerInfo[];\n}\nexport interface WmsCapabilities {\n    version: string;\n    title: string;\n    abstract?: string;\n    formats: string[];\n    featureInfoFormats: string[];\n    getMapUrl?: string;\n    getFeatureInfoUrl?: string;\n    layers: WmsLayerInfo[];\n}\nexport interface GetMapParams {\n    baseUrl: string;\n    version: string;\n    layers: string[];\n    bbox: {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    };\n    width: number;\n    height: number;\n    crs: string;\n    format?: string;\n    transparent?: boolean;\n    styles?: string[];\n    time?: string;\n    vendorParams?: Record<string, string>;\n}\nexport interface GetFeatureInfoParams {\n    baseUrl: string;\n    version: string;\n    layers: string[];\n    bbox: {\n        minX: number;\n        minY: number;\n        maxX: number;\n        maxY: number;\n    };\n    width: number;\n    height: number;\n    x: number;\n    y: number;\n    crs: string;\n    format?: string;\n    featureCount?: number;\n    vendorParams?: Record<string, string>;\n}\n//# sourceMappingURL=types.d.ts.map","wms/url-builder.d.ts":"/**\n * WMS URL Builder\n *\n * Builds GetMap and GetFeatureInfo URLs for WMS 1.1.1 and 1.3.0.\n * Handles the critical BBOX coordinate order difference between versions:\n * - WMS 1.1.1: BBOX is always lon,lat,lon,lat (minX,minY,maxX,maxY)\n * - WMS 1.3.0: For EPSG:4326, BBOX is lat,lon,lat,lon (minY,minX,maxY,maxX)\n */\nimport type { GetMapParams, GetFeatureInfoParams } from './types';\n/**\n * Build a WMS GetMap URL.\n */\nexport declare function buildGetMapUrl(params: GetMapParams): string;\n/**\n * Build a WMS GetFeatureInfo URL.\n */\nexport declare function buildGetFeatureInfoUrl(params: GetFeatureInfoParams): string;\n//# sourceMappingURL=url-builder.d.ts.map","wms/wms-adapter.d.ts":"/**\n * WMS Adapter\n *\n * Implements the IMapImageryAdapter interface for WMS services.\n * Supports WMS 1.1.1 and 1.3.0 with automatic CRS negotiation,\n * OnlineResource URL resolution, and proxy support.\n */\nimport type { IMapImageryAdapter, MapImageryCapabilities, MapImageryRequest, FeatureInfoRequest, FeatureInfoResult } from './adapter-types';\nexport interface WmsAdapterOptions {\n    /** WMS service base URL */\n    url: string;\n    /** Preferred WMS version. Defaults to '1.3.0'. */\n    version?: string;\n    /** Optional proxy URL prefix. Requests will be sent as: proxyUrl?url=<encoded_target_url> */\n    proxyUrl?: string;\n}\nexport declare class WmsAdapter implements IMapImageryAdapter {\n    private readonly url;\n    private readonly version;\n    private readonly proxyUrl?;\n    private capabilities;\n    constructor(options: WmsAdapterOptions);\n    /**\n     * Build a fetch-ready URL, optionally routing through a proxy.\n     */\n    private buildFetchUrl;\n    /**\n     * Fetch and parse WMS GetCapabilities.\n     */\n    getCapabilities(): Promise<MapImageryCapabilities>;\n    /**\n     * Resolve the base URL for GetMap / GetFeatureInfo requests.\n     * Uses the OnlineResource URL from capabilities if available,\n     * otherwise falls back to the user-provided URL.\n     */\n    private getBaseUrl;\n    /**\n     * Build a GetMap URL for the given parameters.\n     */\n    getMapUrl(params: MapImageryRequest): string;\n    /**\n     * Execute a GetFeatureInfo request and return parsed results.\n     */\n    getFeatureInfo(params: FeatureInfoRequest): Promise<FeatureInfoResult>;\n    /**\n     * Negotiate CRS for a set of layer names based on cached capabilities.\n     */\n    private negotiateCrsForLayers;\n    /**\n     * Find a layer by name in a (possibly nested) layer tree.\n     */\n    private findLayer;\n    /**\n     * Negotiate the best GetFeatureInfo format from capabilities.\n     * Preference: geo+json > json > gml > html > text/plain\n     */\n    private negotiateInfoFormat;\n    /**\n     * Parse a JSON GetFeatureInfo response.\n     */\n    private parseJsonFeatureInfo;\n}\n//# sourceMappingURL=wms-adapter.d.ts.map","xyz/xyz-adapter.d.ts":"/**\n * XYZ Tile Adapter\n *\n * Supports standard XYZ ({z}/{x}/{y}) and TMS ({z}/{x}/{-y}) tile URL templates.\n */\nexport interface XyzAdapterOptions {\n    /** URL template. Use {z}, {x}, {y} placeholders. For TMS, use {-y}. */\n    urlTemplate: string;\n    /** Minimum zoom level (default: 0) */\n    minZoom?: number;\n    /** Maximum zoom level (default: 22) */\n    maxZoom?: number;\n}\nexport declare class XyzAdapter {\n    private readonly urlTemplate;\n    private readonly isTms;\n    readonly minZoom: number;\n    readonly maxZoom: number;\n    constructor(options: XyzAdapterOptions);\n    /**\n     * Generate the tile URL for the given tile coordinates.\n     *\n     * @param z - Zoom level\n     * @param x - Tile column\n     * @param y - Tile row (in standard XYZ / slippy map convention: 0 = top)\n     * @returns The resolved tile URL\n     */\n    getTileUrl(z: number, x: number, y: number): string;\n}\n//# sourceMappingURL=xyz-adapter.d.ts.map"},"@mapgpu/tiles3d":{"TileBoundingVolume.d.ts":"/**\n * 3D Tiles bounding volumes: box, sphere, and region.\n *\n * Used for frustum culling and SSE (Screen-Space Error) calculation.\n */\n/** Oriented bounding box: center (3) + half-axes (9) = 12 floats */\nexport interface BoundingBox {\n    type: 'box';\n    center: [number, number, number];\n    halfAxes: [\n        number,\n        number,\n        number,\n        number,\n        number,\n        number,\n        number,\n        number,\n        number\n    ];\n}\n/** Bounding sphere: center (3) + radius (1) = 4 floats */\nexport interface BoundingSphere {\n    type: 'sphere';\n    center: [number, number, number];\n    radius: number;\n}\n/** Geographic region: west, south, east, north (radians) + min/max height (meters) */\nexport interface BoundingRegion {\n    type: 'region';\n    west: number;\n    south: number;\n    east: number;\n    north: number;\n    minHeight: number;\n    maxHeight: number;\n}\nexport type TileBoundingVolume = BoundingBox | BoundingSphere | BoundingRegion;\n/** Parse a bounding volume from tileset.json */\nexport declare function parseBoundingVolume(raw: Record<string, unknown>): TileBoundingVolume | null;\n/** Compute the distance from a camera position to a bounding volume center. */\nexport declare function distanceToBoundingVolume(bv: TileBoundingVolume, cameraPos: [number, number, number]): number;\n//# sourceMappingURL=TileBoundingVolume.d.ts.map","TileCache.d.ts":"/**\n * LRU tile cache for 3D Tiles content.\n *\n * Manages loaded tile content and evicts least-recently-used entries\n * when the cache exceeds its capacity.\n */\nexport interface CachedTileContent {\n    /** Loaded tile content (parsed GLB, point cloud, etc.) */\n    data: unknown;\n    /** Approximate size in bytes for capacity tracking */\n    byteSize: number;\n    /** Last access timestamp */\n    lastUsed: number;\n}\nexport declare class TileCache {\n    /** Maximum cache size in bytes (default 256 MB) */\n    private readonly _maxBytes;\n    private _cache;\n    private _totalBytes;\n    constructor(\n    /** Maximum cache size in bytes (default 256 MB) */\n    _maxBytes?: number);\n    get(key: string): CachedTileContent | undefined;\n    has(key: string): boolean;\n    set(key: string, data: unknown, byteSize: number): void;\n    delete(key: string): void;\n    clear(): void;\n    get size(): number;\n    get totalBytes(): number;\n    private _evictOldest;\n}\n//# sourceMappingURL=TileCache.d.ts.map","TileContentLoader.d.ts":"/**\n * 3D Tiles content decoder.\n *\n * Decodes B3DM, I3DM, PNTS, and CMPT tile formats\n * into parsed model data for rendering.\n */\nimport type { ParsedGltf } from '@mapgpu/render-webgpu';\n/** Result of decoding a tile's content */\nexport interface DecodedTileContent {\n    type: 'model' | 'points' | 'composite';\n    /** Parsed glTF model (for B3DM/I3DM) */\n    model?: ParsedGltf;\n    /** Point cloud positions (for PNTS) */\n    positions?: Float32Array;\n    /** Point cloud colors (for PNTS, optional) */\n    colors?: Uint8Array;\n    /** Point count (for PNTS) */\n    pointCount?: number;\n    /** Instance transforms (for I3DM) */\n    instanceTransforms?: Float32Array;\n    instanceCount?: number;\n    /** Sub-contents (for CMPT) */\n    children?: DecodedTileContent[];\n}\n/** Decode a 3D Tiles content buffer based on its magic number. */\nexport declare function decodeTileContent(data: ArrayBuffer): DecodedTileContent;\n//# sourceMappingURL=TileContentLoader.d.ts.map","TileStyle.d.ts":"/**\n * 3D Tiles Styling Language — Declarative feature styling for 3D Tiles.\n *\n * CesiumJS Cesium3DTileStyle equivalent. Evaluates JSON-based style conditions\n * against batch table properties to produce per-feature color, show, and pointSize.\n *\n * @example\n * ```ts\n * const style = new TileStyle({\n *   color: {\n *     conditions: [\n *       ['${height} > 100', 'color(\"red\")'],\n *       ['${height} > 50',  'color(\"orange\")'],\n *       ['true',            'color(\"white\")'],\n *     ],\n *   },\n *   show: '${type} !== \"parking\"',\n *   pointSize: '${population} / 1000 + 3',\n * });\n * ```\n */\nexport interface TileStyleDefinition {\n    /** Color expression or conditions. */\n    color?: string | {\n        conditions: [string, string][];\n    };\n    /** Show/hide expression (boolean result). */\n    show?: string | boolean;\n    /** Point size expression (number result). */\n    pointSize?: string | number;\n}\nexport interface TileStyleResult {\n    color: [number, number, number, number];\n    show: boolean;\n    pointSize: number;\n}\nexport declare class TileStyle {\n    private _definition;\n    constructor(definition?: TileStyleDefinition);\n    get definition(): TileStyleDefinition;\n    /**\n     * Evaluate style against a set of feature properties.\n     *\n     * @param properties - Batch table properties for a single feature.\n     * @returns Resolved color, show flag, and point size.\n     */\n    evaluate(properties: Record<string, unknown>): TileStyleResult;\n    private _evaluateColor;\n    private _evaluateShow;\n    private _evaluatePointSize;\n}\n//# sourceMappingURL=TileStyle.d.ts.map","TileTraversal.d.ts":"/**\n * SSE-based tile selection for 3D Tiles.\n *\n * Computes Screen-Space Error for each tile and determines\n * which tiles to render vs. refine (load children).\n */\nimport type { TileBoundingVolume } from './TileBoundingVolume';\n/** A node in the parsed tileset tree. */\nexport interface TileNode {\n    /** Unique URI or path for content loading */\n    contentUri?: string;\n    /** Geometric error in meters */\n    geometricError: number;\n    /** Bounding volume */\n    boundingVolume: TileBoundingVolume;\n    /** Child tiles */\n    children: TileNode[];\n    /** Refinement strategy */\n    refine: 'ADD' | 'REPLACE';\n    /** Transform (4×4 column-major, identity if not specified) */\n    transform?: Float64Array;\n    /** Loading state */\n    _loaded?: boolean;\n    _loading?: boolean;\n}\n/** Parameters for SSE-based tile traversal */\nexport interface TraversalParams {\n    cameraPosition: [number, number, number];\n    viewportHeight: number;\n    fieldOfView: number;\n    sseThreshold: number;\n}\n/** Result of traversal — tiles to render and tiles to request */\nexport interface TraversalResult {\n    /** Tiles whose content should be rendered this frame */\n    render: TileNode[];\n    /** Tiles whose content needs to be loaded */\n    load: TileNode[];\n}\n/**\n * Compute Screen-Space Error for a tile.\n *\n * SSE = geometricError × (viewportHeight / (2 × distance × tan(fov/2)))\n */\nexport declare function computeSSE(geometricError: number, distance: number, viewportHeight: number, fov: number): number;\n/**\n * Traverse the tile tree and determine which tiles to render.\n *\n * Uses SSE (Screen-Space Error) to decide refinement:\n * - SSE > threshold → refine (recurse into children)\n * - SSE ≤ threshold → render this tile\n */\nexport declare function traverseTileset(root: TileNode, params: TraversalParams): TraversalResult;\n//# sourceMappingURL=TileTraversal.d.ts.map","Tileset3DLayer.d.ts":"/**\n * Tileset3DLayer — ILayer implementation for 3D Tiles.\n *\n * Loads a tileset.json, manages tile tree traversal (SSE-based),\n * content loading, and integrates with the mapgpu render engine.\n */\nimport type { Feature } from '@mapgpu/core';\nimport type { TileNode } from './TileTraversal';\nimport { type TraversalResult } from './TileTraversal';\nimport { type DecodedTileContent } from './TileContentLoader';\nexport interface Tileset3DLayerOptions {\n    id: string;\n    /** URL to tileset.json */\n    url: string;\n    /** Screen-Space Error threshold (default 16) */\n    sseThreshold?: number;\n    /** Max cache size in bytes (default 256 MB) */\n    maxCacheBytes?: number;\n    /** Visibility */\n    visible?: boolean;\n}\nexport declare class Tileset3DLayer {\n    readonly id: string;\n    readonly type: \"tileset3d\";\n    visible: boolean;\n    private _url;\n    private _sseThreshold;\n    private _root;\n    private _cache;\n    private _loadingCount;\n    private _ready;\n    renderer: null;\n    constructor(options: Tileset3DLayerOptions);\n    /** Whether the tileset root has been loaded and parsed. */\n    get ready(): boolean;\n    /** Number of tiles currently being loaded. */\n    get loadingCount(): number;\n    /** Total bytes cached. */\n    get cacheBytes(): number;\n    /** Tiles in cache. */\n    get cacheSize(): number;\n    /** Load the tileset.json and parse the tile tree. */\n    load(): Promise<void>;\n    /**\n     * Per-frame update: traverse the tile tree and determine which tiles\n     * to render / load based on the current camera.\n     *\n     * @returns Traversal result with render and load lists.\n     */\n    update(cameraPosition: [number, number, number], viewportHeight: number, fieldOfView: number): TraversalResult | null;\n    /** Get the decoded content for a tile. */\n    getContent(tile: TileNode): DecodedTileContent | undefined;\n    /** ILayer compatibility — 3D Tiles don't expose features directly. */\n    getFeatures(): Feature[];\n    destroy(): void;\n    private _loadTileContent;\n}\n//# sourceMappingURL=Tileset3DLayer.d.ts.map","TilesetParser.d.ts":"/**\n * Tileset JSON parser.\n *\n * Parses the tileset.json descriptor and builds the tile tree structure.\n */\nimport type { TileNode } from './TileTraversal';\n/**\n * Parse a tileset.json into a TileNode tree.\n *\n * @param json    - The tileset.json object.\n * @param baseUrl - The base URL for resolving content URIs.\n */\nexport declare function parseTileset(json: unknown, baseUrl: string): TileNode;\n//# sourceMappingURL=TilesetParser.d.ts.map","index.d.ts":"export { Tileset3DLayer } from './Tileset3DLayer';\nexport type { Tileset3DLayerOptions } from './Tileset3DLayer';\nexport { parseTileset } from './TilesetParser';\nexport { decodeTileContent } from './TileContentLoader';\nexport type { DecodedTileContent } from './TileContentLoader';\nexport { traverseTileset, computeSSE } from './TileTraversal';\nexport type { TileNode, TraversalParams, TraversalResult } from './TileTraversal';\nexport { parseBoundingVolume, distanceToBoundingVolume } from './TileBoundingVolume';\nexport type { TileBoundingVolume, BoundingBox, BoundingSphere, BoundingRegion, } from './TileBoundingVolume';\nexport { TileCache } from './TileCache';\nexport { TileStyle } from './TileStyle';\nexport type { TileStyleDefinition, TileStyleResult } from './TileStyle';\n//# sourceMappingURL=index.d.ts.map"},"@mapgpu/milsymbol":{"index.d.ts":"export * from './milsym/index';\nexport type { IIconSink, IconAnchor, AtlasCapacity } from './integration/IIconSink';\nexport type { IBatchLoader, BatchLoadOptions } from './integration/IBatchLoader';\nexport { MapViewIconSink } from './integration/MapViewIconSink';\nexport { MilBatchLoader, makeIconId } from './integration/MilBatchLoader';\nexport { MilSymbolLayer } from './integration/MilSymbolLayer';\nexport type { MilSymbolLayerOptions } from './integration/MilSymbolLayer';\nexport { createMilSymbolRenderer } from './integration/createMilSymbolRenderer';\nexport type { MilSymbolRendererOptions } from './integration/createMilSymbolRenderer';\n/** @deprecated Use MilBatchLoader + IIconSink instead */\nexport { loadMilIcon, batchLoadMilIcons } from './integration/icon-loader';\n//# sourceMappingURL=index.d.ts.map","integration/IBatchLoader.d.ts":"/**\n * IBatchLoader — Interface for batch-loading military symbols into an icon sink.\n *\n * Decouples the batch loading strategy from both the rendering engine\n * and the target platform. Implementations can vary concurrency, caching,\n * and prioritization strategies.\n */\nimport type { IIconSink } from './IIconSink';\n/**\n * Options for batch loading operations.\n */\nexport interface BatchLoadOptions {\n    /** Maximum number of icons to rasterize in parallel. Default: 16 */\n    concurrency?: number;\n}\n/**\n * Platform-agnostic batch symbol loader.\n *\n * Manages the lifecycle of symbol rasterization and loading:\n *   SIDC string → SVG render → Bitmap rasterize → IIconSink.loadIcon()\n */\nexport interface IBatchLoader {\n    /**\n     * Load multiple symbols into the icon sink in batches.\n     *\n     * Duplicate SIDCs (or already-loaded ones) are automatically skipped.\n     *\n     * @param sidcs   Array of SIDC strings to load\n     * @param size    Icon size in pixels\n     * @param sink    Target icon sink\n     * @param options Optional batch configuration\n     */\n    loadSymbols(sidcs: readonly string[], size: number, sink: IIconSink, options?: BatchLoadOptions): Promise<void>;\n    /**\n     * Load a single symbol into the icon sink.\n     *\n     * No-op if the symbol is already loaded.\n     *\n     * @param sidc  SIDC string\n     * @param size  Icon size in pixels\n     * @param sink  Target icon sink\n     */\n    loadSymbol(sidc: string, size: number, sink: IIconSink): Promise<void>;\n    /**\n     * Check whether a symbol (by SIDC + size) has been loaded.\n     *\n     * @param sidc  SIDC string\n     * @param size  Icon size in pixels\n     */\n    isLoaded(sidc: string, size: number): boolean;\n    /** Number of unique symbols currently loaded. */\n    getLoadedCount(): number;\n    /** Clear the loaded symbol tracking set (does NOT remove icons from the sink). */\n    clear(): void;\n}\n//# sourceMappingURL=IBatchLoader.d.ts.map","integration/IIconSink.d.ts":"/**\n * IIconSink — Platform-agnostic interface for loading icons into a sprite atlas.\n *\n * Decouples the milsym rendering engine from any specific map platform.\n * Implementations adapt to a concrete map engine (MapView, Leaflet, OpenLayers, etc.)\n *\n * @example\n * ```ts\n * // MapView adapter (built-in)\n * const sink = new MapViewIconSink(mapView);\n *\n * // Custom adapter for another platform\n * class LeafletIconSink implements IIconSink {\n *   loadIcon(id, bitmap, anchor) { ... }\n *   ...\n * }\n * ```\n */\n/**\n * Anchor point for an icon, in pixel coordinates relative to the top-left corner.\n * Used to position the icon correctly at its geographic location.\n */\nexport interface IconAnchor {\n    /** X offset in pixels from the left edge of the bitmap */\n    readonly x: number;\n    /** Y offset in pixels from the top edge of the bitmap */\n    readonly y: number;\n}\n/**\n * Reports the current capacity of the sprite atlas (icon texture).\n */\nexport interface AtlasCapacity {\n    /** Number of icon slots currently in use */\n    readonly used: number;\n    /** Total number of icon slots available */\n    readonly total: number;\n}\n/**\n * Platform-agnostic icon sink interface.\n *\n * Allows the milsym rendering pipeline to load rasterized icons\n * into any map platform's sprite atlas without knowing the platform details.\n */\nexport interface IIconSink {\n    /**\n     * Load a rasterized icon into the sprite atlas.\n     *\n     * @param id      Unique icon identifier (e.g. \"mil-10031000001211000000-48\")\n     * @param bitmap  The rasterized icon as an ImageBitmap\n     * @param anchor  Optional anchor point; defaults to center of bitmap\n     */\n    loadIcon(id: string, bitmap: ImageBitmap, anchor?: IconAnchor): void | Promise<void>;\n    /**\n     * Check whether an icon with the given ID is already loaded.\n     *\n     * @param id  Icon identifier to check\n     * @returns true if the icon is present in the atlas\n     */\n    hasIcon(id: string): boolean;\n    /**\n     * Remove an icon from the sprite atlas and free its slot.\n     *\n     * @param id  Icon identifier to remove\n     */\n    removeIcon(id: string): void;\n    /**\n     * Query the current sprite atlas capacity.\n     *\n     * Useful for monitoring resource usage and deciding when to evict\n     * unused icons before hitting atlas limits.\n     */\n    getAtlasCapacity(): AtlasCapacity;\n}\n//# sourceMappingURL=IIconSink.d.ts.map","integration/MapViewIconSink.d.ts":"/**\n * MapViewIconSink — Adapter that bridges IIconSink to MapView.loadIcon().\n *\n * This is the default IIconSink implementation for the MapGPU engine.\n * It wraps a MapView instance and delegates icon loading to its sprite atlas.\n *\n * @example\n * ```ts\n * const sink = new MapViewIconSink(mapView);\n * const loader = new MilBatchLoader();\n * await loader.loadSymbols(['10031000001211000000'], 48, sink);\n * ```\n */\nimport type { MapView } from '@mapgpu/core';\nimport type { IIconSink, IconAnchor, AtlasCapacity } from './IIconSink';\n/**\n * Default IIconSink implementation backed by a MapView instance.\n *\n * Tracks loaded icons locally so `hasIcon()` is O(1) without\n * querying the GPU sprite atlas.\n */\nexport declare class MapViewIconSink implements IIconSink {\n    private readonly _mapView;\n    private readonly _loadedIcons;\n    /**\n     * @param _mapView  The MapView whose sprite atlas receives the icons.\n     *                  Must be ready (i.e. mapView.when() resolved).\n     */\n    constructor(_mapView: MapView);\n    /**\n     * Load an icon into the MapView sprite atlas.\n     *\n     * Anchor is recorded but currently unused by the MapView icon pipeline\n     * (MapView centers icons by default). Kept for future use / other sinks.\n     */\n    loadIcon(id: string, bitmap: ImageBitmap, _anchor?: IconAnchor): Promise<void>;\n    /** Check if an icon has already been loaded through this sink. */\n    hasIcon(id: string): boolean;\n    /**\n     * Remove an icon from the tracking set.\n     *\n     * Note: MapView does not currently expose a removeIcon() API,\n     * so the GPU texture slot is not freed. This method updates the\n     * local tracking set so the icon can be re-loaded if needed.\n     */\n    removeIcon(id: string): void;\n    /**\n     * Report atlas capacity.\n     *\n     * MapView does not expose atlas metrics directly, so we report\n     * the number of icons tracked by this sink. Subclasses or future\n     * engine versions can override with real GPU atlas metrics.\n     */\n    getAtlasCapacity(): AtlasCapacity;\n    /** Expose the underlying MapView for advanced use cases. */\n    get mapView(): MapView;\n}\n//# sourceMappingURL=MapViewIconSink.d.ts.map","integration/MilBatchLoader.d.ts":"/**\n * MilBatchLoader — Default IBatchLoader implementation using the milsym engine.\n *\n * Renders SIDC strings to SVG via MilStdIconRenderer, rasterizes to ImageBitmap,\n * and loads into an IIconSink. Uses parallel chunk processing for throughput.\n *\n * Render Pipeline (optimized in Phase 3):\n *   SIDC → SVG string → Blob → createImageBitmap → IIconSink.loadIcon()\n *\n * @example\n * ```ts\n * const loader = new MilBatchLoader();\n * const sink = new MapViewIconSink(mapView);\n * await loader.loadSymbols(sidcArray, 48, sink);\n * ```\n */\nimport type { IIconSink } from './IIconSink';\nimport type { IBatchLoader, BatchLoadOptions } from './IBatchLoader';\n/**\n * Build a deterministic icon ID from SIDC + size.\n * Must match the format expected by the renderer pipeline.\n */\nexport declare function makeIconId(sidc: string, size: number): string;\n/**\n * Default IBatchLoader implementation backed by the milsym engine.\n *\n * Tracks loaded symbols in a local Set to avoid duplicate work.\n * Parallel rasterization is chunked for throughput vs memory balance.\n */\nexport declare class MilBatchLoader implements IBatchLoader {\n    private readonly _loaded;\n    loadSymbols(sidcs: readonly string[], size: number, sink: IIconSink, options?: BatchLoadOptions): Promise<void>;\n    loadSymbol(sidc: string, size: number, sink: IIconSink): Promise<void>;\n    isLoaded(sidc: string, size: number): boolean;\n    getLoadedCount(): number;\n    clear(): void;\n}\n//# sourceMappingURL=MilBatchLoader.d.ts.map","integration/MilSymbolLayer.d.ts":"/**\n * MilSymbolLayer — High-level layer for military symbols on the map.\n *\n * Wraps GeoJSONLayer + CallbackRenderer. Automatically loads SIDC icons\n * into the sprite atlas via IIconSink and renders them.\n *\n * Responsibilities (thin adapter only):\n *   - GeoJSON layer configuration\n *   - IIconSink + IBatchLoader wiring\n *   - Renderer setup via createMilSymbolRenderer\n *\n * SVG rendering and SIDC parsing are delegated to the milsym engine.\n *\n * @example\n * ```ts\n * // New API (platform-agnostic):\n * const sink = new MapViewIconSink(mapView);\n * const loader = new MilBatchLoader();\n * const layer = new MilSymbolLayer({ data: fc, sink, loader });\n * await layer.attach();\n * mapView.map.add(layer.layer);\n *\n * // Legacy API (backward-compatible):\n * const layer = new MilSymbolLayer({ data: fc });\n * await layer.attachTo(mapView);\n * mapView.map.add(layer.layer);\n * ```\n */\nimport type { MapView } from '@mapgpu/core';\nimport { GeoJSONLayer } from '@mapgpu/layers';\nimport type { IIconSink } from './IIconSink';\nimport type { IBatchLoader } from './IBatchLoader';\ninterface GeoJsonFeature {\n    type: 'Feature';\n    id?: string | number;\n    geometry: {\n        type: string;\n        coordinates: unknown;\n    };\n    properties?: Record<string, unknown>;\n}\ninterface GeoJsonFeatureCollection {\n    type: 'FeatureCollection';\n    features: GeoJsonFeature[];\n}\nexport interface MilSymbolLayerOptions {\n    /** Layer ID */\n    id?: string;\n    /** GeoJSON FeatureCollection with SIDC in properties */\n    data: GeoJsonFeatureCollection;\n    /** Feature property field containing SIDC. Default: 'sidc' */\n    sidcField?: string;\n    /** Icon size in pixels. Default: 48 */\n    size?: number;\n    /** Tint color [R,G,B,A]. Default: [255,255,255,255] */\n    color?: [number, number, number, number];\n    /** Custom icon sink (platform adapter). If omitted, created from MapView in attach/attachTo. */\n    sink?: IIconSink;\n    /** Custom batch loader. If omitted, uses default MilBatchLoader. */\n    loader?: IBatchLoader;\n}\nexport declare class MilSymbolLayer {\n    /** The underlying GeoJSONLayer */\n    readonly layer: GeoJSONLayer;\n    private readonly _sidcField;\n    private readonly _size;\n    private readonly _color;\n    private readonly _data;\n    private readonly _loader;\n    private _sink;\n    constructor(options: MilSymbolLayerOptions);\n    /**\n     * Attach using a pre-configured IIconSink (platform-agnostic).\n     *\n     * @param sink  Optional override; uses constructor sink if provided there.\n     */\n    attach(sink?: IIconSink): Promise<void>;\n    /**\n     * Attach to a MapView (backward-compatible convenience method).\n     *\n     * @deprecated Prefer `attach(new MapViewIconSink(mapView))` for new code.\n     * @param mapView  The MapView instance (GPU must be ready).\n     */\n    attachTo(mapView: MapView): Promise<void>;\n    /** Collect unique SIDCs from the GeoJSON data. */\n    private _collectUniqueSidcs;\n}\nexport {};\n//# sourceMappingURL=MilSymbolLayer.d.ts.map","integration/createMilSymbolRenderer.d.ts":"/**\n * createMilSymbolRenderer — Factory that returns a CallbackRenderer\n * resolving each feature's SIDC to a pre-loaded icon symbol.\n *\n * Usage:\n *   const renderer = createMilSymbolRenderer(mapView, { size: 48, sidcField: 'sidc' });\n *   geojsonLayer.renderer = renderer;\n *\n * Icons are loaded lazily on the first render pass that encounters a new SIDC.\n */\nimport type { MapView, IRenderer } from '@mapgpu/core';\nimport type { IIconSink } from './IIconSink';\nimport type { IBatchLoader } from './IBatchLoader';\nexport interface MilSymbolRendererOptions {\n    /** Icon size in pixels. Default: 48 */\n    size?: number;\n    /** Feature attribute field containing the SIDC. Default: 'sidc' */\n    sidcField?: string;\n    /** Tint color [R,G,B,A]. Default: [255,255,255,255] (preserve original) */\n    color?: [number, number, number, number];\n    /** Custom icon sink. If omitted, created from mapView. */\n    sink?: IIconSink;\n    /** Custom batch loader. If omitted, uses default MilBatchLoader. */\n    loader?: IBatchLoader;\n}\n/**\n * Create a CallbackRenderer that renders military symbols from SIDC attributes.\n *\n * @param mapView  The MapView instance (needed for lazy icon loading)\n * @param options  Renderer options\n * @returns An IRenderer that can be assigned to any IFeatureLayer\n */\nexport declare function createMilSymbolRenderer(mapView: MapView, options?: MilSymbolRendererOptions): IRenderer;\n//# sourceMappingURL=createMilSymbolRenderer.d.ts.map","integration/icon-loader.d.ts":"/**\n * icon-loader — Backward-compatible facade for loading MIL-STD-2525D/E symbols.\n *\n * These functions delegate to MilBatchLoader + MapViewIconSink internally\n * but preserve the original MapView-based API for existing callers.\n *\n * @deprecated Prefer using MilBatchLoader + IIconSink directly for new code.\n *             These functions will be removed in a future major version.\n *\n * Render Pipeline (optimized):\n *   SVG string → Blob URL → createImageBitmap → GPU\n */\nimport type { MapView } from '@mapgpu/core';\nimport { makeIconId } from './MilBatchLoader';\n/**\n * Load a single military symbol icon into the MapView sprite atlas.\n *\n * @deprecated Use `MilBatchLoader.loadSymbol()` with an `IIconSink` instead.\n *\n * @param mapView  The MapView to load the icon into\n * @param sidc     The SIDC string (20-digit 2525D/E format)\n * @param size     Icon size in pixels (default 48)\n * @param options  Optional: loadedIcons set (ignored, kept for API compat)\n */\nexport declare function loadMilIcon(mapView: MapView, sidc: string, size?: number, _options?: {\n    loadedIcons?: Set<string>;\n}): Promise<void>;\n/**\n * Load multiple military symbol icons in batch with parallel processing.\n *\n * @deprecated Use `MilBatchLoader.loadSymbols()` with an `IIconSink` instead.\n *\n * @param mapView  The MapView to load icons into\n * @param sidcs    Array of SIDC strings\n * @param size     Icon size in pixels (default 48)\n * @param options  Optional: concurrency override (loadedIcons ignored)\n */\nexport declare function batchLoadMilIcons(mapView: MapView, sidcs: readonly string[], size?: number, options?: {\n    loadedIcons?: Set<string>;\n    concurrency?: number;\n}): Promise<void>;\n/** Re-export makeIconId from the canonical location */\nexport { makeIconId };\n//# sourceMappingURL=icon-loader.d.ts.map","milsym/canvas-factory.d.ts":"/**\n * canvas-factory — Browser-first canvas creation with Node.js fallback.\n *\n * Uses OffscreenCanvas in browser environments.\n * Falls back to 'canvas' npm package in Node.js (must be installed as devDependency).\n */\nexport declare function createMeasureCanvas(w: number, h: number): any;\n//# sourceMappingURL=canvas-factory.d.ts.map","milsym/generators/channel-generator.d.ts":"import { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"./shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\n/**\n * CELineArray Channels class calculates the channel points\n *\n */\nexport declare class Channels {\n    private static readonly maxLength;\n    private static readonly minLength;\n    private static readonly _className;\n    private static _client;\n    static setClient(value: string): void;\n    private static _shiftLines;\n    static getShiftLines(): boolean;\n    private static ConnectArrayTrueDouble;\n    private static GetChannel2Double;\n    private static GetLowerChannelLineDouble;\n    private static GetUpperChannelLineDouble;\n    private static FenceType;\n    /**\n    * Calculates the point count for the concertina wire and fence channels.\n    *\n    * @param pLinePoints client points\n    * @param vblCounter the client point count\n    * @param linetype the line type\n    *\n    * @return the number of points required to render the symbol\n    */\n    static GetTripleCountDouble(pLinePoints: POINT2[], vblCounter: number, linetype: number): number;\n    protected static CoordIL2Double(nPrinter: number, pLinePoints: POINT2[], nUpperLower: number, vblCounter: number, linetype: number, vblChannelWidth: number): POINT2[];\n    /**\n     * gets the axis of advance arrowhead\n     * @param dPrinter\n     * @param pLowerLinePoints\n     * @param lLowerCounter\n     * @param pUpperLinePoints\n     * @param lUpperCounter\n     * @param ArrowLinePoint\n     * @param pLinePoints\n     * @param vbiDrawThis\n     * @param dOffsetFactor\n     */\n    private static GetAXADDouble;\n    /**\n     * Calculates a channel line and is called once each time for lower and upper channel lines.\n     *\n     * @param nPrinter always 1\n     * @param pLinePoints client points\n     * @param nUpperLower 0 for lower channel line, 1 for upper channel line\n     * @param vblCounter the client point count\n     * @param vbiDrawThis the line type\n     * @param vblChannelWidth the channel width\n     *\n     * @return the channel line array as POINT2\n     */\n    static GetChannelArray2Double(nPrinter: number, pLinePoints: POINT2[], nUpperLower: number, vblCounter: number, vbiDrawThis: number, vblChannelWidth: number): POINT2[];\n    private static GetTrueEndPointDouble;\n    private static ConnectTrueDouble2;\n    /**\n     * Calculates the channel points\n     * @param lpsaUpperVBPoints the client points as 2-tuples\n     * @param lpsaLowerVBPoints the client points as 2 tuples\n     * @param resultVBPoints the result points as 3-tuples x,y,linestyle\n     * @param vblUpperCounter the number of client 2-tuples\n     * @param vblLowerCounter the number of client 2-tuples\n     * @param vblChannelWidth the channel width in pixels\n     * @param useptr the distance in pixels from the arrow tip to the back of the arrowhead\n     * @param shapes the ShapeInfo array, each object contains the GeneralPath\n     * @return\n     */\n    static GetChannel1Double(tg: TacticalGraphic, lpsaUpperVBPoints: number[], lpsaLowerVBPoints: number[], resultVBPoints: number[], vblUpperCounter: number, vblLowerCounter: number, vblChannelWidth: number, useptr: number, shapes: Array<Shape2>): number;\n    /**\n     * They decided that axis of advance must enable fill\n     * @param lineType\n     * @param pLinePoints\n     * @return\n     */\n    private static getAXADFillShapes;\n}\n//# sourceMappingURL=channel-generator.d.ts.map","milsym/generators/channel-points.d.ts":"import { POINT2 } from \"../types/point\";\n/**\n * A class for channel points used by ChannelUtils\n *\n */\nexport declare class CChannelPoints2 {\n    m_Line1: POINT2;\n    m_Line2: POINT2;\n    constructor();\n    constructor(pts: CChannelPoints2);\n}\n//# sourceMappingURL=channel-points.d.ts.map","milsym/generators/count-calculator.d.ts":"import { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\n/**\n * A class to calculate the number of pixels based points required for a line\n *\n */\nexport declare class countsupport {\n    private static readonly maxLength;\n    private static readonly minLength;\n    private static readonly _className;\n    /**\n     * The main function to return the number of points needed for a symbol\n     * @param vblCounter the number of client points\n     * @param pLinePoints the client point array\n     * @return the number of points required to draw the symbol\n     */\n    static GetCountersDouble(tg: TacticalGraphic, vblCounter: number, pLinePoints: POINT2[], clipBounds: Rectangle2D | null): number;\n    private static GetReefCount;\n    private static GetRestrictedAreaCount;\n    private static GetPipeCount;\n    static GetXPointsCount(pOriginalLinePoints: POINT2[], segmentLength: number, vblCounter: number): number;\n    static GetLVOCount(pOriginalLinePoints: POINT2[], segmentLength: number, vblCounter: number): number;\n    private static GetIcingCount;\n    protected static GetITDQty(pLinePoints: POINT2[], length: number, vblCounter: number): number;\n    protected static GetConvergenceQty(pLinePoints: POINT2[], length: number, vblCounter: number): number;\n    /**\n     * Calculates the points for ATDITCH, ATDITCHC, ATDITCHM\n     * @param pLinePoints the client point array\n     * @param vblCounter the number of client points\n     * @param vbiDrawThis the line type\n     * @return\n     */\n    private static GetDitchCountDouble;\n    static GetSquallQty(pLinePoints: POINT2[], quantity: number, length: number, numPoints: number): number;\n    static GetSquallSegQty(StartPt: POINT2, EndPt: POINT2, quantity: number, length: number): number;\n    /**\n     * returns number of points required for ATWALL, FORT and other symbols\n     * @param pLinePoints the client points\n     * @param vblCounter the number of client points\n     * @return\n     */\n    static GetFORTLCountDouble(tg: TacticalGraphic, pLinePoints: POINT2[], vblCounter: number): number;\n    private static GetByDifSegment;\n    /**\n     * clipBounds is used because of the glyphs on one segment\n     * @param FirstLinePoint\n     * @param LastLinePoint\n     * @param clipBounds\n     * @return\n     */\n    protected static GetDISMFixCountDouble(FirstLinePoint: POINT2, LastLinePoint: POINT2, clipBounds: Rectangle2D): number;\n}\n//# sourceMappingURL=count-calculator.d.ts.map","milsym/generators/dism-generator.d.ts":"import { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"./shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\n/**\n * A class which imported many of the C++ functions from Trident\n * Systems Dismounted Intelligence Situational Awareness System (DISM) for\n * rendering Mil-Standard-2525 tactical lines. This class does not get instantiated\n * Much of the code is still the original DISM code.\n *\n */\nexport declare class DISMSupport {\n    private static readonly LEFT_SIDE;\n    private static readonly RIGHT_SIDE;\n    private static readonly COLINEAR;\n    private static readonly CONST_PI;\n    private static readonly maxLength;\n    private static readonly minLength;\n    private static readonly _className;\n    private static GetTGFontSize;\n    private static ArcApproximationDouble;\n    private static DrawOpenRectangleDouble;\n    private static DetermineDirectionDouble;\n    private static CalcEndpieceDeltasDouble;\n    /**\n     * Calculates the points for line24 and line33 (DELAY, WITHDRAW, DISENGAGE, WDRAWUP, RETIRE, FPOL, RPOL, PURSUIT)\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     */\n    static GetDelayGraphicEtcDouble(points: POINT2[], lineType: number): number;\n    private static IsEnvelopmentArcReversed;\n    /**\n     * Calculates the points for ENVELOPMENT\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     */\n    static GetEnvelopmentGraphicDouble(points: POINT2[]): number;\n    /**\n     * Calculates the points for SCREEN, COVER, GUARD, SARA.\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param lineType the line type.\n     */\n    static GetDISMCoverDouble(points: POINT2[], lineType: number): number;\n    /**\n     * rev C uses 4\n     * @param points\n     * @param linetype\n     * @return\n     */\n    static GetDISMCoverDoubleRevC(points: POINT2[], linetype: number, vblSaveCounter: number): number;\n    /**\n     * Calculates the points for BYPASS\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMBypassDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for BREACH.\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMBreachDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for CANALIZE\n     *\n     * @param points - OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMCanalizeDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Gets shape for Feint, decoy, or dummy indicator. Does not check if tactical graphic should have indicator\n     * @param tg used to get line color and stroke\n     * @param ptA bottom left point of triangle\n     * @param ptC bottom right point of triangle\n     * @return Dummy indicator shape\n     */\n    static getFDIShape(tg: TacticalGraphic, ptA: POINT2, ptC: POINT2): Shape2;\n    /**\n     * Gets shape for Feint, decoy, or dummy indicator for symbols with arrowhead. Does not check if tactical graphic should have indicator\n     * Extends each point outside arrow as necessary\n     * @param tg used to get line color and stroke\n     * @param ptA bottom left point of arrow\n     * @param ptB arrow point\n     * @param ptC bottom right point of arrow\n     * @return Dummy indicator shape\n     */\n    static getFDIShape(tg: TacticalGraphic, ptA: POINT2, ptB: POINT2, ptC: POINT2): Shape2;\n    /**\n     * Calculates the points for DISRUPT\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMDisruptDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for CONTAIN\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMContainDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for FIX, MNFLDFIX\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMFixDouble(points: POINT2[], linetype: number, clipBounds: Rectangle2D | null): number;\n    /**\n     * Calculates the points for CLEAR.\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMClearDouble(points: POINT2[], linetype: number): number;\n    private static IsSeizeArcReversed;\n    /**\n     * Calculates the points for SEIZE\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMSeizeDouble(points: POINT2[], linetype: number, radius: number): number;\n    /**\n     * Used twice for RIP to determine if the points are clockwise.\n     * @param x1\n     * @param y1\n     * @param x2\n     * @param y2\n     * @param px\n     * @param py\n     * @return RIGHT_SIDE if 3 points are clockwise\n     */\n    private static side;\n    /**\n     * Calculates the points for RIP\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type\n     */\n    static GetDISMRIPDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for BYDIF\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMByDifDouble(points: POINT2[], linetype: number, clipBounds: Rectangle2D | null): number;\n    /**\n     * Calculates the points for PENETRATE\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMPenetrateDouble(points: POINT2[], linetype: number): void;\n    /**\n     * Calculates the points for BYIMP\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMByImpDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for SPTBYFIRE\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMSupportByFireDouble(points: POINT2[], linetype: number): number;\n    private static ReorderAtkByFirePoints;\n    private static ReorderSptByFirePoints;\n    /**\n     * Calculates the points for ATKBYFIRE\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMATKBYFIREDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for GAP\n     *\n     * @param points OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMGapDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for MNFLDDIS\n     *\n     * @param points - OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMMinefieldDisruptDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for LINTGT\n     *\n     * @param points - OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     * @param vblCounter the number of points required to display the symbol\n     */\n    static GetDISMLinearTargetDouble(points: POINT2[], linetype: number, vblCounter: number): number;\n    /**\n     * Calculates the points for BLOCK, MNFLDBLK\n     *\n     * @param points - OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMBlockDouble2(points: POINT2[], linetype: number): void;\n    /**\n     * Calculates the points for PAA_RECTANGULAR.\n     *\n     * @param points - OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    protected static GetDISMPAADouble(points: POINT2[], linetype: number): void;\n    private static ReverseDelayArc;\n    private static isInRange;\n    private static getAngleBetweenPoints;\n    /**\n     * Returns an angle from 0 to 360\n     *\n     * @param angle the angle to normalize\n     * @return an angle in range from 0 to 360\n     */\n    static normalizeAngle(angle: number): number;\n    private static DrawEndpieceDeltasDouble;\n    /**\n     * Calculates the points for EASY\n     *\n     * @param points - OUT - the client points, also used for the returned points.\n     * @param linetype the line type.\n     */\n    static GetDISMEasyDouble(points: POINT2[], linetype: number): number;\n    /**\n     * Calculates the points for AMBUSH\n     *\n     * @param pLinePoints - OUT - the client points, also used for the returned points.\n     */\n    static AmbushPointsDouble(pLinePoints: POINT2[]): number;\n}\n//# sourceMappingURL=dism-generator.d.ts.map","milsym/generators/flotation-generator.d.ts":"import { POINT2 } from \"../types/point\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\n/**\n * A class for calculating flot ellipses. Some functions use the same array for the client points\n * and the return points. The caller allocates a size large enough to hold the return points.\n *\n *\n */\nexport declare class flot {\n    private static readonly _className;\n    static GetAnchorageFlotSegment(vbPoints: number[], x1: number, y1: number, x2: number, y2: number, segment: number, floatDiameter: number, points: number[], bFlipVal: number, lDirectionVal: number, lLastDirectionVal: number): {\n        count: number;\n        bFlip: number;\n        lDirection: number;\n        lLastDirection: number;\n    };\n    static GetAnchorageCountDouble(vbPoints: POINT2[], floatDiameter: number, numPts: number): number;\n    static GetFlotCount2Double(tg: TacticalGraphic, vbPoints: POINT2[], numPts: number): number;\n    static GetFlot2Double(tg: TacticalGraphic, vbPoints2: POINT2[], numPts: number): number;\n    private static GetFlotSegment2;\n    static GetOFYCountDouble(pLinePoints: POINT2[], interval: number, numPts: number): number;\n    static GetOccludedPointsDouble(tg: TacticalGraphic, pLinePoints: POINT2[], numPts: number): number;\n    static GetOccludedCountDouble(pLinePoints: POINT2[], numPts: number): number;\n    private static CalcNewPoint;\n    /**\n     * Calculates points for a flot segment. Assumes the caller allocated the points array\n     * @param vbPoints the client points\n     * @param segment the segment index\n     * @param points the returned points\n     * @return the number of points\n     */\n    private static GetFlotSegment;\n    /**\n     * Calculates the points for FLOT, LC\n     * @param vbPoints2 OUT the clinet points also used for the return points\n     * @param numPts\n     * @return\n     */\n    static GetFlotDouble(vbPoints2: POINT2[], flotDiameter: number, numPts: number): number;\n    private static CalcAnglePoints;\n    /**\n     * Calculates the number of points required for a flot\n     * @param vbPoints the clinet points\n     * @param numPts the number of client points\n     * @return the number of points required\n     */\n    static GetFlotCountDouble(vbPoints: POINT2[], flotDiameter: number, numPts: number): number;\n    static GetOFYPointsDouble(tg: TacticalGraphic, pLinePoints: POINT2[], numPts: number): number;\n    static GetSFPointsDouble(tg: TacticalGraphic, pLinePoints: POINT2[], numPts: number): number;\n    static GetSFCountDouble(pLinePoints: POINT2[], numPts: number): number;\n}\n//# sourceMappingURL=flotation-generator.d.ts.map","milsym/generators/line-array.d.ts":"import { TacticalGraphic } from \"../tactical/tactical-graphic\";\n/**\n * A class for the interface between the points calculation CELineArray and\n * the tactical renderer.\n *\n *\n */\nexport declare class CELineArray {\n    private static readonly _className;\n    /**\n    * public function to return the line count required for all of the symbols\n    *\n    * @param plArrayOfLongs the client points as an array of POINT2 in pixels.\n    * @param lElements the number of client points.\n    * @param ChannelWidth the chanel width in pixels\n    *\n    * @return the number of points which will be required for the symbol.\n    */\n    static CGetLineCountDouble(tg: TacticalGraphic, plArrayOfLongs: number[], lElements: number, //number of points\n    ChannelWidth: number): number;\n    /**\n     * Return true is the line type is a channel type\n     * @param lineType line type\n     * @return\n     */\n    static CIsChannel(lineType: number): number;\n    private static _client;\n    static setClient(value: string): void;\n    static getClient(): string;\n}\n//# sourceMappingURL=line-array.d.ts.map","milsym/generators/line-generator.d.ts":"import { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"./shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n * Class to process the pixel arrays\n *\n */\nexport declare class arraysupport {\n    private static readonly maxLength;\n    private static readonly minLength;\n    private static dACP;\n    private static readonly _className;\n    private static FillPoints;\n    /**\n     * This is the interface function to CELineArray from MultipointRenderer2 for\n     * non-channel types\n     *\n     * @param pts the client points\n     * @param shapes the symbol ShapeInfo objects\n     * @param clipBounds the rectangular clipping bounds\n     */\n    static GetLineArray2(tg: TacticalGraphic, pts: Array<POINT2>, shapes: Array<Shape2>, clipBounds: Rectangle2D | null, converter: IPointConversion | null): Array<POINT2> | null;\n    /**\n     * A function to calculate the points for FORTL\n     *\n     * @param pLinePoints OUT - the points arry also used for the return points\n     * @param vblSaveCounter the number of client points\n     * @return\n     */\n    private static GetFORTLPointsDouble;\n    private static GetATWallPointsDouble2;\n    static GetInsideOutsideDouble2(pt0: POINT2, pt1: POINT2, pLinePoints: POINT2[], vblCounter: number, index: number, lineType: number): number;\n    /**\n     * BELT and others\n     *\n     * @param pLinePoints\n     * @param vblSaveCounter\n     * @return\n     */\n    protected static GetZONEPointsDouble2(tg: TacticalGraphic, pLinePoints: POINT2[], vblSaveCounter: number): number;\n    private static IsTurnArcReversed;\n    private static GetIsolatePointsDouble;\n    private static AirfieldCenterFeature;\n    private static GetATWallPointsDouble;\n    private static GetRidgePointsDouble;\n    protected static GetSquallDouble(pLinePoints: POINT2[], amplitude: number, quantity: number, length: number, numPoints: number): number;\n    protected static GetSevereSquall(pLinePoints: POINT2[], length: number, numPoints: number): number;\n    private static GetConvergencePointsDouble;\n    private static GetITDPointsDouble;\n    private static GetXPoints;\n    /**\n     * returns a 37 point ellipse\n     *\n     * @param ptCenter\n     * @param ptWidth\n     * @param ptHeight\n     * @return\n     */\n    private static getEllipsePoints;\n    /**\n     * Calculate an ellipse and rotate about it's center by azimuth in degrees\n     *\n     * @param ptCenter\n     * @param ptWidth\n     * @param ptHeight\n     * @param azimuth\n     * @return\n     */\n    private static getRotatedEllipsePoints;\n    private static GetLVOPoints;\n    private static GetIcingPointsDouble;\n    protected static GetAnchorageDouble(vbPoints2: POINT2[], floatDiameter: number, numPts: number): number;\n    private static GetPipePoints;\n    private static GetReefPoints;\n    private static GetRestrictedAreaPoints;\n    private static getOverheadWire;\n    /**\n     * Calculates the points for the non-channel symbols. The points will be\n     * stored in the original POINT2 array in pixels, pLinePoints. The client\n     * points occupy the first vblSaveCounter positions in pLinePoints and will\n     * be overwritten by the symbol points.\n     *\n     * @param pLinePoints - OUT - an array of POINT2\n     * @param vblCounter the number of points allocated\n     * @param vblSaveCounter the number of client points\n     *\n     * @return the symbol point count\n     */\n    private static GetLineArray2Double;\n    private static addPolyline;\n    /**\n     * Returns which side of the line segment the arrow(s) go on for supply routes\n     */\n    static SupplyRouteArrowSide(pt0: POINT2, pt1: POINT2): number;\n    static getScaledSize(originalSize: number, lineWidth: number, patternScale: number): number;\n}\n//# sourceMappingURL=line-generator.d.ts.map","milsym/generators/shape-builder.d.ts":"export declare class BasicShapes {\n    /**\n     * Anchor Points: This symbol requires at least two anchor points, points 1\n     * and 2, to define the line. Additional points can be defined to extend the\n     * line.\n     * <p>\n     * Size/Shape: The first and last anchor points determine the length of the\n     * line.\n     * <p>\n     * Orientation: Orientation is determined by the order in which the anchor points are entered.\n     * <p>\n     * Modifiers: T\n     *\n     * @see DrawRules.LINE1\n     */\n    static readonly LINE = 10000000;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area's size and shape.\n     * <p>\n     * Size/Shape: Determined by the anchor points. The information fields\n     * should be moveable and scalable as a block within the area.\n     * <p>\n     * Modifiers: T\n     *\n     * @see DrawRules.AREA1\n     */\n    static readonly AREA = 11000000;\n    /**\n     * Anchor Points: This symbol requires one anchor point. This anchor point\n     * represents the center of an ellipse and, therefore, the geographic\n     * location of that ellipse.\n     * <p>\n     * Size/Shape: The size and shape of this symbol is determined by three\n     * additional numeric values; A major axis radius, a minor axis radius, and\n     * a rotation angle. The radii should be expressed in the appropriate map\n     * distance units.\n     * <p>\n     * Orientation: The orientation of this symbol is determined by the rotation\n     * angle provided, where 0 degrees is east/west and a positive rotation\n     * angle rotates the ellipse in a counter-clockwise direction.\n     * <p>\n     * Modifiers: AM, AN, T\n     *\n     * @see DrawRules.ELLIPSE1\n     */\n    static readonly ELLIPSE = 13000001;\n    /**\n     * Anchor Points: This symbol requires one (1) anchor point and a radius.\n     * Point 1 defines the center point of the symbol.\n     * <p>\n     * Size/Shape: Size: The radius defines the size.\n     * <p>\n     * Orientation: Not applicable\n     * <p>\n     * Modifiers: AM, T\n     *\n     * @see DrawRules.CIRCULAR1\n     */\n    static readonly CIRCLE = 13000002;\n    /**\n     * Anchor Points: This symbol requires one (1) anchor point to define the\n     * center of the area.\n     * <p>\n     * Size/Shape: Size is determined by the anchor point, the length (in meters)\n     * and width (in meters).\n     * <p>\n     * Orientation: The orientation of this symbol is determined by the rotation\n     * angle provided, where 0 degrees is east/west and a positive rotation\n     * angle rotates the ellipse in a clockwise direction.\n     * <p>\n     * Modifiers: AM, AN, T\n     *\n     * @see DrawRules.RECTANGULAR2\n     */\n    static readonly RECTANGLE = 14000001;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The center point\n     * defines/is the center of the symbol.\n     * <p>\n     * Size/Shape: Line width defines the size of the point. The radius defines the size of the outline.\n     * <p>\n     * Orientation: Not applicable\n     * <p>\n     * Modifiers: AM, T\n     *\n     * @see DrawRules.POINT2\n     */\n    static readonly POINT = 15000002;\n}\n//# sourceMappingURL=shape-builder.d.ts.map","milsym/generators/shape2.d.ts":"import { Rectangle } from \"../graphics/Rectangle\";\nimport { POINT2 } from \"../types/point\";\nimport { ShapeInfo } from \"../renderer/utilities/ShapeInfo\";\n/**\n *\n *\n */\nexport declare class Shape2 extends ShapeInfo {\n    constructor(value: number);\n    style: number;\n    lineTo(pt: POINT2): void;\n    moveTo(pt: POINT2): void;\n    getBounds(): Rectangle;\n    getPoints(): Array<POINT2>;\n}\n//# sourceMappingURL=shape2.d.ts.map","milsym/graphics/AffineTransform.d.ts":"export declare class AffineTransform {\n    constructor();\n}\n//# sourceMappingURL=AffineTransform.d.ts.map","milsym/graphics/Area.d.ts":"import { GeneralPath } from \"./GeneralPath\";\nimport { Line2D } from \"./Line2D\";\nimport { Polygon } from \"./Polygon\";\nimport { Rectangle2D } from \"./Rectangle2D\";\nimport { Shape } from \"./Shape\";\nimport { POINT2 } from \"../types/point\";\n/**\n *\n *\n */\nexport declare class Area extends GeneralPath {\n    private static readonly _className;\n    constructor(arg: Polygon | Shape);\n    /**\n     * organizes intersect points by increasing distance from the hatch line origin\n     * @param hatchLine\n     * @param pts\n     */\n    private static reorderPointsByDistance;\n    protected getMBR(polygon: Array<POINT2>): Rectangle2D;\n    protected static isVertical(edge: Line2D): boolean;\n    private static adjustVerticalLine;\n    /**\n     *\n     * @param hatchLine the hatch line to intersect against the area points.\n     * the thatch line is assumed to start outside the area (polygon) MBR\n     * @return the GeneralPath which represents the intersection\n     */\n    private static getLineIntersectPoints;\n    /**\n     * this is functionality for GEUtils.buildHatchFillwhich calls hatchLineArea.intersect(shapeArea).\n     * so it assumes that this._pts is the hatch lines so it is hatchLines.intersect(shape) where\n     * shape is the polygon to be filled with hatch lines\n     * @param area\n     */\n    intersect(area: Area): void;\n}\n//# sourceMappingURL=Area.d.ts.map","milsym/graphics/BasicStroke.d.ts":"import { Polygon } from \"./Polygon\";\nimport { Shape } from \"./Shape\";\nimport { Stroke } from \"./Stroke\";\nimport { POINT2 } from \"../types/point\";\n/**\n *\n *\n */\nexport declare class BasicStroke implements Stroke {\n    /**\n     * Joins path segments by extending their outside edges until they meet.\n     */\n    static readonly JOIN_MITER: number;\n    /**\n     * Joins path segments by rounding off the corner at a radius of half the\n     * line width.\n     */\n    static readonly JOIN_ROUND: number;\n    /**\n     * Joins path segments by connecting the outer corners of their wide\n     * outlines with a straight segment.\n     */\n    static readonly JOIN_BEVEL: number;\n    /**\n     * Ends unclosed subpaths and dash segments with no added decoration.\n     */\n    static readonly CAP_BUTT: number;\n    /**\n     * Ends unclosed subpaths and dash segments with a round decoration that has\n     * a radius equal to half of the width of the pen.\n     */\n    static readonly CAP_ROUND: number;\n    /**\n     * Ends unclosed subpaths and dash segments with a square projection that\n     * extends beyond the end of the segment to a distance equal to half of the\n     * line width.\n     */\n    static readonly CAP_SQUARE: number;\n    protected width: number;\n    protected join: number;\n    protected cap: number;\n    protected miterlimit: number;\n    protected dash: number[] | null;\n    protected dash_phase: number;\n    /**\n     * Constructs a new <code>BasicStroke</code> with defaults for all\n     * attributes. The default attributes are a solid line of width 1.0,\n     * CAP_SQUARE, JOIN_MITER, a miter limit of 10.0.\n     */\n    constructor();\n    /**\n     * Constructs a solid <code>BasicStroke</code> with the specified line width\n     * and with default values for the cap and join styles.\n     *\n     * @param width\n     * the width of the <code>BasicStroke</code>\n     * @throws IllegalArgumentException\n     * if <code>width</code> is negative\n     */\n    constructor(width: number);\n    constructor(width: number, cap: number, join: number);\n    constructor(width: number, cap: number, join: number, miterlimit: number);\n    constructor(width: number, cap: number, join: number, miterlimit: number, dash: number[] | null, dash_phase: number);\n    /**\n     * Returns a <code>Shape</code> whose interior defines the stroked outline\n     * of a specified <code>Shape</code>.\n     *\n     * @param s\n     * the <code>Shape</code> boundary be stroked\n     * @return the <code>Shape</code> of the stroked outline.\n     */\n    createStrokedShape(poly: Polygon): Shape;\n    getLineWidth(): number;\n    /**\n     * Returns the end cap style.\n     *\n     * @return the end cap style of this <code>BasicStroke</code> as one of the\n     * static <code>int</code> values that define possible end cap\n     * styles.\n     */\n    getEndCap(): number;\n    getLineJoin(): number;\n    /**\n     * Returns the limit of miter joins.\n     *\n     * @return the limit of miter joins of the <code>BasicStroke</code>.\n     */\n    getMiterLimit(): number;\n    /**\n     * Returns the array representing the lengths of the dash segments.\n     * Alternate entries in the array represent the user space lengths of the\n     * opaque and transparent segments of the dashes. As the pen moves along the\n     * outline of the <code>Shape</code> to be stroked, the user space distance\n     * that the pen travels is accumulated. The distance value is used to index\n     * into the dash array. The pen is opaque when its current cumulative\n     * distance maps to an even element of the dash array and transparent\n     * otherwise.\n     *\n     * @return the dash array.\n     */\n    getDashArray(): number[] | null;\n    getDashPhase(): number;\n    /**\n     * Returns the hashcode for this stroke.\n     *\n     * @return a hash code for this stroke.\n     */\n    hashCode(): number;\n    static GetInteriorPoints(pLinePoints: POINT2[], vblCounter: number, lineType: number, dist: number): Array<POINT2>;\n}\n//# sourceMappingURL=BasicStroke.d.ts.map","milsym/graphics/BasicTypes.d.ts":"export type char = number;\nexport type byte = number;\nexport type short = number;\nexport type long = bigint;\n//# sourceMappingURL=BasicTypes.d.ts.map","milsym/graphics/BufferedImage.d.ts":"import { Graphics2D } from \"./Graphics2D\";\n/**\n *\n *\n */\nexport declare class BufferedImage {\n    static readonly TYPE_INT_ARGB: number;\n    constructor(width: number, height: number, imageType: number);\n    createGraphics(): Graphics2D;\n    flush(): void;\n    getWidth(): number;\n    getHeight(): number;\n}\n//# sourceMappingURL=BufferedImage.d.ts.map","milsym/graphics/Font.d.ts":"/**\n *\n *\n */\nexport declare class Font {\n    static readonly PLAIN: number;\n    static readonly BOLD: number;\n    static readonly ITALIC: number;\n    protected _size: number;\n    protected _text: string;\n    protected _type: number;\n    constructor(s: string, type: number, size: number);\n    getSize(): number;\n    getName(): string;\n    getType(): number;\n    isBold(): boolean;\n    getTypeString(): string;\n    static getTypeString(type: number): string;\n    static getTypeInt(type: string): number;\n    toString(): string;\n}\n//# sourceMappingURL=Font.d.ts.map","milsym/graphics/FontMetrics.d.ts":"import { Font } from \"./Font\";\nimport { FontRenderContext } from \"./FontRenderContext\";\n/**\n *\n *\n */\nexport declare class FontMetrics {\n    protected _fontRenderContext: FontRenderContext;\n    protected _font: Font;\n    constructor(font: Font);\n    stringWidth(str: string): number;\n    getFontRenderContext(): FontRenderContext;\n}\n//# sourceMappingURL=FontMetrics.d.ts.map","milsym/graphics/FontRenderContext.d.ts":"/**\n *\n *\n */\nexport declare class FontRenderContext {\n    constructor();\n}\n//# sourceMappingURL=FontRenderContext.d.ts.map","milsym/graphics/GeneralPath.d.ts":"import { AffineTransform } from \"./AffineTransform\";\nimport { PathIterator } from \"./PathIterator\";\nimport { Point2D } from \"./Point2D\";\nimport { Rectangle } from \"./Rectangle\";\nimport { Rectangle2D } from \"./Rectangle2D\";\nimport { Shape } from \"./Shape\";\nexport declare class GeneralPath implements Shape {\n    private _pathIterator;\n    constructor();\n    lineTo(x: number, y: number): void;\n    moveTo(x: number, y: number): void;\n    quadTo(x1: number, y1: number, x2: number, y2: number): void;\n    cubicTo(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number): void;\n    curveTo(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number): void;\n    computeBounds(rect: Rectangle2D): void;\n    closePath(): void;\n    contains(pt: Point2D): boolean;\n    contains(r: Rectangle2D): boolean;\n    contains(x: number, y: number): boolean;\n    contains(x: number, y: number, width: number, height: number): boolean;\n    getBounds2D(): Rectangle2D;\n    getBounds(): Rectangle;\n    /**\n     * called only when the GeneralPath is a rectangle\n     * @param rect\n     * @return\n     */\n    intersects(rect: Rectangle2D): boolean;\n    /**\n     * Only tests against the bounds, used only when the GeneralPath is a rectangle\n     * @param x\n     * @param y\n     * @param w\n     * @param h\n     * @return\n     */\n    intersects(x: number, y: number, w: number, h: number): boolean;\n    append(shape: Shape, connect: boolean): void;\n    getPathIterator(tx: AffineTransform | null): PathIterator;\n}\n//# sourceMappingURL=GeneralPath.d.ts.map","milsym/graphics/Graphics2D.d.ts":"import { AffineTransform } from \"./AffineTransform\";\nimport { BasicStroke } from \"./BasicStroke\";\nimport { Font } from \"./Font\";\nimport { FontMetrics } from \"./FontMetrics\";\nimport { FontRenderContext } from \"./FontRenderContext\";\nimport { Color } from \"../renderer/utilities/Color\";\n/**\n *\n *\n */\nexport declare class Graphics2D {\n    private _font;\n    private _fontMetrics;\n    private _fontRenderContext;\n    constructor();\n    setFont(value: Font): void;\n    getFont(): Font;\n    setFontMetrics(value: FontMetrics): void;\n    getFontMetrics(): FontMetrics;\n    setColor(color: Color): void;\n    setBackground(color: Color): void;\n    setTransform(id: AffineTransform): void;\n    getTransform(): null;\n    setStroke(stroke: BasicStroke): void;\n    drawLine(x1: number, y1: number, x2: number, y2: number): void;\n    dispose(): void;\n    rotate(theta: number, x: number, y: number): void;\n    clearRect(x: number, y: number, width: number, height: number): void;\n    drawString(s: string, x: number, y: number): void;\n    getFontRenderContext(): FontRenderContext;\n}\n//# sourceMappingURL=Graphics2D.d.ts.map","milsym/graphics/IPathIterator.d.ts":"export declare namespace IPathIterator {\n    const SEG_CLOSE: number;\n    const SEG_CUBICTO: number;\n    const SEG_LINETO: number;\n    const SEG_MOVETO: number;\n    const SEG_QUADTO: number;\n    const WIND_EVEN_ODD: number;\n    const WIND_NON_ZERO: number;\n}\n//# sourceMappingURL=IPathIterator.d.ts.map","milsym/graphics/Line2D.d.ts":"import { Point2D } from \"./Point2D\";\nimport { Rectangle2D } from \"./Rectangle2D\";\n/**\n * This <code>Line2D</code> represents a line segment in {@code (x,y)}\n * coordinate space. This class, like all of the Java 2D API, uses a default\n * coordinate system called <i>user space</i> in which the y-axis values\n * increase downward and x-axis values increase to the right. For more\n * information on the user space coordinate system, see the <a href=\n * \"http://java.sun.com/j2se/1.3/docs/guide/2d/spec/j2d-intro.fm2.html#61857\">\n * Coordinate Systems</a> section of the Java 2D Programmer's Guide.\n * <p>\n * This class is only the abstract superclass for all objects that store a 2D\n * line segment. The actual storage representation of the coordinates is left to\n * the subclass.\n *\n * @author Jim Graham\n * @since 1.2\n */\nexport declare class Line2D {\n    intersectsLine(edge: Line2D): boolean;\n    static linesIntersect(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number, x4: number, y4: number): boolean;\n    /**\n     * The X coordinate of the start point of the line segment.\n     *\n     * @since 1.2\n     * @serial\n     */\n    x1: number;\n    /**\n     * The Y coordinate of the start point of the line segment.\n     *\n     * @since 1.2\n     * @serial\n     */\n    y1: number;\n    /**\n     * The X coordinate of the end point of the line segment.\n     *\n     * @since 1.2\n     * @serial\n     */\n    x2: number;\n    /**\n     * The Y coordinate of the end point of the line segment.\n     *\n     * @since 1.2\n     * @serial\n     */\n    y2: number;\n    /**\n     * Constructs and initializes a Line with coordinates (0, 0) -&gt; (0, 0).\n     *\n     * @since 1.2\n     */\n    constructor();\n    /**\n     * Constructs and initializes a <code>Line2D</code> from the specified\n     * <code>Point2D</code> objects.\n     *\n     * @param p1\n     * the start <code>Point2D</code> of this line segment\n     * @param p2\n     * the end <code>Point2D</code> of this line segment\n     * @since 1.2\n     */\n    constructor(p1: Point2D, p2: Point2D);\n    /**\n     * Constructs and initializes a <code>Line2D</code> from the specified\n     * coordinates.\n     *\n     * @param x1\n     * the X coordinate of the start point\n     * @param y1\n     * the Y coordinate of the start point\n     * @param x2\n     * the X coordinate of the end point\n     * @param y2\n     * the Y coordinate of the end point\n     * @since 1.2\n     */\n    constructor(x1: number, y1: number, x2: number, y2: number);\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getX1(): number;\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getY1(): number;\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getP1(): Point2D;\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getX2(): number;\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getY2(): number;\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getP2(): Point2D;\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getBounds2D(): Rectangle2D;\n    private static readonly serialVersionUID;\n    /**\n     * Sets the location of the end points of this <code>Line2D</code> to the\n     * same as those end points of the specified <code>Line2D</code>.\n     *\n     * @param l\n     * the specified <code>Line2D</code>\n     * @since 1.2\n     */\n    setLine(l: Line2D): void;\n    /**\n     * Sets the location of the end points of this <code>Line2D</code> to the\n     * specified <code>Point2D</code> coordinates.\n     *\n     * @param p1\n     * the start <code>Point2D</code> of the line segment\n     * @param p2\n     * the end <code>Point2D</code> of the line segment\n     * @since 1.2\n     */\n    setLine(p1: Point2D, p2: Point2D): void;\n    /**\n     * Sets the location of the end points of this <code>Line2D</code> to the\n     * specified double coordinates.\n     *\n     * @param x1\n     * the X coordinate of the start point\n     * @param y1\n     * the Y coordinate of the start point\n     * @param x2\n     * the X coordinate of the end point\n     * @param y2\n     * the Y coordinate of the end point\n     * @since 1.2\n     */\n    setLine(x1: number, y1: number, x2: number, y2: number): void;\n    /**\n     * Returns an indicator of where the specified point {@code (px,py)} lies\n     * with respect to the line segment from {@code (x1,y1)} to {@code (x2,y2)}.\n     * The return value can be either 1, -1, or 0 and indicates in which\n     * direction the specified line must pivot around its first end point,\n     * {@code (x1,y1)}, in order to point at the specified point {@code (px,py)}\n     * .\n     * <p>\n     * A return value of 1 indicates that the line segment must turn in the\n     * direction that takes the positive X axis towards the negative Y axis. In\n     * the default coordinate system used by Java 2D, this direction is\n     * counterclockwise.\n     * <p>\n     * A return value of -1 indicates that the line segment must turn in the\n     * direction that takes the positive X axis towards the positive Y axis. In\n     * the default coordinate system, this direction is clockwise.\n     * <p>\n     * A return value of 0 indicates that the point lies exactly on the line\n     * segment. Note that an indicator value of 0 is rare and not useful for\n     * determining colinearity because of floating point rounding issues.\n     * <p>\n     * If the point is colinear with the line segment, but not between the end\n     * points, then the value will be -1 if the point lies \"beyond {@code\n     * (x1,y1)}\" or 1 if the point lies \"beyond {@code (x2,y2)}\".\n     *\n     * @param x1\n     * the X coordinate of the start point of the specified line\n     * segment\n     * @param y1\n     * the Y coordinate of the start point of the specified line\n     * segment\n     * @param x2\n     * the X coordinate of the end point of the specified line\n     * segment\n     * @param y2\n     * the Y coordinate of the end point of the specified line\n     * segment\n     * @param px\n     * the X coordinate of the specified point to be compared with\n     * the specified line segment\n     * @param py\n     * the Y coordinate of the specified point to be compared with\n     * the specified line segment\n     * @return an integer that indicates the position of the third specified\n     * coordinates with respect to the line segment formed by the first\n     * two specified coordinates.\n     * @since 1.2\n     */\n    static relativeCCW(x1: number, y1: number, x2: number, y2: number, px: number, py: number): number;\n    /**\n     * Returns an indicator of where the specified point {@code (px,py)} lies\n     * with respect to this line segment. See the method comments of\n     * {@link #relativeCCW(double, double, double, double, double, double)} to\n     * interpret the return value.\n     *\n     * @param px\n     * the X coordinate of the specified point to be compared with\n     * this <code>Line2D</code>\n     * @param py\n     * the Y coordinate of the specified point to be compared with\n     * this <code>Line2D</code>\n     * @return an integer that indicates the position of the specified\n     * coordinates with respect to this <code>Line2D</code>\n     * @see #relativeCCW(double, double, double, double, double, double)\n     * @since 1.2\n     */\n    relativeCCW(px: number, py: number): number;\n    /**\n     * Returns the square of the distance from a point to a line. The distance\n     * measured is the distance between the specified point and the closest\n     * point on the infinitely-extended line defined by the specified\n     * coordinates. If the specified point intersects the line, this method\n     * returns 0.0.\n     *\n     * @param x1\n     * the X coordinate of the start point of the specified line\n     * @param y1\n     * the Y coordinate of the start point of the specified line\n     * @param x2\n     * the X coordinate of the end point of the specified line\n     * @param y2\n     * the Y coordinate of the end point of the specified line\n     * @param px\n     * the X coordinate of the specified point being measured against\n     * the specified line\n     * @param py\n     * the Y coordinate of the specified point being measured against\n     * the specified line\n     * @return a double value that is the square of the distance from the\n     * specified point to the specified line.\n     * @since 1.2\n     */\n    static ptLineDistSq(x1: number, y1: number, x2: number, y2: number, px: number, py: number): number;\n    /**\n     * Returns the distance from a point to a line. The distance measured is the\n     * distance between the specified point and the closest point on the\n     * infinitely-extended line defined by the specified coordinates. If the\n     * specified point intersects the line, this method returns 0.0.\n     *\n     * @param x1\n     * the X coordinate of the start point of the specified line\n     * @param y1\n     * the Y coordinate of the start point of the specified line\n     * @param x2\n     * the X coordinate of the end point of the specified line\n     * @param y2\n     * the Y coordinate of the end point of the specified line\n     * @param px\n     * the X coordinate of the specified point being measured against\n     * the specified line\n     * @param py\n     * the Y coordinate of the specified point being measured against\n     * the specified line\n     * @return a double value that is the distance from the specified point to\n     * the specified line.\n     * @since 1.2\n     */\n    static ptLineDist(x1: number, y1: number, x2: number, y2: number, px: number, py: number): number;\n    /**\n     * Returns the square of the distance from a specified <code>Point2D</code>\n     * to this line. The distance measured is the distance between the specified\n     * point and the closest point on the infinitely-extended line defined by\n     * this <code>Line2D</code>. If the specified point intersects the line,\n     * this method returns 0.0.\n     *\n     * @param pt\n     * the specified <code>Point2D</code> being measured against this\n     * line\n     * @return a double value that is the square of the distance from a\n     * specified <code>Point2D</code> to the current line.\n     * @since 1.2\n     */\n    ptLineDistSq(pt: Point2D): number;\n    /**\n     * Returns the square of the distance from a point to this line. The\n     * distance measured is the distance between the specified point and the\n     * closest point on the infinitely-extended line defined by this\n     * <code>Line2D</code>. If the specified point intersects the line, this\n     * method returns 0.0.\n     *\n     * @param px\n     * the X coordinate of the specified point being measured against\n     * this line\n     * @param py\n     * the Y coordinate of the specified point being measured against\n     * this line\n     * @return a double value that is the square of the distance from a\n     * specified point to the current line.\n     * @since 1.2\n     */\n    ptLineDistSq(px: number, py: number): number;\n    /**\n     * Returns the distance from a <code>Point2D</code> to this line. The\n     * distance measured is the distance between the specified point and the\n     * closest point on the infinitely-extended line defined by this\n     * <code>Line2D</code>. If the specified point intersects the line, this\n     * method returns 0.0.\n     *\n     * @param pt\n     * the specified <code>Point2D</code> being measured\n     * @return a double value that is the distance from a specified\n     * <code>Point2D</code> to the current line.\n     * @since 1.2\n     */\n    ptLineDist(pt: Point2D): number;\n    /**\n     * Returns the distance from a point to this line. The distance measured is\n     * the distance between the specified point and the closest point on the\n     * infinitely-extended line defined by this <code>Line2D</code>. If the\n     * specified point intersects the line, this method returns 0.0.\n     *\n     * @param px\n     * the X coordinate of the specified point being measured against\n     * this line\n     * @param py\n     * the Y coordinate of the specified point being measured against\n     * this line\n     * @return a double value that is the distance from a specified point to the\n     * current line.\n     * @since 1.2\n     */\n    ptLineDist(px: number, py: number): number;\n    /**\n     * Tests if a given <code>Point2D</code> is inside the boundary of this\n     * <code>Line2D</code>. This method is required to implement the\n     * {@link Shape} interface, but in the case of <code>Line2D</code> objects\n     * it always returns <code>false</code> since a line contains no area.\n     *\n     * @param p\n     * the specified <code>Point2D</code> to be tested\n     * @return <code>false</code> because a <code>Line2D</code> contains no\n     * area.\n     * @since 1.2\n     */\n    contains(p: Point2D): boolean;\n    /**\n     * Tests if the interior of this <code>Line2D</code> entirely contains the\n     * specified <code>Rectangle2D</code>. This method is required to implement\n     * the <code>Shape</code> interface, but in the case of <code>Line2D</code>\n     * objects it always returns <code>false</code> since a line contains no\n     * area.\n     *\n     * @param r\n     * the specified <code>Rectangle2D</code> to be tested\n     * @return <code>false</code> because a <code>Line2D</code> contains no\n     * area.\n     * @since 1.2\n     */\n    contains(r: Rectangle2D): boolean;\n    /**\n     * Tests if a specified coordinate is inside the boundary of this\n     * <code>Line2D</code>. This method is required to implement the\n     * {@link Shape} interface, but in the case of <code>Line2D</code> objects\n     * it always returns <code>false</code> since a line contains no area.\n     *\n     * @param x\n     * the X coordinate of the specified point to be tested\n     * @param y\n     * the Y coordinate of the specified point to be tested\n     * @return <code>false</code> because a <code>Line2D</code> contains no\n     * area.\n     * @since 1.2\n     */\n    contains(x: number, y: number): boolean;\n    /**\n     * Tests if the interior of this <code>Line2D</code> entirely contains the\n     * specified set of rectangular coordinates. This method is required to\n     * implement the <code>Shape</code> interface, but in the case of\n     * <code>Line2D</code> objects it always returns false since a line contains\n     * no area.\n     *\n     * @param x\n     * the X coordinate of the upper-left corner of the specified\n     * rectangular area\n     * @param y\n     * the Y coordinate of the upper-left corner of the specified\n     * rectangular area\n     * @param w\n     * the width of the specified rectangular area\n     * @param h\n     * the height of the specified rectangular area\n     * @return <code>false</code> because a <code>Line2D</code> contains no\n     * area.\n     * @since 1.2\n     */\n    contains(x: number, y: number, w: number, h: number): boolean;\n    /**\n     * Creates a new object of the same class as this object.\n     *\n     * @return a clone of this instance.\n     * @exception OutOfMemoryError\n     * if there is not enough memory.\n     * @see java.lang.Cloneable\n     * @since 1.2\n     */\n    clone(): Line2D | null;\n}\n//# sourceMappingURL=Line2D.d.ts.map","milsym/graphics/PathIterator.d.ts":"import { AffineTransform } from \"./AffineTransform\";\nimport { Rectangle2D } from \"./Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\n/**\n *\n *\n */\nexport declare class PathIterator {\n    static readonly SEG_CLOSE: number;\n    static readonly SEG_CUBICTO: number;\n    static readonly SEG_LINETO: number;\n    static readonly SEG_MOVETO: number;\n    static readonly SEG_QUADTO: number;\n    static readonly WIND_EVEN_ODD: number;\n    static readonly WIND_NON_ZERO: number;\n    private _currentSeg;\n    private _pts;\n    constructor(tx: AffineTransform | null);\n    getPoints(): Array<POINT2>;\n    currentSegment(coords: number[]): number;\n    getWindingRule(): number;\n    isDone(): boolean;\n    next(): void;\n    reset(): void;\n    moveTo(x: number, y: number): void;\n    lineTo(x: number, y: number): void;\n    cubicTo(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number): void;\n    curveTo(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number): void;\n    quadTo(x1: number, y1: number, x2: number, y2: number): void;\n    getBounds(): Rectangle2D;\n    setPathIterator(pts: Array<POINT2>): void;\n}\n//# sourceMappingURL=PathIterator.d.ts.map","milsym/graphics/Point.d.ts":"import { Point2D } from \"./Point2D\";\n/**\n *\n *\n */\nexport declare class Point {\n    x: number;\n    y: number;\n    constructor();\n    constructor(x1: number, y1: number);\n    getX(): number;\n    getY(): number;\n    setLocation(x1: number, y1: number): void;\n    /**\n     * Returns a string representing one of the shape types\n     * from \"armyc2.c2sd.renderer.so.ShapeTypes\"\n     * @returns {String}\n     */\n    getShapeType(): string;\n    /**\n     * move x & y by specified amounts.\n     * @param {Number} x shift x point by this value\n     * @param {Number} y shift y point by this value\n     * @returns {void}\n     */\n    shift(x: number, y: number): void;\n    /**\n     * @returns {String} like \"{x:#,y:#}\"\n     */\n    toStringFormatted(): string;\n    /**\n     * Makes a copy of this point object.\n     * @returns {armyc2.c2sd.renderer.so.Point} Copy of original point.\n     */\n    clone(): Point;\n    toPoint2D(): Point2D;\n    /**\n     * @param {OffscreenCanvasRenderingContext2D} context object from html5 canvas\n     * @returns {void}\n     */\n    setPath(context: OffscreenCanvasRenderingContext2D): void;\n    /**\n     * @param {context} context object from html5 canvas\n     * @returns {void}\n     */\n    stroke(context: OffscreenCanvasRenderingContext2D): void;\n    /**\n     * @param {context} context object from html5 canvas\n     * @returns {void}\n     */\n    fill(context: OffscreenCanvasRenderingContext2D): void;\n}\n//# sourceMappingURL=Point.d.ts.map","milsym/graphics/Point2D.d.ts":"import { POINT2 } from \"../types/point\";\n/**\n * The <code>Point2D</code> class defines a point representing a location in\n *\n * <p>\n * This class is only the abstract superclass for all objects that store a 2D\n * coordinate. The actual storage representation of the coordinates is left to\n * the subclass.\n *\n * @author Jim Graham\n * @since 1.2\n */\nexport declare class Point2D {\n    /**\n     * The X coordinate of this <code>Point2D</code>.\n     *\n     * @since 1.2\n     */\n    x: number;\n    /**\n     * The Y coordinate of this <code>Point2D</code>.\n     *\n     * @since 1.2\n     */\n    y: number;\n    /**\n     * Constructs and initializes a <code>Point2D</code> with coordinates\n     * (0,&nbsp;0).\n     *\n     * @since 1.2\n     */\n    constructor();\n    /**\n     * add the constructor\n     * @param pt\n     */\n    constructor(pt: POINT2);\n    /**\n     * Constructs and initializes a <code>Point2D</code> with the specified\n     * coordinates.\n     *\n     * @param x\n     * the X coordinate of the newly constructed\n     * <code>Point2D</code>\n     * @param y\n     * the Y coordinate of the newly constructed\n     * <code>Point2D</code>\n     * @since 1.2\n     */\n    constructor(x: number, y: number);\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getX(): number;\n    /**\n     * {@inheritDoc}\n     *\n     * @since 1.2\n     */\n    getY(): number;\n    /**\n     * Returns a <code>String</code> that represents the value of this\n     * <code>Point2D</code>.\n     *\n     * @return a string representation of this <code>Point2D</code>.\n     * @since 1.2\n     */\n    toString(): string;\n    clone(): Point2D;\n    private static readonly serialVersionUID;\n    /**\n     * Sets the location of this <code>Point2D</code> to the same coordinates as\n     * the specified <code>Point2D</code> object.\n     *\n     * @param p\n     * the specified <code>Point2D</code> to which to set this\n     * <code>Point2D</code>\n     * @since 1.2\n     */\n    setLocation(p: Point2D): void;\n    /**\n     * Sets the location of this <code>Point2D</code> to the specified\n     * <code>double</code> coordinates.\n     *\n     * @param x\n     * the new X coordinate of this {Point2D}\n     * @param y\n     * the new Y coordinate of this {Point2D}\n     * @since 1.2\n     */\n    setLocation(x: number, y: number): void;\n    /**\n     * Returns the square of the distance between two points.\n     *\n     * @param x1\n     * the X coordinate of the first specified point\n     * @param y1\n     * the Y coordinate of the first specified point\n     * @param x2\n     * the X coordinate of the second specified point\n     * @param y2\n     * the Y coordinate of the second specified point\n     * @return the square of the distance between the two sets of specified\n     * coordinates.\n     * @since 1.2\n     */\n    static distanceSq(x1: number, y1: number, x2: number, y2: number): number;\n    /**\n     * Returns the distance between two points.\n     *\n     * @param x1\n     * the X coordinate of the first specified point\n     * @param y1\n     * the Y coordinate of the first specified point\n     * @param x2\n     * the X coordinate of the second specified point\n     * @param y2\n     * the Y coordinate of the second specified point\n     * @return the distance between the two sets of specified coordinates.\n     * @since 1.2\n     */\n    static distance(x1: number, y1: number, x2: number, y2: number): number;\n    /**\n     * Returns the distance from this <code>Point2D</code> to\n     * a specified point.\n     *\n     * @param px the X coordinate of the specified point to be measured\n     *           against this <code>Point2D</code>\n     * @param py the Y coordinate of the specified point to be measured\n     *           against this <code>Point2D</code>\n     * @return the distance between this <code>Point2D</code>\n     * and a specified point.\n     * @since 1.2\n     */\n    distance(px: number, py: number): number;\n    /**\n     * Determines whether or not two points are equal. Two instances of\n     * <code>Point2D</code> are equal if the values of their <code>x</code> and\n     * <code>y</code> member fields, representing their position in the\n     * coordinate space, are the same.\n     *\n     * @param obj\n     * an object to be compared with this <code>Point2D</code>\n     * @return <code>true</code> if the object to be compared is an instance of\n     * <code>Point2D</code> and has the same values; <code>false</code>\n     * otherwise.\n     * @since 1.2\n     */\n    equals(obj: Point2D): boolean;\n}\n//# sourceMappingURL=Point2D.d.ts.map","milsym/graphics/Polygon.d.ts":"/**\n * @author Denis M. Kishenko\n * @version $Revision$\n */\nimport { AffineTransform } from \"./AffineTransform\";\nimport { PathIterator } from \"./PathIterator\";\nimport { Point } from \"./Point\";\nimport { Point2D } from \"./Point2D\";\nimport { Rectangle } from \"./Rectangle\";\nimport { Rectangle2D } from \"./Rectangle2D\";\n/**\n *\n *\n */\nexport declare class Polygon {\n    /**\n     * The points buffer capacity\n     */\n    private static BUFFER_CAPACITY;\n    npoints: number;\n    xpoints: number[];\n    ypoints: number[];\n    protected bounds: Rectangle;\n    constructor();\n    /**\n     *\n     * @param xpoints\n     * @param ypoints\n     * @param npoints\n     */\n    constructor(xpoints: number[], ypoints: number[], npoints: number);\n    private copyOf;\n    reset(): void;\n    invalidate(): void;\n    addPoint(px: number, py: number): void;\n    getBounds(): Rectangle;\n    getBoundingBox(): Rectangle;\n    contains(p: Point | Point2D): boolean;\n    contains(r: Rectangle2D): boolean;\n    contains(x: number, y: number): boolean;\n    contains(x: number, y: number, w: number, h: number): boolean;\n    getBounds2D(): null;\n    intersects(r: Rectangle2D): boolean;\n    intersects(x: number, y: number, w: number, h: number): boolean;\n    getPathIterator(at: AffineTransform | null): PathIterator;\n    getPathIterator(at: AffineTransform, flatness: number): PathIterator;\n}\n//# sourceMappingURL=Polygon.d.ts.map","milsym/graphics/Rectangle.d.ts":"import { AffineTransform } from \"./AffineTransform\";\nimport { Point2D } from \"./Point2D\";\nimport { Rectangle2D } from \"./Rectangle2D\";\nimport { Shape } from \"./Shape\";\nimport { Point } from \"./Point\";\n/**\n *\n *\n */\nexport declare class Rectangle implements Shape {\n    static readonly OUT_LEFT = 1;\n    static readonly OUT_TOP = 2;\n    static readonly OUT_RIGHT = 4;\n    static readonly OUT_BOTTOM = 8;\n    x: number;\n    y: number;\n    width: number;\n    height: number;\n    constructor();\n    constructor(x1: number, y1: number, width1: number, height1: number);\n    getShapeType(): string;\n    getBounds(): Rectangle;\n    getPathIterator(at: AffineTransform): null;\n    intersects(rect: Rectangle2D): boolean;\n    intersects(x1: number, y1: number, width1: number, height1: number): boolean;\n    contains(pt: Point2D): boolean;\n    contains(x1: number, y1: number): boolean;\n    contains(x1: number, y1: number, width1: number, height1: number): boolean;\n    getBounds2D(): Rectangle2D;\n    getX(): number;\n    getY(): number;\n    getMinX(): number;\n    getMinY(): number;\n    getMaxX(): number;\n    getMaxY(): number;\n    getHeight(): number;\n    getWidth(): number;\n    getBottom(): number;\n    getRight(): number;\n    getCenterX(): number;\n    /**\n     *\n     * @returns {Number}\n     */\n    getCenterY(): number;\n    grow(h: number, v: number): void;\n    setRect(rect: Rectangle): void;\n    /**\n * setLocation x,y (top,left) while maintaining the width and height.\n * @param x\n * @param y\n */\n    setLocation(x: number, y: number): void;\n    isEmpty(): boolean;\n    shift(x: number, y: number): void;\n    /**\n     * moves top,left points leaving bottom,right intact.\n     * adjusts the height & width values as necessary\n     * @param x\n     * @param y\n     */\n    shiftTL(x: number, y: number): void;\n    /**\n     * moves bottom,right points leaving top,left intact.\n     * adjusts the height & width values as necessary\n     * @param {type} x the amount to move the right point by\n     * @param {type} y the amount to move the bottom point by\n     * @returns {_L7.Anonym$0.Rectangle.shiftTL}\n     */\n    shiftBR(x: number, y: number): void;\n    /**\n * Will merge the bounds of two rectangle.\n * @param rect\n */\n    union(rect: Rectangle): void;\n    unionPoint(point: Point): void;\n    /**\n*\n* @param point\n* @returns\n*/\n    containsPoint(point: Point): boolean;\n    containsRectangle(rect: Rectangle): boolean;\n    /**\n * Ported from Java\n */\n    private outcode;\n    /**\n    * Tests if the specified line segment intersects the interior of this\n    * <code>Rectangle</code>. Ported from java.\n    *\n    * @param x1 the X coordinate of the start point of the specified\n    *           line segment\n    * @param y1 the Y coordinate of the start point of the specified\n    *           line segment\n    * @param x2 the X coordinate of the end point of the specified\n    *           line segment\n    * @param y2 the Y coordinate of the end point of the specified\n    *           line segment\n    * @return <code>true</code> if the specified line segment intersects\n    * the interior of this <code>Rectangle</code>; <code>false</code>\n    * otherwise.\n    */\n    intersectsLine(x1: number, y1: number, x2: number, y2: number): boolean;\n    setPath(context: OffscreenCanvasRenderingContext2D): void;\n    stroke(context: OffscreenCanvasRenderingContext2D): void;\n    fill(context: OffscreenCanvasRenderingContext2D): void;\n    clone(): Rectangle;\n    /**\n     * ported from java\n     */\n    /**\n     *\n     * @param stroke named color or value of rgb(#,#,#)\n     * @param strokeWidth width of line in # of pixels\n     * @param fill named color or value of rgb(#,#,#)\n     * @returns\n     */\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null): string;\n}\n//# sourceMappingURL=Rectangle.d.ts.map","milsym/graphics/Rectangle2D.d.ts":"import { Line2D } from \"./Line2D\";\nimport { Point2D } from \"./Point2D\";\n/**\n *\n *\n */\nexport declare class Rectangle2D {\n    x: number;\n    y: number;\n    width: number;\n    height: number;\n    add(newx: number, newy: number): void;\n    createIntersection(r: Rectangle2D): Rectangle2D | null;\n    createUnion(r: Rectangle2D): Rectangle2D;\n    getX(): number;\n    getY(): number;\n    getCenterX(): number;\n    getCenterY(): number;\n    getMinX(): number;\n    getMinY(): number;\n    getMaxX(): number;\n    getMaxY(): number;\n    getHeight(): number;\n    getWidth(): number;\n    contains(rect: Rectangle2D): boolean;\n    contains(pt: Point2D): boolean;\n    contains(x1: number, y1: number): boolean;\n    contains(x: number, y: number, width: number, height: number): boolean;\n    intersects(rect: Rectangle2D): boolean;\n    intersects(x1: number, y1: number, width1: number, height1: number): boolean;\n    intersectsLine(line: Line2D): boolean;\n    isEmpty(): boolean;\n    setRect(r: Rectangle2D): void;\n    setRect(x1: number, y1: number, width1: number, height1: number): void;\n    grow(size: number): void;\n    grow(h: number, v: number): void;\n    stroke(context: OffscreenCanvasRenderingContext2D): void;\n    fill(context: OffscreenCanvasRenderingContext2D): void;\n    clone(): Rectangle2D;\n    /**\n     * Will merge the bounds of two rectangle.\n     * @param rect\n     */\n    union(rect: Rectangle2D): void;\n    constructor();\n    constructor(x1: number, y1: number, width1: number, height1: number);\n    /**\n *\n * @param stroke named color or hex color\n * @param strokeWidth width of line in # of pixels\n * @param fill named color or hex color\n * @returns\n */\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null): string;\n}\n//# sourceMappingURL=Rectangle2D.d.ts.map","milsym/graphics/Shape.d.ts":"import { AffineTransform } from \"./AffineTransform\";\nimport { PathIterator } from \"./PathIterator\";\nimport { Point2D } from \"./Point2D\";\nimport { Rectangle } from \"./Rectangle\";\nimport { Rectangle2D } from \"./Rectangle2D\";\n/**\n *\n *\n */\nexport interface Shape {\n    contains(x: number, y: number): boolean;\n    contains(x: number, y: number, width: number, height: number): boolean;\n    contains(pt: Point2D): boolean;\n    getBounds2D(): Rectangle2D;\n    getBounds(): Rectangle;\n    intersects(x: number, y: number, w: number, h: number): boolean;\n    intersects(rect: Rectangle2D): boolean;\n    getPathIterator(at: AffineTransform | null): PathIterator;\n}\n//# sourceMappingURL=Shape.d.ts.map","milsym/graphics/Stroke.d.ts":"import { Shape } from \"./Shape\";\n/**\n *\n *\n */\nexport interface Stroke {\n    createStrokedShape(s: Shape): Shape;\n}\n//# sourceMappingURL=Stroke.d.ts.map","milsym/graphics/TextLayout.d.ts":"import { AffineTransform } from \"./AffineTransform\";\nimport { Font } from \"./Font\";\nimport { FontRenderContext } from \"./FontRenderContext\";\nimport { Rectangle } from \"./Rectangle\";\nimport { Shape } from \"./Shape\";\n/**\n *\n *\n */\nexport declare class TextLayout {\n    protected _font: Font;\n    protected _str: string;\n    constructor(s: string, font: Font, frc: FontRenderContext);\n    getOutline(tx: AffineTransform | null): Shape;\n    getPixelBounds(frc: FontRenderContext, x: number, y: number): null;\n    getBounds(): Rectangle;\n}\n//# sourceMappingURL=TextLayout.d.ts.map","milsym/graphics/TexturePaint.d.ts":"import { Rectangle2D } from \"./Rectangle2D\";\n/**\n *\n *\n * @deprecated\n */\nexport declare class TexturePaint {\n    private _rect;\n    private _g2d;\n    private _bi;\n    constructor(bi: ImageBitmap, rect: Rectangle2D | null);\n}\n//# sourceMappingURL=TexturePaint.d.ts.map","milsym/index.d.ts":"export { WebRenderer } from './web/WebRenderer';\nexport { MilStdIconRenderer } from './renderer/MilStdIconRenderer';\nexport { SinglePointSVGRenderer, singlePointSVGRenderer } from './renderer/SinglePointSVGRenderer';\nexport { RendererSettings, rendererSettings } from './renderer/utilities/RendererSettings';\nexport { MilStdAttributes } from './renderer/utilities/MilStdAttributes';\nexport { Modifiers } from './renderer/utilities/Modifiers';\nexport { SymbolID } from './renderer/utilities/SymbolID';\nexport { SymbolUtilities } from './renderer/utilities/SymbolUtilities';\nexport { MSLookup, msLookup } from './renderer/utilities/MSLookup';\nexport { SVGLookup, svgLookup } from './renderer/utilities/SVGLookup';\nexport { MilStdSymbol } from './renderer/utilities/MilStdSymbol';\nexport { ShapeInfo } from './renderer/utilities/ShapeInfo';\nexport { Color } from './renderer/utilities/Color';\nexport { ErrorLogger } from './renderer/utilities/ErrorLogger';\nexport { MultiPointHandler } from './web/MultiPointHandler';\nexport { MultiPointHandlerSVG } from './web/MultiPointHandlerSVG';\nexport { Point2D } from './graphics/Point2D';\nexport { Rectangle2D } from './graphics/Rectangle2D';\nexport { POINT2 } from './types/point';\nexport type { IPointConversion } from './renderer/utilities/IPointConversion';\n//# sourceMappingURL=index.d.ts.map","milsym/math/clip-quad.d.ts":"import { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { Point2D } from \"../graphics/Point2D\";\n/**\n * Class to clip polygons\n *\n */\nexport declare class ClipQuad {\n    private static readonly _className;\n    /**\n     * Use the new version which takes an array for polygon clip bounds instead of rectangle\n     * @param polygon\n     * @param clipBounds\n     * @return\n     */\n    private static AddBoundaryPointsForLines;\n    private static calcTrueIntersectAbs;\n    /**\n     * Gets theoretical intersection of an edge with the line connecting previous and current points.\n     * @param previous\n     * @param current\n     * @param currentEdge the current edge of the clip area, assumed to not be vertical\n     * @return\n     */\n    private static intersectPoint2;\n    /**\n     * clips array of pts against a side of the clip bounds polygon\n     * assumes clipBounds has no vertical or horizontal segments\n     * @param pts array of points to clip against the clip bounds\n     * @param index starting index of clipBounds for the side to clip against\n     * @param clipBounds a quadrilateral or a polygon array that is the clipping area\n     * @return the clipped array of points\n     */\n    private static clipSide;\n    /**\n     * for pre-clipped lines which also require fill but need the processed points\n     * to create the fill. This function is called after the clip, so the fill\n     * does not get clipped.\n     * @param tg\n     * @param shapes\n     */\n    protected static addAbatisFill(tg: TacticalGraphic, shapes: Array<Shape2>): void;\n    /**\n     * for lines with glyphs the fill must be handled (clipped) as a separate shape.\n     * this function needs to be called before the clipping is done to the line\n     * @param tg\n     * @param clipBounds\n     * @return\n     */\n    static LinesWithFill(tg: TacticalGraphic, clipBounds: Array<Point2D>): Array<Shape2> | null;\n    /**\n     * closes an area\n     * @param tg\n     */\n    private static closeAreaTG;\n    /**\n     * DMA, DMAF fill must be handled separately because of the feint\n     * @param tg\n     * @param clipBounds\n     * @return\n     */\n    protected static fillDMA(tg: TacticalGraphic, clipBounds: Array<Point2D>): Array<Shape2>;\n    /**\n     *\n     * @param tg\n     * @param clipBounds polygon representing clipping area\n     * @return\n     */\n    static ClipPolygon(tg: TacticalGraphic, clipBounds: Array<Point2D>): Array<Point2D>;\n}\n//# sourceMappingURL=clip-quad.d.ts.map","milsym/math/clip.d.ts":"import { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\n/**\n * A class to clip tactical lines and areas\n *\n */\nexport declare class ClipPolygon {\n    private static readonly _className;\n    /**\n     * Calculate the point the line intersects an edge of the clipbounds\n     * @param pt0 start point of the line\n     * @param pt1 end point of the line\n     * @param currentEdge\n     * @return\n     */\n    private static intersectPoint;\n    /**\n     * clip the top\n     * on the line is considered inside\n     * @param pts\n     * @param clipBounds\n     * @return\n     */\n    private static clipTop;\n    /**\n     * on the boundary is considered inside\n     * clip the bottom\n     * @param pts\n     * @param clipBounds\n     * @return\n     */\n    private static clipBottom;\n    /**\n     * on the bounds is considered inside\n     * clip the right side\n     * @param pts\n     * @param clipBounds\n     * @return\n     */\n    private static clipRight;\n    /**\n     * on the line is considered inside\n     * clip the left side\n     * @param pts\n     * @param clipBounds\n     * @return\n     */\n    private static clipLeft;\n    /**\n     * for non-areas add points to the ends as necessary to make the algorithm work\n     * @param polygon\n     * @param clipBounds\n     */\n    private static AddBoundaryPointsForLines;\n    /**\n     * closes an area\n     * @param tg\n     */\n    private static closeAreaTG;\n    /**\n     * DMA, DMAF fill must be handled separately because of the feint\n     * @param tg\n     * @param clipBounds\n     * @return\n     */\n    protected static fillDMA(tg: TacticalGraphic, clipBounds: Rectangle2D): Array<Shape2>;\n    /**\n     * for pre-clipped lines which also require fill but need the processed points\n     * to create the fill. This functioni is called after the clip, so the fill\n     * does not get clipped.\n     * @param tg\n     * @param shapes\n     */\n    static addAbatisFill(tg: TacticalGraphic, shapes: Array<Shape2>): void;\n    /**\n     * for lines with glyphs the fill must be handled (clipped) as a separate shape.\n     * this function needs to be called before the clipping is done to the line\n     * @param tg\n     * @param clipBounds\n     */\n    static LinesWithFill(tg: TacticalGraphic, clipBounds: Rectangle2D | null): Array<Shape2> | null;\n    /**\n     * @deprecated\n     * for polygon completely outside the clip area\n     * pass back a small box to be able to continue normal processing\n     * @param clipBounds\n     * @return\n     */\n    private static buildBox;\n    /**\n     * Works for tactical lines and areas\n     * @param tg\n     * @param clipBounds\n     * @return\n     */\n    static ClipPolygon(tg: TacticalGraphic, clipBounds: Rectangle2D): Array<Point2D>;\n}\n//# sourceMappingURL=clip.d.ts.map","milsym/math/geodesic.d.ts":"import { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\n/**\n * Class to calculate the geodesic based shapes for the Fire Support Areas\n *\n */\nexport declare class Geodesic {\n    private static readonly _className;\n    private static readonly sm_a;\n    private static DegToRad;\n    private static RadToDeg;\n    /**\n     * Returns the azimuth from true north between two points\n     * @param c1\n     * @param c2\n     * @return the azimuth from c1 to c2\n     */\n    static GetAzimuth(c1: POINT2, c2: POINT2): number;\n    /**\n     * Calculates the distance in meters between two geodesic points.\n     * Also calculates the azimuth from c1 to c2 and from c2 to c1.\n     *\n     * @param c1 the first point\n     * @param c2 the last point\n     * @param a12 OUT - an object with a member to hold the calculated azimuth in degrees from c1 to c2\n     * @param a21 OUT - an object with a member to hold the calculated azimuth in degrees from c2 to c1\n     * @return the distance in meters between c1 and c2\n     */\n    static geodesic_distance(c1: POINT2, c2: POINT2): {\n        distance: number;\n        a12: number;\n        a21: number;\n    };\n    /**\n     * Calculates a geodesic point and given distance and azimuth from the srating geodesic point\n     *\n     * @param start the starting point\n     * @param distance the distance in meters\n     * @param azimuth the azimuth or bearing in degrees\n     *\n     * @return the calculated point\n     */\n    static geodesic_coordinate(start: POINT2, distance: number, azimuth: number): POINT2;\n    /**\n     * Calculates an arc from geodesic point and uses them for the change 1 circular symbols\n     *\n     * @param pPoints array of 3 points, currently the last 2 points are the same. The first point\n     * is the center and the next point defines the radius.\n     *\n     * @return points for the geodesic circle\n     */\n    static GetGeodesicArc(pPoints: POINT2[]): Array<POINT2> | null;\n    /**\n     * Calculates the sector points for a sector range fan.\n     *\n     * @param pPoints array of 3 points. The first point\n     * is the center and the next two points define either side of the sector\n     * @param pPoints2 OUT - the calculated geodesic sector points\n     *\n     * @return true if the sector is a circle\n     */\n    static GetGeodesicArc2(pPoints: Array<POINT2>, pPoints2: Array<POINT2>): boolean;\n    /**\n     * returns intersection of two lines, each defined by a point and a bearing\n     * <a href=\"http://creativecommons.org/licenses/by/3.0/\"><img alt=\"Creative Commons License\" style=\"border-width:0\" src=\"http://i.creativecommons.org/l/by/3.0/88x31.png\"></a><br>This work is licensed under a <a href=\"http://creativecommons.org/licenses/by/3.0/\">Creative Commons Attribution 3.0 Unported License</a>.\n     * @param p1 1st point\n     * @param brng1 first line bearing in degrees from true north\n     * @param p2 2nd point\n     * @param brng2 2nd point bearing in degrees from true north\n     * @return\n     * @deprecated\n     */\n    static IntersectLines(p1: POINT2, brng1: number, p2: POINT2, brng2: number): POINT2 | null;\n    /**\n     * Normalizes geo points for arrays which span the IDL\n     *\n     * @param geoPoints\n     * @return\n     */\n    static normalize_points(geoPoints: Array<POINT2>): Array<POINT2>;\n    /**\n     * calculates the geodesic MBR, intended for regular shaped areas\n     *\n     * @param geoPoints\n     * @return\n     */\n    static geodesic_mbr(geoPoints: Array<POINT2>): Rectangle2D | null;\n    /**\n     * Currently used by AddModifiers for greater accuracy on center labels\n     *\n     * @param geoPoints\n     * @return\n     */\n    static geodesic_center(geoPoints: Array<POINT2>): POINT2 | null;\n    /**\n     * rotates a point from a center point in degrees\n     * @param ptCenter center point to rotate about\n     * @param ptRotate point to rotate\n     * @param rotation rotation angle in degrees\n     * @return\n     */\n    private static geoRotatePoint;\n    /**\n     * Calculates points for a geodesic ellipse and rotates the points by rotation\n     * @param ptCenter\n     * @param majorRadius\n     * @param minorRadius\n     * @param rotation  rotation angle in degrees\n     * @return\n     */\n    static getGeoEllipse(ptCenter: POINT2, majorRadius: number, minorRadius: number, rotation: number): POINT2[];\n}\n//# sourceMappingURL=geodesic.d.ts.map","milsym/math/line-ops.d.ts":"import { Point } from \"../graphics/Point\";\nimport { Point2D } from \"../graphics/Point2D\";\nimport { Shape } from \"../graphics/Shape\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n * A class to provide the utility functions required for calculating the line\n * points.\n *\n *\n */\nexport declare class LineUtility {\n    private static readonly _className;\n    static readonly extend_left: number;\n    static readonly extend_right: number;\n    static readonly extend_above: number;\n    static readonly extend_below: number;\n    /**\n     * Resizes the array to the length speicifed, called by the Channels class.\n     *\n     * @param pLinePoints the array to resize\n     * @param length the length to which to resize the array.\n     * @return the resized array\n     */\n    static ResizeArray(pLinePoints: POINT2[], length: number): POINT2[];\n    /**\n     * post-segments a line segment into 50 pixel intervals\n     *\n     * @param pt0\n     * @param pt1\n     * @param shape\n     */\n    protected static SegmentLineShape(pt0: POINT2, pt1: POINT2, shape: Shape2): void;\n    /**\n     * Calculates the middle segment for the Direction of Attack Aviation symbol\n     *\n     * @param pLinePoints the point array\n     * @param vblSaveCounter the size of the point array\n     * @return the middle segment\n     */\n    static GetDirAtkAirMiddleSegment(pLinePoints: POINT2[], vblSaveCounter: number): number;\n    /**\n     * Computes the angle in radians between two points\n     *\n     * @param pt0 the first point\n     * @param pt1 the last point\n     *\n     * @return the angle in radians\n     */\n    static calcSegmentAngle(pt0: POINT2, pt1: POINT2): number;\n    /**\n     * POINT2 in previous applications has been a struct that did not require\n     * initialization.\n     *\n     * @param pts array of points to instantiate.\n     */\n    static InitializePOINT2Array(pts: POINT2[]): void;\n    /**\n     * Calculates the center point of an area using the first vblCounter points\n     * in the array.\n     *\n     * @param pLinePoints the client points\n     * @param vblCounter the number of points in the array to use\n     *\n     * @return the center point\n     */\n    static CalcCenterPointDouble(pLinePoints: POINT2[], vblCounter: number): POINT2;\n    /**\n     * Called by renderer Modifier2 class after ArrayList.ToArray was called,\n     * which produces an array of objects.\n     *\n     * @param pLinePoints\n     * @param vblCounter\n     * @return\n     */\n    static CalcCenterPointDouble2(pLinePoints: POINT2[], vblCounter: number): POINT2;\n    /**\n     * Calculates the distance in pixels between two points\n     *\n     * @param p1 the first point\n     * @param p2 the last point\n     *\n     * @return the distance between p1 and p2 in pixels\n     */\n    static calcDistance(p1: POINT2 | Point2D, p2: POINT2 | Point2D): number;\n    /**\n     * Computes the slope of a line\n     *\n     * @param firstLinePoint the first line point\n     * @param lastLinePoint the last line point\n     * @param slope OUT - object with member to hold the slope of the line\n     *\n     * @return 1 if successful, else return 0\n     */\n    static calcTrueSlope(firstLinePoint: POINT2, lastLinePoint: POINT2): {\n        result: number;\n        slope: number;\n    };\n    /**\n     * reverses the first vblCounter points\n     *\n     * @param pLowerLinePoints OUT - points to reverse\n     * @param vblCounter\n     */\n    static reversePoints(pLowerLinePoints: POINT2[], vblCounter: number): void;\n    static calcTrueSlopeForRoutes(firstLinePoint: POINT2, lastLinePoint: POINT2): {\n        result: boolean;\n        slope: number;\n    };\n    /**\n     * Computes the slope of a line\n     *\n     * @param firstLinePoint the first line point\n     * @param lastLinePoint the last line point\n     * @param slope OUT - object with member to hold the slope of the line\n     *\n     * @return true if successful\n     */\n    static calcTrueSlope2(firstLinePoint: POINT2, lastLinePoint: POINT2): {\n        result: boolean;\n        slope: number;\n    };\n    /**\n     * Calculates the slopes and y intercepts in pixels for the line from pt1 to\n     * pt2 and a parallel line a vertical distance from the line\n     *\n     * @param nDistance the distance in pixels\n     * @param linePoint1 first point on the line\n     * @param linePoint2 last point on the line\n     * @param pdResult OUT - array to hold m, b for both lines\n     *\n     * @return 1 if the lines are not vertical, else return 0\n     */\n    static calcTrueLines(nDistance: number, linePoint1: POINT2, linePoint2: POINT2): {\n        result: number;\n        values: number[];\n    };\n    /**\n     * Calculates the intersection of two lines.\n     *\n     * @param m1 slope of first line\n     * @param b1 Y intercept of first line\n     * @param m2 slope of second line\n     * @param b2 Y intercept of second line\n     * @param bolVertical1 0 if first line is vertical, else 1\n     * @param bolVertical2 0 if second line is vertical, else 1\n     * @param X1 X intercept if first line is vertical\n     * @param X2 X intercept if 2nd line is vertical.\n     *\n     * @return intersection point\n     */\n    static calcTrueIntersect(m1: number, b1: number, m2: number, b2: number, bolVertical1: number, bolVertical2: number, X1: number, //x intercept if line1 is vertical\n    X2: number): POINT2;\n    /**\n     * Calculates an offset point for channel types which require arrows.\n     *\n     * @param startLinePoint the first point\n     * @param endLinePoint the last point\n     * @param nOffset the offset in pixels\n     *\n     * @return the offset point\n     */\n    static GetOffsetPointDouble(startLinePoint: POINT2, endLinePoint: POINT2, nOffset: number): POINT2;\n    /**\n     * Used for DMAF\n     *\n     * @param pLinePoints the client points\n     * @return ArrayList of X points\n     */\n    static LineOfXPoints(tg: TacticalGraphic, pLinePoints: POINT2[]): Array<POINT2>;\n    /**\n     * Computes the distance in pixels of pt3 to the line from pt1 to pt2.\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param pt3 point distance to compute\n     * @return distance to pt3\n     */\n    static calcDistanceToLine(pt1: POINT2, pt2: POINT2, pt3: POINT2): number;\n    /**\n     * Calculates a point along a line. Returns the past point if the distance\n     * is 0.\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param dist extension distance in pixels from the beginning of the line\n     *\n     * @return the extension point\n     */\n    static ExtendLineDouble(pt1: POINT2, pt2: POINT2, dist: number): POINT2;\n    /**\n     * Extends a point along a line. If dist is 0 returns last point.\n     *\n     * @param pt1 first point on the line\n     * @param pt2 last point on the line\n     * @param dist the distance in pixels from pt1\n     *\n     * @return the extended point\n     */\n    static extendAlongLine(pt1: POINT2, pt2: POINT2, dist: number): POINT2;\n    static extendAlongLine(pt1: POINT2, pt2: POINT2, dist: number, styl: number): POINT2;\n    static extendAlongLine2(pt1: POINT2, pt2: POINT2, dist: number): POINT2;\n    static extendAlongLine2(pt1: Point2D, pt2: Point2D, dist: number): Point2D;\n    /**\n     * Extends a point above a line\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param pt3 point at which to extend\n     * @param d distance in pixels to extend above the line\n     * @param X OUT - extended point x value\n     * @param Y OUT - extended point y value\n     * @param direction direction to extend the line\n     *\n     * @return 1 if successful, else return 0\n     */\n    protected static ExtendLineAbove(pt1: POINT2, pt2: POINT2, pt3: POINT2, d: number, direction: number): {\n        result: number;\n        x: number;\n        y: number;\n    };\n    /**\n     * Extends a point to the left of a line\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param pt3 point at which to extend\n     * @param d distance in pixels to extend above the line\n     * @param X OUT - extended point x value\n     * @param Y OUT - extended point y value\n     * @param direction direction to extend the line\n     *\n     * @return 1 if successful, else return 0\n     */\n    protected static ExtendLineLeft(pt1: POINT2, pt2: POINT2, pt3: POINT2, d: number, direction: number): {\n        result: number;\n        x: number;\n        y: number;\n    };\n    /**\n     * Calculates the direction of a point relative to a line\n     *\n     * @param pt0 first point fo the line\n     * @param pt1 last point of the line\n     * @param ptRelative relative point\n     * @return 0 if left, 1 if right, 2 if above, 3 if below\n     */\n    static CalcDirectionFromLine(pt0: POINT2, pt1: POINT2, ptRelative: POINT2): number;\n    /**\n     * Returns a point extended perpendicularly from a line at a given direction\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param pt0 on line from which to extend\n     * @param direction the direction to extend: above, below, left, right\n     * @param d the length to extend in pixels\n     *\n     */\n    static ExtendDirectedLine(pt1: POINT2, pt2: POINT2, pt0: POINT2, direction: number, d: number): POINT2;\n    /**\n     * Returns a point extended perpendicularly from a line at a given direction\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param pt0 on line from which to extend\n     * @param direction the direction to extend: above, below, left, right\n     * @param d the length to extend in pixels\n     * @param style the style to assign the return point\n     *\n     */\n    static ExtendDirectedLine(pt1: POINT2, pt2: POINT2, pt0: POINT2, direction: number, d: number, style: number): POINT2;\n    /**\n     * @deprecated Returns a point extended perpendicularly from a line at a\n     * given direction same as original function except it accounts for vertical\n     * lines and negative d values\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param pt0 on line from which to extend\n     * @param direction the direction to extend: above, below, left, right\n     * @param d the length to extend in pixels\n     *\n     */\n    static ExtendDirectedLineText(pt1: POINT2, pt2: POINT2, pt0: POINT2, direction: number, d: number): POINT2;\n    /**\n     * Calculates a point along a line\n     *\n     * @param pt1 first line point\n     * @param pt2 last line point\n     * @param dist extension distance in pixels from the beginning of the line\n     * @param styl the line style to assign the point\n     *\n     * @return the extension point\n     */\n    static extendLine(pt1: POINT2, pt2: POINT2, dist: number, styl: number): POINT2;\n    /**\n     * Extends a point at an angle from a line.\n     *\n     * @param pt0 the first line point\n     * @param pt1 the second line point\n     * @param pt2 point on line from which to extend\n     * @param alpha angle of extension in degrees\n     * @param d the distance in pixels to extend\n     *\n     * @return the extension point\n     */\n    static ExtendAngledLine(pt0: POINT2, pt1: POINT2, pt2: POINT2, alpha: number, d: number): POINT2;\n    /**\n     * Returns an integer indicating the quadrant for the direction of the line\n     * from pt1 to pt2\n     *\n     * @param pt1 first line point\n     * @param pt2 second line point\n     *\n     * @return the quadrant\n     */\n    static GetQuadrantDouble(pt1: POINT2, pt2: POINT2): number;\n    static GetQuadrantDouble(x1: number, y1: number, x2: number, y2: number): number;\n    /**\n     * Returns the smallest x and y pixel values from an array of points\n     *\n     * @param ptsSeize array of points from which to find minimum vaules\n     * @param vblCounter the number of points to test in the array\n     * @param x OUT - an object with a member to hold the xminimum\n     * @param y OUT - an object with a member to hold the y minimum value\n     *\n     */\n    static GetPixelsMin(ptsSeize: POINT2[], vblCounter: number): {\n        x: number;\n        y: number;\n    };\n    /**\n     * Returns the largest x and y pixel values from an array of points\n     *\n     * @param ptsSeize array of points from which to find maximum values\n     * @param vblCounter the number of points to test in the array\n     * @param x OUT - an object with a member to hold the x maximum value\n     * @param y OUT - an object with a member to hold the y maximum value\n     *\n     */\n    static GetPixelsMax(ptsSeize: POINT2[], vblCounter: number): {\n        x: number;\n        y: number;\n    };\n    /**\n     * Returns center point for a clockwise arc to connect pts 1 and 2. Also\n     * returns an extended point on the line between pt1 and the new center\n     * Caller passes a POINT1 array of size 2 for ptsSeize, passes pt1 and pt2\n     * in ptsSeize Returns the radius of the 90 degree arc between C (arc\n     * center) and pt1\n     *\n     * @param ptsSeize OUT - two point array also used for the returned two\n     * points\n     *\n     * @return the radius\n     */\n    static calcClockwiseCenter(ptsSeize: POINT2[]): number;\n    /**\n     * Computes the points for an arrowhead based on a line segment\n     *\n     * @param startLinePoint segment start point\n     * @param endLinePoint segment end point\n     * @param nBiSector bisecotr in pixels\n     * @param nBase base size in pixels\n     * @param pResultLinePoints OUT - the arrowhead points\n     * @param styl the line style to assign the last aroowhead point\n     */\n    static GetArrowHead4Double(startLinePoint: POINT2, endLinePoint: POINT2, nBiSector: number, nBase: number, pResultLinePoints: POINT2[], styl: number): void;\n    /**\n     * Returns the midpoint between two points.\n     *\n     * @param pt0 the first point\n     * @param pt1 the second point\n     * @param styl the style to assign the mid point\n     *\n     * @return the mid point\n     */\n    static midPoint(pt0: POINT2, pt1: POINT2, styl: number): POINT2;\n    /**\n     * Rotates an the first vblCounter points in the array about its first point\n     *\n     * @param pLinePoints OUT - the points to rotate\n     * @param vblCounter the number of points to rotate\n     * @param lAngle the angle in degrees to rotate\n     *\n     * @return pLinePoints\n     */\n    protected static RotateGeometryDoubleOrigin(pLinePoints: POINT2[], vblCounter: number, lAngle: number): POINT2[];\n    /**\n     * Returns a point a distance d pixels perpendicular to the pt0-pt1 line and\n     * going toward pt2\n     *\n     * @param pt0 the first line point\n     * @param pt1 the second line point\n     * @param pt2 the relative line point\n     * @param d the distance in pixels\n     * @param styl the linestyle to assign the computed point\n     *\n     * @return the extended point\n     */\n    static extendTrueLinePerp(pt0: POINT2, pt1: POINT2, pt2: POINT2, d: number, styl: number): POINT2;\n    /**\n     * Calculates the intersection of 2 lines pelative to a point. if one of the\n     * lines is vertical use a distance dWidth above or below the line. pass\n     * bolVertical1 = 1, or bolVertical2 = 1 if either line segment is vertical,\n     * else pass 0. return the unique intersection in X,Y pointers. p2 is the\n     * point that connects the 2 line segments to which the intersecting lines\n     * are related, i.e. the intersecting lines are a distance dWidth pixels\n     * above or below p2. uses dWidth and lOrient for cases in which at least\n     * one of the lines is vertical. for normal lines this function assumes the\n     * caller has passed the m, b for the appropriate upper or lower lines to\n     * get the desired intgercept. this function is used for calculating the\n     * upper and lower channel lines for channel types. For lOrient: see\n     * comments in Channels.ConnectTrueDouble2\n     *\n     * @param m1 slope of the first line\n     * @param b1 intercept of the first line\n     * @param m2 slope of the second line\n     * @param b2 y intercept of the second line\n     * @param p2 point that connects the 2 line segments to which the\n     * intersecting lines are related\n     * @param bolVerticalSlope1 1 if first segment is vertical, else 0\n     * @param bolVerticalSlope2 1 if second line segment is vertical, else 0\n     * @param dWidth the distance of the intersecting lines from p2 in pixels\n     * @param lOrient the orientation of the intersecting lines relative to the\n     * segments connecting p2\n     * @param X OUT - object holds the x value of the intersection point\n     * @param Y OUT - object holds the y value of the intersection point\n     */\n    static calcTrueIntersectAbs(m1: number, b1: number, m2: number, b2: number, p2: POINT2, //can use for vertical lines\n    bolVerticalSlope1: number, bolVerticalSlope2: number, dWidth: number, //use for vertical lines, use + for upper line, - for lower line\n    lOrient: number): {\n        result: number;\n        x: number;\n        y: number;\n    };\n    /**\n     * Returns the distance in pixels from x1,y1 to x2,y2\n     *\n     * @param x1 first point x location in pixels\n     * @param y1 first point y location in pixels\n     * @param x2 second point x location in pixels\n     * @param y2 second point y location in pixels\n     *\n     * @return the distance\n     */\n    static CalcDistance2(x1: number, y1: number, x2: number, y2: number): number;\n    /**\n     * gets the middle line for Rev B air corridors AC, LLTR, MRR, UAV\n     * Middle line is handled separately now because the line may have been segmented\n     * @param pLinePoints\n     * @return\n     */\n    protected static GetSAAFRMiddleLine(pLinePoints: POINT2[]): POINT2[];\n    /**\n     * Computes the points for a SAAFR segment\n     *\n     * @param pLinePoints OUT - the client points also used for the returned\n     * points\n     * @param lineType the line type\n     * @param dMRR the symbol width\n     */\n    static GetSAAFRSegment(pLinePoints: POINT2[], lineType: number, dMRR: number): void;\n    /**\n     * Called by arraysupport for SAAFR and AC fill shapes\n     * @param pLinePoints\n     * @param dMRR\n     */\n    static GetSAAFRFillSegment(pLinePoints: POINT2[], dMRR: number): void;\n    /**\n     * Computes an arc.\n     *\n     * @param pResultLinePoints OUT - contains center and start point and holds\n     * the result arc points\n     * @param vblCounter the number of client points\n     * @param dRadius the arc radius in pixels\n     * @param linetype the linetype determines start andgle and end angle for\n     * the arc\n     *\n     */\n    static arcArray(pResultLinePoints: POINT2[], vblCounter: number, dRadius: number, linetype: number, converter: IPointConversion | null): POINT2[];\n    /**\n     * Gets geodesic circle using the converter\n     * @param Center in pixels\n     * @param pt1 a point on the radius in pixels\n     * @param numpts number of points to return\n     * @param CirclePoints the result points\n     * @param converter\n     */\n    static CalcCircleDouble2(Center: POINT2, pt1: POINT2, numpts: number, CirclePoints: POINT2[], converter: IPointConversion): void;\n    /**\n     * Computes the points for a circle. Assumes CirclePoints has been allocated\n     * with size numpts.\n     *\n     * @param Center the cicle center\n     * @param radius the circle radius in pixels\n     * @param numpts the number of circle points\n     * @param CirclePoints - OUT - array of circle points\n     * @param styl the style to set the last circle point\n     */\n    static CalcCircleDouble(Center: POINT2, radius: number, numpts: number, CirclePoints: POINT2[], styl: number): void;\n    static CalcCircleShape(Center: POINT2, radius: number, numpts: number, CirclePoints: POINT2[], styl: number): Shape2;\n    private static GetSquallCurve;\n    /**\n     * Gets the squall curves for a line segment Assumes pSquallPts has been\n     * allocated the proper number of points.\n     *\n     * @param StartPt segment start point\n     * @param EndPt segment end point\n     * @param pSquallPts OUT - the squall points\n     * @param sign OUT - an object with a member to hold the starting curve sign\n     * for the segment.\n     * @param amplitude the sin curve amplitutde\n     * @param quantity the number of points for each sin curve\n     * @param length the desired length of the curve along the segment for each\n     * sin curve\n     *\n     * @return segment squall points count\n     */\n    static GetSquallSegment(StartPt: POINT2, EndPt: POINT2, pSquallPts: POINT2[], signVal: number, amplitude: number, quantity: number, length: number): number;\n    private static PointInBounds;\n    /**\n     * @param pt\n     * @param ul\n     * @param lr\n     * @return\n     */\n    private static PointInBounds2;\n    /**\n     * Analyzes if line from pt0 to pt 1 intersects a side and returns the\n     * intersection or null assumes pt0 to pt1 is not vertical. the caller will\n     * replace pt0 with the intersection point if it is not null\n     *\n     * @param pt0\n     * @param pt1\n     * @param sidePt0 vertical or horizontal side first point\n     * @param sidePt1\n     * @return null if it does not intersect the side\n     */\n    private static intersectSegment;\n    /**\n     * side 1 ----- | | side 0 | | side 2 | | ------ side 3 bounds one segment\n     * for autoshapes that need it: bydif, fordif, fix, mnfldfix if null is\n     * returned the client should conect the original line points (i.e. no\n     * jaggies)\n     *\n     * @param pt0\n     * @param pt1\n     * @param ul\n     * @param lr\n     * @return bounded segment or null\n     */\n    static BoundOneSegment(pt0: POINT2, pt1: POINT2, ul: POINT2, lr: POINT2): POINT2[] | null;\n    private static DisplayIntersectPixels;\n    /**\n     * Computes Ditch spikes for the ATDITCH line types. This function uses\n     * linestyles provided by the caller to skip segments.\n     *\n     * @param pLinePoints OUT - the client points also used for the return\n     * points\n     * @param nOldCounter the number of client points\n     * @param bWayIs the parallel line to use (0) for inner or outer spikes\n     *\n     * @return the symbol point count\n     */\n    static getDitchSpike(tg: TacticalGraphic, pLinePoints: POINT2[], nOldCounter: number, bWayIs: number): number;\n    /**\n     * Moves pixels if points are identical, used for the channel types\n     *\n     * @param pLinePoints OUT - client points also for returned points\n     */\n    static MoveChannelPixels(pLinePoints: POINT2[]): void;\n    /**\n     * Single Concertina cannot have horizontal first segment\n     *\n     * @param linetype\n     * @param pLinePoints\n     */\n    static moveSingleCPixels(linetype: number, pLinePoints: POINT2[]): void;\n    /**\n     * Rotates an the first vblCounter points in the array about its first point\n     *\n     * @param pLinePoints OUT - the points to rotate\n     * @param vblCounter the number of points to rotate\n     * @param lAngle the angle in degrees to rotate\n     */\n    static RotateGeometryDouble(pLinePoints: POINT2[], vblCounter: number, lAngle: number): void;\n    /**\n     * Returns the point on line (pt0 to pt1) closest to ptRelative\n     *\n     * @param pt0 the first point on line\n     * @param pt1 the second point on line\n     * @param ptRelative the second point on line\n     * @return the point closest to ptRelative on the line\n     */\n    static ClosestPointOnLine(pt0: POINT2, pt1: POINT2, ptRelative: POINT2): POINT2;\n    /**\n     * Returns the intersection between two line segments or null if it doesn't exist\n     *\n     * @param pt1\n     * @param pt2\n     * @param pt3\n     * @param pt4\n     * @return\n     */\n    private static getIntersectionPoint;\n    /**\n     * Returns the intersection between a polygon and a line or null if it doesn't exist\n     *\n     * @param polyPts\n     * @param pt0\n     * @param pt1\n     * @return\n     */\n    static intersectPolygon(polyPts: POINT2[], pt0: POINT2, pt1: POINT2): POINT2;\n    /**\n     * Returns the point perpendicular to the line (pt0 to pt1) at the midpoint\n     * the same distance from (and on the same side of) the the line as\n     * ptRelative.\n     *\n     * @param pt0 the first point\n     * @param pt1 the second point\n     * @param ptRelative the point to use for computing the return point\n     *\n     * @return the point perpendicular to the line at the midpoint\n     */\n    static PointRelativeToLine(pt0: POINT2, pt1: POINT2, ptRelative: POINT2): POINT2;\n    /**\n     * Returns the point perpendicular to the line (pt0 to pt1) at atPoint the\n     * same distance from (and on the same side of) the the line as ptRelative.\n     *\n     * @param pt0 the first point\n     * @param pt1 the second point\n     * @param atPoint the point on the line at which to compute the extended\n     * point\n     * @param ptRelative the point to use for computing the return point\n     *\n     * @return the point perpendicular to the line at ptRelative\n     */\n    static PointRelativeToLine(pt0: POINT2, pt1: POINT2, atPoint: POINT2, ptRelative: POINT2): POINT2;\n    /**\n     * shift the control point to match the shift that occurs in\n     * Channels.GetAXADDouble for CATKBYFIRE. This is because the rotary feature\n     * arrow tip must align with the anchor point\n     *\n     * @param linetype\n     * @param pLinePoints the anchor points including the control point\n     * @param dist the minimum required distance from the front of the rotary\n     * arrow\n     */\n    static adjustCATKBYFIREControlPoint(linetype: number, pLinePoints: Array<POINT2>, dist: number): void;\n    /**\n     * Returns in pt2 and pt3 the line segment parallel to segment pt0-pt1 which\n     * would contain ptRelative. pt2 corresponds to pt0 and pt3 corresponds to\n     * pt1.\n     *\n     * @param pt0 first line point\n     * @param pt1 second line point\n     * @param ptRelative relative line point\n     * @param pt2 OUT - first computed relative line point\n     * @param pt3 OUT - second computed relative line point\n     */\n    static LineRelativeToLine(pt0: POINT2, pt1: POINT2, ptRelative: POINT2, pt2: POINT2, pt3: POINT2): void;\n    private static calcMBR;\n    static calcMBRPoints(pLinePoints: POINT2[], numpts: number, ul: POINT2, lr: POINT2): void;\n    /**\n     * Computes the distance in pixels from upper left to lower right of the\n     * minimum bounding rectangle for the first numpts of pLinePoints\n     *\n     * @param pLinePoints the inpupt point array\n     * @param numpts the number of points to use\n     *\n     * @return the distance in pixels\n     */\n    static MBRDistance(pLinePoints: POINT2[], numpts: number): number;\n    /**\n     * Swaps two points.\n     *\n     * @param pt1 OUT - first point\n     * @param pt2 OUT - second point\n     *\n     */\n    static Reverse2Points(pt1: POINT2, pt2: POINT2): void;\n    /**\n     * Creates a GeneralPath from a Path2D\n     *\n     * @param shape\n     * @return\n     */\n    static createStrokedShape(shape: Shape): Shape;\n    /**\n     * @deprecated Returns the determinant of the point matrix This determinant\n     * tells how far p3 is from vector p1p2 and on which side it is\n     * @param p1\n     * @param p2\n     * @param p3\n     * @return\n     */\n    private static distance;\n    /**\n     * @deprecated Returns the determinant of the point matrix This determinant\n     * tells how far p3 is from vector p1p2 and on which side it is\n     * @param p1\n     * @param p2\n     * @param p3\n     * @return\n     */\n    private static distance2;\n    /**\n     * @deprecated @param array\n     * @return\n     */\n    static cHull(array: Array<Point>): Array<Point>;\n    /**\n     * @deprecated @param points\n     * @param l\n     * @param r\n     * @param path\n     */\n    static cHull(points: Array<Point>, l: Point, r: Point, path: Array<Point>): void;\n    /**\n     * @deprecated @param array\n     * @return\n     */\n    static cHull2(array: Array<POINT2>): Array<POINT2>;\n    /**\n     * @deprecated @param points\n     * @param l\n     * @param r\n     * @param path\n     */\n    static cHull2(points: Array<POINT2>, l: POINT2, r: POINT2, path: Array<POINT2>): void;\n    static getExteriorPoints(pLinePoints: POINT2[], vblCounter: number, lineType: number, interior: boolean): void;\n    static getDeepCopy(pts: Array<POINT2>): Array<POINT2>;\n    static reverseDirection(direction: number): number;\n}\n//# sourceMappingURL=line-ops.d.ts.map","milsym/multipoint/cpof-utils.d.ts":"import { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle } from \"../graphics/Rectangle\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n * CPOF utility functions taken from JavaLineArrayCPOF\n *\n *\n */\nexport declare class CPOFUtils {\n    private static readonly _className;\n    /**\n     *\n     * @param ptLatLong\n     * @param converter\n     * @return\n     */\n    private static PointLatLongToPixels;\n    /**\n     * for the change 1 fire support areas\n     *\n     * @param tg\n     * @param lineType\n     * @param radius\n     * @param width\n     * @param length\n     * @param attitude\n     */\n    private static GetNumericFields;\n    /**\n     * Do a 360 degree horizontal shift for points on either side of the\n     * midpoint of the display, if the MBR for the pixels is greater than 180\n     * degrees wide. Builds pixels for two symbols to draw a symbol flipped\n     * about the left edge and also a symbol flipped about the right edge. This\n     * function is typically used at world view. Caller must instantiate last\n     * two parameters.\n     *\n     * @param tg\n     * @param converter\n     * @param farLeftPixels - OUT - the resultant pixels for left shift symbol\n     * @param farRightPixels - OUT - the result pixels for the right shift\n     * symbol\n     */\n    static GetFarPixels(tg: TacticalGraphic, converter: IPointConversion, farLeftPixels: POINT2[], farRightPixels: POINT2[]): void;\n    /**\n     *\n     * @param tg\n     * @param lineType\n     * @param converter\n     * @param shapes\n     * @return\n     */\n    static Change1TacticalAreas(tg: TacticalGraphic, lineType: number, converter: IPointConversion, shapes: Array<Shape2>): boolean;\n    /**\n     * build shapes arraylist from tg.Pixels for the Change 1 symbols\n     *\n     * @param tg\n     * @param shapes - OUT - caller instantiates the arraylist\n     */\n    private static Change1PixelsToShapes;\n    private static GetConcentricCircles;\n    /**\n     * if tg.H2 is filled then the max range sector is used to determine the\n     * orientation\n     *\n     * @param tg\n     * @return left,right,min,max\n     */\n    private static GetMaxSector;\n    /**\n     * Create a tg with a new line type to used for circular range fan fill\n     *\n     * @param tg\n     * @return\n     */\n    static GetCircularRangeFanFillTG(tg: TacticalGraphic): TacticalGraphic;\n    /**\n     *\n     * @param tg\n     * @param converter\n     * @return\n     */\n    private static GetSectorRangeFan;\n    private static RangeFanOrientation;\n    /**\n     * after filtering pixels it needs to reinitialize the style to 0 or it\n     * causes CELineArraydotNet to build wrong shapes\n     *\n     * @param tg\n     */\n    static ClearPixelsStyle(tg: TacticalGraphic): void;\n    /**\n     * Filters too close points after segmenting and clipping\n     *\n     * @param tg\n     * @param converter\n     */\n    static FilterPoints2(tg: TacticalGraphic, converter: IPointConversion): void;\n    /**\n     * returns true if the line type can be clipped before calculating the\n     * shapes\n     *\n     * @param tg tactical graphic\n     * @return true if can pre-clip points\n     */\n    static canClipPoints(tg: TacticalGraphic): boolean;\n    /**\n     * These get clipped so the fill must be treated as a separate shape.\n     * Normally lines with fill do not have a separate shape for the fill.\n     *\n     * @param linetype\n     * @return\n     */\n    static LinesWithSeparateFill(linetype: number, shapes: Array<Shape2>): boolean;\n    /**\n     * uses a hash map to set the POINT2 style when creating tg.Pixels from\n     * Point2D ArrayList\n     *\n     * @param pts2d\n     * @param hashMap\n     * @return\n     */\n    static Point2DtoPOINT2Mapped(pts2d: Array<Point2D>, hashMap: Map<string, Point2D>): Array<POINT2>;\n    protected static Point2DtoPOINT2(pts2d: Array<Point2D>): Array<POINT2>;\n    static POINT2toPoint2D(pts: Array<POINT2>): Array<Point2D>;\n    /**\n     * Builds a single shape from a point array. Currently we assume the array\n     * represents a moveTo followed by a series of lineTo operations\n     *\n     * @param pts2d\n     * @return\n     */\n    private static BuildShapeFromPoints;\n    /**\n     * Clips a ShapeSpec. Assumes we are not post clipping splines, therefore\n     * all the operations are moveTo, lineTo. Each ShapeSpec is assumed to be:\n     * moveTo, lineTo ... lineTo, followed by another moveTo, lineTo, ...\n     * lineTo, followed by ...\n     *\n     * @param shapeSpec\n     * @param pts\n     * @param clipArea\n     * @return a single clipped shapeSpec\n     */\n    protected static buildShapeSpecFromPoints(tg0: TacticalGraphic, shapeSpec: Shape2, //the original ShapeSpec\n    pts: Array<POINT2>, clipArea: Rectangle | Rectangle2D | Array<Point2D>): Array<Shape2>;\n    /**\n     * Currently assumes no MeTOC symbols are post clipped\n     *\n     * @param tg\n     * @param shapeSpecsArray\n     * @param clipArea\n     * @return\n     */\n    static postClipShapes(tg: TacticalGraphic, shapeSpecsArray: Array<Shape2>, clipArea: Point2D[] | Rectangle | Rectangle2D): Array<Shape2> | null;\n    /**\n     * For the 3d map we cannot pre-segment the auto-shapes or fire support\n     * areas. We do need to pre-segment generic lines regardless of the status\n     * if clipping is set. Currently we are not pre-segmenting axis of advance\n     * symbols.\n     *\n     * @param tg\n     * @return true if pre-segmenting is to be used\n     */\n    private static segmentAnticipatedLine;\n    /**\n     * cannot pre-segment the fire support areas, must post segment them after\n     * the pixels were calculated\n     *\n     * @param tg\n     * @param converter\n     */\n    protected static postSegmentFSA(tg: TacticalGraphic, converter: IPointConversion): void;\n    /**\n     * Similar to Vincenty algorithm for more accurate interpolation of geo\n     * anchor points\n     *\n     * @return the interpolated points\n     */\n    private static toGeodesic;\n    /**\n     * Pre-segment the lines based on max or min latitude for the segment\n     * interval. This is necessary because GeoPixelconversion does not work well\n     * over distance greater than 1M meters, especially at extreme latitudes.\n     *\n     * @param tg\n     * @param converter\n     */\n    static SegmentGeoPoints(tg: TacticalGraphic, converter: IPointConversion, zoomFactor: number): void;\n    /**\n     * Calculating the signed area will tell you which direction the points are going.\n     * Negative = Clock-wise, Positive = counter clock-wise\n     * A = 1/2 * (x1*y2 - x2*y1 + x2*y3 - x3*y2 + ... + xn*y1 - x1*yn)\n     */\n    static CalculateSignedAreaOfPolygon(coords: POINT2[]): number;\n}\n//# sourceMappingURL=cpof-utils.d.ts.map","milsym/multipoint/ge-utils.d.ts":"import { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle } from \"../graphics/Rectangle\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { ShapeInfo } from \"../renderer/utilities/ShapeInfo\";\n/**\n * Utilities require for GoogleEarth functionality\n *\n */\nexport declare class GEUtils {\n    private static readonly _className;\n    static setSplineLinetype(tg: TacticalGraphic): void;\n    /**\n     * GE has no capability for dashed lines. This function sets each polyline in the array as a new\n     * polyline broken into points corresponding to the dash pattern\n     * @param polylines\n     * @param shape\n     */\n    private static createDashedPolylines;\n    private static createSimpleFillShape;\n    private static createSimplePatternFillShape;\n    private static allowFillForThese;\n    static SetShapeInfosPolylines(tg: TacticalGraphic, shapeInfos: Array<ShapeInfo>, clipBounds: Rectangle2D | Rectangle | Array<Point2D> | null): void;\n    /**\n     * Separates the Shape into separate polylines, eas as an ArrayList of Point2D\n     * @param shape\n     * @return\n     */\n    private static createRenderablesFromShape;\n    /**\n     * Assumes a convex polygon for the clipping area.\n     * expand the polygon using pixels and a similar algorithm to what flash renderer does for DEPTH AREA\n     * @param pts clipping area to expand\n     * @param expand pixels expansion\n     * @return\n     */\n    static expandPolygon(pts: Array<Point2D>, expand: number): Array<Point2D>;\n    /**\n     * use cheap algorithm to expand polygons, works best on regular 4+ sided convex polygons\n     * used primarily for expanding the original clipping areas. After clipping a tactical line against\n     * the expanded clipping area, the original clipping area can be used to drop the clip lines\n     * @param pts points to expand, usually a clipping area\n     * @param expandX X expansion factor, e.g 10% growth would be 1.1\n     * @param expandY Y expansion factor\n     * @return points for the expanded polygon\n     */\n    protected static expandPolygon2(pts: Array<Point2D>, expandX: number, expandY: number): Array<Point2D>;\n    /**\n     * @deprecated\n     * For tactical lines break up the arraylists into separate arraylists within the bounds.\n     * This was added for the Google Earth 3D map because small scales cut off and we want the clip lines\n     * to not be visible.\n     * @param ptsPoly\n     * @param clipBounds\n     * @return\n     */\n    private static ptsPolyToPtsPoly;\n    /**\n     * removes leading or trailing segments after the points were clipped\n     * @param tg\n     * @param clipArea\n     */\n    static removeTrailingPoints(tg: TacticalGraphic, clipArea: Point2D[] | Rectangle | Rectangle2D): void;\n    /**\n         * tests of a Line2D intersects a polygon by using line.intersectsLine on each segment of the polygon\n         * assumes clip clipping area was parsed to shift points of vertical segments to make them not vertical\n         * @param line a clipping line in the clipping polygon\n         * @param clipPts array of clip points assumed to be closed\n         * @return true if the line intersects the clip bounds\n         */\n    private static lineIntersectsClipArea;\n    /**\n     * returns true if segment data set for MSR, ASR, Boundary\n     * @param tg\n     * @return\n     */\n    protected static segmentColorsSet(tg: TacticalGraphic): boolean;\n    /**\n     * Use clipping rectangle or clip points to build a zoom factor if the client zoomed in after the initial render.\n     * Multiply the geo segmenting interval by this factor.\n     * @param rect\n     * @param clipPoints\n     * @param pixels\n     * @return\n     */\n    static getZoomFactor(rect: Rectangle2D, clipPoints: Array<Point2D>, pixels: Array<POINT2>): number;\n}\n//# sourceMappingURL=ge-utils.d.ts.map","milsym/multipoint/multipoint-renderer.d.ts":"import { Graphics2D } from \"../graphics/Graphics2D\";\nimport { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle } from \"../graphics/Rectangle\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { Color } from \"../renderer/utilities/Color\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\nimport { MilStdSymbol } from \"../renderer/utilities/MilStdSymbol\";\nimport { ShapeInfo } from \"../renderer/utilities/ShapeInfo\";\n/**\n * Rendering class\n *\n *\n */\nexport declare class MultipointRenderer {\n    private static readonly _className;\n    /**\n     * Set tg geo points from the client points\n     *\n     * @param milStd\n     * @param tg\n     */\n    private static setClientCoords;\n    private static getClientCoords;\n    /**\n * Build a tactical graphic object from the client MilStdSymbol\n *\n * @param milStd MilstdSymbol object\n * @param converter geographic to pixels converter\n * @param lineType {@link BasicShapes}\n * @return tactical graphic\n */\n    static createTacticalGraphicFromMilStdSymbolBasicShape(milStd: MilStdSymbol, converter: IPointConversion, lineType: number): TacticalGraphic;\n    /**\n     * Create MilStdSymbol from tactical graphic\n     *\n     * @deprecated\n     * @param tg tactical graphic\n     * @param converter geographic to pixels to converter\n     * @return MilstdSymbol object\n     */\n    static createMilStdSymboFromTacticalGraphic(tg: TacticalGraphic, converter: IPointConversion): MilStdSymbol;\n    /**\n     * Build a tactical graphic object from the client MilStdSymbol\n     *\n     * @param milStd MilstdSymbol object\n     * @param converter geographic to pixels converter\n     * @return tactical graphic\n     */\n    static createTacticalGraphicFromMilStdSymbol(milStd: MilStdSymbol, converter: IPointConversion): TacticalGraphic;\n    /**\n     * @deprecated @param milStd\n     * @param converter\n     * @param computeChannelPt\n     * @return\n     */\n    static createTacticalGraphicFromMilStdSymbol(milStd: MilStdSymbol, converter: IPointConversion, computeChannelPt: boolean): TacticalGraphic;\n    private static createAltitudeLabel;\n    private static Shape2ToShapeInfo;\n    /**\n     * Added function to handle when coords or display area spans IDL but not\n     * both, it prevents the symbol from rendering if the bounding rectangles\n     * don't intersect.\n     *\n     * @param tg\n     * @param converter\n     * @param clipArea\n     * @return\n     */\n    static intersectsClipArea(tg: TacticalGraphic, converter: IPointConversion, clipArea: Point2D[] | Rectangle | Rectangle2D): boolean;\n    /**\n     * Adds Feint, decoy, or dummy indicator to shapes. Does not check if tactical graphic should have indicator\n     */\n    private static addFDI;\n    /**\n     * GoogleEarth renderer uses polylines for rendering\n     *\n     * @param mss MilStdSymbol object\n     * @param converter the geographic to pixels coordinate converter\n     * @param clipArea the clip bounds\n     */\n    static renderWithPolylines(mss: MilStdSymbol, converter: IPointConversion, clipArea: Point2D[] | Rectangle | Rectangle2D): void;\n    /**\n     * @param mss\n     * @param converter\n     * @param clipArea\n     * @param g2d\n     * @deprecated Graphics2D not used\n     */\n    static renderWithPolylines(mss: MilStdSymbol, converter: IPointConversion, clipArea: Point2D[] | Rectangle | Rectangle2D, g2d: Graphics2D): void;\n    /**\n     * Google Earth renderer: Called by mapfragment-demo This is the public\n     * interface for Google Earth renderer assumes tg.Pixels is filled assumes\n     * the caller instantiated the ShapeInfo arrays\n     *\n     * @param tg tactical graphic\n     * @param shapeInfos symbol ShapeInfo array\n     * @param modifierShapeInfos modifier ShapeInfo array\n     * @param converter geographic to pixels coordinate converter\n     * @param clipArea clipping bounds in pixels\n     */\n    static render_GE(tg: TacticalGraphic, shapeInfos: Array<ShapeInfo>, modifierShapeInfos: Array<ShapeInfo>, converter: IPointConversion, clipArea: Point2D[] | Rectangle | Rectangle2D): void;\n    /**\n     * See render_GE below for comments\n     *\n     * @param tg\n     * @param shapeInfos\n     * @param modifierShapeInfos\n     * @param converter\n     * @param clipArea\n     * @param g2d test android-gradle\n     * @deprecated Graphics2D not used\n     */\n    static render_GE(tg: TacticalGraphic, shapeInfos: Array<ShapeInfo>, modifierShapeInfos: Array<ShapeInfo>, converter: IPointConversion, clipArea: Point2D[] | Rectangle | Rectangle2D, g2d: Graphics2D): void;\n    /**\n     * creates a shape for known symbols. The intent is to use client points for\n     * the shape and is intended for use with ellipse. If hatch &gt; 1 it creates 2 shapes\n     * one for the hatch pattern, the second one is for the outline.\n     *\n     * @param milStd\n     * @param ipc\n     * @param clipArea\n     * @param shapeType\n     * @param lineColor\n     * @param fillColor\n     * @param hatch\n     */\n    static render_Shape(milStd: MilStdSymbol, ipc: IPointConversion, clipArea: Point2D[] | Rectangle | Rectangle2D, shapeType: number, lineColor: Color, fillColor: Color, hatch: number): void;\n    private static resolvePostClippedShapes;\n    /**\n     * set the clip rectangle as an arraylist or a Rectangle2D depending on the\n     * object\n     *\n     * @param clipBounds\n     * @param clipRect\n     * @param clipArray\n     * @return\n     */\n    private static setClip;\n    /**\n     * public render function transferred from JavaLineArrayCPOF project. Use\n     * this function to replicate CPOF renderer functionality.\n     *\n     * @param mss the milStdSymbol object\n     * @param converter the geographic to pixels coordinate converter\n     * @param clipBounds the pixels based clip bounds\n     */\n    static render(mss: MilStdSymbol, converter: IPointConversion, clipBounds: Rectangle2D | Array<Point2D> | null): void;\n    /**\n     * Generic tester button says Tiger or use JavaRendererSample. Generic\n     * renderer testers: called by JavaRendererSample and TestJavaLineArray\n     * public render function transferred from JavaLineArrayCPOF project. Use\n     * this function to replicate CPOF renderer functionality.\n     *\n     * @param mss MilStdSymbol\n     * @param converter geographic to pixels converter\n     * @param shapeInfos ShapeInfo array\n     * @param modifierShapeInfos modifier ShapeInfo array\n     * @param clipBounds clip bounds\n     */\n    static render(mss: MilStdSymbol, converter: IPointConversion, shapeInfos: Array<ShapeInfo>, modifierShapeInfos: Array<ShapeInfo>, clipBounds: Rectangle2D | Rectangle | Array<Point2D> | null): void;\n    static getCMLineType(version: number, entityCode: number): number;\n    /**\n     * Some symbol's points are reversed when moving from 2525C to 2525D. This method should be called at the start of each render.\n     *\n     * It's a simpler fix to reverse the points order at start than to reverse order when rendering.\n     *\n     * Note: Make sure to only call once to not reverse reversed points\n     * @param tg\n     */\n    private static reversePointsRevD;\n}\n//# sourceMappingURL=multipoint-renderer.d.ts.map","milsym/multipoint/multipoint-renderer2.d.ts":"import { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle } from \"../graphics/Rectangle\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n * Rendering helper class\n *\n */\nexport declare class MultipointRenderer2 {\n    private static readonly _className;\n    /**\n     * MSR and ASR use segment data for segment colors\n     * Assumes tg.H has been revised for clipping\n     * @param tg\n     * @param shapes\n     */\n    private static getMSRShapes;\n    /**\n     *\n     * @param tg\n     * @param converter client converter\n     * @param isTextFlipped\n     * @return\n     */\n    static GetLineArray(tg: TacticalGraphic, converter: IPointConversion, isTextFlipped: boolean, clipBounds: Rectangle2D | Rectangle | Array<Point2D> | null): Array<Shape2> | null;\n    /**\n     * Isolate and others require special handling for the fill shapes.\n     * @param tg\n     * @param shapes the existing shapes which characterize the graphic\n     */\n    static getAutoshapeFillShape(tg: TacticalGraphic, shapes: Array<Shape2>): void;\n}\n//# sourceMappingURL=multipoint-renderer2.d.ts.map","milsym/multipoint/multipoint-utils.d.ts":"import { Point } from \"../graphics/Point\";\nimport { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\nimport { ShapeInfo } from \"../renderer/utilities/ShapeInfo\";\n/**\n * Server general utility class\n *\n */\nexport declare class MultipointUtils {\n    private static readonly _className;\n    static readonly Hatch_ForwardDiagonal: number;\n    static readonly Hatch_BackwardDiagonal: number;\n    static readonly Hatch_Vertical: number;\n    static readonly Hatch_Horizontal: number;\n    static readonly Hatch_Cross: number;\n    /**\n     * Adds hatch fill to shapes via PatternFillRendererD.MakeHatchPatternFill() or buildHatchFill()\n     * @param tg\n     * @param shapes\n     */\n    static addHatchFills(tg: TacticalGraphic, shapes: Array<ShapeInfo>): void;\n    /**\n     * Build Hatch fill. Does not use texture paint or shader.\n     * @param tg\n     * @param shape\n     * @param hatchStyle\n     * @return\n     */\n    static buildHatchArea(tg: TacticalGraphic, shape: ShapeInfo, hatchStyle: number, spacing: number): Shape2;\n    protected static POINT2ToPoint(pt2: POINT2): Point;\n    protected static PointToPOINT2(pt: Point): POINT2;\n    protected static POINT2ToPoint2D(pt2: POINT2): Point2D;\n    static Points2DToPOINT2(pts2d: Array<Point2D>): Array<POINT2>;\n    static Point2DToPOINT2(pt2d: Point2D): POINT2;\n    /**\n     * @deprecated\n     * @param tg\n     * @return\n     */\n    protected static addModifiersBeforeClipping(tg: TacticalGraphic): boolean;\n    /**\n     * @deprecated\n     */\n    protected static FilterPoints(tg: TacticalGraphic): void;\n    static PixelsToLatLong(pts: Array<POINT2>, converter: IPointConversion): Array<POINT2>;\n    static LatLongToPixels(pts: Array<POINT2>, converter: IPointConversion): Array<POINT2>;\n    private static PointLatLongToPixels;\n    static FilterAXADPoints(tg: TacticalGraphic, converter: IPointConversion): void;\n    /**\n     *\n     * @param tg\n     */\n    static RemoveDuplicatePoints(tg: TacticalGraphic): void;\n    static PointPixelsToLatLong(ptPixels: POINT2, converter: IPointConversion): POINT2;\n    static getMBR(clipBounds: Array<Point2D>): Rectangle2D;\n    static GetMBR(shapes: Array<Shape2>, ptUl: POINT2, ptUr: POINT2, ptLr: POINT2, ptLl: POINT2): void;\n}\n//# sourceMappingURL=multipoint-utils.d.ts.map","milsym/renderer/IIconRenderer.d.ts":"import { ImageInfo } from \"./utilities/ImageInfo\";\n/**\n *\n * @deprecated\n */\nexport interface IIconRenderer {\n    CanRender(symbolID: string, modifiers: Map<string, string>): boolean;\n    RenderIcon(symbolID: string, modifiers: Map<string, string>): ImageInfo;\n    getRendererID(): string;\n}\n//# sourceMappingURL=IIconRenderer.d.ts.map","milsym/renderer/MilStdIconRenderer.d.ts":"import { MSInfo } from \"./utilities/MSInfo\";\nimport { SVGInfo } from \"./utilities/SVGInfo\";\nimport { SVGSymbolInfo } from \"./utilities/SVGSymbolInfo\";\n/**\n * This class is used for rendering icons that represent the single point graphics in the MilStd 2525.\n * It can also be used for rendering icon previews for multipoint graphics.\n */\nexport declare class MilStdIconRenderer {\n    private TAG;\n    private static _instance;\n    private _initSuccess;\n    private _SPSVGR;\n    private constructor();\n    static getInstance(): MilStdIconRenderer;\n    /**\n     *\n     *\n     */\n    private init;\n    isReady(): boolean;\n    /**\n     * Checks symbol codes and returns whether they can be rendered.\n     * For multi-point graphics, modifiers are ignored because we don't need that\n     * information to show preview icons in the SymbolPicker.\n     *\n     * @param symbolID 20-30 digit 2525D Symbol ID Code\n     * @param attributes (currently unused)\n     * @return true if the basic form of the graphic can be rendered\n     */\n    CanRender(symbolID: string, attributes: Map<string, string>): boolean;\n    RenderSVG(symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>): SVGSymbolInfo | null;\n    AddCustomSymbol(msInfo: MSInfo, svgInfo: SVGInfo): boolean;\n}\n//# sourceMappingURL=MilStdIconRenderer.d.ts.map","milsym/renderer/ModifierRenderer.d.ts":"import { Color } from \"./utilities/Color\";\nimport { SettingsEventListener } from \"./utilities/SettingsEventListener\";\nimport { SVGSymbolInfo } from \"./utilities/SVGSymbolInfo\";\nimport { SymbolDimensionInfo } from \"./utilities/SymbolDimensionInfo\";\nimport { TextInfo } from \"./utilities/TextInfo\";\n/**\n * This class is used for rendering the labels/amplifiers/modifiers around the single point symbol.\n * It acts as a thin facade that delegates to specialized sub-renderer modules.\n */\nexport declare class ModifierRenderer implements SettingsEventListener {\n    private static _instance;\n    private static _className;\n    private static RS;\n    private static _modifierFont;\n    private static _modifierFontHeight;\n    private static _modifierFontDescent;\n    private static isNode;\n    private static isBrowser;\n    private static OSCDefined;\n    private static _bmp;\n    private static _frc;\n    SettingsEventChanged(type: string): void;\n    private constructor();\n    /**\n     * Instance of the ModifierRenderer class\n     * @return the instance\n     */\n    static getInstance(): ModifierRenderer;\n    /**\n     * Processes unit display modifiers by delegating to specialized sub-renderers.\n     * This is the orchestration method that coordinates all modifier rendering.\n     */\n    static processUnitDisplayModifiers(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo | null;\n    /**\n     * @deprecated no longer a thing in 2525D\n     * TODO: remove\n     */\n    private static getYPositionForSCC;\n    static processSpeedLeader(sdi: SVGSymbolInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>): SVGSymbolInfo;\n    static processUnknownTextModifiers(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo;\n    static processSPTextModifiers(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo;\n    static ProcessTGSPWithSpecialModifierLayout(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, lineColor: Color, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo | null;\n    static ProcessTGSPModifiers(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, lineColor: Color, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo;\n    static renderText(g2d: OffscreenCanvasRenderingContext2D, tiArray: TextInfo[], textColor: Color, textBackgroundColor: Color): void;\n    static hasDisplayModifiers(symbolID: string, modifiers: Map<string, string>): boolean;\n    static hasTextModifiers(symbolID: string, modifiers: Map<string, string>): boolean;\n}\n//# sourceMappingURL=ModifierRenderer.d.ts.map","milsym/renderer/PatternFillRenderer.d.ts":"import { TacticalGraphic } from \"../tactical/tactical-graphic\";\nimport { Color } from \"./utilities/Color\";\nimport { SVGSymbolInfo } from \"./utilities/SVGSymbolInfo\";\n/**\n * Created by michael.spinelli on 8/23/2017.\n */\nexport declare class PatternFillRenderer {\n    private static readonly svgBeachSlopeModerate;\n    private static readonly svgBeachSlopeSteep;\n    private static readonly svgFoulGround;\n    private static readonly svgKelp;\n    private static readonly svgRigField;\n    private static readonly svgSweptArea;\n    private static readonly svgWeirs;\n    /**\n     * @param hatchStyle Direction of hatch lines - constants from clsUtility\n     * @param spacing horizontal spacing between lines\n     * @param strokeWidth width of lines\n     * @param color Color of lines\n     */\n    static MakeHatchPatternFill(hatchStyle: number, spacing: number, strokeWidth: number, color: Color): SVGSymbolInfo;\n    static MakeMetocPatternFill(tg: TacticalGraphic): SVGSymbolInfo;\n}\n//# sourceMappingURL=PatternFillRenderer.d.ts.map","milsym/renderer/SinglePointSVGRenderer.d.ts":"import { SVGSymbolInfo } from \"./utilities/SVGSymbolInfo\";\n/**\n *\n */\nexport declare class SinglePointSVGRenderer {\n    private readonly TAG;\n    private static _instance;\n    static readonly RENDERER_ID: string;\n    private _buffer;\n    private _fontRenderContext;\n    private static isNode;\n    private static isBrowser;\n    private static OSCDefined;\n    constructor();\n    private init;\n    static getInstance(): SinglePointSVGRenderer;\n    render(symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>): SVGSymbolInfo;\n    RenderUnit(symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>): SVGSymbolInfo;\n    RenderSP(symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>): SVGSymbolInfo | null;\n    /**\n     *\n     * @param symbolID\n     * @param attributes\n     * @returns\n     */\n    RenderModifier(symbolID: string, attributes: Map<string, string>): SVGSymbolInfo;\n    /**\n     *\n     * @param si\n     * @returns\n     */\n    private makeDescTag;\n    /**\n     *\n     * @param symbolID\n     * @param si\n     * @returns\n     */\n    private makeMetadataTag;\n    private getSVGString;\n}\nexport declare const singlePointSVGRenderer: SinglePointSVGRenderer;\n//# sourceMappingURL=SinglePointSVGRenderer.d.ts.map","milsym/renderer/modifiers/DirectionArrowRenderer.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Color } from \"../utilities/Color\";\n/**\n * Utility functions for rendering direction arrows in DOM context\n */\nexport declare function createDOMArrowPoints(symbolID: string, bounds: Rectangle2D, center: Point2D, angle: number, isY: boolean, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D, modifierFontHeight: number): Point2D[];\nexport declare function createDOMArrowHead(lpt1: Point2D, lpt2: Point2D): Point2D[];\nexport declare function drawDOMArrow(g2d: OffscreenCanvasRenderingContext2D, domPoints: Point2D[], color: Color, strokeWidth: number): void;\n//# sourceMappingURL=DirectionArrowRenderer.d.ts.map","milsym/renderer/modifiers/DisplayModifierComposer.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Line } from \"../shapes/line\";\nimport { Ellipse } from \"../shapes/ellipse\";\nimport { Path } from \"../shapes/path\";\nimport { Color } from \"../utilities/Color\";\nimport { SVGSymbolInfo } from \"../utilities/SVGSymbolInfo\";\nimport { SVGTextInfo } from \"../utilities/SVGTextInfo\";\n/**\n * Result interface for engagement bar building\n */\nexport interface EngagementBarResult {\n    stiAO?: SVGTextInfo;\n    ebRectangle?: Rectangle2D;\n    ebBounds?: Rectangle2D;\n    ebColor?: Color;\n}\n/**\n * Result interface for affiliation modifier building\n */\nexport interface AffiliationModifierResult {\n    stiAM?: SVGTextInfo;\n    amBounds?: Rectangle2D;\n}\n/**\n * Result interface for HQ staff building\n */\nexport interface HQStaffResult {\n    pt1HQ?: Point2D;\n    pt2HQ?: Point2D;\n    hqBounds?: Rectangle2D;\n}\n/**\n * Result interface for restricted indicator building\n */\nexport interface RestrictedResult {\n    rPath?: Path;\n    rPath2?: Path;\n    rCircle?: Ellipse;\n    rBounds?: Rectangle2D;\n    rStrokeWidth: number;\n}\n/**\n * Result interface for no strike indicator building\n */\nexport interface NoStrikeResult {\n    nsCircle?: Ellipse;\n    nsLine?: Line;\n    nsBounds?: Rectangle2D;\n    nsStrokeWidth: number;\n}\n/**\n * Builds the engagement bar (AO modifier)\n * @param symbolID The symbol ID\n * @param modifiers Map of modifiers\n * @param symbolBounds The symbol bounds\n * @param symbolCenter The symbol center point\n * @param fillColor The fill color\n * @param modifierFont The modifier font\n * @param frc The font render context\n * @param attributes The symbol attributes\n * @param imageBounds The image bounds (will be updated)\n * @param fdiBounds The FDI bounds (if present)\n * @param tfBounds The task force bounds (if present)\n * @param echelonBounds The echelon bounds (if present)\n * @returns The engagement bar result\n */\nexport declare function buildEngagementBar(symbolID: string, modifiers: Map<string, string>, symbolBounds: Rectangle2D, symbolCenter: Point2D, fillColor: Color, modifierFont: string, frc: any, attributes: Map<string, string>, imageBounds: Rectangle2D, fdiBounds?: Rectangle2D, tfBounds?: Rectangle2D, echelonBounds?: Rectangle2D): EngagementBarResult;\n/**\n * Builds the affiliation modifier\n * @param symbolID The symbol ID\n * @param symbolBounds The symbol bounds\n * @param echelonBounds The echelon bounds (if present)\n * @param imageBounds The image bounds (will be updated)\n * @param frc The font render context\n * @returns The affiliation modifier result\n */\nexport declare function buildAffiliationModifier(symbolID: string, symbolBounds: Rectangle2D, echelonBounds: Rectangle2D, imageBounds: Rectangle2D, frc: any): AffiliationModifierResult;\n/**\n * Builds the HQ staff indicator\n * @param symbolID The symbol ID\n * @param symbolBounds The symbol bounds\n * @param centerPoint The center point (will be updated if HQ)\n * @param imageBounds The image bounds (will be updated)\n * @returns The HQ staff result\n */\nexport declare function buildHQStaff(symbolID: string, symbolBounds: Rectangle2D, centerPoint: Point2D, imageBounds: Rectangle2D): HQStaffResult;\n/**\n * Builds the restricted indicator\n * @param symbolID The symbol ID\n * @param symbolBounds The symbol bounds\n * @param imageBounds The image bounds (will be updated)\n * @returns The restricted indicator result\n */\nexport declare function buildRestrictedIndicator(symbolID: string, symbolBounds: Rectangle2D, imageBounds: Rectangle2D): RestrictedResult;\n/**\n * Builds the no strike indicator\n * @param symbolID The symbol ID\n * @param symbolBounds The symbol bounds\n * @param imageBounds The image bounds (will be updated)\n * @returns The no strike indicator result\n */\nexport declare function buildNoStrikeIndicator(symbolID: string, symbolBounds: Rectangle2D, imageBounds: Rectangle2D): NoStrikeResult;\n/**\n * Assembles all display modifiers into SVG format\n * @param sdi The source symbol display info\n * @param centerPoint The center point\n * @param symbolBounds The symbol bounds\n * @param imageBounds The image bounds\n * @param lineColor The line color\n * @param fillColor The fill color\n * @param textColor The text color\n * @param textBackgroundColor The text background color\n * @param strokeWidth The stroke width\n * @param strokeWidthNL The stroke width for non-linear\n * @param alpha The alpha transparency\n * @param ad The angle descriptor\n * @param pixelSize The pixel size\n * @param hqBounds The HQ bounds result\n * @param pt1HQ The first HQ point\n * @param pt2HQ The second HQ point\n * @param ebBounds The engagement bar bounds\n * @param ebRectangle The engagement bar rectangle\n * @param ebColor The engagement bar color\n * @param stiAO The engagement bar text info\n * @param stiAM The affiliation modifier text info\n * @param amBounds The affiliation modifier bounds\n * @param stiEchelon The echelon text info\n * @param echelonBounds The echelon bounds\n * @param tfBounds The task force bounds\n * @param tfRectangle The task force rectangle\n * @param fdiBounds The FDI bounds\n * @param fdiTop The FDI top point\n * @param fdiLeft The FDI left point\n * @param fdiRight The FDI right point\n * @param liBounds The leadership indicator bounds\n * @param liPath The leadership indicator path\n * @param ociBounds The OCI bounds\n * @param ociShape The OCI shape\n * @param ociSlashShape The OCI slash shape\n * @param mobilityBounds The mobility bounds\n * @param mobilityPath The mobility path\n * @param domBounds The DOM arrow bounds\n * @param domPoints The DOM arrow points\n * @param rBounds The restricted indicator bounds\n * @param rPath The restricted path\n * @param rPath2 The restricted path 2\n * @param rCircle The restricted circle\n * @param rStrokeWidth The restricted stroke width\n * @param nsBounds The no strike bounds\n * @param nsCircle The no strike circle\n * @param nsLine The no strike line\n * @param nsStrokeWidth The no strike stroke width\n * @returns The SVG symbol info\n */\nexport declare function assembleSVG(sdi: any, symbolID: string, centerPoint: Point2D, symbolBounds: Rectangle2D, imageBounds: Rectangle2D, lineColor: Color, fillColor: Color, textColor: Color, textBackgroundColor: Color, strokeWidth: number, strokeWidthNL: number, alpha: number, ad: number, pixelSize: number, hqBounds?: Rectangle2D, pt1HQ?: Point2D, pt2HQ?: Point2D, ebBounds?: Rectangle2D, ebRectangle?: Rectangle2D, ebColor?: Color, stiAO?: SVGTextInfo, stiAM?: SVGTextInfo, amBounds?: Rectangle2D, stiEchelon?: SVGTextInfo, echelonBounds?: Rectangle2D, tfBounds?: Rectangle2D, tfRectangle?: Rectangle2D, fdiBounds?: Rectangle2D, fdiTop?: Point2D, fdiLeft?: Point2D, fdiRight?: Point2D, liBounds?: Rectangle2D, liPath?: Path, ociBounds?: Rectangle2D, ociShape?: Rectangle2D, ociSlashShape?: Path, mobilityBounds?: Rectangle2D, mobilityPath?: any[], domBounds?: Rectangle2D, domPoints?: Point2D[], rBounds?: Rectangle2D, rPath?: Path, rPath2?: Path, rCircle?: Ellipse, rStrokeWidth?: number, nsBounds?: Rectangle2D, nsCircle?: Ellipse, nsLine?: Line, nsStrokeWidth?: number): SVGSymbolInfo;\n//# sourceMappingURL=DisplayModifierComposer.d.ts.map","milsym/renderer/modifiers/EchelonTFRenderer.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Path } from \"../shapes/path\";\nimport { SVGTextInfo } from \"../utilities/SVGTextInfo\";\n/**\n * Result interface for echelon, task force, and feint dummy indicator building\n */\nexport interface EchelonTFResult {\n    liPath?: Path;\n    liBounds?: Rectangle2D;\n    liTop?: Point2D;\n    liLeft?: Point2D;\n    liRight?: Point2D;\n    stiEchelon?: SVGTextInfo;\n    echelonBounds?: Rectangle2D;\n    tfRectangle?: Rectangle2D;\n    tfBounds?: Rectangle2D;\n    fdiTop?: Point2D;\n    fdiLeft?: Point2D;\n    fdiRight?: Point2D;\n    fdiBounds?: Rectangle2D;\n}\n/**\n * Builds echelon, task force, and feint dummy indicator modifiers for a symbol.\n * Extracts logic from ModifierRenderer.processUnitDisplayModifiers (lines 472-676).\n *\n * @param symbolID The symbol ID string\n * @param symbolBounds The bounding rectangle of the symbol\n * @param imageBounds The current image bounds (will be updated with new bounds)\n * @param frc The rendering context\n * @param pixelSize The pixel size\n * @returns Object containing the result data and updated imageBounds\n */\nexport declare function buildEchelonTFModifiers(symbolID: string, symbolBounds: Rectangle2D, imageBounds: Rectangle2D, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D, pixelSize: number): {\n    result: EchelonTFResult;\n    imageBounds: Rectangle2D;\n};\n//# sourceMappingURL=EchelonTFRenderer.d.ts.map","milsym/renderer/modifiers/MobilityRenderer.d.ts":"import { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Rectangle } from \"../shapes/rectangle\";\n/**\n * Builds mobility modifier shapes based on symbol ID and bounds\n * @param symbolID The symbol ID\n * @param symbolBounds The bounding rectangle of the symbol\n * @param ad The amplifier descriptor (echelon/mobility)\n * @param strokeWidth The stroke width for rendering\n * @returns Object containing mobilityPath, mobilityPathFill, mobilityBounds, and strokeWidthNL\n */\nexport declare function buildMobilityModifiers(symbolID: string, symbolBounds: Rectangle2D, ad: number, strokeWidth: number): {\n    mobilityPath: Array<any>;\n    mobilityPathFill: Array<any>;\n    mobilityBounds: Rectangle | null;\n    strokeWidthNL: number;\n};\n//# sourceMappingURL=MobilityRenderer.d.ts.map","milsym/renderer/modifiers/ModifierRenderUtils.d.ts":"import { Font } from \"../../graphics/Font\";\nimport { Color } from \"../utilities/Color\";\nimport { SymbolDimensionInfo } from \"../utilities/SymbolDimensionInfo\";\nimport { TextInfo } from \"../utilities/TextInfo\";\n/**\n * Utility function to check if C modifier is on top\n * @param symbolID The symbol ID to check\n * @returns true if C modifier should be rendered on top\n */\nexport declare function isCOnTop(symbolID: string): boolean;\n/**\n * Get Font from attributes or use default\n * @param attributes Map of attributes\n * @returns Font object\n */\nexport declare function getFont(attributes: Map<string, string>): Font;\n/**\n * Get font height and descent values\n * @param font Font to be used\n * @param ctx OPTIONAL: If user wants to re-use a context for performance\n * @returns Array with [height, descent]\n */\nexport declare function getFontHeightandDescent(font: Font, ctx?: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D | null): number[];\n/**\n * Render a single text element to SVG\n * @param tiArray Array of TextInfo elements\n * @param color Text color\n * @param backgroundColor Background color\n * @returns SVG string\n */\nexport declare function renderTextElement(tiArray: Array<TextInfo>, color: Color, backgroundColor: Color): string;\n/**\n * Render multiple text elements to SVG with group styling\n * @param tiArray Array of TextInfo elements\n * @param color Text color\n * @param backgroundColor Background color\n * @returns SVG string\n */\nexport declare function renderTextElements(tiArray: Array<TextInfo>, color: Color, backgroundColor: Color | null): string;\n/**\n * Render text elements to a canvas context\n * @param g2d Canvas rendering context\n * @param tiArray Array of TextInfo elements\n * @param textColor Text color\n * @param textBackgroundColor Background color\n */\nexport declare function renderText(g2d: OffscreenCanvasRenderingContext2D, tiArray: TextInfo[], textColor: Color, textBackgroundColor: Color): void;\n/**\n * Shift unit points and draw modifiers\n * @param tiArray Array of TextInfo elements\n * @param sdi Symbol dimension info\n * @param attributes Map of attributes\n * @returns SVGSymbolInfo or null\n */\nexport declare function shiftUnitPointsAndDraw(tiArray: Array<TextInfo>, sdi: SymbolDimensionInfo, attributes: Map<string, string>): SymbolDimensionInfo;\n//# sourceMappingURL=ModifierRenderUtils.d.ts.map","milsym/renderer/modifiers/OperationalConditionRenderer.d.ts":"import { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Path } from \"../shapes/path\";\n/**\n * Process the Operational Condition Indicator bar for a symbol\n * @param symbolID the symbol ID to extract status from\n * @param symbolBounds the bounds of the symbol\n * @param offsetY the Y offset for the bar\n * @returns a Rectangle2D representing the operational condition bar, or undefined\n */\nexport declare function processOperationalConditionIndicator(symbolID: string, symbolBounds: Rectangle2D, offsetY: number): Rectangle2D | undefined;\n/**\n * Process the Operational Condition Indicator slash (for Damaged/Destroyed status)\n * @param symbolID the symbol ID to extract status from\n * @param symbolBounds the bounds of the symbol\n * @returns a Path representing the slash indicator, or undefined\n */\nexport declare function processOperationalConditionIndicatorSlash(symbolID: string, symbolBounds: Rectangle2D): Path | undefined;\n//# sourceMappingURL=OperationalConditionRenderer.d.ts.map","milsym/renderer/modifiers/SpeedLeaderRenderer.d.ts":"import { SVGSymbolInfo } from \"../utilities/SVGSymbolInfo\";\n/**\n * Renders the speed leader modifier for military symbols.\n * Extracts the speed leader method from ModifierRenderer.\n */\nexport declare function processSpeedLeader(sdi: SVGSymbolInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>): SVGSymbolInfo;\n//# sourceMappingURL=SpeedLeaderRenderer.d.ts.map","milsym/renderer/modifiers/TGSPModifierRenderer.d.ts":"import { Color } from \"../utilities/Color\";\nimport { SymbolDimensionInfo } from \"../utilities/SymbolDimensionInfo\";\n/**\n * Tactical Graphic Single Point (TGSP) Modifier Renderer\n * Extracts and processes modifiers specifically for TGSP symbols\n */\nexport declare function ProcessTGSPWithSpecialModifierLayout(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, lineColor: Color, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo | null;\n/**\n * Process modifiers for action points\n */\nexport declare function ProcessTGSPModifiers(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, lineColor: Color, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo;\n//# sourceMappingURL=TGSPModifierRenderer.d.ts.map","milsym/renderer/modifiers/TextModifierLayout.d.ts":"import { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Modifier } from \"../utilities/Modifier\";\nexport declare function getLabelPositionIndexes(symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>): Array<Modifier> | null;\n/**\n *\n * @param bounds bounds of the core icon\n * @param labelWidth height of the label to be placed\n * @param buffer additional horizontal spacing buffer between label and symbol if desired\n * @param location -1 left, 0 center, 1 right\n * @param modifierFontHeight\n * @returns\n */\nexport declare function getLabelXPosition(bounds: Rectangle2D, labelWidth: number, location: number, modifierFontHeight: number): number;\n/**\n *\n * @param bounds bounds of the core icon\n * @param labelHeight height of the label to be placed\n * @param descent descent of the label to be placed\n * @param bufferText additional vertical spacing buffer between labels if desired\n * @param centered if true, there will be a center label location identified by 0\n * @param location positive 1, 2, 3 to be above symbol mid-point or negative values to be below\n * @returns y position\n */\nexport declare function getLabelYPosition(bounds: Rectangle2D, labelHeight: number, descent: number, bufferText: number, centered: boolean, location: number): number;\n//# sourceMappingURL=TextModifierLayout.d.ts.map","milsym/renderer/modifiers/UnitTextModifierRenderer.d.ts":"import { SymbolDimensionInfo } from \"../utilities/SymbolDimensionInfo\";\n/**\n * Process unknown text modifiers for units\n * @param sdi Symbol dimension info\n * @param symbolID Symbol ID\n * @param modifiers Modifiers map\n * @param attributes Attributes map\n * @param frc Rendering context\n * @returns Updated SymbolDimensionInfo\n */\nexport declare function processUnknownTextModifiers(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo;\n/**\n * Process special (SP) text modifiers for units\n * @param sdi Symbol dimension info\n * @param symbolID Symbol ID\n * @param modifiers Modifiers map\n * @param attributes Attributes map\n * @param frc Rendering context\n * @returns Updated SymbolDimensionInfo\n */\nexport declare function processSPTextModifiers(sdi: SymbolDimensionInfo, symbolID: string, modifiers: Map<string, string>, attributes: Map<string, string>, frc: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D): SymbolDimensionInfo;\n//# sourceMappingURL=UnitTextModifierRenderer.d.ts.map","milsym/renderer/modifiers/index.d.ts":"/**\n * Modifier sub-renderers — Extracted from the monolithic ModifierRenderer.ts\n *\n * Each module handles a specific responsibility area of the modifier rendering pipeline.\n * ModifierRenderer.ts acts as a facade that orchestrates these sub-renderers.\n */\nexport * from './ModifierRenderUtils';\nexport * from './DirectionArrowRenderer';\nexport * from './OperationalConditionRenderer';\nexport * from './SpeedLeaderRenderer';\nexport * from './MobilityRenderer';\nexport * from './EchelonTFRenderer';\nexport * from './DisplayModifierComposer';\nexport * from './TextModifierLayout';\nexport * from './UnitTextModifierRenderer';\nexport * from './TGSPModifierRenderer';\n//# sourceMappingURL=index.d.ts.map","milsym/renderer/shapes/arc.d.ts":"import { Rectangle } from \"./rectangle\";\nexport declare class Arc {\n    private x;\n    private y;\n    private r;\n    private sa;\n    private ea;\n    private rectangle;\n    constructor(x: number, y: number, r: number, sa: number, ea: number);\n    getShapeType(): string;\n    getBounds(): Rectangle;\n    shift(x: number, y: number): void;\n    setPath(context: CanvasRenderingContext2D): void;\n    stroke(context: CanvasRenderingContext2D): void;\n    fill(context: CanvasRenderingContext2D): void;\n}\n//# sourceMappingURL=arc.d.ts.map","milsym/renderer/shapes/bcurve.d.ts":"import { Rectangle } from \"./rectangle\";\nexport declare class BCurve {\n    private x1;\n    private y1;\n    private x2;\n    private y2;\n    private x3;\n    private y3;\n    private x4;\n    private y4;\n    private rectangle;\n    constructor(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number, x4: number, y4: number);\n    getShapeType(): string;\n    getBounds(): Rectangle;\n    shift(x: number, y: number): void;\n    setPath(context: CanvasRenderingContext2D): void;\n    stroke(context: CanvasRenderingContext2D): void;\n    fill(context: CanvasRenderingContext2D): void;\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null): string;\n}\n//# sourceMappingURL=bcurve.d.ts.map","milsym/renderer/shapes/ellipse.d.ts":"import { Rectangle } from \"./rectangle\";\nexport declare class Ellipse {\n    private rectangle;\n    constructor(x: number, y: number, w: number, h: number);\n    getShapeType(): string;\n    getBounds(): Rectangle;\n    shift(x: number, y: number): void;\n    setPath(context: CanvasRenderingContext2D): void;\n    stroke(context: CanvasRenderingContext2D): void;\n    fill(context: CanvasRenderingContext2D): void;\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null): string;\n}\n//# sourceMappingURL=ellipse.d.ts.map","milsym/renderer/shapes/line.d.ts":"import { Point } from \"./point\";\nimport { Rectangle } from \"./rectangle\";\nexport declare class Line {\n    private pt1;\n    private pt2;\n    private rectangle;\n    constructor(x1: number, y1: number, x2: number, y2: number);\n    getShapeType(): string;\n    getBounds(): Rectangle;\n    getP1(): Point;\n    getP2(): Point;\n    shift(x: number, y: number): void;\n    /**\n * Tests if the specified line segment intersects this line segment.\n * @param line the specified <code>Line</code>\n * @return <code>true</code> if this line segment and the specified line\n *\t\t\tsegment intersect each other;\n *\t\t\t<code>false</code> otherwise.\n */\n    intersectsLine(line: Line): boolean;\n    setPath(context: CanvasRenderingContext2D): void;\n    stroke(context: CanvasRenderingContext2D): void;\n    fill(context: CanvasRenderingContext2D): void;\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null): string;\n}\n//# sourceMappingURL=line.d.ts.map","milsym/renderer/shapes/path.d.ts":"import { PathIterator } from \"./pathiterator\";\nimport { Point } from \"./point\";\nimport { Rectangle } from \"./rectangle\";\nexport declare class Path {\n    private _actions;\n    private _dashArray;\n    private _startPoint;\n    private _endPoint;\n    private _lastMoveTo;\n    private _rectangle;\n    private _method;\n    /**\n     * @return {ShapeTypes} ShapeTypes.Path\n     */\n    getShapeType(): string;\n    setLineDash(dashArray: string): void;\n    getBounds(): Rectangle;\n    shift(x: number, y: number): void;\n    /**\n     * The number of this._actions on the path\n     */\n    getLength(): void;\n    /**\n     * Adds a point to the path by moving to the specified coordinates specified\n     * @param x\n     * @param y\n     */\n    moveTo(x: number, y: number): void;\n    /**\n     * Adds a point to the path by drawing a straight line from the current\n     * coordinates to the new specified coordinates specified\n     * @param x\n     * @param y\n     */\n    lineTo(x: number, y: number): void;\n    /**\n     * Adds a point to the path by drawing a straight line from the current\n     * coordinates to the new specified coordinates specified\n     * @param x\n     * @param y\n     * @param pattern\n     */\n    dashedLineTo(x: number, y: number, pattern: Array<number>): void;\n    /**\n     * Adds a curved segment, defined by three new points, to the path by\n     * drawing a Bézier curve that intersects both the current coordinates\n     * and the specified coordinates (x,y), using the specified points\n     * (cp1x,xp1y) and (cp2x,cp2y) as Bézier control points.\n     * @param cp1x\n     * @param cp1y\n     * @param cp2x\n     * @param cp2y\n     * @param x\n     * @param y\n     */\n    bezierCurveTo(cp1x: number, cp1y: number, cp2x: number, cp2y: number, x: number, y: number): void;\n    /**\n     * Adds a curved segment, defined by two new points, to the path by\n     * drawing a Quadratic curve that intersects both the current\n     * coordinates and the specified coordinates (x,y), using the\n     * specified point (cpx,cpy) as a quadratic parametric control point.\n     * @param cpx\n     * @param cpy\n     * @param x\n     * @param y\n     * @returns\n     */\n    quadraticCurveTo(cpx: number, cpy: number, x: number, y: number): void;\n    /**\n     * The arcTo() method creates an arc/curve between two tangents on the canvas.\n     * @param x1 The x-coordinate of the beginning of the arc\n     * @param y1 The y-coordinate of the beginning of the arc\n     * @param x2 The x-coordinate of the end of the arc\n     * @param y2 The y-coordinate of the end of the arc\n     * @param r The radius of the arc\n     * @returns\n     */\n    arcTo(x1: number, y1: number, x2: number, y2: number, r: number): void;\n    /**\n     * The arc() method creates an arc/curve\n     * (use to create circles. or parts of circles).\n     * @param x The x-coordinate of the center of the circle\n     * @param y The y-coordinate of the center of the circle\n     * @param r The radius of the circle\n     * @param sAngle The starting angle, in degrees\n     * (0 is at the 3 -'clock position of the arc's circle)\n     * @param eAngle The ending angle, in degrees\n     * @param counterclockwise Optional. Specifies wheter the drawing\n     * should be counterclockwise or clockwise.  False=clockwise,\n     * true=counter-clockwise;\n     * @returns\n     */\n    arc(x: number, y: number, r: number, sAngle: number, eAngle: number, counterclockwise: boolean): void;\n    /**\n     * Closes the current subpath by drawing a straight line back to the coordinates of the last moveTo.\n     * @return\n     */\n    closePath(): void;\n    /**\n     * @return Point\n     */\n    getCurrentPoint(): Point;\n    /**\n     * @return PathIterator\n     */\n    getPathIterator(): PathIterator;\n    /**\n     * Apply the path to the passed context (doesn't draw)\n     * @param context\n     * @return\n     */\n    private setPath;\n    /**\n     * Draws the path to the passed context\n     * @param context\n     * @return\n     */\n    stroke(context: OffscreenCanvasRenderingContext2D): void;\n    /**\n     * Fills the path on the passed context\n     * @param context\n     * @return\n     */\n    fill(context: OffscreenCanvasRenderingContext2D): void;\n    fillPattern(context: OffscreenCanvasRenderingContext2D, fillPattern: CanvasImageSource): void;\n    /**\n     * Arc and ArcTo do not covert currently\n     */\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null, strokeOpacity?: number, fillOpacity?: number, lineCap?: string | null, svgFormat?: number): string;\n}\n//# sourceMappingURL=path.d.ts.map","milsym/renderer/shapes/pathiterator.d.ts":"export declare class PathIterator {\n    private arrActions;\n    private size;\n    private index;\n    constructor(actions: Array<any>);\n    /**\n     * Tests if the iteration is complete.\n     *\n     */\n    isDone(): boolean;\n    /**\n     * Moves the iterator to the next segment of the path forwards\n     * along the primary direction of traversal as long as there are\n     * more points in that direction.\n     */\n    next(): void;\n    /**\n     * Returns the coordinates and type of the current path segment in\n     * the iteration.\n     * The return value is the path-segment type:\n     * MOVE_TO, LINE_TO, QUAD_TO, CURVE_TO, or CLOSE.\n     * A float array of length 6 must be passed in and can be used to\n     * store the coordinates of the point(s).\n     * Each point is stored as a pair of float x,y coordinates.\n     * MOVE_TO and LINE_TO types returns one point,\n     * QUAD_TO returns two points,\n     * CURVE_TO returns 3 points.\n     * ARC_TO\n     * ARC\n     * CLOSE will never get returned as it is replace with LINE_TO.\n     * @return the path-segment where the first index is the segment type.\n     * @see ActionTypes\n     */\n    currentSegment(): any[];\n}\n//# sourceMappingURL=pathiterator.d.ts.map","milsym/renderer/shapes/point.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nexport declare class Point {\n    x: number;\n    y: number;\n    /**\n     *\n     * @param x\n     * @param y\n     */\n    constructor(x: number, y: number);\n    /**\n     * Returns a string representing one of the shape types\n     * from \"armyc2.c2sd.renderer.so.ShapeTypes\"\n     * @returns {String}\n     */\n    getShapeType(): string;\n    /**\n     *\n     * @returns {Number}\n     */\n    getX(): number;\n    /**\n     *\n     * @returns {Number}\n     */\n    getY(): number;\n    /**\n     * Reset the x & y of this point object.\n     * @param {Number} x\n     * @param {Number} y\n     * @returns {void}\n     */\n    setLocation(x: number, y: number): void;\n    /**\n     * move x & y by specified amounts.\n     * @param {Number} x shift x point by this value\n     * @param {Number} y shift y point by this value\n     * @returns {void}\n     */\n    shift(x: number, y: number): void;\n    /**\n     * @returns {String} like \"{x:#,y:#}\"\n     */\n    toStringFormatted(): string;\n    /**\n     * Makes a copy of this point object.\n     * @returns {armyc2.c2sd.renderer.so.Point} Copy of original point.\n     */\n    clone(): Point;\n    /**\n     * @param {CanvasRenderingContext2D} context object from html5 canvas\n     * @returns {void}\n     */\n    setPath(context: CanvasRenderingContext2D): void;\n    /**\n     * @param {context} context object from html5 canvas\n     * @returns {void}\n     */\n    stroke(context: CanvasRenderingContext2D): void;\n    /**\n     * @param {context} context object from html5 canvas\n     * @returns {void}\n     */\n    fill(context: CanvasRenderingContext2D): void;\n    toPoint2D(): Point2D;\n}\n//# sourceMappingURL=point.d.ts.map","milsym/renderer/shapes/rectangle.d.ts":"import { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Point } from \"./point\";\nexport declare class Rectangle {\n    private x;\n    private y;\n    private width;\n    private height;\n    private bottom;\n    private right;\n    static readonly OUT_LEFT = 1;\n    static readonly OUT_TOP = 2;\n    static readonly OUT_RIGHT = 4;\n    static readonly OUT_BOTTOM = 8;\n    constructor(x: number, y: number, width: number, height: number);\n    getShapeType(): string;\n    getBounds(): Rectangle;\n    toRectangle2D(): Rectangle2D;\n    getX(): number;\n    getY(): number;\n    getWidth(): number;\n    getHeight(): number;\n    getBottom(): number;\n    getRight(): number;\n    getCenterX(): number;\n    /**\n     *\n     * @returns {Number}\n     */\n    getCenterY(): number;\n    /**\n     * setLocation x,y (top,left) while maintaining the width and height.\n     * @param x\n     * @param y\n     */\n    setLocation(x: number, y: number): void;\n    isEmpty(): boolean;\n    shift(x: number, y: number): void;\n    /**\n     * moves top,left points leaving bottom,right intact.\n     * adjusts the height & width values as necessary\n     * @param x\n     * @param y\n     */\n    shiftTL(x: number, y: number): void;\n    /**\n     * moves bottom,right points leaving top,left intact.\n     * adjusts the height & width values as necessary\n     * @param {type} x the amount to move the right point by\n     * @param {type} y the amount to move the bottom point by\n     * @returns {_L7.Anonym$0.Rectangle.shiftTL}\n     */\n    shiftBR(x: number, y: number): void;\n    /**\n     * Grow the rectangle by this many pixels in every direction\n     * @param pixel\n     */\n    grow(pixel: number): void;\n    /**\n     * Will merge the bounds of two rectangle.\n     * @param rect\n     */\n    union(rect: Rectangle): void;\n    unionPoint(point: Point): void;\n    /**\n     * if 2 values passed in, they are assumed to be the x,y of a point.\n     * if 4 values passed in, they are assumed to be the x,y,w,h values\n     * of a Rectangle.\n     * @param x\n     * @param y\n     * @param w\n     * @param h\n     * @returns\n     */\n    contains(x: number, y: number, w?: number, h?: number): boolean;\n    /**\n     *\n     * @param point\n     * @returns\n     */\n    containsPoint(point: Point): boolean;\n    containsRectangle(rect: Rectangle): boolean;\n    /**\n     * Ported from Java\n     */\n    private outcode;\n    /**\n    * Tests if the specified line segment intersects the interior of this\n    * <code>Rectangle</code>. Ported from java.\n    *\n    * @param x1 the X coordinate of the start point of the specified\n    *           line segment\n    * @param y1 the Y coordinate of the start point of the specified\n    *           line segment\n    * @param x2 the X coordinate of the end point of the specified\n    *           line segment\n    * @param y2 the Y coordinate of the end point of the specified\n    *           line segment\n    * @return <code>true</code> if the specified line segment intersects\n    * the interior of this <code>Rectangle</code>; <code>false</code>\n    * otherwise.\n    */\n    intersectsLine(x1: number, y1: number, x2: number, y2: number): boolean;\n    setPath(context: OffscreenCanvasRenderingContext2D | any): void;\n    stroke(context: OffscreenCanvasRenderingContext2D): void;\n    fill(context: OffscreenCanvasRenderingContext2D): void;\n    clone(): Rectangle;\n    /**\n     * ported from java\n     */\n    intersects(r: Rectangle): boolean;\n    /**\n     *\n     * @param stroke named color or value of rgb(#,#,#)\n     * @param strokeWidth width of line in # of pixels\n     * @param fill named color or value of rgb(#,#,#)\n     * @returns\n     */\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null): string;\n}\n//# sourceMappingURL=rectangle.d.ts.map","milsym/renderer/shapes/roundedrectangle.d.ts":"import { Rectangle } from \"./rectangle\";\nexport declare class RoundedRectangle {\n    private radius;\n    private rectangle;\n    constructor(x: number, y: number, w: number, h: number, radius: number);\n    /**\n     *\n     */\n    getShapeType(): string;\n    /**\n     *\n     */\n    getBounds(): Rectangle;\n    shift(x: number, y: number): void;\n    setPath(context: CanvasRenderingContext2D): void;\n    stroke(context: CanvasRenderingContext2D): void;\n    fill(context: CanvasRenderingContext2D): void;\n    toSVGElement(stroke: string | null, strokeWidth: number, fill: string | null): string;\n}\n//# sourceMappingURL=roundedrectangle.d.ts.map","milsym/renderer/shapes/types.d.ts":"export declare class ShapeTypes {\n    static readonly RECTANGLE = \"RECTANGLE\";\n    static readonly POINT = \"POINT\";\n    static readonly ELLIPSE = \"ELLIPSE\";\n    static readonly ROUNDED_RECTANGLE = \"ROUNDED_RECTANGLE\";\n    static readonly LINE = \"LINE\";\n    static readonly BCURVE = \"BCURVE\";\n    static readonly ARC = \"ARC\";\n    static readonly PATH = \"PATH\";\n}\nexport declare class ActionTypes {\n    static readonly ACTION_MOVE_TO = 0;\n    static readonly ACTION_LINE_TO = 1;\n    static readonly ACTION_CURVE_TO = 2;\n    static readonly ACTION_QUAD_TO = 3;\n    static readonly ACTION_ARC_TO = 4;\n    static readonly ACTION_ARC = 5;\n    static readonly ACTION_DASHED_LINE_TO = 6;\n}\n//# sourceMappingURL=types.d.ts.map","milsym/renderer/shapes/utilities.d.ts":"import { Rectangle } from \"./rectangle\";\nexport declare class ShapeUtilties {\n    /**\n    * Returns an indicator of where the specified point\n    * {@code (px,py)} lies with respect to the line segment from\n    * {@code (x1,y1)} to {@code (x2,y2)}.\n    * The return value can be either 1, -1, or 0 and indicates\n    * in which direction the specified line must pivot around its\n    * first end point, {@code (x1,y1)}, in order to point at the\n    * specified point {@code (px,py)}.\n    * <p>A return value of 1 indicates that the line segment must\n    * turn in the direction that takes the positive X axis towards\n    * the negative Y axis.  In the default coordinate system used by\n    * Java 2D, this direction is counterclockwise.\n    * <p>A return value of -1 indicates that the line segment must\n    * turn in the direction that takes the positive X axis towards\n    * the positive Y axis.  In the default coordinate system, this\n    * direction is clockwise.\n    * <p>A return value of 0 indicates that the point lies\n    * exactly on the line segment.  Note that an indicator value\n    * of 0 is rare and not useful for determining colinearity\n    * because of floating point rounding issues.\n    * <p>If the point is colinear with the line segment, but\n    * not between the end points, then the value will be -1 if the point\n    * lies \"beyond {@code (x1,y1)}\" or 1 if the point lies\n    * \"beyond {@code (x2,y2)}\".\n    *\n    * @param x1 the X coordinate of the start point of the\n    *           specified line segment\n    * @param y1 the Y coordinate of the start point of the\n    *           specified line segment\n    * @param x2 the X coordinate of the end point of the\n    *           specified line segment\n    * @param y2 the Y coordinate of the end point of the\n    *           specified line segment\n    * @param px the X coordinate of the specified point to be\n    *           compared with the specified line segment\n    * @param py the Y coordinate of the specified point to be\n    *           compared with the specified line segment\n    * @return an integer that indicates the position of the third specified\n    *\t\t\tcoordinates with respect to the line segment formed\n    *\t\t\tby the first two specified coordinates.\n    * @since 1.2\n    */\n    static relativeCCW(x1: number, y1: number, x2: number, y2: number, px: number, py: number): 0 | 1 | -1;\n    static linesIntersect(x1: number, y1: number, x2: number, y2: number, x3: number, y3: number, x4: number, y4: number): boolean;\n    /**\n    * Intersects the pair of specified source <code>Rectangle</code>\n    * objects and puts the result into the specified destination\n    * <code>Rectangle</code> object.  One of the source rectangles\n    * can also be the destination to avoid creating a third Rectangle2D\n    * object, but in this case the original points of this source\n    * rectangle will be overwritten by this method.\n    * @param src1 the first of a pair of <code>Rectangle</code>\n    * objects to be intersected with each other\n    * @param src2 the second of a pair of <code>Rectangle</code>\n    * objects to be intersected with each other\n    * @return the <code>Rectangle</code> that holds the\n    * results of the intersection of <code>src1</code> and\n    * <code>src2</code>\n    * @since 1.2\n    */\n    static intersectRects(src1: Rectangle, src2: Rectangle): Rectangle;\n}\n//# sourceMappingURL=utilities.d.ts.map","milsym/renderer/utilities/AffiliationColors.d.ts":"import { Color } from \"./Color\";\n/**\n * Default Affiliation Colors for the symbols\n *\n */\nexport declare class AffiliationColors {\n    static FriendlyUnitFillColor: Color;\n    static HostileUnitFillColor: Color;\n    static NeutralUnitFillColor: Color;\n    static UnknownUnitFillColor: Color;\n    static SuspectUnitFillColor: Color;\n    static FriendlyGraphicFillColor: Color;\n    static HostileGraphicFillColor: Color;\n    static NeutralGraphicFillColor: Color;\n    static UnknownGraphicFillColor: Color;\n    static SuspectGraphicFillColor: Color;\n    static FriendlyUnitLineColor: Color;\n    static HostileUnitLineColor: Color;\n    static NeutralUnitLineColor: Color;\n    static UnknownUnitLineColor: Color;\n    static SuspectUnitLineColor: Color;\n    static FriendlyGraphicLineColor: Color;\n    static HostileGraphicLineColor: Color;\n    static NeutralGraphicLineColor: Color;\n    static UnknownGraphicLineColor: Color;\n    static SuspectGraphicLineColor: Color;\n    static ObstacleGreenDark: Color;\n    static ObstacleGreen: Color;\n    static WeatherRed: Color;\n    static WeatherBlue: Color;\n    static WeatherPurpleDark: Color;\n    static WeatherPurpleLight: Color;\n    static WeatherBrownDark: Color;\n    static WeatherBrownLight: Color;\n}\n//# sourceMappingURL=AffiliationColors.d.ts.map","milsym/renderer/utilities/CanvasUtilities.d.ts":"/**\n * Utility methods that reference Canvas\n * Not exported in index.ts to hide Canvas import from client\n */\nexport declare class CanvasUtilities {\n    static getCanvas(width: number, height: number): OffscreenCanvas | any;\n    /**\n     *\n     * @param canvas\n     * @returns OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D\n     */\n    static getContext(canvas: OffscreenCanvas | any): any;\n}\n//# sourceMappingURL=CanvasUtilities.d.ts.map","milsym/renderer/utilities/ChunkRegistry.d.ts":"/**\n * AUTO-GENERATED by scripts/split-json.js — do not edit manually.\n * Generated at: 2026-03-21T12:44:39.056Z\n *\n * Maps symbol set codes to dynamic import() functions for lazy loading.\n * Bundlers (Vite, webpack, Rollup) will code-split on these boundaries.\n */\nexport type ChunkLoader = () => Promise<any>;\n/** SVG data chunk loaders by version and symbol set */\nexport declare const svgChunkRegistry: Record<string, Record<string, ChunkLoader>>;\n/** MS (symbol definition) chunk loaders by version and symbol set */\nexport declare const msChunkRegistry: Record<string, Record<string, ChunkLoader>>;\n/** Chunk metadata from manifest (element counts and byte sizes) */\nexport declare const chunkManifest: {\n    readonly '2525d': {\n        readonly svg: {\n            readonly '10': {\n                readonly count: 596;\n                readonly bytes: 362891;\n            };\n            readonly '11': {\n                readonly count: 51;\n                readonly bytes: 22498;\n            };\n            readonly '12': {\n                readonly count: 6;\n                readonly bytes: 4613;\n            };\n            readonly '13': {\n                readonly count: 12;\n                readonly bytes: 10323;\n            };\n            readonly '15': {\n                readonly count: 258;\n                readonly bytes: 193229;\n            };\n            readonly '20': {\n                readonly count: 173;\n                readonly bytes: 201513;\n            };\n            readonly '25': {\n                readonly count: 629;\n                readonly bytes: 640527;\n            };\n            readonly '30': {\n                readonly count: 148;\n                readonly bytes: 56696;\n            };\n            readonly '31': {\n                readonly count: 13;\n                readonly bytes: 8100;\n            };\n            readonly '32': {\n                readonly count: 6;\n                readonly bytes: 4890;\n            };\n            readonly '33': {\n                readonly count: 14;\n                readonly bytes: 9367;\n            };\n            readonly '35': {\n                readonly count: 59;\n                readonly bytes: 18214;\n            };\n            readonly '36': {\n                readonly count: 126;\n                readonly bytes: 91531;\n            };\n            readonly '40': {\n                readonly count: 194;\n                readonly bytes: 164198;\n            };\n            readonly '41': {\n                readonly count: 14;\n                readonly bytes: 11837;\n            };\n            readonly '42': {\n                readonly count: 7;\n                readonly bytes: 5933;\n            };\n            readonly '43': {\n                readonly count: 13;\n                readonly bytes: 10438;\n            };\n            readonly '45': {\n                readonly count: 179;\n                readonly bytes: 119255;\n            };\n            readonly '46': {\n                readonly count: 181;\n                readonly bytes: 113888;\n            };\n            readonly '50': {\n                readonly count: 36;\n                readonly bytes: 8863;\n            };\n            readonly '51': {\n                readonly count: 59;\n                readonly bytes: 13654;\n            };\n            readonly '52': {\n                readonly count: 57;\n                readonly bytes: 13264;\n            };\n            readonly '53': {\n                readonly count: 53;\n                readonly bytes: 12426;\n            };\n            readonly '54': {\n                readonly count: 42;\n                readonly bytes: 10114;\n            };\n            readonly '60': {\n                readonly count: 59;\n                readonly bytes: 22459;\n            };\n            readonly '61': {\n                readonly count: 14;\n                readonly bytes: 12059;\n            };\n            readonly '62': {\n                readonly count: 7;\n                readonly bytes: 6044;\n            };\n            readonly '63': {\n                readonly count: 14;\n                readonly bytes: 12018;\n            };\n            readonly '98': {\n                readonly count: 1;\n                readonly bytes: 287;\n            };\n            readonly common: {\n                readonly count: 685;\n                readonly bytes: 580167;\n            };\n            readonly '05': {\n                readonly count: 46;\n                readonly bytes: 28700;\n            };\n            readonly '01': {\n                readonly count: 104;\n                readonly bytes: 26656;\n            };\n            readonly '00': {\n                readonly count: 1;\n                readonly bytes: 270;\n            };\n            readonly '02': {\n                readonly count: 26;\n                readonly bytes: 7218;\n            };\n            readonly '06': {\n                readonly count: 20;\n                readonly bytes: 5650;\n            };\n        };\n        readonly ms: {\n            readonly '10': {\n                readonly count: 203;\n                readonly bytes: 13865;\n            };\n            readonly '11': {\n                readonly count: 12;\n                readonly bytes: 827;\n            };\n            readonly '15': {\n                readonly count: 230;\n                readonly bytes: 15149;\n            };\n            readonly '20': {\n                readonly count: 129;\n                readonly bytes: 9132;\n            };\n            readonly '25': {\n                readonly count: 598;\n                readonly bytes: 75057;\n            };\n            readonly '30': {\n                readonly count: 94;\n                readonly bytes: 6299;\n            };\n            readonly '35': {\n                readonly count: 21;\n                readonly bytes: 1492;\n            };\n            readonly '36': {\n                readonly count: 66;\n                readonly bytes: 5061;\n            };\n            readonly '40': {\n                readonly count: 152;\n                readonly bytes: 10031;\n            };\n            readonly '45': {\n                readonly count: 205;\n                readonly bytes: 25553;\n            };\n            readonly '46': {\n                readonly count: 206;\n                readonly bytes: 25376;\n            };\n            readonly '47': {\n                readonly count: 1;\n                readonly bytes: 101;\n            };\n            readonly '50': {\n                readonly count: 5;\n                readonly bytes: 329;\n            };\n            readonly '51': {\n                readonly count: 5;\n                readonly bytes: 329;\n            };\n            readonly '52': {\n                readonly count: 5;\n                readonly bytes: 329;\n            };\n            readonly '53': {\n                readonly count: 5;\n                readonly bytes: 329;\n            };\n            readonly '54': {\n                readonly count: 5;\n                readonly bytes: 329;\n            };\n            readonly '60': {\n                readonly count: 7;\n                readonly bytes: 528;\n            };\n            readonly '01': {\n                readonly count: 52;\n                readonly bytes: 3674;\n            };\n            readonly '02': {\n                readonly count: 2;\n                readonly bytes: 145;\n            };\n            readonly '05': {\n                readonly count: 35;\n                readonly bytes: 2364;\n            };\n            readonly '06': {\n                readonly count: 2;\n                readonly bytes: 145;\n            };\n        };\n    };\n    readonly '2525e': {\n        readonly svg: {\n            readonly '10': {\n                readonly count: 581;\n                readonly bytes: 360817;\n            };\n            readonly '11': {\n                readonly count: 51;\n                readonly bytes: 22676;\n            };\n            readonly '12': {\n                readonly count: 6;\n                readonly bytes: 4613;\n            };\n            readonly '13': {\n                readonly count: 12;\n                readonly bytes: 10323;\n            };\n            readonly '15': {\n                readonly count: 224;\n                readonly bytes: 164691;\n            };\n            readonly '20': {\n                readonly count: 180;\n                readonly bytes: 189896;\n            };\n            readonly '25': {\n                readonly count: 649;\n                readonly bytes: 668456;\n            };\n            readonly '27': {\n                readonly count: 91;\n                readonly bytes: 32601;\n            };\n            readonly '30': {\n                readonly count: 146;\n                readonly bytes: 56416;\n            };\n            readonly '31': {\n                readonly count: 13;\n                readonly bytes: 8100;\n            };\n            readonly '32': {\n                readonly count: 6;\n                readonly bytes: 4890;\n            };\n            readonly '33': {\n                readonly count: 14;\n                readonly bytes: 9367;\n            };\n            readonly '35': {\n                readonly count: 59;\n                readonly bytes: 18214;\n            };\n            readonly '36': {\n                readonly count: 126;\n                readonly bytes: 91531;\n            };\n            readonly '40': {\n                readonly count: 189;\n                readonly bytes: 179749;\n            };\n            readonly '41': {\n                readonly count: 14;\n                readonly bytes: 11837;\n            };\n            readonly '42': {\n                readonly count: 7;\n                readonly bytes: 5933;\n            };\n            readonly '43': {\n                readonly count: 13;\n                readonly bytes: 10438;\n            };\n            readonly '45': {\n                readonly count: 179;\n                readonly bytes: 119272;\n            };\n            readonly '46': {\n                readonly count: 181;\n                readonly bytes: 113911;\n            };\n            readonly '50': {\n                readonly count: 68;\n                readonly bytes: 15536;\n            };\n            readonly '51': {\n                readonly count: 55;\n                readonly bytes: 11673;\n            };\n            readonly '52': {\n                readonly count: 53;\n                readonly bytes: 11283;\n            };\n            readonly '53': {\n                readonly count: 49;\n                readonly bytes: 10445;\n            };\n            readonly '54': {\n                readonly count: 38;\n                readonly bytes: 8133;\n            };\n            readonly '60': {\n                readonly count: 100;\n                readonly bytes: 76330;\n            };\n            readonly '61': {\n                readonly count: 14;\n                readonly bytes: 12059;\n            };\n            readonly '62': {\n                readonly count: 7;\n                readonly bytes: 6044;\n            };\n            readonly '63': {\n                readonly count: 14;\n                readonly bytes: 12018;\n            };\n            readonly '98': {\n                readonly count: 1;\n                readonly bytes: 287;\n            };\n            readonly common: {\n                readonly count: 585;\n                readonly bytes: 500815;\n            };\n            readonly '05': {\n                readonly count: 50;\n                readonly bytes: 30798;\n            };\n            readonly '01': {\n                readonly count: 102;\n                readonly bytes: 26456;\n            };\n            readonly '00': {\n                readonly count: 1;\n                readonly bytes: 270;\n            };\n            readonly '02': {\n                readonly count: 27;\n                readonly bytes: 7564;\n            };\n            readonly '06': {\n                readonly count: 21;\n                readonly bytes: 5962;\n            };\n        };\n        readonly ms: {\n            readonly '10': {\n                readonly count: 195;\n                readonly bytes: 13335;\n            };\n            readonly '11': {\n                readonly count: 12;\n                readonly bytes: 827;\n            };\n            readonly '15': {\n                readonly count: 197;\n                readonly bytes: 13088;\n            };\n            readonly '20': {\n                readonly count: 132;\n                readonly bytes: 9299;\n            };\n            readonly '25': {\n                readonly count: 599;\n                readonly bytes: 81443;\n            };\n            readonly '27': {\n                readonly count: 45;\n                readonly bytes: 2975;\n            };\n            readonly '30': {\n                readonly count: 95;\n                readonly bytes: 6380;\n            };\n            readonly '35': {\n                readonly count: 21;\n                readonly bytes: 1492;\n            };\n            readonly '36': {\n                readonly count: 65;\n                readonly bytes: 5001;\n            };\n            readonly '40': {\n                readonly count: 148;\n                readonly bytes: 9761;\n            };\n            readonly '45': {\n                readonly count: 205;\n                readonly bytes: 28207;\n            };\n            readonly '46': {\n                readonly count: 206;\n                readonly bytes: 27931;\n            };\n            readonly '47': {\n                readonly count: 1;\n                readonly bytes: 101;\n            };\n            readonly '50': {\n                readonly count: 5;\n                readonly bytes: 329;\n            };\n            readonly '60': {\n                readonly count: 41;\n                readonly bytes: 2556;\n            };\n            readonly '01': {\n                readonly count: 54;\n                readonly bytes: 3814;\n            };\n            readonly '02': {\n                readonly count: 2;\n                readonly bytes: 145;\n            };\n            readonly '05': {\n                readonly count: 36;\n                readonly bytes: 2425;\n            };\n            readonly '06': {\n                readonly count: 2;\n                readonly bytes: 145;\n            };\n        };\n    };\n};\n//# sourceMappingURL=ChunkRegistry.d.ts.map","milsym/renderer/utilities/Color.d.ts":"/**\n * Integer based Color class with utility functions\n */\nexport declare class Color {\n    /**\n     * The color white.  In the default sRGB space.\n     */\n    static readonly white: Color;\n    /**\n     * The color white.  In the default sRGB space.\n     *\n     */\n    static readonly WHITE: Color;\n    /**\n     * The color light gray.  In the default sRGB space.\n     */\n    static readonly lightGray: Color;\n    /**\n     * The color light gray.  In the default sRGB space.\n     *\n     */\n    static readonly LIGHT_GRAY: Color;\n    /**\n     * The color gray.  In the default sRGB space.\n     */\n    static readonly gray: Color;\n    /**\n     * The color gray.  In the default sRGB space.\n     *\n     */\n    static readonly GRAY: Color;\n    /**\n     * The color dark gray.  In the default sRGB space.\n     */\n    static readonly darkGray: Color;\n    /**\n     * The color dark gray.  In the default sRGB space.\n     *\n     */\n    static readonly DARK_GRAY: Color;\n    /**\n     * The color black.  In the default sRGB space.\n     */\n    static readonly black: Color;\n    /**\n     * The color black.  In the default sRGB space.\n     *\n     */\n    static readonly BLACK: Color;\n    /**\n     * The color red.  In the default sRGB space.\n     */\n    static readonly red: Color;\n    /**\n     * The color red.  In the default sRGB space.\n     *\n     */\n    static readonly RED: Color;\n    /**\n     * The color pink.  In the default sRGB space.\n     */\n    static readonly pink: Color;\n    /**\n     * The color pink.  In the default sRGB space.\n     *\n     */\n    static readonly PINK: Color;\n    /**\n     * The color orange.  In the default sRGB space.\n     */\n    static readonly orange: Color;\n    /**\n     * The color orange.  In the default sRGB space.\n     *\n     */\n    static readonly ORANGE: Color;\n    /**\n     * The color yellow.  In the default sRGB space.\n     */\n    static readonly yellow: Color;\n    /**\n     * The color yellow.  In the default sRGB space.\n     *\n     */\n    static readonly YELLOW: Color;\n    /**\n     * The color green.  In the default sRGB space.\n     */\n    static readonly green: Color;\n    /**\n     * The color green.  In the default sRGB space.\n     *\n     */\n    static readonly GREEN: Color;\n    /**\n     * The color magenta.  In the default sRGB space.\n     */\n    static readonly magenta: Color;\n    /**\n     * The color magenta.  In the default sRGB space.\n     *\n     */\n    static readonly MAGENTA: Color;\n    /**\n     * The color cyan.  In the default sRGB space.\n     */\n    static readonly cyan: Color;\n    /**\n     * The color cyan.  In the default sRGB space.\n     *\n     */\n    static readonly CYAN: Color;\n    /**\n     * The color blue.  In the default sRGB space.\n     */\n    static readonly blue: Color;\n    /**\n     * The color blue.  In the default sRGB space.\n     *\n     */\n    static readonly BLUE: Color;\n    private _A;\n    private _R;\n    private _G;\n    private _B;\n    /**\n *\n * @param hexValue - String representing hex value (formatted \"0xRRGGBB\"\n * i.e. \"0xFFFFFF\") OR formatted \"0xAARRGGBB\" i.e. \"0x00FFFFFF\" for a color\n * with an alpha value I will also put up with \"RRGGBB\" and \"AARRGGBB\"\n * without the starting \"0x\" or \"#\"\n * @return\n */\n    private getColorsFromHexString;\n    constructor();\n    constructor(color: Color);\n    constructor(color: number);\n    constructor(hexString: string);\n    constructor(R: number, G: number, B: number);\n    constructor(R: number, G: number, B: number, A: number);\n    static makeColor(r: number, g: number, b: number): Color;\n    toRGB(): number;\n    toARGB(): number;\n    private convert;\n    /**\n     * A hex string in the format of AARRGGBB\n     * @param {Boolean} withAlpha Optional, default is true. If set to false,\n     * will return a hex string without alpha values.\n     */\n    toHexString(withAlpha?: boolean): string;\n    toString(): string;\n    getRed(): number;\n    getGreen(): number;\n    getBlue(): number;\n    getAlpha(): number;\n    /**\n     *\n     * @param alpha 0-255\n     */\n    setAlpha(alpha: number): void;\n    toInt(): number;\n    /**\n     * get alpha value from uint\n     * */\n    private getAlphaFromColor;\n    /**\n     * get red value from uint\n     * */\n    private getRedFromColor;\n    /**\n     * get green value from uint\n     * */\n    private getGreenFromColor;\n    /**\n     * get blue value from uint\n     * */\n    private getBlueFromColor;\n}\n//# sourceMappingURL=Color.d.ts.map","milsym/renderer/utilities/DataLoader.d.ts":"import type { IDataLoader, DataLoaderStats } from './IDataLoader';\n/**\n * Generic LRU data chunk cache for lazy-loading symbol set data.\n *\n * Supports two data types:\n *   - 'svg': SVG element chunks from svgd/svge split files\n *   - 'ms':  Symbol definition chunks from msd/mse split files\n *\n * Usage:\n *   const loader = DataLoader.getInstance();\n *   const svgData = await loader.loadSymbolSet('10', '2525d');  // Land Unit SVGs\n *   const msData  = await loader.loadMSSymbolSet('10', '2525d'); // Land Unit definitions\n */\nexport declare class DataLoader implements IDataLoader<any> {\n    private static _instance;\n    /** LRU caches keyed by \"version:type:symbolSet\" e.g. \"2525d:svg:10\" */\n    private _cache;\n    private _maxChunks;\n    /** In-flight loading promises to prevent duplicate loads */\n    private _pending;\n    /** Track total estimated memory usage */\n    private _totalBytes;\n    private constructor();\n    static getInstance(maxChunks?: number): DataLoader;\n    /**\n     * Reset the singleton (useful for testing).\n     */\n    static resetInstance(): void;\n    /**\n     * Load SVG data for a symbol set. Returns the SVGElements array.\n     * Common SVG data (frames, amplifiers) is auto-loaded as a dependency.\n     */\n    loadSymbolSet(symbolSet: string, version: string): Promise<any>;\n    /**\n     * Load MS (symbol definition) data for a symbol set.\n     */\n    loadMSSymbolSet(symbolSet: string, version: string): Promise<any>;\n    private _loadChunk;\n    private _doLoad;\n    private _getByteSize;\n    private _ensureCapacity;\n    private _evictByKey;\n    preload(symbolSets: string[], version: string): Promise<void>;\n    getMemoryUsage(): DataLoaderStats;\n    evict(symbolSet: string, version: string): boolean;\n    clear(): void;\n    /**\n     * Check if a symbol set's SVG data is already loaded (cached).\n     */\n    isSVGLoaded(symbolSet: string, version: string): boolean;\n    /**\n     * Check if a symbol set's MS data is already loaded (cached).\n     */\n    isMSLoaded(symbolSet: string, version: string): boolean;\n    /**\n     * Get cached SVG data synchronously. Returns null if not loaded yet.\n     * Use loadSymbolSet() for async loading.\n     */\n    getCachedSVG(symbolSet: string, version: string): any | null;\n    /**\n     * Get cached MS data synchronously. Returns null if not loaded yet.\n     */\n    getCachedMS(symbolSet: string, version: string): any | null;\n    /**\n     * Load a chunk synchronously by storing it directly (for migration from eager loading).\n     * This allows existing code to inject data without async loading.\n     */\n    injectChunk(type: 'svg' | 'ms', symbolSet: string, version: string, data: any): void;\n}\nexport declare const dataLoader: DataLoader;\n//# sourceMappingURL=DataLoader.d.ts.map","milsym/renderer/utilities/DistanceUnit.d.ts":"/**\n * Units of Measure to be used with {@link MilStdAttributes#DistanceUnits}\n * Default is meters.\n */\nexport declare class DistanceUnit {\n    private static readonly FEET_PER_METER;\n    private static readonly FLIGHT_LEVEL_PER_METER;\n    readonly conversionFactor: number;\n    readonly label: string;\n    constructor(conversionFactor: number, label: string);\n    static parse(distanceUnitText: string): DistanceUnit | null;\n    toAttribute(): string;\n    static METERS: DistanceUnit;\n    static FEET: DistanceUnit;\n    static FLIGHT_LEVEL: DistanceUnit;\n}\n//# sourceMappingURL=DistanceUnit.d.ts.map","milsym/renderer/utilities/DrawRules.d.ts":"/**\n * Draw Rules for Control Measures\n *\n */\nexport declare class DrawRules {\n    /**\n     * Usually an entry in the MilStd that is just a category containing other symbols\n     * and not something that gets drawn itself.\n     */\n    static readonly DONOTDRAW: number;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area’s size and shape.\n     *\n     * Size/Shape: Determined by the anchor points. The information fields\n     * should be moveable and scalable as a block within the area.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA1: number;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area’s size and shape. The LAA point symbol\n     * requires one anchor point and is connected to the area symbol with a\n     * straight line.\n     *\n     * Size/Shape: Determined by the anchor points. The information fields\n     * should be moveable and scalable as a block within the area.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly AREA2: number;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area’s size and shape.\n     *\n     * Size/Shape: Determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly AREA3: number;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area’s size and shape.\n     *\n     * Size/Shape: Determined by the anchor points. The information fields\n     * should be moveable and scalable as a block within the area. The default\n     * tic length should be the same as the text height of the echelon\n     * field (B). Spacing between the tics should also be the height of B.\n     * Users should be provided a facility to allow them to manually alter the\n     * height of B, which in turn should affect the tic length and spacing\n     * accordingly.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly AREA4: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the semicircle's opening. Point 3 defines the end\n     * of the arrow.\n     *\n     * Size/Shape: Points 1 and 2 determine the diameter of the semicircle and\n     * point 3 determines the length of the arrow. The tip of the arrowhead will\n     * be at the center point of the semicircle's diameter and will project\n     * perpendicularly from the line between points 1 and 2. The default tic\n     * length should be the same as the text height of the echelon field (B).\n     * Spacing between the tics should also be the height of B. Users should be\n     * provided a facility to allow them to manually alter the height of B,\n     * which in turn should affect the tic length and spacing accordingly.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA5: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Point 1 defines\n     * the center point of the symbol and point 2 defines the symbol’s start\n     * point and radius.\n     *\n     * Size/Shape: Points 1 and 2 will determine a radius that is long enough\n     * for the graphic to encompass the feature(s) being retained. The opening\n     * will be a 30-degree arc of the circle. The default tic length should be\n     * the same as the text height of the echelon field (R). Spacing between\n     * the tics should also be the height of R. Users should be provided a\n     * facility to allow them to manually alter the height of R, which in turn\n     * should affect the tic length and spacing accordingly.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA6: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Point 1 is the\n     * tip of the arrowhead. Points 2 and 3 define the endpoints of the straight\n     * line on the back side of the symbol.\n     *\n     * Size/Shape: Points 2 and 3 determine the length of the straight line on\n     * the back side of the symbol. The rear of the arrowhead line shall connect\n     * to the midpoint of the line between points 2 and 3. The arrowhead line shall be perpendicular to the line formed by points 2 and 3.\n     *\n     * Orientation: Orientation is determined by the anchor points. The back\n     * side of the symbol encompasses the firing position, while the arrowhead\n     * typically points at the target.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA7: number;\n    /**\n     * Anchor Points: This symbol requires four anchor points. Points 1 and 2\n     * define the endpoints of the straight line on the back side of the symbol.\n     * Points 3 and 4 define the tips of the arrowheads.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the straight line on\n     * the back side of the symbol. The rear of the arrows should connect to\n     * points 1 and 2.\n     *\n     * Orientation: Orientation is determined by the anchor points. The back\n     * side of the symbol encompasses the firing position, while the arrowheads\n     * typically indicate the left and right limits of coverage that the firing\n     * position is meant to support.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA8: number;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area’s size and shape.\n     *\n     * Size/Shape: Determined by the anchor points.\n     *\n     * Orientation: Not applicable. The area will encompass two or more fire\n     * support symbols (point/single target, nuclear target, circular target,\n     * rectangular target, or area target).\n     * The naming convention determines whether the area describes a series or\n     * group of targets.\n     *\n     * Used by: 2525D,Dch1,E\n     *\n     */\n    static readonly AREA9: number;\n    /**\n     * Anchor Points: This symbol requires a minimum of three (3) and a maximum\n     * of six (6) anchor points to define the boundary of the area. The anchor\n     * points shall be sequentially numbered, in increments of one (1),\n     * beginning with point one (1).\n     *\n     * Size/Shape: Determined by the anchor points. The information fields\n     * should be moveable and scalable within the area.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA10: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the symbol’s vertical line. Point 3 defines the\n     * endpoint of the symbol’s horizontal line.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the vertical line.\n     * The length of the horizontal line is determined by plotting point 3 on a\n     * plane extending perpendicularly from the midpoint of the vertical line.\n     *\n     * Orientation: The head of the \"T\" typically faces enemy forces.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA11: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the end points of the symbol’s vertical line. Point 3 defines the\n     * tip of the longest arrow.\n     *\n     * Size/Shape: Points 1 and 2 determine the height of the symbol and point\n     * 3 determines its length. The spacing between the symbol’s arrows will\n     * stay proportional to the symbol’s vertical line. The length of the short\n     * arrows will remain in proportion to the length of the longest arrow.\n     *\n     * Orientation: The arrows point away from enemy forces.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA12: number;\n    /**\n     * Anchor Points: This symbol requires at least two anchor points. Points\n     * 1 and 2 define the corners of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the straight line.\n     * The radius of the semicircle is ½ the length of the straight line.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA13: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. The center point\n     * defines the center of the symbol. Points 1 and 2 define the radii of\n     * circles 1 and 2.\n     *\n     * Size/Shape: As defined by the operator.\n     *\n     * Orientation: The center point is typically centered over Ground Zero (GZ)\n     * or Designated Ground Zero (DGZ).\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA14: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Point 1 defines\n     * the center point of the symbol and point 2 defines the symbol’s start\n     * point and radius.\n     *\n     * Size/Shape: The radius will be long enough for the symbol to encompass\n     * the UEI(s) or feature(s) being isolated. The opening will be a 30 degree\n     * arc of the circle.\n     *\n     * Orientation: The opening will be on the friendly side of the symbol.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA15: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Point 1 defines\n     * the center point of the symbol and point 2 defines the symbol’s start\n     * point and radius.\n     *\n     * Size/Shape: Points 1 and 2 will determine a radius that is long enough\n     * for the symbol to encompass the feature(s) being occupied. The opening\n     * will be a 30-degree arc of the circle.\n     *\n     * Orientation: The opening will be on the friendly side of the\n     * control measure.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA16: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the symbol’s vertical line. Point 3 defines the\n     * rear of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the height of the symbol and\n     * point 3 determines its length. The arrow will project perpendicularly\n     * from the midpoint of the vertical line.\n     *\n     * Orientation: The arrow points toward enemy forces.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA17: number;\n    /**\n     * Anchor Points: This symbol requires four anchor points. Point 1 defines\n     * the tip of the first arrowhead. Point 2 defines the end of the straight\n     * line portion of the first arrow. Point 3 defines the tip of the second\n     * arrowhead. Point 4 defines the end of the second arrow.\n     *\n     * Size/Shape: Points 1 and 2 and points 3 and 4 determine the length of\n     * each arrow. Points 2 and 3 shall be connected by a smooth, curved line.\n     *\n     * Orientation: Determined by the anchor points. The unit being relieved is\n     * typically located at the base of the curve and the unit performing the\n     * relief is typically located at the end of the symbol. The arrowhead\n     * typically points to the location the relieved unit should move to.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA18: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Point 1 defines\n     * the center point of the symbol and point 2 defines the symbol’s start\n     * point and radius.\n     *\n     * Size/Shape: Points 1 and 2 will determine a radius that is long enough\n     * for the symbol to encompass the feature(s) being secured. The opening\n     * will be a 30-degree arc of the circle.\n     *\n     * Orientation: The opening will be on the friendly side of the symbol.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA19: number;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area’s size and shape.\n     *\n     * Size/Shape: Determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly AREA20: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Point 1 defines\n     * the vertex of the symbol. Points 2 and 3 define the tips of the\n     * arrowheads.\n     *\n     * Size/Shape: Points 1 and 2 and points 1 and 3 determine the length of the\n     * arrows. The length and orientation of the arrows can vary independently.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     * The arrowheads may touch other symbols that define the limits of the\n     * task. The tactical symbol indicator is centered over point 1.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA21: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The center point\n     * defines the center of the symbol.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol is typically centered over the desired location.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA22: number;\n    /**\n     * Anchor Points: This symbol requires at least three anchor points to\n     * define the boundary of the area.\n     *\n     * Size/Shape: Determined by the anchor points\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly AREA23: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the symbol’s vertical line. Point 3 defines the\n     * endpoint of the symbol’s horizontal line.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the vertical line.\n     * Points 2 and 3 determine the length of the horizontal line, which will\n     * project perpendicularly from the midpoint of the vertical line.\n     *\n     * Orientation: The head of the \"T\" typically faces enemy forces.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly AREA24: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the end points of the symbol’s vertical line. Point 3 defines the\n     * tip of the longest arrow.\n     *\n     * Size/Shape: Points 1 and 2 determine the height of the symbol and\n     * point 3 determines its length. The spacing between the symbol’s arrows\n     * will stay proportional to the symbol’s vertical line. The length of the\n     * short arrows will remain in proportion to the length of the longest\n     * arrow. The arrows are perpendicular to the baseline (vertical line) and\n     * parallel to each other.\n     *\n     * Orientation: The arrows typically point toward enemy forces.\n     *\n     * Used by: 2525D,Dch1\n     */\n    static readonly AREA25: number;\n    /**\n     * Anchor Points: This symbol requires a minimum of 6 anchor points. Add as\n     * many pairs of points as needed to accurately define the areas. The number\n     * of points shall always be an even number, with an equal number of points\n     * for both polygons. Points 1 through N/2 define the inner safe zone\n     * (zone 1). Points N/2 +1 though point N defines the outer zone (zone 2).\n     *\n     * Size/Shape: Determined by the anchor points.\n     *\n     * Orientation: The symbol will typically be oriented upright.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AREA26: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Point 1 defines\n     * the tip of the arrowhead. Point 2 defines the end of the straight-line\n     * portion of the symbol. Point 3 defines the diameter and orientation of\n     * the 180 degree circular arc.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the straight-line\n     * portion of the symbol. Point 3 defines which side of the line the arc\n     * is on and the diameter of the arc\n     *\n     * Orientation: Determined by the anchor points.\n     *\n     * Used by: Ech1\n     */\n    static readonly AREA27: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The anchor point\n     * defines/is the tip of the inverted cone.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol will typically be oriented upright.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT1: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The center point\n     * defines/is the center of the symbol.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol is typically centered over the desired location.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT2: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point.\n     * The center point defines the center of the symbol.\n     *\n     * Size/Shape: Static. Maneuver area symbol shall be drawn with a black\n     * border. Maneuver areas may be either unfilled or filled with\n     * performance-contoured color options\n     *\n     * Orientation: The symbol is typically centered over the desired location.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT3: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The point defines\n     * the bottom of the central vertical line in the symbol where the curved\n     * and vertical lines meet.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol will typically be oriented upright (as shown in the template and example).\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT4: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The point defines\n     * the point where all the lines meet.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol will typically be oriented upright (as shown in\n     * the example).\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT5: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The anchor point\n     * defines/is the center of the bottom of the control measure symbol as\n     * shown in the template and example.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol will typically be oriented upright.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT6: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The anchor point\n     * defines the midpoint of the symbol's base.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol will typically be oriented upright (as shown in\n     * the template and example).\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT7: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The center point\n     * defines the center of the symbol.\n     *\n     * Size/Shape: Static. The symbol's corners form a 70- degree angle.\n     *\n     * Orientation: The symbol is typically centered over the desired location.\n     * A user can use this symbol to define a new type of point if the selection\n     * that follows is not sufficient.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly POINT8: number;\n    /**\n     * Anchor Points: This symbol requires one anchor (center) point. The point\n     * defines the center of the symbol.\n     *\n     * Size/Shape: Static. The symbol's height should be 2x the symbol's width.\n     *\n     * Orientation: The symbol's center point is typically centered over the\n     * desired location. The symbol shall be oriented upright, as shown in the\n     * examples.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly POINT9: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The point defines\n     * the center of the circle. (Sonobuoy)\n     *\n     * Size/Shape: Static. The diameter of the circle should be 1/2 the height\n     * of the symbol.\n     *\n     * Orientation: The symbol's center point is typically centered over the\n     * desired location. The symbol will be oriented upright, as shown in the\n     * example.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly POINT10: number;\n    /**\n     * Anchor Points: This symbol requires one center point. The point defines\n     * the center of the symbol.\n     *\n     * Size/Shape: Static. Length is 2x the size of height.\n     *\n     * Orientation: The symbol is centered over the desired location. The symbol\n     * shall be oriented upright, as shown in the example.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly POINT11: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the tips of the arrowheads and point 3 defines the rear of the\n     * symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the symbol's height and point 3\n     * determines its length. The vertical line at the rear of the symbol shall\n     * be the same length as the opening, and shall be perpendicular to the\n     * parallel lines formed with the rear of symbol vertical line and the lines\n     * ending with points 1 and 2.\n     *\n     * Orientation: The opening typically faces the applicable obstacle.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT12: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The center point\n     * defines the center of the circle.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol is typically centered over the desired location.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT13: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The center point\n     * defines the center of the symbol.\n     *\n     * Size/Shape: There should be 45 degrees of angular separation between the\n     * two arrows.\n     *\n     * Orientation: The symbol is typically centered over the desired location.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly POINT14: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. The anchor point\n     * defines \"nose\" of the symbol.\n     *\n     * Size/Shape: Static.\n     *\n     * Orientation: The symbol is typically centered over the desired location.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT15: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point; the point defines\n     * the circle at the base of the tower.\n     *\n     * Size/Shape: The symbol is a high-angle cone.\n     *\n     * Orientation: The symbol will remain upright.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT16: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. This anchor point\n     * represents the center of the rectangle and, therefore, the geographic\n     * location of that rectangle.\n     *\n     * Size/Shape: The size and shape of this symbol is determined by three\n     * additional numeric values; A length (AM1), a width (AM2), and a\n     * rotation angle. The length and width should be expressed in the\n     * appropriate map distance units. The length is aligned with the axis of\n     * rotation. The width is aligned perpendicular to the axis of rotation.\n     *\n     * Orientation: The orientation of this symbol is determined by the\n     * rotation angle provided, where 0 degrees is North and a positive\n     * rotation angle rotates the rectangle in a clockwise direction.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT17: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point that defines the\n     * axis of angular rotation.\n     *\n     * Size/Shape: The size and shape of this symbol is determined by additional\n     * numeric values; A search axis azimuth, a start range, a stop range and a\n     * stop relative bearing. The stop relative bearing is an equal angle either\n     * side of the search axis. The start and stop range should be expressed in\n     * the appropriate map distance units. Field T should be positioned in the\n     * center of the search area aligned with the search axis.\n     *\n     * Orientation: The orientation of this symbol is determined by the search\n     * axis azimuth provided.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POINT18: number;\n    /**\n     * Anchor Points: This symbol requires at least two anchor points, points 1\n     * and 2, to define the line. Additional points can be defined to extend the\n     * line.\n     *\n     * Size/Shape: The first and last anchor points determine the length of the\n     * line.\n     *\n     * Orientation: Orientation is determined by the order in which the anchor points are entered.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE1: number;\n    /**\n     * Anchor Points: This symbol requires at least two anchor points, points\n     * 1 and 2, to define the line. Additional points can be defined to extend\n     * the line.\n     *\n     * Size/Shape: The first and last anchor points determine the length of the line.\n     * The end-of line information will typically be posted at the ends of the line\n     * as it is displayed on the screen.\n     *\n     * Orientation: Orientation is determined by the order in which the anchor points\n     * are entered.\n     *\n     * Used by: 2525D,Dch1\n     */\n    static readonly LINE2: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Point 1 defines\n     * the vertex of the symbol. Points 2 and 3 define the tips of the arrowheads.\n     *\n     * Size/Shape: The length and orientation of the arrows can vary\n     * independently.\n     *\n     * Orientation: Orientation is determined by the anchor points. The\n     * arrowheads may touch other symbols that define the limits of the task.\n     * The top of the tactical symbol indicator may touch point 1\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE3: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Points 1 and 2\n     * define the corner points of the symbol.\n     *\n     * Size/Shape: The symbol varies only in length.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE4: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Points 1 and 2\n     * define the endpoints of the symbol.\n     *\n     * Size/Shape: The symbol varies only in length.\n     *\n     * Orientation: One point defines the origin from which the bearing is being\n     * taken and the other point defines the location or direction from which a\n     * contact is made.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly LINE5: number;\n    /**\n     * Anchor Points: This symbol requires 3 anchor points. Point 1 defines the\n     * vertex of the symbol and points 2 and 3 define its endpoints.\n     *\n     * Size/Shape: Points 1, 2 and 3 determine the length of the lines\n     * connecting them. The line defined by points 1 and 2 is typically the same\n     * length as the line between points 2 and 3.\n     *\n     * Orientation: Orientation is determined by the anchor points\n     *\n     * Used by: 2525D,Dch1\n     */\n    static readonly LINE6: number;\n    /**\n     * Anchor Points: This symbol requires at least two anchor points, points\n     * 1 and 2, to define the line. Additional points can be defined to extend\n     * the line.\n     *\n     * Size/Shape: The first and last anchor points determine the length of the\n     * line. The line information will be posted once at the center of the line\n     * as it is displayed on the screen.\n     *\n     * Orientation: Orientation is determined by the order in which the anchor\n     * points are entered.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly LINE7: number;\n    /**\n     * Anchor Points: This symbol requires a minimum of two (2) anchor points.\n     * Up to 298 additional points can be added to extend the line. The first\n     * point (point 1) defines the start point. The last point defines the\n     * endpoint. The points are numbered sequentially beginning with point one (1), in increments of one.\n     *\n     * Size/Shape: The anchor points define the size and shape.\n     *\n     * Orientation: The orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE8: number;\n    /**\n     * Anchor Points: This symbol requires 2 anchor points. Point 1 defines the\n     * tip of the arrowhead and point 2 defines the rear of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the symbol, which\n     * varies only in length.\n     *\n     * Orientation: The orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE9: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Point 1 defines\n     * the tip of the arrowhead and point 2 defines the rear of the symbol.\n     * Point 3 defines the 90 degree arc.\n     *\n     * Size/Shape: Points 1 and 2 are connected by a 90 degree arc. Point 3\n     * indicates on which side of the line the arc is placed.\n     *\n     * Orientation: The rear of the symbol identifies the enemy’s location and\n     * the arrow points in the direction the obstacle should force the enemy to\n     * turn.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE10: number;\n    /**\n     * Anchor Points: This symbol requires four points. Points 1 and 2 define\n     * one side of the gap and points 3 and 4 define the opposite side of the\n     * gap.\n     *\n     * Size/Shape. Determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE11: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the symbol and point 3 defines the location of\n     * one side of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the centerline of the symbol and\n     * point 3 determines its width.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE12: number;\n    /**\n     * Anchor Points: This symbol requires at least two anchor points, points 1\n     * and 2, to define the line. Additional points can be defined to extend the\n     * line.\n     *\n     * Size/Shape: The first and last anchor points determine the length of the\n     * line. The size of the tooth does not change.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly LINE13: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Points 1 and 2\n     * define the tips of the arrowheads.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the symbol, which\n     * varies only in length. The lines of the arrowhead will form an acute\n     * angle.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE14: number;\n    /**\n     * Anchor Points: This symbol requires four points. Points 1 and 2 define\n     * one side of the assault crossing site and points 3 and 4 define the\n     * opposite side of the assault crossing site.\n     *\n     * Size/Shape: Determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE16: number;\n    /**\n     * Anchor Points. This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the first line. Point 3 defines the location of\n     * the parallel line.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the symbol. Point 3\n     * determines its width.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE17: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Points 1 and two\n     * define the tips of the arrowheads.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the symbol, which\n     * varies only in length. The arrowheads will be filled-in versions of a\n     * common arrowhead.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly LINE18: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points. Points 1 and two\n     * define the corners on the front of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the symbol, which\n     * varies only in length.\n     *\n     * Orientation: Orientation is determined by the anchor points\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE19: number;\n    /**\n     * Anchor Points: This graphic requires two anchor points. Point 1 defines\n     * the tip of the arrowhead, and point 2 defines the rear of the graphic.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the graphic, which\n     * varies only in length.\n     *\n     * Orientation: The arrow points to the location where the convoy has halted.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly LINE20: number;\n    /**\n     * Anchor Points: This symbol requires at least two anchor points to define\n     * the line. Additional points can be defined to extend and shape the line.\n     *\n     * Size/Shape: The first and last anchor points determine the length of the\n     * line. The line segment between each pair of anchor points will repeat all\n     * information associated with the line segment.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly LINE21: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the symbol’s opening and point 3 defines the rear\n     * of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the symbol’s height and point 3\n     * determines its length. The vertical line at the rear of the symbol will\n     * be the same height as the opening and parallel to it.\n     *\n     * Orientation: The opening defines the span of the breach and typically\n     * faces enemy forces.\n     *\n     * Used by: 2525D,Dch1\n     */\n    static readonly LINE22: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the symbol’s vertical line and point 3 defines\n     * the rear of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the symbol’s height and point 3\n     * determines its length. The spacing between the symbol’s arrows will stay\n     * proportional to the symbol’s height. The tip of the middle arrowhead will\n     * be at the midpoint of the vertical line. The arrows will stay\n     * perpendicular to the vertical line, regardless of the rotational\n     * orientation of the symbol as a whole.\n     *\n     * Orientation: The arrows typically point toward enemy forces.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE23: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Point 1 defines\n     * the tip of the arrowhead. Point 2 defines the end of the straight line\n     * portion of the symbol. Point 3 defines the diameter and orientation of\n     * the 180 degree circular arc.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the straight line\n     * portion of the symbol.\n     * Point 3 defines which side of the line the arc is on and the diameter of\n     * the arc.\n     *\n     * Orientation: The arrow points in the direction of the action. The tip of\n     * the arrowhead may indicate the location where the action is to conclude.\n     * The unit’s current location is typically represented at the base of the\n     * arc. The 180 degree circular arc is always perpendicular to the line.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE24: number;\n    /**\n     * Anchor Points: This symbol requires exactly two anchor points. Point 1\n     * defines the tip of the arrowhead and point 2 defines the rear of the\n     * symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the symbol, which\n     * varies only in length.\n     *\n     * Orientation: The arrow typically points in the direction of the action.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE25: number;\n    /**\n     * Anchor Points: Where four points are available Point 1 and Point 2 define\n     * the ends of one arrow and Point 3 and Point 4 define the ends of the\n     * other arrow. Point 1 and Point 4 define the ends of their respective\n     * arrowheads. Where three points are available Point 1 defines the vertex\n     * of the symbol. Points 2 and 3 define the tips of the arrowheads.\n     *\n     * Size/Shape: Where four points are available, Points 1 and 2 and Points\n     * 3 and 4 determine the length of the arrows. Where three points are\n     * available Points 1 and 2 and points 1 and 3 determine the length of the\n     * arrows. The length and orientation of the arrows can vary independently.\n     *\n     * Orientation: Orientation is determined by the anchor points.\n     * The arrowheads may touch other symbols that define the limits of the\n     * task. The tactical symbol indicator is centered between point 2 and\n     * point 3 when four points are in use or centered on Point 1 when three\n     * points are in use.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE26: number;\n    /**\n     * Anchor Points: Where four points are available Point 1 defines the center\n     * of the circle. Point 2 defines the radius of the circle. Point 3 defines\n     * the curvature of the arc. Point 4 defines the end of the arrow. Where\n     * three points are available Point 1 defines the center point of the\n     * circle. Point 2 defines the tip of the arrowhead. Point 3 defines the\n     * 90 degree arc.\n     *\n     * Size/Shape: Where four points are available, Points 1 and 2 define the\n     * size of the circle, which should be adjusted as needed to contain the\n     * unit assigned the task. Point 3 controls the curvature of the arc.\n     * Point 4 defines the end of the arrow. Where three points are available\n     * Points 1 and 2 are connected by a 90 degree arc. The circle will at least\n     * be large enough to accommodate a tactical symbol. Point 3 indicates on\n     * which side of the line the arc is placed.\n     *\n     * Orientation: The arrowhead identifies the location/object to be seized\n     * and the circle identifies the unit(s) assigned the task. See 5.3.11 for\n     * options to accommodate multiple units.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE27: number;\n    /**\n     * Anchor Points: This symbol requires 2 anchor points. Point 1 defines the\n     * tip of the arrowhead, and point 2 defines the rear of the symbol.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the symbol, which\n     * varies only in length.\n     *\n     * Orientation: The arrow typically points toward enemy forces with the tip\n     * of the arrowhead indicating the location of the action.\n     *\n     * Duplicate of Line9 so removed in 2525E\n     *\n     * Used by: 2525D,Dch1\n     */\n    static readonly LINE28: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Point 1 is the\n     * tip of the arrowhead. Points 2 and 3 define the endpoints of the curved\n     * line on the back side of the symbol.\n     *\n     * Size/Shape: Points 2 and 3 determine the length of the curved line on the\n     * back side of the symbol. The rear of the arrowhead line shall connect to\n     * the midpoint of the line between points 2 and 3. The arrowhead line shall\n     * be perpendicular to the line formed by points 2 and 3.\n     *\n     * Orientation: Orientation is determined by the anchor points. The back\n     * side of the symbol encompasses the ambush position, while the arrowhead\n     * typically points at the target.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly LINE29: number;\n    /**\n * Anchor Points: Point 1 defines the tip of the arrowhead. Point 2 defines\n * the end of the symbol. Point 3’s distance from Point 2 defines the length\n * of the four angled lines making up the arrowhead and dashed “tail”. Angles\n * a are always drawn at 45 degrees. Angle b is always drawn at 90 degrees.\n *\n * Size/Shape: The symbol varies only in length.\n *\n * Used by: Ech1\n */\n    static readonly LINE30: number;\n    /**\n     * Anchor Points: This graphic requires four anchor points. Point 1 defines the\n     * beginning of the straight line. Point 2 defines the end of the straight-line\n     * portion of the graphic. Point 3 defines the diameter. Point 4 defines the\n     * orientation of the 180 degree circular arc.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the straight-line portion\n     * of the symbol. Point 3 defines the diameter of the arc. Point 4 defines which\n     * side of the line the arc is on.\n     *\n     * Used by: Ech1\n     */\n    static readonly LINE31: number;\n    /**\n     * Anchor Points: This graphic requires three anchor points. Point 1 defines the\n     * end of the straight-line portion of the graphic. Point 2 defines the center of\n     * the two 90 degree circular arcs. Point 3 defines the tip of the arrowhead.\n     *\n     * Size/Shape: Points 1 and 3 determine the length of the symbol.\n     *\n     * Used by: Ech1\n     */\n    static readonly LINE32: number;\n    /**\n     * Anchor Points: This graphic requires three anchor points.\n     * Point 1 defines the beginning of the straight line. Point 2\n     * defines the end of the straight line portion of the graphic.\n     * Point 3 defines the diameter and orientation of the 180 degree\n     * circular arc and the tip of the arrowhead.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of the straight\n     * line portion of the symbol. Point 3 defines which side of the\n     * line the arc is on and the diameter of the arc.\n     *\n     * Used by: Ech1\n     */\n    static readonly LINE33: number;\n    /**\n     * Anchor Points: This symbol may contain multiple segments. Each segment\n     * requires 2 anchor points. Point numbers that define the trace of the\n     * segment are sequential beginning with point 1, in increments of 1, up to\n     * a max of 99 points. Each anchor point defines the endpoint of a segment’s\n     * center line. The anchor points are Air Control Points (ACP),\n     * Communications Checkpoints (CCP) or both.\n     *\n     * Size/Shape: Points 1 and 2 determine the length of a segment. The\n     * information field inside each segment should be moveable and scalable\n     * within each segment. The information box outside the symbol should be\n     * placed between points 1 and 2 in such a way it does not obscure the\n     * symbol.\n     *\n     * Orientation: The anchor points determine orientation.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly CORRIDOR1: number;\n    /**\n     * Anchor Points: The symbol requires N anchor points, where N is between\n     * 3 and 50. Point 1 defines the tip of the arrowhead. Point N-1 defines\n     * the rear of the symbol. Point N defines the back of the arrowhead.\n     * Anchor points are numbered sequentially beginning with point number\n     * one (1), in increments of one (1).\n     *\n     * Size/Shape: Points 1 through N-1 and 2 determine the symbol’s center line\n     * and Point N determines the width. The crossover point on the symbol shall\n     * occur between Points 1 and 2.\n     *\n     * Orientation: The arrowhead typically points toward enemy forces.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly AXIS1: number;\n    /**\n     * Anchor Points: The symbol requires N anchor points, where N is between 3\n     * and 50. Point 1 defines the tip of the arrowhead. Point N-1 defines the\n     * rear of the symbol. Point N defines the back of the arrowhead. Anchor\n     * points are numbered sequentially beginning with point number one (1), in\n     * increments of one (1).\n     *\n     * Size/Shape: Points 1 through N-1 and 2 determine the symbol’s center line\n     * and Point N determines the width.\n     *\n     * Orientation: The arrowhead typically points toward enemy forces.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly AXIS2: number;\n    /**\n     * Anchor Points: This symbol requires three anchor points. Points 1 and 2\n     * define the endpoints of the infiltration lane and point 3 defines the\n     * width on one side of the lane.\n     *\n     * Size/Shape: Points 1 and 2 determine the center line of the symbol and\n     * point 3 determines the width of the infiltration lane. The rest of the\n     * symbol stays proportional to the length of the center line.\n     *\n     * Orientation: Orientation is determined by points 1 and 2.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly POLYLINE1: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point. This anchor point\n     * represents the center of an ellipse and, therefore, the geographic\n     * location of that ellipse.\n     *\n     * Size/Shape: The size and shape of this symbol is determined by three\n     * additional numeric values; A major axis radius, a minor axis radius, and\n     * a rotation angle. The radii should be expressed in the appropriate map\n     * distance units.\n     *\n     * Orientation: The orientation of this symbol is determined by the rotation\n     * angle provided, where 0 degrees is east/west and a positive rotation\n     * angle rotates the ellipse in a counter-clockwise direction.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly ELLIPSE1: number;\n    /**\n     * Anchor Points: This symbol requires two anchor points and a width,\n     * defined in meters, to define the boundary of the area. Points 1 and 2\n     * will be located in the center of two opposing sides of the rectangle.\n     *\n     * Size/Shape: Size: As determined by the anchor points. The anchor points\n     * determine the length of the rectangle. The width, defined in meters,\n     * will determine the width of the rectangle. Shape: Rectangle. The\n     * information fields should be moveable and scalable.\n     *\n     * Orientation: As determined by the anchor points.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly RECTANGULAR1: number;\n    /**\n     * Anchor Points: This symbol requires one (1) anchor point to define the\n     * center of the area.\n     *\n     * Size/Shape: Size Is determined by the anchor point, the target length\n     * (in meters) and target width (in meters). A rectangular target is wider\n     * and longer than 200 meters. The information fields should be moveable and\n     * saleable within the area. Shape: Rectangle.\n     *\n     * Orientation: As determined by the Target Attitude (in mils).\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly RECTANGULAR2: number;\n    /**\n     * Anchor Points: This symbol requires one anchor (center) point to define\n     * the center of the symbol. The target tactical symbol shall be centered\n     * upon the center of the area. The size and the orientation of the target\n     * symbol are fixed within the area.\n     *\n     * Size/Shape: As determined by the anchor points. The anchor points\n     * determine the area’s length.\n     * Width, determined in meters, will define the width of the rectangle.\n     *\n     * Orientation: As determined by the anchor points. The center point of the\n     * area shall always have the target symbol with the same\n     * upright orientation.\n     *\n     * Used by: 2525D,Dch1,E\n     */\n    static readonly RECTANGULAR3: number;\n    /**\n     * Anchor Points: This symbol requires one (1) anchor point and a radius.\n     * Point 1 defines the center point of the symbol.\n     *\n     * Size/Shape: Size: The radius defines the size. Shape: Circle. The\n     * information fields should be scalable within the circle.\n     *\n     * Orientation: Not applicable\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly CIRCULAR1: number;\n    /**\n     * Anchor Points: This symbol requires one anchor point that defines an\n     * object at a dynamic grid location. This coordinate, which pinpoints the\n     * current physical location of a specific unit, weapon or acquisition\n     * system, may change with the movement of the object. The symbol for that\n     * object is located at the anchor point.\n     *\n     * Size/Shape: The size is determined by the distance in meters from the\n     * object at the center of the range fan. The shapes are concentric circles.\n     * A minimum of one (1) and a maximum of three (3) concentric circles can be\n     * used.\n     *\n     * Orientation: The center point is typically centered over the known\n     * location of a weapon or sensor system.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly CIRCULAR2: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point that defines an\n     * object at a dynamic grid location. This coordinate, which pinpoints the\n     * current physical location of a specific unit, weapon or sensor system,\n     * may change with the movement of the object. The symbol for that object\n     * is located at the anchor point.\n     *\n     * Size/Shape: Determined by the anchor point, azimuths measured from true\n     * north, and the distance (range) in meters. The Left Sector Azimuth is the\n     * angle measured from true north to the left sector limit/edge of the\n     * Sector Range Fan. The Right Sector Azimuth is the angle measured from\n     * true north to the right sector limit/edge of the Sector Range Fan.\n     * Multiple distances (ranges) and/or left and right sector limits/edges\n     * of the sector, as well as altitude, may be added as required to define\n     * the sector. All azimuths are in degrees. All distances (ranges) are in\n     * meters. All altitudes are in feet.\n     *\n     * Orientation: The center point is typically centered over the known\n     * location of a weapon or sensor system. The orientation may change as\n     * the object moves or changes.\n     *\n     * Used by: 2525D,Dch1,E,Ech1\n     */\n    static readonly ARC1: number;\n}\n//# sourceMappingURL=DrawRules.d.ts.map","milsym/renderer/utilities/EntityCode.d.ts":"export declare class EntityCode {\n    static readonly EntityCode_FLOT: number;\n    static readonly EntityCode_BioContaminatedArea: number;\n    static readonly EntityCode_ChemContaminatedArea: number;\n    static readonly EntityCode_NuclearContaminatedArea: number;\n    static readonly EntityCode_RadiologicalContaminatedArea: number;\n    static readonly EntityCode_BioEvent: number;\n    static readonly EntityCode_ChemicalEvent: number;\n    static readonly EntityCode_NuclearEvent: number;\n    static readonly EntityCode_RadiologicalEvent: number;\n    static readonly EntityCode_AnchoragePoint: number;\n    /**\n     * Returns the modifier icon for a given contamination area\n     * @param contaminationArea the entity code of the contamination area\n     * @return the entity code of the icon that should be displayed within it\n     */\n    static getSymbolForContaminationArea(contaminationArea: number): number;\n}\n//# sourceMappingURL=EntityCode.d.ts.map","milsym/renderer/utilities/ErrorLogger.d.ts":"import { LogLevel } from \"./LogLevel\";\n/**\n * Error Logging class for Renderer\n *\n */\nexport declare class ErrorLogger {\n    static readonly LoggerName: string;\n    private static _level;\n    private static _LoggingEnabled;\n    private static dateFormatOptions;\n    /**\n     * True if logging is enabled\n     * @return {@link Boolean}\n     */\n    static getLoggingStatus(): boolean;\n    /**\n     * Takes a throwable and puts it's stacktrace into a string.\n     * @param error {@link Error}\n     * @return {@link String}\n     */\n    static getStackTrace(error: Error): string;\n    /**\n     * TRUE: Creates a file handler that will log message to a file.\n     * FALSE: logging just goes to console.\n     * @param enable {@link Boolean}\n     */\n    static EnableLogging(enable: boolean): void;\n    /**\n     * Folder location to store the log file.\n     * Defaults to \"System.getProperty(\"user.dir\")\"\n     * @param path {@link String}\n     * @deprecated\n     */\n    static setLoggingPath(path: string): void;\n    /**\n     * clears log files that are beyond a passed number of days old\n     * @param DaysOld {@link Integer}\n     * @deprecated\n     */\n    static CleanupOldFiles(DaysOld: number): void;\n    /**\n     * Set minimum level at which an item can be logged.\n     * In descending order:\n     * Severe\n     * Warning\n     * Info\n     * Config\n     * Fine\n     * Finer\n     * Finest\n     * @param newLevel {@link Level}\n     */\n    static setLevel(newLevel: LogLevel): void;\n    /**\n     * Set minimum level at which an item can be logged.\n     * In descending order:\n     * Severe\n     * Warning\n     * Info\n     * Config\n     * Fine\n     * Finer\n     * Finest\n     * @param newLevel {@link Level}\n     * @param setConsoleHandler logger could be set to FINE but the console\n     * handler could be set to INFO.  In that case, anything logged at FINE\n     * wouldn't show because it'd get blocked by the console handler.  Set to\n     * \"true\" to make sure the console handler will let you log at the level\n     * you want.  If you're only concerned with the log file, you can leave\n     * \"false\"\n     */\n    static setLevel(newLevel: LogLevel, setConsoleHandler: boolean): void;\n    /**\n     * Specify whether or not this logger should send its output\n     * to it's parent Logger.  This means that any LogRecords will\n     * also be written to the parent's Handlers, and potentially\n     * to its parent, recursively up the namespace.\n     * Defaults to true;\n     *\n     * @param useParentHandlers   true if output is to be sent to the\n     *\t\tlogger's parent.\n     */\n    static setUseParentHandlers(useParentHandlers: boolean): void;\n    /**\n     * Gets the java.util.logging.Level that the logger is set to.\n     * @return {@link Level}\n     */\n    static getLevel(): LogLevel;\n    /**\n     *\n     * @return {@link String}\n     * @deprecated\n     */\n    private static getFileName;\n    /**\n     * Log a method entry.\n     * <p>\n     * This is a convenience method that can be used to log entry\n     * to a method.  A LogRecord with message \"ENTRY\", log level\n     * FINER, and the given sourceMethod and sourceClass is logged.\n     * <p>\n     * @param   sourceClass    name of class that issued the logging request\n     * @param   sourceMethod   name of method that is being entered\n     */\n    static Entering(sourceClass: string, sourceMethod: string): void;\n    /**\n     * Log a method entry, with one parameter.\n     * <p>\n     * This is a convenience method that can be used to log entry\n     * to a method.  A LogRecord with message \"ENTRY {0}\", log level\n     * FINER, and the given sourceMethod, sourceClass, and parameter\n     * is logged.\n     * <p>\n     * @param   sourceClass    name of class that issued the logging request\n     * @param   sourceMethod   name of method that is being entered\n     * @param   param1\t       parameter to the method being entered\n     */\n    static Entering(sourceClass: string, sourceMethod: string, param1: any): void;\n    /**\n     * Log a method entry, with an array of parameters.\n     * <p>\n     * This is a convenience method that can be used to log entry\n     * to a method.  A LogRecord with message \"ENTRY\" (followed by a\n     * format {N} indicator for each entry in the parameter array),\n     * log level FINER, and the given sourceMethod, sourceClass, and\n     * parameters is logged.\n     * <p>\n     * @param   sourceClass    name of class that issued the logging request\n     * @param   sourceMethod   name of method that is being entered\n     * @param   params\t       array of parameters to the method being entered\n     */\n    static Entering(sourceClass: string, sourceMethod: string, params: any[]): void;\n    /**\n     * Log a method return.\n     * <p>\n     * This is a convenience method that can be used to log returning\n     * from a method.  A LogRecord with message \"RETURN\", log level\n     * FINER, and the given sourceMethod and sourceClass is logged.\n     * <p>\n     * @param   sourceClass    name of class that issued the logging request\n     * @param   sourceMethod   name of the method\n     */\n    static Exiting(sourceClass: string, sourceMethod: string): void;\n    /**\n     * Log a method return, with result object.\n     * <p>\n     * This is a convenience method that can be used to log returning\n     * from a method.  A LogRecord with message \"RETURN {0}\", log level\n     * FINER, and the gives sourceMethod, sourceClass, and result\n     * object is logged.\n     * <p>\n     * @param   sourceClass    name of class that issued the logging request\n     * @param   sourceMethod   name of the method\n     * @param   result  Object that is being returned\n     */\n    static Exiting(sourceClass: string, sourceMethod: string, result: any): void;\n    /**\n     * Defaults to Level.INFO\n     * @param message {@link String}\n     */\n    static LogMessage(message: string): void;\n    /**\n     * Defaults to Level.INFO\n     * @param message {@link String}\n     * @param showMessageBox (@link {@link Boolean}\n     */\n    static LogMessage(message: string, showMessageBox: boolean): void;\n    /**\n     *\n     * @param message {@link String}\n     * @param lvl {@link Level}\n     * @param showMessageBox {@link Boolean}\n     */\n    static LogMessage(message: string, lvl: LogLevel, showMessageBox: boolean): void;\n    static LogMessage(sourceClass: string, sourceMethod: string, message: string): void;\n    static LogMessage(sourceClass: string, sourceMethod: string, message: string, showMessageBox: boolean): void;\n    static LogMessage(sourceClass: string, sourceMethod: string, message: string, lvl: LogLevel): void;\n    static LogMessage(sourceClass: string, sourceMethod: string, message: string, lvl: LogLevel, showMessageBox: boolean): void;\n    static LogMessage(sourceClass: string, sourceMethod: string, message: string, lvl: LogLevel, param1: any, showMessageBox: boolean): void;\n    static LogMessage(sourceClass: string, sourceMethod: string, message: string, lvl: LogLevel, params: any[], showMessageBox: boolean): void;\n    static LogException(sourceClass: string, sourceMethod: string, exc: Error): void;\n    static LogException(sourceClass: string, sourceMethod: string, exc: Error, showMessageBox: boolean): void;\n    static LogException(sourceClass: string, sourceMethod: string, exc: Error, lvl: LogLevel): void;\n    static LogException(sourceClass: string, sourceMethod: string, exc: Error, lvl: LogLevel, showMessageBox: boolean): void;\n    static PrintList(list: Array<any>): string;\n    static PrintObjectMap(map: Map<string, any>): string;\n    static PrintStringMap(map: Map<string, string>): string;\n}\n//# sourceMappingURL=ErrorLogger.d.ts.map","milsym/renderer/utilities/GENCLookup.d.ts":"/**\n * Utility class that takes the 3 digit country code from the symbol ID and returns the 3 character string representation\n * of that country. For example, 840 turns into \"USA\" for the United States.\n */\nexport declare class GENCLookup {\n    private static gencJSON;\n    private static _instance;\n    private static _initCalled;\n    private static _isReady;\n    private static _GENCLookup;\n    private static genc;\n    /**\n     *\n     * @param url\n     * @deprecated\n     */\n    static setData(url: string): Promise<void>;\n    private constructor();\n    static getInstance(): GENCLookup;\n    isReady(): boolean;\n    private init;\n    get3CharCode(id: number): string;\n}\nexport declare const gencLookup: GENCLookup;\n//# sourceMappingURL=GENCLookup.d.ts.map","milsym/renderer/utilities/GeoPixelConversion3D.d.ts":"/**\n *\n *\n */\nexport declare class GeoPixelConversion3D {\n    static metersPerPixel(scale: number): number;\n    static lat2y(latitude: number, scale: number, latOrigin: number, metPerPix: number): number;\n    static y2lat(yPosition: number, scale: number, latOrigin: number, metPerPix: number): number;\n    static long2x(longitude: number, scale: number, longOrigin: number, latitude: number, metPerPix: number): number;\n    static x2long(xPosition: number, scale: number, longOrigin: number, latitude: number, metPerPix: number): number;\n    static Deg2Rad(deg: number): number;\n    static GetMetersPerDegAtLat(lat: number): number;\n}\n//# sourceMappingURL=GeoPixelConversion3D.d.ts.map","milsym/renderer/utilities/IDataLoader.d.ts":"/**\n * Interface for lazy-loading symbol set data chunks.\n * Replaces the monolithic eager-load pattern with on-demand chunk loading.\n */\nexport interface IDataLoader<T> {\n    /**\n     * Load a specific symbol set's data chunk.\n     * Returns the loaded data, or retrieves from cache if already loaded.\n     * @param symbolSet - Two-digit symbol set code (e.g., \"10\", \"25\")\n     * @param version - Version identifier (\"2525d\" or \"2525e\")\n     */\n    loadSymbolSet(symbolSet: string, version: string): Promise<T>;\n    /**\n     * Preload one or more symbol sets without blocking.\n     * Useful for warming the cache when upcoming usage is predictable.\n     */\n    preload(symbolSets: string[], version: string): Promise<void>;\n    /**\n     * Returns current memory usage stats.\n     */\n    getMemoryUsage(): DataLoaderStats;\n    /**\n     * Evict a specific symbol set from the cache.\n     * @param symbolSet - Two-digit symbol set code\n     * @param version - Version identifier\n     * @returns true if the entry was evicted, false if not found\n     */\n    evict(symbolSet: string, version: string): boolean;\n    /**\n     * Clear all cached data.\n     */\n    clear(): void;\n}\nexport interface DataLoaderStats {\n    /** Number of symbol sets currently loaded */\n    loadedChunks: number;\n    /** Maximum number of chunks allowed in cache */\n    maxChunks: number;\n    /** Estimated memory usage in bytes */\n    estimatedBytes: number;\n    /** List of currently loaded chunk keys */\n    loadedKeys: string[];\n}\n//# sourceMappingURL=IDataLoader.d.ts.map","milsym/renderer/utilities/IMultiPointRenderer.d.ts":"/**\n * @deprecated\n */\ninterface IMultiPointRenderer {\n    /**\n     * Renders all multi-point symbols, creating KML that can be used to draw\n     * it on a Google map.  Multipoint symbols cannot be draw the same\n     * at different scales. For instance, graphics with arrow heads will need to\n     * redraw arrowheads when you zoom in on it.  Similarly, graphics like a\n     * Forward Line of Troops drawn with half circles can improve performance if\n     * clipped when the parts of the graphic that aren't on the screen.  To help\n     * readjust graphics and increase performance, this function requires the\n     * scale and bounding box to help calculate the new locations.\n     * @param id A unique identifier used to identify the symbol by Google map.\n     * The id will be the folder name that contains the graphic.\n     * @param name a string used to display to the user as the name of the\n     * graphic being created.\n     * @param description a brief description about the graphic being made and\n     * what it represents.\n     * @param symbolCode A 15 character symbolID corresponding to one of the\n     * graphics in the MIL-STD-2525C\n     * @param controlPoints The vertices of the graphics that make up the\n     * graphic.  Passed in the format of a string, using decimal degrees\n     * separating lat and lon by a comma, separating coordinates by a space.\n     * The following format shall be used \"x1,y1[,z1] [xn,yn[,zn]]...\"\n     * @param altitudeMode Indicates whether the symbol should interpret\n     * altitudes as above sea level or above ground level. Options are\n     * \"clampToGround\", \"relativeToGround\" (from surface of earth), \"absolute\"\n     * (sea level), \"relativeToSeaFloor\" (from the bottom of major bodies of\n     * water).\n     * @param scale A number corresponding to how many meters one meter of our\n     * map represents. A value \"50000\" would mean 1:50K which means for every\n     * meter of our map it represents 50000 meters of real world distance.\n     * @param bbox The viewable area of the map.  Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" Not required\n     * but can speed up rendering in some cases.\n     * example: \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers A JSON string representing all the possible symbol\n     * modifiers represented in the MIL-STD-2525C.  Format of the string will be\n     * {\"modifiers\": {\"attributeName\":\"value\"[,\"attributeNamen\":\"valuen\"]...}}\n     * The quotes are literal in the above notation.  Example:\n     * {\"modifiers\": {\"quantity\":\"4\",\"speed\":\"300\",\"azimuth\":[100,200]}}\n     * @param format An enumeration: 0 for KML, 1 for JSON.\n     * @param symStd An enumeration: 0 for 2525Bch2, 1 for 2525C.\n     * @return A JSON string representation of the graphic.\n     */\n    RenderSymbol(id: string, name: string, description: string, symbolCode: string, controlPoints: string, altitudeMode: string, scale: number, bbox: string, modifiers: string, format: number, symStd: number): string;\n    /**\n     * Renders all multi-point symbols, creating KML or JSON for the user to\n     * parse and render as they like.\n     * This function requires the bounding box to help calculate the new\n     * locations.\n     * @param id A unique identifier used to identify the symbol by Google map.\n     * The id will be the folder name that contains the graphic.\n     * @param name a string used to display to the user as the name of the\n     * graphic being created.\n     * @param description a brief description about the graphic being made and\n     * what it represents.\n     * @param symbolCode A 15 character symbolID corresponding to one of the\n     * graphics in the MIL-STD-2525C\n     * @param controlPoints The vertices of the graphics that make up the\n     * graphic.  Passed in the format of a string, using decimal degrees\n     * separating lat and lon by a comma, separating coordinates by a space.\n     * The following format shall be used \"x1,y1 [xn,yn]...\"\n     * @param pixelWidth pixel dimensions of the viewable map area\n     * @param pixelHeight pixel dimensions of the viewable map area\n     * @param bbox The viewable area of the map.  Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\"\n     * example: \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers A JSON string representing all the possible symbol\n     * modifiers represented in the MIL-STD-2525C.  Format of the string will be\n     * {\"modifiers\": {\"attributeName\":\"value\"[,\"attributeNamen\":\"valuen\"]...}}\n     * The quotes are literal in the above notation.  Example:\n     * {\"modifiers\": {\"quantity\":\"4\",\"speed\":\"300\",\"azimuth\":[100,200]}}\n     * @param format An enumeration: 0 for KML, 1 for JSON.\n     * @param symStd An enumeration: 0 for 2525Bch2, 1 for 2525C.\n     * @return A JSON (1) or KML (0) string representation of the graphic.\n     */\n    RenderSymbol2D(id: string, name: string, description: string, symbolCode: string, controlPoints: string, pixelWidth: number, pixelHeight: number, bbox: string, modifiers: string, format: number, symStd: number): string;\n}\n//# sourceMappingURL=IMultiPointRenderer.d.ts.map","milsym/renderer/utilities/IPointConversion.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\n/**\n * Interface for Point Conversion objects.  Recommend using the functions\n * that take and return Point2D objects.\n *\n */\nexport interface IPointConversion {\n    PixelsToGeo(pixel: Point2D): Point2D;\n    GeoToPixels(coord: Point2D): Point2D;\n}\n//# sourceMappingURL=IPointConversion.d.ts.map","milsym/renderer/utilities/ImageInfo.d.ts":"import { Point } from \"../../graphics/Point\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { SymbolDimensionInfo } from \"./SymbolDimensionInfo\";\n/**\n * Object that holds an image of the symbol and all the information\n * needed to place the symbol on the screen.\n *\n */\nexport declare class ImageInfo implements SymbolDimensionInfo {\n    static readonly FormatPNG: string;\n    static readonly FormatJPG: string;\n    private _Image;\n    private _X;\n    private _Y;\n    private _symbolCenterX;\n    private _symbolCenterY;\n    protected _symbolBounds: Rectangle2D;\n    /**\n     *\n     */\n    constructor();\n    /**\n     * ImageInfo holds and image and holds the position at which the image\n     * should be drawn.  Use for Multipoint and single point graphics.\n     * @param image {@link BufferedImage}\n     * @param x position of where the image should be drawn\n     * @param y position of where the image should be drawn\n     */\n    constructor(image: ImageBitmap, x: number, y: number);\n    /**\n     * Creates a new ImageInfo object\n     * @param bi {@link BufferedImage}\n     * @param centerPoint can also be the anchor point of the symbol if it isn't the center of the image (action point)\n     * @param symbolBounds {@link Rectangle2D}\n     */\n    constructor(bi: ImageBitmap, centerPoint: Point2D, symbolBounds: Rectangle2D);\n    /**\n     * ImageInfo holds and image and holds the position at which the image\n     * should be drawn.  Use this if the image is a single point graphic.\n     * @param image {@link BufferedImage}\n     * @param x position of where the image should be drawn\n     * @param y position of where the image should be drawn\n     * @param symbolCenterX center point of image may be different from the center\n     * point of the symbol within the image. (single point graphics)\n     * @param symbolCenterY center point of image may be different from the center\n     * point of the symbol within the image.  (single point graphics)\n     * @deprecated\n     */\n    constructor(image: ImageBitmap, x: number, y: number, symbolCenterX: number, symbolCenterY: number);\n    /**\n     *\n     * ImageInfo holds and image and holds the position at which the image\n     * should be drawn.  Use this if the image is a single point graphic.\n     * @param image {@link BufferedImage}\n     * @param x position of where the image should be drawn\n     * @param y position of where the image should be drawn\n     * @param symbolCenterX center point of image may be different center\n     * point of the symbol within the image. (single point graphics)\n     * @param symbolCenterY center point of image may be different center\n     * point of the symbol within the image.  (single point graphics)\n     * @param symbolBounds minimum bounding rectangle for the core symbol. Does\n     * not include modifiers, display or otherwise.\n     */\n    constructor(image: ImageBitmap, x: number, y: number, symbolCenterX: number, symbolCenterY: number, symbolBounds: Rectangle2D);\n    /**\n     * The BufferedImage\n     * @return the actual image\n     */\n    getImage(): ImageBitmap;\n    /**\n     * X position of where the image should be drawn\n     * @return {@link Integer}\n     */\n    getX(): number;\n    /**\n     * Y position of where the image should be drawn\n     * @return {@link Integer}\n     */\n    getY(): number;\n    /**\n     * position of where the image should be drawn\n     * @return {@link Point}\n     */\n    getPoint(): Point;\n    /**\n     * The x value the image should be centered on or the \"anchor point\".\n     * @return {@link Integer}\n     */\n    getSymbolCenterX(): number;\n    /**\n     * The y value the image should be centered on or the \"anchor point\".\n     * @return {@link Integer}\n     */\n    getSymbolCenterY(): number;\n    /**\n     * The point the image should be centered on or the \"anchor point\".\n     * @return {@link Point}\n     */\n    getSymbolCenterPoint(): Point2D;\n    /**\n     * minimum bounding rectangle for the core symbol. Does\n     * not include modifiers, display or otherwise.\n     * @return {@link Rectangle2D}\n     */\n    getSymbolBounds(): Rectangle2D;\n    /**\n     * Dimension of the entire image.\n     * @return {@link Rectangle2D}\n     */\n    getImageBounds(): Rectangle2D;\n    /**\n     * Takes an image and a center point and generates a new, bigger image\n     * that has the symbol centered in it\n     * @param image {@link BufferedImage}\n     * @param point {@link Point2D}\n     * @return {@link BufferedImage}\n     */\n    static CenterImageOnPoint(image: ImageBitmap, point: Point2D): ImageBitmap;\n    /**\n     * Adds padding as needed to make the image a nice square.\n     * @return {@link ImageInfo}\n     */\n    getSquareImageInfo(): ImageInfo;\n}\n//# sourceMappingURL=ImageInfo.d.ts.map","milsym/renderer/utilities/LRUCache.d.ts":"import { ImageInfo } from \"./ImageInfo\";\n/**\n * Modified LRUCache implementation from: https://techblogstation.com/java/lru-cache-implementation-in-java/\n * and\n * https://www.baeldung.com/java-lru-cache\n */\nexport declare class LRUCache {\n    private lruElement;\n    private mruElement;\n    private container;\n    private capacity;\n    private currentSize;\n    constructor(capacity: number);\n    get(key: string): ImageInfo | null;\n    put(key: string, value: ImageInfo): void;\n}\n//# sourceMappingURL=LRUCache.d.ts.map","milsym/renderer/utilities/LRUEntry.d.ts":"import { ImageInfo } from \"./ImageInfo\";\nexport declare class LRUEntry {\n    key: string;\n    value: ImageInfo;\n    next: LRUEntry;\n    prev: LRUEntry;\n    constructor(prev: LRUEntry, next: LRUEntry, key: string, value: ImageInfo);\n}\n//# sourceMappingURL=LRUEntry.d.ts.map","milsym/renderer/utilities/LogLevel.d.ts":"/**\n * Port of java.util.logging.Level class\n */\nexport declare class LogLevel {\n    /**\n     * OFF is a special level that can be used to turn off logging.\n     * This level is initialized to <CODE>Integer.MAX_VALUE</CODE>.\n     */\n    static OFF: LogLevel;\n    /**\n     * SEVERE is a message level indicating a serious failure.\n     * <p>\n     * In general SEVERE messages should describe events that are\n     * of considerable importance and which will prevent normal\n     * program execution.   They should be reasonably intelligible\n     * to end users and to system administrators.\n     * This level is initialized to <CODE>1000</CODE>.\n     */\n    static SEVERE: LogLevel;\n    /**\n     * WARNING is a message level indicating a potential problem.\n     * <p>\n     * In general WARNING messages should describe events that will\n     * be of interest to end users or system managers, or which\n     * indicate potential problems.\n     * This level is initialized to <CODE>900</CODE>.\n     */\n    static WARNING: LogLevel;\n    /**\n     * INFO is a message level for informational messages.\n     * <p>\n     * Typically INFO messages will be written to the console\n     * or its equivalent.  So the INFO level should only be\n     * used for reasonably significant messages that will\n     * make sense to end users and system administrators.\n     * This level is initialized to <CODE>800</CODE>.\n     */\n    static INFO: LogLevel;\n    /**\n     * CONFIG is a message level for static configuration messages.\n     * <p>\n     * CONFIG messages are intended to provide a variety of static\n     * configuration information, to assist in debugging problems\n     * that may be associated with particular configurations.\n     * For example, CONFIG message might include the CPU type,\n     * the graphics depth, the GUI look-and-feel, etc.\n     * This level is initialized to <CODE>700</CODE>.\n     */\n    static CONFIG: LogLevel;\n    /**\n     * FINE is a message level providing tracing information.\n     * <p>\n     * All of FINE, FINER, and FINEST are intended for relatively\n     * detailed tracing.  The exact meaning of the three levels will\n     * vary between subsystems, but in general, FINEST should be used\n     * for the most voluminous detailed output, FINER for somewhat\n     * less detailed output, and FINE for the  lowest volume (and\n     * most important) messages.\n     * <p>\n     * In general the FINE level should be used for information\n     * that will be broadly interesting to developers who do not have\n     * a specialized interest in the specific subsystem.\n     * <p>\n     * FINE messages might include things like minor (recoverable)\n     * failures.  Issues indicating potential performance problems\n     * are also worth logging as FINE.\n     * This level is initialized to <CODE>500</CODE>.\n     */\n    static FINE: LogLevel;\n    /**\n     * FINER indicates a fairly detailed tracing message.\n     * By default logging calls for entering, returning, or throwing\n     * an exception are traced at this level.\n     * This level is initialized to <CODE>400</CODE>.\n     */\n    static FINER: LogLevel;\n    /**\n     * FINEST indicates a highly detailed tracing message.\n     * This level is initialized to <CODE>300</CODE>.\n     */\n    static FINEST: LogLevel;\n    /**\n     * ALL indicates that all messages should be logged.\n     * This level is initialized to <CODE>Integer.MIN_VALUE</CODE>.\n     */\n    static ALL: LogLevel;\n    private name;\n    private value;\n    constructor(name: string, value: number);\n    intValue(): number;\n    getName(): string;\n    toString(): string;\n}\n//# sourceMappingURL=LogLevel.d.ts.map","milsym/renderer/utilities/MODrawRules.d.ts":"/**\n * Draw Rules for METEOROLOGICAL AND OCEANOGRAPHIC SYMBOLOGY\n *\n */\nexport declare class MODrawRules {\n    /**\n     * Usually an entry in the MilStd that is just a category containing other symbols\n     * and not something that gets drawn itself.\n     */\n    static readonly DONOTDRAW: number;\n    /**\n     * Anchor Points: This graphic requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area's size and shape.\n     * Size/Shape: Scalable.\n     * Orientation: Not applicable.\n     */\n    static readonly AREA1: number;\n    /**\n     * Anchor Points: This graphic requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area's size and shape.\n     * Size/Shape: Determined by the anchor points.\n     * Orientation: Not applicable.\n     */\n    static readonly AREA2: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point. The center point\n     * defines the center of the graphic.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is typically centered over the desired location.\n     */\n    static readonly POINT1: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point. The center point\n     * defines the geometric center of the graphic.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is centered over the anchor location.\n     */\n    static readonly POINT2: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point. The center point\n     * defines the center of the graphic.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is oriented upright on the display and operator-centered over the desired location.\n     */\n    static readonly POINT3: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point. The center point\n     * defines the center of the graphic.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is centered over the location of the reported wind.\n     */\n    static readonly POINT4: number;\n    /**\n     * Anchor Points: This graphic requires a minimum of two anchor points.\n     * The first point defines the location of the plot circle.\n     * Additional points define the wind shaft and the speed of the wind.\n     * Wind speed is depicted on the shaft using a combination of the shaft\n     * alone (1-2 knots), half barbs (5 knots), barbs (10 knots)\n     * and pennants (50 knots). Wind speeds 5 knots or greater are rounded\n     * to the nearest 5 knots. Missing wind speed is depicted by an \"X\" at\n     * the end of the wind shaft. Winds with missing direction are not displayed.\n     *\n     * Size/Shape: Not applicable.\n     *\n     * Orientation: The shaft of the graphic is oriented with reference to\n     * true north in the direction from which the wind is blowing to the\n     * nearest 10 degrees. The barbs and pennants lie back from the shaft at\n     * an angle of 120 degrees and are oriented to the left of the shaft in\n     * the Northern Hemisphere and to the right in the Southern Hemisphere.\n     * The graphic is operator-centered over the desired location.\n     */\n    static readonly POINT5: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point. The center point\n     * defines the center of the graphic.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is centered over the location of the reported cloud cover.\n     */\n    static readonly POINT6: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point. The center point\n     * defines the center of the graphic.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is centered over the location of the reported conditions.\n     */\n    static readonly POINT7: number;\n    /**\n     * Anchor Points: This graphic requires one anchor point. The center point\n     * defines the center of the graphic.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is centered over the position of the tropical system.\n     */\n    static readonly POINT8: number;\n    /**\n     * Anchor Points: This graphic requires at least two anchor points,\n     * points 1 and 2, to define the line. Additional points can be defined\n     * to extend the line.\n     * Size/Shape: Scalable/Curve. The curvature of the line is operator defined.\n     * Orientation: The first and last anchor points determine the length of\n     * the line.\n     */\n    static readonly LINE1: number;\n    /**\n     * Anchor Points: This graphic requires at least two anchor points,\n     * points 1 and 2, to define the line. Additional points can be defined to\n     * extend the line.\n     *\n     * Size/Shape: Scalable/Curve. The points are typically connected with a\n     * straight line consisting of a short line section and an alternating\n     * V shape. The curvature and amplitude of the waves of the line is\n     * operator defined.\n     *\n     * Orientation: The first and last anchor points determine the length of\n     * the line. The line should be drawn so the \"V\" shapes are facing in\n     * the direction of movement. The \"V\" shapes and short line segment will\n     * alternate along the line.\n     */\n    static readonly LINE2: number;\n    /**\n     * Anchor Points: This graphic requires at least two anchor points,\n     * points 1 and 2, to define the line. Additional points can be defined to\n     * extend the line.\n     *\n     * Size/Shape: Scalable/Curve. The points are typically connected with a\n     * straight line consisting of a short line section and alternating two\n     * dots. The curvature and amplitude of the waves of the line is\n     * operator defined.\n     *\n     * Orientation: The first and last anchor points determine the length of the line. The two dots and the short line segment will alternate along the line.\n     */\n    static readonly LINE3: number;\n    /**\n     * Anchor Points: This graphic requires at least two anchor points,\n     * points 1 and 2, to define the line. Additional points can be defined to\n     * extend the line.\n     *\n     * Size/Shape: Scalable/Curve. The points are typically connected with a\n     * curved/wavy line consisting of a short line and one dot. The curvature\n     * and amplitude of the waves of the line is operator defined.\n     *\n     * Orientation: The first and last anchor points determine the length of\n     * the line. The dot and the short line segment will alternate along\n     * the line.\n     */\n    static readonly LINE4: number;\n    /**\n     * Anchor Points: This graphic requires at least three anchor points to\n     * define the boundary of the area. Add as many points as necessary to\n     * accurately reflect the area's size and shape.\n     *\n     * Size/Shape: Determined by the anchor points.\n     *\n     * Orientation: The first and last anchor points determine the length of\n     * the line. The dual line segments will be parallel to slightly wider at\n     * the western end.\n     */\n    static readonly LINE5: number;\n    /**\n     * Anchor Points: This graphic requires at least two anchor points,\n     * points 1 and 2, to define the line. Additional points can be defined to\n     * extend the line.\n     * Size/Shape: Scalable/Curve. The points are typically connected with a\n     * solid straight line with alternating slanted lines connected as depicted\n     * in the example to indicate convergence.\n     * Orientation: The first and last anchor points determine the length of\n     * the line. The alternating slanted lines will be evenly spaced along\n     * the line. Orientation is determined by the anchor points.\n     */\n    static readonly LINE6: number;\n    /**\n     * Anchor Points: This graphic requires at least two anchor points,\n     * points 1 and 2, to define the line. Additional points can be defined to\n     * extend the line.\n     *\n     * Size/Shape: Scalable/Curve. The points are typically connected with a\n     * dashed straight or curved line. The curvature of the line is operator\n     * defined.\n     *\n     * Orientation: The first and last anchor points determine the length of\n     * the line. The red and green line segments will alternate along the line.\n     * Orientation is determined by the anchor points.\n     */\n    static readonly LINE7: number;\n    /**\n     * Anchor Points: This graphic requires at least two anchor points,\n     * points 1 and 2, to define the line. Additional points can be defined to\n     * extend the line.\n     * Size/Shape: Scalable.\n     * Orientation: The graphic is oriented upright on the display as shown in\n     * the example and operator-centered over the desired location.\n     */\n    static readonly LINE8: number;\n}\n//# sourceMappingURL=MODrawRules.d.ts.map","milsym/renderer/utilities/MSInfo.d.ts":"/**\n * This class holds information about the MilStd Symbol.\n * Name, geometry, point count, Draw Rule, etc...\n * Can be retrived from {@link MSLookup}.\n */\nexport declare class MSInfo {\n    private _Version;\n    private _Name;\n    private _Path;\n    private _SymbolSet;\n    private _SymbolSetInt;\n    private _EntityCode;\n    private _Geometry;\n    private _Modifiers;\n    private _MinPointCount;\n    private _MaxPointCount;\n    private _DrawRule;\n    /**\n     * @param version SymbolID.Version_2525Dch1 (11), SymbolID.Version_2525E (13)\n     * @param symbolSet the 5th &amp; 6th character in the symbol Code, represents Battle Dimension\n     * @param entity descriptor\n     * @param entityType descriptor\n     * @param entitySubType  descriptor\n     * @param entityCode characters 11 - 16 in the symbol code\n     * @param modifiers String[] of modifier codes\n     */\n    constructor(version: number, symbolSet: string, entity: string, entityType: string, entitySubType: string, entityCode: string, modifiers: Array<string>);\n    /**\n     * @param version SymbolID.Version_2525Dch1 (11), SymbolID.Version_2525E (13)\n     * @param symbolSet the 5th &amp; 6th character in the symbol Code, represents Battle Dimension\n     * @param entity descriptor\n     * @param entityType descriptor\n     * @param entitySubType  descriptor\n     * @param entityCode characters 11 - 16 in the symbol code\n     * @param geometry \"point\", \"line\", \"area\"\n     * @param drawRule as defined in 2525D for Control Measures and METOC (i.e. \"Point1\")\n     * @param modifiers ArrayList of modifiers that are allowed for this symbol\n     */\n    constructor(version: number, symbolSet: string, entity: string, entityType: string, entitySubType: string, entityCode: string, geometry: string, drawRule: string, modifiers: Array<string>);\n    private parseDrawRule;\n    static parseSymbolSetName(symbolID: string): string;\n    static parseSymbolSetName(ss: string, version: number): string;\n    /**\n     *\n     * @param drawRule - Like DrawRules.CIRCULAR2\n     * @return int[] where the first index is the minimum required points and\n     * the next index is the maximum allowed points\n     */\n    private static getMinMaxPointsFromDrawRule;\n    private static getMinMaxPointsFromMODrawRule;\n    getVersion(): number;\n    getName(): string;\n    getPath(): string;\n    getGeometry(): string;\n    getDrawRule(): number;\n    getSymbolSet(): number;\n    getEntityCode(): number;\n    getBasicSymbolID(): string;\n    getMinPointCount(): number;\n    getMaxPointCount(): number;\n    getModifiers(): Array<string>;\n}\n//# sourceMappingURL=MSInfo.d.ts.map","milsym/renderer/utilities/MSLookup.d.ts":"import { MSInfo } from \"./MSInfo\";\n/**\n * Class that holds all the  objects with symbol information\n */\nexport declare class MSLookup {\n    private static mse;\n    private static msd;\n    private static _instance;\n    private static _initCalled;\n    private static _isReady;\n    private static _MSLookupD;\n    private static _MSLookupE;\n    private _IDListD;\n    private _IDListE;\n    private static msdJSON;\n    private static mseJSON;\n    /** Tracks which symbol sets have been lazily loaded into the maps */\n    private static _loadedSymbolSetsD;\n    private static _loadedSymbolSetsE;\n    /** When true, init() skips eager loading of monolithic JSON */\n    private static _lazyMode;\n    /** Promise tracking for in-flight lazy loads */\n    private static _loadingPromises;\n    /**\n     * @deprecated\n     */\n    static setData(urls: string[]): Promise<void>;\n    private constructor();\n    static getInstance(): MSLookup;\n    /**\n     * Initialize MSLookup in lazy mode — no monolithic JSON is loaded upfront.\n     * Symbol-set-specific data is loaded on demand via ensureSymbolSet().\n     *\n     * @returns Promise that resolves when the instance is ready for lazy loading\n     */\n    static initLazy(): Promise<MSLookup>;\n    /**\n     * Ensure a symbol set's MS data is loaded and available for synchronous lookup.\n     * In eager mode, this is a no-op. In lazy mode, it loads the chunk on demand.\n     *\n     * @param symbolSet Two-digit symbol set code (e.g., \"10\", \"25\")\n     * @param version SymbolID version constant\n     */\n    static ensureSymbolSet(symbolSet: string, version: number): Promise<void>;\n    /**\n     * Ensure all symbol sets needed for a full SIDC are loaded.\n     */\n    static ensureForSIDC(symbolID: string): Promise<void>;\n    /**\n     * Returns whether lazy mode is active.\n     */\n    static isLazyMode(): boolean;\n    /**\n     * Check if a specific symbol set is loaded (useful in lazy mode).\n     */\n    static isSymbolSetLoaded(symbolSet: string, version: number): boolean;\n    private init;\n    isReady(): boolean;\n    private populateLookup;\n    private AddVersion10Symbols;\n    /**\n     * Populate lookup maps from a lazily-loaded chunk.\n     * The chunk format is: { symbolSet: \"NN\", symbols: [...] }\n     * where symbols is the same format as the original JSON SYMBOL array.\n     */\n    private populateLookupFromChunk;\n    /**\n     * Async version of getMSLInfo — ensures the required symbol set is loaded\n     * before performing the lookup. Safe to use in both eager and lazy modes.\n     */\n    getMSLInfoAsync(basicID: string, version: number): Promise<MSInfo | null>;\n    getMSLInfoAsync(symbolID: string): Promise<MSInfo | null>;\n    /**\n     * Get memory usage stats from the DataLoader (lazy mode only).\n     */\n    static getDataLoaderStats(): import(\"./IDataLoader\").DataLoaderStats;\n    /**\n     * Reset the singleton for testing purposes.\n     */\n    static resetForTesting(): void;\n    private populateModifierList;\n    /**\n     * @param symbolID Full 20-30 digits from the symbol code\n     * @return\n     */\n    getMSLInfo(symbolID: string): MSInfo;\n    /**\n     * @param basicID id SymbolSet + Entity code like 50110100\n     * @param version like SymbolID.Version_2525Dch1\n     * @return\n     */\n    getMSLInfo(basicID: string, version: number): MSInfo;\n    /**\n     * returns a list of all the keys in the order they are listed in the MilStd 2525D document.\n     * @param version see {@link SymbolID.Version_2525E} and {@link SymbolID.Version_2525Dch1}\n     * @return\n     */\n    getIDList(version: number): Array<string>;\n    addCustomSymbol(msInfo: MSInfo): boolean;\n}\nexport declare const msLookup: MSLookup;\n//# sourceMappingURL=MSLookup.d.ts.map","milsym/renderer/utilities/MilStdAttributes.d.ts":"/**\n * Symbol attribute constants\n */\nexport declare class MilStdAttributes {\n    /**\n     * Line color of the symbol. hex value.\n     */\n    static readonly LineColor: string;\n    /**\n     * Fill color of the symbol. hex value\n     */\n    static readonly FillColor: string;\n    /**\n     * Main color of internal icon.  Only relevant to framed symbols. hex value\n     */\n    static readonly IconColor: string;\n    /**\n     * size of the single point image\n     */\n    static readonly PixelSize: string;\n    /**\n     * defaults to true\n     */\n    static readonly KeepUnitRatio: string;\n    /**\n     * transparency value of the symbol with values from 0 - 255.\n     */\n    static readonly Alpha: string;\n    /**\n     * outline the symbol, true/false\n     */\n    static readonly OutlineSymbol: string;\n    /**\n     * specify and outline color rather than letting renderer picking\n     * the best contrast color. hex value\n     */\n    static readonly OutlineColor: string;\n    /**\n     * just draws the core symbol\n     */\n    static readonly DrawAsIcon: string;\n    /**\n     * Specifies the line width of the multipoint symbology\n     */\n    static readonly LineWidth: string;\n    /**\n     * Specifies the color for text labels\n     */\n    static readonly TextColor: string;\n    /**\n     * Specifies the color for the text background (color outline or fill)\n     */\n    static readonly TextBackgroundColor: string;\n    /**\n     * If false, the renderer will create a bunch of little lines to create\n     * the \"dash\" effect (expensive but necessary for KML).\n     * If true, it will be on the user to create the dash effect using the\n     * DashArray from the Stroke object from the ShapeInfo object.\n     */\n    static readonly UseDashArray: string;\n    /**\n     * The mode that altitude labels will be displayed in, the default value is AMSL.\n     *\n     * This value acts as a label, appending whatever string that is passed in to the end of the altitude units.\n     * Currently only effective for multi-point graphics.\n     */\n    static readonly AltitudeMode: string;\n    /**\n     * At the moment, this refers to the optional range fan labels.\n     */\n    static readonly HideOptionalLabels: string;\n    /**\n     * For internal use\n     */\n    static readonly UsePatternFill: string;\n    /**\n     * For internal use\n     */\n    static readonly PatternFillType: string;\n    /**\n     * The conversion factor and the label that you want all distances to display in. The conversion factor\n     * is converting from meters. The default unit is meters.<br><br>\n     *\n     * Must be in the form [conversionFactor],[label]. So for example converting to feet would be \"3.28084,FT\".\n     * The helper class {@link DistanceUnit} can be used.\n     */\n    static readonly DistanceUnits: string;\n    /**\n     * The conversion factor and the label that you want all distances to display in.\n     * Conventionally, the conversion factor is converting from meters by default,\n     * but other values could be passed, like \"1,KM\" to use an unaltered value in kilometers.<br><br>\n     *\n     * Must be in the form [conversionFactor],[label]. So for example converting meters to feet would be \"3.28084,FT\".\n     * The helper class {@link DistanceUnit} can be used.\n     * Currently only effective for multi-point graphics.\n     */\n    static readonly AltitudeUnits: string;\n    /**\n     * If the engagement/target amplifier bar is to be used to designate targets, non-targets, and\n     * pruned or expired targets, a different coloring schema shall be used. Hostile tracks which\n     * are deemed targets shall have a red bar (RGB: 255, 0, 0) to indicate target. For hostile\n     * tracks deemed to be non-targets, white (RGB: 255, 255, 255) should be used to indicate non\n     * target. Finally, for hostile tracks which have been pruned or have expired shall be colored\n     * orange (RGB: 255, 120, 0).\n     * This attribute expects a hex string for the color\n     */\n    static readonly EngagementBarColor: string;\n    /**\n     * Multipoint features and patterns scale with line width ({@link MilStdAttributes#LineWidth}).\n     * {@code PatternScale} is the ratio of how much to increase features and patterns by with line width.\n     * default value is {@link RendererSettings#getPatternScale()}\n     */\n    static readonly PatternScale: string;\n    /**\n     * like \"arial\"\n     */\n    static readonly FontFamily: string;\n    /**\n     * Like Font.BOLD\n     */\n    static readonly FontStyle: string;\n    static readonly FontSize: string;\n    /**\n     * Strict (\"0\") for always placing their labels in the specified location\n     * even if there's empty space from other labels that weren't populated\n     * Flexible (\"1\") to collapse label vertically to the center to eliminate\n     * empty space from labels that weren't populated.\n     * Does not apply to Control Measures or METOCS\n     * Set with values like:\n     * RendererSettings.ModifierPlacement_STRICT (\"0\")\n     * RendererSettings.ModifierPlacement_FLEXIBLE (\"1\")\n     */\n    static readonly ModifierPlacement: string;\n    /**\n     * No Longer relevant\n     * @return\n     * @deprecated see {@link GetAttributesList()}\n     */\n    static GetModifierList(): Array<string>;\n    static GetAttributesList(symbolID: string): Array<string>;\n    /**\n     * @param attribute constant like MilStdAttributes.LineColor\n     * @return attribute name based on attribute constants\n     */\n    static getAttributeName(attribute: string): string;\n    /**\n     * Takes a string representation of an attribute and returns the appropriate int key value\n     * @param attribute \"LINECOLOR\" will return MilStdAtttributes.LineColor\n     * @return number value representing Attribute constant.\n     */\n    static getAttributeKey(attribute: string): string | null;\n}\n//# sourceMappingURL=MilStdAttributes.d.ts.map","milsym/renderer/utilities/MilStdSymbol.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nimport { TexturePaint } from \"../../graphics/TexturePaint\";\nimport { Color } from \"./Color\";\nimport { DistanceUnit } from \"./DistanceUnit\";\nimport { ShapeInfo } from \"./ShapeInfo\";\n/**\n * Object that holds information on how to draw a multipoint symbol after {@link armyc2.c5isr.web.render.WebRenderer#RenderMultiPointAsMilStdSymbol(String, String, String, String, String, String, double, String, Map, Map)}  is called.\n */\nexport declare class MilStdSymbol {\n    /**\n     * modifiers\n     */\n    private _Properties;\n    private _X_Altitude;\n    private _AM_Distance;\n    private _AN_Azimuth;\n    private _symbolID;\n    /**\n     * unique ID for this symbol, for client use\n     */\n    private _UUID;\n    private _SymbolShapes;\n    /**\n     * collection of shapes for the modifiers\n     */\n    private _ModifierShapes;\n    private _Coordinates;\n    private _UnitSize;\n    private _scale;\n    private _KeepUnitRatio;\n    protected _LineWidth: number;\n    protected _LineColor: Color;\n    protected _FillColor: Color;\n    protected _TextColor: Color;\n    protected _TextBackgroundColor: Color;\n    protected _Rotation: number;\n    protected _Outline: boolean;\n    protected _OutLineColor: Color;\n    protected _OutLineWidth: number;\n    protected _tp: TexturePaint;\n    protected _fs: boolean;\n    protected _patternFillType: number;\n    private patternScale;\n    private static _AltitudeMode;\n    private static _AltitudeUnit;\n    private static _DistanceUnit;\n    private static _useDashArray;\n    private static _hideOptionalLabels;\n    private static _DrawAffiliationModifierAsLabel;\n    private static _UseLineInterpolation;\n    protected _Tag: any;\n    /**\n     * Text is not affected by scale changes\n     */\n    static readonly TextScaleSensitive_No: number;\n    /**\n     * Text is affected only by significant scale changes\n     */\n    static readonly TextScaleSensitive_OnSlightZoomIn: number;\n    /**\n     * Text is affected by zoom in scale changes\n     */\n    static readonly TextScaleSensitive_OnZoomIn: number;\n    /**\n     * Text is affected by any scale changes\n     */\n    static readonly TextScaleSensitive_OnZoomInOut: number;\n    /**\n     * Symbol is not affected by scale changes\n     */\n    static readonly SymbolScaleSensitive_No: number;\n    /**\n     * Symbol has arrow heads that are affected by scale change\n     */\n    static readonly SymbolScaleSensitive_ArrowHeads: number;\n    /**\n     * Symbol has lines details that are affected by scale change (like FLOT)\n     */\n    static readonly SymbolScaleSensitive_DecoratedLines: number;\n    /**\n     * Symbol has pattern fills that are affected by scale if you're not using the fill pattern image\n     * ShapeInfo.getPatternFillImage()\n     */\n    static readonly SymbolScaleSensitive_PatternFills: number;\n    /**\n     *\n     * @param symbolID code, 20-30 digits long that represents the symbol\n     * @param uniqueID for the client's use\n     * @param modifiers use keys from Modifiers.\n     * @param Coordinates {@link ArrayList} of {@link Point2D} coordinates for the symbol\n     * @param keepUnitRatio - default TRUE\n     * modifiers\n     */\n    constructor(symbolID: string, uniqueID: string, Coordinates: Array<Point2D>, modifiers: Map<string, string>, keepUnitRatio?: boolean);\n    getFillStyle(): TexturePaint;\n    setFillStyle(value: TexturePaint): void;\n    getUseFillPattern(): boolean;\n    setUseFillPattern(value: boolean): void;\n    /**\n     * @deprecated\n     */\n    getPatternFillType(): number;\n    /**\n     * 0=Solid, 2=ForwardHatch, 3=BackwardHatch, 4=verticalHatch, 5=horizonalHatch, 8=CrossHatch\n     * Only affects Basic Shapes.  Will not apply to MilStd Symbology so as to not confuse some\n     * symbols with others.\n     * @param value {@link Integer}\n     *\n     * @deprecated\n     */\n    setPatternFillType(value: number): void;\n    getAltitudeMode(): string;\n    setAltitudeMode(value: string): void;\n    getAltitudeUnit(): DistanceUnit;\n    setAltitudeUnit(unit: DistanceUnit): void;\n    getDistanceUnit(): DistanceUnit;\n    setDistanceUnit(unit: DistanceUnit): void;\n    getUseDashArray(): boolean;\n    setUseDashArray(value: boolean): void;\n    getHideOptionalLabels(): boolean;\n    setHideOptionalLabels(value: boolean): void;\n    setUseLineInterpolation(value: boolean): void;\n    getUseLineInterpolation(): boolean;\n    setUnitSize(pixelSize: number): void;\n    getUnitSize(): number;\n    setKeepUnitRatio(value: boolean): void;\n    getKeepUnitRatio(): boolean;\n    /**\n     * Determines how to draw the Affiliation Modifier. True to draw as modifier\n     * label in the \"E/F\" location. False to draw at the top right corner of the\n     * symbol\n     * @param value {@link Boolean}\n     *\n     * @deprecated\n     */\n    setDrawAffiliationModifierAsLabel(value: boolean): void;\n    /**\n     * True to draw as modifier label in the \"E/F\" location. False to draw at\n     * the top right corner of the symbol\n     * @return {@link Boolean}\n     *\n     * @deprecated\n     */\n    getDrawAffiliationModifierAsLabel(): boolean;\n    /**\n     * Returns the modifier map for the symbol\n     * @return {@link Map}\n     */\n    getModifierMap(): Map<string, string>;\n    /**\n     * sets the modifier map for the symbol\n     * @param modifiers {@link Map}\n     */\n    setModifierMap(modifiers: Map<string, string>): void;\n    /**\n     * Get a modifier value\n     * @param modifier {@link Modifiers}\n     * @return {@link String}\n     */\n    getModifier(modifier: string): string;\n    /**\n     * Gets modifier value based on modifier constant and index in array\n     * @param modifier {@link Modifiers}\n     * @param index {@link Integer} array location, only applicable to AM, AN and X\n     * @return {@link String}\n     */\n    getModifier(modifier: string, index: number): string;\n    /**\n     * Set a modifier value\n     * @param modifier {@link Modifiers}\n     * @param value {@link String}\n     */\n    setModifier(modifier: string, value: string): void;\n    /**\n     * Modifiers must be added in order. No setting index 2 without first\n     * setting index 0 and 1. If setting out of order is attempted, the value\n     * will just be added to the end of the list.\n     *\n     * @param modifier {@link Modifiers}\n     * @param value {@link String}\n     * @param index {@link Integer}\n     */\n    setModifier(modifier: string, value: string, index: number): void;\n    /**\n     * Get modifier value for AM, AN or X\n     * @param modifier {@link Modifiers}\n     * @param index {@link Integer} array location\n     * @return {@link Double}\n     */\n    getModifier_AM_AN_X(modifier: string, index: number): number | null;\n    setModifier_AM_AN_X(modifier: string, value: number, index: number): void;\n    getModifiers_AM_AN_X(modifier: string): Array<number> | null;\n    setModifiers_AM_AN_X(modifier: string, modifiers: Array<number>): void;\n    /**\n     *\n     * @param value {@link Color}\n     */\n    setFillColor(value: Color): void;\n    /**\n     *\n     * @return {@link Color}\n     */\n    getFillColor(): Color;\n    /**\n    *\n    * @param value {@link Color}\n    */\n    setTextColor(value: Color): void;\n    /**\n     *\n     * @return {@link Color}\n     */\n    getTextColor(): Color;\n    /**\n    *\n    * @param value {@link Color}\n    */\n    setTextBackgroundColor(value: Color): void;\n    /**\n     *\n     * @return {@link Color}\n     */\n    getTextBackgroundColor(): Color;\n    /**\n     *\n     * @param value {@link Integer}\n     */\n    setLineWidth(value: number): void;\n    /**\n     *\n     * @return {@link Integer}\n     */\n    getLineWidth(): number;\n    /**\n     * If value is null or SymbolUtilities.isGreenProtectionGraphic() is true then value is ignored\n     *\n     * @param value {@link Color}\n     */\n    setLineColor(value: Color | null): void;\n    /**\n     *\n     * @return {@link Color}\n     */\n    getLineColor(): Color;\n    /**\n     * if null, renderer will use white or black for the outline based on the color\n     * of the symbol. Otherwise, it will used the passed color value.\n     *\n     * @param value {@link Color}\n     */\n    setOutlineColor(value: Color): void;\n    getOutlineColor(): Color;\n    /**\n     * Extra value for client. defaults to null. Not used for rendering by\n     * JavaRenderer\n     *\n     * @param value  {@link Object}\n     * @deprecated\n     */\n    setTag(value: any): void;\n    /**\n     * Extra value for client. defaults to null. Not used for rendering by\n     * JavaRenderer\n     *\n     * @return {@link Object}\n     * @deprecated\n     */\n    getTag(): any;\n    /**\n     *\n     * @param value {@link ArrayList}\n     */\n    setCoordinates(value: Array<Point2D>): void;\n    /**\n     *\n     * @return {@link ArrayList}\n     */\n    getCoordinates(): Array<Point2D>;\n    /**\n     * Shapes that represent the symbol modifiers\n     *\n     * @param value ArrayList&lt;Shape&gt;\n     */\n    setModifierShapes(value: Array<ShapeInfo>): void;\n    /**\n     * Shapes that represent the symbol modifiers\n     *\n     * @return {@link ArrayList}\n     */\n    getModifierShapes(): Array<ShapeInfo>;\n    /**\n     * the java shapes that make up the symbol\n     *\n     * @param value ArrayList&lt;ShapeInfo&gt;\n     */\n    setSymbolShapes(value: Array<ShapeInfo>): void;\n    /**\n     * the java shapes that make up the symbol\n     *\n     * @return {@link ArrayList}\n     */\n    getSymbolShapes(): Array<ShapeInfo>;\n    /**\n     * The Symbol Id of the MilStdSymbol.\n     *\n     * @return {@link String}\n     */\n    getSymbolID(): string;\n    /**\n     * Unique ID of the Symbol. For client use.\n     *\n     * @return {@link String}\n     */\n    getUUID(): string;\n    /**\n     * Unique ID of the Symbol. For client use.\n     *\n     * @param ID {@link String}\n     */\n    setUUID(ID: string): void;\n    /**\n     * Sets the Symbol ID for the symbol. Should be a 20-30 digit string from\n     * the milstd.\n     *\n     * @param value {@link String}\n     */\n    setSymbolID(value: string): void;\n    private _wasClipped;\n    /**\n     *\n     * @param value\n     * @deprecated see {@link #setWasClipped(boolean)}\n     */\n    set_WasClipped(value: boolean): void;\n    /**\n     *\n     * @returns\n     * @deprecated see {@link #getWasClipped()}\n     */\n    get_WasClipped(): boolean;\n    setWasClipped(value: boolean): void;\n    getWasClipped(): boolean;\n    /**\n     * Determines if the symbol has integral or modifier/amplifier text that would\n     * be impacted if the maps is zoomed in or out after initial draw.\n     * @return 0=not sensitive, 1=slightly little zoom in sensitive, 2=zoom in sensitive, 3=zoom in/out sensitive\n     */\n    isTextScaleSensitive(): number;\n    /**\n    * Checks if the symbol has features that make it scale aware and would require a refresh\n    * on zooming in or out.\n    * @return 0=No,1=arrowheads,2=decoratedLines,3=patternFills\n    */\n    isSymbolScaleSensitive(): number;\n    /**\n     * Multipoint features and patterns scale with line width ({@link #getLineWidth()}).\n     * {@link #patternScale} is the ratio of how much to increase features and patterns by with line width.\n     * default value is 1.0. Can be set with {@link RendererSettings#setPatternScale(double)} and {@link MilStdAttributes#PatternScale}\n     * @param scale\n     */\n    setPatternScale(scale: number): void;\n    getPatternScale(): number;\n}\n//# sourceMappingURL=MilStdSymbol.d.ts.map","milsym/renderer/utilities/Modifier.d.ts":"/**\n *\n */\nexport declare class Modifier {\n    private _modifierID;\n    private _yIndex;\n    private _xIndex;\n    private _x;\n    private _y;\n    private _text;\n    private _centered;\n    constructor(id: string, text: string, indexX: number, indexY: number, centered: boolean, x?: number, y?: number);\n    getID(): string;\n    getText(): string;\n    getIndexX(): number;\n    getIndexY(): number;\n    getCentered(): boolean;\n    getX(): number;\n    setX(x: number): void;\n    getY(): number;\n    setY(y: number): void;\n}\n//# sourceMappingURL=Modifier.d.ts.map","milsym/renderer/utilities/Modifiers.d.ts":"/**\n * Modifier Constants to be used as keys in the modifiers map\n */\nexport declare class Modifiers {\n    /**\n     * <pre>\n     * Symbol Icon\n     * The innermost part of a symbol, comprised of an icon and optional modifiers, that represents a joint military object (see 5.3.4).\n     * Format: Graphic\n     * Symbol Set: All\n     * Remarks: Determined by SIDC positions 11-20.\n     * </pre>\n     */\n    static readonly A_SYMBOL_ICON: string;\n    /**\n     * <pre>\n     * Echelon\n     * An amplifier in a unit symbol that identifies command level (see 5.3.7.1 and table VII).\n     * Format: Graphic\n     * Symbol Set: All\n     * Remarks: Determined by SIDC positions 9-10.\n     * </pre>\n     */\n    static readonly B_ECHELON: string;\n    /**\n     * <pre>\n     * Quantity\n     * An amplifier in an equipment symbol that identifies the number of items present.\n     * Examples include:\n     * 350 Beds 50 Gallons\n     * Format:\n     * Alphanumeric - {1,19}\n     * [#########] [XXXXXXXXXX]\n     * Symbol Set: 10, 11, 15, 25, 27, 60\n     * Remarks: Two-part composite field.\n     * Where # is the numeric value [1-999999999], and X is the unit of measure.\n     * Note: There should be a space between the numeric and the unit of measure values.\n     * </pre>\n     */\n    static readonly C_QUANTITY: string;\n    /**\n     * <pre>\n     * Task Force Indicator\n     * An amplifier that identifies a unit or activities symbol as a task force (see 5.3.7.2 and figure 14).\n     * Format: Graphic\n     * Symbol Set: 10\n     * Remarks: Determined by SIDC position 8.\n     * </pre>\n     */\n    static readonly D_TASK_FORCE_INDICATOR: string;\n    /**\n     * <pre>\n     * Frame Shape Modifier\n     * A graphic modifier that displays standard identity, battle dimension, or exercise\n     * amplifying descriptors of an object (see 5.3.1 and table II).\n     * Format: Graphic\n     * Symbol Set: ALL BUT 25,45,46,47\n     * Remarks: 2525C, not processed as a modifier in 2525D+\n     * </pre>\n     */\n    static readonly E_FRAME_SHAPE_MODIFIER: string;\n    /**\n     * <pre>\n     * Reinforced or Reduced\n     * An amplifier in a unit symbol that displays (+) for reinforced, (-) for reduced, (<u>+</u>) reinforced and reduced.\n     * Format: Alphanumeric - {1,1}\n     * Symbol Set: 10\n     * Remarks:\n     * </pre>\n     */\n    static readonly F_REINFORCED_REDUCED: string;\n    /**\n     * <pre>\n     * Staff Comments\n     * An amplifier for units, equipment and installations; content is implementation specific.\n     * Format: Alphanumeric - {1,20}\n     * Symbol Set: 01, 05, 10, 15, 20, 27, 30, 35, 40\n     * Remarks:\n     * </pre>\n     */\n    static readonly G_STAFF_COMMENTS: string;\n    /**\n     * <pre>\n     * Additional Information\n     * An amplifier for units, equipment and installations; content is implementation specific.\n     * Format: Alphanumeric - {1,20}\n     * Symbol Set: ALL\n     * Remarks:\n     * </pre>\n     */\n    static readonly H_ADDITIONAL_INFO_1: string;\n    /**\n     * <pre>\n     * Unlisted Point Information\n     * An alphanumeric text amplifier used to provide an up to a three-character letter field acronym to describe a point that is not already listed.\n     * Format: Alphanumeric - {1,3}\n     * Symbol Set: 25\n     * Remarks: Only used with Action Points (General) control measure. SIDC 130100.\n     * </pre>\n     */\n    static readonly H1_ADDITIONAL_INFO_2: string;\n    /**\n     * A text modifier for tactical graphics; content is\n     * implementation specific.\n     * CM: P,L,A,N,B/C,R/N\n     * Length: 20\n     * @deprecated 2525C\n     */\n    static readonly H2_ADDITIONAL_INFO_3: string;\n    /**\n     * <pre>\n     * Evaluation Rating\n     * An amplifier that consists of a one-letter reliability rating and a one-number credibility rating. (See ATP 2-33.4)\n     * Format: Alphanumeric - {2,2} [X][#]\n     * Symbol Set: 10, 15, 20,27, 40\n     * Remarks:\n     *\n     * Reliability Ratings:\n     * A-completely reliable\n     * B-usually reliable\n     * C-fairly reliable\n     * D-not usually reliable\n     * E-unreliable\n     * F-reliability cannot be judged\n     *\n     * Credibility Ratings:\n     * 1-confirmed by other sources\n     * 2-probably true\n     * 3-possibly true\n     * 4-doubtfully true\n     * 5-improbable\n     * 6-truth cannot be judged\n     * </pre>\n     */\n    static readonly J_EVALUATION_RATING: string;\n    /**\n     * <pre>\n     * Effectiveness\n     * An amplifier for units and installations that indicates unit effectiveness or installation capability.\n     * Format: Alphanumeric - {2,3}\n     * Symbol Set: 10, 15, 27\n     * Remarks:\n     * List of Values:\n     * FO - Fully Operational\n     * SO - Substantially operational\n     * MO - Marginally operational\n     * NO - Not operational\n     * UNK - Unknown\n     * </pre>\n     */\n    static readonly K_COMBAT_EFFECTIVENESS: string;\n    /**\n     * <pre>\n     * Signature Equipment\n     * An amplifier for hostile equipment; \"!\" indicates detectable electronic signatures.\n     * Format: Alphanumeric - {1,1}\n     * Symbol Set: 15\n     * Remarks: The amplifier displayed is the exclamation mark \"!\".\n     * </pre>\n     */\n    static readonly L_SIGNATURE_EQUIP: string;\n    /**\n     * <pre>\n     * Higher Formation\n     * An amplifier for units that indicates number or title of higher echelon command (corps are designated by Roman numerals).\n     * Format: Alphanumeric - {1,21}\n     * Symbol Set: 10\n     * Remarks: The amplifier displayed is the exclamation mark \"!\".\n     * </pre>\n     */\n    static readonly M_HIGHER_FORMATION: string;\n    /**\n     * <pre>\n     * Hostile (Enemy)\n     * An amplifier for equipment; letters \"ENY\" denote hostile symbols.\n     * Format: Alphanumeric - {3,3}\n     * Symbol Set: 15,25\n     * Remarks: Determined by SIDC position 4.\n     * Note: This amplifier must be used when displaying enemy/hostile control measures on monochromatic displays\n     * </pre>\n     */\n    static readonly N_HOSTILE: string;\n    /**\n     * <pre>\n     * IFF/SIF/AIS\n     * An amplifier displaying IFF/SIF/AIS Identification modes and codes.\n     * Format:\n     * Symbol Set: 01, 10, 15, 27, 30, 35\n     * Remarks: 'MODE' is not required when displaying.\n     * </pre>\n     */\n    static readonly P_IFF_SIF_AIS: string;\n    /**\n     * <pre>\n     * Direction of Movement Indicator\n     * An amplifier consisting of a line with arrow that identifies the direction of movement or intended movement of an object (see 5.3.7.9 and figure 14).\n     * Format: Graphic (Alphanumeric for exchange only)\n     * Symbol Set: 10, 15, 20, 25, 27\n     * Remarks:\n     * Renderer Assumes a value in degrees with no text\n     * MilStd:\n     *\n     * </pre>\n     */\n    static readonly Q_DIRECTION_OF_MOVEMENT: string;\n    /**\n     * <pre>\n     * Mobility Indicator\n     * An amplifier that depicts the mobility of an object not intrinsic to the entity itself (see 5.3.7.9, figure 14 and table IX).\n     * Format: Graphic\n     * Symbol Set: 10\n     * Remarks: Determined by SIDC positions 9-10\n     * </pre>\n     */\n    static readonly R_MOBILITY_INDICATOR: string;\n    /**\n     * <pre>\n     * SIGINT Mobility Indicator\n     * An amplifier that indicates the mobility of a SIGINT unit.\n     * Format: Alphanumeric - {1,1}\n     * Symbol Set: 50, 51, 52, 53, 54\n     * Remarks:\n     * List of Values:\n     * M = Mobile,\n     * S = Static\n     * U = Uncertain.\n     * </pre>\n     */\n    static readonly R2_SIGNIT_MOBILITY_INDICATOR: string;\n    /**\n     * <pre>\n     * Headquarters Staff Indicator\n     * An amplifier for units, installations and activities that identifies them as a headquarters (see figure 14 and figure 13).\n     * Format: Graphic\n     * Symbol Set: 10, 20, 40\n     * Remarks: Determined by SIDC position 8.\n     * </pre>\n     */\n    static readonly S_HQ_STAFF_INDICATOR: string;\n    /**\n     * <pre>\n     * Offset Location Indicator\n     * An amplifier used to indicate the offset or precise location of a single point symbol (see 5.3.7.5, 5.3.12, and figure 14).\n     * Format: Graphic\n     * Symbol Set: 10, 20, 25, 27, 40\n     * Remarks: Determined by individual system implementations, not implemented by the renderer.\n     * </pre>\n     */\n    static readonly S2_OFFSET_INDICATOR: string;\n    /**\n     * <pre>\n     * Unique Designation\n     * An amplifier that uniquely identifies a particular symbol or track number. Identifies acquisitions number when used with SIGINT symbology.\n     * Format: Alphanumeric - {1,30}\n     * Symbol Set: All\n     * Remarks:\n     * </pre>\n     */\n    static readonly T_UNIQUE_DESIGNATION_1: string;\n    /**\n     * <pre>\n     * Lines - Unique Identifier or Primary Purpose\n     * An amplifier that uniquely identifies a particular symbol or track number. Identifies acquisitions number when used with SIGINT symbology.\n     * Format: Alphanumeric - Lines: {1,30}, Points: Alphanumeric - {1,7}\n     * Symbol Set: 25\n     * Remarks:\n     * </pre>\n     */\n    static readonly T1_UNIQUE_DESIGNATION_2: string;\n    /**\n     * <pre>\n     * Lines - Unique Identifier or Primary Purpose\n     * An amplifier used to provide the designation of the Establishing Headquarters.\n     * Format: Alphanumeric - {1,7}\n     * Symbol Set: 25\n     * Remarks: Used with Fire Support Control Measures only.\n     * </pre>\n     */\n    static readonly T2_UNIQUE_DESIGNATION_3: string;\n    /**\n     * <pre>\n     * Type\n     * An amplifier for equipment that indicates types of equipment.\n     * Format: Alphanumeric - {1,24}\n     * Symbol Set: Not Installation(20), Activites(40), or CyberSpace(60)\n     * Remarks:\n     * Example:\n     * AH-64 for Attack Helicopter\n     * </pre>\n     */\n    static readonly V_EQUIP_TYPE: string;\n    /**\n     * <pre>\n     * Date/Time Group (DTG) W\n     * An amplifier for displaying a date-time group (DDHHMMSSZMONYYYY) or (DDHHMMZMMMYYYY) or \"O/O\" for On Order.\n     * Field \"W\" is used to provide DTG or if used with field \"W1\" the start of a period of time.\n     * Field \"W1\" is used to provide the end of a period of time.\n     * Format: Alphanumeric - {3,16}\n     * Symbol Set: 10, 15, 20, 25, 27, 40, 45\n     * Remarks:\n     * The date-time group is composed of a group of six numeric digits with a time zone suffix and the standardized three-letter abbreviation for the month followed by four digits representing the year.\n     * The first pair of digits represents the day; the second pair, the hour; the third pair, the minutes. For automated systems, two digits may be added before the time zone suffix and after the minutes to designate seconds (see 5.3.7.8).\n     * </pre>\n     */\n    static readonly W_DTG_1: string;\n    /**\n     * <pre>\n     * Date/Time Group (DTG) W1\n     * An amplifier for displaying a date-time group (DDHHMMSSZMONYYYY) or (DDHHMMZMMMYYYY) or \"O/O\" for On Order.\n     * Field \"W\" is used to provide DTG or if used with field \"W1\" the start of a period of time.\n     * Field \"W1\" is used to provide the end of a period of time.\n     * Format: Alphanumeric - {3,16}\n     * Symbol Set: 25\n     * Remarks:\n     * The date-time group is composed of a group of six numeric digits with a time zone suffix and the standardized three-letter abbreviation for the month followed by four digits representing the year.\n     * The first pair of digits represents the day; the second pair, the hour; the third pair, the minutes. For automated systems, two digits may be added before the time zone suffix and after the minutes to designate seconds (see 5.3.7.8).\n     * </pre>\n     */\n    static readonly W1_DTG_2: string;\n    /**\n     * <pre>\n     * Altitude/Depth\n     * An amplifier that displays either altitude, flight level, depth for submerged objects or height of equipment or structures on the ground, the minimum, maximum and/or specific altitude (in feet or meters in relation to a reference datum) or depth (for submerged objects in feet below sea level). See 5.3.7.6 for content.\n     * Format:\n     * Symbol Set: All but 40 & 60\n     * Remarks:\n     * SM = Statute Miles\n     * DM = Data Miles\n     * Notes:\n     * The Renderer handles the whole value as a string for Single Point Icons.\n     * For multipoints, it expects just a number and an accompanying attribute {@link MilStdAttributes.AltitudeUnits}\n     * Default behavior assumes feet.\n     * </pre>\n     */\n    static readonly X_ALTITUDE_DEPTH: string;\n    /**\n     * <pre>\n     * Location\n     * An amplifier that displays a symbol's location in degrees, minutes and decimal minutes (or in MGRS, GARS, or other applicable display formats).\n     * Format: Alphanumeric - {3,16}\n     * Symbol Set: 10, 15, 20, 25, 27, 30, 40\n     * Remarks: Exchange format is implementation specific.\n     * </pre>\n     */\n    static readonly Y_LOCATION: string;\n    /**\n     * <pre>\n     * Speed\n     * An amplifier that displays velocity (see 5.3.7.7).\n     * Format: Alphanumeric - {5,9} [#####] [XXX]\n     * Symbol Set: NOT 20, 35, 40, 60\n     * Remarks:\n     * Two-part composite field.\n     * Where # is the numeric value [1-999999999], and XXX is the unit of measure (KPH, KPS, MPH, NMH, KTS).\n     * Note: There should be a space between the numeric and the unit of measure values.\n     * </pre>\n     */\n    static readonly Z_SPEED: string;\n    /**\n     * <pre>\n     * Special C2 Headquarters\n     * A amplifier that is contained inside the frame in place of the main icon and contains the name of the special C2 Headquarters.\n     * Format: Alphanumeric - {1,9}\n     * Symbol Set: 10\n     * Remarks:\n     * Examples: Named command such as SHAPE, PACOM, and joint, multinational, or coalition commands such as CJTF, JTF, or MJTF.\n     * </pre>\n     */\n    static readonly AA_SPECIAL_C2_HQ: string;\n    /**\n     * <pre>\n     * Feint/Dummy indicator\n     * An amplifier that identifies an offensive or defensive unit, intended to draw the enemy's attention away from the area of the main attack.\n     * Format: Graphic\n     * Symbol Set: 10, 15, 20, 25\n     * Remarks: Determined by SIDC position 8.\n     * </pre>\n     */\n    static readonly AB_FEINT_DUMMY_INDICATOR: string;\n    /**\n     * <pre>\n     * Platform Type\n     * An amplifier that identifies the electronic identification for a pulsed or non-pulsed electromagnetic emission.\n     * Format: Alphanumeric - {5,5}\n     * Symbol Set: 10, 15\n     * Remarks:\n     * List of Values:\n     * ELNOT = Electronic intelligence notation\n     * CENOT = Communications intelligence notation\n     * </pre>\n     */\n    static readonly AD_PLATFORM_TYPE: string;\n    /**\n     * <pre>\n     * Platform Type\n     * An amplifier that identifies equipment teardown time in minutes.\n     * Format: Numeric – {1,3}\n     * Symbol Set: 10(2525E), 15\n     * Remarks:\n     * </pre>\n     */\n    static readonly AE_EQUIPMENT_TEARDOWN_TIME: string;\n    /**\n     * <pre>\n     * Common Identifier\n     * An amplifier to provide a common name used to identify an entity.\n     * Format: Alphanumeric - {1,12}\n     * Symbol Set: 10(2525E), 15, 27\n     * Remarks: Example: \"Hawk\" for Hawk SAM system.\n     * </pre>\n     */\n    static readonly AF_COMMON_IDENTIFIER: string;\n    /**\n     * <pre>\n     * Auxiliary Equipment Indicator\n     * An amplifier for equipment that indicates the presence of a towed sonar array (see 5.3.7.11, figure 14 and table IX).\n     * Format: Graphic\n     * Symbol Set: 15\n     * Remarks: Determined by SIDC positions 9-10.\n     * </pre>\n     */\n    static readonly AG_AUX_EQUIP_INDICATOR: string;\n    /**\n     * <pre>\n     * Area of Uncertainty\n     * An amplifier that indicates the area where an object is most likely to be, based on the object's last report and\n     * the reporting accuracy of the sensor that detected the object (see 5.3.7.13.1 and figure 18.\n     * Format: Graphic (Alphanumeric for exchange only)\n     * Symbol Set: 01, 05, 10, 15, 20, 25, 27, 30, 35, 40\n     * Remarks:\n     * The amplifier can be displayed as an ellipse, a bearing box, or a line of bearing, depending on the report received for the object.\n     * Notes: Not implemented by the renderer\n     * </pre>\n     */\n    static readonly AH_AREA_OF_UNCERTAINTY: string;\n    /**\n     * <pre>\n     * Dead Reckoning Trailer\n     * An amplifier that identifies where an object should be located at present, given its last reported course and speed (see 5.3.7.13.2).\n     * Format: Graphic\n     * Symbol Set: 01, 05, 10, 15, 20, 25, 27, 30, 35, 40\n     * Remarks:\n     * Locally derived information. This datum is not exchanged.\n     * Notes: Not implemented by the renderer\n     * </pre>\n     */\n    static readonly AI_DEAD_RECKONING_TRAILER: string;\n    /**\n     * <pre>\n     * Speed Leader\n     * An amplifier that depicts the speed and direction of movement of an object (see 5.3.7.13.3 and figure 18).\n     * Format: Graphic\n     * Symbol Set: 10, 15, 30, 35\n     * Remarks: Set as \"[Speed] [unit of measure] [angle in degrees if 3 characters \"045\", and in mils if 4 characters \"0150]\" like [100 KPH 045]\n     * Valid units of measure are: KPH, KPS, MPH, NMH, KTS\n     * Notes: It is recommended users handle this amplifier within their map engine many maps always show icons in the upright position when the map\n     * is rotated, invalidating the line that's render with the symbol.\n     * </pre>\n     */\n    static readonly AJ_SPEED_LEADER: string;\n    /**\n     * <pre>\n     * Pairing Line\n     * An amplifier that connects two objects and is updated dynamically as the positions of the two objects change (see 5.3.7.13.4 and figure 18).\n     * Format: Graphic\n     * Symbol Set: NA\n     * Remarks:\n     * Notes: Not implemented by the renderer\n     * </pre>\n     */\n    static readonly AK_PAIRING_LINE: string;\n    /**\n     * <pre>\n     * Operational Condition\n     * An amplifier that indicates operational condition or capacity.\n     * Format: Graphic\n     * Symbol Set: 01, 05,10, 15, 20, 30, 35\n     * Remarks: Determined by SIDC position 7.\n     * </pre>\n     */\n    static readonly AL_OPERATIONAL_CONDITION: string;\n    /**\n     * <pre>\n     * Distance\n     * An amplifier that displays a minimum, maximum, or a specific distance (range, radius, width, length, etc.), in meters.\n     * Format: Alphanumeric - {3,9} [#] [X]\n     * Symbol Set: 25\n     * Remarks:\n     * \"#\" is the value (range 0-99999)\n     * XXX is the distance unit.\n     * There should be a space between the integer and the unit of measure values.\n     * Where more than one distance is specified the AM amplifier will be suffixed with a numeric entry, e.g. AM1, AM2.\n     * Note:\n     * Renderer expects just a number or a comma delimited string of numbers and an accompanying attribute {@link MilStdAttributes.DistanceUnits}\n     * Default behavior assumes meters.\n     * </pre>\n     */\n    static readonly AM_DISTANCE: string;\n    /**\n     * <pre>\n     * Azimuth\n     * An amplifier that displays an angle measured from true north to any other line in degrees.\n     * Format: Alphanumeric - {7,8} [#####] [XXX]\n     * Symbol Set: 25\n     * Remarks:\n     * For Degrees entries.\n     * XXX DGT, where XXX is degrees from 000-359 and DGT is referenced to TRUE North.\n     * For Mils entries.\n     * XXXX MGT, where XXXX is MILS from 0000-6399 and MGT is referenced to TRUE North.\n     * Where more than one angle is specified the AN amplifier will be suffixed with a numeric entry, e.g. AN1, AN2.\n     * Note:\n     * Renderer expects just a number or a comma delimited string of numbers\n     * </pre>\n     */\n    static readonly AN_AZIMUTH: string;\n    /**\n     * Engagement Bar\n     * A graphic amplifier placed immediately atop the symbol. May denote:\n     * A) local/remote engagement status - 'R' for remote, 'B' for mix of local/remote, none for local\n     * B) engagement status\n     * C) weapon type.\n     *\n     * Format:\n     * Composite list of values\n     * Alphanumeric - {6,10}\n     * A:BBB-CC\n     * Symbol Set: 01, 05, 10, 30, 35\n     * Remarks: See 5.3.7.15.3 for explanation of engagement bar structure and codes.\n     * A - Type of Engagement\n     * BBB - Engagement Stage\n     * CC - Type of Weapon Assignment\n     */\n    static readonly AO_ENGAGEMENT_BAR: string;\n    /**\n     * <pre>\n     * Target Number\n     * An amplifier used in Fire Support operations to uniquely designate targets in accordance with STANAG 2934.\n     * Format: Alphanumeric - {6,6} [XX][####]\n     * Symbol Set: 25\n     * Remarks:\n     * Two-part composite field.\n     * Where positions 1-2 are text, and positions 3-6 are numeric.\n     * </pre>\n     */\n    static readonly AP_TARGET_NUMBER: string;\n    /**\n     * <pre>\n     * Target Number Extension\n     * An amplifier used to identify a target number extension which is a sequentially assigned number identifying the individual elements in a target (MIL-STD-6017),\n     * Format: Numeric – {2,3} [-##]\n     * Symbol Set: 25\n     * Remarks:\n     * Position 1 is a dash (-) and positions 2-3 are numbers, from 1 through 15.\n     * It is applicable only to the \"Point or Single Target\" symbol.\n     * It is conditional upon the presence of the Target Number amplifier and is visually displayed appended to the Target Number amplifier.\n     * </pre>\n     */\n    static readonly AP1_TARGET_NUMBER_EXTENSION: string;\n    /**\n     * <pre>\n     * Guarded Unit\n     * An amplifier used during ballistic missile defense. Some tracks are designated as guarded by a particular unit.\n     * Format: Alphanumeric - {6,6} [XX]\n     * Symbol Set: 10(2525E), 15, 20(2525E), 30\n     * Remarks:\n     * Single value:\n     * BG - Guarded Unit\n     * </pre>\n     */\n    static readonly AQ_GUARDED_UNIT: string;\n    /**\n     * <pre>\n     * Special Designator\n     * An amplifier that identifies special track designators.\n     * Format: Alphanumeric - {3,3}\n     * Symbol Set: 10, 30, 35\n     * Remarks:\n     * List of values:\n     * NRT - Non-Real Time.\n     * SIG - Tactically Significant Tracks.\n     * </pre>\n     */\n    static readonly AR_SPECIAL_DESIGNATOR: string;\n    /**\n     * <pre>\n     * Country\n     * A three-letter code that indicates the country of origin of the organization (US systems shall use GENC).\n     * Format: Alphanumeric - {3,3}\n     * Symbol Set: All\n     * Remarks: Determined by SIDC positions 28-30.\n     * </pre>\n     */\n    static readonly AS_COUNTRY: string;\n    /**\n     * <pre>\n     * Capacity of Installation\n     * Capacity of installation displayed.\n     * Format: Alphanumeric - {1,19} [#########] [XXXXXXXXXX]\n     * Symbol Set: 2525E addition, not currently defined.  Probably 20\n     * Remarks:\n     * Two-part composite field.\n     * Comprised of:\n     * Quantity 0-99999999 followed by the unit of measure.\n     * e.g. 400 Beds\n     * </pre>\n     */\n    static readonly AT_CAPACITY_OF_INSTALLATION: string;\n    /**\n     * <pre>\n     * Leadership\n     * Identifies Leadership (ONLY IN DISMOUNTED INDIVIDUAL)\n     * Format: Graphic\n     * Symbol Set: 27(2525E)\n     * Remarks: Determined by SIDC positions 9-10.\n     * </pre>\n     */\n    static readonly AV_LEADERSHIP: string;\n    /**\n     * <pre>\n     * Headquarters Element\n     * An amplifier that indicates what type of element of a headquarters is being represented, such as TOC, MAIN2.\n     * Format: Alphanumeric {0,8}\n     * Symbol Set: 10(2525E)\n     * Remarks: Location currently undefined\n     * </pre>\n     */\n    static readonly AW_HEADQUARTERS_ELEMENT: string;\n    /**\n     * <pre>\n     * Installation Composition\n     * Indicates the component type of the installation\n     * Format: Alphanumeric {3,7}\n     * Symbol Set: 20(2525E)\n     * Remarks: Location currently undefined\n     * List of Values:\n     * DEVELOP - Development.\n     * RSRCH - Research.\n     * PROD - Production.\n     * SVC - Service.\n     * STORE - Storage.\n     * UTIL - Utility.\n     * </pre>\n     */\n    static readonly AX_INSTALLATION_COMPOSITION: string;\n    /**\n     * <pre>\n     * Network Identifier\n     * Indicates the network the entity has privileges within.\n     * Format: Alphanumeric\n     * Symbol Set: undefined\n     * Remarks:\n     * The colour of the graphic shall be assignable by the operator.\n     * Notes: undefined\n     * </pre>\n     */\n    static readonly AY_NETWORK_IDENTIFIER: string;\n    /**\n     * Returns an Arraylist of the all the modifiers that appear as labels and are not\n     * derived from the symbol code or are external to symbol data like offset indicator.\n     * Also includes modifiers that control the shape of a symbol like AM &amp; AN.\n     * @return Array<string>\n     */\n    static GetModifierList(): Array<string>;\n    /**\n     * Returns an ArrayList of the modifier constants that are determined by the symbol code\n     * and therefore don't need their values to be manually typed or selected.\n     * (Based on the comments in GetModifierList, GetUnitModifierList, and GetControlMeasureModifierList.)\n     * @return Array<string>\n     */\n    static GetSymbolCodeModifiersList(): Array<string>;\n    /**\n     * Returns an Arraylist of the modifier names for units\n     * @return Array<string>\n     */\n    static GetUnitModifierList(): Array<string>;\n    /**\n     * Returns an Arraylist of the modifiers for Control Measures\n     * @return Array<string>\n     */\n    static GetControlMeasureModifierList(): Array<string>;\n    /**\n     *\n     * @param modifier like Modifiers.C_QUANTITY\n     * @return modifier name based on modifier constants\n     */\n    static getModifierName(modifier: string): string;\n    /**\n     *\n     * @param modifier like Modifiers.C_QUANTITY\n     * @return modifier name based on modifier constants\n     */\n    static getModifierLetterCode(modifier: string): string;\n    /**\n     * Returns the description for a modifier constant\n     * @param modifier {@link Modifiers}\n     * @return\n     */\n    static getModifierDescription(modifier: string): string;\n    /**\n     * go from \"T\" or \"T1\" to integer constant values 9 and 10\n     * @param modLetter\n     * @return {@link Modifiers}\n     */\n    static getModifierKey(modLetter: string): string | null;\n}\n//# sourceMappingURL=Modifiers.d.ts.map","milsym/renderer/utilities/PointConversion.d.ts":"import { Point } from \"../../graphics/Point\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { IPointConversion } from \"./IPointConversion\";\n/**\n *\n *\n */\nexport declare class PointConversion implements IPointConversion {\n    protected _pixelWidth: number;\n    protected _PixelHeight: number;\n    protected _geoTop: number;\n    protected _geoLeft: number;\n    protected _geoBottom: number;\n    protected _geoRight: number;\n    protected _normalize: boolean;\n    protected _pixelMultiplierX: number;\n    protected _pixelMultiplierY: number;\n    set_normalize(value: boolean): void;\n    constructor(pixelWidth: number, pixelHeight: number, geoTop: number, geoLeft: number, geoBottom: number, geoRight: number);\n    UpdateExtents(pixelWidth: number, pixelHeight: number, geoTop: number, geoLeft: number, geoBottom: number, geoRight: number): void;\n    PixelsToGeo(pixel: Point): Point;\n    PixelsToGeo(pixel: Point2D): Point2D;\n    GeoToPixels(coord: Point): Point;\n    GeoToPixels(coord: Point2D): Point2D;\n    getPixelWidth(): number;\n    getPixelHeight(): number;\n    getUpperLat(): number;\n    getLowerLat(): number;\n    getLeftLon(): number;\n    getRightLon(): number;\n}\n//# sourceMappingURL=PointConversion.d.ts.map","milsym/renderer/utilities/PointConversionDummy.d.ts":"import { Point } from \"../../graphics/Point\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { IPointConversion } from \"./IPointConversion\";\n/**\n * Makes no change to the passed points.  Useful for when the points\n * are already in pixels.\n *\n */\nexport declare class PointConversionDummy implements IPointConversion {\n    constructor();\n    PixelsToGeo(pixel: Point): Point;\n    PixelsToGeo(pixel: Point2D): Point2D;\n    GeoToPixels(coord: Point): Point;\n    GeoToPixels(coord: Point2D): Point2D;\n}\n//# sourceMappingURL=PointConversionDummy.d.ts.map","milsym/renderer/utilities/PointConverter3D.d.ts":"import { Point } from \"../../graphics/Point\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { IPointConversion } from \"./IPointConversion\";\n/**\n *\n *\n */\nexport declare class PointConverter3D implements IPointConversion {\n    private _controlLat;\n    private _controlLong;\n    private _scale;\n    private _metersPerPixel;\n    constructor(controlLong: number, controlLat: number, scale: number);\n    PixelsToGeo(pixel: Point): Point2D;\n    PixelsToGeo(pixel: Point2D): Point2D;\n    GeoToPixels(coord: Point2D): Point;\n    GeoToPixels(coord: Point2D): Point2D;\n}\n//# sourceMappingURL=PointConverter3D.d.ts.map","milsym/renderer/utilities/RectUtilities.d.ts":"import { Rectangle } from \"../../graphics/Rectangle\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nexport declare class RectUtilities {\n    static makeRectangleFromRect(x1: number, y1: number, x2: number, y2: number): Rectangle;\n    static makeRectangle2DFromRect(x1: number, y1: number, x2: number, y2: number): Rectangle2D;\n    /**\n     * Copies a Rectangle\n     * @param rect {@link Rectangle2D}\n     * @return {@link Rectangle2D}\n     */\n    static copyRect(rect: Rectangle2D): Rectangle2D;\n    /**\n     * copies and rounds the points.  x,y round down &amp; width,height round up\n     * @param rect {@link Rectangle2D}\n     * @return {@link Rectangle2D}\n     */\n    static roundRect(rect: Rectangle2D): Rectangle2D;\n    static grow(rect: Rectangle2D, size: number): void;\n    static shift(rect: Rectangle2D, x: number, y: number): void;\n    static shiftBR(rect: Rectangle2D, x: number, y: number): void;\n    static toRectangle(b: Rectangle2D): Rectangle;\n    static toRectangle(x: number, y: number, w: number, h: number): Rectangle;\n    static toRectangle2D(b: Rectangle): Rectangle2D;\n    static toRectangle2D(x: number, y: number, w: number, h: number): Rectangle2D;\n}\n//# sourceMappingURL=RectUtilities.d.ts.map","milsym/renderer/utilities/RendererException.d.ts":"/**\n * @deprecated Use plain Error instead. Kept for backward compatibility.\n */\n//# sourceMappingURL=RendererException.d.ts.map","milsym/renderer/utilities/RendererSettings.d.ts":"import { Font } from \"../../graphics/Font\";\nimport { Color } from \"./Color\";\nimport { SettingsEventListener } from \"./SettingsEventListener\";\n/**\n *Static class that holds the setting for the JavaRenderer.\n * Allows different parts of the renderer to know what\n * values are being used.\n *\n */\nexport declare class RendererSettings {\n    private static _instance;\n    private static _TextBackgroundMethod;\n    /**\n     * There will be no background for text\n     */\n    static readonly TextBackgroundMethod_NONE: number;\n    /**\n     * There will be a colored box behind the text\n     */\n    static readonly TextBackgroundMethod_COLORFILL: number;\n    /**\n     * There will be an adjustable outline around the text (expensive)\n     * Outline width of 4 is recommended.\n     */\n    static readonly TextBackgroundMethod_OUTLINE: number;\n    /**\n     * Value from 0 to 255. The closer to 0 the lighter the text color has to be\n     * to have the outline be black. Default value is 160.\n     */\n    private static _TextBackgroundAutoColorThreshold;\n    private static _TextOutlineWidth;\n    private static _ColorLabelForeground;\n    private static _ColorLabelBackground;\n    private static _PixelSize;\n    /**\n     * Collapse labels for fire support areas when the symbol isn't large enough to show all\n     * the labels.\n     */\n    private static _AutoCollapseModifiers;\n    /**\n     * @deprecated\n     */\n    private static _SymbolOutlineWidth;\n    private static _OutlineSPControlMeasures;\n    private static _ActionPointDefaultFill;\n    /**\n     * If true (default), when HQ Staff is present, location will be indicated by the free\n     * end of the staff\n     */\n    private static _CenterOnHQStaff;\n    /**\n     * Text modifiers/amplifiers are placed where they belong even if there's empty space\n     * from other modifiers that weren't populated\n     */\n    static ModifierPlacement_STRICT: number;\n    /**\n     * Text modifiers/amplifiers will collapse vertically towards the center to eliminate\n     * empty space from modifiers that weren't populated.\n     */\n    static ModifierPlacement_FLEXIBLE: number;\n    /**\n     * Same as flexible but the modifier letter is put at the beginning of the value string\n     * to prevent confusion from modifiers not being in their strict location.\n     * if P (IFF/SIF) is set to \"2:1234\", it would be rendered as \"P:2:1234\"\n     */\n    private static _ModifierPlacementApproach;\n    static OperationalConditionModifierType_SLASH: number;\n    static OperationalConditionModifierType_BAR: number;\n    private static _OCMType;\n    static readonly SeaMineRenderMethod_MEDAL: number;\n    static readonly SeaMineRenderMethod_ALT: number;\n    static _SeaMineRenderMethod: number;\n    private static _UseLineInterpolation;\n    private static _ModifierFontName;\n    private static _ModifierFontType;\n    private static _ModifierFontWeight;\n    private static _ModifierFontSize;\n    private static _ModifierFontKerning;\n    private static _ModifierFontTracking;\n    private _scaleEchelon;\n    private _DrawAffiliationModifierAsLabel;\n    private static _MPLabelFontName;\n    private static _MPLabelFontType;\n    private static _MPLabelFontSize;\n    private static _KMLLabelScale;\n    private static _DPI;\n    private _TwoLabelOnly;\n    private _scaleMainIconWithoutSectorMods;\n    private _patternScale;\n    private _overscanScale;\n    private _autoAdjustScale;\n    private _friendlyUnitFillColor;\n    private _hostileUnitFillColor;\n    private _neutralUnitFillColor;\n    private _unknownUnitFillColor;\n    private _friendlyGraphicFillColor;\n    private _hostileGraphicFillColor;\n    private _neutralGraphicFillColor;\n    private _unknownGraphicFillColor;\n    private _friendlyUnitLineColor;\n    private _hostileUnitLineColor;\n    private _neutralUnitLineColor;\n    private _unknownUnitLineColor;\n    private _friendlyGraphicLineColor;\n    private _hostileGraphicLineColor;\n    private _neutralGraphicLineColor;\n    private _unknownGraphicLineColor;\n    private _Listeners;\n    private constructor();\n    static getInstance(): RendererSettings;\n    private Init;\n    addEventListener(sel: SettingsEventListener): void;\n    private raiseEvents;\n    /**\n     * None, outline (default), or filled background.\n     * If set to OUTLINE, TextOutlineWidth changed to default of 4.\n     * If set to OUTLINE_QUICK, TextOutlineWidth changed to default of 1.\n     * Use setTextOutlineWidth if you'd like a different value.\n     * @param textBackgroundMethod like RenderSettings.TextBackgroundMethod_NONE\n     */\n    setTextBackgroundMethod(textBackgroundMethod: number): void;\n    /**\n     * None, outline (default), or filled background.\n     * @return method like RenderSettings.TextBackgroundMethod_NONE\n     */\n    getTextBackgroundMethod(): number;\n    /**\n     * default size single point icons will render on the map\n     * @param size\n     */\n    setDefaultPixelSize(size: number): void;\n    /**\n     * default size single point icons will render on the map\n     * @return\n     */\n    getDefaultPixelSize(): number;\n    /**\n     * Set the operational condition modifier to be slashes or bars\n     * @param value like RendererSettings.OperationalConditionModifierType_SLASH\n     */\n    setOperationalConditionModifierType(value: number): void;\n    getOperationalConditionModifierType(): number;\n    setSeaMineRenderMethod(method: number): void;\n    getSeaMineRenderMethod(): number;\n    /**\n     * For lines symbols with \"decorations\" like FLOT or LOC, when points are\n     * too close together, we will start dropping points until we get enough\n     * space between 2 points to draw the decoration.  Without this, when points\n     * are too close together, you run the chance that the decorated line will\n     * look like a plain line because there was no room between points to\n     * draw the decoration.\n     * @param value boolean\n     */\n    setUseLineInterpolation(value: boolean): void;\n    /**\n     * Returns the current setting for Line Interpolation.\n     * @return boolean\n     */\n    getUseLineInterpolation(): boolean;\n    /**\n     * set the screen DPI so the renderer can take DPI into account when\n     * rendering for things like dashed lines and decorated lines.\n     * @param value\n     */\n    setDeviceDPI(value: number): void;\n    getDeviceDPI(): number;\n    /**\n     * Collapse Modifiers for fire support areas when the symbol isn't large enough to show all\n     * the labels.  Identifying label will always be visible.  Zooming in, to make the symbol larger,\n     * will make more modifiers visible.  Resizing the symbol can also make more modifiers visible.\n     * @param value boolean\n     */\n    setAutoCollapseModifiers(value: boolean): void;\n    getAutoCollapseModifiers(): boolean;\n    /**\n     * if true (default), when HQ Staff is present, location will be indicated by the free\n     * end of the staff\n     * @param value\n     */\n    setCenterOnHQStaff(value: boolean): void;\n    /**\n     * if true (default), when HQ Staff is present, location will be indicated by the free\n     * end of the staff\n     */\n    getCenterOnHQStaff(): boolean;\n    /**\n     * if RenderSettings.TextBackgroundMethod_OUTLINE is used,\n     * the outline will be this many pixels wide.\n     *\n     * @param width\n     */\n    /**\n     * if RenderSettings.TextBackgroundMethod_OUTLINE is used,\n     * the outline will be this many pixels wide.\n     * @return\n     */\n    getTextOutlineWidth(): number;\n    /**\n     * Refers to text color of modifier labels\n     * @return\n     *\n     */\n    /**\n     * Refers to text color of modifier labels\n     * Default Color is Black.  If NULL, uses line color of symbol\n     * @param value\n     *\n     */\n    /**\n     * Refers to background color of modifier labels\n     * @return\n     *\n     */\n    /**\n     * Refers to text color of modifier labels\n     * Default Color is White.\n     * Null value means the optimal background color (black or white)\n     * will be chose based on the color of the text.\n     * @param value\n     *\n     */\n    /**\n     * Value from 0 to 255. The closer to 0 the lighter the text color has to be\n     * to have the outline be black. Default value is 160.\n     * @param value\n     */\n    setTextBackgroundAutoColorThreshold(value: number): void;\n    /**\n     * Value from 0 to 255. The closer to 0 the lighter the text color has to be\n     * to have the outline be black. Default value is 160.\n     * @return\n     */\n    getTextBackgroundAutoColorThreshold(): number;\n    /**\n     * This applies to Single Point Tactical Graphics.\n     * Setting this will determine the default value for milStdSymbols when created.\n     * 0 for no outline,\n     * 1 for outline thickness of 1 pixel,\n     * 2 for outline thickness of 2 pixels,\n     * greater than 2 is not currently recommended.\n     * @deprecated\n     * @param width\n     */\n    setSinglePointSymbolOutlineWidth(width: number): void;\n    /**\n     * This applies to Single Point Tactical Graphics.\n     * @return\n     * @deprecated\n     */\n    getSinglePointSymbolOutlineWidth(): number;\n    setOutlineSPControlMeasures(value: boolean): void;\n    getOutlineSPControlMeasures(): boolean;\n    setActionPointDefaultFill(value: boolean): void;\n    getActionPointDefaultFill(): boolean;\n    /**\n     * false to use label font size\n     * true to scale it using symbolPixelBounds / 3.5\n     * @param value\n     */\n    setScaleEchelon(value: boolean): void;\n    /**\n     * Returns the value determining if we scale the echelon font size or\n     * just match the font size specified by the label font.\n     * @return true or false\n     */\n    getScaleEchelon(): boolean;\n    /**\n     * Determines how to draw the Affiliation modifier.\n     * True to draw as modifier label in the \"E/F\" location.\n     * False to draw at the top right corner of the symbol\n     */\n    setDrawAffiliationModifierAsLabel(value: boolean): void;\n    /**\n     * True to draw as modifier label in the \"E/F\" location.\n     * False to draw at the top right corner of the symbol\n     */\n    getDrawAffiliationModifierAsLabel(): boolean;\n    /**\n     * Sets the font to be used for modifier labels\n     * @param name Like \"arial\"\n     * @param weight Like \"normal\" or \"bold\"\n     * @param size Like 12\n     */\n    setLabelFont(name: string, weight: string, size: number): void;\n    /**\n     *\n     * @param name Like \"arial\"\n     * @param type Like Font.BOLD\n     * @param size Like 12\n     */\n    setLabelFont(name: string, type: number, size: number): void;\n    setMPLabelFont(name: string, weight: string, size: number): void;\n    setMPLabelFont(name: string, weight: string, size: number, kmlScale: number): void;\n    setMPLabelFont(name: string, type: number, size: number): void;\n    setMPLabelFont(name: string, type: number, size: number, kmlScale: number): void;\n    /**\n     * the font name to be used for modifier labels\n     * @return name of the label font\n     */\n    getLabelFontName(): string;\n    /**\n     * Like Font.BOLD\n     * @return type of the label font\n     */\n    getLabelFontType(): number;\n    /**\n     * get font point size\n     * @return size of the label font\n     */\n    getLabelFontSize(): number;\n    /**\n     * get font object used for labels\n     * @return Font object\n     */\n    getLabelFont(): Font;\n    /**\n     * get font object used for labels\n     * @return Font object\n     */\n    getMPLabelFont(): Font;\n    getKMLLabelScale(): number;\n    getSPModifierPlacement(): number;\n    /**\n     * Strict (0) for always placing their labels in the specified location\n     * even if there's empty space from other labels that weren't populated\n     * Flexible (1) to collapse label vertically to the center to eliminate\n     * empty space from labels that weren't populated.\n     * Does not apply to Control Measures or METOCS\n     * Set with values like:\n     * RendererSettings.ModifierPlacement_STRICT (0)\n     * RendererSettings.ModifierPlacement_FLEXIBLE (1)\n     * @param modifierPlacementApproach\n     */\n    setSPModifierPlacement(modifierPlacementApproach: number): void;\n    /**\n     * the font name to be used for modifier labels\n     * @return name of the label font\n     */\n    getMPLabelFontName(): string;\n    /**\n     * Like Font.BOLD\n     * @return type of the label font\n     */\n    getMPLabelFontType(): number;\n    /**\n     * get font point size\n     * @return size of the label font\n     */\n    getMPLabelFontSize(): number;\n    /**\n     ** Get a boolean indicating between the use of supply routes labels in all segments (false) or\n     * to only set 2 labels one at the north and the other one at the south of the graphic (true).\n     * @return {boolean}\n    +* @deprecated\n     */\n    getTwoLabelOnly(): boolean;\n    /**\n     * Set a boolean indicating between the use of supply routes labels in all segments (false) or\n     * to only set 2 labels one at the north and the other one at the south of the graphic (true).\n     * @param TwoLabelOnly\n     * @deprecated functionally disabled\n     */\n    setTwoLabelOnly(TwoLabelOnly: boolean): void;\n    /**\n     * When true, if the main icon is normally small to allow room for sector modifiers,\n     * make it larger when no sector modifiers are present for better visibility.\n     * @param scaleMainIcon\n     */\n    setScaleMainIcon(scaleMainIcon: boolean): void;\n    /**\n     * When true, if the main icon is normally small to allow room for sector modifiers,\n     * main icon is made larger when no sector modifiers are present for better visibility.\n     */\n    getScaleMainIcon(): boolean;\n    /**\n     * Multipoint features and patterns scale with line width ({@link MilStdAttributes#LineWidth}).\n     * {@code patternScale} is the ratio of how much to increase features and patterns by with line width.\n     * default value is 1.0. Can be overwritten on render with {@link MilStdAttributes#PatternScale}\n     * @param patternScale\n     */\n    setPatternScale(patternScale: number): void;\n    getPatternScale(): number;\n    /**\n     * Optionally expand multipoint rendering outside bounding box by a scale factor.\n     * Useful when panning map before rendering with updated bounding box.\n     * Only referenced when bounding box is a valid rectangle.\n     * For example, setting overscanScale to 3 would render all shapes within range 3 * the width and 3 * the height of the bounding box\n     * @param overscanScale default is 1 and minimum is 1\n     */\n    setOverscanScale(overscanScale: number): void;\n    getOverscanScale(): number;\n    /**\n     * Will attempt to adjust the scale if it doesn't seem to make sense with the passed in bbox.\n     * If you wish to have absolute control over the scale, set to false.\n     * @param autoAdjustScale default true\n     */\n    setAutoAdjustScale(autoAdjustScale: boolean): void;\n    getAutoAdjustScale(): boolean;\n    /**\n     * get the preferred fill affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getFriendlyUnitFillColor(): Color;\n    /**\n     * Set the preferred fill affiliation color for units\n     *\n     * @param friendlyUnitFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setFriendlyUnitFillColor(friendlyUnitFillColor: Color): void;\n    /**\n     * get the preferred fill affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getHostileUnitFillColor(): Color;\n    /**\n     * Set the preferred fill affiliation color for units\n     *\n     * @param hostileUnitFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setHostileUnitFillColor(hostileUnitFillColor: Color): void;\n    /**\n     * get the preferred fill affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getNeutralUnitFillColor(): Color;\n    /**\n     * Set the preferred line affiliation color for units\n     *\n     * @param neutralUnitFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setNeutralUnitFillColor(neutralUnitFillColor: Color): void;\n    /**\n     * get the preferred fill affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getUnknownUnitFillColor(): Color;\n    /**\n     * Set the preferred fill affiliation color for units\n     *\n     * @param unknownUnitFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setUnknownUnitFillColor(unknownUnitFillColor: Color): void;\n    /**\n     * get the preferred fill affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getHostileGraphicFillColor(): Color;\n    /**\n     * Set the preferred fill affiliation color for graphics\n     *\n     * @param hostileGraphicFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setHostileGraphicFillColor(hostileGraphicFillColor: Color): void;\n    /**\n     * get the preferred fill affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getFriendlyGraphicFillColor(): Color;\n    /**\n     * Set the preferred fill affiliation color for graphics\n     *\n     * @param friendlyGraphicFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setFriendlyGraphicFillColor(friendlyGraphicFillColor: Color): void;\n    /**\n     * get the preferred fill affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getNeutralGraphicFillColor(): Color;\n    /**\n     * Set the preferred fill affiliation color for graphics\n     *\n     * @param neutralGraphicFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setNeutralGraphicFillColor(neutralGraphicFillColor: Color): void;\n    /**\n     * get the preferred fill affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getUnknownGraphicFillColor(): Color;\n    /**\n     * Set the preferred fill affiliation color for graphics\n     *\n     * @param unknownGraphicFillColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setUnknownGraphicFillColor(unknownGraphicFillColor: Color): void;\n    /**\n     * get the preferred line affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getFriendlyUnitLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for units\n     *\n     * @param friendlyUnitLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setFriendlyUnitLineColor(friendlyUnitLineColor: Color): void;\n    /**\n     * get the preferred line   affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getHostileUnitLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for units\n     *\n     * @param hostileUnitLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setHostileUnitLineColor(hostileUnitLineColor: Color): void;\n    /**\n     * get the preferred line affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getNeutralUnitLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for units\n     *\n     * @param neutralUnitLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setNeutralUnitLineColor(neutralUnitLineColor: Color): void;\n    /**\n     * get the preferred line affiliation color for units.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getUnknownUnitLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for units\n     *\n     * @param unknownUnitLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setUnknownUnitLineColor(unknownUnitLineColor: Color): void;\n    /**\n     * get the preferred line affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getFriendlyGraphicLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for graphics\n     *\n     * @param friendlyGraphicLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setFriendlyGraphicLineColor(friendlyGraphicLineColor: Color): void;\n    /**\n     * get the preferred line affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getHostileGraphicLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for graphics\n     *\n     * @param hostileGraphicLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setHostileGraphicLineColor(hostileGraphicLineColor: Color): void;\n    /**\n     * get the preferred line affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getNeutralGraphicLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for graphics\n     *\n     * @param neutralGraphicLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setNeutralGraphicLineColor(neutralGraphicLineColor: Color): void;\n    /**\n     * get the preferred line affiliation color for graphics.\n     *\n     * @return Color like  Color(255, 255, 255)\n     *\n     * */\n    getUnknownGraphicLineColor(): Color;\n    /**\n     * Set the preferred line affiliation color for graphics\n     *\n     * @param unknownGraphicLineColor Color like  Color(255, 255, 255)\n     *\n     * */\n    setUnknownGraphicLineColor(unknownGraphicLineColor: Color): void;\n    /**\n     * Set the preferred line and fill affiliation color for tactical graphics.\n     *\n     * @param friendlyGraphicLineColor Color\n     * @param hostileGraphicLineColor Color\n     * @param neutralGraphicLineColor Color\n     * @param unknownGraphicLineColor Color\n     * @param friendlyGraphicFillColor Color\n     * @param hostileGraphicFillColor Color\n     * @param neutralGraphicFillColor Color\n     * @param unknownGraphicFillColor Color\n     */\n    setGraphicPreferredAffiliationColors(friendlyGraphicLineColor: Color, hostileGraphicLineColor: Color, neutralGraphicLineColor: Color, unknownGraphicLineColor: Color, friendlyGraphicFillColor: Color, hostileGraphicFillColor: Color, neutralGraphicFillColor: Color, unknownGraphicFillColor: Color): void;\n    /**\n     * Set the preferred line and fill affiliation color for units and tactical graphics.\n     *\n     * @param friendlyUnitLineColor Color like  Color(255, 255, 255). Set to null to ignore setting\n     * @param hostileUnitLineColor Color\n     * @param neutralUnitLineColor Color\n     * @param unknownUnitLineColor Color\n     * @param friendlyUnitFillColor Color\n     * @param hostileUnitFillColor Color\n     * @param neutralUnitFillColor Color\n     * @param unknownUnitFillColor Color\n     */\n    setUnitPreferredAffiliationColors(friendlyUnitLineColor: Color, hostileUnitLineColor: Color, neutralUnitLineColor: Color, unknownUnitLineColor: Color, friendlyUnitFillColor: Color, hostileUnitFillColor: Color, neutralUnitFillColor: Color, unknownUnitFillColor: Color): void;\n}\nexport declare const rendererSettings: RendererSettings;\n//# sourceMappingURL=RendererSettings.d.ts.map","milsym/renderer/utilities/RendererUtilities.d.ts":"import { Color } from \"./Color\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { SVGInfo } from \"./SVGInfo\";\nexport declare class RendererUtilities {\n    private static readonly OUTLINE_SCALING_FACTOR;\n    static imgToBase64String(img: OffscreenCanvas | any): Promise<string>;\n    private static pastIdealOutlineColors;\n    /**\n     *\n     * @param color {String} color like \"#FFFFFF\"\n     * @return {String}\n     */\n    static getIdealOutlineColor(color: Color): Color;\n    /**\n     * Create a copy of the {@Color} object with the passed alpha value.\n     * @param color {@Color} object used for RGB values\n     * @param alpha {@float} value between 0 and 1\n     * @return\n     */\n    static setColorAlpha(color: Color, alpha: number): Color;\n    /**\n     *\n     * @param color\n     * @return 8 character hex code, will have to prepend '#' or '0x' depending on your usage\n     */\n    private static ColorToHex;\n    /**\n     *\n     * @param color\n     * @param withAlpha\n     * @return\n     */\n    static colorToHexString(color: Color, withAlpha: boolean): string;\n    /**\n     * Clients should use getTextBounds\n     * @param {String} fontName like \"Arial\" or \"Arial, sans-serif\" so a backup is\n     * available in case 'Arial' is not present.\n     * @param {Number} fontSize like 12\n     * @param {String} fontStyle like \"bold\"\n     * @param {String} text include if you want a width value.\n     * @param {OffscreenCanvasRenderingContext2D}\n     * @returns {Object} {width:Number,height:Number,descent:Number,fullHeight:Number}\n     */\n    static measureText(fontName: string, fontSize: number, fontStyle: string, text: string, context: OffscreenCanvasRenderingContext2D | any | null): Rectangle2D;\n    static measureText(font: string, text: string, context: OffscreenCanvasRenderingContext2D | any | null): Rectangle2D;\n    static measureText(text: string, context: OffscreenCanvasRenderingContext2D | any): Rectangle2D;\n    /**\n     *\n     * @param hexValue - String representing hex value (formatted \"0xRRGGBB\"\n     * i.e. \"0xFFFFFF\") OR formatted \"0xAARRGGBB\" i.e. \"0x00FFFFFF\" for a color\n     * with an alpha value I will also put up with \"RRGGBB\" and \"AARRGGBB\"\n     * without the starting \"0x\" or \"#\"\n     * @return\n     */\n    static getColorFromHexString(hexValue: string): Color | null;\n    static getRecommendedTextOutlineWidth(): number;\n    /**\n     * For Renderer Use Only\n     * Assumes a fresh SVG String from the SVGLookup with its default values\n     * @param symbolID\n     * @param svg\n     * @param strokeColor hex value like \"#FF0000\";\n     * @param fillColor hex value like \"#FF0000\";\n     * @return SVG String\n     */\n    static setSVGFrameColors(symbolID: string, svg: string, strokeColor: Color, fillColor: Color): string;\n    static setSVGSPCMColors(symbolID: string, svg: string, strokeColor: Color, fillColor: Color): string;\n    /**\n     * For Renderer Use Only\n     * Changes colors for single point control measures\n     * @param symbolID\n     * @param svg\n     * @param strokeColor hex value like \"#FF0000\";\n     * @param fillColor hex value like \"#FF0000\";\n     * @param isOutline true if this represents a thicker outline to render first beneath the normal symbol (the function must be called twice)\n     * @return SVG String\n     *\n     */\n    static setSVGSPCMColors(symbolID: string, svg: string, strokeColor: Color, fillColor: Color, isOutline: boolean): string;\n    /**\n     * Sets SVG stroke-dasharray when action points are in planned status\n     * @param symbolID\n     * @param siIcon\n     * @returns\n     */\n    static setAffiliationDashArray(symbolID: string, siIcon: SVGInfo): SVGInfo;\n    static findWidestStrokeWidth(svg: string): number;\n    static findInstIndIndex(svg: string): number;\n    /**\n     * Searches an SVG string and increments all stroke-width values by 2.\n     * @param svgString The input SVG content as a string.\n     * @param increaseBy the number to add to the current stroke value\n     * @returns A new SVG string with updated stroke-width values.\n     */\n    static increaseStrokeWidth(svgString: string, increaseBy: number): string;\n    static getDistanceBetweenPoints(pt1: Point2D, pt2: Point2D): number;\n    /**\n     * A starting point for calculating map scale.\n     * The User may prefer a different calculation depending on how their maps works.\n     * @param mapPixelWidth Width of your map in pixels\n     * @param eastLon East Longitude of your map\n     * @param westLon West Longitude of your map\n     * @param dpi Dots Per Inch of your device.  If not included, will use default renderer value.\n     * @return Map scale value to use in the RenderSymbol function {@link armyc2.c5isr.web.render.WebRenderer#RenderSymbol(String, String, String, String, String, String, double, String, Map, Map, int)}\n     */\n    static calculateMapScale(mapPixelWidth: number, eastLon: number, westLon: number, dpi?: number): number;\n    static scaleIcon(symbolID: string, icon: SVGInfo): SVGInfo;\n    static getData(path: string): Promise<any>;\n}\n//# sourceMappingURL=RendererUtilities.d.ts.map","milsym/renderer/utilities/SVGInfo.d.ts":"import { Rectangle2D } from \"../../graphics/Rectangle2D\";\nexport declare class SVGInfo {\n    private _ID;\n    private _Bbox;\n    private _SVG;\n    constructor(id: string, measurements: Rectangle2D, svg: string);\n    getID(): string;\n    getBbox(): Rectangle2D;\n    getSVG(): string;\n    toString(): string;\n}\n//# sourceMappingURL=SVGInfo.d.ts.map","milsym/renderer/utilities/SVGLookup.d.ts":"import { SVGInfo } from \"./SVGInfo\";\nexport declare class SVGLookup {\n    private static _instance;\n    private static _initCalled;\n    private static _isReady;\n    private static _SVGLookupD;\n    private static _SVGLookupE;\n    private static svgd;\n    private static svge;\n    private static svgdJSON;\n    private static svgeJSON;\n    /** Tracks which symbol sets have been lazily loaded into the maps */\n    private static _loadedSymbolSetsD;\n    private static _loadedSymbolSetsE;\n    /** When true, init() skips eager loading of monolithic JSON */\n    private static _lazyMode;\n    /** Promise tracking for in-flight lazy loads */\n    private static _loadingPromises;\n    /**\n     * @deprecated\n     */\n    static setData(urls: string[]): Promise<void>;\n    private constructor();\n    static getInstance(): SVGLookup;\n    /**\n     * Initialize SVGLookup in lazy mode — no monolithic JSON is loaded upfront.\n     * Only common SVG data (frames, amplifiers) is loaded immediately.\n     * Symbol-set-specific data is loaded on demand via ensureSymbolSet().\n     *\n     * @returns Promise that resolves when common data is ready\n     */\n    static initLazy(): Promise<SVGLookup>;\n    /**\n     * Ensure a symbol set's SVG data is loaded and available for synchronous lookup.\n     * In eager mode, this is a no-op. In lazy mode, it loads the chunk on demand.\n     *\n     * @param symbolSet Two-digit symbol set code (e.g., \"10\", \"25\")\n     * @param version SymbolID version constant\n     */\n    static ensureSymbolSet(symbolSet: string, version: number): Promise<void>;\n    /**\n     * Ensure all symbol sets needed for a full SIDC are loaded.\n     * Extracts the symbol set from the SIDC and loads it.\n     */\n    static ensureForSIDC(symbolID: string): Promise<void>;\n    private init;\n    isReady(): boolean;\n    /**\n     * Returns whether lazy mode is active.\n     */\n    static isLazyMode(): boolean;\n    /**\n     * Check if a specific symbol set is loaded (useful in lazy mode).\n     */\n    static isSymbolSetLoaded(symbolSet: string, version: number): boolean;\n    private populateLookup;\n    /**\n     *\n     * @param id\n     * @return\n     */\n    getSVGLInfo(id: string, version: number): SVGInfo | null;\n    /**\n     * Async version of getSVGLInfo — ensures the required symbol set is loaded\n     * before performing the lookup. Safe to use in both eager and lazy modes.\n     *\n     * @param id SVG element ID\n     * @param version SymbolID version constant\n     * @param symbolSet Two-digit symbol set code (if known, avoids re-parsing)\n     */\n    getSVGLInfoAsync(id: string, version: number, symbolSet?: string): Promise<SVGInfo | null>;\n    getSVGOctagon(): SVGInfo | null;\n    static getFrameID(symbolID: string): string;\n    static getMainIconID(symbolID: string): string;\n    private static getPostFixForIcon;\n    static getMod1ID(symbolID: string): string;\n    static getMod2ID(symbolID: string): string;\n    static getEchelonAmplifier(symbolID: string): string;\n    static getHQTFFD(symbolID: string): string;\n    static getOCA(symbolID: string, useSlash: boolean): string | null;\n    static getAllKeys(): Array<string>;\n    addCustomSymbol(svgInfo: SVGInfo, version: number): boolean;\n    /**\n     * Get memory usage stats from the DataLoader (lazy mode only).\n     */\n    static getDataLoaderStats(): import(\"./IDataLoader\").DataLoaderStats;\n    /**\n     * Reset the singleton for testing purposes.\n     */\n    static resetForTesting(): void;\n}\nexport declare const svgLookup: SVGLookup;\n//# sourceMappingURL=SVGLookup.d.ts.map","milsym/renderer/utilities/SVGSymbolInfo.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { SymbolDimensionInfo } from \"./SymbolDimensionInfo\";\nexport declare class SVGSymbolInfo implements SymbolDimensionInfo {\n    private _svg;\n    private _svgDataURI;\n    private _anchorX;\n    private _anchorY;\n    private _symbolBounds;\n    private _bounds;\n    constructor(svg: string, anchorPoint: Point2D, symbolBounds: Rectangle2D, svgBounds: Rectangle2D);\n    private bytesToBase64;\n    private escapeChars;\n    getSVGDataURI(): string;\n    getSVG(): string;\n    /**\n     * The x value the image should be centered on or the \"anchor point\".\n     * @return\n     */\n    getSymbolCenterX(): number;\n    /**\n     * The y value the image should be centered on or the \"anchor point\".\n     * @return\n     */\n    getSymbolCenterY(): number;\n    /**\n     * The point the image should be centered on or the \"anchor point\".\n     * @return\n     */\n    getSymbolCenterPoint(): Point2D;\n    /**\n     * minimum bounding rectangle for the core symbol. Does\n     * not include modifiers, display or otherwise.\n     * @return\n     */\n    getSymbolBounds(): Rectangle2D;\n    /**\n     * Dimension of the entire image.\n     * @return\n     */\n    getImageBounds(): Rectangle2D;\n}\n//# sourceMappingURL=SVGSymbolInfo.d.ts.map","milsym/renderer/utilities/SVGTextInfo.d.ts":"import { Font } from \"../../graphics/Font\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\n/**\n *\n *\n */\nexport declare class SVGTextInfo {\n    protected _text: string;\n    protected _font: string;\n    protected _fontName: string;\n    protected _fontSize: number;\n    protected _fontStyle: string;\n    protected _location: Point2D;\n    protected _bounds: Rectangle2D;\n    protected _descent: number;\n    protected _aboveBaseHeight: number;\n    private justification;\n    private angle;\n    private alignmentBaseline;\n    private isNode;\n    private isBrowser;\n    private OSCDefined;\n    constructor(text: string, position: Point2D, font: Font, justification: string, angle: number);\n    constructor(text: string, x: number, y: number, font: Font, context: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D | null);\n    constructor(text: string, x: number, y: number, font: string, context: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D | null);\n    constructor(text: string, x: number, y: number, fontName: string, fontSize: number, fontStyle: string, context: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D | null);\n    setLocation(x: number, y: number): void;\n    getLocation(): Point2D;\n    shift(x: number, y: number): void;\n    getText(): string;\n    /**\n     * includes the descent\n     * @returns\n     */\n    getTextBounds(): Rectangle2D;\n    getTextOutlineBounds(): Rectangle2D;\n    getDescent(): number;\n    getFontName(): string;\n    getFontSize(): number;\n    getFontStyle(): string;\n    /**\n     *\n     * @param textColor hex color string\n     * @param outlineColor hex color string or null if no outline\n     * @param outlineWidth number value for outline width\n     * @param justification \"start\", \"middle\", or \"end\"\n     * @param svgFormat 0 - untouched, 1 - replace speacial characters with escape codes, 2 use encodeURIComponent (expensive)\n     * @returns\n     */\n    toSVGElement(textColor: string, outlineColor: string | null, outlineWidth?: number, justification?: string, svgFormat?: number): string;\n    /**\n     * @param {Rectangle2D} rectangle\n     * @returns {Rectangle2D}\n     */\n    static getRotatedRectangleBounds(rectangle: Rectangle2D, pivotPt: Point2D, angle: number, justification?: string): Rectangle2D;\n    /**\n     * @param {Point2D} pt Point to rotate\n     * @param {Point2D} pivotPt Point to rotate around\n     * @param {number} angle angle in degrees\n     */\n    static rotatePoint(pt: Point2D, pivotPt: Point2D, angle: number): void;\n}\n//# sourceMappingURL=SVGTextInfo.d.ts.map","milsym/renderer/utilities/SectorModUtils.d.ts":"/**\n *\n */\nexport declare class SectorModUtils {\n    private static _instance;\n    private static _initCalled;\n    private static _isReady;\n    private static _sectorMods;\n    private static _sectorModLists;\n    private static smd;\n    private static sme;\n    /**\n     *\n     * @param url\n     * @deprecated\n     */\n    static setData(url: string): Promise<void>;\n    private constructor();\n    static getInstance(): SectorModUtils;\n    isReady(): boolean;\n    private init;\n    private loadData;\n    /**\n     *\n     * @param version like SymbolID.Version_2525Dch1 or SymbolID.Version_2525Ech1  Only tracks sector mods for these 2 versions.\n     * @param symbolSet  like SymbolID.SymbolSet_Air; use 0 for Common Modifiers as they are not tied to a symbol set.\n     * @param location 1 for top, 2 for bottom\n     * @return and ArrayList of String[] like [\"00\",\"Unspecified\"],[\"01\",\"Attack/Strike\"]\n     */\n    getSectorModList(version: number, symbolSet: number, location: number): Array<string[]>;\n    /**\n     *\n     * @param version like SymbolID.Version_2525Dch1 or SymbolID.Version_2525Ech1  Only tracks sector mods for these 2 versions.\n     * @param symbolSet  like SymbolID.SymbolSet_Air; use 0 for Common Modifiers as they are not tied to a symbol set.\n     * @param location 1 for top, 2 for bottom\n     * @param code like \"01\" or \"100\"\n     */\n    getName(version: number, symbolSet: number, location: number, code: string): string;\n}\n//# sourceMappingURL=SectorModUtils.d.ts.map","milsym/renderer/utilities/SettingsChangedEvent.d.ts":"export declare class SettingsChangedEvent {\n    static readonly EventType_CacheSizeChanged: string;\n    static readonly EventType_CacheToggled: string;\n    static readonly EventType_FontChanged: string;\n    private _EventType;\n    constructor(eventType: string);\n    getEventType(): string;\n}\n//# sourceMappingURL=SettingsChangedEvent.d.ts.map","milsym/renderer/utilities/SettingsChangedEventListener.d.ts":"import { SettingsChangedEvent } from \"./SettingsChangedEvent\";\nexport interface SettingsChangedEventListener {\n    onSettingsChanged(sce: SettingsChangedEvent): void;\n}\n//# sourceMappingURL=SettingsChangedEventListener.d.ts.map","milsym/renderer/utilities/SettingsEventListener.d.ts":"export interface SettingsEventListener {\n    SettingsEventChanged(type: string): void;\n}\n//# sourceMappingURL=SettingsEventListener.d.ts.map","milsym/renderer/utilities/Shape2SVG.d.ts":"import { Font } from \"../../graphics/Font\";\nimport { FontRenderContext } from \"../../graphics/FontRenderContext\";\nimport { TextInfo } from \"./TextInfo\";\nexport declare class Shape2SVG {\n    /**\n     *\n     * @param shape like {@link Shape}\n     * @param stroke like \"#000000\n     * @param fill like \"#0000FF\" or \"none\"\n     * @param strokeWidth \"#\"\n     * @param strokeOpacity \"1.0\"\n     * @param fillOpacity \"1.0\"\n     * @param dashArray \"4 1 2 3\"\n     * @return\n     */\n    static Convert(textInfo: TextInfo, stroke: string, fill: string, strokeWidth: string, strokeOpacity: string, fillOpacity: string, dashArray: string): string;\n    static Convert(text: string, x: number, y: number, font: Font, frc: FontRenderContext, stroke: string, fill: string, strokeWidth: string, strokeOpacity: string, fillOpacity: string, dashArray: string): string;\n    /**\n     * Assumes common font properties will be defined in the group.\n     * @param textInfo\n     * @param stroke\n     * @param fill\n     * @param strokeWidth\n     * @param strokeOpacity\n     * @param fillOpacity\n     * @param dashArray\n     * @return\n     */\n    static ConvertForGroup(textInfo: TextInfo, stroke: string, fill: string, strokeWidth: string, strokeOpacity: string, fillOpacity: string, dashArray: string): string | null;\n    static makeBase64Safe(svg: string): string | null;\n    /**\n     *\n     * @param path2D like {@link Path2D}\n     * @param stroke like \"#000000\n     * @param fill like \"#0000FF\" or \"none\"\n     * @param strokeWidth \"#\"\n     * @param strokeOpacity \"1.0\"\n     * @param fillOpacity \"1.0\"\n     * @param dashArray \"4 1 2 3\"\n     * @return\n     */\n    private static convertPath;\n    private static convertRect;\n}\n//# sourceMappingURL=Shape2SVG.d.ts.map","milsym/renderer/utilities/ShapeInfo.d.ts":"import { BasicStroke } from \"../../graphics/BasicStroke\";\nimport { Point } from \"../../graphics/Point\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle } from \"../../graphics/Rectangle\";\nimport { Shape } from \"../../graphics/Shape\";\nimport { TextLayout } from \"../../graphics/TextLayout\";\nimport { TexturePaint } from \"../../graphics/TexturePaint\";\nimport { Color } from \"./Color\";\nimport { SVGSymbolInfo } from \"./SVGSymbolInfo\";\n/**\n * Holds information on how to draw the pieces of a multipoint symbol.\n * Can be retrieved from {@link MilStdSymbol#getSymbolShapes()} and\n * {@link MilStdSymbol#getModifierShapes()} after {@link armyc2.c5isr.web.render.WebRenderer#RenderMultiPointAsMilStdSymbol(String, String, String, String, String, String, double, String, Map, Map)} is called.\n */\nexport declare class ShapeInfo {\n    static SHAPE_TYPE_POLYLINE: number;\n    static SHAPE_TYPE_FILL: number;\n    static SHAPE_TYPE_MODIFIER: number;\n    static SHAPE_TYPE_MODIFIER_FILL: number;\n    static SHAPE_TYPE_UNIT_FRAME: number;\n    static SHAPE_TYPE_UNIT_FILL: number;\n    static SHAPE_TYPE_UNIT_SYMBOL1: number;\n    static SHAPE_TYPE_UNIT_SYMBOL2: number;\n    static SHAPE_TYPE_UNIT_DISPLAY_MODIFIER: number;\n    static SHAPE_TYPE_UNIT_ECHELON: number;\n    static SHAPE_TYPE_UNIT_AFFILIATION_MODIFIER: number;\n    static SHAPE_TYPE_UNIT_HQ_STAFF: number;\n    static SHAPE_TYPE_TG_SP_FILL: number;\n    static SHAPE_TYPE_TG_SP_FRAME: number;\n    static SHAPE_TYPE_TG_Q_MODIFIER: number;\n    static SHAPE_TYPE_TG_SP_OUTLINE: number;\n    static SHAPE_TYPE_SINGLE_POINT_OUTLINE: number;\n    static SHAPE_TYPE_UNIT_OUTLINE: number;\n    static SHAPE_TYPE_UNIT_OPERATIONAL_CONDITION: number;\n    static justify_left: number;\n    static justify_center: number;\n    static justify_right: number;\n    protected _Shape: Shape;\n    private stroke;\n    private gp;\n    private fillStyle;\n    /**\n     * @deprecated\n     */\n    private texturePaint;\n    private _ShapeType;\n    private lineColor;\n    private fillColor;\n    private textBackgoundColor;\n    private _TextLayout;\n    private _Position;\n    private _ModifierString;\n    private _ModifierPosition;\n    private _ModifierImageInfo;\n    private _ModifierAngle;\n    private _Tag;\n    /**\n     * @deprecated\n     */\n    private _shader;\n    private _patternFillInfo;\n    private _justify;\n    private _Polylines;\n    constructor();\n    constructor(shape: Shape);\n    constructor(textLayout: TextLayout, position: Point2D);\n    /**\n     *\n     * @param shape\n     * @param shapeType\n     * ShapeInfo.SHAPE_TYPE_\n     */\n    constructor(shape: Shape, shapeType: number);\n    getShape(): Shape;\n    setShape(value: Shape): void;\n    getTextLayout(): TextLayout;\n    setTextLayout(value: TextLayout): void;\n    setModifierString(value: string): void;\n    getModifierString(): string;\n    setModifierPosition(value: Point2D): void;\n    getModifierPosition(): Point2D;\n    setModifierAngle(value: number): void;\n    getModifierAngle(): number;\n    /**\n     *\n     * @deprecated use {@link setModifierPosition()}\n     */\n    setModifierStringPosition(value: Point2D): void;\n    /**\n     *\n     * @deprecated use {@link getModifierPosition()}\n     */\n    getModifierStringPosition(): Point2D;\n    /**\n     *\n     * @deprecated use {@link setModifierAngle()}\n     */\n    setModifierStringAngle(value: number): void;\n    /**\n     *\n     * @deprecated use {@link getModifierAngle()}\n     */\n    getModifierStringAngle(): number;\n    setModifierImage(value: SVGSymbolInfo): void;\n    getModifierImage(): string;\n    getModifierImageInfo(): SVGSymbolInfo;\n    /**\n     * Object that can be used to store anything.\n     * Will not be looked at when rendering.\n     * Null by default\n     * @param value\n     */\n    setTag(value: any): void;\n    /**\n     * Object that can be used to store anything.\n     * Will not be looked at when rendering.\n     * Null by default\n     * @return\n     */\n    getTag(): any;\n    /**\n     * Gets bounds for the shapes.  Incorporates AffineTransform if not null\n     * in the ShapeInfo object.\n     * @return\n     */\n    getBounds(): Rectangle | null;\n    /**\n     * needed to draw Glyphs and TextLayouts\n     * @param position\n     */\n    setGlyphPosition(position: Point | Point2D): void;\n    /**\n     * needed to draw Glyphs and TextLayouts\n     * @return\n     */\n    getGlyphPosition(): Point2D;\n    setLineColor(value: Color | null): void;\n    getLineColor(): Color;\n    setFillColor(value: Color | null): void;\n    getFillColor(): Color | null;\n    setTextBackgroundColor(value: Color): void;\n    getTextBackgroundColor(): Color;\n    getStroke(): BasicStroke;\n    /**\n     * @deprecated\n     */\n    getTexturePaint(): TexturePaint | null;\n    /**\n     * @deprecated\n     */\n    setTexturePaint(value: TexturePaint | null): void;\n    getFillStyle(): number;\n    setFillStyle(value: number): void;\n    setStroke(s: BasicStroke): void;\n    /**\n     * For Internal Renderer use\n     * @param value\n     * ShapeInfo.SHAPE_TYPE_\n     *\n     */\n    setShapeType(value: number): void;\n    /**\n     * For Internal Renderer use\n     * @return ShapeInfo.SHAPE_TYPE_\n     *\n     */\n    getShapeType(): number;\n    getPolylines(): Array<Array<Point2D>>;\n    setPolylines(value: Array<Array<Point2D>>): void;\n    /**\n     * @deprecated\n     */\n    setShader(value: ImageBitmap): void;\n    /**\n     * @deprecated\n     */\n    getShader(): ImageBitmap;\n    setPatternFillImage(img: SVGSymbolInfo): void;\n    getPatternFillImage(): string;\n    getPatternFillImageInfo(): SVGSymbolInfo;\n    getTextJustify(): number;\n    setTextJustify(value: number): void;\n}\n//# sourceMappingURL=ShapeInfo.d.ts.map","milsym/renderer/utilities/ShapeUtilities.d.ts":"import { Point } from \"../../graphics/Point\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\n/**\n *\n *\n */\nexport declare class ShapeUtilities {\n    static grow(rect: Rectangle2D, size: number): void;\n    static offset(rect: Rectangle2D, offsetX: number, offsetY: number): void;\n    static offset(point: Point2D, offsetX: number, offsetY: number): void;\n    static offset(point: Point, offsetX: number, offsetY: number): void;\n    static offset(path: Path2D, offsetX: number, offsetY: number): void;\n}\n//# sourceMappingURL=ShapeUtilities.d.ts.map","milsym/renderer/utilities/SymbolDimensionInfo.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nexport interface SymbolDimensionInfo {\n    /**\n     * The x value the image should be centered on or the \"anchor point\".\n     * @return {@link Integer}\n     */\n    getSymbolCenterX(): number;\n    /**\n     * The y value the image should be centered on or the \"anchor point\".\n     * @return {@link Integer}\n     */\n    getSymbolCenterY(): number;\n    /**\n     * The point the image should be centered on or the \"anchor point\".\n     * @return {@link Point2D}\n     */\n    getSymbolCenterPoint(): Point2D;\n    /**\n     * minimum bounding rectangle for the core symbol. Does\n     * not include modifiers, display or otherwise.\n     * @return {@link Rectangle2D}\n     */\n    getSymbolBounds(): Rectangle2D;\n    /**\n     * Dimension of the entire image.\n     * @return {@link Rectangle2D}\n     */\n    getImageBounds(): Rectangle2D;\n}\n//# sourceMappingURL=SymbolDimensionInfo.d.ts.map","milsym/renderer/utilities/SymbolID.d.ts":"/**\n * Utility class for helping to parse out parts of the symbol ID.\n *\n */\nexport declare class SymbolID {\n    static readonly Version_2525D: number;\n    static readonly Version_2525Dch1: number;\n    /**\n     * @deprecated withdrawn from standard\n     */\n    static readonly Version_APP6Dch2: number;\n    static readonly Version_2525E: number;\n    static readonly Version_APP6Ech1: number;\n    static readonly Version_2525Ech1: number;\n    static readonly Version_APP6Ech2: number;\n    static readonly StandardIdentity_Context_Reality: number;\n    static readonly StandardIdentity_Context_Exercise: number;\n    static readonly StandardIdentity_Context_Simulation: number;\n    static readonly StandardIdentity_Context_Restricted_Target_Reality: number;\n    static readonly StandardIdentity_Context_No_Strike_Entity_Reality: number;\n    static readonly StandardIdentity_Affiliation_Pending: number;\n    static readonly StandardIdentity_Affiliation_Unknown: number;\n    static readonly StandardIdentity_Affiliation_AssumedFriend: number;\n    static readonly StandardIdentity_Affiliation_Friend: number;\n    static readonly StandardIdentity_Affiliation_Neutral: number;\n    static readonly StandardIdentity_Affiliation_Suspect_Joker: number;\n    static readonly StandardIdentity_Affiliation_Hostile_Faker: number;\n    static readonly SymbolSet_Unknown: number;\n    static readonly SymbolSet_Air: number;\n    static readonly SymbolSet_AirMissile: number;\n    static readonly SymbolSet_Space: number;\n    static readonly SymbolSet_SpaceMissile: number;\n    static readonly SymbolSet_LandUnit: number;\n    static readonly SymbolSet_LandCivilianUnit_Organization: number;\n    static readonly SymbolSet_LandEquipment: number;\n    static readonly SymbolSet_LandInstallation: number;\n    static readonly SymbolSet_ControlMeasure: number;\n    static readonly SymbolSet_DismountedIndividuals: number;\n    static readonly SymbolSet_SeaSurface: number;\n    static readonly SymbolSet_SeaSubsurface: number;\n    static readonly SymbolSet_MineWarfare: number;\n    static readonly SymbolSet_Activities: number;\n    static readonly SymbolSet_Atmospheric: number;\n    static readonly SymbolSet_Oceanographic: number;\n    static readonly SymbolSet_MeteorologicalSpace: number;\n    /**\n     * in 2525E+, there is only SymbolSet_SignalsIntelligence and the frame shape position is required to be populated.\n     * Valid frames are space, air, land, land equipment, sea surface, sea subsurface and cyberspace.\n     */\n    static readonly SymbolSet_SignalsIntelligence: number;\n    /**\n     * 2525D/Dch1 only\n     */\n    static readonly SymbolSet_SignalsIntelligence_Space: number;\n    /**\n     * 2525D/Dch1 only\n     */\n    static readonly SymbolSet_SignalsIntelligence_Air: number;\n    /**\n     * 2525D/Dch1 only\n     */\n    static readonly SymbolSet_SignalsIntelligence_Land: number;\n    /**\n     * 2525D/Dch1 only\n     */\n    static readonly SymbolSet_SignalsIntelligence_SeaSurface: number;\n    /**\n     * 2525D/Dch1 only\n     */\n    static readonly SymbolSet_SignalsIntelligence_SeaSubsurface: number;\n    static readonly SymbolSet_CyberSpace: number;\n    static readonly SymbolSet_InvalidSymbol: number;\n    static readonly SymbolSet_VersionExtensionFlag: number;\n    static readonly Status_Present: number;\n    static readonly Status_Planned_Anticipated_Suspect: number;\n    static readonly Status_Present_FullyCapable: number;\n    static readonly Status_Present_Damaged: number;\n    static readonly Status_Present_Destroyed: number;\n    static readonly Status_Present_FullToCapacity: number;\n    static readonly Status_Present_VersionExtensionFlag: number;\n    static readonly HQTFD_Unknown: number;\n    static readonly HQTFD_FeintDummy: number;\n    static readonly HQTFD_Headquarters: number;\n    static readonly HQTFD_FeintDummy_Headquarters: number;\n    static readonly HQTFD_TaskForce: number;\n    static readonly HQTFD_FeintDummy_TaskForce: number;\n    static readonly HQTFD_TaskForce_Headquarters: number;\n    static readonly HQTFD_FeintDummy_TaskForce_Headquarters: number;\n    static readonly HQTFD_VersionExtensionFlag: number;\n    static readonly Echelon_Unknown: number;\n    static readonly Echelon_Team_Crew: number;\n    static readonly Echelon_Squad: number;\n    static readonly Echelon_Section: number;\n    static readonly Echelon_Platoon_Detachment: number;\n    static readonly Echelon_Company_Battery_Troop: number;\n    static readonly Echelon_Battalion_Squadron: number;\n    static readonly Echelon_Regiment_Group: number;\n    static readonly Echelon_Brigade: number;\n    static readonly Echelon_VersionExtensionFlag: number;\n    static readonly Echelon_Division: number;\n    static readonly Echelon_Corps_MEF: number;\n    static readonly Echelon_Army: number;\n    static readonly Echelon_ArmyGroup_Front: number;\n    static readonly Echelon_Region_Theater: number;\n    static readonly Echelon_Region_Command: number;\n    static readonly Echelon_VersionExtensionFlag2: number;\n    static readonly Mobility_Unknown: number;\n    static readonly Mobility_WheeledLimitedCrossCountry: number;\n    static readonly Mobility_WheeledCrossCountry: number;\n    static readonly Mobility_Tracked: number;\n    static readonly Mobility_Wheeled_Tracked: number;\n    static readonly Mobility_Towed: number;\n    static readonly Mobility_Rail: number;\n    static readonly Mobility_PackAnimals: number;\n    static readonly Mobility_OverSnow: number;\n    static readonly Mobility_Sled: number;\n    static readonly Mobility_Barge: number;\n    static readonly Mobility_Amphibious: number;\n    static readonly Mobility_ShortTowedArray: number;\n    static readonly Mobility_LongTowedArray: number;\n    static readonly Leadership_Individual: number;\n    static readonly FrameShape_Unknown: string;\n    static readonly FrameShape_Space: string;\n    static readonly FrameShape_Air: string;\n    static readonly FrameShape_LandUnit: string;\n    static readonly FrameShape_LandEquipment: string;\n    static readonly FrameShape_LandInstallation: string;\n    static readonly FrameShape_DismountedIndividuals: string;\n    static readonly FrameShape_SeaSubsurface: string;\n    static readonly FrameShape_Activity_Event: string;\n    static readonly FrameShape_Cyberspace: string;\n    static readonly FrameShape_SeaSurface: string;\n    /**\n     * Attempts to resolve a bad symbol ID into a value that can be found in {@link MSLookup}.\n     * If it fails, it will return the symbol code for a invalid symbol which is displayed as\n     * an inverted question mark (110098000010000000000000000000)\n     * @param symbolID 30 character string\n     * @return 30 character string representing the resolved symbol ID.\n     * @deprecated See {@link SymbolUtilities.reconcileSymbolID()}\n     */\n    static reconcileSymbolID(symbolID: string): string;\n    /**\n     * Gets the version number from the Symbol ID at positions 1-2\n     * @param symbolID 30 character string\n     * @return number like {@link SymbolID.Version_2525Dch1} (11)\n     */\n    static getVersion(symbolID: string): number;\n    /**\n     * Sets the version number of the Symbol ID at positions 1-2\n     * @param symbolID 30 character string\n     * @param ver number like {@link SymbolID.Version_2525Dch1} (11)\n     * @return updated 30 character string symbol ID\n     */\n    static setVersion(symbolID: string, ver: number): string;\n    /**\n     * Gets the version number from the Symbol ID at positions 3-4.\n     * Restricted and No-Strike values are NATO only.\n     * @param symbolID 30 character string\n     * @return number, number will not be padded if it starts with a '0'\n     */\n    static getStandardIdentity(symbolID: string): number;\n    /**\n     * Sets the version number of the Symbol ID at positions 3-4\n     * @param symbolID 30 character string\n     * @param si number If number is a single digit, will pad with a '0' before inserting into symbol ID\n     * @return updated 30 character string symbol ID\n     */\n    static setStandardIdentity(symbolID: string, si: number): string;\n    /**\n     * Get Context (Reality (0), Exercise (1), Simulation (2)) at position 3\n     * Higher values are NATO only\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getContext(symbolID: string): number;\n    /**\n     * Set Context (Reality (0), Exercise (1), Simulation (2)) at position 3\n     * @param symbolID 30 Character string\n     * @param context number\n     * @return string updated Symbol ID.\n     */\n    static setContext(symbolID: string, context: number): string;\n    /**\n     * Get Affiliation from position 4\n     * @param symbolID 30 Character string\n     * @return number like {@link SymbolID.StandardIdentity_Affiliation_Friend}\n     */\n    static getAffiliation(symbolID: string): number;\n    /**\n     * Set Affiliation at position 4\n     * @param symbolID 30 Character string\n     * @param affiliation like {@link SymbolID.StandardIdentity_Affiliation_Friend}\n     * @return string updated Symbol ID.\n     */\n    static setAffiliation(symbolID: string, affiliation: number): string;\n    /**\n     * Get Symbol Set from positions 5-6\n     * @param symbolID 30 Character string\n     * @return number like {@link SymbolID.SymbolSet_LandCivilianUnit_Organization}\n     */\n    static getSymbolSet(symbolID: string): number;\n    /**\n     * Set Symbol Set at positions 5-6\n     * @param symbolID 30 Character string\n     * @param ss like {@link SymbolID.SymbolSet_LandCivilianUnit_Organization}\n     * @return string updated Symbol ID.\n     */\n    static setSymbolSet(symbolID: string, ss: number): string;\n    /**\n     * Get Symbol Set from position 7\n     * @param symbolID 30 Character string\n     * @return number like {@link SymbolID.Status_Present}\n     */\n    static getStatus(symbolID: string): number;\n    /**\n     * Set Symbol Set at position 7\n     * @param symbolID 30 Character string\n     * @param status like {@link SymbolID.Status_Present}\n     * @return string updated Symbol ID.\n     */\n    static setStatus(symbolID: string, status: number): string;\n    /**\n     * Get Symbol Set from position 8\n     * @param symbolID 30 Character string\n     * @return number like {@link SymbolID.HQTFD_Headquarters}\n     */\n    static getHQTFD(symbolID: string): number;\n    /**\n     * Set Symbol Set at position 8\n     * @param symbolID 30 Character string\n     * @param HQTFD like {@link SymbolID.HQTFD_Headquarters}\n     * @return string updated Symbol ID.\n     */\n    static setHQTFD(symbolID: string, HQTFD: number): string;\n    /**\n     * Get Amplifier Descriptor which can describe things like\n     * Echelon / Mobility / Towed Array / Leadership Indicator from positions 9-10\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getAmplifierDescriptor(symbolID: string): number;\n    /**\n     * Set Amplifier Descriptor which can describe things like\n     * Echelon / Mobility / Towed Array / Leadership Indicator at positions 9-10\n     * @param symbolID 30 Character string\n     * @param ad\n     * @return string\n     */\n    static setAmplifierDescriptor(symbolID: string, ad: number): string;\n    /**\n     * Get Entity Code from positions 11-16.\n     * These six digits identify a unique symbol within a symbol set.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getEntityCode(symbolID: string): number;\n    /**\n     * Set Entity Code at positions 11-16.\n     * These six digits identify a unique symbol within a symbol set.\n     * @param symbolID 30 Character string\n     * @param entityCode number\n     * @return string\n     */\n    static setEntityCode(symbolID: string, entityCode: number): string;\n    /**\n     * Get Entity from positions 11-12.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getEntity(symbolID: string): number;\n    /**\n     * Get Entity Type from positions 13-14.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getEntityType(symbolID: string): number;\n    /**\n     * Get Entity Subtype from positions 15-16.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getEntitySubtype(symbolID: string): number;\n    /**\n     * Get Sector 1 Modifier from positions 17-18.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getModifier1(symbolID: string): number;\n    /**\n     * Set Sector 1 Modifier at positions 17-18.\n     * @param symbolID 30 Character string\n     * @param mod1 number 0-99 && 161-255 OR string \"00\" to \"FF\"\n     * @return string\n     */\n    static setModifier1(symbolID: string, mod1: number | string): string;\n    /**\n     * Get Common Sector 1 Modifier from position 21.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getCommonModifier1(symbolID: string): number;\n    /**\n     * Set Common Sector 1 Modifier at position 21.\n     * @param symbolID 30 Character string\n     * @param mod1I number\n     * @return string\n     */\n    static setCommonModifier1(symbolID: string, mod1I: number): string;\n    /**\n     * Get Sector 2 Modifier from positions 17-18.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getModifier2(symbolID: string): number;\n    /**\n     * Set Sector 2 Modifier at positions 17-18.\n     * @param symbolID 30 Character string\n     * @param mod2 number 0-255 or string \"00\" to \"FF\"\n     * @return string\n     */\n    static setModifier2(symbolID: string, mod2: number | string): string;\n    /**\n     * Get Common Sector 2 Modifier from position 22.\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getCommonModifier2(symbolID: string): number;\n    /**\n     * Set Common Sector 2 Modifier at position 22.\n     * @param symbolID 30 Character string\n     * @param mod1I number\n     * @return string\n     */\n    static setCommonModifier2(symbolID: string, mod1I: number): string;\n    /**\n     * Returns true is symbol is of the Meteorological variety.\n     * @param symbolID 30 Character string\n     * @return boolean\n     * @deprecated See {@link SymbolUtilities.isWeather()}\n     */\n    static isMETOC(symbolID: string): boolean;\n    /**\n     * Build string to grab appropriate SVG file\n     * @param symbolID 30 Character string\n     * @return string\n     * @deprecated See {@link SVGLookup.getFrameID()}\n     */\n    static getFrameID(symbolID: string): string;\n    /**\n     * Build string to grab appropriate SVG file\n     * @param symbolID 30 Character string\n     * @return string\n     * @deprecated See {@link SVGLookup.getMainIconID()}\n     */\n    static getMainIconID(symbolID: string): string;\n    /**\n     * Build string to grab appropriate SVG file\n     * @param symbolID 30 Character string\n     * @return string\n     * @deprecated See {@link SVGLookup.getMod2ID()} (String)}\n     */\n    static getMod2ID(symbolID: string): string;\n    /**\n     * Build string to grab appropriate SVG file\n     * @param symbolID 30 Character string\n     * @return string\n     * @deprecated See {@link SVGLookup.getMod1ID()} (String)}\n     */\n    static getMod1ID(symbolID: string): string;\n    /**\n     * Gets the 3 digit county code from positions 28-30\n     * @param symbolID 30 Character string\n     * @return number\n     */\n    static getCountryCode(symbolID: string): number;\n    /**\n     * In 2525E, position 23 of the symbol code has the Frame Shape modifier.\n     * This lets a user force a different frame shape than what a symbol would normally have.\n     * Like you could have Air Fixed Wing with a ground unit rectangle frame for when it's on the ground.\n     * This function returns the frame shape value for what the default frame would be for a specific symbol set.\n     * @param symbolID 30 Character string\n     * @return string (1 character)\n     */\n    static getDefaultFrameShape(symbolID: string): string;\n    /**\n     *\n     * @param symbolID\n     * @param frameShape like SymbolID.FrameShape_LandEquipment\n     * @return\n     */\n    static setFrameShape(symbolID: string, frameShape: string): string;\n    /**\n     * Gets the Frame Shape override from position 23.\n     * @param symbolID 30 Character string\n     * @return string (1 character)\n     */\n    static getFrameShape(symbolID: string): string;\n}\n//# sourceMappingURL=SymbolID.d.ts.map","milsym/renderer/utilities/SymbolUtilities.d.ts":"import { Point } from \"../../graphics/Point\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\nimport { Color } from \"./Color\";\n/**\n * Has various utility functions for prcessing the symbol code.\n * See {@link SymbolID} for additional functions related to parsing the symbol code.\n *\n */\nexport declare class SymbolUtilities {\n    private static readonly Digits;\n    private static readonly HexDigits;\n    private static readonly Exp;\n    private static readonly fpRegex;\n    private static readonly pIsNumber;\n    /**\n     * Determines if a String represents a valid number\n     *\n     * @param text string\n     * @return \"1.56\" == true, \"1ab\" == false\n     */\n    static isNumber(text: string): boolean;\n    /**\n     * Converts a Java Date object into a properly formatted String for W or W1.\n     * DDHHMMSSZMONYYYY\n     * Field W: D = day, H = hour, M = minute, S = second, Z = Greenwich or local time, MON= month and Y = year.\n     * @param date {@link Date}\n     * @return string\n     */\n    static getDateLabel(date: Date): string;\n    /**\n     * Given date, return character String representing which NATO time zone\n     * you're in.\n     *\n     * @param time {@link Date}\n     * @return string\n     */\n    private static getZuluCharFromTimeZoneOffset;\n    /**\n     * Determines if a symbol, based on it's symbol ID, can have the specified modifier/amplifier.\n     * @param symbolID 30 Character string\n     * @param modifier {@link Modifiers}\n     * @return\n     */\n    static hasModifier(symbolID: string, modifier: string): boolean;\n    /**\n     * Gets Basic Symbol ID which is the Symbol Set + Entity Code\n     * @param id 30 Character string\n     * @return 8 character string (Symbol Set + Entity Code)\n     */\n    static getBasicSymbolID(id: string): string;\n    /**\n     * Gets the basic Symbol ID for a 2525C symbol\n     * S*F*GPU---*****\n     * G*G*GPP---****X\n     * @param strSymbolID 15 Character string\n     * @return 15 Character string\n     * @deprecated function will be removed\n     */\n    static getBasicSymbolID2525C(strSymbolID: string): string;\n    /**\n     * Attempts to resolve a bad symbol ID into a value that can be found in {@link MSLookup}.\n     * If it fails, it will return the symbol code for a invalid symbol which is displayed as\n     * an inverted question mark (110098000010000000000000000000)\n     * @param symbolID 30 character string\n     * @return 30 character string representing the resolved symbol ID.\n     */\n    static reconcileSymbolID(symbolID: string): string;\n    /**\n     * Gets line color used if no line color has been set. The color is specified based on the affiliation of\n     * the symbol and whether it is a unit or not.\n     * @param symbolID 30 character string\n     * @return\n     */\n    static getLineColorOfAffiliation(symbolID: string): Color | null;\n    /**\n     * For Control Measures, returns the default color for a symbol when it differs from the\n     * affiliation line color.  If there is no default color, returns the value from {@link getLineColorOfAffiliation()}\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static getDefaultLineColor(symbolID: string): Color | null;\n    /**\n     * Checks if a symbol should be filled by default\n     *\n     * @param strSymbolID The 20 digit representation of the 2525D symbol\n     * @return true if there is a default fill\n     */\n    static hasDefaultFill(strSymbolID: string): boolean;\n    /**\n     * Determines if the symbol is a tactical graphic\n     *\n     * @param strSymbolID 30 Character string\n     * @return true if symbol set is 25 (control measure), or is a weather graphic\n     */\n    static isTacticalGraphic(strSymbolID: string): boolean;\n    /**\n     * Determines if the Symbol can be rendered as a multipoint graphic and not just as an icon\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isMultiPoint(symbolID: string): boolean;\n    static isActionPoint(symbolID: string): boolean;\n    /**\n     * Control Measures and Tactical Graphics that have labels but not with the Action Point layout\n     * @param strSymbolID 30 Character string\n     * @return\n     * @deprecated see {@link isSPWithSpecialModifierLayout(String)}\n     */\n    static isTGSPWithSpecialModifierLayout(strSymbolID: string): boolean;\n    /**\n     * Returns the fill color for the symbol based on its affiliation\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static getFillColorOfAffiliation(symbolID: string): Color | null;\n    /**\n     *\n     * @param symbolID 30 Character string\n     * @param modifier {@link Modifiers}\n     * @return\n     * @deprecated see {@link hasModifier()}\n     */\n    static canSymbolHaveModifier(symbolID: string, modifier: string): boolean;\n    /**\n     * Checks if the Symbol Code has FDI set.\n     * Does not check if the symbol can have an FDI.\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static hasFDI(symbolID: string): boolean;\n    /**\n     * Returns true if graphic is protection graphic (obstacles which render green)\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isGreenProtectionGraphic(symbolID: string): boolean;\n    /***\n     * Returns true if graphic is protection graphic (obstacles which render green)\n     * Assumes control measure symbol code where SS == 25\n     * @param entity\n     * @param entityType\n     * @param entitySubtype\n     * @return\n     */\n    static isGreenProtectionGraphic(entity: number, entityType: number, entitySubtype: number): boolean;\n    /**\n     * Returns true if Symbol ID represents a chemical, biological, radiological or nuclear incident.\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isCBRNEvent(symbolID: string): boolean;\n    /**\n     * Returns true if Symbol ID represents a Sonobuoy.\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isSonobuoy(symbolID: string): boolean;\n    /**\n     * Obstacles are generally required to have a green line color\n     * @param symbolID 30 Character string\n     * @return\n     * @deprecated see {@link isGreenProtectionGraphic()}\n     */\n    static isObstacle(symbolID: string): boolean;\n    /**\n     * Return true if symbol is from the Atmospheric, Oceanographic or Meteorological Space Symbol Sets.\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isWeather(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol has the HQ staff indicated by the symbol ID\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isHQ(symbolID: string): boolean;\n    /**\n     * Checks if this is a single point control measure or meteorological graphic with a unique layout.\n     * Basically anything that's not an action point style graphic with modifiers\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isSPWithSpecialModifierLayout(symbolID: string): boolean;\n    /**\n     * Gets the anchor point for single point Control Measure as the anchor point isn't always they center of the symbol.\n     * @param symbolID 30 Character string\n     * @param bounds {@link Rectangle2D} representing the bound of the core symbol in the image.\n     * @return  representing the point in the image that is the anchor point of the symbol.\n     */\n    static getCMSymbolAnchorPoint(symbolID: string, bounds: Rectangle2D): Point;\n    /**\n     * Returns true if the symbol is an installation\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isInstallation(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol is from an air based symbol set\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isAir(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol is from a space based symbol set\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isSpace(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol is from a land based symbol set\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isLand(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol ID has the task for indicator\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isTaskForce(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol ID indicates the context is Reality\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isReality(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol ID indicates the context is Exercise\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isExercise(symbolID: string): boolean;\n    /**\n     * Returns true if the symbol ID indicates the context is Simulation\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static isSimulation(symbolID: string): boolean;\n    /**\n     * Reads the Symbol ID string and returns the text that represents the echelon\n     * code.\n     * @param echelon  from positions 9-10 in the symbol ID\n     * See {@link SymbolID.getAmplifierDescriptor()}\n     * @return string (23 (Army) would be \"XXXX\")\n     */\n    static getEchelonText(echelon: number): string;\n    /**\n     * Returns the Standard Identity Modifier based on the Symbol ID\n     * @param symbolID 30 Character string\n     * @return string\n     */\n    static getStandardIdentityModifier(symbolID: string): string;\n    /**\n     * Returns true if the unit has a rectangle frame\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static hasRectangleFrame(symbolID: string): boolean;\n    /**\n     * Returns the height ratio for the unit specified by the symbol ID\n     * Based on Figure 4 in 2525E.\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static getUnitRatioHeight(symbolID: string): number;\n    /**\n     * Returns the width ratio for the unit specified by the symbol ID\n     * Based on Figure 4 in 2525E.\n     * @param symbolID 30 Character string\n     * @return\n     */\n    static getUnitRatioWidth(symbolID: string): number;\n    /**\n    * @param linetype the line type\n    * @return true if the line is a basic shape\n    */\n    static isBasicShape(linetype: number): boolean;\n}\n//# sourceMappingURL=SymbolUtilities.d.ts.map","milsym/renderer/utilities/TextInfo.d.ts":"import { Font } from \"../../graphics/Font\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { Rectangle2D } from \"../../graphics/Rectangle2D\";\n/**\n *\n *\n */\nexport declare class TextInfo {\n    protected _fontName: string;\n    protected _fontStyle: number;\n    protected _fontSize: number;\n    protected _text: string;\n    protected _location: Point2D;\n    protected _bounds: Rectangle2D;\n    protected _descent: number;\n    protected _aboveBaseHeight: number;\n    private isNode;\n    private isBrowser;\n    private OSCDefined;\n    private OSCanvasDefined;\n    constructor(text: string, x: number, y: number, font: Font | string, context: OffscreenCanvasRenderingContext2D | CanvasRenderingContext2D | null);\n    setLocation(x: number, y: number): void;\n    getLocation(): Point2D;\n    shift(x: number, y: number): void;\n    getText(): string;\n    /**\n     * includes the descent\n     * @returns\n     */\n    getTextBounds(): Rectangle2D;\n    getTextOutlineBounds(): Rectangle2D;\n    getDescent(): number;\n    getFontName(): string;\n    getFontSize(): number;\n    getFontStyle(): number;\n    strokeText(context: OffscreenCanvasRenderingContext2D): void;\n    fillText(context: OffscreenCanvasRenderingContext2D): void;\n}\n//# sourceMappingURL=TextInfo.d.ts.map","milsym/tactical/channel-utils.d.ts":"import { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { P1 } from \"./p1\";\nimport { TacticalGraphic } from \"./tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n * A class to process channel types.\n *\n *\n */\nexport declare class ChannelUtils {\n    private static readonly _className;\n    /**\n     * Gets partitions from the client points based on the segments generated by\n     * GetSegments. Partitions are used handle double-backed segments. Each\n     * partition is a continuous sequence of points for a channel.\n     *\n     * @param segments\n     * @param partitions OUT the partitions\n     * @return\n     */\n    private static GetPartitions;\n    /**\n     * Draws a partition to the shapes array and stores the calculated channel\n     * points\n     *\n     * @param fromSegment\n     * @param toSegment\n     * @param pixels\n     * @param channelWidth\n     * @param bolLastSegment\n     * @param shapes\n     * @param channelPoints\n     * @param distanceToChannelPoint\n     * @return\n     */\n    private static DrawGoodChannel2;\n    /**\n     * Draws the channel partitions to the shapes array\n     *\n     * @param pixels\n     * @param partitions\n     * @param channelWidth channel width in pixels\n     * @param shapes\n     * @param channelPoints\n     * @param distanceToChannelPoint distance in pixels from the tip to the back\n     * of the arrow\n     */\n    private static DrawSegments;\n    private static DrawLCSingleLineSegments;\n    /**\n     * Handle symbol too small for line of contact\n     * @param tg\n     * @param pixels\n     * @return\n     */\n    private static getLCPixels;\n    /**\n     * The main interface to ChannelUtils calls DrawChannel2 after stuffing\n     * the points into an array of 2-tuples x,y\n     *\n     * @param pixels the client points\n     * @param linetype the line type\n     * @param tg the tactical graphic\n     * @param shapes\n     * @param channelPoints\n     */\n    static DrawChannel(pixels: Array<POINT2>, linetype: number, tg: TacticalGraphic, shapes: Array<Shape2>, channelPoints: Array<POINT2> | null, clipBounds: Rectangle2D, converter: IPointConversion): void;\n    /**\n     * utility for METOC to handle double-backed segments\n     *\n     * @param tg the tactical graphic object\n     */\n    static GetPartitions2(tg: TacticalGraphic): Array<P1> | null;\n    /**\n     * The main function for processing channel types. Gets segments, then gets\n     * partitions from the segments and then draws the partitions.\n     *\n     * @param pixels the client points as an array of 2-tuples x,y\n     * @param linetype the line type\n     * @param tg the tactical graphic object\n     * @param shapes\n     * @param channelPoints\n     */\n    private static DrawChannel2;\n}\n//# sourceMappingURL=channel-utils.d.ts.map","milsym/tactical/labels.d.ts":"import { TacticalGraphic } from \"./tactical-graphic\";\n/**\n * Returns a generic label for the symbol per Mil-Std-2525\n *\n * @param tg\n * @return\n */\nexport declare function getCenterLabel(tg: TacticalGraphic): string;\n//# sourceMappingURL=labels.d.ts.map","milsym/tactical/metoc.d.ts":"import { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"./tactical-graphic\";\n/**\n * Class to calculate the points for the Weather symbols\n *\n */\nexport declare class METOC {\n    private static readonly _className;\n    static getWeatherLinetype(version: number, entityCode: number): number;\n    /**\n     * @param symbolID Mil-Standard 2525 20-30 digit code\n     * @return the line type as an integer if it is a weather symbol, else return -1\n     */\n    static IsWeather(symbolID: string): number;\n    /**\n     * Sets tactical graphic properties based on Mil-Std-2525 Appendix C.\n     * @param tg\n     */\n    private static SetMeTOCProperties;\n    /**\n     *\n     * Rotates axis by theta for point and curve\n     * Finds next closest point with same x position on the splinePoints curve as pt\n     * walks up the curve and if it does not find a range that straddles x it return null.\n     * We ultimately will draw a line from pt to the extrapolated point on the splinePoints spline.\n     * used for ICE_OPENINGS_FROZEN_LEAD\n     *\n     * @param splinePoints - the points on the opposite spline\n     * @param pt - the point in the original curve from which the line will start\n     * @param theta angle of curve at pt. Perpendicular to new line to be drawn\n     *\n     * @return The extrapolated point on the opposite spline to which the line will be drawn\n     */\n    private static ExtrapolatePointFromCurve;\n    /**\n     * The public interface, main function to return METOC shapes\n     * @param tg the tactical graphic\n     * @param shapes the ShapeInfo array\n     */\n    static GetMeTOCShape(tg: TacticalGraphic, shapes: Array<Shape2>): void;\n    /**\n     * Sets the shape properties based on the tacttical graphic properties and also based on shape\n     * styles which may have been set by JavaLineArray\n     * @param tg\n     * @param shapes shapes array to set properties\n     */\n    protected static SetShapeProperties(tg: TacticalGraphic, shapes: Array<Shape2>): Promise<void>;\n    /**\n     * Draws an arrow to the GeneralPath object from pt1 to pt2.\n     *\n     * @param pt1 arrow tip\n     * @param pt2 - arrow base\n     * @param size - arrow size in pixels\n     * @param lineObject - general path to draw the arrow\n     *\n     * @return arrow sprite\n     */\n    private static DrawArrow;\n    /**\n     * Returns a GeneralPath for symbols which require splines. Also returns the calculated\n     * spline points for those symbols with additional features based on them.\n     * @param tg\n     * @param splinePoints2 spline points in pixels\n     * @return\n     */\n    private static DrawSplines;\n    /**\n     * Calculates a point on a segment using a ratio of the segment length.\n     * This function is used for calculating control points on Bezier curves.\n     *\n     * @param P0 the 1st point on the segment.\n     * @param P1 the last point on the segment\n     * @param ratio the fraction of the segment length\n     *\n     * @return calculated point on the P0-P1 segment.\n     */\n    private static getPointOnSegment;\n    /**\n     * This function will trace a cubic approximation of the cubic Bezier\n     * It will calculate a series of (control point/Destination point] which\n     * will be used to draw quadratic Bezier starting from P0\n     *\n     * @param lineObject - the sprite to use for drawing\n     * @param P0 - 1st client point\n     * @param P1 - 1st control point for a cubic Bezier\n     * @param P2 - 2nd control point\n     * @param P3 - 2nd client point\n     *\n     * @return an array of points along the spline at linetype specific intervals\n     */\n    private static drawCubicBezier2;\n    /**\n     * Call this function with segment\n     * @param Pixels a segment of tg.Pixels\n     * @return\n     */\n    private static ParallelLines2;\n}\n//# sourceMappingURL=metoc.d.ts.map","milsym/tactical/modifier-placement.d.ts":"import { AffineTransform } from \"../graphics/AffineTransform\";\nimport { Font } from \"../graphics/Font\";\nimport { Graphics2D } from \"../graphics/Graphics2D\";\nimport { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { Shape } from \"../graphics/Shape\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"./tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\nimport { SVGSymbolInfo } from \"../renderer/utilities/SVGSymbolInfo\";\n/**\n * This class handles everything having to do with text for a\n * tactical graphic. Note: labels are handled the same as text modifiers.\n *\n *\n */\nexport declare class Modifier2 {\n    textPath: POINT2[];\n    private textID;\n    private featureID;\n    text: string;\n    image: SVGSymbolInfo;\n    private iteration;\n    private justify;\n    type: number;\n    lineFactor: number;\n    private static readonly _className;\n    isIntegral: boolean;\n    private fitsMBR;\n    protected constructor();\n    private static readonly toEnd;\n    private static readonly aboveMiddle;\n    private static readonly area;\n    private static readonly screen;\n    private static readonly aboveEnd;\n    private static readonly aboveMiddlePerpendicular;\n    private static readonly aboveStartInside;\n    private static readonly aboveEndInside;\n    private static readonly areaImage;\n    private static fillAlphaCanObscureText;\n    private static DoublesBack;\n    /**\n     * Returns a generic label for the symbol per Mil-Std-2525.\n     * Delegates to standalone function in labels.ts.\n     */\n    static GetCenterLabel(tg: TacticalGraphic): string;\n    get_TextPath(): POINT2[];\n    protected set_TextPath(value: POINT2[]): void;\n    private static AddOffsetModifier;\n    /**\n     *\n     * @param tg\n     * @param text\n     * @param type\n     * @param lineFactor\n     * @param ptStart\n     * @param ptEnd\n     */\n    private static AddModifier;\n    private static AddModifier2;\n    private static AddIntegralModifier;\n    /**\n     * sets modifier.textId to the modifier type, e.g. label, T, T1, etc.\n     *\n     * @param tg\n     * @param text\n     * @param type\n     * @param lineFactor\n     * @param pt0\n     * @param pt1\n     * @param modifierType\n     */\n    private static AddAreaModifier;\n    private static AddIntegralAreaModifier;\n    private static AddImageModifier;\n    /**\n     * Returns symbol MBR. Assumes points have been initialized with value of\n     * 0th point\n     *\n     * @param tg the tactical graphic object\n     * @param ptUl OUT - MBR upper left\n     * @param ptUr OUT - MBR upper right\n     * @param ptLr OUT - MBR lower right\n     * @param ptLl OUT - MBR lower left\n     */\n    static GetMBR(tg: TacticalGraphic, ptUl: POINT2, ptUr: POINT2, ptLr: POINT2, ptLl: POINT2): void;\n    /**\n     * Tests segment of a Boundary\n     *\n     * @param tg\n     * @param g2d\n     * @param middleSegment\n     * @return\n     */\n    static GetBoundarySegmentTooShort(tg: TacticalGraphic, g2d: Graphics2D, middleSegment: number): boolean;\n    /**\n     * Handles the line breaks for Boundary and Engineer Work Line\n     *\n     * @param tg\n     * @param g2d\n     */\n    private static AddBoundaryModifiers;\n    /**\n     * added for USAS\n     *\n     * @param tg\n     * @param metrics\n     * @deprecated\n     */\n    private static AddNameAboveDTG;\n    /**\n     * shifts the path for modifiers that use toEnd to prevent vertical paths\n     *\n     * @param tg\n     * @param pt0\n     * @param pt1\n     * @param ptLast\n     * @param ptNextToLast\n     */\n    private static shiftModifierPath;\n    /**\n     * Adds two or four labels on area outline\n     *\n     * @param label\n     * @param tg\n     * @param twoLabelOnly - when true only two labels are added to line (east and west most segment midpoints)\n     *                     when false, four labels are added to line (north, south, east and west most segment midpoints)\n     */\n    private static addModifierOnLine;\n    /**\n     * Adds N modifier on area outline\n     */\n    private static addNModifier;\n    private static addModifierBottomSegment;\n    private static addModifierTopSegment;\n    private static addDTG;\n    private static getVisibleMiddleSegment;\n    /**\n     * called repeatedly by RemoveModifiers to remove modifiers which fall\n     * outside the symbol MBR\n     *\n     * @param tg\n     * @param modifierType\n     */\n    private static removeModifier;\n    /**\n     * removes text modifiers for CPOF tactical areas which do not fit inside\n     * the symbol MBR\n     *\n     * @param tg\n     * @param g2d\n     * @param isTextFlipped true if text is flipped from the last segment\n     * orientation\n     * @param iteration the instance count for this modifier\n     */\n    static RemoveModifiers(tg: TacticalGraphic, g2d: Graphics2D, isTextFlipped: boolean, iteration: number): void;\n    /**\n     * Calculates a segment in the pixels middle by length to hold a string.\n     *\n     * @param tg\n     * @param stringWidth\n     * @param segPt0\n     * @param segPt1\n     */\n    private static getPixelsMiddleSegment;\n    private static getChange1Height;\n    /**\n     * scale the line factor for closed areas\n     *\n     * @param tg\n     */\n    private static scaleModifiers;\n    /**\n     * Calculate modifiers identical to addModifiers except use geodesic\n     * calculations for the center point.\n     *\n     * @param tg\n     * @param g2d\n     * @param clipBounds\n     * @param converter\n     */\n    static AddModifiersGeo(tg: TacticalGraphic, g2d: Graphics2D, clipBounds: Rectangle2D | Array<Point2D> | null, converter: IPointConversion): void;\n    /**\n     * RFA, NFA, FFA need these for line spacing\n     *\n     * @param tg\n     * @return\n     */\n    private static getRFALines;\n    /**\n     * Added sector range fan modifiers based using the calculated orientation\n     * indicator points\n     *\n     * @param tg\n     * @param converter\n     * @return\n     */\n    private static addSectorModifiers;\n    /**\n     * Called by the renderer after tg.Pixels has been filled with the\n     * calculated points. The modifier path depends on points calculated by\n     * CELineArray.\n     *\n     * @param tg\n     */\n    static AddModifiers2(tg: TacticalGraphic, converter: IPointConversion): void;\n    /**\n     * Displays the tg modifiers using a client Graphics2D.\n     * Delegates to standalone function in modifier-shapes.ts.\n     * @deprecated\n     */\n    static DisplayModifiers(tg: TacticalGraphic, g2d: Graphics2D): void;\n    /**\n     * Returns a Shape object for the text background for labels and modifiers.\n     * Delegates to standalone function in modifier-shapes.ts.\n     */\n    static BuildModifierShape(tg: TacticalGraphic, pt0: POINT2, pt1: POINT2, stringWidth: number, stringHeight: number, lineFactor: number, isTextFlipped: boolean): Shape2;\n    /**\n     * For BOUNDARY and other line types which require breaks for the integral text.\n     * Delegates to standalone function in modifier-shapes.ts.\n     */\n    static GetIntegralTextShapes(tg: TacticalGraphic, g2d: Graphics2D, shapes: Array<Shape2>): void;\n    /**\n     * Displays the modifiers to a Graphics2D from a BufferedImage.\n     * Delegates to standalone function in modifier-shapes.ts.\n     */\n    static DisplayModifiers2(tg: TacticalGraphic, g2d: Graphics2D, shapes: Array<Shape2>, isTextFlipped: boolean, converter: IPointConversion): void;\n    /**\n     * Builds a shape object to wrap text.\n     * Delegates to standalone function in modifier-shapes.ts.\n     */\n    static getTextShape(g2d: Graphics2D, str: string, font: Font, tx: AffineTransform): Shape;\n    /**\n     * Creates text outline as a shape.\n     * Delegates to standalone function in modifier-shapes.ts.\n     */\n    static createTextOutline(originalText: Shape2): Shape2;\n    private static removeDecimal;\n}\n//# sourceMappingURL=modifier-placement.d.ts.map","milsym/tactical/modifier-shapes.d.ts":"import { AffineTransform } from \"../graphics/AffineTransform\";\nimport { Font } from \"../graphics/Font\";\nimport { Graphics2D } from \"../graphics/Graphics2D\";\nimport { Shape } from \"../graphics/Shape\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { TacticalGraphic } from \"./tactical-graphic\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n * Displays the tg modifiers using a client Graphics2D, this is an option\n * provided to clients for displaying modifiers without using shapes\n *\n * @param tg the tactical graphic\n * @param g2d the graphics object for drawing\n * @deprecated\n */\nexport declare function displayModifiers(tg: TacticalGraphic, g2d: Graphics2D): void;\n/**\n * Returns a Shape object for the text background for labels and modifiers\n *\n * @param tg the tactical graphic object\n * @param pt0 1st point of segment\n * @param pt1 last point of segment\n * @param stringWidth string width\n * @param stringHeight string height\n * @param lineFactor number of text lines above or below the segment\n * @param isTextFlipped true if text is flipped\n * @return the modifier shape\n */\nexport declare function buildModifierShape(tg: TacticalGraphic, pt0: POINT2, pt1: POINT2, stringWidth: number, stringHeight: number, lineFactor: number, isTextFlipped: boolean): Shape2;\n/**\n * For BOUNDARY and other line types which require breaks for the integral\n * text. Currently only boundary uses this\n *\n * @param tg\n * @param g2d the graphics object for drawing\n * @param shapes the shape array\n */\nexport declare function getIntegralTextShapes(tg: TacticalGraphic, g2d: Graphics2D, shapes: Array<Shape2>): void;\n/**\n * Displays the modifiers to a Graphics2D from a BufferedImage\n *\n * @param tg the tactical graphic\n * @param g2d the Graphic for drawing\n * @param shapes the shape array\n * @param isTextFlipped true if text is flipped\n * @param converter to convert between geographic and pixel coordinates\n */\nexport declare function displayModifiers2(tg: TacticalGraphic, g2d: Graphics2D, shapes: Array<Shape2>, isTextFlipped: boolean, converter: IPointConversion): void;\n/**\n * Builds a shape object to wrap text\n *\n * @param g2d the Graphic object for drawing\n * @param str text to wrap\n * @param font the draw font\n * @param tx the drawing transform, text rotation and translation\n * @return\n */\nexport declare function getTextShape(g2d: Graphics2D, str: string, font: Font, tx: AffineTransform): Shape;\n/**\n * Creates text outline as a shape\n *\n * @param originalText the original text\n * @return text shape\n */\nexport declare function createTextOutline(originalText: Shape2): Shape2;\n//# sourceMappingURL=modifier-shapes.d.ts.map","milsym/tactical/p1.d.ts":"/**\n * Class used by channels for dtermining segments. Segments are used by\n * ChannelUtils to handle double-backed segments so that the lines\n * will not go off the display area.\n *\n *\n */\nexport declare class P1 {\n    start: number;\n    end_Renamed: number;\n}\n//# sourceMappingURL=p1.d.ts.map","milsym/tactical/tactical-graphic.d.ts":"import { Font } from \"../graphics/Font\";\nimport { TexturePaint } from \"../graphics/TexturePaint\";\nimport { POINT2 } from \"../types/point\";\nimport { Modifier2 } from \"./modifier-placement\";\nimport { Color } from \"../renderer/utilities/Color\";\n/**\n * A class to encapsulate the tactical graphic object. Many of the properties\n * correspond to a client MilStdSymbol object.\n *\n *\n */\nexport declare class TacticalGraphic {\n    LatLongs: Array<POINT2>;\n    private static readonly _className;\n    Pixels: Array<POINT2>;\n    modifiers: Array<Modifier2>;\n    texturePaint: TexturePaint;\n    protected maskOff: boolean;\n    constructor();\n    font: Font;\n    private iconSize;\n    /**\n     * Set the icon size for areas that have a symbol like LAA or Biological Contaminated Area\n     * @param pixelSize\n     */\n    setIconSize(pixelSize: number): void;\n    getIconSize(): number;\n    keepUnitRatio: boolean;\n    lineType: number;\n    lineStyle: number;\n    lineColor: Color | null;\n    fillStyle: number;\n    fillColor: Color | null;\n    fontBackColor: Color;\n    textColor: Color;\n    lineThickness: number;\n    private _name;\n    get name(): string;\n    set name(value: string);\n    client: string;\n    private _t1;\n    get t1(): string;\n    set t1(value: string);\n    private _am;\n    get am(): string;\n    set am(value: string);\n    private _am1;\n    get am1(): string;\n    set am1(value: string);\n    private _an;\n    get an(): string;\n    set an(value: string);\n    private _v;\n    get v(): string;\n    set v(value: string);\n    private _ap;\n    get ap(): string;\n    set ap(value: string);\n    private _as;\n    get as(): string;\n    set as(value: string);\n    x: string;\n    x1: string;\n    private _h;\n    get h(): string;\n    set h(value: string);\n    private _location;\n    get location(): string;\n    set location(value: string);\n    private _h1;\n    /**\n     * @deprecated\n     */\n    get h1(): string;\n    /**\n     * @deprecated\n     */\n    set h1(value: string);\n    n: string;\n    private _h2;\n    /**\n     * @deprecated\n     */\n    get h2(): string;\n    /**\n     * @deprecated\n     */\n    set h2(value: string);\n    /**\n     * Only used for range fan\n     * left azimuth,right azimuth,min radius,max radius\n     */\n    leftRightMinMax: string;\n    private _dtg;\n    get dtg(): string;\n    set dtg(value: string);\n    private _dtg1;\n    get dtg1(): string;\n    set dtg1(value: string);\n    standardIdentity: string;\n    /**\n     * @return true if standard identity is suspect/joker or hostile/faker\n     */\n    isHostile(): boolean;\n    echelonSymbol: string;\n    private _symbolId;\n    get symbolId(): string;\n    status: string;\n    /**\n     * Sets tactical graphic properties based on the 20-30 digit Mil-Std-2525 symbol code\n     *\n     * @param value\n     */\n    set symbolId(value: string);\n    visibleModifiers: boolean;\n    visibleLabels: boolean;\n    useLineInterpolation: boolean;\n    useDashArray: boolean;\n    useHatchFill: boolean;\n    wasClipped: boolean;\n    hideOptionalLabels: boolean;\n    lineCap: number;\n    patternScale: number;\n}\n//# sourceMappingURL=tactical-graphic.d.ts.map","milsym/tactical/tactical-utils.d.ts":"import { BasicStroke } from \"../graphics/BasicStroke\";\nimport { Line2D } from \"../graphics/Line2D\";\nimport { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle2D } from \"../graphics/Rectangle2D\";\nimport { POINT2 } from \"../types/point\";\nimport { Shape2 } from \"../generators/shape2\";\nimport { P1 } from \"./p1\";\nimport { TacticalGraphic } from \"./tactical-graphic\";\nimport { Color } from \"../renderer/utilities/Color\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n * A general utility class for the tactical renderer\n *\n */\nexport declare class TacticalUtils {\n    private static readonly _className;\n    protected static POINT2ToPoint2D(pt2: POINT2): Point2D | null;\n    /**\n     * returns true if the line segments are all outside the bounds\n     * @param tg the tactical graphic\n     * @param clipBounds the pixels based clip bounds\n     * @return\n     */\n    static linesOutsideClipBounds(tg: TacticalGraphic, clipBounds: Rectangle2D): boolean;\n    /**\n     * Returns the minimum client points needed for the symbol\n     * @param lineType line type\n     * @return minimum number of clients required to render the line\n     * @deprecated use MSInfo.getMinPointCount()\n     */\n    static GetMinPoints(lineType: number): number;\n    /**\n     * @param linetype line type\n     * @return true if the line is a closed area\n     */\n    static isClosedPolygon(linetype: number): boolean;\n    /**\n     * Closes the polygon for areas\n     * @param Pixels the client points\n     */\n    static ClosePolygon(Pixels: Array<POINT2>): void;\n    /**\n     * for change 1 symbol the W/w1 modifiers run too close to the symbol outline\n     * so it shifts the line along the line away from the edge\n     * @param p1\n     * @param p2\n     * @param shift\n     */\n    static shiftModifiersLeft(p1: POINT2, p2: POINT2, shift: number): void;\n    /**\n     * Overrides shape properties for symbols based on Mil-Std-2525\n     * @param tg\n     * @param shape\n     */\n    protected static ResolveModifierShape(tg: TacticalGraphic, shape: Shape2): void;\n    static GetOpaqueColor(color: Color): Color;\n    /**\n     * These lines allow fill\n     * @param linetype\n     * @return\n     */\n    static LinesWithFill(linetype: number): boolean;\n    /**\n     * @deprecated\n     * if the line color and fill color are the same or very close then we want to\n     * tweak the fill color a bit to make the line appear distinct from the fill.\n     * @param tg\n     */\n    static tweakFillColor(tg: TacticalGraphic): void;\n    /**\n     * @deprecated\n     * Test to see if two colors are similar\n     * @param c1\n     * @param c2\n     * @return true is same (or similar) color\n     */\n    static isSameColor(c1: Color, c2: Color): boolean;\n    /**\n     * Customer requested routine for setting the stroke dash pattern\n     * Scales dash length with line width and DPI\n     * @param width\n     * @param style\n     * @param cap\n     * @param join\n     * @return\n     */\n    static getLineStroke(width: number, style: number, cap: number, join: number): BasicStroke;\n    /**\n     * Sets shape properties based on other properties which were set by JavaLineArray\n     * @param tg tactical graphic\n     * @param shapes the ShapeInfo array\n     */\n    static SetShapeProperties(tg: TacticalGraphic, shapes: Array<Shape2>): void;\n    /**\n     * Returns a boolean indicating whether the line type is a change 1 area\n     * @param lineType the line type\n     * @return true if change 1 area\n     */\n    static IsChange1Area(lineType: number): boolean;\n    /**\n     * Calculates point where two lines intersect.\n     * First line defined by pt1, m1.\n     * Second line defined by pt2, m2.\n     * result will be written to ptIntersect.\n     * @param pt1 first line point\n     * @param m1 slope of first line\n     * @param pt2 second line point\n     * @param m2 slope of second line\n     * @param ptIntersect OUT - intersection point\n     */\n    protected static CalcIntersectPt(pt1: POINT2, m1: number, pt2: POINT2, m2: number, ptIntersect: POINT2): void;\n    /**\n     * Calculates the channel width in pixels for channel types\n     * @param pixels the client points as 2-tuples x,y in pixels\n     * @param distanceToChannelPOINT2 OUT - the calculated distance in pixels from the tip of the\n     * arrowhead to the back of the arrowhead.\n     * @return the channel width in pixels\n     */\n    static ChannelWidth(pixels: number[]): {\n        width: number;\n        distanceToChannelPoint: number;\n    };\n    private static InYOrder;\n    private static InXOrder;\n    /**\n     * For each sector calculates left azimuth, right azimuth, min radius, max radius\n     * and stuff H2 with the string delimited result. The function is public, called by JavaRendererServer\n     * @param tg tactical graphic\n     */\n    static GetSectorRadiiFromPoints(tg: TacticalGraphic): void;\n    /**\n     * Reverses the pixels except for the last point. This is used for\n     * the axis of advance type routes. The pixels are 2-tuples x,y\n     *\n     * @param pixels OUT - Array of client points\n     */\n    static ReorderPixels(pixels: number[]): void;\n    /**\n     * do not allow vertical segments for these, move the point x value by 1 pixel\n     * @param tg tactical graphic\n     */\n    static FilterVerticalSegments(tg: TacticalGraphic): void;\n    /**\n     * Client utility to calculate the channel points for channel types.\n     * This code was ported from CJMTK.\n     * @param arrLocation the client points\n     * @return the channel point\n     */\n    static ComputeLastPoint(arrLocation: Array<POINT2>): POINT2;\n    /**\n     * Called by ChannelUtils. The segments are used for managing double-backed segments\n     * for channel types. If the new point is double-backed then the segment at that index will be false.\n     *\n     * @param pixels the client points as 2-tuples x,y in pixels\n     * @param segments OUT - the segments\n     * @param factor a steepness factor for calculating whether the segment is double-backed\n     */\n    static GetSegments(pixels: number[], segments: boolean[], factor: number): void;\n    static GetLCPartitions(pixels: number[], LCChannelWith: number, partitions: Array<P1>, singleLinePartitions: Array<P1>): void;\n    /**\n     * Sets the color for the current shape depending on the affiliation\n     * @param tg\n     * @param shape\n     */\n    protected static SetLCColor(tg: TacticalGraphic, shape: Shape2): void;\n    /**\n     * USAS requires a left-right orientation for ENY, which negates the upper-lower\n     * orientation we used for Mil-Std-2525 ENY compliance. Therefore we must reverse\n     * the client points for two of the quadrants\n     * @param tg tactical graphic\n     */\n    static ReverseUSASLCPointsByQuadrant(tg: TacticalGraphic): void;\n    /**\n     * use str if tg is null\n     * @param symbolId Mil=Standard-2525 symbol id\n     * @return line type\n     */\n    static GetLinetypeFromString(symbolId: string): number;\n    /**\n     * An auto-shape is a symbol with a fixed number of anchor points\n     *\n     * @param tg tactical graphic\n     * @return true if auto-shape\n     */\n    static isAutoshape(tg: TacticalGraphic): boolean;\n    /**\n     * Client will send the segment colors within a modifier.\n     * Format is 0:FFBBBB,4:FFAAAA,...\n     * For the time being will assume the modifier being used is the H modifier\n     * @param tg\n     * @return\n     */\n    static getMSRSegmentColors(tg: TacticalGraphic): Map<number, Color> | null;\n    static getMSRSegmentColorStrings(tg: TacticalGraphic): Map<number, string> | null;\n    /**\n     * tg.H must be revised for clipped MSR, ASR and Boundary\n     * This function is called after the pixels were clipped\n     * @param originalPixels the tactical graphic pixels before clipping\n     * @param tg\n     */\n    static reviseHModifier(originalPixels: Array<POINT2>, tg: TacticalGraphic): void;\n    /**\n     * Adds extra points to LC if there are angles too small to fit the channel\n     * @param tg\n     * @param converter\n     */\n    static SegmentLCPoints(tg: TacticalGraphic, converter: IPointConversion): void;\n    /**\n     * Interpolate pixels for lines with points too close together.\n     * Drops successive points until the next point is at least 10 pixels from the preceding point\n     * @param tg\n     */\n    static InterpolatePixels(tg: TacticalGraphic): void;\n    /**\n     * construct a line segment outside the polygon corresponding to some index\n     * @param tg\n     * @param index\n     * @param dist\n     * @return\n     */\n    protected static getExtendedLine(tg: TacticalGraphic, index: number, dist: number): Line2D;\n}\n//# sourceMappingURL=tactical-utils.d.ts.map","milsym/types/enums.d.ts":"/**\n * Class to provide the symbols with values corresponding to the Mil-Standard-2525 hierarchy.\n *\n */\nexport declare class TacticalLines {\n    static readonly BS_LINE = 10000000;\n    static readonly BS_AREA = 11000000;\n    /**\n     * @deprecated\n     */\n    static readonly BS_CROSS = 12000000;\n    /**\n     * @deprecated\n     */\n    static readonly BS_ELLIPSE = 13000000;\n    static readonly PBS_ELLIPSE = 13000001;\n    static readonly PBS_CIRCLE = 13000002;\n    /**\n     * @deprecated\n     */\n    static readonly BS_RECTANGLE = 14000000;\n    static readonly PBS_RECTANGLE = 14000001;\n    /**\n     * @deprecated\n     */\n    static readonly PBS_SQUARE = 14000002;\n    /**\n     * @deprecated\n     */\n    static readonly BBS_LINE = 15000000;\n    /**\n     * @deprecated\n     */\n    static readonly BBS_AREA = 15000001;\n    static readonly BBS_POINT = 15000002;\n    /**\n     * @deprecated\n     */\n    static readonly BBS_RECTANGLE = 15000003;\n    /**\n     * @deprecated\n     */\n    static readonly BS_BBOX = 15000004;\n    static readonly BS_ORBIT = 16000001;\n    static readonly BS_ROUTE = 16000002;\n    static readonly BS_RADARC = 16000003;\n    static readonly BS_POLYARC = 16000004;\n    static readonly BS_CAKE = 16000005;\n    static readonly BS_TRACK = 16000006;\n    static readonly PZ: number;\n    static readonly LZ: number;\n    static readonly DZ: number;\n    static readonly FAADZ: number;\n    static readonly MEZ: number;\n    static readonly LOMEZ: number;\n    static readonly HIMEZ: number;\n    static readonly ROZ: number;\n    static readonly AARROZ: number;\n    static readonly UAROZ: number;\n    static readonly WEZ: number;\n    static readonly FEZ: number;\n    static readonly JEZ: number;\n    static readonly WFZ_REVD: number;\n    static readonly WFZ: number;\n    static readonly EZ: number;\n    static readonly AIRFIELD: number;\n    static readonly BASE_CAMP_REVD: number;\n    static readonly BASE_CAMP: number;\n    static readonly GUERILLA_BASE_REVD: number;\n    static readonly GUERILLA_BASE: number;\n    static readonly HIDACZ: number;\n    static readonly FPF: number;\n    static readonly ENCIRCLE: number;\n    static readonly LAA: number;\n    static readonly DRCL: number;\n    static readonly BYPASS: number;\n    static readonly BYDIF: number;\n    static readonly BYIMP: number;\n    static readonly LINTGT: number;\n    static readonly BLOCK: number;\n    static readonly LINTGTS: number;\n    static readonly BREACH: number;\n    static readonly CANALIZE: number;\n    static readonly MNFLDDIS: number;\n    static readonly DISRUPT: number;\n    static readonly EASY: number;\n    static readonly CONTAIN: number;\n    static readonly CLEAR: number;\n    static readonly ISOLATE: number;\n    static readonly OCCUPY: number;\n    static readonly PENETRATE: number;\n    static readonly RETAIN: number;\n    static readonly SECURE: number;\n    static readonly SEIZE: number;\n    static readonly ASLTXING: number;\n    static readonly GAP: number;\n    static readonly COVER: number;\n    static readonly SCREEN: number;\n    static readonly GUARD: number;\n    static readonly SARA: number;\n    static readonly FIX: number;\n    static readonly PDF: number;\n    static readonly ATKBYFIRE: number;\n    static readonly SPTBYFIRE: number;\n    static readonly CLUSTER: number;\n    static readonly TURN_REVD: number;\n    static readonly TURN: number;\n    static readonly PLANNED: number;\n    static readonly ESR1: number;\n    static readonly ESR2: number;\n    static readonly ROADBLK: number;\n    static readonly FOXHOLE: number;\n    static readonly PAA: number;\n    static readonly CONVOY: number;\n    static readonly HCONVOY: number;\n    static readonly FORDIF: number;\n    static readonly MSDZ: number;\n    static readonly STRIKWARN: number;\n    static readonly RETIRE: number;\n    static readonly RIP: number;\n    static readonly TRIP: number;\n    static readonly NAVIGATION: number;\n    static readonly MSR: number;\n    static readonly ASR: number;\n    static readonly LL: number;\n    static readonly EWL: number;\n    static readonly FERRY: number;\n    static readonly FORDSITE: number;\n    static readonly MFLANE: number;\n    static readonly RAFT: number;\n    static readonly FOLSP: number;\n    static readonly ATDITCH: number;\n    static readonly ATDITCHC: number;\n    static readonly ATWALL: number;\n    static readonly ATDITCHM: number;\n    static readonly DELAY: number;\n    static readonly DIRATKAIR: number;\n    static readonly DIRATKGND: number;\n    static readonly DIRATKSPT: number;\n    static readonly WITHDRAW: number;\n    static readonly CORDONSEARCH: number;\n    static readonly CORDONKNOCK: number;\n    static readonly WDRAWUP: number;\n    static readonly CFL: number;\n    static readonly LOD: number;\n    static readonly PL: number;\n    static readonly FEBA: number;\n    static readonly FSCL: number;\n    static readonly BCL_REVD: number;\n    static readonly BCL: number;\n    static readonly ICL: number;\n    static readonly IFF_OFF: number;\n    static readonly IFF_ON: number;\n    static readonly RFL: number;\n    static readonly FCL: number;\n    static readonly LOA: number;\n    static readonly BRDGHD: number;\n    static readonly BRDGHD_GE: number;\n    static readonly PLD: number;\n    static readonly FLOT: number;\n    static readonly AMBUSH: number;\n    static readonly LLTR: number;\n    static readonly SAAFR: number;\n    static readonly AC: number;\n    static readonly MRR: number;\n    static readonly SL: number;\n    static readonly TC: number;\n    static readonly SC: number;\n    static readonly MNFLDFIX: number;\n    static readonly MNFLDBLK: number;\n    static readonly MSR_ONEWAY: number;\n    static readonly MSR_ALT: number;\n    static readonly MSR_TWOWAY: number;\n    static readonly ASR_ONEWAY: number;\n    static readonly ASR_ALT: number;\n    static readonly ASR_TWOWAY: number;\n    static readonly TRAFFIC_ROUTE_ONEWAY: number;\n    static readonly TRAFFIC_ROUTE_ALT: number;\n    static readonly BEARING: number;\n    static readonly BEARING_EW: number;\n    static readonly BEARING_J: number;\n    static readonly BEARING_RDF: number;\n    static readonly ELECTRO: number;\n    static readonly ACOUSTIC: number;\n    static readonly ACOUSTIC_AMB: number;\n    static readonly TORPEDO: number;\n    static readonly OPTICAL: number;\n    static readonly BOUNDARY: number;\n    static readonly HOLD: number;\n    static readonly HOLD_GE: number;\n    static readonly RELEASE: number;\n    static readonly ABATIS: number;\n    static readonly LINE: number;\n    static readonly FOLLA: number;\n    static readonly AIRAOA: number;\n    static readonly AAAAA: number;\n    static readonly MAIN: number;\n    static readonly MAIN_STRAIGHT: number;\n    static readonly LC: number;\n    static readonly IL: number;\n    static readonly LDLC: number;\n    static readonly SPT: number;\n    static readonly SPT_STRAIGHT: number;\n    static readonly CATK: number;\n    static readonly CATKBYFIRE: number;\n    static readonly TRIPLE: number;\n    static readonly DOUBLEC: number;\n    static readonly SINGLEC: number;\n    static readonly HWFENCE: number;\n    static readonly LWFENCE: number;\n    static readonly UNSP: number;\n    static readonly DOUBLEA: number;\n    static readonly SFENCE: number;\n    static readonly DFENCE: number;\n    static readonly CHANNEL: number;\n    static readonly CHANNEL_FLARED: number;\n    static readonly CHANNEL_DASHED: number;\n    static readonly OVERHEAD_WIRE: number;\n    static readonly PAA_RECTANGULAR: number;\n    static readonly BATTLE: number;\n    static readonly STRONG: number;\n    static readonly ASSY: number;\n    static readonly FFA: number;\n    static readonly FFA_RECTANGULAR: number;\n    static readonly FFA_CIRCULAR: number;\n    static readonly FSA: number;\n    static readonly FSA_RECTANGULAR: number;\n    static readonly FSA_CIRCULAR: number;\n    static readonly NOTACK: number;\n    static readonly RFA: number;\n    static readonly RFA_RECTANGULAR: number;\n    static readonly RFA_CIRCULAR: number;\n    static readonly PAA_CIRCULAR: number;\n    static readonly ATI: number;\n    static readonly ATI_RECTANGULAR: number;\n    static readonly ATI_CIRCULAR: number;\n    static readonly CFFZ: number;\n    static readonly CFFZ_RECTANGULAR: number;\n    static readonly CFFZ_CIRCULAR: number;\n    static readonly SENSOR: number;\n    static readonly SENSOR_RECTANGULAR: number;\n    static readonly SENSOR_CIRCULAR: number;\n    static readonly CENSOR: number;\n    static readonly CENSOR_RECTANGULAR: number;\n    static readonly CENSOR_CIRCULAR: number;\n    static readonly DA: number;\n    static readonly DA_RECTANGULAR: number;\n    static readonly DA_CIRCULAR: number;\n    static readonly CFZ: number;\n    static readonly CFZ_RECTANGULAR: number;\n    static readonly CFZ_CIRCULAR: number;\n    static readonly ZOR: number;\n    static readonly ZOR_RECTANGULAR: number;\n    static readonly ZOR_CIRCULAR: number;\n    static readonly TBA: number;\n    static readonly TBA_RECTANGULAR: number;\n    static readonly TBA_CIRCULAR: number;\n    static readonly TVAR: number;\n    static readonly TVAR_RECTANGULAR: number;\n    static readonly TVAR_CIRCULAR: number;\n    static readonly TAI: number;\n    static readonly OBJ: number;\n    static readonly BSA: number;\n    static readonly DSA: number;\n    static readonly CSA: number;\n    static readonly EA: number;\n    static readonly NAI: number;\n    static readonly FARP: number;\n    static readonly FORT_REVD: number;\n    static readonly FORT: number;\n    static readonly ACA: number;\n    static readonly ACA_RECTANGULAR: number;\n    static readonly ACA_CIRCULAR: number;\n    static readonly AT: number;\n    static readonly SMOKE: number;\n    static readonly SERIES: number;\n    static readonly BOMB: number;\n    static readonly DHA_REVD: number;\n    static readonly DHA: number;\n    static readonly EPW: number;\n    static readonly RHA: number;\n    static readonly RSA: number;\n    static readonly GENERAL: number;\n    static readonly JTAA: number;\n    static readonly SAA: number;\n    static readonly SGAA: number;\n    static readonly PNO: number;\n    static readonly ASSAULT: number;\n    static readonly ATKPOS: number;\n    static readonly AO: number;\n    static readonly AIRHEAD: number;\n    static readonly PEN: number;\n    static readonly DEPICT: number;\n    static readonly MINED: number;\n    static readonly MINE_LINE: number;\n    static readonly FENCED: number;\n    static readonly UXO: number;\n    static readonly FORTL: number;\n    static readonly OBSAREA: number;\n    static readonly ZONE: number;\n    static readonly OBSFAREA: number;\n    static readonly NFA: number;\n    static readonly NFA_RECTANGULAR: number;\n    static readonly NFA_CIRCULAR: number;\n    static readonly CHEM: number;\n    static readonly BIO: number;\n    static readonly RAD: number;\n    static readonly NUC: number;\n    static readonly NFL: number;\n    static readonly MFP: number;\n    static readonly TGMF: number;\n    static readonly RECTANGULAR: number;\n    static readonly CIRCULAR: number;\n    static readonly BDZ: number;\n    static readonly KILLBOXBLUE: number;\n    static readonly KILLBOXBLUE_RECTANGULAR: number;\n    static readonly KILLBOXBLUE_CIRCULAR: number;\n    static readonly KILLBOXPURPLE: number;\n    static readonly KILLBOXPURPLE_RECTANGULAR: number;\n    static readonly KILLBOXPURPLE_CIRCULAR: number;\n    static readonly RANGE_FAN: number;\n    static readonly RANGE_FAN_FILL: number;\n    static readonly RANGE_FAN_SECTOR: number;\n    static readonly LAUNCH_AREA: number;\n    static readonly DEFENDED_AREA_CIRCULAR: number;\n    static readonly DEFENDED_AREA_RECTANGULAR: number;\n    static readonly SHIP_AOI_CIRCULAR: number;\n    static readonly SHIP_AOI_RECTANGULAR: number;\n    static readonly CF: number;\n    static readonly UCF: number;\n    static readonly CFG: number;\n    static readonly CFY: number;\n    static readonly WF: number;\n    static readonly WFG: number;\n    static readonly WFY: number;\n    static readonly UWF: number;\n    static readonly OCCLUDED: number;\n    static readonly UOF: number;\n    static readonly OFY: number;\n    static readonly SF: number;\n    static readonly USF: number;\n    static readonly SFG: number;\n    static readonly SFY: number;\n    static readonly TROUGH: number;\n    static readonly UPPER_TROUGH: number;\n    static readonly INSTABILITY: number;\n    static readonly SHEAR: number;\n    static readonly ITC: number;\n    static readonly CONVERGENCE: number;\n    static readonly ITD: number;\n    static readonly RIDGE: number;\n    static readonly SQUALL: number;\n    static readonly JET: number;\n    static readonly JET_GE: number;\n    static readonly STREAM: number;\n    static readonly STREAM_GE: number;\n    static readonly IFR: number;\n    static readonly MVFR: number;\n    static readonly TURBULENCE: number;\n    static readonly ICING: number;\n    static readonly NON_CONVECTIVE: number;\n    static readonly CONVECTIVE: number;\n    static readonly FROZEN: number;\n    static readonly THUNDERSTORMS: number;\n    static readonly FOG: number;\n    static readonly SAND: number;\n    static readonly FREEFORM: number;\n    static readonly ISOBAR: number;\n    static readonly ISOBAR_GE: number;\n    static readonly UPPER_AIR: number;\n    static readonly UPPER_AIR_GE: number;\n    static readonly ISOTHERM: number;\n    static readonly ISOTHERM_GE: number;\n    static readonly ISOTACH: number;\n    static readonly ISOTACH_GE: number;\n    static readonly ISODROSOTHERM: number;\n    static readonly ISODROSOTHERM_GE: number;\n    static readonly ISOPLETHS: number;\n    static readonly ISOPLETHS_GE: number;\n    static readonly OPERATOR_FREEFORM: number;\n    static readonly LVO: number;\n    static readonly UNDERCAST: number;\n    static readonly LRO: number;\n    static readonly ICE_EDGE: number;\n    static readonly ICE_EDGE_GE: number;\n    static readonly ESTIMATED_ICE_EDGE: number;\n    static readonly ESTIMATED_ICE_EDGE_GE: number;\n    static readonly ICE_EDGE_RADAR: number;\n    static readonly ICE_EDGE_RADAR_GE: number;\n    static readonly CRACKS: number;\n    static readonly CRACKS_GE: number;\n    static readonly CRACKS_SPECIFIC_LOCATION: number;\n    static readonly CRACKS_SPECIFIC_LOCATION_GE: number;\n    static readonly ICE_OPENINGS_LEAD: number;\n    static readonly ICE_OPENINGS_LEAD_GE: number;\n    static readonly ICE_OPENINGS_FROZEN: number;\n    static readonly ICE_OPENINGS_FROZEN_GE: number;\n    static readonly DEPTH_CURVE: number;\n    static readonly DEPTH_CURVE_GE: number;\n    static readonly DEPTH_CONTOUR: number;\n    static readonly DEPTH_CONTOUR_GE: number;\n    static readonly DEPTH_AREA: number;\n    static readonly COASTLINE: number;\n    static readonly COASTLINE_GE: number;\n    static readonly ISLAND: number;\n    static readonly BEACH: number;\n    static readonly WATER: number;\n    static readonly FORESHORE_LINE: number;\n    static readonly FORESHORE_AREA: number;\n    static readonly ANCHORAGE_LINE: number;\n    static readonly ANCHORAGE_AREA: number;\n    static readonly PIER: number;\n    static readonly PIER_GE: number;\n    static readonly FISH_TRAPS: number;\n    static readonly DRYDOCK: number;\n    static readonly LOADING_FACILITY_LINE: number;\n    static readonly LOADING_FACILITY_AREA: number;\n    static readonly RAMP_ABOVE_WATER: number;\n    static readonly RAMP_ABOVE_WATER_GE: number;\n    static readonly RAMP_BELOW_WATER: number;\n    static readonly RAMP_BELOW_WATER_GE: number;\n    static readonly JETTY_ABOVE_WATER: number;\n    static readonly JETTY_ABOVE_WATER_GE: number;\n    static readonly JETTY_BELOW_WATER: number;\n    static readonly JETTY_BELOW_WATER_GE: number;\n    static readonly SEAWALL: number;\n    static readonly SEAWALL_GE: number;\n    static readonly PERCHES: number;\n    static readonly LEADING_LINE: number;\n    static readonly UNDERWATER_HAZARD: number;\n    static readonly FOUL_GROUND: number;\n    static readonly KELP: number;\n    static readonly BREAKERS: number;\n    static readonly REEF: number;\n    static readonly DISCOLORED_WATER: number;\n    static readonly EBB_TIDE: number;\n    static readonly EBB_TIDE_GE: number;\n    static readonly FLOOD_TIDE: number;\n    static readonly FLOOD_TIDE_GE: number;\n    static readonly RESTRICTED_AREA: number;\n    static readonly PIPE: number;\n    static readonly TRAINING_AREA: number;\n    static readonly VDR_LEVEL_12: number;\n    static readonly VDR_LEVEL_23: number;\n    static readonly VDR_LEVEL_34: number;\n    static readonly VDR_LEVEL_45: number;\n    static readonly VDR_LEVEL_56: number;\n    static readonly VDR_LEVEL_67: number;\n    static readonly VDR_LEVEL_78: number;\n    static readonly VDR_LEVEL_89: number;\n    static readonly VDR_LEVEL_910: number;\n    static readonly BEACH_SLOPE_FLAT: number;\n    static readonly BEACH_SLOPE_GENTLE: number;\n    static readonly BEACH_SLOPE_MODERATE: number;\n    static readonly BEACH_SLOPE_STEEP: number;\n    static readonly SOLID_ROCK: number;\n    static readonly CLAY: number;\n    static readonly VERY_COARSE_SAND: number;\n    static readonly COARSE_SAND: number;\n    static readonly MEDIUM_SAND: number;\n    static readonly FINE_SAND: number;\n    static readonly VERY_FINE_SAND: number;\n    static readonly VERY_FINE_SILT: number;\n    static readonly FINE_SILT: number;\n    static readonly MEDIUM_SILT: number;\n    static readonly COARSE_SILT: number;\n    static readonly BOULDERS: number;\n    static readonly OYSTER_SHELLS: number;\n    static readonly PEBBLES: number;\n    static readonly SAND_AND_SHELLS: number;\n    static readonly BOTTOM_SEDIMENTS_LAND: number;\n    static readonly BOTTOM_SEDIMENTS_NO_DATA: number;\n    static readonly BOTTOM_ROUGHNESS_SMOOTH: number;\n    static readonly BOTTOM_ROUGHNESS_MODERATE: number;\n    static readonly BOTTOM_ROUGHNESS_ROUGH: number;\n    static readonly CLUTTER_LOW: number;\n    static readonly CLUTTER_MEDIUM: number;\n    static readonly CLUTTER_HIGH: number;\n    static readonly IMPACT_BURIAL_0: number;\n    static readonly IMPACT_BURIAL_10: number;\n    static readonly IMPACT_BURIAL_20: number;\n    static readonly IMPACT_BURIAL_75: number;\n    static readonly IMPACT_BURIAL_100: number;\n    static readonly BOTTOM_CATEGORY_A: number;\n    static readonly BOTTOM_CATEGORY_B: number;\n    static readonly BOTTOM_CATEGORY_C: number;\n    static readonly BOTTOM_TYPE_A1: number;\n    static readonly BOTTOM_TYPE_A2: number;\n    static readonly BOTTOM_TYPE_A3: number;\n    static readonly BOTTOM_TYPE_B1: number;\n    static readonly BOTTOM_TYPE_B2: number;\n    static readonly BOTTOM_TYPE_B3: number;\n    static readonly BOTTOM_TYPE_C1: number;\n    static readonly BOTTOM_TYPE_C2: number;\n    static readonly BOTTOM_TYPE_C3: number;\n    static readonly SWEPT_AREA: number;\n    static readonly OIL_RIG_FIELD: number;\n    static readonly SUBMERGED_CRIB: number;\n    static readonly CABLE: number;\n    static readonly MARITIME_LIMIT: number;\n    static readonly MARITIME_AREA: number;\n    static readonly CANAL: number;\n    static readonly OPERATOR_DEFINED: number;\n    static readonly RECTANGULAR_TARGET: number;\n    static readonly CUED_ACQUISITION: number;\n    static readonly RADAR_SEARCH: number;\n    static readonly GENERIC_LINE: number;\n    static readonly GENERIC_AREA: number;\n    static readonly HOL: number;\n    static readonly BHL: number;\n    static readonly TRAFFIC_ROUTE: number;\n    static readonly FPOL: number;\n    static readonly RPOL: number;\n    static readonly AREA_DEFENSE: number;\n    static readonly FRONTAL_ATTACK: number;\n    static readonly TURNING_MOVEMENT: number;\n    static readonly MOBILE_DEFENSE: number;\n    static readonly KILL_ZONE: number;\n    static readonly MOVEMENT_TO_CONTACT: number;\n    static readonly EXPLOIT: number;\n    static readonly DEMONSTRATE: number;\n    static readonly ENVELOPMENT: number;\n    static readonly INFILTRATION: number;\n    static readonly PURSUIT: number;\n    static readonly DISENGAGE: number;\n    static readonly EVACUATE: number;\n}\n//# sourceMappingURL=enums.d.ts.map","milsym/types/point.d.ts":"/**\n * Class to provide a Point object with a linestyle to facilitate drawing.\n *\n */\nexport declare class POINT2 {\n    x: number;\n    y: number;\n    style: number;\n    segment: number;\n    constructor();\n    constructor(pt: POINT2);\n    constructor(x: number, y: number);\n    constructor(x: number, y: number, style: number);\n    constructor(x: number, y: number, segment: number, style: number);\n    getX(): number;\n    getY(): number;\n}\n//# sourceMappingURL=point.d.ts.map","milsym/web/GeoPixelConversion.d.ts":"export declare class GeoPixelConversion {\n    static readonly INCHES_PER_METER: number;\n    static readonly METERS_PER_DEG: number;\n    static metersPerPixel(scale: number): number;\n    static lat2y(latitude: number, scale: number, latOrigin: number, metPerPix: number): number;\n    static y2lat(yPosition: number, scale: number, latOrigin: number, metPerPix: number): number;\n    static long2x(longitude: number, scale: number, longOrigin: number, latitude: number, metPerPix: number, normalize: boolean): number;\n    static x2long(xPosition: number, scale: number, longOrigin: number, latitude: number, metPerPix: number): number;\n    static Deg2Rad(deg: number): number;\n    static GetMetersPerDegAtLat(lat: number): number;\n}\n//# sourceMappingURL=GeoPixelConversion.d.ts.map","milsym/web/MultiPointHandler.d.ts":"import { Point2D } from \"../graphics/Point2D\";\nimport { Rectangle } from \"../graphics/Rectangle\";\nimport { Color } from \"../renderer/utilities/Color\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\nimport { MilStdSymbol } from \"../renderer/utilities/MilStdSymbol\";\nimport { ShapeInfo } from \"../renderer/utilities/ShapeInfo\";\nexport declare class MultiPointHandler {\n    private static readonly _maxPixelWidth;\n    private static readonly _minPixelWidth;\n    /**\n     * GE has the unusual distinction of being an application with coordinates\n     * outside its own extents. It appears to only be a problem when lines cross\n     * the IDL\n     *\n     * @param pts2d the client points\n     */\n    static NormalizeGECoordsToGEExtents(leftLongitude: number, rightLongitude: number, pts2d: Array<Point2D>): void;\n    /**\n     * GE recognizes coordinates in the range of -180 to +180\n     *\n     * @param pt2d\n     * @return\n     */\n    static NormalizeCoordToGECoord(pt2d: Point2D): Point2D;\n    /**\n     * We have to ensure the bounding rectangle at least includes the symbol or\n     * there are problems rendering, especially when the symbol crosses the IDL\n     *\n     * @param controlPoints the client symbol anchor points\n     * @param bbox the original bounding box\n     * @return the modified bounding box\n     */\n    private static getBoundingRectangle;\n    /**\n     * need to use the symbol to get the upper left control point in order to\n     * produce a valid PointConverter\n     *\n     * @param geoCoords\n     * @return\n     */\n    private static getControlPoint;\n    /**\n     * Assumes a reference in which the north pole is on top.\n     *\n     * @param geoCoords the geographic coordinates\n     * @return the upper left corner of the MBR containing the geographic\n     * coordinates\n     */\n    static getGeoUL(geoCoords: Array<Point2D>): Point2D;\n    static getBboxFromCoords(geoCoords: Array<Point2D>): string;\n    static crossesIDL(geoCoords: Array<Point2D>): boolean;\n    /**\n     * Checks if a symbol is one with decorated lines which puts a strain on\n     * google earth when rendering like FLOT. These complicated lines should be\n     * clipped when possible.\n     *\n     * @param symbolID\n     * @param useDashArray default true, some symbols don't need to be clipped if using dash array MilStdAttribute\n     * @param useFillPattern default true, some symbols don't need to be clipped if using fill pattern MilStdAttribute\n     * @return\n     */\n    static ShouldClipSymbol(symbolID: string, useDashArray?: boolean, useFillPattern?: boolean): boolean;\n    /**\n     * Assumes bbox is of form left,bottom,right,top and it is currently only\n     * using the width to calculate a reasonable scale. If the original scale is\n     * within the max and min range it returns the original scale.\n     *\n     * @param bbox\n     * @param origScale\n     * @return\n     */\n    static getReasonableScale(bbox: string, origScale: number): number;\n    /**\n     *\n     * @param id\n     * @param name\n     * @param description\n     * @param symbolCode\n     * @param controlPoints\n     * @param scale\n     * @param bbox\n     * @param symbolModifiers {@link Map}, keyed using constants from\n     * Modifiers. Pass in comma delimited String for modifiers with multiple\n     * values like AM, AN &amp; X\n     * @param symbolAttributes {@link Map}, keyed using constants from\n     * MilStdAttributes. pass in number[] for AM, AN and X; Strings for the\n     * rest.\n     * @param format\n     * @return\n     */\n    static RenderSymbol(id: string, name: string, description: string, symbolCode: string, controlPoints: string, scale: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>, format: number): string;\n    /**\n     *\n     * @param id\n     * @param name\n     * @param description\n     * @param symbolCode\n     * @param controlPoints\n     * @param scale\n     * @param bbox\n     * @param symbolModifiers\n     * @param symbolAttributes\n     * @return\n     */\n    static RenderSymbolAsMilStdSymbol(id: string, name: string, description: string, symbolCode: string, controlPoints: string, scale: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>): MilStdSymbol;\n    private static ConvertPolylinePixelsToCoords;\n    /**\n     * Multipoint Rendering on flat 2D maps\n     *\n     * @param id A unique ID for the symbol. only used in KML currently\n     * @param name\n     * @param description\n     * @param symbolCode\n     * @param controlPoints\n     * @param pixelWidth pixel dimensions of the viewable map area\n     * @param pixelHeight pixel dimensions of the viewable map area\n     * @param bbox The viewable area of the map. Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" example:\n     * \"-50.4,23.6,-42.2,24.2\"\n     * @param symbolModifiers Modifier with multiple values should be comma\n     * delimited\n     * @param symbolAttributes\n     * @param format An enumeration: 0 for KML, 1 for JSON.\n     * @return A JSON or KML string representation of the graphic.\n     */\n    static RenderSymbol2D(id: string, name: string, description: string, symbolCode: string, controlPoints: string, pixelWidth: number, pixelHeight: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>, format: number): string;\n    static getOverscanClipBounds(rect: Rectangle, ipc: IPointConversion): Rectangle;\n    /**\n     * For Mike Deutch testing\n     *\n     * @param id\n     * @param name\n     * @param description\n     * @param symbolCode\n     * @param controlPoints\n     * @param pixelWidth\n     * @param pixelHeight\n     * @param bbox\n     * @param symbolModifiers\n     * @param shapes\n     * @param modifiers\n     * @param format\n     * @return\n     * @deprecated\n     */\n    static RenderSymbol2DX(id: string, name: string, description: string, symbolCode: string, controlPoints: string, pixelWidth: number, pixelHeight: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>, shapes: Array<ShapeInfo>, modifiers: Array<ShapeInfo>, format: number): string;\n    private static MilStdSymbolToSymbolInfo;\n    /**\n     * Populates a symbol with the modifiers from a JSON string. This function\n     * will overwrite any previously populated modifier data.\n     *\n     *\n     *\n     * @param symbol An existing MilStdSymbol\n     * @return\n     */\n    static populateModifiers(saModifiers: Map<string, string>, saAttributes: Map<string, string>, symbol: MilStdSymbol): boolean;\n    private static KMLize;\n    /**\n     *\n     * @param shapes\n     * @param modifiers\n     * @param ipc\n     * @param geMap\n     * @param normalize\n     * @return\n     * @deprecated Use GeoJSONize()\n     */\n    private static JSONize;\n    static getIdealTextBackgroundColor(fgColor: Color): Color;\n    private static LabelToGeoJSONString;\n    private static ShapeToGeoJSONString;\n    private static ImageToGeoJSONString;\n    private static GeoJSONize;\n    /**\n     *\n     * @param shapes\n     * @param modifiers\n     * @param ipc\n     * @param normalize\n     * @deprecated\n     */\n    private static MakeWWReady;\n    static normalizePoints(shape: Array<Point2D>, ipc: IPointConversion): boolean;\n    /**\n     * @deprecated\n     */\n    private static IsOnePointSymbolCode;\n    private static ShapeToKMLString;\n    /**\n     *\n     * @param shapeInfo\n     * @param ipc\n     * @param normalize\n     * @return\n     * @deprecated\n     */\n    private static ShapeToWWReady;\n    private static LabelToWWReady;\n    /**\n     * Google earth centers text on point rather than drawing from that point.\n     * So we need to adjust the point to where the center of the text would be.\n     *\n     * @param modifier\n     */\n    private static AdjustModifierPointToCenter;\n    /**\n     *\n     * @param shapeInfo\n     * @param ipc\n     * @param geMap\n     * @param normalize\n     * @return\n     * @deprecated\n     */\n    private static ShapeToJSONString;\n    private static LabelToKMLString;\n    /**\n     *\n     * @param shapeInfo\n     * @param ipc\n     * @param normalize\n     * @return\n     * @deprecated\n     */\n    private static LabelToJSONString;\n    static canRenderMultiPoint(symbolID: string, modifiers: Map<string, string>, numPoints: number): string;\n    private static hasRequiredModifiers;\n    /**\n     *\n     * @param id\n     * @param name\n     * @param description\n     * @param basicShapeType\n     * @param controlPoints\n     * @param scale\n     * @param bbox\n     * @param symbolModifiers\n     * @param symbolAttributes\n     * @return\n     */\n    static RenderBasicShapeAsMilStdSymbol(id: string, name: string, description: string, basicShapeType: number, controlPoints: string, scale: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>): MilStdSymbol;\n    /**\n     *\n     * @param id\n     * @param name\n     * @param description\n     * @param basicShapeType\n     * @param controlPoints\n     * @param scale\n     * @param bbox\n     * @param symbolModifiers {@link Map}, keyed using constants from\n     * Modifiers. Pass in comma delimited String for modifiers with multiple\n     * values like AM, AN &amp; X\n     * @param symbolAttributes {@link Map}, keyed using constants from\n     * MilStdAttributes. pass in number[] for AM, AN and X; Strings for the\n     * rest.\n     * @param format\n     * @return\n     */\n    static RenderBasicShape(id: string, name: string, description: string, basicShapeType: number, controlPoints: string, scale: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>, format: number): string;\n}\n//# sourceMappingURL=MultiPointHandler.d.ts.map","milsym/web/MultiPointHandlerSVG.d.ts":"import { Rectangle } from \"../renderer/shapes/rectangle\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\nimport { ShapeInfo } from \"../renderer/utilities/ShapeInfo\";\nimport { SVGTextInfo } from \"../renderer/utilities/SVGTextInfo\";\nexport declare class MultiPointHandlerSVG {\n    /**\n     * Generates an SVG which can be draped on a map.\n     * Better with RenderSymbol2D\n     *\n     * @param {string} id\n     * @param {string} name\n     * @param {string} description\n     * @param {string} symbolID\n     * @param {ShapeInfo[]} shapes\n     * @param {ShapeInfo[]} modifiers\n     * @param {IPointConversion} ipc\n     * @param {boolean} normalize\n     * @param {string} textColor\n     * @param {string} textBackgroundColor\n     * @param {boolean} wasClipped\n     * @return {string}\n     */\n    static GeoSVGize(id: string, name: string, description: string, symbolID: string, shapes: ShapeInfo[], modifiers: ShapeInfo[], ipc: IPointConversion, normalize: boolean, textColor: string, textBackgroundColor: string, wasClipped: boolean, bbox?: Rectangle): string;\n    /**\n    * @param {SVGTextInfo[]} tiArray\n    * @param {string} color a hex string \"#000000\"\n    * @param {string} outlineColor a hex string \"#000000\"\n    */\n    static renderTextElement(tiArray: SVGTextInfo[], color: string, outlineColor: string): string[];\n    /**\n     * @param {string} symbolID\n     * @param {ShapeInfo} shapeInfo\n     * @param {IPointConversion} ipc\n     * @param {boolean} normalize\n     * @returns {object} { svg: string, bounds: Rectangle, fillPattern: string }\n     */\n    static ShapesToGeoSVG(symbolID: string, shapeInfo: ShapeInfo, ipc: IPointConversion, normalize: boolean): {\n        svg: string;\n        bounds: Rectangle;\n        fillPattern: string;\n    };\n}\n//# sourceMappingURL=MultiPointHandlerSVG.d.ts.map","milsym/web/PointConverter.d.ts":"import { Point } from \"../graphics/Point\";\nimport { Point2D } from \"../graphics/Point2D\";\nimport { IPointConversion } from \"../renderer/utilities/IPointConversion\";\n/**\n *\n *\n */\nexport declare class PointConverter implements IPointConversion {\n    private _controlLat;\n    private _controlLong;\n    private _scale;\n    private _metersPerPixel;\n    private _normalize;\n    set_normalize(value: boolean): void;\n    constructor(controlLong: number, controlLat: number, scale: number);\n    /**\n     * add constructor to handle when earth is flipped about it's X axis (South is on top)\n     * @param left\n     * @param right\n     * @param top\n     * @param bottom\n     * @param scale\n     */\n    constructor(left: number, top: number, right: number, bottom: number, scale: number);\n    PixelsToGeo(pixel: Point): Point;\n    PixelsToGeo(pixel: Point2D): Point2D;\n    GeoToPixels(coord: Point): Point;\n    GeoToPixels(coord: Point2D): Point2D;\n}\n//# sourceMappingURL=PointConverter.d.ts.map","milsym/web/Shape3DHandler.d.ts":"export declare class Shape3DHandler {\n    /**\n     * 3D version of {@link MultiPointHandler.RenderSymbol()}\n     */\n    static RenderMilStd3dSymbol(id: string, name: string, description: string, symbolCode: string, controlPoints: string, altitudeMode: string, scale: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>, format: number): string;\n    /**\n     *\n     * @param id\n     * @param name\n     * @param description\n     * @param basicShapeType\n     * @param controlPoints\n     * @param scale\n     * @param bbox\n     * @param symbolModifiers {@link Map}, keyed using constants from\n     * Modifiers. Pass in comma delimited String for modifiers with multiple\n     * values like AM, AN &amp; X\n     * @param symbolAttributes {@link Map}, keyed using constants from\n     * MilStdAttributes. pass in double[] for AM, AN and X; Strings for the\n     * rest.\n     * @param format\n     * @return\n     */\n    static RenderBasic3DShape(id: string, name: string, description: string, basicShapeType: number, controlPoints: string, altitudeMode: string, scale: number, bbox: string, symbolModifiers: Map<string, string>, symbolAttributes: Map<string, string>, format: number): string;\n    /**\n     * 3D Version of {@link MultiPointHandler.KMLize()}\n     */\n    private static KMLize;\n    /**\n     * 3D Version of {@link MultiPointHandler.ShapeToKMLString()}\n     */\n    private static ShapeToKMLString;\n    /**\n     * 3D Version of {@link MultiPointHandler.LabelToKMLString()}\n     */\n    private static LabelToKMLString;\n    /**\n     * 3D Version of {@link MultiPointHandler.GeoJSONize()}\n     */\n    private static GeoJSONize;\n    /**\n     * 3D Version of {@link MultiPointHandler.ShapeToGeoJSONString()}\n     */\n    private static ShapeToGeoJSONString;\n    /**\n     * 3D Version of {@link MultiPointHandler.ImageToGeoJSONString()}\n     */\n    private static ImageToGeoJSONString;\n    /**\n     * 3D Version of {@link MultiPointHandler.LabelToGeoJSONString()}\n     */\n    private static LabelToGeoJSONString;\n}\n//# sourceMappingURL=Shape3DHandler.d.ts.map","milsym/web/SymbolModifiers.d.ts":"/**\n *\n */\nexport declare class SymbolModifiers {\n    A_SYMBOL_ICON: string;\n    B_ECHELON: string;\n    C_QUANTITY: string;\n    H_ADDITIONAL_INFO_1: string;\n    H1_ADDITIONAL_INFO_2: string;\n    H2_ADDITIONAL_INFO_3: string;\n    N_HOSTILE: string;\n    Q_DIRECTION_OF_MOVEMENT: string;\n    S_OFFSET_INDICATOR: string;\n    T_UNIQUE_DESIGNATION_1: string;\n    T1_UNIQUE_DESIGNATION_2: string;\n    V_EQUIP_TYPE: string;\n    W_DTG_1: string;\n    W1_DTG_2: string;\n    X_ALTITUDE_DEPTH: Array<number>;\n    Y_LOCATION: string;\n    AM_DISTANCE: Array<number>;\n    AN_AZIMUTH: Array<number>;\n    FillColor: string;\n    LineColor: string;\n}\n//# sourceMappingURL=SymbolModifiers.d.ts.map","milsym/web/WebRenderer.d.ts":"import { LogLevel } from \"../renderer/utilities/LogLevel\";\nimport { MilStdSymbol } from \"../renderer/utilities/MilStdSymbol\";\n/**\n * Main class for rendering multi-point graphics such as Control Measures, Atmospheric, and Oceanographic.\n *\n */\nexport declare class WebRenderer {\n    static readonly OUTPUT_FORMAT_KML = 0;\n    /**\n     * @deprecated\n     */\n    static readonly OUTPUT_FORMAT_JSON = 1;\n    static readonly OUTPUT_FORMAT_GEOJSON = 2;\n    static readonly OUTPUT_FORMAT_GEOSVG = 3;\n    static readonly MIN_ALT_DEFAULT: number;\n    static readonly MAX_ALT_DEFAULT: number;\n    static readonly RADIUS1_DEFAULT: number;\n    static readonly RADIUS2_DEFAULT: number;\n    static readonly LEFT_AZIMUTH_DEFAULT: number;\n    static readonly RIGHT_AZIMUTH_DEFAULT: number;\n    static readonly ERR_ATTRIBUTES_NOT_FORMATTED: string;\n    static readonly DEFAULT_ATTRIBUTES: string;\n    private static _initSuccess;\n    private static init;\n    /**\\\n        * Set minimum level at which an item can be logged.\n        * In descending order:\n        * OFF = Integer.MAX_VALUE\n        * Severe = 1000\n        * Warning = 900\n        * Info = 800\n        * Config = 700\n        * Fine = 500\n        * Finer = 400\n        * Finest = 300\n        * All = Integer.MIN_VALUE\n        * Use like WebRenderer.setLoggingLevel(Level.INFO);\n        * or\n        * Use like WebRenderer.setLoggingLevel(800);\n        */\n    static setLoggingLevel(level: LogLevel | number): void;\n    /**\n     * Single Point Tactical Graphics are rendered from font files.\n     * The font size you specify here determines how big the symbols will\n     * be rendered.  This should be set once at startup.\n     * @param size\n     */\n    static setTacticalGraphicPointSize(size: number): void;\n    /**\n     * Units are rendered from font files.\n     * The font size you specify here determines how big the symbols will\n     * be rendered.  This should be set once at startup.\n     * @param size\n     */\n    static setUnitPointSize(size: number): void;\n    /**\n     * Modifier Text Color will by default match the line color.\n     * This will override all modifier text color.\n     * @param hexColor\n     */\n    /**\n     * Renders all multi-point symbols, creating KML, GeoJSON or SVG that can be used to draw\n     * it on a Google map.  Multipoint symbols cannot be draw the same\n     * at different scales. For instance, graphics with arrow heads will need to\n     * redraw arrowheads when you zoom in on it.  Similarly, graphics like a\n     * Forward Line of Troops drawn with half circles can improve performance if\n     * clipped when the parts of the graphic that aren't on the screen.  To help\n     * readjust graphics and increase performance, this function requires the\n     * scale and bounding box to help calculate the new locations.\n     * @param id A unique identifier used to identify the symbol by Google map.\n     * The id will be the folder name that contains the graphic.\n     * @param name a string used to display to the user as the name of the\n     * graphic being created.\n     * @param description a brief description about the graphic being made and\n     * what it represents.\n     * @param symbolCode A 20-30 digit symbolID corresponding to one of the\n     * graphics in the MIL-STD-2525D\n     * @param controlPoints The vertices of the graphics that make up the\n     * graphic.  Passed in the format of a string, using decimal degrees\n     * separating lat and lon by a comma, separating coordinates by a space.\n     * The following format shall be used \"x1,y1[,z1] [xn,yn[,zn]]...\"\n     * @param altitudeMode Indicates whether the symbol should interpret\n     * altitudes as above sea level or above ground level. Options are\n     * \"clampToGround\", \"relativeToGround\" (from surface of earth), \"absolute\"\n     * (sea level), \"relativeToSeaFloor\" (from the bottom of major bodies of\n     * water).\n     * @param scale A number corresponding to how many meters one meter of our\n     * map represents. A value \"50000\" would mean 1:50K which means for every\n     * meter of our map it represents 50000 meters of real world distance.\n     * <a href=\"https://github.com/missioncommand/mil-sym-java/wiki/2525D--Renderer-Overview#3316-scale-for-3d\">Scale Calculation Example</a>\n     * @param bbox The viewable area of the map.  Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" Not required\n     * but can speed up rendering in some cases.\n     * example: \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers {@link Map}, keyed using constants from Modifiers.\n     * Pass in comma delimited String for modifiers with multiple values like AM, AN &amp; X\n     * @param attributes {@link Map}, keyed using constants from MilStdAttributes.\n     * @param format {@link OUTPUT_FORMAT_KML}, {@link OUTPUT_FORMAT_GEOJSON} or {@link OUTPUT_FORMAT_GEOSVG}\n     * @return A KML, GeoJSON or SVG string representation of the graphic.\n     */\n    static RenderSymbol(id: string, name: string, description: string, symbolCode: string, controlPoints: string, altitudeMode: string, scale: number, bbox: string, modifiers: Map<string, string>, attributes: Map<string, string>, format: number): string;\n    /**\n     * Renders all multi-point symbols, creating KML, GeoJSON or SVG for the user to\n     * parse and render as they like.\n     * This function requires the bounding box to help calculate the new\n     * locations.\n     * @param id A unique identifier used to identify the symbol by Google map.\n     * The id will be the folder name that contains the graphic.\n     * @param name a string used to display to the user as the name of the\n     * graphic being created.\n     * @param description a brief description about the graphic being made and\n     * what it represents.\n     * @param symbolCode A 20-30 digit symbolID corresponding to one of the\n     * graphics in the MIL-STD-2525D\n     * @param controlPoints The vertices of the graphics that make up the\n     * graphic.  Passed in the format of a string, using decimal degrees\n     * separating lat and lon by a comma, separating coordinates by a space.\n     * The following format shall be used \"x1,y1 [xn,yn]...\"\n     * @param pixelWidth pixel dimensions of the viewable map area\n     * @param pixelHeight pixel dimensions of the viewable map area\n     * @param bbox The viewable area of the map.  Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\"\n     * example: \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers {@link Map}, keyed using constants from Modifiers.\n     * Pass in comma delimited String for modifiers with multiple values like AM, AN &amp; X\n     * @param attributes {@link Map}, keyed using constants from MilStdAttributes.\n     * @param format {@link OUTPUT_FORMAT_KML}, {@link OUTPUT_FORMAT_GEOJSON} or {@link OUTPUT_FORMAT_GEOSVG}\n     * @return A KML, GeoJSON or SVG string representation of the graphic.\n     */\n    static RenderSymbol2D(id: string, name: string, description: string, symbolCode: string, controlPoints: string, pixelWidth: number, pixelHeight: number, bbox: string, modifiers: Map<string, string>, attributes: Map<string, string>, format: number): string;\n    /**\n     * Renders all 3d multi-point symbols, creating KML or GeoJSON that can be\n     * used to draw it on a Google map.\n     * 3D version of {@link RenderSymbol()}\n     * @param id A unique identifier used to identify the symbol by Google map.\n     * The id will be the folder name that contains the graphic.\n     * @param name a string used to display to the user as the name of the\n     * graphic being created.\n     * @param description a brief description about the graphic being made and\n     * what it represents.\n     * @param symbolCode A 20-30 digit symbolID corresponding to one of the\n     * graphics in the MIL-STD-2525D\n     * @param controlPoints The 2D vertices of the graphics that make up the\n     * graphic.  Passed in the format of a string, using decimal degrees\n     * separating lat and lon by a comma, separating coordinates by a space.\n     * The following format shall be used \"x1,y1[,z1] [xn,yn[,zn]]...\"\n     * @param altitudeMode Indicates whether the symbol should interpret\n     * altitudes as above sea level or above ground level. Options are\n     * \"clampToGround\", \"relativeToGround\" (from surface of earth), \"absolute\"\n     * (sea level), \"relativeToSeaFloor\" (from the bottom of major bodies of\n     * water).\n     * @param scale A number corresponding to how many meters one meter of our\n     * map represents. A value \"50000\" would mean 1:50K which means for every\n     * meter of our map it represents 50000 meters of real world distance.\n     * <a href=\"https://github.com/missioncommand/mil-sym-java/wiki/2525D--Renderer-Overview#3316-scale-for-3d\">Scale Calculation Example</a>\n     * @param bbox The viewable area of the map.  Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" Not required\n     * but can speed up rendering in some cases.\n     * example: \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers {@link Map}, keyed using constants from Modifiers.\n     * Pass in comma delimited String for modifiers with multiple values like AM, AN &amp; X\n     * @param attributes {@link Map}, keyed using constants from MilStdAttributes.\n     * @param format {@link OUTPUT_FORMAT_KML} or {@link OUTPUT_FORMAT_GEOJSON}\n     * @return A KML or GeoJSON string representation of the graphic.\n     */\n    static RenderMilStd3dSymbol(id: string, name: string, description: string, symbolCode: string, controlPoints: string, altitudeMode: string, scale: number, bbox: string, modifiers: Map<string, string>, attributes: Map<string, string>, format: number): string;\n    /**\n     * Renders all MilStd 2525 multi-point symbols, creating MilStdSymbol that contains the\n     * information needed to draw the symbol on the map.\n     * DOES NOT support RADARC, CAKE, TRACK etc...\n     * ArrayList&lt;Point2D&gt; milStdSymbol.getSymbolShapes[index].getPolylines()\n     * and\n     * ShapeInfo = milStdSymbol.getModifierShapes[index].\n     *\n     *\n     * @param id\n     *            A unique identifier used to identify the symbol by Google map.\n     *            The id will be the folder name that contains the graphic.\n     * @param name\n     *            a string used to display to the user as the name of the\n     *            graphic being created.\n     * @param description\n     *            a brief description about the graphic being made and what it\n     *            represents.\n     * @param symbolCode\n     *            A 20-30 digit symbolID corresponding to one of the graphics\n     *            in the MIL-STD-2525D\n     * @param controlPoints\n     *            The vertices of the graphics that make up the graphic. Passed\n     *            in the format of a string, using decimal degrees separating\n     *            lat and lon by a comma, separating coordinates by a space. The\n     *            following format shall be used \"x1,y1[,z1] [xn,yn[,zn]]...\"\n     * @param altitudeMode\n     *            ignored\n     * @param scale\n     *            A number corresponding to how many meters one meter of our map\n     *            represents. A value \"50000\" would mean 1:50K which means for\n     *            every meter of our map it represents 50000 meters of real\n     *            world distance.\n     *            <a href=\"https://github.com/missioncommand/mil-sym-java/wiki/2525D--Renderer-Overview#3316-scale-for-3d\">Scale Calculation Example</a>\n     * @param bbox\n     *            The viewable area of the map. Passed in the format of a string\n     *            \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" Not required\n     *            but can speed up rendering in some cases. example:\n     *            \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers\n     *            Used like:\n     *            modifiers.set(Modifiers.T_UNIQUE_DESIGNATION_1, \"T\");\n     *            Or\n     *            modifiers.set(Modifiers.AM_DISTANCE, \"1000,2000,3000\");\n     * @param attributes\n     * \t\t\t  Used like:\n     *            attributes.set(MilStdAttributes.LineWidth, \"3\");\n     *            Or\n     *            attributes.set(MilStdAttributes.LineColor, \"#00FF00\");\n     * @return MilStdSymbol\n     */\n    static RenderMultiPointAsMilStdSymbol(id: string, name: string, description: string, symbolCode: string, controlPoints: string, altitudeMode: string, scale: number, bbox: string, modifiers: Map<string, string>, attributes: Map<string, string>): MilStdSymbol;\n    /**\n     * ArrayList&lt;Point2D&gt; milStdSymbol.getSymbolShapes[index].getPolylines()\n     * and\n     * ShapeInfo = milStdSymbol.getModifierShapes[index].\n     *\n     *\n     * @param id\n     *            A unique identifier used to identify the symbol by Google map.\n     *            The id will be the folder name that contains the graphic.\n     * @param name\n     *            a string used to display to the user as the name of the\n     *            graphic being created.\n     * @param description\n     *            a brief description about the graphic being made and what it\n     *            represents.\n     * @param basicShapeType\n     *             {@link BasicShapes}\n     * @param controlPoints\n     *            The vertices of the graphics that make up the graphic. Passed\n     *            in the format of a string, using decimal degrees separating\n     *            lat and lon by a comma, separating coordinates by a space. The\n     *            following format shall be used \"x1,y1[,z1] [xn,yn[,zn]]...\"\n     * @param altitudeMode\n     *            Ignored\n     * @param scale\n     *            A number corresponding to how many meters one meter of our map\n     *            represents. A value \"50000\" would mean 1:50K which means for\n     *            every meter of our map it represents 50000 meters of real\n     *            world distance.\n     *            <a href=\"https://github.com/missioncommand/mil-sym-java/wiki/2525D--Renderer-Overview#3316-scale-for-3d\">Scale Calculation Example</a>\n     * @param bbox\n     *            The viewable area of the map. Passed in the format of a string\n     *            \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" Not required\n     *            but can speed up rendering in some cases. example:\n     *            \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers\n     *            Used like:\n     *            modifiers.set(Modifiers.T_UNIQUE_DESIGNATION_1, \"T\");\n     *            Or\n     *            modifiers.set(Modifiers.AM_DISTANCE, \"1000,2000,3000\");\n     * @param attributes\n     * \t\t\t  Used like:\n     *            attributes.set(MilStdAttributes.LineWidth, \"3\");\n     *            Or\n     *            attributes.set(MilStdAttributes.LineColor, \"#00FF00\");\n     * @return MilStdSymbol\n     */\n    static RenderBasicShapeAsMilStdSymbol(id: string, name: string, description: string, basicShapeType: number, controlPoints: string, altitudeMode: string, scale: number, bbox: string, modifiers: Map<string, string>, attributes: Map<string, string>): MilStdSymbol;\n    /**\n     * Renders basic shapes, creating KML, GeoJSON or SVG that can be used to draw\n     * it on a Google map.\n     * @param id A unique identifier used to identify the symbol by Google map.\n     * The id will be the folder name that contains the graphic.\n     * @param name a string used to display to the user as the name of the\n     * graphic being created.\n     * @param description a brief description about the graphic being made and\n     * what it represents.\n     * @param basicShapeType {@link BasicShapes}\n     * @param controlPoints The vertices of the graphics that make up the\n     * graphic.  Passed in the format of a string, using decimal degrees\n     * separating lat and lon by a comma, separating coordinates by a space.\n     * The following format shall be used \"x1,y1[,z1] [xn,yn[,zn]]...\"\n     * @param altitudeMode ignored\n     * @param scale A number corresponding to how many meters one meter of our\n     * map represents. A value \"50000\" would mean 1:50K which means for every\n     * meter of our map it represents 50000 meters of real world distance.\n     * <a href=\"https://github.com/missioncommand/mil-sym-java/wiki/2525D--Renderer-Overview#3316-scale-for-3d\">Scale Calculation Example</a>\n     * @param bbox The viewable area of the map.  Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" Not required\n     * but can speed up rendering in some cases.\n     * example: \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers {@link Map}, keyed using constants from Modifiers.\n     * Pass in comma delimited String for modifiers with multiple values like AM, AN &amp; X\n     * @param attributes {@link Map}, keyed using constants from MilStdAttributes.\n     * @param format {@link OUTPUT_FORMAT_KML}, {@link OUTPUT_FORMAT_GEOJSON} or {@link OUTPUT_FORMAT_GEOSVG}\n     * @return A KML, GeoJSON or SVG string representation of the graphic.\n     */\n    static RenderBasicShape(id: string, name: string, description: string, basicShapeType: number, controlPoints: string, altitudeMode: string, scale: number, bbox: string, modifiers: Map<string, string>, attributes: Map<string, string>, format: number): string;\n    /**\n     * Renders basic 3D shapes, creating KML or GeoJSON that can be used to draw\n     * it on a Google map.\n     * @param id A unique identifier used to identify the symbol by Google map.\n     * The id will be the folder name that contains the graphic.\n     * @param name a string used to display to the user as the name of the\n     * graphic being created.\n     * @param description a brief description about the graphic being made and\n     * what it represents.\n     * @param basicShapeType {@link Basic3DShapes}\n     * @param controlPoints The vertices of the graphics that make up the\n     * graphic.  Passed in the format of a string, using decimal degrees\n     * separating lat and lon by a comma, separating coordinates by a space.\n     * The following format shall be used \"x1,y1[,z1] [xn,yn[,zn]]...\"\n     * @param altitudeMode Indicates whether the symbol should interpret\n     * altitudes as above sea level or above ground level. Options are\n     * \"clampToGround\", \"relativeToGround\" (from surface of earth), \"absolute\"\n     * (sea level), \"relativeToSeaFloor\" (from the bottom of major bodies of\n     * water).\n     * @param scale A number corresponding to how many meters one meter of our\n     * map represents. A value \"50000\" would mean 1:50K which means for every\n     * meter of our map it represents 50000 meters of real world distance.\n     * <a href=\"https://github.com/missioncommand/mil-sym-java/wiki/2525D--Renderer-Overview#3316-scale-for-3d\">Scale Calculation Example</a>\n     * @param bbox The viewable area of the map.  Passed in the format of a\n     * string \"lowerLeftX,lowerLeftY,upperRightX,upperRightY.\" Not required\n     * but can speed up rendering in some cases.\n     * example: \"-50.4,23.6,-42.2,24.2\"\n     * @param modifiers {@link Map}, keyed using constants from Modifiers.\n     * Pass in comma delimited String for modifiers with multiple values like AM, AN &amp; X\n     * @param attributes {@link Map}, keyed using constants from MilStdAttributes.\n     * @param format {@link OUTPUT_FORMAT_KML}, {@link OUTPUT_FORMAT_GEOJSON}\n     * @return A KML or GeoJSON string representation of the graphic.\n     */\n    static RenderBasic3DShape(id: string, name: string, description: string, basicShapeType: number, controlPoints: string, altitudeMode: string, scale: number, bbox: string, modifiers: Map<string, string>, attributes: Map<string, string>, format: number): string;\n    /**\n     * Given a symbol code meant for a single point symbol, returns the\n     * anchor point at which to display that image based off the image returned\n     * from the URL of the SinglePointServer.\n     *\n     * @param symbolID - the 20-30 digit symbolID of a single point MilStd2525\n     * symbol.\n     * @return A pixel coordinate of the format \"x,y\".\n     * Returns an empty string if an error occurs.\n     * @deprecated\n     */\n    getSinglePointAnchor(symbolID: string): string;\n    /**\n     * Given a symbol code meant for a single point symbol, returns the\n     * anchor point at which to display that image based off the image returned\n     * from the URL of the SinglePointServer.\n     *\n     * @param symbolID - the 20-30 digit symbolID of a single point MilStd2525\n     * symbol.\n     * @return A pixel coordinate of the format \"anchorX,anchorY,SymbolBoundsX,\n     * SymbolBoundsY,SymbolBoundsWidth,SymbolBoundsHeight,IconWidth,IconHeight\".\n     * Anchor, represents the center point of the core symbol within the image.\n     * The image should be centered on this point.\n     * Symbol bounds represents the bounding rectangle of the core symbol within\n     * the image.\n     * IconWidth/Height represents the height and width of the image in its\n     * entirety.\n     * Returns an empty string if an error occurs.\n     * @deprecated\n     */\n    static getSinglePointInfo(symbolID: string): string;\n    /**\n     * Returns true if we recommend clipping a particular symbol.\n     * Would return false for and Ambush but would return true for a Line of\n     * Contact due to the decoration on the line.\n     * @param symbolID\n     * @return\n     */\n    static ShouldClipMultipointSymbol(symbolID: string): string;\n    /**\n    * Given a symbol code meant for a single point symbol, returns the\n    * symbol as a byte array.\n    *\n    * @param symbolID - the 20-30 digit symbolID of a single point MilStd2525\n    * symbol.\n    * @return byte array.\n     * @deprecated\n    */\n    static getSinglePointByteArray(symbolID: string): null;\n    /**\n     * Converts a rectangle with two points and a width to a polygon that contains the four corners of the rectangle\n     *\n     * @param endpoints the two end points of the rectangle\n     * @param width  the width of the rectangle\n     * @returns the points of a polygon containing the four points of a rectangle\n     */\n    static getRectangleCorners(endpoints: string, width: number): string;\n}\n//# sourceMappingURL=WebRenderer.d.ts.map","milsym/web/utilities/Basic3DShapes.d.ts":"export declare class Basic3DShapes {\n    /**\n     * Anchor Points: This shape requires one anchor point\n     *\n     * Modifiers: radius ({@link Modifiers.AM_DISTANCE}), and min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     *\n     * @see DrawRules.CIRCULAR1\n     */\n    static readonly CYLINDER = 13000002;\n    /**\n     * Anchor Points: This shape requires two anchor points\n     *\n     * Modifiers: width ({@link Modifiers.AM_DISTANCE}), and min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     */\n    static readonly ORBIT = 16000001;\n    /**\n     * Anchor Points: This shape requires at least two anchor points\n     *\n     * Modifiers: width ({@link Modifiers.AM_DISTANCE}), and min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     */\n    static readonly ROUTE = 16000002;\n    /**\n     * Anchor Points: This shape requires at least three anchor points\n     *\n     * Modifiers: min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     */\n    static readonly POLYGON = 11000000;\n    /**\n     * Anchor Points: This shape requires one anchor point\n     *\n     * Modifiers: min radius and max radius ({@link Modifiers.AM_DISTANCE}), left and right azimuth ({@link Modifiers.AN_AZIMUTH}), and min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     */\n    static readonly RADARC = 16000003;\n    /**\n     * Anchor Points: This shape requires at least three anchor points\n     *\n     * Modifiers: radius ({@link Modifiers.AM_DISTANCE}), left and right azimuth ({@link Modifiers.AN_AZIMUTH}), and min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     */\n    static readonly POLYARC = 16000004;\n    /**\n     * A collection of radarcs\n     *\n     * Anchor Points: This shape requires one anchor point\n     *\n     * Modifiers (for each radarc): min radius and max radius ({@link Modifiers.AM_DISTANCE}), left and right azimuth ({@link Modifiers.AN_AZIMUTH}), and min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     */\n    static readonly CAKE = 16000005;\n    /**\n     * A collection of routes\n     *\n     * Anchor Points: This shape requires at least two anchor points\n     *\n     * Modifiers (for each segment): left and right width ({@link Modifiers.AM_DISTANCE}), and min and max altitude ({@link Modifiers.X_ALTITUDE_DEPTH}).\n     */\n    static readonly TRACK = 16000006;\n}\n//# sourceMappingURL=Basic3DShapes.d.ts.map","milsym/web/utilities/JavaRendererUtilities.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\n/**\n *\n */\nexport declare class JavaRendererUtilities {\n    static readonly HOSTILE_FILL_COLOR: string;\n    static readonly FRIENDLY_FILL_COLOR: string;\n    static readonly NEUTRAL_FILL_COLOR: string;\n    static readonly UNKNOWN_FILL_COLOR: string;\n    /**\n     * Converts ARGB string format to the Google used ABGR string format. Google\n     * reverses the blue and red positioning.\n     *\n     * @param rgbString A color string of the format AARRGGBB in hex value.\n     * @return the reverse of the input string in hex. The format should now be\n     * AABBGGRR\n     */\n    static ARGBtoABGR(rgbString: string): string;\n    /**\n     * Returns a symbolId with just the identifiable symbol Id pieces. All\n     * variable information is returned as '*'. For example, a boundary,\n     * \"GFGPGLB----KUSX\" returns \"G*G*GLB---****X\";\n     *\n     * @param symbolCode A 15 character symbol ID.\n     * @return The normalized SymbolCode.\n     * @deprecated\n     */\n    static normalizeSymbolCode(symbolCode: string): string;\n    static addAltModeToModifiersString(attributes: Map<string, string>, altMode: string): void;\n    /**\n     *\n     * @param SymbolInfo something like\n     * \"SymbolID?LineColor=0x000000&amp;FillColor=0xFFFFFF&amp;size=35\"\n     */\n    static createParameterMapFromURL(SymbolInfo: string): Map<string, string>;\n    static is3dSymbol(symbolCode: string): boolean;\n    /**\n     * Determines if a String represents a valid number\n     *\n     * @param text\n     * @return \"1.56\" == true, \"1ab\" == false\n     */\n    static isNumber(text: string): boolean;\n    /**\n     * Takes a throwable and puts it's stacktrace into a string.\n     *\n     * @param thrown\n     * @return\n     */\n    static getStackTrace(thrown: Error): string;\n    static getEndPointWithAngle(ptStart: Point2D, angle: number, distance: number): Point2D;\n    /**\n     *\n     * @param latitude1\n     * @param longitude1\n     * @param latitude2\n     * @param longitude2\n     * @param unitOfMeasure meters, kilometers, miles, feet, yards, nautical,\n     * nautical miles.\n     * @return\n     */\n    static measureDistance(latitude1: number, longitude1: number, latitude2: number, longitude2: number, unitOfMeasure: string): number;\n}\n//# sourceMappingURL=JavaRendererUtilities.d.ts.map","milsym/web/utilities/LineInfo.d.ts":"import { BasicStroke } from \"../../graphics/BasicStroke\";\nimport { Point2D } from \"../../graphics/Point2D\";\nimport { Color } from \"../../renderer/utilities/Color\";\n/**\n *\n *\n */\nexport declare class LineInfo {\n    private lineColor;\n    private fillColor;\n    private stroke;\n    private _Polylines;\n    constructor();\n    setLineColor(value: Color): void;\n    getLineColor(): Color;\n    setFillColor(value: Color): void;\n    getFillColor(): Color;\n    getStroke(): BasicStroke;\n    setStroke(s: BasicStroke): void;\n    getPolylines(): Array<Array<Point2D>>;\n    setPolylines(value: Array<Array<Point2D>>): void;\n}\n//# sourceMappingURL=LineInfo.d.ts.map","milsym/web/utilities/Point3D.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\nexport declare class Point3D extends Point2D {\n    z: number;\n    constructor(pt: Point2D, z: number);\n    constructor(x: number, y: number, z: number);\n    getZ(): number;\n}\n//# sourceMappingURL=Point3D.d.ts.map","milsym/web/utilities/ShapeInfo3D.d.ts":"import { Point3D } from \"./Point3D\";\nimport { ShapeInfo } from \"../../renderer/utilities/ShapeInfo\";\nexport declare class ShapeInfo3D extends ShapeInfo {\n    private _ModifierPosition3D;\n    private _Polylines3D;\n    setModifierPosition(value: Point3D): void;\n    getModifierPosition(): Point3D;\n    getPolylines(): Array<Array<Point3D>>;\n    setPolylines(value: Array<Array<Point3D>>): void;\n}\n//# sourceMappingURL=ShapeInfo3D.d.ts.map","milsym/web/utilities/SymbolInfo.d.ts":"import { LineInfo } from \"./LineInfo\";\nimport { TextInfo } from \"./TextInfo\";\n/**\n *\n *\n */\nexport declare class SymbolInfo {\n    private _LineInfo;\n    private _TextInfo;\n    constructor();\n    constructor(ti: Array<TextInfo>, li: Array<LineInfo>);\n    getTextInfoList(): Array<TextInfo>;\n    getLineInfoList(): Array<LineInfo>;\n}\n//# sourceMappingURL=SymbolInfo.d.ts.map","milsym/web/utilities/TextInfo.d.ts":"import { Point2D } from \"../../graphics/Point2D\";\n/**\n *\n *\n */\nexport declare class TextInfo {\n    private _ModifierString;\n    private _ModifierStringPosition;\n    private _ModifierStringAngle;\n    constructor();\n    setModifierString(value: string): void;\n    getModifierString(): string;\n    setModifierStringPosition(value: Point2D): void;\n    getModifierStringPosition(): Point2D;\n    setModifierStringAngle(value: number): void;\n    getModifierStringAngle(): number;\n}\n//# sourceMappingURL=TextInfo.d.ts.map"}}